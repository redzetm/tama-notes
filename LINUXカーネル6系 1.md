---
title: "LINUXカーネル6系の研究1　概要"
---

# LINUXカーネル6系の研究　１

この研究は、Linuxカーネルという巨大なオープンソースを題材にして、生涯研究するためのノートになります。
この様な低レイヤーの文献、書籍は少ないことから、一旦自分なりにAIを使いまとめた感じです。
基本自分用なのですが、カーネルの理解・解析には、生涯かかる可能性もあります。
そんな私なりの研究です。
Linuxカーネル 6.18 LTS をそのまま研究するために、私なりに書籍を読み替えて整理したものです。
古い Linux 2.6 系の文脈をそのまま追いかけるのではなく、いま自分が Linux 6.18 LTS を読むなら、
どういう観点で理解すると分かりやすいか、という形でまとめていきます。

UmuOS の研究にもつながるように、なるべく「カーネルとは何をしているのか」「どこが抽象化されていて、
どこがハード依存なのか」「プロセスやファイルや権限といった概念が、OS の中でどういう役目を持つのか」
を意識して書いていく感じです。

## １章　概要

Linux は、UNIX 系の設計思想を強く受け継いだカーネルです。
ただし、昔の UNIX をそのまま再現したものではなく、現在ではサーバー、デスクトップ、仮想マシン、組み込み機器、
コンテナ基盤まで支える、とても大きな汎用カーネルになっています。

古い本だと、Linux は UNIX の仲間であり、商用 UNIX と肩を並べる存在になった、という説明から始まることが多いです。
この流れ自体は今でも間違いではないですが、Linux 6.18 LTS を研究する立場では、単に UNIX に似ていると理解するだけでは
足りないです。
Linux 6 系では、マルチコア CPU、NUMA、仮想化、コンテナ、セキュリティ拡張、豊富なドライバ群、
巨大なファイルシステム層など、かなり現代的な前提の上に構造が成り立っているからです。

まず大切なのは、Linux という言葉が 2 通りに使われることを分けて考えることかなと思います。
1 つは、CPU やメモリやデバイスを管理するカーネルそのものとしての Linux です。
もう 1 つは、glibc、systemd、シェル、各種コマンド、パッケージ管理、GUI などを含めた実用システム全体としての 
Linux です。
今回このノートで研究したいのは、前者の Linux カーネルそのものです。

Linux カーネルを理解するというのは、単にコマンドの使い方を知ることではなく、
ユーザー空間のプログラムが、どのようにしてカーネルへ要求を出し、カーネルが CPU、メモリ、ファイル、
ネットワーク、デバイスをどう管理しているのかを知ることです。
UmuOS のような自作 OS の研究でも、この「ユーザー空間の要求を、カーネルがどう受けて、どう処理して返すか」
という見方は、かなり大事だと思います。

### １章の１　LinuxとほかのUNIX系カーネル

Linux は UNIX 系の考え方を強く受け継いでいます。
たとえば、プロセス、ファイルディスクリプタ、パイプ、シグナル、端末、権限、fork と exec、こういった考え方は
とても UNIX 的です。
そのため、Linux を学ぶことは、広く UNIX 系 OS の考え方を学ぶことにもつながると思います。

ただし、ここで注意したいのは、ユーザーから見える API やふるまいが似ているからといって、
内部実装まで他の UNIX 系 OS と同じとは限らないことです。
Linux は Linux として独自に発展してきたカーネルであり、VFS、ページキャッシュ、スケジューラ、
同期機構、デバイスモデル、ネットワークスタックなど、多くの部分で Linux 独自の構造を持っています。

#### １章の１の１　Linux とはなにか

Linux をひとことで言えば、ハードウェア資源を安全に管理しながら、ユーザープログラムへ実行環境を提供するカーネルです。
CPU をどう割り当てるか、メモリをどう見せるか、ファイルをどう扱うか、ネットワークをどう使わせるか、
そういった重要な仕事を Linux カーネルがまとめて受け持っています。

アプリケーションは、勝手に物理メモリを書き換えたり、任意のデバイスを直接叩いたりはできません。
必要な機能は、システムコールという入口を通してカーネルへ依頼することになります。
Linux カーネルは、その依頼が許されるかを確認し、必要なら実際のハードウェア操作を代行します。
この「直接触らせず、カーネルを通させる」という設計が、OS の安全性や安定性の土台になっています。

#### １章の１の２　POSIX と Linux

古い UNIX 系の本では、POSIX 準拠であることがよく語られます。
これは今でも重要ですが、POSIX を満たすことと、Linux カーネルの中身を理解することは別問題です。
POSIX は、主にユーザー空間から見た API や動作の約束ごとを定めたものです。
read()、write()、fork()、exec()、signal など、ユーザーが見える世界の共通部分をそろえるためのものと考えると
分かりやすいです。

しかし、POSIX はカーネル内部の設計までは決めません。
同じ read() が使えても、内部でどのように VFS を通り、どのようにページキャッシュやデバイスドライバに
到達しているかは、OS ごとにかなり違います。
なので、Linux を研究する際は、POSIX は入口として重要だけれども、中身は Linux 固有の実装として学ぶ必要がある、
という感じです。

#### １章の１の３　モノリシックカーネル

Linux は、分類としてはモノリシックカーネルです。
これは、カーネルに必要な主要機能が、同じカーネル空間の中で動いているという意味になります。
スケジューラ、メモリ管理、VFS、ドライバ、ネットワーク、割り込み処理などが、同じ特権空間で密接に連携して動くわけです。

モノリシックと聞くと、全部がベタベタにつながった単純な構造を想像しがちですが、現代の Linux 6.18 LTS はそんなに単純ではないです。
内部は非常に細かいサブシステムへ分かれていて、それぞれが明確な役割を持っています。
つまり、分類としてはモノリシックだけれど、実際の中身は巨大で、かなり整理された構造になっている感じです。

また Linux では、モジュールという仕組みによって、ドライバや一部機能を起動後に読み込んだり外したりできます。
これにより、完全に静的な一枚岩ではなく、必要に応じて機能を組み替えられる柔軟性も持っています。

#### １章の１の４　プロセスとスレッドの見方

古い UNIX の説明では、プロセスが基本で、その拡張としてスレッドが出てくるような書き方をされることがあります。
しかし Linux 6.18 LTS を研究するうえでは、内部では task という共通の実行単位で扱っている、と見た方が分かりやすいです。

ユーザーから見ると、独立したアドレス空間を持つものがプロセスに見えますし、アドレス空間やファイル表などを共有して並行実行されるものが
スレッドに見えます。
けれど Linux カーネルの内部では、どちらも task_struct を中心にした実行主体として扱われます。
何を共有し、何を共有しないかで、プロセスらしさ、スレッドらしさが決まる感じです。

また、Linux にはカーネルスレッドもたくさんあります。
書き戻し、メモリ回収、RCU、workqueue など、裏で OS を支える仕事をする task たちが存在しています。
なので「ユーザープロセスだけが動いている」と思うと、Linux の実態をかなり見誤るかなと思います。

#### １章の１の５　SMP と並行性

Linux 6.18 LTS は、最初から複数 CPU で同時に動くことを前提に設計されています。
今どきの Linux では、シングル CPU の感覚だけで考えると、かなり危ないです。
複数の CPU が、同じカーネルデータを同時に触る可能性があるため、ロック、atomic 操作、RCU、per-CPU データ、
メモリ順序などが非常に重要になります。

UmuOS の研究でも、最初から Linux 並みの SMP を実装する必要はないと思います。
ただ、「複数の実行文脈が同時に同じ状態を触ると壊れる」という発想自体は、かなり早い段階で持っておく方がよいです。

#### １章の１の６　ファイルシステムの見方

Linux のファイルシステムと言うと、ext4 や xfs や btrfs などの名前が先に出てきがちです。
でも研究の入口としては、まず VFS を理解する方がかなり大事です。
VFS は Virtual File System の略で、複数のファイルシステムに共通した操作の枠組みを作る層です。

ユーザー空間の open()、read()、write()、mount() といった操作は、いきなり ext4 の関数へ飛ぶわけではありません。
まず VFS が共通の入口として受け止め、そこから個別ファイルシステムやデバイス側の実装へ処理を渡していきます。
この「共通インタフェースを作り、その先で個別実装に分配する」という発想は、Linux のいろいろな部分で見られる重要な考え方だと思います。

### １章の２　ハードウェア依存

Linux は、多数の CPU アーキテクチャやハードウェアを支えるカーネルです。
そのため、ソースコードの中では、ハード依存の部分と、なるべく共通化された部分とを分ける構造が非常に重要になります。

古い本では、arch ディレクトリ配下に対応アーキテクチャの一覧がずらっと並んでいて、
何種類のプラットフォームに対応しているか、という見方が前面に出てきます。
もちろん当時としてはそれも大事だったと思います。
ただ、Linux 6.18 LTS を研究する今の立場では、対応数を覚えることよりも、
「どうやって共通コードと依存コードを分離しているか」を理解する方がずっと大事です。

#### １章の２の１　arch ディレクトリの役目

Linux のソースツリーには、arch、include、kernel、mm、fs、net、drivers など、役割ごとに分かれたディレクトリがあります。
このうち arch は、CPU アーキテクチャごとの依存部分を持つ場所です。

たとえば、x86_64 と arm64 では、命令セットも違えば、例外の入り方も違い、ページテーブル形式も違います。
syscall の入口、割り込みの入口、レジスタ保存の方法、特権レベルの扱いなども異なります。
そのため、CPU ごとの差を arch 配下に押し込み、共通コードはなるべく同じ論理で動くようにしているわけです。

ここで重要なのは、arch/ は単に「CPU ごとのコード置き場」ではなく、Linux 共通コードがその CPU 上で動けるようにする土台でもあることです。
例外処理、割り込み、syscall 入口、コンテキスト切り替え、ページングなど、OS の芯に近いところほど、ハード依存が強くなります。

#### １章の２の２　Linux 6.18 LTS でまず見るべきアーキテクチャ

現在の研究対象としては、まず x86_64 を主軸にするのがよいと思います。
サーバー、PC、仮想マシンで広く使われていて、資料も多く、観測もしやすいからです。
次に arm64 を見ると、現代の組み込みやモバイル、さらに一部サーバー世界とのつながりも見えてきます。
さらに将来性や学習用途を考えるなら riscv もかなり面白いです。

ただし、最初から全部を追う必要はないです。
UmuOS の研究としては、まず 1 つのアーキテクチャで、ブート、割り込み、ページング、syscall 入口を追えるようになれば十分価値があります。

### １章の３　Linux のバージョン

古い Linux 本を読むと、2.4、2.5、2.6 の頃のバージョン体系の説明が出てきます。
これは当時の開発運用を知る意味では面白いですが、そのまま Linux 6.18 LTS の理解へ持ち込むと混乱しやすいです。

現在の Linux では、昔のように「偶数は安定版、奇数は開発版」という見方ではありません。
Linus Torvalds のメインライン開発が継続し、その中から stable 系列や longterm 系列が保守される形になっています。

#### １章の３の１　LTS とはなにか

LTS は Long Term Support の略で、長期保守の対象になる系列です。
Linux 6.18 LTS というのは、単に 6.18.0 の状態で止まっているものではなく、6.18.y という形で、
バグ修正や安定化のための更新が継続的に積み重なっていく系列です。

研究で重要なのは、「6.18 系を読んでいる」というだけでなく、「6.18 のどの時点を読んでいるか」を意識することです。
同じ 6.18 系でも、stable 更新の積み重ねで細かな実装は変わり得るからです。

#### １章の３の２　版数よりも対象ソースを固定する

Linux の解説でズレやすいのは、別系列の情報が混ざることです。
ネットで調べると、5.x 系や 6.1 系、6.8 系の情報が普通に混ざります。
関数名が同じでも、内部構造が変わっていることは珍しくありません。

なので、このノートでは「Linux 6.18 LTS を主語にする」と決めておくのが大事かなと思います。
さらに実際にソースを読むときは、tag や release を明示しておくと、あとで見直すときにかなり助かります。

### １章の４　オペレーティングシステムの基本概念

この節は、Linux 固有というより、OS 全般を理解するための土台になるところです。
でも Linux 6.18 LTS を読むなら、教科書的な定義だけで終わらせず、Linux の実装にどうつながるかを意識した方がよいです。

#### １章の４の１　カーネルとはなにか

カーネルは、システム起動後にメモリへ読み込まれ、ハードウェアとユーザープログラムのあいだを仲介する中核ソフトウェアです。
CPU、メモリ、デバイス、ファイルシステム、ネットワーク、プロセス、権限管理など、OS の根幹を担当します。

ユーザーから見ると、アプリケーションは自分がメモリを持っていて、自分のファイルを開き、自分の処理を進めているように見えます。
しかし、その裏ではカーネルが資源を切り分け、順番を決め、保護し、抽象化して見せています。
OS を理解するというのは、この抽象化の裏側を知ることとも言えます。

#### １章の４の２　ユーザモードとカーネルモード

近代的な OS では、CPU に少なくとも 2 つの実行モードがあります。
普通のアプリケーションが動く非特権のモードと、カーネルが動く特権モードです。
Linux では、これをユーザモードとカーネルモードと呼びます。

ここはかなり重要です。
ユーザー空間のプログラムは、勝手に任意の I/O ポートや物理メモリへ触ったり、特権命令を実行したりはできません。
必要な処理は、システムコールや例外や割り込みを通して、カーネルに処理を依頼することになります。

また Linux の実装を読むときは、「いま process context なのか、interrupt context なのか、softirq なのか」といった、
どの実行文脈で走っているかがかなり重要です。
この違いを意識しないと、sleep してよい場所か、ロックを取ってよい場所か、どのメモリ確保 API を使うべきか、
といった判断を間違えやすいです。

#### １章の４の３　マルチユーザシステム

UNIX 系 OS の重要な特徴の 1 つが、マルチユーザであることです。
これは単に、複数の人がログインできるというだけの話ではありません。
同じシステム上で複数の利用者や複数の処理が、それぞれの権限と資源制限のもとで、互いに干渉しすぎないように動けることが重要です。

Linux 6.18 LTS では、昔ながらの UID や GID だけでなく、capabilities、namespaces、cgroups、LSM などの仕組みも加わっています。
つまり現代の Linux における「保護」は、単純なファイルパーミッションだけではなく、複数の層で構成されているわけです。

UmuOS では、最初からここまで複雑にする必要はないと思います。
でも、誰の権限で動いているのか、どこまで触れてよいのか、という発想自体はかなり早めに持っておくべきかなと思います。

#### １章の４の４　ユーザとグループ

Linux では、ユーザーは UID、グループは GID によって識別されます。
各ファイルやプロセスには所有者や所属グループがあり、アクセス可否の判断に使われます。
これは、ただログイン管理のためだけにあるのではなく、システム全体の保護機構の土台になっています。

また現代 Linux では、root か非 root かだけで物事を考えると粗すぎます。
capabilities によって、従来 root に集中していた権限を細かく分割できるからです。
たとえば、ネットワーク管理に関わる権限、モジュール操作に関わる権限、システム管理に近い権限などが分かれています。
このあたりは、古典的な UNIX の説明よりも、かなり現代的な Linux らしいところです。

#### １章の４の５　プロセス

プロセスは、OS のかなり中心的な概念です。
ただし Linux 6.18 LTS を読むなら、「実行中のプログラム」という一言だけでは足りないです。
プロセスは、CPU レジスタ状態、仮想アドレス空間、オープンファイル、資格情報、シグナル状態、スケジューリング状態など、
多くの状態のまとまりとして理解した方がよいです。

また Linux では、ユーザーから見たプロセスやスレッドを、内部では task という共通の実行主体として扱います。
fork 系は独立性の高い task を作り、clone 系は共有を伴う task を作る感じです。
つまり、プロセスとスレッドを全く別の種類のものとして覚えるより、
「共有のしかたが違う task」と見た方が、Linux の内部実装に近いと思います。

スケジューラは、どの task に CPU を割り当てるかを決めます。
単一 CPU なら同時に動ける処理は 1 つですが、現代の Linux は複数 CPU 前提なので、複数の task が本当に同時に走ります。
そのため、競合や同期が本質的な問題になります。

#### １章の４の６　process/kernel モデル

古い UNIX 本では、プロセス/カーネルモデルという説明が出てきます。
これは今でも大事です。
ユーザープログラムがシステムコールを発行すると、CPU はユーザモードからカーネルモードへ切り替わり、
カーネルがそのプロセスの要求を処理します。

ただし Linux 6.18 LTS では、それに加えて、割り込み文脈、softirq、workqueue、カーネルスレッドなど、
単純に「いまこのユーザープロセスの代わりにカーネルが動いている」とは言い切れない実行文脈も多いです。
このあたりまで視野に入ると、Linux が単なるプロセスの集合ではなく、さまざまな実行文脈が重なって成り立つ OS だと見えてきます。

#### １章の４の７　カーネルアーキテクチャ

Linux はモノリシックカーネルですが、同時に強くモジュール化されたカーネルでもあります。
マイクロカーネルと比べると、主要機能の多くが同じカーネル空間で動くため、サブシステム同士の連携が速く、直接的です。
その代わり、バグの影響範囲は大きく、同期や参照管理を誤ると深刻な問題になりやすいです。

モジュール機構は、Linux の実用性を支える重要な仕組みです。
ドライバや一部の機能を、必要なときだけロードできるので、柔軟性が高いです。
ただし、モジュールはユーザー空間の別プロセスではなく、ロード後は同じカーネル空間で動きます。
この点は、マイクロカーネル的な分離とはかなり違います。

UmuOS の研究では、最初からマイクロカーネルを目指す必要はないかなと思います。
まずは、単純なモノリシック構造で、起動、割り込み、メモリ、プロセス、ファイル I/O を成立させる方が現実的です。
そのうえで、Linux のように、機能ごとの責務分担と、あとから差し替えやすいインタフェース設計を少しずつ取り入れていくのがよさそうです。

### １章の５　UNIXファイルシステムの概要

UNIX 系 OS を理解するうえで、ファイルシステムはかなり中心的な存在です。
Linux でもこれは同じで、プロセス、デバイス、端末、パイプ、ソケット、権限管理など、多くの概念がファイルという考え方と
深く結びついています。
昔からよく言われる「すべてはファイルである」という言い方は、少し大ざっぱではあるものの、
Linux の設計思想をつかむ入口としてはやはり重要です。

ただし、ここで言う「すべて」は、何でも普通のテキストファイルと同じという意味ではありません。
Linux では、いろいろな対象を file descriptor を通して、似た操作体系で扱えるようにしている、という理解の方が正確です。
たとえば read()、write()、open()、close() で扱える対象がとても広いわけです。

#### １章の５の１　ファイル

UNIX における通常ファイルは、基本的にはバイト列です。
カーネルは、その中身が文章なのか、画像なのか、実行ファイルなのか、といった意味までは基本的に解釈しません。
ユーザー空間のプログラムやライブラリが、そのバイト列をどう読むかによって、意味が決まります。

たとえば、CSV、JPEG、ELF 実行ファイル、設定ファイル、ログファイルは、ユーザーから見ると全然違う種類のデータです。
しかし、Linux カーネルから見れば、まずはバイトの並びとして扱われます。
この考え方はかなり大事です。
高水準の構造はユーザー空間が担当し、低水準の保存と入出力の抽象化はカーネルが担当する、という役割分担が見えてくるからです。

また、ユーザーから見ると、ファイルはディレクトリツリーの中に配置されます。
ルートディレクトリ / を起点に、その下へディレクトリとファイルがぶら下がっていく形です。
このツリー構造によって、ユーザーはパス名で目的の対象を指定できます。

ファイル名そのものと、ファイルの実体は同じではない、という点も重要です。
普段は「名前のついたファイルがある」と感じますが、Linux の内部では、名前はディレクトリエントリ側の情報であり、
実体は別のデータ構造で管理されています。
この感覚は、あとで iノードやハードリンクを理解するときに効いてきます。

#### １章の５の２　ディレクトリとパス名

ディレクトリは、単なる見た目上のフォルダではなく、名前から実体へたどるための管理情報を持つ特殊なファイルです。
ユーザーはディレクトリの中にファイルが入っているように見ますが、内部的には「名前」と「対応する対象への参照」が管理されています。

パス名には、絶対パスと相対パスがあります。
絶対パスは / を起点にして対象を指定する方法で、相対パスは現在の作業ディレクトリを基準に指定する方法です。
Linux のプログラムを書いていると、この区別はかなりよく出てきます。
とくに openat() 系の syscall を理解していくと、単純なパス文字列の扱いよりも、
「どのディレクトリを基準に名前解決するのか」が重要だと見えてきます。

また、「.」は現在のディレクトリ、「..」は親ディレクトリを表します。
普段はシェルで当然のように使っていますが、これも名前解決の仕組みの上に成り立っています。

#### １章の５の３　ハードリンクとシンボリックリンク

Linux では、1つの実体に対して複数の名前を持たせることができます。
このうち、実体そのものに対して別名を増やすのがハードリンクです。
一方、別のパス名を中に記録した参照用の特別なファイルがシンボリックリンクです。

ここは混乱しやすいです。
ハードリンクは、「別の名前だが、同じ実体を指している」という感じです。
そのため、片方の名前を消しても、もう片方が残っていれば実体は消えません。
これに対してシンボリックリンクは、「あっちを見に行け」と書いてある案内札のようなものです。
参照先が消えると、リンクだけ残って壊れた状態になることもあります。

ハードリンクには制限もあります。
一般的にはディレクトリへのハードリンクは作れませんし、別ファイルシステムをまたぐこともできません。
これは、ディレクトリツリーの整合性や、ファイルシステムごとの管理境界に関わる話です。

シンボリックリンクは、この制限をかなり緩くできます。
別ファイルシステム上の対象も指せますし、ディレクトリを指すこともできます。
ただし、内部的には「パス名をたどり直す」動きになるため、名前解決の途中でシンボリックリンク処理が入ることを意識する必要があります。

#### １章の５の４　ファイルの種類

Linux のファイルには、通常ファイル以外にもさまざまな種類があります。
代表的には、次のようなものがあります。

①　通常ファイル
テキスト、バイナリ、実行ファイルなどの一般的なファイルです。

②　ディレクトリ
名前解決のための管理情報を持つ特殊な対象です。

③　シンボリックリンク
別のパス名を参照するための特別なファイルです。

④　ブロックデバイスファイル
ディスクのように、ブロック単位でアクセスするデバイスを表します。

⑤　キャラクタデバイスファイル
シリアル端末や一部のデバイスのように、文字やバイト列を順次扱うデバイスを表します。

⑥　FIFO、パイプ
プロセス間通信に使われるデータの流れ口です。

⑦　ソケット
ローカルまたはネットワーク越しの通信に使われる端点です。

ここで重要なのは、「見た目は全部パス名を持つが、内部の扱いはかなり違う」ことです。
しかし Linux は、それらをある程度統一的なインタフェースで扱えるようにしています。
この統一感が、UNIX 系の強さの 1 つだと思います。

#### １章の５の５　ファイルディスクリプタと iノード

ここはかなり重要です。
Linux では、ファイルの内容そのものと、そのファイルを管理するための情報とを分けて考えます。
ユーザーは「名前のついたファイル」を見ていますが、カーネルは内部で、実体を表す inode と、
開いているやり取りを表す open file のような別の管理単位を使っています。

iノードには、ファイルの種類、所有者、権限、サイズ、時刻、リンク数などの情報が入ります。
逆に言うと、ファイル名そのものは iノードの中には直接入っていません。
名前はディレクトリ側で管理され、ディレクトリエントリから iノードへたどる構造になっています。

また、ユーザー空間のプログラムが open() を呼ぶと、返ってくるのは file descriptor、つまり fd です。
この fd は、あくまで「そのプロセスが開いているやり取りの窓口番号」です。
ファイルの実体そのものではありません。

ここを区別すると、dup() で同じ open file を別の fd から見られること、
fork() 後に親子で fd を共有しうること、unlink() しても開いているあいだ実体が残りうること、
こういった UNIX らしい挙動がかなり理解しやすくなります。

#### １章の５の６　アクセス権とファイルモード

Linux のファイルには、所有者、グループ、その他、という 3 種類の利用者区分があり、
それぞれに対して read、write、execute の権限が設定されます。
普段 ls -l で見ている rwxr-xr-x のような表示がそれです。

ここも最初は単純に見えますが、実際にはかなり重要です。
ファイルを読めるか、書けるか、実行できるかは、単にユーザーの気分ではなく、カーネルが毎回権限を見て判断しています。
また、ファイル作成時には umask も関わりますし、ディレクトリに対する write/execute 権限の意味も、通常ファイルとは少し違います。

さらに suid、sgid、sticky といった特別なモードもあります。
古い説明では sticky が実行ファイルのメモリ常駐に結びついて語られることがありますが、現代 Linux で重要なのは主にディレクトリに対する sticky bit の方です。
たとえば /tmp のように、誰でも書けるが他人のファイルを勝手に消せないようにする場面で使われます。

#### １章の５の７　ファイルを扱うシステムコール

Linux でファイルを扱うとき、ユーザー空間のプログラムは直接ディスクを触るわけではありません。
open()、read()、write()、lseek()、close()、rename()、unlink() などの syscall を通して、カーネルへ処理を依頼します。

つまり、ファイル操作とは「ストレージを直接読むこと」ではなく、「VFS と個別ファイルシステムを通してカーネルに処理してもらうこと」です。
この見方は、Linux カーネルを読むうえでかなり大事です。

##### １章の５の７の１　ファイルのオープン

プロセスがファイルを扱うには、まず open() などでオープンする必要があります。
open() は、対象のパス名、どういうモードで開くか、必要なら新規作成時の権限を指定して呼びます。
そして返ってくるのが fd です。

この fd は、プロセスごとの表に登録された整数番号です。
同じファイルを別々に open() すれば、同じ実体を指していても別の open file 状態を持つことがあります。
一方で dup() や fork() によって、同じ open file 状態を複数の fd から共有することもあります。
この違いは、ファイルオフセット共有の挙動などに効いてきます。

##### １章の５の７の２　オープンしたファイルへのアクセス

open() のあと、read() や write() によって実際の I/O を行います。
通常ファイルでは、現在のファイルオフセットから読み書きが行われ、成功したぶんだけ位置が進みます。
位置を明示的に変えたいときは lseek() を使います。

この「現在位置」が open file 側の状態として管理されることがポイントです。
同じ実体でも、別々に open すれば別々の位置を持てますし、同じ open file を共有していれば位置も共有されます。
シェルのリダイレクトやパイプを深く理解していくと、この区別がかなり大事になってきます。

##### １章の５の７の３　ファイルのクローズ

不要になった fd は close() で閉じます。
close() は、単に「ファイルを終わりにする」くらいの軽い話ではなく、その fd からの参照を外す操作です。
同じ open file を別の fd でも参照しているなら、最後の参照が消えるまで内部状態は残ることがあります。

また、プロセス終了時には、まだ開いている fd はカーネルによって閉じられます。
ただし「終了すれば勝手に片付く」と考えすぎると、長時間動くプロセスやデーモンでは fd leak の原因になります。

##### １章の５の７の４　ファイル名変更と削除

rename() や unlink() は、ファイルの中身を直接いじるというより、名前と実体の対応を変える操作です。
ここが UNIX ファイルシステムの面白いところです。

unlink() は、「ファイルの中身を即座に消す」ではなく、「その名前から実体へのリンクを 1 つ減らす」と考える方が正確です。
リンク数が 0 になり、さらに誰からも open されていなくなったときに、はじめて実体が本当に解放されます。

この仕組みがあるので、実行中のプロセスが open したログファイルを unlink() しても、
そのプロセスが閉じるまでは領域が残る、というような現象が起きます。
ここは Linux の運用でもよく出てくる重要な挙動です。

### １章の６　UNIXカーネルの概要

ここまでで、Linux がどういう種類の OS で、ファイルシステムやプロセスや権限といった概念がどう重要なのかを見てきました。
この 1.6 では、さらに一段進めて、UNIX カーネルそのものがどういう部品でできていて、どういうふうに動いているのか、
その全体像を整理しておきたいです。

Linux 6.18 LTS を読むうえでは、この節はかなり大事かなと思います。
なぜかというと、後の章で出てくるプロセス管理、メモリ管理、割り込み、ドライバ、同期などが、
ここで説明するカーネルの実行モデルの上に全部乗っているからです。

#### １章の６の１　プロセス/カーネルモデル

CPU は、少なくともユーザモードとカーネルモードという 2 つの状態を使い分けます。
ユーザモードでは、通常のアプリケーションが動作し、カーネル内部のデータや特権的なハードウェア機能には直接触れられません。
一方、カーネルモードでは、Linux カーネルが必要な資源管理やハードウェア操作を実行できます。

つまり、アプリケーションは普段ずっとユーザモードで動いていて、必要なときだけシステムコール、例外、割り込みなどをきっかけに
カーネルモードへ入ります。
Linux は、この切り替えを前提にして作られている OS です。

ここで重要なのは、カーネル自身は 1 個の「普通のプロセス」ではないことです。
カーネルはプロセスを管理する側であり、各プロセスの要求に応じて、そのプロセスの文脈の中で動いたり、
あるいは割り込み文脈やカーネルスレッドとして動いたりします。

古い説明では、カーネルスレッドを「特権プロセス」のように書くことがありますが、Linux 6.18 LTS の感覚では、
ユーザー空間を持たない task と見た方が分かりやすいです。
端末と対話する通常プロセスとは違い、カーネル内部の仕事だけを受け持つ実行主体になります。
書き戻し、メモリ回収、workqueue、RCU 関連など、多くの常駐処理がここに含まれます。

#### １章の６の２　カーネルに入るきっかけ

Linux カーネルは、ただシステムコールを待っているだけではありません。
カーネルコードが動き出すきっかけは、だいたい次の 4 種類に整理できます。

①　システムコール
ユーザープロセスが read()、write()、open() などを呼び、カーネルへ処理を依頼する場合です。

②　例外
不正命令、ページフォルト、ゼロ除算など、CPU が異常や特殊条件を検出した場合です。

③　割り込み
デバイスが「I/O が終わった」「新しいデータが来た」などを CPU に通知する場合です。

④　カーネルスレッドの実行
Linux 自身が内部処理のために起こしている task が動く場合です。

この 4 つを分けて考えることはかなり重要です。
同じカーネルコードでも、どの入口から入ったかによって、sleep してよいか、どのロックを取れるか、
どのメモリ確保方法が使えるか、といった条件が変わるからです。

#### １章の６の３　プロセスの実装

Linux は、各プロセスやスレッドを task_struct を中心とするデータで管理しています。
古い本では process descriptor という言い方が出てきますが、Linux 6.18 LTS で見に行く対象としては task_struct を思い浮かべるとよいです。

プロセスを一時停止するとき、カーネルは CPU レジスタ状態やスケジューリング状態など、再開に必要な情報を保存します。
代表的には、プログラムカウンタ、スタックポインタ、汎用レジスタ、CPU 状態、メモリ管理に関わる情報などです。

そのため、プロセスは止められても、あとで再開すると「途中から続きが動く」ように見えます。
これは、カーネルが実行状態をちゃんと保存しているからです。

また、CPU 上で動いていないプロセスは、多くの場合なにかを待っています。
I/O 完了待ち、ロック待ち、タイマ待ち、子プロセス待ちなどです。
Linux では、こうした待ち状態を wait queue や各種スケジューリング状態と組み合わせて管理します。

#### １章の６の４　再入可能なカーネル

Linux カーネルは再入可能です。
これは、カーネルコードのある経路を実行している最中でも、別のイベントによって別のカーネル実行経路が動き得る、という意味です。

たとえば、あるプロセスがシステムコールの途中で I/O 待ちに入ることがあります。
その間に別のプロセスが実行され、別のカーネルコードが走ります。
さらに、その最中に割り込みが入れば、割り込み処理が割り込んできます。
つまり Linux カーネルは、単純に 1 本の大きな手続きが順番に動いているわけではありません。

ここで出てくるのが、カーネル実行パスという見方です。
システムコール、例外、割り込みのたびに、カーネルの中のある命令列が動きます。
しかもそれらは、状況によって中断、再開、入れ子実行されます。
Linux を理解するうえでは、「どのカーネル実行パスが、どの文脈で走っているのか」を意識するのが大事です。

#### １章の６の５　プロセスアドレス空間

各プロセスは、固有の仮想アドレス空間の中で動きます。
ユーザモードでは、そのプロセスに見えているコード領域、データ領域、ヒープ、スタックなどを使います。
一方、カーネルモードへ入ると、カーネル側のコードやデータにもアクセスできるようになります。

ここで勘違いしやすいのは、「各プロセスは完全に独立したメモリ世界に閉じている」という見方です。
確かに仮想アドレス空間はプロセスごとに分かれていますが、共有ライブラリ、共有メモリ、mmap() によるマッピング、
コピーオンライトなどにより、一部は共有されたり、遅延複製されたりします。

Linux 6.18 LTS を研究するなら、あとで必ず mmap() が重要になってきます。
ファイル I/O、共有ライブラリ、匿名メモリ、デバイスマッピングなど、多くの話題がここへつながるからです。

#### １章の６の６　同期とクリティカル区間

Linux カーネルが再入可能で、しかも SMP 前提で動く以上、同期は避けて通れません。
複数の実行文脈が同じデータを同時に触ると、競合状態になります。
この競合が起きると、たまたま動くこともあれば、たまたま壊れることもあり、かなり危険です。

たとえば、ある共有カウンタを 2 つの実行経路が同時に更新したとします。
読み出し、加算、書き戻しがバラバラに入り交じると、本来の値にならないことがあります。
これが race condition の典型例です。

このため、クリティカル区間では、ほかの実行経路が同じデータへ不正に入らないようにする必要があります。
Linux では、状況に応じてさまざまな同期手法を使い分けます。

##### １章の６の６の１　カーネル内プリエンプトの制御

古い UNIX では、カーネル全体がプリエンプト不可に近い発想で作られていた時代もありました。
しかし Linux 6.18 LTS は、かなりプリエンプト可能な方向へ進んでいます。
ただし、クリティカル区間の一部では、必要に応じてプリエンプションを抑えて安全性を守る場面があります。

単一 CPU であれば、プリエンプト禁止だけで守れる部分もありますが、SMP 環境ではそれだけでは不十分です。
別 CPU が同時に同じデータを見る可能性があるからです。

##### １章の６の６の２　割り込みの禁止

割り込み禁止は、単一 CPU 上では比較的強力な保護手段です。
ただし、長く禁止するとデバイス応答性やシステム全体の遅延が悪化します。
しかも SMP では、自 CPU の割り込みを止めても、他 CPU の実行までは止まりません。

なので現代 Linux では、「とりあえず全部止める」よりも、必要最小限の範囲で、より細かな同期原語を使う方が基本になります。

##### １章の６の６の３　セマフォ、mutex、スピンロック

古い本ではセマフォが中心に説明されることがあります。
もちろん今でもセマフォは存在しますが、Linux 6.18 LTS を読むときは、mutex や spinlock の方が日常的によく目に入ると思います。

sleep して待てる文脈では mutex や semaphore 系が使えます。
一方、割り込み文脈や very short な保護では spinlock が使われます。
spinlock は、待つ側がその場でくるくる回り続けるので、短時間の保護向きです。
逆に長い待ちには向きません。

また Linux では、rwsem、seqlock、completion、RCU など、用途別の同期機構も多いです。
なので「Linux の同期 = セマフォ」と思わない方がよいかなと思います。

##### １章の６の６の４　デッドロックの回避

ロックが複数ある世界では、デッドロックが起きます。
たとえば A を取ってから B を取りに行く経路と、B を取ってから A を取りに行く経路があると、
お互い待ち合って止まることがあります。

Linux では、ロック取得順序のルールをそろえたり、設計段階で階層を決めたり、lockdep のような仕組みで問題を発見したりします。
これは理論だけの話ではなく、巨大カーネルを保守するためのかなり実践的な知恵です。

#### １章の６の７　シグナルとプロセス間通信

シグナルは、プロセスへ事象を通知する仕組みです。
たとえば CTRL+C による SIGINT、異常なメモリアクセスによる SIGSEGV などが典型です。

シグナルは軽量な通知手段として重要ですが、データを大量にやり取りするための仕組みではありません。
「何か起きた」という非同期通知として使うと理解するとよいです。

一方、プロセス間通信には、System V IPC や POSIX IPC、パイプ、FIFO、ソケット、共有メモリなど、さまざまな仕組みがあります。
Linux 6.18 LTS を実際に使う立場では、昔ながらの System V IPC も残っていますが、
実務ではパイプ、UNIX ドメインソケット、mmap 系共有メモリの方がよく見える場面も多いです。

共有メモリはとても高速ですが、整合性の管理をアプリケーション側で考える必要があります。
つまり「速いが難しい」手段です。

#### １章の６の８　プロセス管理

UNIX 系では、プロセスの生成と、実行するプログラムの差し替えを分けて考えます。
fork() で新しいプロセスを作り、exec() 系でそのプロセスに新しいプログラムを読み込ませます。
この分離は、UNIX 的でとても重要な考え方です。

Linux 6.18 LTS では、内部的には copy_process() や execve() 系の経路がこれを支えています。
fork() がそのまま全メモリ複製を意味するわけではなく、コピーオンライトによってかなり効率化されています。
そのため、見た目ほど重くないのが現代 Linux の特徴です。

##### １章の６の８の１　ゾンビプロセス

子プロセスが終了しても、親が終了状態を回収していないあいだは、ゾンビとして情報が残ります。
これは無駄ではなく、親が終了コードや資源使用情報を受け取るために必要です。

ただし、放置するとゾンビがたまります。
親が先に死んだ場合は、init 系プロセスが引き取って wait 系 syscall で回収します。
現代 Linux では PID 1 が systemd であることが多いですが、役割としては「孤児の引き取り手」という点が重要です。

##### １章の６の８の２　プロセスグループとセッション

シェルを理解するとき、プロセスグループとセッションはかなり重要です。
パイプラインでつながった複数プロセスを、シェルは 1 つの job のように扱います。
そのためにプロセスグループというまとまりを使います。

さらに、ログインセッションという単位もあります。
同じ端末を共有するジョブたちは、前景、つまりフォアグラウンドか、後景、つまりバックグラウンドかで扱いが変わります。
端末制御シグナルが関わるため、シェルや TTY を深く理解したいなら避けて通れません。

#### １章の６の９　メモリ管理

Linux カーネルの中でも、メモリ管理は特に巨大で複雑です。
仮想記憶、ページテーブル、ページフォルト、ページキャッシュ、匿名メモリ、mmap、スラブ、OOM、NUMA など、
かなり多くの概念がここへ集まります。

##### １章の６の９の１　仮想記憶

各プロセスは、物理メモリそのものではなく、仮想アドレス空間を見ています。
CPU と MMU が、ページテーブルを使って仮想アドレスを物理アドレスへ変換します。

この仕組みによって、各プロセスは自分専用の広いメモリ世界を持っているように見えます。
しかも、物理メモリが連続していなくても、仮想的には連続して見せられます。
これが現代 OS の基本的な強みです。

##### １章の６の９の２　RAM の使い道

RAM は、単にプロセスのヒープやスタックのためだけにあるわけではありません。
カーネル自身のデータ、ページキャッシュ、バッファ、スラブキャッシュ、mmap された領域など、
さまざまな目的で使われます。

Linux は、この限られた RAM をどう配分するかで性能が大きく変わります。
空きメモリが減ったとき、どのページを回収するか、どのキャッシュを残すか、どこまで再利用するか、
このへんはかなり経験則の強い世界です。

##### １章の６の９の３　カーネルメモリアロケータ

Linux では、物理ページの大きな管理だけでなく、カーネル内部オブジェクトの細かな確保も必要です。
task_struct や inode、dentry、各種バッファなど、多くの小さなオブジェクトが頻繁に生成、破棄されます。

そのため Linux は、ページアロケータの上に slab 系アロケータを重ねて使います。
古い本でもスラブアロケータの話が出てきますが、Linux 6.18 LTS を見るなら、実際には SLUB が主流実装として出てくることが多いです。

##### １章の６の９の４　仮想アドレス空間の取り扱い

プロセスの仮想アドレス空間には、コード、初期化済みデータ、未初期化データ、ヒープ、スタック、共有ライブラリ、
メモリマップドファイルなどが入ります。
ただし、最初から全部の物理ページが張り付いているわけではありません。

Linux はデマンドページングにより、必要になったときにページを実際に割り当てます。
ページフォルトが起きた時点で、適切なページを用意して関連付ける感じです。
この遅延割り当てとコピーオンライトがあるおかげで、Linux はかなり効率よくメモリを使えます。

##### １章の６の９の５　キャッシュ

ストレージは RAM よりかなり遅いです。
そのため Linux は、空きメモリを積極的にキャッシュへ使います。
昔ながらの「バッファキャッシュ」という言い方もありますが、現代 Linux ではページキャッシュの理解が重要です。

一度読んだファイルデータをメモリへ残しておけば、次のアクセスを速くできます。
書き込みも、すぐにディスクへ全部書き出すのではなく、いったん汚れたページとして保持し、あとでまとめて書き戻します。
このあたりは性能に大きく効きます。

#### １章の６の１０　デバイスドライバ

Linux カーネルは、デバイスドライバを通してハードウェアを操作します。
ディスク、キーボード、マウス、NIC、サウンド、USB 機器など、実際の装置に近い制御はドライバが担当します。

ドライバはカーネルの一部ですが、ほかのカーネル部分とは一定のインタフェースを通してつながります。
このおかげで、カーネル全体は一貫した方法でデバイスを扱えますし、新しいハードウェアへの対応も追加しやすくなります。

ユーザー空間から見ると、多くのデバイスは /dev 配下のデバイスファイルとして見えます。
つまり、ユーザー空間はファイル操作に似たやり方でデバイスとやり取りし、その裏で VFS とドライバ層が実際の装置へ到達しているわけです。

ただし、現代 Linux は単純な「/dev を open して終わり」だけではありません。
ネットワークスタック、DRM/KMS、input subsystem、block layer、TTY layer など、デバイス種別ごとの大きなサブシステムが存在します。
そのため、ドライバ研究では「1 デバイス 1 ドライバ」だけでなく、「どのサブシステムを経由しているか」を見るのが大事です。

### １章の７　今回の研究の入口

この 1章は、まだ本格的なコード読解に入る前の全体像の整理という感じです。
ですが、ここでの見方を固めておくと、後で Linux ソースを読むときにだいぶ迷いにくくなると思います。

私としては、最初に次の入口を押さえるとよさそうだと思いました。

①　起動の入口
init/main.c を見て、カーネルがどの順で立ち上がるのかを把握する。

②　プロセス生成の入口
kernel/fork.c を見て、task がどう作られるのかを追う。

③　スケジューラの入口
kernel/sched/ を見て、task にどう CPU が割り当てられるのかを把握する。

④　ファイルシステムの入口
fs/ を見て、VFS がどう共通入口になっているかを知る。

⑤　メモリ管理の入口
mm/ を見て、仮想アドレス空間やページ管理の全体像に触れる。

UmuOS の視点では、この 1章で大事なのは、Linux の全部を最初から細かく覚えることではなく、
OS を構成する大きな責務を、どの部品が担当しているのかをつかむことかなと思います。
起動、権限、プロセス、メモリ、ファイル、割り込み、このへんをどう分けるかという発想自体が、かなり役に立つはずです。
