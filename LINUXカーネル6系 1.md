---
title: "LINUXカーネル6系の研究1　概要"
date: 2026-08-31
---

# LINUXカーネル6系の研究1　概要

このノートは、オライリー書籍「詳解LINUXカーネル 第3版（日本語版）」の 1章「概要」と 1.1「LinuxとほかのUNIX系カーネル」を入口にしつつ、
Linuxカーネル 6.6 LTS をそのまま研究するために再構成したものです。

今回は、細かな実装コードへ潜る前段階として、Linux 6.6 LTS をどういう種類のカーネルとして捉えるべきか、
どの機能がカーネルの本体で、どの機能がユーザー空間の構成要素なのか、
そして現代の Linux がどのような設計思想の上に成り立っているのかを整理します。

## 今回扱う範囲

- Linux は何者か
- Linux カーネルと GNU/Linux ディストリビューションの違い
- Linux 6.6 LTS が備える基本的な性格
- モノリシックカーネル、モジュール、プロセス、スレッド、SMP、VFS などの入口
- UmuOS の学習と接続しやすい観点

## 1. この節の要点

- Linux 6.6 LTS は、単なる「UNIX 互換 OS」ではなく、多数の CPU アーキテクチャ、デバイス、ファイルシステム、仮想化基盤を支える汎用カーネルです。
- Linux で重要なのは「カーネル本体」と「ユーザー空間のツール群」を分けて考えることです。
- Linux カーネルは、構造としてはモノリシックカーネルですが、実際にはモジュール、サブシステム、抽象化層を強く使う巨大なカーネルです。
- Linux 6.6 LTS を理解するには、POSIX 互換性だけでなく、スケジューラ、メモリ管理、VFS、デバイスモデル、割り込み、同期、ネットワーク、セキュリティ拡張まで視野に入れる必要があります。
- 「Linux は 1 つのプログラム」という見方は半分正しく、半分危険です。1つのカーネルイメージとして起動しますが、内部は多数のサブシステムと実行文脈に分かれています。

## 2. Linux 6.6 LTS での理解

### Linux とは何か

Linux 6.6 LTS でまず区別すべきなのは、Linux という言葉が 2 通りに使われることです。

- 狭義の Linux: カーネル本体
- 広義の Linux: カーネルに加えて、glibc、systemd、シェル、coreutils、udev、コンパイラ、パッケージ管理などを含む実用システム全体

研究対象としての Linux カーネル 6.6 LTS は、前者です。
つまり、ユーザープログラムのために CPU、メモリ、デバイス、ファイルシステム、ネットワーク、プロセス、権限管理を仲介する特権ソフトウェアです。

### Linux 6.6 LTS の位置づけ

Linux 6.6 LTS は長期保守系列のカーネルで、単に古い安定版という意味ではありません。
実運用で必要な安定性を持ちながら、現代 Linux の主要機能群を十分備えた、研究対象としても実務対象としても扱いやすい系列です。

この世代で前提にしてよいものとして、少なくとも次があります。

- マルチコア CPU を前提とした SMP 動作
- NUMA を意識したメモリ管理
- 完全に一般化された仮想ファイルシステム層
- カーネルモジュールの動的ロード
- POSIX 系 API を基盤にしつつ、Linux 独自拡張を多数持つ syscall 群
- cgroups、namespaces、seccomp、LSM などの隔離と保護の仕組み
- KVM、eBPF、io_uring など、現代 Linux を特徴づける重要機能

したがって、Linux 6.6 LTS を「昔ながらの UNIX の延長」とだけ見るのは不十分です。
UNIX の設計思想を土台にしつつ、サーバー、組み込み、仮想化、コンテナ、デスクトップまでを 1 つのカーネル系統で支える巨大な汎用基盤として捉える必要があります。

### UNIX 系との関係

Linux は UNIX 系の設計思想を強く受け継いでいますが、商用 UNIX のどれかをそのまま再実装したものではありません。
ユーザー空間から見れば、プロセス、ファイルディスクリプタ、パイプ、シグナル、fork/exec、パーミッション、端末といった概念に UNIX 的な共通性があります。
しかし、内部実装は Linux 独自です。

たとえば現代の Linux では、次のような理解が重要です。

- POSIX 互換は重要だが、カーネル内部設計を決め切るものではない
- ユーザー空間 API が似ていても、内部実装は他 UNIX と大きく異なりうる
- Linux は VFS、ページキャッシュ、スケジューラ、同期機構、デバイスモデル、ネットワークスタックを独自に発展させてきた

### モノリシックカーネルとは何か

Linux 6.6 LTS はモノリシックカーネルに分類されます。
これは「すべてが 1 ファイルにベタ書き」という意味ではありません。
カーネル空間で動く主要機能が、同じ特権アドレス空間の中で密接に連携している、という意味です。

この性格により、次の特徴があります。

- サブシステム間の呼び出しが比較的直接的で高速
- ドライバ、VFS、MM、ネットワークなどが同一カーネル空間で協調する
- バグの影響範囲は大きいので、同期、参照カウント、ライフサイクル管理が非常に重要

一方で Linux は、完全静的な一枚岩でもありません。
ロード可能モジュールにより、ドライバや一部機能を起動後に組み込み、取り外すことができます。
したがって、理解としては「モノリシックだが、モジュール化された巨大カーネル」と捉えるのが実態に近いです。

### カーネルスレッド、プロセス、スレッド

現代の Linux では、プロセスとスレッドを厳密に別物として実装するよりも、task という実行単位を共通に扱い、
何を共有するかで振る舞いを分ける考え方が重要です。

ユーザー空間のプロセスもスレッドも、カーネル内部では基本的に task_struct を持つ task として扱われます。
複数の task がメモリ空間、ファイルテーブル、シグナルハンドラなどを共有するなら、それはユーザーから見てスレッドらしく見えます。
共有せずに独立していれば、プロセスらしく見えます。

また、カーネル自身も多くのカーネルスレッドを持ちます。
たとえば、書き戻し、ワークキュー処理、メモリ回収、rcu 関連処理など、バックグラウンドの仕事を受け持つ kthread 群が存在します。
したがって、Linux 6.6 LTS を理解する際に「カーネルスレッドは限定的にしか使わない」という理解は不十分です。

### SMP と並行性

Linux 6.6 LTS は複数 CPU で同時実行されることを前提に設計されています。
これは「複数 CPU で動ける」というだけではなく、常に競合、可視性、ロック、メモリ順序、割り込みとの相互作用を考える必要があるという意味です。

そのため、現代 Linux の研究では次が中核になります。

- spinlock、mutex、rwsem、seqlock、completion などの同期原語
- atomic 操作と memory ordering
- RCU による read-mostly データの保護
- per-CPU 変数による競合削減
- preemption と interrupt context の違い

### ファイルシステム観

Linux 6.6 LTS の研究では、特定のファイルシステムだけを見るより、まず VFS を入口にする方が有効です。
ext4、xfs、btrfs、tmpfs、procfs、sysfs など多様なファイルシステムは、共通の VFS 抽象化の上に乗ります。

重要なのは、ユーザーが見る open、read、write、mount が、そのまま個別ファイルシステムへ飛ぶのではなく、
VFS のデータ構造と操作テーブルを経由して到達することです。

### 「Linux は無料で強力」という説明の現代的な読み方

古い導入文では、Linux の長所として無料、安定、高速、小さい、といった表現が並びます。
研究ノートとして重要なのは、感想ではなく構造です。

現代的には次のように読むとよいです。

- 無料: ライセンスとソース公開により、読める、ビルドできる、追跡できる
- 強力: 幅広いハードウェア、ファイルシステム、ネットワーク、仮想化を 1 系統で支える
- 安定: LTS 系列とメンテナンス体制が確立している
- 小さい: 構成次第で小型化できるが、現代の汎用カーネル全体は決して小さくない

## 3. 処理の流れ

この節は概念説明が中心なので、ここでは Linux 6.6 LTS を使うときに起きる大まかな責務の流れとして整理します。

1. ブートローダがカーネルイメージと必要なら initramfs をメモリへ配置し、カーネルへ制御を渡す。
2. カーネルは CPU 初期化、メモリ初期化、割り込み系初期化、各種サブシステム初期化を行う。
3. VFS、スケジューラ、メモリ管理、デバイスモデル、ドライバ基盤など、OS の基本骨格を立ち上げる。
4. 最初のユーザー空間へ制御を渡し、以後はユーザープログラムが syscall を通じてカーネル機能を利用する。
5. 実行中は、ユーザー task、カーネルスレッド、割り込み処理、softirq、workqueue など複数の実行文脈が並行に動く。
6. ファイルアクセスは VFS 経由で各ファイルシステムへ渡され、デバイスアクセスはドライバとサブシステムを通じて処理される。
7. 複数 CPU 上でこれらが同時に進むため、同期、参照管理、プリエンプション制御が常に重要になる。

## 4. 関係する主要な構造体・関数・サブシステム

この節では、1章の導入理解に役立つ入口だけ挙げます。

### 実行単位とスケジューリング

- task_struct: Linux の task を表す中心構造体。プロセスやスレッドの状態を持つ。
- mm_struct: 仮想アドレス空間を表す。
- sched_class: スケジューリング方針の実装を表す枠組み。
- kernel_clone(): 新しい task を作る中核経路の 1 つ。
- copy_process(): fork 系処理で task を生成する主要関数。

### メモリ管理

- mm/: 仮想記憶管理の中核ディレクトリ。
- struct page / folio: 物理ページ管理を理解する入口。
- slab、slub: カーネル内オブジェクト確保の基盤。

### ファイルシステム

- fs/: VFS と各ファイルシステム実装の入口。
- super_block: マウントされたファイルシステム全体を表す。
- inode: ファイルの実体情報を表す。
- dentry: パス名解決の中間表現。
- file: open 済みファイルを表す。

### ドライバとデバイスモデル

- drivers/: 各種デバイスドライバ。
- struct device: デバイスモデルの中心。
- struct bus_type: bus ごとの管理。
- struct file_operations: 文字デバイスなどで I/O 操作を束ねる。

### モジュール

- kernel/module/: モジュールロード処理。
- struct module: ロード済みモジュールの管理情報。
- init_module / finit_module syscall: ユーザー空間からモジュールを読み込む経路。

### 割り込みと並行実行

- irq_desc: IRQ 管理の中核。
- softirq、tasklet、workqueue: 後段処理の代表例。
- kthread_run(): カーネルスレッド生成の入口。

### 設定項目の例

- CONFIG_SMP: SMP 有効化
- CONFIG_PREEMPT_DYNAMIC または関連プリエンプション設定: プリエンプション挙動
- CONFIG_MODULES: モジュール機能
- CONFIG_NUMA: NUMA 対応
- CONFIG_KALLSYMS: シンボル解決支援

## 5. 用語補足

### カーネル

CPU の特権モードで動作し、ハードウェア資源とユーザー空間を仲介する中核ソフトウェアです。

### ディストリビューション

カーネル単体ではなく、ユーザー空間ツール、初期化システム、パッケージ群を含む実用環境全体です。

### POSIX

UNIX 系 OS で共通的に使える API や動作の標準化群です。
ただし、POSIX は主にユーザー空間から見た挙動を定めるもので、カーネル内部構造を直接決めるものではありません。

### モノリシックカーネル

主要機能が同一のカーネル空間で動作するカーネル構造です。
Linux はこの分類に入りますが、内部は強くモジュール化されています。

### カーネルモジュール

起動後にロード、アンロード可能なカーネルコードです。
典型例はデバイスドライバです。

### task

Linux カーネルが扱う実行単位です。
ユーザーから見たプロセスやスレッドも、内部では task として表現されます。

### VFS

Virtual File System の略です。
複数のファイルシステムに共通したファイル操作の枠組みを提供します。

### LTS

Long Term Support の略です。
長期保守を前提に継続的な修正が入る系列です。

## 6. 初学者が誤解しやすい点

- Linux と GNU/Linux ディストリビューションを同じものだと思いやすいです。研究対象としてはカーネル本体とユーザー空間を分ける必要があります。
- POSIX 準拠なら内部実装も他 UNIX と似ている、とは限りません。似るのは主に見える API です。
- モノリシックカーネルだから単純、ではありません。現代 Linux は巨大で、抽象化と分業が深いです。
- プロセスとスレッドを別の種類の構造体で持つと思いがちですが、Linux では task の共有関係として理解する方が本質に近いです。
- モジュール化されているからマイクロカーネルに近い、という理解は誤りです。モジュールも基本的には同じカーネル空間で動きます。
- 「Linux は小さい」は文脈依存です。最小構成は小さくできますが、汎用 6.6 LTS 全体は多機能で巨大です。

## 7. 調査対象

最初に追うと理解が進みやすい対象を挙げます。

- init/main.c: 起動後にどの順でカーネルが初期化されるか
- kernel/fork.c: process と thread の生成経路
- kernel/sched/: スケジューラの全体像
- fs/: VFS の入り口
- mm/: 仮想記憶とページ管理の入口
- kernel/module/: モジュールのロード機構
- Documentation/: 公式に近い説明群
- procfs と sysfs: 実行中システムの観測入口

観測コマンドの入口としては、次が有効です。

- uname -a: 実行中カーネルの把握
- cat /proc/version: ビルド情報の確認
- lsmod: 読み込まれたモジュール確認
- ps -eLf: task と thread の観測
- cat /proc/filesystems: 利用可能ファイルシステム確認

## 8. Linux 6.6 LTS 向けに書き直した本文

Linux 6.6 LTS は、UNIX 系の設計思想を受け継ぎながら発展してきた現代的な汎用カーネルです。
研究対象としては、Linux を単に「UNIX に似た OS」と見るだけでは足りません。Linux 6.6 LTS は、多数の CPU アーキテクチャ、
多様なメモリモデル、豊富なファイルシステム、巨大なデバイス群、仮想化、コンテナ、セキュリティ拡張まで支える基盤です。

ここでまず大切なのは、Linux カーネルそのものと、Linux ベースの実用システム全体を分けて考えることです。
カーネルは、CPU、メモリ、割り込み、デバイス、ファイルシステム、プロセス、通信といった資源を管理し、
ユーザープログラムに syscall を通じて機能を提供します。
一方で、シェル、ライブラリ、init システム、各種コマンド、コンパイラ、パッケージ管理などは、主にユーザー空間の構成要素です。

Linux 6.6 LTS の構造は、分類としてはモノリシックカーネルです。
ただし、これは単純な一枚岩という意味ではなく、1つの特権アドレス空間の中で多数のサブシステムが密接に連携する構造を指します。
スケジューラ、メモリ管理、VFS、ネットワークスタック、デバイスモデル、ドライバ群は同じカーネル空間で協調して動作します。
さらにロード可能モジュールにより、起動後に機能追加やドライバ導入も行えます。

実行単位の理解も重要です。
Linux では、ユーザーから見たプロセスとスレッドを、内部では task という共通の実行単位として扱います。
それぞれの task が、仮想アドレス空間、ファイルテーブル、シグナル処理などをどこまで共有するかによって、
プロセスらしさ、スレッドらしさが生まれます。
またカーネル自身も、書き戻し、メモリ回収、ワークキュー処理などのために、多くのカーネルスレッドを動かしています。

現代の Linux 6.6 LTS は、複数 CPU が同時に実行することを前提に設計されています。
そのため、ロック、RCU、atomic 操作、per-CPU データ、プリエンプション制御などの並行性設計が本質的です。
単に 1 つのプログラムとして読むのではなく、複数の実行文脈が同時進行する巨大な制御システムとして読む必要があります。

ファイルシステムについても、ext4 や xfs の個別実装から入るより、まず VFS を理解する方が効果的です。
ユーザー空間の open、read、write、mount といった操作は、VFS の抽象化を通して各ファイルシステムへ渡されます。
この「共通入口を作ってから個別実装へ分配する」という発想は、Linux 全体で非常に重要です。

研究の入口としては、起動なら init/main.c、task 生成なら kernel/fork.c、スケジューリングなら kernel/sched、
ファイルシステムなら fs、メモリ管理なら mm を追うのがよいです。
Linux 6.6 LTS を理解するとは、UNIX 的な概念を覚えることだけではなく、こうしたサブシステムごとの責務分担と接続点を追えるようになることです。

## 9. UmuOS向けの観点

- UmuOS では、まず「カーネル本体」と「上で動くユーザー空間」を明確に分ける設計意識が重要です。
- Linux の task モデルを見ると、プロセス管理を設計するときに「何を共有し、何を共有しないか」という観点が重要だと分かります。
- VFS の考え方は、将来 UmuOS で複数のファイルシステムや疑似ファイルを扱うときの強い参考になります。
- SMP や並行性の全実装は UmuOS にすぐ必要なくても、「複数の実行文脈が同時に状態を触る」という発想は早めに持った方がよいです。
- Linux のモジュール機構は、そのまま真似しなくても、「後から機能を追加できる構造」を考える参考になります。
- Linux の巨大さに圧倒される必要はなく、UmuOS ではまず最小の責務分担を作り、その後に VFS、task、driver 抽象化へ進めば十分です。