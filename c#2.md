---
title: "何も知らない人向けC#.net２（初級）　配列・メソッド・文字列 編"
---

# 何も知らない人向けC#.net 2 — 配列・メソッド・文字列 編

このシリーズは最終的に、読み手が C# のコードを見ても身構えず、落ち着いて読めるようになることを目標にします。  
第2巻では、第1巻で学んだ「表示」「入力」「条件」「繰り返し」の上に、

- 配列
- メソッド
- 文字列

を積み上げます。

この巻の範囲：

- 第五章：配列
- 第六章：メソッド
- 第七章：文字列

前提：

- OS：Windows 11
- 開発環境：VS Code
- 対象：.NET Framework 4.8.1 のコンソールアプリ

---

## 第五章でやること：

- 配列を「同じ種類の値を順番に並べて持つ箱」として理解する
- 配列の場所番号を使って、1つずつ取り出せる
- `for` を使って、配列の全部の要素を順に処理できる
- 配列の初期化、入力、合計、最大値、コピーを扱える
- 二次元配列を「行と列」で読める

前提：配列を理解すると、「たくさんの値をまとめて扱う」コードが一気に読みやすくなります。

---

## 配列

### 配列

配列は、「同じ種類の値を、順番に並べて持つ箱」です。

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        int[] scores = new int[5];

        scores[0] = 10;
        scores[1] = 20;
        scores[2] = 30;
        scores[3] = 40;
        scores[4] = 50;

        Console.WriteLine($"scores[0] = {scores[0]}");
        Console.WriteLine($"scores[4] = {scores[4]}");
    }
}
```

出力イメージ：

```text
scores[0] = 10
scores[4] = 50
```

ここで大事なこと：

- 最初の場所は `0`
- 5個あるなら最後は `4`
- 存在しない場所を読むとエラーになります

### 配列の走査

配列の全部を順番に読むことを **走査** と呼びます。  
ここでは「最初から最後まで順に見ること」だと思えば大丈夫です。

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        int[] scores = { 10, 20, 30, 40, 50 };

        for (int i = 0; i < scores.Length; i++)
        {
            Console.WriteLine($"scores[{i}] = {scores[i]}");
        }
    }
}
```

出力イメージ：

```text
scores[0] = 10
scores[1] = 20
scores[2] = 30
scores[3] = 40
scores[4] = 50
```

`Length` は「いくつ入っているか」です。

### 配列の初期化

配列は、作るときに最初の値をまとめて入れられます。

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        int[] a = { 1, 2, 3, 4, 5 };
        int[] b = new int[5];
        int[] c = { 7, 8 };

        Console.WriteLine($"a[0] = {a[0]}");
        Console.WriteLine($"b[0] = {b[0]}");
        Console.WriteLine($"c[1] = {c[1]}");
    }
}
```

出力イメージ：

```text
a[0] = 1
b[0] = 0
c[1] = 8
```

ポイント：

- `new int[5]` の整数配列は、最初は全部 `0`
- `{ 1, 2, 3 }` のように並べると、要素数は自動で決まります

### 配列の要素に値を読み込む

入力した値を配列に順番に入れてみます。

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        int[] numbers = new int[3];

        for (int i = 0; i < numbers.Length; i++)
        {
            Console.Write($"numbers[{i}] に入れる数字を入力してください: ");
            numbers[i] = int.Parse(Console.ReadLine());
        }

        Console.WriteLine("入力された値:");

        for (int i = 0; i < numbers.Length; i++)
        {
            Console.WriteLine(numbers[i]);
        }
    }
}
```

出力イメージ：

```text
numbers[0] に入れる数字を入力してください: 5
numbers[1] に入れる数字を入力してください: 8
numbers[2] に入れる数字を入力してください: 12
入力された値:
5
8
12
```

### 配列の要素の並びを反転する

前からと後ろからを入れ替えると、並びを逆順にできます。

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        int[] numbers = { 10, 20, 30, 40, 50 };

        for (int i = 0; i < numbers.Length / 2; i++)
        {
            int temp = numbers[i];
            numbers[i] = numbers[numbers.Length - 1 - i];
            numbers[numbers.Length - 1 - i] = temp;
        }

        for (int i = 0; i < numbers.Length; i++)
        {
            Console.WriteLine(numbers[i]);
        }
    }
}
```

出力イメージ：

```text
50
40
30
20
10
```

### 配列要素の最大値と最小値

配列の中で、いちばん大きい値といちばん小さい値を探します。

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        int[] numbers = { 24, 11, 90, 37, 5 };
        int max = numbers[0];
        int min = numbers[0];

        for (int i = 1; i < numbers.Length; i++)
        {
            if (numbers[i] > max)
            {
                max = numbers[i];
            }

            if (numbers[i] < min)
            {
                min = numbers[i];
            }
        }

        Console.WriteLine($"最大値 = {max}");
        Console.WriteLine($"最小値 = {min}");
    }
}
```

出力イメージ：

```text
最大値 = 90
最小値 = 5
```

### 配列の要素数

配列に何個入っているかは `Length` で調べます。

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        int[] numbers = { 3, 6, 9, 12 };

        Console.WriteLine(numbers.Length);
    }
}
```

出力イメージ：

```text
4
```

### 配列のコピー

配列の中身を別の配列へ写すこともできます。

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        int[] source = { 1, 2, 3, 4, 5 };
        int[] target = new int[source.Length];

        for (int i = 0; i < source.Length; i++)
        {
            target[i] = source[i];
        }

        for (int i = 0; i < target.Length; i++)
        {
            Console.WriteLine(target[i]);
        }
    }
}
```

出力イメージ：

```text
1
2
3
4
5
```

## 多次元配列

### 多次元配列

二次元配列は「行と列」で読む配列です。  
表のような形をイメージするとわかりやすいです。

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        int[,] table =
        {
            { 1, 2, 3 },
            { 4, 5, 6 }
        };

        for (int row = 0; row < table.GetLength(0); row++)
        {
            for (int col = 0; col < table.GetLength(1); col++)
            {
                Console.Write($"{table[row, col]} ");
            }

            Console.WriteLine();
        }
    }
}
```

出力イメージ：

```text
1 2 3 
4 5 6 
```

`GetLength(0)` は行の数、`GetLength(1)` は列の数です。

---

## まとめ

- 配列は、同じ種類の値を順番に並べて持つ箱
- 最初の場所は `0`
- `Length` で要素数を調べられる
- `for` を使うと、全部の要素を順に扱いやすい
- 二次元配列は、行と列で考える

---

## 第六章でやること：

- メソッドを「仕事をまとめた部品」として理解する
- 引数と戻り値を使えるようになる
- 同じ処理を何度も書かずに済むようにする
- 配列をメソッドに渡して使えるようにする
- 変数が見える範囲を理解する

---

## メソッド

### メソッドとは

#### Main メソッドと組み込みのメソッド

`Main` はプログラムの入口でした。  
それ以外にも、C# には最初から使える便利なメソッドがあります。

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        Console.WriteLine("Main から始まります");
        Console.WriteLine(Math.Abs(-5));
    }
}
```

出力イメージ：

```text
Main から始まります
5
```

#### メソッドとは

メソッドは、「ひとまとまりの仕事に名前を付けたもの」です。  
何度も使う処理を、ひとつの部品にできます。

#### メソッド定義

自分でメソッドを作ることを、ここでは「メソッドを定義する」と呼びます。

```csharp
using System;

class Program
{
    static void SayHello()
    {
        Console.WriteLine("こんにちは");
    }

    static void Main(string[] args)
    {
        SayHello();
    }
}
```

出力イメージ：

```text
こんにちは
```

#### メソッド呼び出し

作ったメソッドを使うことを **呼び出し** といいます。

```csharp
using System;

class Program
{
    static void PrintLine()
    {
        Console.WriteLine("----------");
    }

    static void Main(string[] args)
    {
        PrintLine();
        Console.WriteLine("本文です");
        PrintLine();
    }
}
```

出力イメージ：

```text
----------
本文です
----------
```

#### 3値の最大値を求めるメソッド

```csharp
using System;

class Program
{
    static int MaxOfThree(int a, int b, int c)
    {
        int max = a;

        if (b > max)
        {
            max = b;
        }

        if (c > max)
        {
            max = c;
        }

        return max;
    }

    static void Main(string[] args)
    {
        int result = MaxOfThree(15, 22, 18);
        Console.WriteLine(result);
    }
}
```

出力イメージ：

```text
22
```

#### メソッドの返り値を別のメソッドに渡す

返ってきた値は、そのまま別のメソッドに渡せます。

```csharp
using System;

class Program
{
    static int Add(int a, int b)
    {
        return a + b;
    }

    static void Main(string[] args)
    {
        Console.WriteLine(Add(10, 20));
    }
}
```

出力イメージ：

```text
30
```

#### 自作メソッドを呼び出すメソッド

メソッドの中から、別のメソッドを呼び出すこともできます。

```csharp
using System;

class Program
{
    static void PrintHeader()
    {
        Console.WriteLine("結果を表示します");
    }

    static void ShowResult(int value)
    {
        PrintHeader();
        Console.WriteLine(value);
    }

    static void Main(string[] args)
    {
        ShowResult(99);
    }
}
```

出力イメージ：

```text
結果を表示します
99
```

#### 値渡し

ふつうの引数は、値を渡します。  
中で値を変えても、呼び出し元の変数そのものは変わりません。

```csharp
using System;

class Program
{
    static void AddOne(int number)
    {
        number = number + 1;
        Console.WriteLine($"メソッドの中: {number}");
    }

    static void Main(string[] args)
    {
        int x = 10;

        AddOne(x);
        Console.WriteLine($"Main の中: {x}");
    }
}
```

出力イメージ：

```text
メソッドの中: 11
Main の中: 10
```

### メソッドの設計

#### 値を返さないメソッド（void）

画面に表示するだけのようなときは、返す値がないことがあります。  
そのときは `void` を使います。

```csharp
using System;

class Program
{
    static void ShowMessage()
    {
        Console.WriteLine("準備できました");
    }

    static void Main(string[] args)
    {
        ShowMessage();
    }
}
```

出力イメージ：

```text
準備できました
```

#### メソッドの汎用性

特定の1回だけではなく、いろいろな値で使える形にすると便利です。  
そのために引数を使います。

```csharp
using System;

class Program
{
    static int Square(int number)
    {
        return number * number;
    }

    static void Main(string[] args)
    {
        Console.WriteLine(Square(3));
        Console.WriteLine(Square(5));
    }
}
```

出力イメージ：

```text
9
25
```

#### 引数を受け取らないメソッド

いつも同じ動きをするなら、引数がなくてもかまいません。

```csharp
using System;

class Program
{
    static string GetTitle()
    {
        return "C# 入門";
    }

    static void Main(string[] args)
    {
        Console.WriteLine(GetTitle());
    }
}
```

出力イメージ：

```text
C# 入門
```

#### ブロック有効範囲

変数には「見える範囲」があります。  
波かっこの外では使えない変数もあります。

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        if (true)
        {
            int inside = 10;
            Console.WriteLine(inside);
        }
    }
}
```

出力イメージ：

```text
10
```

`inside` は、作った波かっこの中だけで使えます。

#### クラス有効範囲

クラスの中に置いたメソッドは、そのクラスの中から使えます。

```csharp
using System;

class Program
{
    static void PrintA()
    {
        Console.WriteLine("A");
    }

    static void Main(string[] args)
    {
        PrintA();
    }
}
```

出力イメージ：

```text
A
```

#### 宣言と定義

この教材では、まず「中身まで書く」をメインにします。  
つまり、メソッド名だけ先に書くより、中身も含めて作る形に慣れるのが先です。

#### 配列を引数として受け取る

配列もメソッドに渡せます。

```csharp
using System;

class Program
{
    static int Sum(int[] numbers)
    {
        int total = 0;

        for (int i = 0; i < numbers.Length; i++)
        {
            total += numbers[i];
        }

        return total;
    }

    static void Main(string[] args)
    {
        int[] values = { 10, 20, 30 };
        Console.WriteLine(Sum(values));
    }
}
```

出力イメージ：

```text
60
```

#### 線形探索

先頭から順番に探す方法を **線形探索** と呼びます。  
ここでは「最初から1つずつ調べる探し方」と考えれば十分です。

```csharp
using System;

class Program
{
    static int FindIndex(int[] numbers, int target)
    {
        for (int i = 0; i < numbers.Length; i++)
        {
            if (numbers[i] == target)
            {
                return i;
            }
        }

        return -1;
    }

    static void Main(string[] args)
    {
        int[] numbers = { 4, 8, 15, 16, 23 };
        Console.WriteLine(FindIndex(numbers, 15));
    }
}
```

出力イメージ：

```text
2
```

#### 二次元配列の受け渡し

二次元配列もメソッドに渡せます。

```csharp
using System;

class Program
{
    static void PrintTable(int[,] table)
    {
        for (int row = 0; row < table.GetLength(0); row++)
        {
            for (int col = 0; col < table.GetLength(1); col++)
            {
                Console.Write($"{table[row, col]} ");
            }

            Console.WriteLine();
        }
    }

    static void Main(string[] args)
    {
        int[,] values =
        {
            { 1, 2 },
            { 3, 4 }
        };

        PrintTable(values);
    }
}
```

出力イメージ：

```text
1 2 
3 4 
```

### 有効範囲

#### 有効範囲と名前の見え方

変数やメソッドには、使える場所と使えない場所があります。  
これを意識すると、「なぜ使えないのか」がわかりやすくなります。

#### 静的メソッド

この教材では `static` を付けてメソッドを書いています。  
ここでは「いまの書き方では、これを付ける」と覚えれば十分です。

---

## 第六章まとめ

- メソッドは、仕事に名前を付けた部品
- 引数は、外から渡す値
- 戻り値は、メソッドが返してくる値
- `void` は、返す値がない
- 配列もメソッドに渡せる
- 変数には使える範囲がある

---

## 第七章でやること：

- 文字列を「文字の並び」として理解する
- 長さ、連結、比較、取り出し、検索ができる
- 入力された文字列を加工できる
- 実務でよく見る文字列メソッドに慣れる

---

## 文字列

### 文字列の基本

#### 文字列とは

文字列は「文字の並び」です。  
`"Hello"` のように、ダブルクォートで囲んで書きます。

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        string message = "Hello";
        Console.WriteLine(message);
    }
}
```

出力イメージ：

```text
Hello
```

#### 文字列の長さ

何文字あるかは `Length` で調べます。

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        string text = "CSharp";
        Console.WriteLine(text.Length);
    }
}
```

出力イメージ：

```text
6
```

#### 文字列の連結

文字列どうしをつなげることを **連結** と呼びます。  
ここでは「くっつける」と考えれば大丈夫です。

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        string first = "おはよう";
        string second = "ございます";

        Console.WriteLine(first + second);
    }
}
```

出力イメージ：

```text
おはようございます
```

#### 文字を1つ取り出す

文字列の中の1文字は、場所番号で取り出せます。

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        string text = "ABC";

        Console.WriteLine(text[0]);
        Console.WriteLine(text[1]);
        Console.WriteLine(text[2]);
    }
}
```

出力イメージ：

```text
A
B
C
```

#### 文字列の走査

文字列も、最初から最後まで順に見られます。

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        string text = "CAT";

        for (int i = 0; i < text.Length; i++)
        {
            Console.WriteLine(text[i]);
        }
    }
}
```

出力イメージ：

```text
C
A
T
```

### 文字列の操作

#### 比較

2つの文字列が同じかどうかを比べられます。

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        string a = "cat";
        string b = "cat";

        Console.WriteLine(a == b);
    }
}
```

出力イメージ：

```text
True
```

#### 部分文字列

文字列の一部分だけを切り出せます。

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        string text = "abcdef";
        string part = text.Substring(2, 3);

        Console.WriteLine(part);
    }
}
```

出力イメージ：

```text
cde
```

#### 検索

ある文字や言葉がどこにあるかを探せます。

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        string text = "hello world";

        Console.WriteLine(text.IndexOf("world"));
    }
}
```

出力イメージ：

```text
6
```

#### 置換

見つけた言葉を、別の言葉に置き換えられます。

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        string text = "I like cats.";
        string changed = text.Replace("cats", "dogs");

        Console.WriteLine(changed);
    }
}
```

出力イメージ：

```text
I like dogs.
```

#### 大文字と小文字

英字は、大文字と小文字を変換できます。

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        string text = "Hello";

        Console.WriteLine(text.ToUpper());
        Console.WriteLine(text.ToLower());
    }
}
```

出力イメージ：

```text
HELLO
hello
```

#### 前後の空白を取り除く

入力では、前後に余計な空白が入ることがあります。  
`Trim` を使うと前後の空白を取り除けます。

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        string text = "  hello  ";

        Console.WriteLine($"[{text.Trim()}]");
    }
}
```

出力イメージ：

```text
[hello]
```

#### 分割

区切り記号で分けて、配列にできます。

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        string csv = "red,green,blue";
        string[] items = csv.Split(',');

        for (int i = 0; i < items.Length; i++)
        {
            Console.WriteLine(items[i]);
        }
    }
}
```

出力イメージ：

```text
red
green
blue
```

### 文字列とメソッド

#### 文字列を引数に取るメソッド

```csharp
using System;

class Program
{
    static void Greet(string name)
    {
        Console.WriteLine($"こんにちは、{name} さん");
    }

    static void Main(string[] args)
    {
        Greet("さくら");
    }
}
```

出力イメージ：

```text
こんにちは、さくら さん
```

#### 文字列を返すメソッド

```csharp
using System;

class Program
{
    static string MakeTitle(string subject)
    {
        return subject + " 入門";
    }

    static void Main(string[] args)
    {
        Console.WriteLine(MakeTitle("C#"));
    }
}
```

出力イメージ：

```text
C# 入門
```

#### 入力文字列の加工

入力した文字を、見やすい形に整えてから使うことがあります。

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        Console.Write("文字を入力してください: ");
        string text = Console.ReadLine();
        string cleaned = text.Trim().ToUpper();

        Console.WriteLine(cleaned);
    }
}
```

出力イメージ：

```text
文字を入力してください:  hello 
HELLO
```

#### 文字列配列の走査

文字列の配列も、配列として順に扱えます。

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        string[] names = { "春", "夏", "秋", "冬" };

        for (int i = 0; i < names.Length; i++)
        {
            Console.WriteLine(names[i]);
        }
    }
}
```

出力イメージ：

```text
春
夏
秋
冬
```

---

## 第七章まとめ

- 文字列は、文字の並び
- `Length` で長さを調べられる
- `+` でつなげられる
- `Substring` `IndexOf` `Replace` `Trim` `Split` などがよく使われる
- 文字列もメソッドに渡したり返したりできる
- 文字列配列は、実務でもよく出る

---

## この巻の終わりに

ここまで読めたなら、

- たくさんの値をまとめて扱う
- 処理に名前を付けて部品にする
- 入力された文字を整えて使う

という、実務で頻繁に出る入口まで来ています。

次は、クラス、インスタンス、ファイル処理に進むと、「画面に出すだけ」からもう一歩進んだプログラムが読めるようになります。