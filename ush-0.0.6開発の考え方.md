---
title: "自作シェル ush（うーしゅ）0.0.6 開発の考え方"
date: 2026-07-18
---

# 自作シェル ush（うーしゅ）0.0.6 開発の考え方

このノートは、`ush 0.0.6` の仕様を前提に、「どういう順番で自作シェルを育てていくか」を整理した開発方針メモです。

対象となる仕様解説は次のページです。

- [徹底解説！自作シェル ush（うーしゅ）0.0.6解説書]({{ '/ush-0.0.6徹底解説.html' | relative_url }})

ush は POSIX / bash 互換を目指す巨大なシェルではありません。
目的は、UmuOS 上で観測しやすく、挙動が固定され、未対応構文を明確に拒否できる軽量シェルを作ることです。

そのため、開発の基本方針は次の通りです。

```text
最小の実行器を作る
-> 字句解析を作る
-> 単純文を扱う
-> リダイレクトとパイプを扱う
-> 展開を扱う
-> if / while / for / case を扱う
-> エラーとテストを固める
```

最初から「シェル全部」を作ろうとしないことが重要です。
シェルは見た目は単純ですが、内部では入力、解析、展開、プロセス生成、ファイルディスクリプタ操作、終了ステータス管理が重なっています。

---

## 1. まず最小シェルを作る

最初の目標は、外部コマンドを1つ実行できることです。

```sh
ush> ls
ush> echo hello
ush> /bin/date
```

この段階では、クォート、変数展開、リダイレクト、パイプは不要です。

処理の流れは次のようにします。

```text
1行読む
空白で分割して argv を作る
fork する
子プロセスで exec する
親プロセスで wait する
終了ステータスを保存する
```

この段階で重要なのは、`fork()`、`execve()` または `execvp()`、`waitpid()` の流れを確実に理解することです。

ここが ush の実行器の土台になります。

---

## 2. builtin を実装する

次に、外部コマンドではなく、ush 自身が処理するコマンドを追加します。

ush 0.0.6 の builtin は次の通りです。

- `cd`
- `pwd`
- `export`
- `test`
- `[`
- `exit`
- `help`

特に `cd` は builtin でなければなりません。

理由は、カレントディレクトリが「シェル自身の状態」だからです。
もし `cd` を子プロセスで実行しても、親プロセスである ush のカレントディレクトリは変わりません。

```text
ush 本体で chdir() する
-> ush 自身のカレントディレクトリが変わる
```

この段階で持つべき状態は次です。

- 直前の終了ステータス
- 環境変数
- カレントディレクトリ
- 対話を続けるか、`exit` するか

---

## 3. tokenizer を作る

空白分割だけでは、シェルとしてすぐ限界が来ます。

例えば次の入力は、単純な空白分割では正しく扱えません。

```sh
echo "hello world"
cat < in > out
echo ok && echo next
case "$1" in start) echo start ;; esac
```

そこで、入力文字列をトークン列に分解する tokenizer を作ります。

ush 0.0.6 で扱う主なトークンは次です。

- WORD
- `|`
- `&&`
- `||`
- `;`
- `<`
- `>`
- `>>`
- `;;`
- `)`

例として、次の入力は、

```sh
cat < in > out
```

次のようなトークン列になります。

```text
WORD(cat)
<
WORD(in)
>
WORD(out)
```

この段階では、まだ実行まで進まなくてもよいです。
まずは「入力を正しくトークン列に分解できる」ことを確認します。

---

## 4. クォートとコメントを扱う

次に、シェル入力でよく使うクォートとコメントを扱います。

```sh
echo "hello world"
echo 'no expand $HOME'
echo a # comment
```

ush 0.0.6 では、クォートの仕様をあえて単純にしています。

- `'...'` は1トークン
- `"..."` も1トークン
- `'foo'bar` は構文エラー
- `"foo"bar` も構文エラー

bash のように複雑なクォート連結を許すと、実装も説明も急に難しくなります。
ush では「対応する形」と「対応しない形」をはっきり分けることで、観測しやすいシェルにします。

コメントは、未クォートでトークン先頭に `#` が出た場合だけ、行末までを無視します。

```sh
echo a # ここからコメント
echo a#b # a#b は WORD の一部
```

---

## 5. simple 構文を作る

ここで、1つの単純文を解析します。

対象は次のような入力です。

```sh
echo hello
cat < in
cat > out
cat < in > out
echo ok && echo next
cat file || echo failed
cat file | grep root
```

ush 0.0.6 では、simple 構文を意図的に小さくしています。

- パイプは1段のみ
- リダイレクトはコマンド末尾のみ
- 入力リダイレクト `<` は出力リダイレクト `>` / `>>` より前
- builtin はパイプ不可
- builtin はリダイレクト不可

これは実装を小さく保つための重要な制約です。

例えば、多段パイプを許すと、複数プロセス、複数 pipe、複数 wait を管理する必要があります。
ush 0.0.6 では、まず `A | B` の1段だけに固定します。

```sh
# OK
cat /etc/passwd | grep root

# NG
cat /etc/passwd | grep root | wc -l
```

---

## 6. リダイレクトを実装する

リダイレクトでは、標準入力や標準出力をファイルにつなぎ替えます。

```sh
cat < in
echo hello > out
echo next >> out
```

内部では、子プロセス側で次の処理を行います。

```text
open()
dup2()
close()
exec()
```

例えば、

```sh
echo hello > out
```

なら、子プロセスで `out` を開き、そのファイルを標準出力に割り当ててから `exec` します。

ush 0.0.6 では builtin のリダイレクトは未対応にします。

```sh
# NG
pwd > out
```

これにより、builtin 実行前後で標準入出力を退避・復元する複雑な処理を避けられます。

---

## 7. 1段パイプを実装する

パイプは、左側コマンドの出力を右側コマンドの入力につなぎます。

```sh
cat /etc/passwd | grep root
```

内部処理は次のようになります。

```text
pipe() でパイプを作る
左側コマンド用に fork する
  stdout を pipe の書き込み側へ dup2 する
  exec する
右側コマンド用に fork する
  stdin を pipe の読み込み側へ dup2 する
  exec する
親プロセスは不要な fd を閉じる
両方の子プロセスを wait する
```

パイプはファイルディスクリプタの管理が重要です。
閉じるべき fd を閉じ忘れると、右側のコマンドが入力終了を検出できずに止まることがあります。

---

## 8. expand を作る

次に、変数展開やチルダ展開を実装します。

ush 0.0.6 の主な展開は次です。

- `$NAME`
- `${NAME}`
- `$?`
- `$#`
- `$0` から `$9`
- `~`
- `~/path`
- `$(...)`

重要なのは、tokenize と expand を混ぜないことです。

おすすめの流れは次です。

```text
tokenize する
WORD として保持する
実行直前に expand する
```

例えば、

```sh
export FOO=hello
echo "$FOO"
```

の `$FOO` は、tokenize 時点ではまだ文字列です。
実行直前に環境変数を見て `hello` に変換します。

---

## 9. glob を実装する

glob は、`*.md` のようなパターンをファイル名に展開する処理です。

```sh
ls *.md
for f in *.md
do
  echo "$f"
done
```

ush 0.0.6 の glob 方針は次です。

- 未クォートの語だけ glob 対象
- `*`, `?`, `[...]` を扱う
- マッチ0件なら、その語をそのまま残す
- `[a-z]` のような範囲指定は未対応

マッチ0件でそのまま残す仕様にしておくと、挙動が固定されます。

```sh
ls *.md
```

で `.md` ファイルがなければ、`*.md` という文字列のまま外部コマンドに渡します。

---

## 10. script_parse を作る

ここから `if`、`while`、`for`、`case` を扱います。

ush 0.0.6 では、構文解析を2段階に分けます。

```text
script_parse
  if / while / for / case / ; を扱う

parse_line
  | / && / || / リダイレクトを扱う
```

この分離が重要です。

例えば次の入力は、単純な外部コマンドではありません。

```sh
if test -e /etc/passwd
then
  echo ok
fi
```

script_parse では、これを `ST_IF` のような構造として保持します。

構文木の種類は、ざっくり次のように考えます。

- `ST_SIMPLE`
- `ST_SEQ`
- `ST_IF`
- `ST_WHILE`
- `ST_FOR`
- `ST_CASE`

---

## 11. ブロック入力を扱う

ush 0.0.6 は、入力を1行ごとに即実行するのではなく、「完結したブロック」として扱います。

例えば対話モードで、

```sh
if test -e /etc/passwd
then
  echo ok
fi
```

と入力する場合、1行目の時点ではまだ `if` が閉じていません。

そのため、対話モードでは次の流れにします。

```text
1行読む
内部バッファへ追加する
parse してみる
未完了なら次の行を読む
完了なら実行してバッファをクリアする
エラーなら表示してバッファをクリアする
```

parse の結果は、次のように分けると扱いやすいです。

- `PARSE_OK`
- `PARSE_INCOMPLETE`
- `PARSE_ERROR`
- `PARSE_UNSUPPORTED`

---

## 12. 制御構文を実行する

構文木ができたら、`script_exec` で実行します。

`if` は、条件部の終了ステータスで分岐します。

```text
条件部を実行する
終了ステータスが 0 なら then 部を実行する
0 以外なら elif / else を見る
```

`while` は、条件部が成功する間だけ body を実行します。

```text
条件部を実行する
0 なら body を実行する
もう一度条件部を実行する
0 以外なら終了する
```

`for` は、語リストを1つずつ環境変数に入れて body を実行します。

```text
WORDS を展開する
1語ずつ NAME に setenv する
body を実行する
```

`case` は、対象語を展開し、最初に一致したパターンの body を実行します。

```text
WORD を展開する
PAT と比較する
一致した body を実行する
```

ush 0.0.6 は `break` / `continue` を持たないため、ループ実行は比較的単純にできます。

---

## 13. エラー分類を固定する

ush では、エラーを雑に全部 `syntax error` にしないことが重要です。

大きく次の2つに分けます。

### syntax error

文法として壊れているものです。

- クォート未閉鎖
- `fi` / `done` / `esac` 不足
- `case` の `;;` 不足
- リダイレクトの位置違反

### unsupported syntax

シェル一般ではあり得るが、ush では対応しないものです。

- 多段パイプ
- ヒアドキュメント
- `$(...)` の入れ子
- `NAME=VALUE` 形式
- builtin のパイプ
- builtin のリダイレクト
- `~user`

終了コードの目安も固定します。

- `0`: 成功
- `1`: 実行時エラー
- `2`: 構文エラー、未対応構文、引数不正
- `126`: 実行不可
- `127`: コマンド未発見

エラーの意味が固定されていると、UmuOS 上で観測・切り分けをするときに役立ちます。

---

## 14. テストを作る

ush は仕様を小さく固定しているため、テストを作りやすいです。

最低限、次のカテゴリで確認します。

### tokenize

- 空白区切り
- クォート
- コメント
- 演算子
- 未閉じクォート

### simple 構文

- 外部コマンド
- builtin
- `&&`
- `||`
- `|`
- `<`
- `>`
- `>>`

### expand

- `$NAME`
- `${NAME}`
- `$?`
- `$0` から `$9`
- `~`
- `$(...)`
- glob

### script 構文

- `if`
- `elif`
- `else`
- `while`
- `for`
- `case`

### エラー系

- 多段パイプ
- `$(...)` 入れ子
- `NAME=VALUE`
- builtin のリダイレクト
- `case` の `;;` 忘れ

「動くべきものが動く」だけでなく、「禁止したものが確実にエラーになる」ことも確認します。

---

## 15. バージョンごとの開発目標

段階的に作るなら、次のようにバージョンを切ると分かりやすいです。

### 0.0.1

- 1コマンド実行
- 空白分割
- `fork` / `exec` / `wait`
- 終了ステータス保存

### 0.0.2

- builtin
- `cd`
- `pwd`
- `exit`
- `export`
- PATH 探索

### 0.0.3

- tokenizer
- クォート
- コメント
- `;` による複数文

### 0.0.4

- `&&`
- `||`
- `<`
- `>`
- `>>`
- 1段パイプ

### 0.0.5

- `$NAME`
- `${NAME}`
- `$?`
- `$0` から `$9`
- `~`
- glob
- `$(...)`

### 0.0.6

- `if`
- `while`
- `for`
- `case`
- ブロック入力
- `script_parse`
- `script_exec`

### 0.0.7 以降

- テスト強化
- エラーメッセージ改善
- UmuOS 実機向けスクリプト例の追加
- 必要な機能だけを慎重に追加

---

## まとめ

ush 開発の中心は、最初から大きなシェルを作ることではありません。

まず外部コマンドを実行できる最小の実行器を作り、その上に tokenizer、simple parser、リダイレクト、パイプ、展開、script parser を順番に重ねます。

```text
実行できる
-> 分解できる
-> 構文として読める
-> 展開できる
-> 制御構文として実行できる
```

この順番で進めると、各段階で動作確認しながら ush を育てられます。

ush 0.0.6 の価値は、bash 互換を広く追うことではなく、UmuOS で使うための軽量で観測しやすいシェルとして、仕様と制約を固定している点にあります。
