---
title: "徹底解説！自作エディタuim（ゆーあいえむ）0.0.2徹底解説書"
date: 2026-03-09
---

# uim 0.0.2 徹底解説（UmuOS Interactive Minimal editor）

対象: UmuOS-0.1.7-base-stable 想定

この文書は、自作エディタ uim 0.0.2 を「通常使用エディタ（研究用途でコードを読みながら編集する）」として最大限使うための、仕様と実装（docs の詳細設計書）に基づく実用解説である。

- 目的: UmuOS 上で “最小の vi ライク操作” を安定して使えるようにする（NORMAL/INSERT/`:`/`/`、`dd/yy/p/a`、`gg/G`、`:w/:q/:wq/:q!`、`n`）
- 非目的: vim 互換の追求、undo/redo、置換、visual、複数バッファ、マクロ、正規表現検索、後方検索、検索ハイライト、差分描画など

uim は「できることを絞り、未対応は作らない」方向の設計である。つまり “vim で当然” な操作は多数できないが、その代わり挙動が読みやすく、UmuOS 上での研究・観測の邪魔をしない。

---

## はじめに（読み方・マスター用）

この資料は「キー一覧」だけではなく、
- 何ができる/できないか（制約）
- その制約がどこから来るか（実装上の理由）
- どう回避するか（運用）
をセットで押さえる構成にしている。

おすすめの読み方:

### A. まず “普段使いの最短ルート” を作る

- 「0. 最短チートシート」だけ見て、一度保存まで到達する
- 次に「3. 日常利用の型（運用レシピ）」で、自分の作業の型に合わせる

### B. 次に “ハマりどころ” を潰す

- 「4. 仕様（挙動の細部）と制限」を読み、
  - 検索の開始位置・ラップしない
  - `:q` が拒否される条件
  - 4096 バイト制限
  - UTF-8 の扱い（壊さないが厳密検証ではない）
  を固定する

### C. 最後に “内部構造” を押さえて切り分けを早くする

- 「6. アーキテクチャ（モジュール分割）」→「7. データ構造」→「8. 入力処理」→「9. 描画」
- 何かおかしい時は「10. よくある症状と切り分け」へ戻る

---

## 0. 最短チートシート（まずこれだけ）

### 0.1 起動

- `uim <file>`: ファイル編集（無ければ空バッファで開始）
- `uim --version`: `0.0.2` を出して終了
- `uim --batch <file>`: 標準入力を疑似キーとして読み、描画なしで編集（スモーク用）

### 0.2 モード

- NORMAL: 移動、行操作、検索開始、コマンド開始
- INSERT: 文字入力、改行、Backspace
- COLON（`:`）: `:w/:q/:q!/:wq`
- SEARCH（`/`）: `/pattern` 入力→Enter で検索

### 0.3 移動（NORMAL / INSERT の矢印）

- `h/j/k/l`: 左/下/上/右（矢印キーでも同等）
- `0`: 行頭（ただし “数値プレフィクス入力中ではない” 等の条件あり）
- `$`: 行末
- `gg`: 先頭行
- `G`: 最終行
- `10gg`: 10行目へ
- `10G`: 10行目へ

### 0.4 編集（NORMAL）

- `i`: INSERT へ
- `a`: カーソルを1文字右に動かして INSERT（行末ではそのまま INSERT）

### 0.5 行操作（NORMAL）

- `dd`: 現在行を削除（同時にヤンクにも入る）
- `3dd`: 3行削除（削除した3行がヤンクに入る）
- `yy`: 現在行をヤンク
- `3yy`: 3行ヤンク
- `p`: ヤンク内容を “現在行の下” に貼り付け（複数行ヤンクなら複数行挿入）

### 0.6 検索（NORMAL）

- `/pattern` + Enter: 前方検索（リテラル一致、大小区別あり、ラップしない）
- `n`: 直前の検索をもう一回前方へ

### 0.7 保存と終了（COLON）

- `:w` 保存
- `:q` 終了（変更があると拒否）
- `:q!` 破棄して終了
- `:wq` 保存して終了

---

## 1. uim の位置づけ（vim/vi との使い分け）

uim は「UmuOS で研究する時の編集器」であり、主な価値は次。

- 最小機能で “確実に動く”
- 全画面描画は単純（可視領域の上書き）で、挙動が追いやすい
- UTF-8（日本語コメント）を壊しにくい（境界を守って移動・削除・挿入）

逆に、次を期待すると苦しい。

- undo が無い（ミスったら手で戻す／ファイルコピーを作る、等の運用が必要）
- `x`、`w/b`、置換、visual など、vi/vim の“便利ショートカット”はほぼ無い
- 検索は前方のみ、ラップ無し、正規表現無し

結論: uim は「編集機能を広げる」より「壊れない最小の編集」を優先している。

---

## 2. 実行環境・前提（端末と文字）

- 端末は ANSI エスケープ（カーソル移動、反転表示など）を解釈できること
- 端末が UTF-8 を表示できること
- raw mode（termios）が有効にできること

端末サイズ取得に失敗した場合は 24x80 とみなす。

### 2.1 コマンドライン仕様（起動の入口）

uim 0.0.2 の起動インターフェースは小さい。

- `uim <file>`: interactive（通常の全画面編集）
- `uim --batch <file>`: batch（標準入力を疑似キーとして編集、描画なし）
- `uim --version`: バージョン表示して終了
- `uim -h` / `uim --help`: 使い方を標準エラーへ出して終了

重要な制約:
- **ファイル名（`<file>`）は必須**。省略すると usage を出して終了する。
- 未知のオプション（`-something`）は `uim: unknown option` で終了する。

終了コード（実装に基づく目安）:
- `0`: 正常終了（`--version`、編集終了、batch 完走）
- `1`: 失敗（ファイル open 失敗、batch 読み取り失敗など）
- `2`: 引数不正（usage / unknown option）

### 2.2 ビルドとスモーク（自分で“正”を再現する）

uim は「単一バイナリ」で、参照実装は `uim-0.0.2/uim` 配下にある。

ビルド方針（推奨）:
- `musl-gcc -static` を基本とする
- `-D_POSIX_C_SOURCE=200809L -D_XOPEN_SOURCE=700` を指定（termios/ioctl 等の宣言を確実に見せる意図）

ビルド例（ホスト開発機）:

```sh
musl-gcc -std=c11 -O2 -g -static \
  -D_POSIX_C_SOURCE=200809L -D_XOPEN_SOURCE=700 \
  -Iuim/include \
  uim/src/*.c \
  -o uim
```

スモーク（`--batch` で操作列を流して検証）:

```sh
cd uim
bash tests/smoke_uim.sh
```

スモークが確認していること（抜粋）:
- INSERT + 日本語入力 → `:wq`
- `dd/yy/p`、`3dd/3yy`、複数行貼り付け
- `0/$`、`gg`、`1G` の移動
- `/pattern` と `n`（日本語パターン含む）
- `--version` が `0.0.2`

“普段使い”の意味でも、
- uim のバイナリを更新したらスモークを回す
- 端末や環境が変わって挙動が怪しくなったら `--batch` で再現する
が最短の安全策になる。

---

## 3. 日常利用の型（運用レシピ）

uim は機能が少ないぶん、日常の作業は「型」を作るとラクになる。

### 3.1 最小の編集フロー（作業の呼吸）

1) `uim file` で開く
2) `j/k` で行を合わせる
3) `0` で行頭 → `i` で INSERT
4) 入力 → `ESC`
5) `:w` で保存（こまめに）
6) 終了時は `:wq` または `:q!`

ポイント:
- undo が無いので **小さく保存**（`:w`）が安全
- `:q` は変更があると拒否される（後述）ので、迷ったら `:wq` / `:q!`

### 3.2 追記の型（`a` の使いどころ）

- 行中に1文字足す → その位置に移動 → `a` → 追記 → `ESC`

`a` は内部的に「1文字右へ動かして INSERT」なので、
- 目的が“カーソルの右に挿入”なら自然
- 行末では右に行けないので、結果的に “行末に追記” になる

### 3.3 複数行の移動は “数値プレフィクス” を使う

`10G` / `10gg` が使えると、行番号で移動できる。

- ファイルの先頭へ: `gg`
- ファイルの末尾へ: `G`
- N行目へ: `10G` または `10gg`

### 3.4 行のコピー＆移動は `yy`/`dd` と `p` でやる

- 行を複製したい: `yy` → `p`
- 行を移動したい: `dd` → 移動 → `p`

`dd` は削除と同時にヤンクにも入る（= カット&ペーストの動き）。

### 3.5 検索は “次へ進む” だけと割り切る

- `/pattern` で前方検索
- `n` で次の一致へ

ラップしないので、末尾まで行ったら見つからない。
その場合は `gg` で先頭へ戻ってから再検索する、という運用が確実。

---

## 4. 仕様（挙動の細部）と制限

ここは「普段使いで事故るポイント」だけを実装に基づき固定する。

### 4.1 ファイル読み込みと内部表現（改行の扱い）

読み込み:
- `\n` / `\r` は行末から除去して保持しない
- つまり内部の各行は “改行を含まない C文字列” として管理される

保存:
- 各行の末尾に必ず `\n` を付けて書き出す
- そのため **最終行も改行で終わる**

この仕様は「テキストファイルとして素直」だが、
“最終行に改行が無いファイル” を編集・保存すると、必ず改行が付く。

### 4.2 1行の最大長（4096バイト）

- 1行の最大バイト数は既定 4096
- これを超える挿入は失敗する（挿入できない）

注意:
- 4096 は “文字数” ではなく “バイト数”
- 日本語（UTF-8）は 1文字が複数バイトなので、実質の文字数は減る

### 4.3 UTF-8 を壊さないための約束（ただし厳密検証ではない）

uim が守っている最低限:
- カーソル位置（列）は **UTF-8 文字境界（先頭バイト）**に揃える
- Backspace は「直前の UTF-8 1文字」を削除する

ただし、uim は
- 不正な UTF-8 シーケンスを厳密に検証して拒否する
まではしていない。

実用上の結論:
- 普通の UTF-8 テキストを編集する限り “壊れにくい”
- ただしバイナリや壊れたUTF-8を編集すると挙動は保証しない

### 4.4 表示幅の近似（全角がズレにくい理由）

uim の幅計算は “厳密” ではないが、実用で破綻しにくい近似。

- ASCII: 幅1
- 非ASCII（先頭バイトが 0x80 以上）: 幅2 とみなす
- タブ `\t`: タブストップ（4）に整列

これにより、日本語が混ざっても「カーソルが極端にズレる」問題を避ける。

### 4.5 `0` が “行頭” にならないことがある（数値プレフィクスとの衝突）

NORMAL では数値プレフィクス（例: `3dd`, `10G`）を解釈する。
そのため `0` は常に行頭ではなく、次の条件の時だけ行頭になる。

- 数値プレフィクスが未入力（= 0）
- かつ、ペンディングコマンド（`d/y/g` の1打鍵目）が無い

「`10G` を打とうとして `0` が行頭になってしまう」事故を避けるための仕様。

### 4.6 `:q` の拒否条件（modified フラグ）

- バッファに変更がある（modified=1）場合、`:q` は拒否される
- メッセージ: `No write since last change (use :q!)`

この挙動を理解しておくと、終了で迷わない。

### 4.6.1 Ctrl-C は “NORMAL のときだけ” 終了になる

`Ctrl-C` は term 層で特別キーとして拾われるが、終了処理（`running=0`）が入っているのは NORMAL の分岐だけ。

実用上の注意:
- COLON 入力中（`:`）や SEARCH 入力中（`/`）、INSERT 中は、`Ctrl-C` を押しても終了しない（基本的に無視される）
- “抜けたい” は各モードの作法で戻る
  - INSERT → `ESC`
  - COLON/SEARCH → `ESC` でキャンセル
  - その後 NORMAL で `Ctrl-C` または `:q!` など

### 4.6.2 未対応の `:` コマンドは `unknown command`

COLON（`:`）で受け付けるのは基本的に `w/q/q!/wq` のみ。

- それ以外（例: `:e`, `:set`, `:w <path>` 等）は `unknown command` を表示して編集を継続する
- つまり、uim では「できるコマンドは少ないので、迷ったら `:w`/`:q`/`:q!`/`:wq` に戻る」が基本になる

### 4.7 検索仕様（前方のみ、ラップ無し、境界を守る）

- `/pattern` は前方検索
- 検索開始位置は「カーソル位置の次の文字」から
- バッファ終端まで探し、見つからない場合は “カーソル位置を維持” して `Pattern not found`
- ラップアラウンド無し（末尾まで行ったら終わり）

UTF-8 を壊さない工夫:
- 一致開始位置は UTF-8 文字境界に限定する（継続バイト中の一致は捨てる）

### 4.8 入力バッファ長の上限（地味に効く）

- `:` コマンド入力は 64 バイト上限（ASCII 32..126 のみ取り込む）
- `/` 検索入力は 64 バイト上限（32..255 を取り込む）

日本語検索の注意:
- 日本語は UTF-8 の複数バイトなので、64バイトをすぐ使い切る（長いパターンは入らない）

### 4.9 空の検索 Enter は “直前の検索” を再利用する

SEARCH モードで何も入力せず Enter した場合:
- 直前の検索が無ければ `no previous search`
- 直前の検索があれば、そのパターンで検索を実行する

これは `n` と同様に「最後に成功/登録した検索パターン」を軸に設計されている。

---

## 5. `--batch`（疑似キー入力）を理解して活用する

`--batch` は「描画なしで、標準入力からキー列を流して編集する」モード。

- スモークテスト用
- 手元で “この操作列を流すとこうなる” を再現できる

### 5.1 代表例

```sh
# i で挿入→Hello→ESC→:wq→Enter
printf 'iHello\033:wq\n' | uim --batch /tmp/out.txt
```

キーの対応:
- `\033`（0x1b）: ESC
- `\n` / `\r`: Enter
- `\x7f` / `\b`: Backspace

### 5.2 batch ができる/できないこと

- できる: 編集結果（保存）を再現する
- できない: 画面表示の確認（描画しない）

日常運用の結論:
- “この操作列でファイルがこう変わるはず” を検証するのに便利
- 端末描画が変な時の切り分け（render/term なのか editor/buf なのか）にも使える

---

## 6. アーキテクチャ（モジュール分割）

uim は小さいが、責務を分けてあるので理解しやすい。

- `term`（端末I/O）
  - raw mode（termios）
  - 1キー読み取り、ESCシーケンスの最小解釈（矢印/Home/End）
  - ANSI エスケープで画面制御（clear/move/hide cursor）
  - 端末サイズ取得

- `utf8`（境界/表示幅）
  - 前後移動（prev/next）
  - 表示幅の近似（ASCII=1、非ASCII=2、タブ整列）
  - 表示列↔バイト位置変換（横スクロール、上下移動の列維持）

- `buf`（行配列バッファ）
  - load/save
  - 行削除/挿入（`dd`/`p`）
  - 行内挿入（INSERT）
  - UTF-8 1文字削除（Backspace）
  - 行分割（Enter）

- `render`（描画）
  - 可視領域の全上書き再描画
  - 縦/横スクロール
  - ステータス行（反転）とメッセージ行
  - カーソル位置決定（表示列計算）

- `editor`（状態遷移・キー処理）
  - モード管理
  - `dd/yy/p/a`, `/pattern`, `n`, `:w/:q/:wq/:q!`
  - INSERT で UTF-8 を組み立ててから挿入
  - `--batch` 入力処理

- `main`（起動）
  - 引数解析（`--batch`/`--version`）
  - バッファロード
  - interactive/batch 分岐

「普段使いの挙動」を早く理解したい場合は `editor` を中心に読むと良い。

### 6.1 モジュール別観測表（入力→編集→描画→永続を切り分ける）

uim は小さいので、問題が起きたときは “どの層か” を先に確定すると速い。

横長テーブルは環境によって横スクロールになるので、ここでは「モジュールごとの縦型ミニ表」で整理する。

#### `main`（起動）

| 項目 | 内容 |
|---|---|
| 主体（層） | 起動（引数解析と実行モード分岐） |
| 入力 | `argv`（`--batch` / `--version` / `<file>`） |
| 主要処理 | オプション解析 → `buf_load` → interactive/batch 実行 |
| 出力（次段へ） | エディタ状態を初期化してループへ、またはバージョン文字列 |
| 観測点（成功判定） | `--version` が `0.0.2` / usage が期待通り / ファイルが開ける |
| 失敗時の切り分け | 引数不正（終了コード2）か、open失敗（終了コード1）かを先に分ける |

#### `term`（端末I/O）

| 項目 | 内容 |
|---|---|
| 主体（層） | 端末（raw mode とキー解釈） |
| 入力 | `stdin/tty`（1バイト入力 + ESC シーケンス） |
| 主要処理 | raw mode → 1キー読み → 矢印/Home/End の最小解釈 |
| 出力（次段へ） | キーコード（ASCII または特殊キー） |
| 観測点（成功判定） | 矢印/Home/End が意図通りに移動として効く |
| 失敗時の切り分け | `--batch` では再現しないなら term/render 側が濃い。端末ごとの送出シーケンス差も疑う |

#### `editor`（状態機械）

| 項目 | 内容 |
|---|---|
| 主体（層） | 状態遷移（モード、2打鍵、数値プレフィクス） |
| 入力 | キー列 |
| 主要処理 | モード処理、`pending`（`d/y/g`）と `num_prefix` の合成、コマンド確定 |
| 出力（次段へ） | `buf` 操作（挿入/削除/貼り付け等）と状態更新（カーソル、メッセージ） |
| 観測点（成功判定） | `3dd`/`10G`/`gg`/`n`/`:` 等が期待通りに効く |
| 失敗時の切り分け | `--batch` で編集結果がズレるなら editor/buf/utf8 側。描画だけ変なら render/term 側 |

#### `buf`（行編集）

| 項目 | 内容 |
|---|---|
| 主体（層） | 行配列バッファ（内部表現と保存） |
| 入力 | 行・列（バイト位置）、挿入バイト列 |
| 主要処理 | 行削除/挿入、行分割、UTF-8 1文字削除、modified 更新 |
| 出力（次段へ） | 更新された行配列と modified 状態 |
| 観測点（成功判定） | `:w` でファイルが期待通りに変化する（末尾改行が付く等の仕様含む） |
| 失敗時の切り分け | 4096バイト上限（バイト数）や、カーソルが UTF-8 境界に揃っているかを疑う |

#### `utf8`（境界/表示幅）

| 項目 | 内容 |
|---|---|
| 主体（層） | UTF-8 境界と表示列計算（近似） |
| 入力 | 文字列とバイト位置 |
| 主要処理 | prev/next、表示列↔バイト位置変換、幅計算（ASCII=1/非ASCII=2/タブ整列） |
| 出力（次段へ） | “このバイト位置は画面の何列か” / “この列は何バイト目か” |
| 観測点（成功判定） | 日本語混在でもカーソルが極端に崩れない／上下移動で列維持が効く |
| 失敗時の切り分け | 厳密な幅（EAW）ではないのでズレは起きうる。問題が「編集の破壊」か「表示のズレ」かを分ける |

#### `render`（描画）

| 項目 | 内容 |
|---|---|
| 主体（層） | 画面出力（全上書き再描画） |
| 入力 | 状態（buf/cursor/scroll/status） |
| 主要処理 | 可視領域の上書き、ステータス/メッセージ行、カーソル移動 |
| 出力（次段へ） | ANSI エスケープを含む端末出力 |
| 観測点（成功判定） | ステータスに mode/row/col が出る、入力のたびに表示が破綻しない |
| 失敗時の切り分け | `--batch` で結果が正しいのに interactive だけ壊れるなら render/term。端末互換性も疑う |

---

## 7. データ構造（何を状態として持っているか）

### 7.1 もっとも重要: カーソルは “表示列” ではなく “バイト位置”

uim のカーソルは
- `cur_row`（行）
- `cur_col_byte`（列: 行内のバイトインデックス）
で表される。

表示時には `utf8` の近似幅で
- “そのバイト位置が画面の何列目か”
を計算してカーソルを置く。

この設計の狙い:
- UTF-8 を壊さずに編集する（削除/挿入の単位をバイトで扱いつつ境界を守る）
- 横スクロールや列維持（上下移動）が実装しやすい

### 7.2 `uim_t`（エディタ状態）の中身

代表的なフィールド:

- `mode`: NORMAL/INSERT/COLON/SEARCH
- `path`: 編集対象ファイルパス
- `buf`: 行配列バッファ
- `row_off/col_off`: スクロール（col_off は “表示列” 単位）
- `yank_lines/yank_n`: ヤンクバッファ（複数行対応）
- `colon[]`, `search[]`, `last_search[]`: 入力バッファ類
- `pending`: 2打鍵コマンドの1打鍵目（`d`/`y`/`g`）
- `num_prefix`: 数値プレフィクス
- `u8buf/u8len/u8need`: INSERT での UTF-8 組み立て
- `buf.modified`: 変更フラグ（`:q` 拒否などに使用）

---

## 8. 入力処理（キー → コマンドの確定）

### 8.1 raw mode と 1キー読み取り

interactive では raw mode に入り、1キーずつ読む。

- `Ctrl-C`（0x03）は終了扱い
- `Backspace` は `0x7f` または `0x08`
- `Enter` は `\r` または `\n`
- `ESC` は `0x1b`

補足: 端末に入力が溜まっている場合（大量ペースト等）は、可能な範囲でまとめて入力を処理してから 1 回だけ描画する。
これにより「大量入力のたびに毎回全画面描画が走って重い」状態を避けている。

### 8.2 ESCシーケンスの最小解釈

対応しているのは主に:
- 矢印（CSI `ESC [ A/B/C/D`）
- Home/End（`ESC [ H/F`、`ESC O H/F`、`ESC [ 1~/4~`、`7~/8~` の一部）

これ以上複雑なキー（PageUp 等）は基本 “ESC 扱い” になる。

### 8.3 NORMAL の 2打鍵コマンドは `pending` で実現

- `d` を押した時点では確定せず `pending='d'`
- 次に `d` が来たら `dd`（数値プレフィクスがあれば `Ndd`）

同様に:
- `y` → `yy`
- `g` → `gg`

この方式は “小さな状態機械” になっているので、挙動が読みやすい。

### 8.4 数値プレフィクスの扱い（上限とリセット）

- 数字キーで `num_prefix` を組み立てる（上限 9999）
- コマンド確定時に `prefix_take_or1()` で “未入力なら1” として回収し、プレフィクスは 0 に戻る
- 不正な2打鍵の時もプレフィクスはリセットされる

---

## 9. 描画（render）: なぜチラつきにくいのか

uim の描画は
- 可視領域を上書きで全再描画
- ただし毎フレームの clear は避ける
という方針。

### 9.1 画面レイアウト（最下2行を予約）

- テキスト領域: `rows-2`
- ステータス行（`rows-1`）: 反転表示、モード・ファイル・変更有無・row/col
- メッセージ行（`rows`）: `:`/`/` 入力中は入力内容、それ以外は status メッセージ

### 9.2 スクロール（縦/横）

- 縦スクロール: `row_off` を調整してカーソル行を可視範囲へ入れる
- 横スクロール: `col_off`（表示列）を調整してカーソル列を可視範囲へ入れる

横スクロールは
- “表示列→バイト位置” 変換で開始位置を切り出す
ため、UTF-8 が混ざっても破綻しにくい。

### 9.3 カーソル位置

- “行内バイト位置 → 表示列” を計算
- `text_r/text_c`（可視領域内の座標）へ変換
- 端末に `ESC [ r ; c H` で移動

---

## 10. よくある症状と切り分け（観測ポイント付き）

起動メカニズム資料と同じ発想で「どの層が壊れているか」を切り分ける。
ここでの層は次:

- 入力層: term（raw/キー解釈）
- 編集層: editor（状態遷移・コマンド確定）
- データ層: buf/utf8（行編集・境界・表示列計算）
- 表示層: render（画面出力）
- 永続層: load/save（ファイルI/O）

### 10.1 画面が壊れる／キーが効かない

- 端末が ANSI を解釈できない、または raw mode が効いていない可能性
- `--batch` で “編集結果だけ” が再現できるなら、render/term 側が怪しい

### 10.2 `:w` が失敗する（write failed）

- 保存先 `path` が無い（起動引数が無い）
- パーミッション、ディレクトリ無し、ディスクフル等

uim は `:w` 失敗でもエディタを続行し、ステータスに `write failed` を出す。

### 10.3 `:q` ができない

- modified が立っている
- まず `:w` → `:q`、または `:wq`
- 捨てるなら `:q!`

### 10.4 検索が見つからない／`n` が効かない

- 検索は前方のみ、ラップ無し
- `n` は “前回の検索が成功して last_search がある場合” だけ
- 末尾まで行ったら `gg` して再検索

### 10.5 日本語を含む行でカーソル列がズレる

- uim の幅計算は近似（非ASCII=2）であり、厳密な East Asian Width ではない
- ただし “極端に破綻しない” のが狙い

---

## 11. まとめ（uim を普段使いするための要点）

- uim は “最小 vi ライク” を確実に動かすためのエディタ
- 日常運用では `:w` をこまめに打つ（undo が無い）
- 仕様の核は「カーソルはバイト位置」「表示列は近似」「検索は前方のみでラップ無し」
- `--batch` は再現と切り分けに強力

必要なら、この文書に「UmuOS 運用寄り（ログ採取や設定編集）の具体例」や「uim の拡張ポイント（次に入れたくなる機能と実装箇所のガイド）」を追記していける。

---

## 付録A. 今後追加したい機能一覧（案）

ここは「uim を普段使いする中で、次に欲しくなる機能」のメモ。
実装の優先度や採用可否は未確定（= 思いつきを含む）。

### A.1 編集（NORMAL/INSERT）

- 行内削除: `x`（1文字削除）
- 単語移動: `w`/`b`（最低限）
- 置換: `r`（1文字置換）
- 先頭挿入: `I` / 末尾挿入: `A`（`0` + `i` / `$` + `a` のショートカット）
- 繰り返し: `.`（直前コマンドの再実行）
- undo/redo（最小でも1段）

### A.2 検索

- ラップアラウンド検索（末尾まで行ったら先頭へ）
- 後方検索: `?` と逆方向: `N`
- 大小区別（`ignorecase` 的な最小オプション）
- 検索ハイライト（最小限）

### A.3 コマンド（COLON）

- `:w <path>`（別名保存）
- `:e <path>`（別ファイルを開く）
- `:set tabstop=<n>`（表示幅のパラメータ化）

### A.4 表示/操作性

- PageUp/PageDown（または Ctrl-F/Ctrl-B 風）
- 画面の差分描画（重い端末での体感改善）
- 文字幅の精度改善（East Asian Width のより厳密な扱い）

### A.5 内部・運用

- `--batch` の拡張（矢印/Home/End の擬似入力、テスト記述の簡略化）
- デバッグログ出力（問題切り分け用の任意有効化）
