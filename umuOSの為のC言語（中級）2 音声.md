---
title: "UmuOSの為のC言語２（中級）　２章。　ファイルあいおー"
---

# UmuOSの為のC言語（中級）。　２。　音声版。

## ２章。　ファイルあいおー。

この章では、Linuxのファイルあいおーの基本を整理します。

LinuxやUnix系OSでは、ファイルあいおーはかなり中心的な仕組みです。
通常ファイルを読む。ログを書く。端末へ表示する。パイプで別プロセスへ渡す。ソケットで通信する。デバイスを操作する。という処理の多くが、
ファイルディスクリプタ。を通して扱われます。

UmuOSの視点では、この章はかなり重要です。
なぜなら、シェル、リダイレクト、パイプ、ログ出力、設定ファイル読み込み、デバイス入出力、などが、最終的には fd、オープン関数、read関数、write関数、close関数。 
の理解に、集約されていくからです。

この章では、まず低レベルあいおー。を中心に見ていきます。
標準Cライブラリの。 ファイル、エフオープン関数、エフゲッツ関数、エフプリントエフ関数。 などは便利ですが、
その下には、ファイルディスクリプタを使う、低レベルあいおーがあります。
UmuOSや、ウーシュの設計を考える場合は、まずこちらを押さえると見通しが良くなります。

### ２章の１。　ファイルあいおーの基本。

ファイルを読み書きするには、まずファイルをひらく必要があります。
Linuxでは、ファイルをひらくとカーネルが、そのファイルをプロセスごとに管理し、ユーザープログラムにはファイルディスクリプタ。という整数を返します。

ファイルディスクリプタは、fd。 と呼ばれることが多いです。
C言語では普通の イントとして扱います。
fd専用の特別な型があるわけではありません。

たとえば、ファイルをひらくと次のような流れになります。

```text
ユーザープログラム。
    オープン関数 を呼ぶ。

カーネル。
    ファイルをひらく。
    プロセスの、エフディー対応表に登録する。

ユーザープログラム。
    fdを受け取る。
    以後 read関数、write関数、close関数 に、 fd を渡す。
```

ここで重要なのは、ユーザープログラムが直接。アイノードや、ディスクブロックを触るわけではない、という点です。
プログラムは fd、 という番号を持ち、その番号を、カーネルに渡すことで、読み書きやクローズを依頼します。

#### ２章の１。の１。　fdと、プロセスのファイルテーブル。

プロセスは、自分用のファイルディスクリプタひょうを持っています。
このひょうには、fd番号と、ひらいている、ファイルに関する情報が、対応付けられます。

イメージとしては、次のようになります。

```text
プロセスAのエフディー対応表。

fd、 ゼロ、  -> 標準入力。
fd、 いち、  -> 標準出力。
fd、 に、 -> 標準エラー出力。
fd、 さん、 -> スラ、ホーム、スラ、たま、すら、めもてきすと。
fd、 よん、  -> パイプの読み取り口。
```

fd、そのものは、単なる整数です。
しかし、その先にはカーネルが管理する。「ひらいているファイル」の情報があります。

そこには、たとえば、次のような情報が関係します。

```text
ファイルの実体。
    アイノードやデバイスなど、対象そのものに関係する情報。

ファイルポジション。
    次にread関数やwrite関数する位置。

アクセスモード。
    読み取り専用、書き込み専用、読み書き両用など。

フラグ。
    おーアペンド、オー・ノンブロック、オー・クローズエグゼック、 など。
```

ファイルポジションは、読み書きのたびに進む、「ファイル内の現在位置」です。
通常ファイルを 、read関数をつかい読み込むと、読んだバイト数ぶんだけ位置が進みます。
write関数、で、書き込み した場合も、書いたバイト数ぶんだけ位置が進みます。

UmuOSで、ファイルあいおーを考える場合も、単に。「ファイルをひらく」だけでは足りません。
少なくとも、プロセスごとの、エフディー対応表、fd番号、ファイルポジション、アクセスモード、対象ファイルへの参照、を管理する必要があります。

#### ２章の１、の２。　おや子プロセスとfd。

Linuxでは、フォーク関数、 で、子プロセスを作ると、子プロセスは、おやプロセスの、fd、を受け継ぎます。

これは、シェルや、パイプを理解するうえで非常に重要です。
たとえば、シェルが パイプ関数 で、fd、を作り、フォーク関数 で、子プロセスを作ると、子プロセスもその、fd、を持った状態で開始できます。
その後。デュープツー関数。 で、標準入力や、標準出力につなぎ替え、エグゼック関数 で、別のコマンドへ置き換えることで、パイプ、や、リダイレクトが、
実現できます。

イメージとしては、次のようになります。

```text
おやプロセス。
    fd、 さん。 -> ファイル.txt。

フォーク関数したあとの、子プロセス。
    fd、 さん。 -> ファイル.txt。
```

ただし、おやと、子は、別々のプロセスです。
片方が fdを、close関数で、閉じても、もう片方の、エフディー対応表から、同じfd番号が、自動的にきえるわけではありません。
一方で、フォーク関数直後は。同じ、「ひらいているファイル」を参照するため、ファイルポジションなどを共有する場合があります。

ここは少し混乱しやすいです。
最初は、次のように考えると分かりやすいです。

```text
エフディー対応表。
    プロセスごとにある。

ひらいているファイルの状態。
    フォーク関数した直後に、おやこ、で、共有されることがある。

close、fd。
    そのプロセスの、エフディー対応表から、fd、を、はずす。
```

スレッドの場合は、同じプロセス内で動くため、基本的に、エフディー対応表を、共有します。
そのため、あるスレッドが close、fd、 すると、同じプロセス内の、別スレッドにも影響します。

#### ２章の１、の３。　標準入力。標準出力。標準エラー出力。

通常、プロセスは、起動時点で少なくとも、3つの、fd、を持っています。

```text
fd、 ゼロ。
    標準入力。
    エスティーディーイン。
    エスティーディーインファイルナンバー。

fd、 いち。
    標準出力。
    エスティーディーアウト。
    エスティーディーアウトファイルナンバー。

fd、 に。
    標準エラー出力。
    エスティーディーエラー。
    エスティーディーエラーファイルナンバー。
```
。。

C言語では、直接、ゼロ、いち、に、 と書くこともできます。
しかし、意味が分かりやすいので、低レベルあいおーでは 、
`エスティーディーインファイルナンバー`。`エスティーディーアウトファイルナンバー`。`エスティーディーエラーファイルナンバー`。
 を使う方が読みやすいです。
これらは 、ヘッダファイルの。ユニストディードットエイチ。 で定義されています。

ここで、標準出力へ文字列を書く処理を、音声用に言葉で追ってみます。。

標準出力へ、直接文字を出す場合、低レベルあいおーでは、プリントエフ関数、ではなく、write関数を使います。。
write関数は、どこへ書くのか。何を書き込むのか。何バイト書き込むのか。という3つの情報を受け取ります。。
標準出力へ書きたい場合、書き込み先の、fd、には、エスティーディーアウトファイルナンバー、を指定します。。
これは、標準出力を表す、ファイルディスクリプタで、中身としては1番に対応します。。
ただし、コードを読む人間にとっては、1と直接書くよりも、エスティーディーアウトファイルナンバー、と書いたほうが、
標準出力へ書いているのだと分かりやすくなります。。

次に、書き込みたい文字列を、用意します。。
たとえば、hello、という文字列に改行を付けて出したい場合。実際に書き込むデータは、h、e、l、l、o、改行、という6バイトになります。。
write関数は、C言語の文字列を、文字列として特別扱いするわけではありません。。
write関数から見ると、それは単なるバイト列です。。
そのため、どこから始まるバイト列を、何バイトぶん書くのかを、プログラム側が、明示する、必要があります。。

ここがプリントエフとの大きな違いです。。
プリントエフは、文字列や書式指定を解釈して、人間向けの表示、を作ってくれます。。
一方、write関数は、指定された、fdへ、指定されたメモリ上のデータを、指定されたバイト数だけ送る。かなり素朴な関数です。。
だからこそ、システムコールやファイルディスクリプタの動きを、観察するときには、write関数、のほうが仕組みを見やすいです。。

ただし、実際のプログラムでは、write関数を、呼び出しただけで終わりにしてはいけません。。
システムプログラミングでは、あいオー・は、失敗するものとして扱うのが基本です。。
標準出力への書き込みであっても、出力先がとじられている。パイプの読み取り側が終了している。端末やファイルでエラーが起きている。
シグナルで処理が割り込まれる。などの理由で、失敗することがあります。。
そのため、write関数の、もどりち、は、必ず確認します。。

write関数の、もどりち、には、エスサイズティーという型、がよく使われます。。
エスサイズティーは、サイズをあらわせる、だけでなく、失敗をあらわす、マイナス1も返せる。符号付きの整数型です。。
成功した場合、write関数は、実際に書き込めたバイト数を返します。。
失敗した場合は、マイナス1を返し、詳しい理由はエルノーに、はいります。。
そのため、もどりち、が、ゼロ、以上なら、少なくともエラーではなく、もどりち、がマイナス1なら、書き込みに失敗したと判断します。。

エラーが起きたときには、ペラーを使うと、エルノーに対応した人間向けのメッセージを表示、できます。。
たとえば、write、という文字列をペラーに渡しておくと、write、の処理で何が失敗したのかを追いやすくなります。。
その後、処理を続けられないなら、イグジットで、異常終了させる、という流れになります。。

ここで大切なのは、write関数は、ファイルディスクリプタ。メモリアドレス。バイト数を使って。OSへかなり直接的に出力を依頼する関数、だということです。。
UmuOSの視点では、標準出力も普通の画面表示ではなく、fd の、いちばん、に結びついた出力先として見るのが重要になります。。
つまり、write関数で標準出力へ文字を出すという処理は、OSが、fdを通して入出力先を管理し、ユーザープログラムが、
そのfdへ、バイト列を流し込む、という仕組みの最小モデルになります。。

#### ２章の１、の４。　fd、が、さすもの

fdが、さししめすものは、通常ファイルだけではありません。
Unix系OSの、「すべてのものはファイルである」、という考え方により、いろいろな対象が、fd、で扱えます。

```text
通常ファイル。
    テキストファイル。バイナリファイル。ログファイルなど。

端末。
    キーボード入力や画面出力。

パイプ。
    プロセス間でバイト列を流す通信路。

ファイフォ。
    ファイルシステム上に名前を持つパイプ。

ソケット。
    プロセス間通信やネットワーク通信。

デバイスファイル。
    ディスク。端末。乱数デバイス、など。
```

この統一感が、Linuxの、強いところです。
UmuOSでも、端末。パイプ。ファイル。デバイス。をできるだけ同じ、fdインターフェース、で扱えるようにすると、シェルやコマンドの設計が、
かなりきれいになります。

### ２章の２。　ファイルのオープン。
。。
ファイルを使う流れは、かなり基本的には、次の形になります。

```text
オープン関数。
    ファイルをひらいて、fd、を取得する。

read関数、 または、 write関数。
    fd。を使って読み書きする。

close関数。
    fd、をとじる。
```

現在のLinuxプログラミングでは、ファイル作成も含めて、 `オープン関数`、 を使うことが多いです。
古いコードでは 、`クリエイト関数` が出てくることもありますが。
概念的には 現在のオープン関数に近いものです。
新しく書くコードでは、意図が分かりやすい、 `オープン関数` 、を使うほうがよいと思います。

#### ２章の２の１　オープン関数

オープン関数 は、パス名で指定したファイルを開き、fd、を返します。

オープン関数を使うときは、まず、ファイルをどう開きたいのかを決めます。
読み取り専用で開きたいのか。
書き込み専用で開きたいのか。
読み書きの両方をしたいのか。
存在しない場合は新しく作りたいのか。
すでにある内容の末尾へ追記したいのか。

このような指定をまとめて、オープン関数に渡します。

オープン関数には、大きく分けて、2つの使い方があります。

1つ目は、すでに存在するファイルをひらく使い方です。
この場合は、開きたいファイルのパスと、開き方を指定します。

2つ目は、ファイルを新しく作る可能性がある使い方です。
この場合は、パスと開き方に加えて、作成したファイルの権限も指定します。

ここでいう権限とは、そのファイルを、だれが読めるのか。だれが書けるのか。だれが実行できるのか。という指定です。
たとえば。ファイルを作るときに、所有者は、読み書きできるけれど、ほかのユーザーは、読めるだけにする、といった指定をします。

オープン関数の、ひきすうには。パス。フラグ。モード。を指定します。モードは。新しくファイルを作成する可能性が、ある時だけです。
パス、 は開きたいファイルのパスです。
フラグ、 には、読み取り用なのか。書き込み用なのか。作成するのか。追記するのか。といった指定を渡します。

ファイルを新しく作る可能性がある場合だけ、さらに 、モード、 を渡します。
モード、 は、作成されるファイルの権限です。パーミッションと言った方が、良いかもしれません。
つまり、オープン関数は、ただファイルをひらくだけでなく、必要なら、どの権限で、ファイルを作るか、まで、決められる関数です。

もどりち、は次のようになります。

```text
成功。
    ゼロ以上が、fd、に、はいる。

失敗。
    fd、に、マイナス1が入り、
    エルノー に理由が入る。
```

次は、ファイルを読み取り専用でひらく、れい、です。

たとえば、ホームディレクトリの中にある、メモ用のテキストファイルを読み取り専用でひらく場面を考えます。

まず、オープン関数に、開きたいファイルの場所を渡します。
ここでは、スラホーム、スラたま、スラ、メモテキスト、のようなパスを渡す、と考えてください。

次に、読み取り専用でひらく、というフラグを渡します。オー・りーどおんりー、などのフラグは、次の項目で、説明しますね。
進みます。
読み取り専用なので、この、fd、を使ってファイルの中身を読むことはできます。
しかし、この、fd、を使って、ファイルへ書き込むことはできません。

オープン関数が成功すると、ゼロ以上のあたいが、fd、に、返ります。
この、fd、は、以後、そのファイルを操作するための番号です。
read関数で読むときも、close関数でとじるときも、このfdを使います。

オープン関数が失敗すると、マイナス1が返ります。
失敗した理由は、エルノーに入ります。
たとえば、指定したパスのファイルが存在しない。
読み取り権限がない。
パスの途中のディレクトリが存在しない。
このような場合に、オープン関数は失敗します。

そのため、オープン関数を呼んだあとは、返ってきた値が0以上か、マイナス1かを必ず確認します。
ゼロ以上なら、ファイルを開けています。
マイナス1なら、ファイルをひらけて、いません。

そして、ファイルを使い終わったら、必ずclose関数、で、fd、をとじます。
fd、をとじる、というのは、カーネルに対して、このファイル操作はもう終わりました、と伝えることです。
ひらいたままにしておくと、プロセスが使っている、fd、が増え続けてしまいます。
小さなサンプルではすぐ問題にならないこともありますが、長く動くプログラムでは重要な要素となります。

古いサンプルでは、存在しないユーザーめいや、架空のパスを使うれいが、出てくることがおおいです。。
このノートでは、UmuOSの研究の文脈に合わせて、 `ホーム、スラたま。めもてきすと` のような分かりやすいパスにしています。
実際に試すときは、自分の環境に存在する、ファイルへ変更お願いします。

#### ２章の２、の２。　オープン関数の基本フラグ。

オープン関数 の 、`flags` には、まず次のどれか1つを指定します。（複数の指定もありますが、今はとりあえず、です。）

```text
オー・リードオンリー。
    読み取り専用でひらく。

オー・ライトオンリー。
    書き込み専用でひらく。

オー・アールディーダブリュー。
    読み書き両用でひらく。
```

読み取り専用でひらいたfdには、基本的に 、write関数は使えないです。また、
書き込み専用でひらいたfdからは、基本的に read関数はつかえないです。

ファイルを作成し、書き込み用にひらく例です。

ここでは、メモどっとテキスト、という名前のファイルを考えます。
やりたいことは、かなりシンプルです。
条件として。メモどっとテキスト を、書き込み専用でひらく。もし存在しなければ、新しく作る。
もし。すでに存在していれば、中身をいったん、削除、してから書き込む。
そして、hello 、という文字列を書いて、最後にfdを、とじる。という流れにします。

まず、オープン関数に、ファイルメイとして、 メモどっとテキスト、 を渡します。
次に、flags として、いくつかの指定を組み合わせます。

1つ目は、オー・ライトオンリーです。
これは、書き込み専用でひらく、という意味です。
このfd、では、write関数で書き込むことはできます。
しかし、read関数で読むことは、基本的にはできません。
ここは、普通に、fdにも性格がある、くらいに見ておくと分かりやすいです。
読み取り用のfdなのか、書き込み用のfdなのか、最初に決めてから使う、という感じです。

2つ目は、オー・クリエイトです。
これは、ファイルが存在しない場合に、新しく作る、という指定です。
もし 、メモどっとテキスト がまだ無ければ、オープン関数が新しく作ってくれます。
逆に、オー・クリエイトを付けていない場合、存在しないファイルをひらこうとすると、失敗します。

3つ目は、オー・ トランケートです。
これは、すでにファイルが存在する場合に、中身を、から、にしてから使う、という指定です。
つまり、古い内容を残さず、最初から書き直したいときに使います。
ここは結構大事で、うっかり付けると、既存ファイルの中身がきえます。
なので、オー・ トランケートは、便利だけど強い指定です。
上書き保存するつもりなのか、追記したいのか、ここは意識したほうがいいです。

4つ目は、オー・クローズエグゼックです。
これは、このあとに出てくる、fd、の継承に関係する指定です。
たとえば、C言語で、別プログラムを起動する処理を書く場合、まず、フォーク関数で、子プロセスを作ります。
この時点では、基本的に、親プロセスが開いていたfdは子プロセスにも引き継がれます。
そのあと、子プロセスの中で、エグゼック関数、を使って、別プログラムを起動します。そのとき。
オー・クローズエグゼックを付けた、fd、は、このエグゼック関数が成功したタイミングで、自動的にクローズされます。
つまり、不要な、fd、が別プログラムへ残らないように、エグゼック関数利用じに、自動でとじるためのフラグです。

このように、flags 、には、書き込み専用でひらく、存在しなければ作る、存在していれば、からにする、
エグゼック関数利用じに、fd、をとじる、という指定をまとめて渡します。
複数のフラグは、ビット演算の、オア、で、組み合わせます。
音声で聞くと分かりにくいかもですが、縦棒のような記号で、フラグを演算して、合体させる、イメージです。
つまり、1つのflagsの中に、複数の命令を詰め込んで渡している感じです。

さらに、オー・クリエイトを使ってファイルを新しく作る可能性があるので、モード も指定します。
要は、パーミッションをあたえます。
ここでは、0644 のような権限を指定します。
0644 は、所有者は読み書きできる。
グループは読み取りだけできる。
その他のユーザも読み取りだけできる。
という意味です。
つまり、新しく作られる、メモどっとテキスト、の基本的なパーミッションを、ここで決めています。
オープン関数が成功すると、ゼロ以上の、fd、のあたいが、返ります。
この、fd、を使って、write関数で 、hello、 改行、というデータを書き込みます。
hello、 改行は、文字としては 、hello、 のあとに、改行が、ついたものです。
バイト数で見ると、h、e、l、l、o、改行、で6バイトになります。

write関数も、必ず成功するとは限りません。
ディスクがいっぱいだったり、fdが不正だったり、途中でエラーが起きたりすると失敗します。
なので、write関数の、もどりちも確認します。
失敗したら、ペラー関数で、理由を表示し、あいていたfdを、close関数で、とじてから終了します。
ここでclose関数を呼ぶのは、途中で失敗しても、ひらいた、fd、を放置しないためになります。

最後に、close関数でfdを閉じます。
close関数も失敗する可能性があるので、本当はここでも、もどりち、を確認したほうがイイです。
ファイル操作は、オープンして。使って。とじる。までがワンセットです。
開けたからOK。書けたからOK。ではなく、最後にとじる、ところまで見るのが、システムプログラミングっぽいところです。

ここでは、 `オー・クローズエグゼック` も、つけています。
これは現代的にはかなり重要なフラグです。
エグゼック関数 で、別プログラムを起動するとき、不要なfdが、子プロセスへ漏れないようにするためです。

ウーシュのようなシェルを作る場合、fd、の継承は便利でもあり危険でもあります。
パイプやリダイレクトに必要な、fd、は意図的に渡し、不要な、fd、は渡さない、という設計が必要になります。

#### ２章の２、の３。　オープン関数で、よく使う追加フラグ。

オープン関数 では、基本のアクセスモードに加えて、ビット演算の、オア、で追加フラグを指定できます。

```text
オー・アペンド。
    常にファイル末尾へ追記する。

オー・クリエイト。
    ファイルがなければ作る。

オー・エクスクルーシブ。
    オー・クリエイト、と一緒に使い、すでに存在したら、失敗させる。

オー・トランク。
    既存ファイルをひらいたときに、サイズをゼロに、切り詰める。

オー・クーズエグゼック。
    エグゼック関数利用じに、fd、を自動的にとじる。

オー・ディレクトリ。
    対象がディレクトリでなければ失敗させる。

オー・ノーフォロー。
    最後の要素がシンボリックリンクなら失敗させる。

オー・ノンブロック。
    可能ならノンブロッキングモードでひらく。

オー・シンク。
    書き込みを同期的に行う。

オー・ダイレクト。
    ページキャッシュを、なるべく回避するダイレクトあいおーよう、に、ひらく。
```

古い説明では、 `オー・ラージファイル` が、大きく扱われることがあります。
しかし、現在の64ビットLinuxでは、大きなファイルを扱うことは通常の前提になっており、`オー・ラージファイル` 、を意識する場面は、かなりへっています。
32ビット環境で大きなファイルをあつかうばあいは、`ファイルオフセットビット64` を、使う話が出てきますが、まずは「現代の64ビットLinuxでは通常あまり意識しない」、と
理解しておけばよいと思います。

また、古い説明では `オー・アシンク` が、紹介されることがあります。
これは、あいおー可能になったときに、シグナルを受け取るための仕組みですが、現在の一般的なイベント駆動あいおーでは、
`ポール関数`、`イーポール関数`、場合によっては 、`アイオー・ユーリング”` 、などを
検討することが多いです。
最初の段階では、`オー・アシンク` は歴史的・特殊用途寄り、として見ておけばよいと思います。

##### ２章の２、の３、の１　オー・クリエイトと、オー・エクスクリ

ここでは、新しいファイルを作るときの注意点を見ます。

まず、オー・クリエイトです。
これは、ファイルが存在しない場合に、新しく作る、という指定です。

ただし、オー・クリエイトだけだと、すでに同じ名前のファイルがある場合、その既存ファイルを開きます。
つまり、「無ければ作る」けれど、「あれば普通に使う」という動きです。

ここで問題になるのは、絶対に新しいファイルだけを作りたい場合です。
たとえば、一時ファイルやロックファイルでは、
すでに同じ名前のファイルがあるなら、それを勝手につかっては、いけない場面があります。

そのときに使うのが、オー・エクスクリ です。
オー・クリエイト と、 オー・エクスクリ 、を一緒に指定すると、
ファイルが存在しない場合だけ、新しく作ります。
もし、すでに存在していたら、オープン関数は失敗します。

つまり、この2つを一緒に使うと、「新規作成に成功したか」、「すでに誰かが作っていたので失敗したか」を、はっきり、わけられます。

##### ２章の２。の３の２。　オー・アペンド。

`オー・アペンド` は、書き込みのたびに、ファイル末尾へ、追記するためのフラグです。

ログファイルのように、複数のプロセスから、追記したい場合に便利です。
単に エルシーク関数 で、末尾へ移動してから write関数 、するよりも、`オー・アペンド` 、を使うほうが安全です。
なぜなら、末尾へ移動する処理と、書き込み処理を、カーネルが一連の操作、として、扱えるからです。

たとえば、アップログ、 というログファイルに、started、 という文字列を追記する場面を考えます。

この場合、オープン関数では、まず書き込み専用を指定します。
次に、ファイルが存在しなければ作るために、オー・クリエイトをつけます。
そして、書き込みのたびに必ず末尾へ追加するために、オー・アペンドを付けます。
さらに、不要なfdが、エグゼック直後の、別のプログラムへ残らないように、オー・クローズエグゼックも付けます。

新しくファイルを作る可能性があるので、パーミッションとして 、0644、 も指定します。
0644 は、所有者は読み書きできる。
グループとその他のユーザは読み取りだけできる。
という、ログファイルではよくある権限のイメージです。

オープン関数が成功すると、fdに、あたいがかえります。
そのfdへ、 write関数 で started、 改行、という文字列を書き込みます。
このとき、オー・アペンドが付いているので、書き込み位置は自分で末尾へ動かさなくてもよいです。
カーネルが、書き込みのたびに、今のファイル末尾へ追記するように扱ってくれます。

ここが大事です。
自分で、エルシーク関数で末尾へ移動してからライトする、というやり方だと、複数プロセスが同時にログを書いたときに、タイミングがぶつかる可能性があります。
あるプロセスが末尾を確認した直後に、別のプロセスが先に書いてしまう、ということがありえるからです。
オー・アペンドを使うと、末尾へ追記する、という動きをカーネル側に任せられるので、ログのような追記用途ではかなり素直です。

もちろん、オープン関数も write関数 も失敗することがあります。
オープンできなければ、fdは使えません。
write関数が失敗した場合も、エラーを確認して、必要ならfdをとじて終了します。
最後は close関数 でfdをとじます。

UmuOSでログ機能を考える場合も、追記モードはかなり大事です。
単純に「最後へ移動して書く」だけでは、複数プロセスが同時に書いたときに競合する可能性があります。

##### ２章の２、の３の３。　オー・トランク。

`オー・トランク` は、既存ファイルをひらいたときに中身をからにするフラグです。

たとえば、メモどっとテキストを、書き込み専用でひらく場面を考えます。
ここで、オー・ライトオンリー、オー・クリエイト、オー・トランク、オー・クローズエグゼック、を組み合わせるとします。

この指定では、メモどっとテキストが存在していれば、まずファイルサイズをゼロにします。
つまり、中身をからっぽにしてから、書き込み用にひらきます。

一方で、メモどっとテキストが存在しない場合は、オー・クリエイトがあるので新しく作ります。
新しく作る可能性があるので、ここでも 0644 のようなパーミッションを指定します。

オー・トランクは、ひらくと同時に白紙に戻す指定です。
普通に書き込み用でひらく、とは少し違います。
ひらいた瞬間に、前の内容をけすので、かなり強いフラグです。

注意点として、`オー・トランク` は破壊的です。
既存の内容がきえます。
設定ファイルや重要なデータをあつかうばあいは、あんいに使わないほうがよいです。

##### ２章の２、の３の４。　オー・ノンブロック

`オー・ノンブロック` は、可能なら、あいおーで待たないようにするフラグです。

通常ファイルではあまり意識しません。
一方で、ファイフォ、ソケット、端末、などでは重要になります。
データがくるまで read関数 が待ち続ける、接続できるまで待つ、といった動作をさけたい場合に使います。

ウーシュのようなシェルの基本実装では、最初から `オー・ノンブロック` を多用する必要はありません。
ただし、将来的にジョブ制御、イベントループ、非同期あいおーをあつかうばあいには重要になります。

`オー・ノンブロック` は、`オープン関数` の時点で指定することもできます。

たとえば、サム_ファイフォ という名前付きパイプを読み取り専用でひらくとします。
ここで、オー・リードオンリー、オー・ノンブロック、オー・クローズエグゼック、を組み合わせます。

この場合、読み取り用にひらくけれど、データがくるまでずっと待ち続ける動きはさけたい、という意味になります。
つまり、読めるデータが今なければ、待たずに戻ってきてほしい、という指定です。

また、すでにひらいているファイルディスクリプタに対して、あとから `エフコントロール関数` で設定することもできます。
次の例では、パイプの読み取り側をノンブロッキングにしています。
パイプにまだデータがない場合でも、`read関数` は待ち続けず、エラーとして戻ります。

流れとしては、まずパイプ関数で、読み取り側と書き込み側のfdを作ります。
パイプでは、読み取り側のfdと、書き込み側のfdが、2つセットでできます。
読み取り側は、パイプからデータを読むためのfdです。
書き込み側は、パイプへデータを書くためのfdです。

次に、エフコントロール関数で、読み取り側fdに現在設定されているフラグを取り出します。
ここでいきなり新しいフラグだけを設定するのではなく、まず現在の設定を読むのが大事です。
なぜなら、すでに付いているフラグを消してしまわないためです。

現在のフラグを取り出したら、それに オー・ノンブロック を足して、もう一度 エフコントロール関数で設定します。
つまり、既存の設定は残したまま、ノンブロックだけを追加する、という考え方です。

そのあと、読み取り側fdに対して read関数 を呼びます。
ここで、パイプにまだデータが無い場合、普通なら read関数 はデータがくるまで待ちます。
しかし、オー・ノンブロックを付けているので、待ち続けません。
今は読めません、という意味のエラーで戻ります。

このときに代表的に使われるエラーが、イーアゲイン です。
意味としては、今はデータがないので、あとでもう一度試してください、という感じです。
環境によっては イー・ウッドブロック という名前も出てきます。
なので、移植性を意識する場合は、イーアゲイン と イー・ウッドブロック の両方を見る書き方がよく使われます。

もし read関数 のもどりちがゼロなら、それはデータがない、ではなく、エンドオブファイルです。
パイプの場合は、書き込み側がとじられていて、もうこれ以上データがこない、という意味になります。
この違いは、かなり大事です。

つまり、オー・ノンブロックでは、「データがまだ無い」と「もう終わり」は別物です。
データがまだ無い場合は、イーアゲイン や イー・ウッドブロック のようなエラーになります。
もう終わりの場合は、read関数 がゼロを返します。
ここをごっちゃにすると、パイプやソケットの処理でかなり混乱します。

この例では、書き込み側 `パイプfdいち` をひらいたままにしている点が重要です。
もし書き込み側を先にとじると、読み取り側の `read関数` は「データ待ち」ではなくエンドオブファイルとして ゼロ を返します。

ここまでをまとめると、オー・ノンブロックは、待たずに戻るための指定です。
ただし、待たずに戻った結果が、イーアゲイン なのか、エンドオブファイル のゼロなのかは、きちんと分けて見ます。
ここを分けられると、ファイフォ、ソケット、端末、パイプの動きがかなり見えやすくなります。

##### ２章の２の３の５　オー・シンクとO_DIRECT

`オー・シンク` は、書き込みをディスクへ同期させたい場合に使います。
通常、write関数 が成功しても、その時点で物理ディスクへ完全に書き込まれているとは限りません。
カーネルやファイルシステムのキャッシュに入っている場合があります。

`オー・シンク` を使うと安全性は上がりますが、性能は落ちやすいです。
ログ、データベース、ジャーナリングなど、データ消失をどこまで許容するかによって判断する領域です。

ここでいう「データ消失をどこまで許容するか」とは、プログラムが `write関数` に成功したあと、すぐに
電源断やカーネルパニックが起きた場合に、そのデータが失われてもよいかどうか、という意味です。
通常の `write関数` は、ユーザープログラムからカーネルへデータを渡す処理です。
しかし、その時点で必ずディスク本体へ書き込まれているとは限りません。
いったんページキャッシュに置かれ、あとでまとめてディスクへ書き出されることがあります。

そのため、普通のファイル書き込みでは高速に動きますが、突然電源が落ちた場合、
直前に `write関数` した内容が残らない可能性があります。

一方、`オー・シンク` を使うと、`write関数` が戻るまでにディスクへの反映も強く意識されます。
そのぶん「書いたはずのデータがきえる」可能性は下がりますが、毎回ディスク待ちが発生しやすくなるため、性能は落ちます。
たとえば、次のように考えると分かりやすいです。

- 一時ファイル  
  きえても作り直せるなら、通常の書き込みで十分なことが多いです。

- 通常のログ  
  最後の数行が失われても許容できるなら、毎回 `オー・シンク` にしない方が性能面では有利です。

- 課金記録、取引記録、データベースの重要な更新  
  `write関数` 成功後に失われると困るため、`オー・シンク` や `fsync関数` のような同期処理を検討する領域になります。

つまり `オー・シンク` は、「常に付ければよい安全フラグ」ではありません。
安全性と性能の交換条件を理解したうえで、失うと困るデータに対して使うものです。
なお、`オー・シンク` はファイルをひらく時点で同期書き込みにする方法です。
一方で、必要なタイミングだけ `fsync(fd)` を呼んでディスクへ反映させる方法もあります。
実際のアプリケーションでは、すべての書き込みを同期するのではなく、「ここまで保存できれば復旧できる」という区切りで
`fsync関数` する設計もよく使われます。

`O_DIRECT` は、ページキャッシュをなるべく使わずにあいおーするためのフラグです。
ただし、バッファのアラインメントやサイズ制約があり、初心者向けではありません。
通常のアプリケーションでは、まず普通の read関数、write関数、必要に応じて fsync関数 を理解する方が先です。

ページキャッシュとは、ファイルの内容をカーネルがメモリ上に一時保存しておく仕組みです。
通常の `read関数` や `write関数` は、このページキャッシュを利用することで高速になります。
同じファイルを何度も読む場合は、毎回ディスクへアクセスせず、メモリ上のキャッシュから返せるためです。

`O_DIRECT` は、このページキャッシュをなるべく使わず、アプリケーションのバッファとディスクの間で直接あいおーしようとする指定です。
巨大なファイルを扱うデータベースなど、自前でキャッシュ管理をしたいプログラムで使われることがあります。

ただし、`O_DIRECT` は制約が強いです。
バッファのアドレス、読み書きするサイズ、ファイル上のオフセットなどが、一定の境界に揃っていないと `EINVAL` で
失敗することがあります。
そのため、普通の `char buf[4096];` のようなバッファではなく、`posix_memalign関数` などで
アラインメントを意識したメモリ確保が必要になる場合があります。

つまり、`O_DIRECT` は初心者が性能改善のために気軽に付けるフラグではありません。
通常のアプリケーションでは、カーネルのページキャッシュに任せた方が単純で速いことが多いです。

#### ２章の２の４　新規作成したファイルのオーナ

`オープン関数` に `オー・クリエイト` を付けて新しいファイルを作ると、そのファイルにはオーナとグループが設定されます。

まず、オーナは分かりやすいです。
新規作成したファイルのオーナは、そのファイルを作成したプロセスの実効ユーザーIDになります。

```text
プロセスの実効uid
    新しく作られたファイルのオーナになる
```

ここで出てくる実効uidは、effective user ID のことです。
普段の単純なプログラムでは、ログインしているユーザーのuidと同じだと思ってよいです。
ただし、setuidプログラムなどでは、実ユーザーIDと実効ユーザーIDが異なる場合があります。
パーミッション判定では、この実効uidが重要になります。

一方で、ファイルのグループは少しだけ複雑です。
Linuxの標準的な動作では、新規作成したファイルのグループは、そのファイルを作成したプロセスの実効グループIDになります。

```text
Linuxの標準的な動作
    プロセスの実効gid
        -> 新しく作られたファイルのグループになる
```

これはSystem V系の動作に合わせたものです。
Linuxは多くの部分でSystem V系Unixの流れを受け継いでいるため、この動作が標準として扱われています。

ただし、BSD系Unixでは考え方が違います。
BSD系では、新しく作られたファイルのグループは、おやディレクトリのグループIDを受け継ぎます。

```text
BSD系の動作
    おやディレクトリのgid
        -> 新しく作られたファイルのグループになる
```

Linuxでも、おやディレクトリに setgid ビットが立っている場合は、BSD系に近い動作になります。
つまり、そのディレクトリの中に作られたファイルやディレクトリは、おやディレクトリのグループを受け継ぎます。

これは、共同作業用ディレクトリでよく使われます。
たとえば、複数ユーザーが同じグループで作業するディレクトリでは、作成者ごとの主グループではなく、作業ディレクトリのグループを
引き継いでほしいことがあります。

```text
共同作業ディレクトリ
    group: dev
    setgidあり

その中で新規作成したファイル
    group: dev
```

UmuOSの初期段階では、ここまで複雑なグループ継承をすぐに実装する必要はないかもしれません。
まずは「新規作成ファイルのオーナは作成プロセスの実効uid」「グループは実効gidを使うのが基本」と押さえれば十分です。
そのうえで、将来的にマルチユーザーOSとして育てるなら、おやディレクトリのsetgidによるグループ継承を設計候補に入れるとよいです。

#### ２章の２の５　新規作成したファイルのパーミッション

`オープン関数` は、使い方によって第3引数 `mode` を取ります。
この `mode` は、新しく作るファイルのパーミッションを指定するための値です。

```c
int オープン(const char *path, int flags);
int オープン(const char *path, int flags, mode_t mode);
```

ここで大事なのは、`mode` が必要になるのは `オー・クリエイト` を指定したときだけ、という点です。
すでに存在するファイルをひらくだけなら、`mode` は使われません。

```text
オー・クリエイト なし
    mode は不要

オー・クリエイト あり
    mode が必要
```

`オー・クリエイト` を指定したのに `mode` を渡し忘れると、C言語としてもシステムプログラミングとしても危険です。
関数呼び出しの形としてはコンパイルできてしまう場合がありますが、オープン関数 側は第3引数があるものとして値を読もうとするため、
意図しないパーミッションでファイルが作られる可能性があります。

そのため、`オー・クリエイト` を使うときは必ず第3引数を書く、と覚えておくのが安全です。

```c
fd = オープン("メモどっとテキスト", オー・ライトオンリー | オー・クリエイト | オー・トランク | オー・クローズエグゼック, 0644);
```

`0644` は8進数です。
C言語では、先頭に `0` が付いた整数リテラルは8進数として扱われます。
そのため、`0644` は10進数の644ではなく、Unixのパーミッションひょう記としての `rw-r--r--` をひょうします。

```text
0644
    オーナ: 読み取り + 書き込み
    グループ: 読み取りのみ
    その他: 読み取りのみ

0600
    オーナ: 読み取り + 書き込み
    グループ: 権限なし
    その他: 権限なし

0755
    オーナ: 読み取り + 書き込み + 実行
    グループ: 読み取り + 実行
    その他: 読み取り + 実行
```

数値で書く代わりに、POSIXで定義されている定数をビットORで組み合わせることもできます。
定数を使うと、どの権限を与えているのかが読みやすくなります。

```text
S_IRWXU
    オーナに対する読み取り、書き込み、実行許可

S_IRUSR
    オーナに対する読み取り許可

S_IWUSR
    オーナに対する書き込み許可

S_IXUSR
    オーナに対する実行許可

S_IRWXG
    グループに対する読み取り、書き込み、実行許可

S_IRGRP
    グループに対する読み取り許可

S_IWGRP
    グループに対する書き込み許可

S_IXGRP
    グループに対する実行許可

S_IRWXO
    その他のユーザーに対する読み取り、書き込み、実行許可

S_IROTH
    その他のユーザーに対する読み取り許可

S_IWOTH
    その他のユーザーに対する書き込み許可

S_IXOTH
    その他のユーザーに対する実行許可
```

たとえば、`0644` 相当を定数で書くと次のようになります。

```c
S_IRUSR | S_IWUSR | S_IRGRP | S_IROTH
```

これは次の意味です。

```text
S_IRUSR
    オーナに読み取りを許可

S_IWUSR
    オーナに書き込みを許可

S_IRGRP
    グループに読み取りを許可

S_IROTH
    その他のユーザーに読み取りを許可
```

ファイル作成の例です。

```c
#include <fcntl.h>
#include <エスティーディーio.h>
#include <エスティーディーlib.h>
#include <sys/stat.h>
#include ユニストディードットエイチ。

int main(void) {
    int fd;
    mode_t mode;

    mode = S_IRUSR | S_IWUSR | S_IRGRP | S_IROTH;

    fd = オープン("メモどっとテキスト", オー・ライトオンリー | オー・クリエイト | オー・トランク | オー・クローズエグゼック, mode);

    if (fd < 0) {
        ペラー("オープン");
        イグジット(1);
    }

    if (close(fd) < 0) {
        ペラー("close");
        イグジット(1);
    }

    return 0;
}
```

この例では `mode` として `0644` 相当を渡しています。
ただし、実際にディスク上へ作られるファイルのパーミッションは、必ずしも `mode` そのままではありません。
ここで `umask` が関係します。

##### ２章の２の５の１　umask

`umask` は、ファイル作成時に「落とす権限」を指定する仕組みです。
正式には ファイル creation mask と呼ばれます。

新規作成されるファイルの最終的なパーミッションは、だいたい次の式で決まります。

```text
実際のパーミッション = mode & ~umask
```

たとえば、プログラムが `0666` を指定し、umask が `0022` だったとします。

```text
mode
    0666

umask
    0022

実際のパーミッション
    0666 & ~0022
    0644
```

つまり、プログラムが「みんな読み書きできるファイル」を作ろうとしても、umask がグループとその他ユーザーの書き込み権限を落とすため、
実際には `0644` になります。

よくあるumaskは `0022` です。
この場合、普通のファイルは `0644`、ディレクトリは `0755` になりやすいです。

```text
ファイル作成時によく使う指定
    mode: 0666
    umask: 0022
    result: 0644

ディレクトリ作成時によく使う指定
    mode: 0777
    umask: 0022
    result: 0755
```

なぜファイル作成で `0666` が多いかというと、通常ファイルには実行ビットを最初から付けないのが普通だからです。
実行ファイルやスクリプトとして使う場合は、必要に応じて `chmod` などで実行権限を付けます。

UmuOSの設計でも、umaskはユーザー空間の安全装置として考えると分かりやすいです。
カーネルやファイルシステムは `mode` をそのまま使うのではなく、プロセスが持つumaskで権限を削ってから最終的なパーミッションを決めます。

##### ２章の２の５の２　modeとアクセスモードは別物

ここで混乱しやすい点があります。
`オープン関数` の `flags` に指定する `オー・リードオンリー`、`オー・ライトオンリー`、`オー・アールディーダブリュー` と、第3引数の `mode` は別物です。

```text
flags のアクセスモード
    今回ひらいたfdで、読み取りできるか、書き込みできるかを決める

mode のパーミッション
    新しく作るファイルに、誰が読み書き実行できるかを決める
```

たとえば、次のような指定も形としては可能です。

```c
fd = オープン("メモどっとテキスト", オー・ライトオンリー | オー・クリエイト | オー・トランク | オー・クローズエグゼック, 0444);
```

これは少し変に見えます。
`オー・ライトオンリー` で書き込み用にひらいているのに、作成するファイルのパーミッションは `0444`、つまり読み取り専用だからです。

しかし、これは矛盾ではありません。
`オー・ライトオンリー` は「今回返されるfdで書けるか」を決めます。
`0444` は「作成されたファイルが、その後どのようなパーミッションを持つか」を決めます。

この違いはUmuOSでも重要です。
fdのアクセスモードと、ファイルシステム上のパーミッションは、同じものではありません。

#### ２章の２の６　creat関数システムコール

古いUnix系のサンプルでは、`creat関数` というシステムコールが出てきます。
名前は `create` ではなく `creat` です。
最後の `e` がありません。

`creat関数` は、次の組み合わせを短く書くための関数です。

```text
オー・ライトオンリー | オー・クリエイト | オー・トランク
```

宣言は次のようになります。

```c
#include <fcntl.h>
#include <sys/stat.h>
#include <sys/types.h>

int creat(const char *path, mode_t mode);
```

たとえば、次のコードは `ファイル` で指定されたファイルを書き込み用に作成または切り詰めます。

```c
int fd;

fd = creat(ファイル, 0644);

if (fd < 0) {
    /* error */
}
```

これは、次の `オープン関数` とほぼ同じ意味です。

```c
int fd;

fd = オープン(ファイル, オー・ライトオンリー | オー・クリエイト | オー・トランク, 0644);

if (fd < 0) {
    /* error */
}
```

さらに、考え方としてはユーザー空間でも次のように書けます。

```c
int creat(const char *path, mode_t mode) {
    return オープン(path, オー・ライトオンリー | オー・クリエイト | オー・トランク, mode);
}
```

ただし、現代のコードでは、基本的には `オープン関数` を使う方が分かりやすいです。
なぜなら、`オープン関数` の方が指定しているフラグが明示されるからです。

特に今のLinuxプログラムでは、`オー・クローズエグゼック` を付けたい場面が多くあります。
`creat関数` では `オー・クローズエグゼック` を同時に指定できません。
そのため、fd漏れをさけたい現代的なコードでは、次のように `オープン関数` を使う方が実用的です。

```c
fd = オープン(ファイル, オー・ライトオンリー | オー・クリエイト | オー・トランク | オー・クローズエグゼック, 0644);
```

`creat関数` は歴史的な互換性のために残っているAPI、と考えるとよいです。
古い本や古いコードを読むためには知っておく価値があります。
しかし、UmuOSやウーシュの新しいサンプルを書くなら、基本は `オープン関数` に寄せるのが読みやすいと思います。

#### ２章の２の７　オープン関数 と creat関数 のもどりち、と エルノー

`オープン関数` と `creat関数` は、成功するとファイルディスクリプタを返します。
失敗すると `-1` を返し、失敗理由を `エルノー` に設定します。

```text
成功
    0以上のfdを返す

失敗
    -1を返す
    エルノー に失敗理由が入る
```

ここで注意したいのは、正常なfdとして `0` が返る可能性があることです。
標準入力がとじられている状態で `オープン関数` すると、空いている最小のfdとして `0` が返ることがあります。
そのため、エラー判定は `fd == -1` または `fd < 0` で行います。

```c
int fd;

fd = オープン("メモどっとテキスト", オー・リードオンリー | オー・クローズエグゼック);

if (fd < 0) {
    ペラー("オープン");
    イグジット(1);
}
```

次のような判定はよくありません。

```c
if (!fd) {
    /* wrong */
}
```

なぜなら、`fd` が `0` のとき、本当は成功しているのに失敗扱いしてしまうからです。

`エルノー` は、直前のシステムコールやライブラリ関数が失敗した理由をひょうすための変数です。
ただし、成功したときに `エルノー` が自動的に0へ戻るとは限りません。
そのため、`エルノー` は「もどりち、が失敗を示したときだけ読む」と考えるのが基本です。

```text
正しい考え方
    もどりち、を見る
    失敗だった場合だけ エルノー を見る

さけたい考え方
    エルノー だけを見て成功・失敗を判断する
```

オープン関数 の失敗理由としては、たとえば次のようなものがあります。

```text
ENOENT
    指定したファイルや途中のディレクトリが存在しない

EACCES
    パーミッションがなく、開けない

EEXIST
    オー・クリエイト | オー・エクスクリ 指定時に、ファイルがすでに存在した

EISDIR
    ディレクトリを、通常ファイルのように書き込み用で開こうとした

EMファイル
    そのプロセスで開けるfd数の上限に達した

ENファイル
    システム全体で開けるファイル数の上限に達した
```

実用上は、まず `ペラー関数` でエラー理由を表示できるようにしておくと分かりやすいです。

```c
#include <fcntl.h>
#include <エスティーディーio.h>
#include <エスティーディーlib.h>
#include ユニストディードットエイチ。

int main(void) {
    int fd;

    fd = オープン("メモどっとテキスト", オー・リードオンリー | オー・クローズエグゼック);

    if (fd < 0) {
        ペラー("オープン メモどっとテキスト");
        イグジット(1);
    }

    if (close(fd) < 0) {
        ペラー("close");
        イグジット(1);
    }

    return 0;
}
```

UmuOSの設計でも、システムコールは単に「成功・失敗」を返すだけでは足りません。
失敗したときに、ユーザープログラムが原因を区別できる必要があります。
Linuxの `エルノー` は、そのための代ひょう的な設計です。

### ２章の３　ファイル読み取り: read関数

ファイルをひらいてfdを得たら、次は読み取りです。
Linuxで低レベルにファイルを読み取る基本のシステムコールが `read関数` です。

```c
#include ユニストディードットエイチ。

エスサイズティー read(int fd, void *buf, size_t count);
```

意味は次の通りです。

```text
fd
    読み取り対象のファイルディスクリプタ

buf
    読み取ったデータを入れるバッファ

count
    最大で何バイト読み取るか

もどりち、
    実際に読み取ったバイト数
    エンドオブファイルなら0
    エラーなら-1
```

`read関数` は、fdがさししめす対象から最大 `count` バイトを読み取り、`buf` にコピーします。
通常ファイルの場合は、現在のファイルポジションから読み取り、読み取ったバイト数ぶんファイルポジションが進みます。

```text
read(fd, buf, 100)
    現在位置から最大100バイト読む
    実際に読めたバイト数を返す
    読めた分だけファイルポジションが進む
```

ただし、端末、パイプ、ソケット、キャラクタデバイスなどは、通常ファイルのようなシーク可能なファイルポジションを持たない場合があります。
その場合でも、`read関数` は「今読める入力ストリームから読む」という形で使えます。

#### ２章の３の１　read関数 の最小例

まずは、ファイルから読み取って標準出力へ表示する小さな例です。

```c
#include <fcntl.h>
#include <エスティーディーio.h>
#include <エスティーディーlib.h>
#include ユニストディードットエイチ。

int main(void) {
    char buf[128];
    int fd;
    エスサイズティー n;

    fd = オープン("メモどっとテキスト", オー・リードオンリー | オー・クローズエグゼック);

    if (fd < 0) {
        ペラー("オープン");
        イグジット(1);
    }

    n = read(fd, buf, sizエンドオブファイル(buf));

    if (n < 0) {
        ペラー("read");
        close(fd);
        イグジット(1);
    }

    if (n > 0) {
        if (write(エスティーディーアウトファイルナンバー, buf, n) < 0) {
            ペラー("write");
            close(fd);
            イグジット(1);
        }
    }

    if (close(fd) < 0) {
        ペラー("close");
        イグジット(1);
    }

    return 0;
}
```

この例では、最大128バイトを読み取ります。
重要なのは、`read関数` のもどりち、 `n` をそのまま `write関数` のバイト数に使っている点です。
`buf` 全体を必ず128バイト読み取れるとは限りません。

#### ２章の３の２　read関数 のもどりち、

`read関数` のもどりち、は、かなり重要です。
もどりち、の意味を雑に扱うと、ファイルあいおーのバグがすぐに出ます。

```text
もどりち、 == count
    要求したバイト数をすべて読み取れた

0 < もどりち、 < count
    一部だけ読み取れた
    エラーとは限らない

もどりち、 == 0
    エンドオブファイル
    つまり、これ以上読み取るデータがない

もどりち、 == -1
    エラー
    エルノー に理由が入る
```

ここで特に大事なのは、`read関数` が `count` より小さい正の値を返しても異常とは限らないことです。
たとえば、次のような場合に部分読み取りが起きます。

```text
ファイルの残りが少ない
    100バイト要求したが、残りが30バイトしかなかった

パイプやソケットから読んだ
    今届いている分だけ読めた

端末から読んだ
    ユーザーが入力した1行分だけ読めた

シグナルに割り込まれた
    一部だけ読めたところで処理が戻った
```

つまり、次のようなコードは危険です。

```c
char buf[100];
エスサイズティー n;

n = read(fd, buf, sizエンドオブファイル(buf));

if (n < 0) {
    ペラー("read");
    イグジット(1);
}

/* 危険: 100バイト全部読めたとは限らない */
```

`read関数` が成功した場合でも、実際に読めたバイト数は必ずもどりち、で確認します。

#### ２章の３の３　エンドオブファイルと「まだデータがない」は違う

`read関数` が `0` を返した場合は エンドオブファイル です。
エンドオブファイル は end of ファイル の略で、ファイル終端を意味します。

通常ファイルでは、ファイルの末尾まで読み終わったときに `read関数` が `0` を返します。

```text
read関数 が 0 を返す
    エンドオブファイル
    読み取るデータがもうない
    エラーではない
```

一方で、パイプ、ソケット、端末などでは、「今はまだデータが届いていない」という状態があります。
ブロッキングモードでは、`read関数` はデータがくるまで待ちます。

```text
ブロッキングread関数
    データがある
        読んで返る

    データがまだない
        待つ

    エンドオブファイル
        0を返す
```

つまり、「データがまだない」と「エンドオブファイル」は違います。
エンドオブファイルは終わりです。
データがまだない状態は、将来データがくるかもしれないので、ブロッキングread関数では待ちます。

#### ２章の３の４　EINTR

`read関数` は、シグナルによって中断されることがあります。
1バイトも読み取っていない状態でシグナルに割り込まれた場合、`read関数` は `-1` を返し、`エルノー` に `EINTR` を設定することがあります。

```text
EINTR
    interrupted system call
    システムコールがシグナルで中断された
```

この場合、多くの読み取り処理では、もう一度 `read関数` をやり直せます。

```c
エスサイズティー n;

for (;;) {
    n = read(fd, buf, sizエンドオブファイル(buf));

    if (n < 0 && エルノー == EINTR) {
        continue;
    }

    break;
}
```

ただし、すべてのプログラムで機械的にやり直せばよいわけではありません。
シグナルを受けたら終了したいプログラムもあります。
まずは「EINTRは、重大なあいおー失敗というより、割り込みによる中断をひょうすことがある」と理解するとよいです。

#### ２章の３の５　指定したバイト数を最後まで読み取る

`read(fd, buf, len)` は、`len` バイトを必ず読み取る関数ではありません。
最大で `len` バイト読む関数です。

そのため、「どうしても `len` バイト分ほしい」という処理では、ループが必要です。
たとえば、固定長ヘッダ、構造体、プロトコルの決まった長さのデータを読むときです。

次の関数は、エンドオブファイルまたはエラーになるまで、指定されたバイト数をできるだけ読み取ります。

```c
#include <エルノー.h>
#include <エスティーディーdef.h>
#include ユニストディードットエイチ。

エスサイズティー read_full(int fd, void *buf, size_t len) {
    char *p;
    size_t left;
    エスサイズティー n;

    p = buf;
    left = len;

    while (left > 0) {
        n = read(fd, p, left);

        if (n < 0) {
            if (エルノー == EINTR) {
                continue;
            }

            return -1;
        }

        if (n == 0) {
            break;
        }

        p += n;
        left -= n;
    }

    return (エスサイズティー)(len - left);
}
```

この関数のポイントは、`p` と `left` を更新しているところです。

```text
p
    次に読み込む先の位置

left
    まだ読み取りたい残りバイト数
```

たとえば、100バイト読みたいのに30バイトしか読めなかった場合、次は `buf + 30` の位置へ、残り70バイトを読み取ります。

```text
最初
    p = buf
    left = 100

30バイト読めた後
    p = buf + 30
    left = 70
```

Cでは `void *` のままではバイト単位のポインタ演算をしにくいため、`char *` に変換しています。
`char` は1バイト単位で進むため、読み取りバッファを扱うときに便利です。

使い方は次のようになります。

```c
#include <fcntl.h>
#include <エスティーディーio.h>
#include <エスティーディーlib.h>
#include ユニストディードットエイチ。

int main(void) {
    char header[16];
    int fd;
    エスサイズティー n;

    fd = オープン("メモどっとテキスト", オー・リードオンリー | オー・クローズエグゼック);

    if (fd < 0) {
        ペラー("オープン");
        イグジット(1);
    }

    n = read_full(fd, header, sizエンドオブファイル(header));

    if (n < 0) {
        ペラー("read_full");
        close(fd);
        イグジット(1);
    }

    if ((size_t)n != sizエンドオブファイル(header)) {
        エフプリントエフ(エスティーディーエラー, "short read: expected %zu bytes, got %zd bytes\n",
                sizエンドオブファイル(header), n);
        close(fd);
        イグジット(1);
    }

    close(fd);

    return 0;
}
```

この例では、16バイトのヘッダを必ず読みたい、という想定です。
エンドオブファイルで16バイト未満しか読めなかった場合は、短い読み取りとして扱っています。

#### ２章の３の６　ファイル全体を読む基本ループ

通常ファイルを先頭から最後まで読むなら、「固定バイト数を必ず読む」よりも、「読めるだけ読み、エンドオブファイルまで繰り返す」形がよく使われます。

次は、`メモどっとテキスト` の内容を標準出力へ流す簡単な `cat` 風の例です。

```c
#include <エルノー.h>
#include <fcntl.h>
#include <エスティーディーio.h>
#include <エスティーディーlib.h>
#include ユニストディードットエイチ。

int main(void) {
    char buf[4096];
    int fd;
    エスサイズティー n;

    fd = オープン("メモどっとテキスト", オー・リードオンリー | オー・クローズエグゼック);

    if (fd < 0) {
        ペラー("オープン");
        イグジット(1);
    }

    for (;;) {
        n = read(fd, buf, sizエンドオブファイル(buf));

        if (n < 0) {
            if (エルノー == EINTR) {
                continue;
            }

            ペラー("read");
            close(fd);
            イグジット(1);
        }

        if (n == 0) {
            break;
        }

        if (write(エスティーディーアウトファイルナンバー, buf, n) < 0) {
            ペラー("write");
            close(fd);
            イグジット(1);
        }
    }

    if (close(fd) < 0) {
        ペラー("close");
        イグジット(1);
    }

    return 0;
}
```

このコードでは、`read関数` が0を返したらエンドオブファイルなのでループを抜けます。
`read関数` が正の値を返したら、そのバイト数だけ標準出力へ書き出します。

ただし、この例の `write関数` も本当は部分書き込みを考える必要があります。
読み取りと同じく、書き込みも「要求したバイト数を必ず全部書ける」とは限りません。
この点は、後の `write関数` の節で詳しく扱います。

#### ２章の３の７　ノンブロッキングあいおーと イーアゲイン

`オー・ノンブロック` を付けてfdをひらくと、対象によってはノンブロッキングあいおーになります。
ノンブロッキングあいおーでは、データがまだないときに `read関数` が待たずに戻ります。

```text
ブロッキングあいおー
    データがなければ待つ

ノンブロッキングあいおー
    データがなければ待たずに戻る
```

このとき `read関数` は `-1` を返し、`エルノー` に `イーアゲイン` または `イー・ウッドブロック` を設定することがあります。
Linuxでは、この2つは同じ値であることが多いですが、移植性を考えるなら両方を確認するのが無難です。

```text
イーアゲイン / イー・ウッドブロック
    今は読み取れるデータがない
    後でもう一度試せる
    重大なあいおーエラーとは限らない
```

簡単な例です。

```c
#include <エルノー.h>
#include <fcntl.h>
#include <エスティーディーio.h>
#include <エスティーディーlib.h>
#include ユニストディードットエイチ。

int main(void) {
    char buf[1024];
    int fd;
    エスサイズティー n;

    fd = オープン("メモどっとテキスト", オー・リードオンリー | オー・ノンブロック | オー・クローズエグゼック);

    if (fd < 0) {
        ペラー("オープン");
        イグジット(1);
    }

    n = read(fd, buf, sizエンドオブファイル(buf));

    if (n < 0) {
        if (エルノー == EINTR) {
            エフプリントエフ(エスティーディーエラー, "read was interrupted; try again\n");
        } else if (エルノー == イーアゲイン || エルノー == イー・ウッドブロック) {
            エフプリントエフ(エスティーディーエラー, "no data available now; try again later\n");
        } else {
            ペラー("read");
            close(fd);
            イグジット(1);
        }
    } else if (n == 0) {
        プリントエフ("エンドオブファイル\n");
    } else {
        プリントエフ("read %zd bytes\n", n);
    }

    close(fd);

    return 0;
}
```

通常ファイルでは、`オー・ノンブロック` を付けても大きな意味がないことが多いです。
ノンブロッキングあいおーが重要になるのは、主に端末、パイプ、ファイフォ、ソケットなどです。

また、ノンブロッキングにしたからプログラムが必ず速くなるわけではありません。
データがないのに何度も `read関数` を呼び続けると、CPUを無駄に使うだけになります。
実用的には、`ポール関数`、`select関数`、`イーポール関数` などで「読める状態になったか」を待つ設計と組み合わせることが多いです。

ウーシュのようなシェルを最初に作る段階では、普通のブロッキングあいおーで十分な場面が多いです。
ただし、将来的にジョブ制御、疑似端末、イベントループを扱うなら、`イーアゲイン` とノンブロッキングあいおーの理解は重要になります。

#### ２章の３の８　read関数 でよく見る エルノー

`read関数` が `-1` を返した場合は、`エルノー` に失敗理由が入ります。
代ひょう的なものを整理します。

```text
EBADF
    fdが無効
    または読み取り用に開かれていない

EFAULT
    bufが不正なアドレスを指している

EINVAL
    fdがさししめす対象や指定がread関数に対応していない

EIO
    低レベルのあいおーエラー

EINTR
    シグナルで割り込まれた

イーアゲイン / イー・ウッドブロック
    ノンブロッキングあいおーで、今は読み取れるデータがない
```

UmuOSを作る側の視点では、このようなエラーをどこまで細かく実装するかが設計ポイントになります。
最初は `EBADF` や `EINVAL` のような基本的なエラーからでもよいですが、ユーザープログラムが原因を区別できるようにするほど、
OSらしい振る舞いに近づきます。

#### ２章の３の９　read関数 のサイズ上限

`read関数` の第3引数 `count` は `size_t` です。
`size_t` は、サイズをひょうす符号なし整数型です。

一方で、`read関数` のもどりち、は `エスサイズティー` です。
`エスサイズティー` は、`size_t` の符号付き版のような型です。
なぜ符号付きなのかというと、エラー時に `-1` を返す必要があるからです。

```text
size_t
    サイズをひょうす型
    符号なし

エスサイズティー
    read関数 や write関数 のもどりち、でよく使う型
    符号付き
    -1 でエラーをひょうせる
```

ここで問題になるのは、`count` にあまりにも大きな値を渡した場合です。
POSIXでは、`count` が `SSIZE_MAX` より大きい場合の動作は実装依存です。
つまり、移植性のあるコードでは避けるべきです。

```text
count <= SSIZE_MAX
    扱いやすい範囲

count > SSIZE_MAX
    動作が実装依存
    避ける方がよい
```

汎用的な読み取り関数を書くなら、次のように上限を丸めることがあります。

```c
#include <limits.h>

if (len > SSIZE_MAX) {
    len = SSIZE_MAX;
}
```

また、`count` に0を渡した場合、`read関数` は通常0を返します。
何も読み取らないので、ファイルポジションも進みません。

```c
n = read(fd, buf, 0);
```

これはエラーではありませんが、実用的な読み取り処理ではあまり意味がありません。

#### ２章の３の１０　read関数 の要点

ここまでの `read関数` の要点をまとめます。

```text
read関数 は最大countバイト読む
    countバイト必ず読むわけではない

もどりち、が正
    実際に読めたバイト数

もどりち、が0
    エンドオブファイル

もどりち、が-1
    エラー
    エルノーを見る

EINTR
    シグナルで中断された
    やり直せる場合が多い

イーアゲイン / イー・ウッドブロック
    ノンブロッキングあいおーで、今はデータがない

部分読み取り
    正常に起こり得る
    バグではない
```

UmuOSの視点では、`read関数` は単に「ファイルから読む関数」ではありません。
通常ファイル、端末、パイプ、ソケット、デバイスを同じfdインターフェースで扱うための入口です。
ウーシュの標準入力、パイプ、リダイレクトを実装するうえでも、`read関数` のもどりち、と部分読み取りの扱いはかなり重要になります。

### ２章の４　ファイル書き込み: write関数

ファイルへ書き込むときに、もっとも基本になる低レベルあいおーが `write関数` です。
`write関数` は `read関数` と対になるシステムコールです。

`read関数` が「fdからバッファへ読む」のに対して、`write関数` は「バッファからfdへ書く」と考えると分かりやすいです。

宣言は次のようになります。

```c
#include ユニストディードットエイチ。

エスサイズティー write(int fd, const void *buf, size_t count);
```

ここで出てくる `エスサイズティー write(...)` は、実行コードではなく、関数の形を示すプロトタイプ宣言です。
つまり、`write関数` は `エスサイズティー` 型の値を返す関数、という意味です。

意味は次の通りです。

```text
fd
    書き込み先のファイルディスクリプタ

buf
    書き込みたいデータが入っているバッファ
    const void * なので、write関数 側はbufの中身を書き換えない

count
    最大で何バイト書き込むか

もどりち、
    実際に書き込んだバイト数
    エラーなら-1
```

`write関数` は、`buf` から最大 `count` バイトを取り出し、`fd` がさししめす対象へ書き込みます。
通常ファイルの場合は、現在のファイルポジションへ書き込み、書き込んだバイト数ぶんだけファイルポジションが進みます。

```text
write(fd, buf, 100)
    bufから最大100バイトを書き込む
    実際に書けたバイト数を返す
    書けた分だけファイルポジションが進む
```

古い説明では、シークできないキャラクタデバイスでは常に「先頭」に書く、というひょう現が出てくることがあります。
ただ、現在の理解としては、端末、パイプ、ソケット、キャラクタデバイスなどは、通常ファイルのような「何バイト目」というファイルポジションを
持たないことが多い、と考える方が分かりやすいです。
その場合、`write関数` は「現在の出力ストリームへデータを流す」ような動作になります。

#### ２章の４の１　write関数 の最小例

まずは、文字列を標準出力へ書き込む例です。

```c
#include <エスティーディーio.h>
#include <エスティーディーlib.h>
#include <string.h>
#include ユニストディードットエイチ。

int main(void) {
    const char *message = "My ship is solid!\n";
    エスサイズティー n;

    n = write(エスティーディーアウトファイルナンバー, message, strlen(message));

    if (n < 0) {
        ペラー("write");
        イグジット(1);
    }

    return 0;
}
```

ここでは、`message` の文字列を標準出力へ書いています。
`strlen(message)` は、文字列の長さをバイト数として数えています。

ただし、このコードは最小例です。
`write関数` が成功しても、要求したバイト数を全部書けたとは限りません。
そのため、実用的なコードでは、もどりち、を見て「何バイト書けたか」を確認する必要があります。

#### ２章の４の２　write関数 のもどりち、

`write関数` のもどりち、は、`read関数` と同じく `エスサイズティー` です。
成功時には実際に書き込んだバイト数を返し、失敗時には `-1` を返します。

```text
もどりち、 == count
    要求したバイト数をすべて書き込めた

0 < もどりち、 < count
    一部だけ書き込めた
    エラーとは限らない

もどりち、 == 0
    1バイトも書き込めなかった
    countが0の場合など

もどりち、 == -1
    エラー
    エルノー に理由が入る
```

`read関数` ではもどりち、 `0` が エンドオブファイル を意味しました。
しかし、`write関数` のもどりち、 `0` は エンドオブファイル ではありません。
書き込みには「ファイル終端に達したから終わり」という考え方は基本的にありません。
通常ファイルなら、末尾を越えて書けばファイルサイズが大きくなります。

ここは混同しやすいです。

```text
read関数 の 0
    エンドオブファイル
    もう読めるデータがない

write関数 の 0
    1バイトも書けなかった
    エンドオブファイルという意味ではない
```

#### ２章の４の３　一部しか書き込めない場合

`write関数` も、要求したバイト数を必ず全部書き込む関数ではありません。
最大で `count` バイト書き込む関数です。

通常ファイルでは、十分な空き容量があり、エラーも起きなければ、要求した分を書けることが多いです。
そのため、古い説明では「通常ファイルでは部分書き込みはあまり気にしなくてよい」と書かれることがあります。

ただし、現代の実用コードでは、`write関数` のもどりち、をきちんと扱う方が安全です。
特に、パイプ、ファイフォ、ソケット、端末、ノンブロッキングあいおーでは、部分書き込みが普通に起こり得ます。

たとえば、次のようなコードは不十分です。

```c
unsigned long word = 1720;
size_t count;
エスサイズティー n;

count = sizエンドオブファイル(word);
n = write(fd, &word, count);

if (n < 0) {
    ペラー("write");
    イグジット(1);
}

/* 危険: sizエンドオブファイル(word) バイト全部を書けたとは限らない */
```

このコードでは、`write関数` が `sizエンドオブファイル(word)` より小さい正の値を返した場合に対応できません。
つまり、途中までしか書けていないのに、全部書けたつもりで先へ進んでしまいます。

#### ２章の４の４　指定したバイト数を最後まで書き込む

指定したバイト列を最後まで書き込みたい場合は、ループが必要です。
これは `read_full関数` の書き込み版のようなものです。

```c
#include <エルノー.h>
#include <エスティーディーdef.h>
#include ユニストディードットエイチ。

エスサイズティー write_full(int fd, const void *buf, size_t len) {
    const char *p;
    size_t left;
    エスサイズティー n;

    p = buf;
    left = len;

    while (left > 0) {
        n = write(fd, p, left);

        if (n < 0) {
            if (エルノー == EINTR) {
                continue;
            }

            return -1;
        }

        if (n == 0) {
            break;
        }

        p += n;
        left -= n;
    }

    return (エスサイズティー)(len - left);
}
```

この関数の考え方は `read_full関数` とほぼ同じです。

```text
p
    次に書き込むデータの位置

left
    まだ書き込みたい残りバイト数
```

たとえば、100バイト書きたいのに30バイトしか書けなかった場合、次は `buf + 30` の位置から、残り70バイトを書き込みます。

```text
最初
    p = buf
    left = 100

30バイト書けた後
    p = buf + 30
    left = 70
```

ここで `const void *` を `const char *` に変換しているのは、バイト単位でポインタを進めたいからです。
`const` が付いているのは、書き込み元のバッファ内容をこの関数が変更しないためです。

使い方は次のようになります。

```c
#include <fcntl.h>
#include <エスティーディーio.h>
#include <エスティーディーlib.h>
#include <string.h>
#include ユニストディードットエイチ。

int main(void) {
    const char *message = "hello from write_full\n";
    int fd;
    エスサイズティー n;

    fd = オープン("メモどっとテキスト", オー・ライトオンリー | オー・クリエイト | オー・トランク | オー・クローズエグゼック, 0644);

    if (fd < 0) {
        ペラー("オープン");
        イグジット(1);
    }

    n = write_full(fd, message, strlen(message));

    if (n < 0) {
        ペラー("write_full");
        close(fd);
        イグジット(1);
    }

    if ((size_t)n != strlen(message)) {
        エフプリントエフ(エスティーディーエラー, "short write: expected %zu bytes, wrote %zd bytes\n",
                strlen(message), n);
        close(fd);
        イグジット(1);
    }

    if (close(fd) < 0) {
        ペラー("close");
        イグジット(1);
    }

    return 0;
}
```

この例では、`message` の内容を最後まで書けたか確認しています。

#### ２章の４の５　アペンドモード

`おーアペンド` フラグを付けてファイルをひらくと、書き込みは現在のファイルポジションではなく、常にファイル末尾に対して行われます。

```c
fd = オープン("アップログ", オー・ライトオンリー | オー・クリエイト | おーアペンド | オー・クローズエグゼック, 0644);
```

アペンドモードが重要になる代ひょう例はログファイルです。
複数のプロセスが同じログファイルへ追記する場合、単純に `エルシーク(fd, 0, SEEK_END)` で末尾へ移動してから `write関数` すると
競合する可能性があります。

たとえば、次のような流れが起きるかもしれません。

```text
プロセスA
    ファイル末尾へ移動する

プロセスB
    ファイル末尾へ移動する
    ログを書き込む

プロセスA
    さっき見た末尾位置へログを書き込む
```

この場合、プロセスAが見ていた「末尾」は、プロセスBの書き込み後には本当の末尾ではなくなっています。
これが競合状態です。

`おーアペンド` を使うと、カーネルが各 `write関数` の直前にファイル末尾へ移動してから書き込みます。
この「末尾へ移動して書く」という処理が、他の書き込みに割り込まれない形で扱われるため、ログの追記に向いています。

```text
おーアペンドなし
    エルシーク関数 と write関数 の間に別プロセスが割り込む可能性がある

おーアペンドあり
    write関数 のたびにカーネルが末尾へ追記する
```

ただし、`おーアペンド` を使えば複数プロセスのログが必ずきれいな行単位で混ざらない、という意味ではありません。
1回の `write関数` の単位、ファイルシステム、ログのサイズ、実装条件によって考えるべきことがあります。
ログを書くなら、1行をできるだけ1回の `write関数` で出す、という設計が分かりやすいです。

UmuOSでログ機能を作る場合も、`おーアペンド` 相当の考え方は重要です。
単に「ファイル末尾へカーソルを移動してから書く」だけだと、複数プロセス対応のときに競合が出ます。

#### ２章の４の６　ノンブロッキング書き込み

`オー・ノンブロック` を付けてfdをひらくと、対象によってはノンブロッキング書き込みになります。
ノンブロッキングあいおーでは、本来なら待つ場面で `write関数` が待たずに戻ることがあります。

書き込み側でよくあるのは、パイプやソケットの送信バッファがいっぱいになっている場合です。
ブロッキングあいおーなら空きができるまで待ちます。
ノンブロッキングあいおーなら、`write関数` は `-1` を返し、`エルノー` に `イーアゲイン` または `イー・ウッドブロック` を設定することがあります。

```text
イーアゲイン / イー・ウッドブロック
    今は書き込めない
    後でもう一度試せる
    重大なあいおーエラーとは限らない
```

通常ファイルでは、ノンブロッキング書き込みを意識する場面は多くありません。
重要になるのは、主にパイプ、ファイフォ、ソケット、端末などです。

実用的には、`ポール関数`、`select関数`、`イーポール関数` などで「書き込める状態になったか」を待つ設計と組み合わせます。
データを書けないのに何度も `write関数` を呼び続けると、CPUを無駄に使ってしまうからです。

#### ２章の４の７　write関数 でよく見る エルノー

`write関数` が `-1` を返した場合は、`エルノー` に失敗理由が入ります。
代ひょう的なものを整理します。

```text
EBADF
    fdが無効
    または書き込み用に開かれていない

EFAULT
    bufが不正なアドレスを指している

EFBIG
    ファイルサイズ上限を超える書き込みをしようとした

EINVAL
    fdがさししめす対象や指定がwrite関数に対応していない

EIO
    低レベルのあいおーエラー

ENOSPC
    ファイルシステムの空き容量が足りない

EINTR
    シグナルで割り込まれた

イーアゲイン / イー・ウッドブロック
    ノンブロッキングあいおーで、今は書き込めない

Eパイプ
    パイプやソケットの読み取り側がとじられている
```

`Eパイプ` はシェルやパイプを作るときに重要です。
パイプの読み取り側がとじられている状態で書き込むと、`write関数` は失敗することがあります。
このとき、プロセスには通常 `SIGパイプ` シグナルも送られます。

`SIGパイプ` のデフォルト動作はプロセス終了です。
そのため、プログラムが `SIGパイプ` を無視または処理している場合にだけ、`write関数` のもどりち、として `-1` と `エルノー == Eパイプ` を
観測できることがあります。

これは、シェルのパイプでよく見える現象です。
たとえば、出力側が大量に書いている途中で、読み取り側が先に終了すると、書き込み側に `SIGパイプ` が飛ぶことがあります。

```text
yes | head -n 1

head は1行読んだら終了する
yes はまだ書こうとする
読み取り側が閉じたパイプへ書くため SIGパイプ が発生する
```

ウーシュを作る場合、パイプ処理では `Eパイプ` と `SIGパイプ` の扱いを意識する必要があります。

#### ２章の４の８　write関数 のサイズ上限

`write関数` の第3引数 `count` は `size_t` です。
一方で、もどりち、は `エスサイズティー` です。

`read関数` と同じく、`count` にあまりにも大きな値を渡すのは避けるべきです。
POSIXでは、`count` が `SSIZE_MAX` より大きい場合の動作は実装依存です。

```text
count <= SSIZE_MAX
    扱いやすい範囲

count > SSIZE_MAX
    動作が実装依存
    避ける方がよい
```

なお、貼り付け元の古い説明では「未定義」と説明されることがありますが、POSIXの考え方としては実装依存と捉える方が正確です。
いずれにしても、移植性のあるコードでは `SSIZE_MAX` を超える巨大な1回の `write関数` は避けます。

`count` に0を渡した場合、通常は `write関数` は0を返します。
何も書き込まないので、ファイルポジションも進みません。

```c
n = write(fd, buf, 0);
```

これはエラーではありませんが、実用的な書き込み処理ではあまり意味がありません。

#### ２章の４の９　write関数 が返ってもディスクへ書けたとは限らない

ここは非常に重要です。
`write関数` が成功しても、その時点でデータが物理ディスクへ完全に保存されたとは限りません。

多くの場合、`write関数` はユーザープログラムのバッファから、カーネルが管理するページキャッシュへデータをコピーします。
その後、カーネルが都合のよいタイミングで、実際のストレージへ書き出します。
この後から行われる書き出しを、ライトバック、または writeback と呼びます。

```text
アプリケーション
    write(fd, buf, count)

カーネル
    ユーザーバッファからページキャッシュへコピーする
    write関数 を成功として返す

後で
    dirtyなページをストレージへ書き出す
```

ここで `dirty` とは、メモリ上では変更済みだが、まだストレージへ反映されていない状態を意味します。
日本語では「汚れた」と直訳されることもありますが、実際には「未書き戻しの変更済みデータ」と考えると分かりやすいです。

この仕組みによって、`write関数` はかなり速く戻れます。
毎回すぐにディスクへ同期していたら、プログラムはディスクの遅さに強く引っ張られてしまいます。
カーネルは複数の書き込みをまとめたり、順序を調整したりして、効率よくストレージへ書き出します。

ただし、注意点があります。

```text
write関数 成功
    カーネルには渡せた
    しかし、物理ディスクへ保存済みとは限らない

システムクラッシュ
    ページキャッシュ上の未書き戻しデータが失われる可能性がある
```

つまり、アプリケーションからは書き込みが成功したように見えても、クラッシュ時にはディスクへ残っていない可能性があります。

また、実際の書き戻し時にあいおーエラーが起きる場合もあります。
このエラーは、最初に `write関数` を呼んだプロセスへその場で返せるとは限りません。
なぜなら、エラーが起きる時点では、書き込んだプロセスがすでに終了しているかもしれないからです。

このため、重要なデータでは `fsync関数`、`fdatasync関数`、`オー・シンク` などを使って、どのタイミングで永続化を保証したいかを明示する必要があります。
この話は、次の同期あいおーの節で詳しく扱うことになります。

現在のLinuxでは、dirtyデータの書き戻しタイミングは `/proc/sys/vm/dirty_expire_centisecs` などのカーネルパラメータにも関係します。
古い資料では `/proc/sys/vm/dirty_expire_centiseconds` と説明されることがありますが、現在一般的に使われる名前は `dirty_expire_centisecs` です。
単位は 1/100 秒です。

ただし、ここを最初から細かく調整する必要はありません。
まずは、`write関数` 成功は「カーネルへ渡せた」という意味であり、「ディスクへ完全保存された」という意味ではない、と押さえるのが重要です。

#### ２章の４の１０　write関数 の要点

ここまでの `write関数` の要点をまとめます。

```text
write関数 は最大countバイト書く
    countバイト必ず書くわけではない

もどりち、が正
    実際に書けたバイト数

もどりち、が0
    1バイトも書けなかった
    エンドオブファイルではない

もどりち、が-1
    エラー
    エルノーを見る

部分書き込み
    パイプ、ソケット、端末、ノンブロッキングあいおーで特に重要

おーアペンド
    write関数 のたびにファイル末尾へ追記する
    ログ出力で重要

Eパイプ / SIGパイプ
    パイプやソケットの読み取り側がとじられたときに重要

write関数 成功
    ディスクへの永続化完了とは限らない
```

UmuOSの視点では、`write関数` は単に「文字を出す関数」ではありません。
標準出力、ログ、リダイレクト、パイプ、デバイス出力を同じfdインターフェースで扱うための中心的な入口です。

ウーシュの `echo hello > ファイル.txt` や `cmd1 | cmd2` の裏側でも、最終的には `write関数` 相当の処理が必要になります。
そのとき、部分書き込み、アペンド、パイプ切断、遅延書き込みをどう扱うかが、OSらしい動作の設計ポイントになります。

### ２章の５　同期あいおー

前の節で見たように、`write関数` が成功しても、その時点でデータが物理ディスクへ完全に保存されたとは限りません。
多くの場合、Linuxカーネルは書き込まれたデータをページキャッシュへ置き、あとでまとめてストレージへ書き戻します。

この遅延書き込みは、普通のプログラムにとってはとても大事な最適化です。
毎回の `write関数` で必ずストレージへ同期していたら、ログ出力や一時ファイル作成のたびにプログラムが大きく待たされます。
そのため、現代のLinuxでも、書き込みをいったんメモリ上にためて、効率のよいタイミングでディスクへ反映する仕組みが使われています。

ただし、すべてのプログラムが「あとで書き戻されればよい」と考えられるわけではありません。
たとえば、次のような処理では、どの時点でデータが永続化されたかが重要になります。

```text
データベース
    トランザクションのコミット後にデータを失いたくない

エディタ
    保存完了と表示した直後にクラッシュしても、内容を残したい

パッケージマネージャ
    インストール途中のメタデータ破損をさけたい

ログシステム
    障害直前のログをできるだけ残したい

ファイルシステムやOS開発
    メタデータとデータの順序を明確に制御したい
```

このような場面で使うのが、同期あいオー・または synchronized あいおー です。
同期あいおーは、性能をある程度犠牲にして、書き込んだデータをより確実なタイミングでストレージへ反映させるための仕組みです。

#### ２章の５の１　同期あいおーが必要になる理由

通常の `write関数` は、ざっくり言うと「ユーザープログラムからカーネルへデータを渡す」処理です。
その後、カーネルがページキャッシュ上の dirty データを実際のストレージへ書き戻します。

```text
通常のwrite関数

アプリケーション
    write(fd, buf, count)

カーネル
    ページキャッシュへコピーする
    write関数を成功として返す

あとで
    dirtyデータをストレージへ書き戻す
```

この方式は高速です。
しかし、`write関数` が成功した直後に電源断やカーネルクラッシュが起きると、ページキャッシュ上に残っていたデータは失われる可能性があります。

同期あいおーを使うと、アプリケーションは「この時点までに、必要なデータをストレージへ反映してほしい」とカーネルへ明示できます。
もちろん、同期を増やすほど安全寄りになりますが、そのぶん遅くなります。

```text
安全性を上げる
    同期する回数を増やす
    ただし遅くなりやすい

性能を上げる
    同期する回数を減らす
    ただしクラッシュ時に失う範囲が増えやすい
```

つまり、同期あいおーは「常に使えばよい」ものではありません。
どこまでのデータ消失を許容できるか、どの時点を保存完了と呼びたいかを考えて使うものです。

#### ２章の５の２　fsync関数

`fsync関数` は、指定したfdに関係するデータとメタデータをストレージへ同期するための関数です。
書き込んだデータをディスクへ書き出したいときに、もっとも基本になる同期用APIです。

宣言は次のようになります。

```c
#include ユニストディードットエイチ。

int fsync(int fd);
```

ここで出てくる `int fsync(int fd);` も、実行コードではなくプロトタイプ宣言です。
`fsync関数` は成功すると `0`、失敗すると `-1` を返し、`エルノー` に理由を設定します。

```text
成功
    0

失敗
    -1
    エルノー に理由が入る
```

`fsync(fd)` を呼ぶと、そのfdで参照しているファイルについて、書き込まれたデータと、ファイルサイズや更新時刻などのメタデータをストレージへ反映しようとします。
ここでいうメタデータとは、ファイルの中身そのものではなく、アイノードが持つ属性情報のことです。

たとえば、次のような情報が関係します。

```text
ファイルサイズ
    何バイトのファイルか

更新時刻
    いつ内容が更新されたか

割り当て済みブロック
    どのディスクブロックを使っているか

パーミッションや所有者
    誰が読み書きできるか
```

```text
fsync(fd)
    ファイルのデータ
    ファイルに関係するメタデータ
    これらをストレージへ同期する
```

注意点として、`fsync関数` は「カーネルとストレージに、必要な書き出しとフラッシュを要求する」ためのAPIです。
しかし、壊れたストレージ、揮発性のディスク内部キャッシュ、仮想化環境、RAIDコントローラなどが絡むと、物理媒体へ本当に到達したかをアプリケーションが
完全に証明できるわけではありません。

古い説明では「ディスク内部キャッシュに残っていても、短い間隔で物理的に書き出されるので通常問題ない」とされることがあります。
現在でも普通の用途では過度に心配しすぎる必要はありませんが、データベースやファイルシステムのようにクラッシュ耐性を厳密に考える領域では、
ストレージのライトキャッシュ、バリア、flウーシュ命令、マウントオプションまで含めて考えることがあります。

最初の理解としては、次のように押さえるとよいです。

```text
write関数
    カーネルへデータを渡す

fsync関数
    そのファイルのデータとメタデータをストレージへ反映するよう要求する

ただし
    ハードウェアや仮想化環境が正しく応答することまでは、アプリケーションだけでは保証できない
```

簡単な例です。

```c
#include <fcntl.h>
#include <エスティーディーio.h>
#include <エスティーディーlib.h>
#include <string.h>
#include ユニストディードットエイチ。

int main(void) {
    const char *message = "important data\n";
    int fd;

    fd = オープン("important.txt", オー・ライトオンリー | オー・クリエイト | オー・トランク | オー・クローズエグゼック, 0644);

    if (fd < 0) {
        ペラー("オープン");
        イグジット(1);
    }

    if (write(fd, message, strlen(message)) < 0) {
        ペラー("write");
        close(fd);
        イグジット(1);
    }

    if (fsync(fd) < 0) {
        ペラー("fsync");
        close(fd);
        イグジット(1);
    }

    if (close(fd) < 0) {
        ペラー("close");
        イグジット(1);
    }

    return 0;
}
```

この例では、`write関数` のあとに `fsync関数` を呼んでいます。
これにより、`write関数` でカーネルへ渡したデータを、より確実にストレージへ反映させてから処理を進めます。

ただし、このコードも最小例です。
本当に堅牢にするなら、前の節で作った `write_full関数` のように部分書き込みへ対応する必要があります。

また、`fsync関数` は対象ファイルのデータとメタデータを同期しますが、おやディレクトリのディレクトリエントリまで必ず同期するとは限りません。
ここはかなり重要です。

ディレクトリエントリとは、おやディレクトリの中にある「ファイルメイとアイノード番号の対応」のことです。

```text
ディレクトリエントリ
    ファイルメイ -> アイノード番号

例
    important.txt -> アイノード 12345
```

たとえば、新しいファイルを作ったり、`rename関数` で名前を置き換えたりした場合、ファイル本体だけでなく、おやディレクトリ側の
「この名前がこのアイノードをさししめす」という情報も更新されます。
ファイル本体を `fsync(fd)` しても、おやディレクトリの変更がクラッシュ前に保存されていなければ、
再起動後にその名前でファイルを見つけられない可能性があります。

そのため、重要なファイル作成や `rename関数` 後には、おやディレクトリをひらいて、そのディレクトリfdに対しても `fsync関数` することがあります。

```c
#include <fcntl.h>
#include <エスティーディーio.h>
#include <エスティーディーlib.h>
#include ユニストディードットエイチ。

int main(void) {
    int dirfd;

    dirfd = オープン(".", オー・リードオンリー | オー・ディレクトリ | オー・クローズエグゼック);

    if (dirfd < 0) {
        ペラー("オープン directory");
        イグジット(1);
    }

    if (fsync(dirfd) < 0) {
        ペラー("fsync directory");
        close(dirfd);
        イグジット(1);
    }

    if (close(dirfd) < 0) {
        ペラー("close directory");
        イグジット(1);
    }

    return 0;
}
```

実際の安全な更新では、ファイル本体の `fsync関数` と、おやディレクトリの `fsync関数` を組み合わせます。
この話は後の「安全なファイル更新の基本形」でもう一度整理します。

#### ２章の５の３　fdatasync関数

`fdatasync関数` は、`fsync関数` に似ていますが、主にファイルのデータを同期するための関数です。

宣言は次のようになります。

```c
#include ユニストディードットエイチ。

int fdatasync(int fd);
```

`fsync関数` と `fdatasync関数` の違いは、メタデータの扱いです。

```text
fsync関数
    データと、必要なメタデータを同期する

fdatasync関数
    主にデータを同期する
    ただし、データを正しく読むために必要なメタデータは同期される
```

たとえば、ファイルサイズが伸びた場合、その新しいサイズ情報が保存されないと、あとでデータを正しく読めません。
そのようなメタデータは `fdatasync関数` でも同期対象になります。

一方で、アクセス時刻や更新時刻のような、ファイル内容を読むために必須ではないメタデータは、`fsync関数` より同期範囲が抑えられる場合があります。

```text
データベースやログ
    fdatasync関数 が向く場合がある

ファイル全体の状態をより強く同期したい
    fsync関数 を考える
```

ただし、実際の性能差や挙動はファイルシステム、マウントオプション、ストレージ、カーネルによって変わります。
ファイルシステムによっては、`fdatasync関数` と `fsync関数` をほとんど同じように実装していることもあります。
最初は、`fsync関数` は広めに同期、`fdatasync関数` はデータ中心に同期、と押さえれば十分です。

#### ２章の５の４　fsync関数 と fdatasync関数 のもどりち、と エルノー

`fsync関数` と `fdatasync関数` は、どちらも成功すると `0` を返します。
エラーが発生した場合は `-1` を返し、`エルノー` に理由を設定します。

```text
成功
    0

失敗
    -1
    エルノー に理由が入る
```

代ひょう的な `エルノー` は次の通りです。

```text
EBADF
    fdが無効
    または同期対象として使えないfdである

EINVAL
    fdがさししめす対象が同期書き込みに対応していない

EIO
    書き出し中に低レベルのあいおーエラーが発生した
```

古い説明では、`EBADF` は「渡されたfdが書き込み用にオープンされていない」と説明されることがあります。
実用上は、少なくとも「無効なfdでは失敗する」「書き込み対象として扱えないfdでは失敗し得る」と覚えておくとよいです。

`EIO` は特に重要です。
遅延書き込みでは、実際のあいおーエラーが `write関数` の時点ではなく、後の `fsync関数`、`fdatasync関数`、または `close関数` の時点で見えることがあります。
そのため、重要な保存処理では、`write関数` だけでなく `fsync関数` や `close関数` のもどりち、も確認します。

単純な呼び出しは次のようになります。

```c
int ret;

ret = fsync(fd);

if (ret < 0) {
    ペラー("fsync");
}
```

古い資料には、`fsync関数` が `EINVAL` で失敗した場合だけ `fdatasync関数` も試す、というかなり慎重なコードが載っていることがあります。

```c
#include <エルノー.h>
#include <エスティーディーio.h>
#include ユニストディードットエイチ。

if (fsync(fd) < 0) {
    if (エルノー == EINVAL) {
        if (fdatasync(fd) < 0) {
            ペラー("fdatasync");
        }
    } else {
        ペラー("fsync");
    }
}
```

このコードの考え方は、「基本は `fsync関数` を使いたいが、対象によって `fsync関数` が使えない場合に `fdatasync関数` も試してみる」というものです。
現在のLinuxの一般的なファイルシステムで、通常ファイルを扱うなら、まず `fsync関数` または `fdatasync関数` のどちらを使うべきかを
設計として選ぶ方が普通です。

```text
設定ファイルや置き換え更新
    fsync関数 を考える

ログやデータ本体中心の同期
    fdatasync関数 を考える

特殊なfdや特殊なファイルシステム
    EINVAL などのエラー処理も考える
```

UmuOSの視点では、この `エルノー` 設計も重要です。
同期に対応していない対象へ `fsync関数` した場合、単に失敗とするだけでなく、なぜ失敗したかをユーザープログラムへ返せると、
OSとして扱いやすくなります。

#### ２章の５の５　sync関数 と syncfs関数

`sync関数` は、システム全体のdirtyデータを書き戻すようにカーネルへ依頼する関数です。
対象範囲がとても広いため、個別ファイルの保存処理としては大ざっぱな方法です。

宣言は次のようになります。

```c
#include ユニストディードットエイチ。

void sync(void);
```

`sync関数` は引数を取りません。
もどりち、もありません。

`sync関数` は、特定のファイルだけではなく、システム全体の未書き戻しデータを対象にします。
昔のUnixでは、シャットダウン前に `sync` コマンドを実行する話がよく出てきました。

```text
fsync(fd)
    特定のfdに関係するファイルを同期する

sync関数
    システム全体のdirtyデータを書き戻すよう依頼する
```

POSIXの考え方では、`sync関数` はすべての書き戻し完了まで必ず待つ必要はなく、書き戻し処理を開始すればよい、とされています。
そのため、古いUnixの説明では、念のため `sync` を複数回実行する、という話が出てくることがあります。

Linuxでは、現在の `sync関数` はシステム全体のdirtyデータの書き戻し完了を待つ動作として扱われます。
そのため、Linux上で「念のため何回も `sync関数` を呼ぶ」必要は通常ありません。

ただし、ここでもストレージ側のキャッシュや仮想化環境の問題は残ります。
`fsync関数` の節でも触れたように、カーネルがストレージへフラッシュを要求しても、ハードウェアや下層の環境が正しく永続化したと
応答していることが前提になります。

現代のアプリケーションで「このファイルを保存したい」という目的なら、
普通は `sync関数` ではなく `fsync関数` や `fdatasync関数` を使います。
`sync関数` は対象が広すぎるため、自分のファイルの保存完了を細かく制御する用途には向きません。
システム負荷が高く、dirtyデータが大量にある場合、`sync関数` は長く待たされる可能性があります。

```text
sync関数 が向く場面
    システム全体の書き戻しを促したい
    syncコマンドやシャットダウン処理に近い用途

通常のアプリケーション
    保存したいfdに対して fsync関数 または fdatasync関数 を使う
```

Linuxには `syncfs関数` もあります。
これは、指定したfdが属するファイルシステムを対象に同期する関数です。

```c
#define _GNU_SOURCE
#include ユニストディードットエイチ。

int syncfs(int fd);
```

`syncfs関数` はLinux固有寄りのAPIです。
移植性を重視する通常のPOSIX的なコードでは、まず `fsync関数` と `fdatasync関数` を押さえるのがよいです。

#### ２章の５の６　オー・シンク、O_DSYNC、O_RSYNC

`オープン関数` のフラグとして `オー・シンク` や `O_DSYNC` を指定すると、そのfdへの書き込みを同期的に行うようにできます。
つまり、`write関数` が戻る時点で、書き込んだデータをストレージへ反映する方向に強く制御できます。

```c
fd = オープン("important.log", オー・ライトオンリー | オー・クリエイト | おーアペンド | オー・シンク | オー・クローズエグゼック, 0644);
```

もう少し丁寧に書くと、次のようになります。

```c
#include <fcntl.h>
#include <エスティーディーio.h>
#include <エスティーディーlib.h>
#include ユニストディードットエイチ。

int main(void) {
    int fd;

    fd = オープン("important.log", オー・ライトオンリー | オー・クリエイト | おーアペンド | オー・シンク | オー・クローズエグゼック, 0644);

    if (fd < 0) {
        ペラー("オープン");
        イグジット(1);
    }

    if (close(fd) < 0) {
        ペラー("close");
        イグジット(1);
    }

    return 0;
}
```

`オー・シンク` を付けたfdでは、`write関数` が返る前に、書き込んだデータと必要なメタデータの同期が行われます。
つまり、毎回の `write関数` が `fsync関数` に近い重さを持つ場合があります。

```text
オー・シンク
    write関数 のたびにデータと必要なメタデータの同期性を強める
    安全寄りだが遅くなりやすい

O_DSYNC
    データ同期寄り
    fdatasync関数 に近い考え方

O_RSYNC
    POSIX上は読み取り側の同期性に関係するフラグ
    Linuxでは実用上あまり使わない
```

`オー・シンク` は、「`write関数` の中で毎回 `fsync関数` に近いことをする」と考えると分かりやすいです。
厳密にはカーネル内部ではより効率的に処理されますが、アプリケーションから見ると、書き込みごとに同期の待ち時間が発生しやすくなります。

このコストはかなり大きい場合があります。
CPU時間としては、ユーザー空間で使う user time、カーネル空間で使う kernel time が少し増える程度に見えることもあります。
しかし本当に大きいのは、ストレージあいおーの完了を待つ あいおー wait time です。

```text
user time
    ユーザー空間で消費したCPU時間

kernel time
    カーネル空間で消費したCPU時間

あいおー wait time
    ストレージあいおーの完了を待たされた時間
```

書き込むサイズ、ストレージ性能、ファイルシステム、マウントオプションによっては、`オー・シンク` を付けるだけで
総所要時間が大きく増えることがあります。

`オー・シンク` を使うとコード上は簡単です。
しかし、すべてのログ書き込みへ機械的に `オー・シンク` を付けると、性能が大きく落ちる可能性があります。
多くの場合は、重要な区切りで `fsync関数` する、一定間隔で同期する、ジャーナルや一時ファイルとrenameを組み合わせる、
といった設計を考えます。

```text
毎回 オー・シンク
    実装は簡単
    しかし書き込みのたびに重い

必要な場所で fsync関数 / fdatasync関数
    同期するタイミングを選べる
    多くのアプリケーションでこちらの方が設計しやすい
```

`O_DSYNC` は、`オー・シンク` よりデータ同期寄りのフラグです。
考え方としては、書き込みのたびに `fdatasync関数` に近い同期を行うものです。

古い資料では「Linuxでは `O_DSYNC` と `O_RSYNC` は `オー・シンク` の別名で、動作に違いはない」と説明されることがあります。
しかし、現在のLinuxでは `O_DSYNC` と `オー・シンク` は区別されます。
`オー・シンク` は `O_DSYNC` より強い同期を要求するフラグとして扱われます。

一方、`O_RSYNC` はLinuxで普段のアプリケーションが積極的に使う場面はほとんどありません。
POSIX上は、読み取りに伴う同期性、特に読み取りで更新されるメタデータの同期に関係します。
典型例はアクセス時刻、つまり `stat関数` で見える `st_atime` です。

ただし、現代Linuxでは `relatime`、`noatime`、`lazytime` などのマウントオプションによって、
読み取りのたびに厳密にアクセス時刻を書き戻すとは限りません。
そのため、`O_RSYNC` は「知識としてはあるが、通常のLinuxプログラミングではまず使わない」と見てよいです。

読み取りについては、少し言い方に注意が必要です。
`read関数` は、アプリケーションに返すデータという意味では当然、読めたデータを返します。
しかし、「読み取りによって発生したメタデータ更新まで毎回ディスクへ同期する」という意味では、通常の `read関数` はそこまでしません。

```text
read関数
    データを読み取って返す

read関数 による atime 更新
    ファイルシステムやマウントオプションに左右される
    毎回ただちにディスク同期されるとは限らない
```

UmuOSの視点では、ここは「読み取りの正しさ」と「読み取りに伴うメタデータ更新の永続化」を分けて考えると分かりやすいです。
最初のファイルシステムでは、アクセス時刻を省略する設計も十分あり得ます。
一方で、POSIXらしさを高める段階では、`atime`、`mtime`、`ctime` のような時刻メタデータをどう更新し、
どこまで同期するかが設計課題になります。

#### ２章の５の７　close関数 は fsync関数 の代わりではない

ファイルをとじる `close関数` は、fdをとじるための関数です。
`close関数` によってカーネル内のリソースは解放されますが、`close関数` を呼べば必ず `fsync関数` と同じ意味でデータが
物理ストレージへ同期される、とは考えない方がよいです。

```text
close(fd)
    fdをとじる
    エラーが返ることもある
    しかし fsync関数 の代わりとして考えない
```

重要なデータでは、`close関数` の前に `fsync関数` または `fdatasync関数` を明示的に呼ぶ方が意図がはっきりします。

また、`close関数` 自体も失敗する可能性があります。
遅延書き込みのエラーが `close関数` のタイミングで見えることもあります。
そのため、本当に保存結果を気にするコードでは、`close関数` のもどりち、も確認します。

```text
重要な保存処理
    write_full関数
    fsync関数 または fdatasync関数
    close関数
    それぞれのもどりち、を確認する
```

#### ２章の５の８　安全なファイル更新の基本形

設定ファイルや重要なデータを更新するとき、既存ファイルをいきなり `オー・トランク` で空にして書き直すのは危険な場合があります。
途中でクラッシュすると、元の内容も新しい内容も失う可能性があるからです。

よく使われる考え方は、一時ファイルへ書いて同期し、最後に `rename関数` で置き換える方法です。

```text
安全寄りの更新手順

1. 同じディレクトリに一時ファイルを作る
2. 一時ファイルへ新しい内容を書く
3. 一時ファイルを fsync関数 する
4. 一時ファイルを close関数 する
5. rename関数 で本来のファイルメイへ置き換える
6. ディレクトリを fsync関数 する
```

最後にディレクトリを `fsync関数` するのは、`rename関数` によるディレクトリエントリの変更をストレージへ反映するためです。
ここは見落としやすいですが、クラッシュ耐性を考えると重要です。

ただし、この話はファイルシステムやジャーナリングの挙動にも関係します。
最初から完璧に覚える必要はありません。
まずは「重要なファイル更新では、書いたデータだけでなく、ファイルメイの置き換えやディレクトリ側の変更も同期対象になる」と
理解すればよいです。

UmuOSでファイルシステムを作る場合、この考え方はかなり重要になります。
単に `write関数` でデータブロックを書くだけでは、クラッシュ時にファイルメイ、アイノード、サイズ、データブロックの整合性が
崩れる可能性があります。
OS側は、どの順序でメタデータとデータを反映するかを設計しなければなりません。

#### ２章の５の９　同期あいおーの要点

ここまでの同期あいおーの要点をまとめます。

```text
通常のwrite関数
    ページキャッシュへコピーして戻ることが多い
    物理ストレージへ保存完了とは限らない

同期あいおー
    データをストレージへ反映するタイミングをアプリケーションが制御する

fsync関数
    指定fdのデータと必要なメタデータを同期する

fdatasync関数
    主にデータを同期する

fsync関数 / fdatasync関数 のもどりち、
    成功なら0、失敗なら-1
    エルノーで原因を見る

ディレクトリエントリ
    ファイルメイとアイノード番号の対応
    重要なrename関数後はおやディレクトリのfsync関数も考える

sync関数
    システム全体のdirtyデータを書き戻すよう依頼する
    通常アプリケーションの個別保存には広すぎる

syncfs関数
    指定fdが属するファイルシステムを同期するLinux寄りのAPI

オー・シンク / O_DSYNC / O_RSYNC
    write関数 のたびに同期性を強めるオープン関数フラグ
    O_RSYNC はLinuxの通常プログラミングではあまり使わない

close関数
    fsync関数 の代わりではない

安全なファイル更新
    一時ファイル、fsync関数、rename関数、ディレクトリfsync関数を組み合わせる
```

UmuOSの視点では、同期あいおーは単なる便利機能ではありません。
クラッシュしたときに、ファイルシステムがどの状態まで戻れるのか、どの順序でデータとメタデータを保存するのか、
というOS設計そのものに関係します。

ウーシュのようなシェルでは、最初から同期あいおーを細かく制御する場面は多くないかもしれません。
しかし、エディタ、パッケージ管理、ログ、設定ファイル更新を作る段階では、`write関数` だけで保存完了と考えない感覚が
重要になります。

### ２章の６　ダイレクトあいおー

ここまで見てきた通常のファイルあいおーでは、アプリケーションとストレージの間にカーネルのページキャッシュが入ります。
ページキャッシュは、多くのプログラムにとって非常に有効です。
同じファイルを何度も読むときはメモリ上のキャッシュから返せますし、書き込みもいったんメモリへ集めて効率よくストレージへ書き戻せます。

```text
通常のあいおー

アプリケーション
    read関数 / write関数

カーネル
    ページキャッシュ
    あいおースケジューリング
    ファイルシステム

ストレージ
    SSD、HDD、NVMeなど
```

しかし、一部の高性能アプリケーションでは、このページキャッシュをなるべく使わず、自分でキャッシュやあいおー順序を管理したい場合があります。
代ひょう例はデータベースです。
データベースは自分自身でページキャッシュに似た仕組みを持ち、どのデータをメモリに残すか、いつディスクへ書くかを細かく制御したいことがあります。

このような場合に出てくるのが、ダイレクトあいおーです。
Linuxでは、`オープン関数` に `O_DIRECT` フラグを指定することで、ページキャッシュをなるべく迂回するあいおーを要求できます。

```c
fd = オープン("data.bin", オー・アールディーダブリュー | O_DIRECT | オー・クローズエグゼック);
```

ただし、最初に強調しておきます。
`O_DIRECT` は、普通のアプリケーションを速くする魔法のフラグではありません。
むしろ制約が多く、間違って使うと遅くなったり、`EINVAL` で失敗したりします。

```text
通常のアプリケーション
    まず普通の read関数 / write関数 を使う
    必要な場所で fsync関数 / fdatasync関数 を使う

特殊な高性能アプリケーション
    自前のキャッシュ管理がある場合に O_DIRECT を検討する
```

#### ２章の６の１　O_DIRECT の考え方

`O_DIRECT` は、ユーザーバッファとストレージの間で、ページキャッシュの利用をなるべく避けるためのフラグです。

```text
通常のwrite関数
    ユーザーバッファ
        -> ページキャッシュ
        -> 後でストレージへwriteback

O_DIRECT付きwrite関数
    ユーザーバッファ
        -> なるべく直接ストレージあいおーへ渡す
```

ここで「なるべく」と書いているのは、`O_DIRECT` の細かい挙動がファイルシステム、カーネル、デバイス、あいおーサイズなどに依存するためです。
完全にすべてのカーネル管理を消すわけではありません。
ファイルシステムのメタデータ管理、ブロック割り当て、あいおーキューイングなどは依然としてカーネルが担当します。

つまり、`O_DIRECT` は「カーネルを使わないあいおー」ではありません。
「ページキャッシュをなるべく使わないあいおー」と理解するのがよいです。

#### ２章の６の２　O_DIRECT の制約

`O_DIRECT` を使うと、あいおーのサイズ、ユーザーバッファのアドレス、ファイルオフセットにアラインメント制約がかかります。

```text
O_DIRECTで制約を受けやすいもの

データサイズ
    読み書きするバイト数

バッファアドレス
    ユーザー空間バッファの開始アドレス

ファイルオフセット
    ファイル内の読み書き位置
```

古い説明では「すべてデバイスのセクタサイズ、通常512バイトの倍数にする」と説明されることがあります。
また、古いLinux 2.4時代の説明では、ファイルシステムの論理ブロックサイズ、たとえば4KBに合わせる必要がある、と書かれることがあります。

現在のLinuxでは、必要なアラインメントはファイルシステムやデバイスによって変わります。
512バイト、4KB、あるいはそれ以外の制約が関係することがあります。
そのため、現代的には「固定で512バイト」と覚えるより、対象のファイルシステムとデバイスが要求するアラインメントに合わせる、と考える方が正確です。

```text
古い単純化
    セクタサイズ512バイトに合わせる

現代的な理解
    ファイルシステム、デバイス、カーネルが要求するアラインメントに合わせる
    実用上は4KB境界を意識することが多い
```

アラインメントを満たさない場合、`read関数` や `write関数` が `-1` を返し、`エルノー` が `EINVAL` になることがあります。

#### ２章の６の３　posix_memalign関数 を使う例

`O_DIRECT` で使うバッファは、普通のローカル配列や `malloc関数` で確保したメモリでは条件を満たせないことがあります。
そのため、アラインメントを指定してメモリを確保する `posix_memalign関数` を使うことがあります。

次は、4KB境界にそろえたバッファを確保する例です。

```c
#define _GNU_SOURCE

#include <エルノー.h>
#include <fcntl.h>
#include <エスティーディーio.h>
#include <エスティーディーlib.h>
#include <string.h>
#include ユニストディードットエイチ。

int main(void) {
    void *buf;
    size_t alignment;
    size_t size;
    int fd;
    int ret;

    alignment = 4096;
    size = 4096;
    buf = NULL;

    ret = posix_memalign(&buf, alignment, size);

    if (ret != 0) {
        エフプリントエフ(エスティーディーエラー, "posix_memalign: %s\n", strerror(ret));
        イグジット(1);
    }

    memset(buf, 0, size);

    fd = オープン("direct.bin", オー・ライトオンリー | オー・クリエイト | オー・トランク | O_DIRECT | オー・クローズエグゼック, 0644);

    if (fd < 0) {
        ペラー("オープン");
        free(buf);
        イグジット(1);
    }

    if (write(fd, buf, size) < 0) {
        ペラー("write");
        close(fd);
        free(buf);
        イグジット(1);
    }

    if (close(fd) < 0) {
        ペラー("close");
        free(buf);
        イグジット(1);
    }

    free(buf);

    return 0;
}
```

この例では、バッファサイズもアラインメントも4KBにしています。
ただし、これでもすべての環境で必ず成功するとは限りません。
`O_DIRECT` はファイルシステムやデバイス依存が強いためです。

また、この例でも `write関数` の部分書き込みを完全には扱っていません。
本当に実用コードにするなら、`O_DIRECT` の制約を満たしたまま、部分書き込み、`EINTR`、`EINVAL`、`EIO` などを扱う必要があります。

#### ２章の６の４　O_DIRECT と同期あいおーは同じではない

貼り付け元の古い説明では、`O_DIRECT` のあいおーは同期的に処理され、完了するまでシステムコールから戻らない、と説明されることがあります。
この説明は方向としては分かりやすいですが、現在の理解では少し注意が必要です。

`O_DIRECT` はページキャッシュをなるべく迂回するフラグです。
一方、`オー・シンク` や `fsync関数` は、データをストレージへ永続化するタイミングを制御するための仕組みです。

```text
O_DIRECT
    ページキャッシュをなるべく避ける

オー・シンク / fsync関数
    ストレージへの同期、永続化タイミングを強める
```

この2つは目的が違います。
必要なら `O_DIRECT` と `オー・シンク` を組み合わせることもあります。
ただし、組み合わせれば常に速く安全になるわけではなく、性能も実装もかなり難しくなります。

UmuOSの学習段階では、まずページキャッシュありの普通のあいおーを理解することが先です。
その後で、「キャッシュをさけたい特殊用途」として `O_DIRECT` を位置付けるとよいです。

#### ２章の６の５　ダイレクトあいおーの要点

ここまでのダイレクトあいおーの要点をまとめます。

```text
O_DIRECT
    ページキャッシュをなるべく使わないあいおーを要求する

主な用途
    データベースなど、自前のキャッシュ管理を持つアプリケーション

制約
    バッファアドレス、あいおーサイズ、ファイルオフセットのアラインメントが重要

注意
    普通のアプリケーションを速くする魔法のフラグではない

同期あいおーとの違い
    O_DIRECT はページキャッシュ回避
    オー・シンク や fsync関数 は永続化タイミングの制御
```

UmuOSの視点では、ダイレクトあいおーはすぐに実装する必要はないかもしれません。
まずはページキャッシュに相当する仕組みを作り、普通の `read関数` / `write関数` を安定させる方が重要です。
そのうえで、将来的にデータベース的な用途やブロックデバイスへ近いあいおーを考えるなら、ページキャッシュを通す経路と避ける経路を
分ける設計が出てきます。

### ２章の７　ファイルクローズ: close関数

ファイルディスクリプタを使い終えたら、`close関数` で閉じます。
`close関数` は、プロセスのエフディー対応表からそのfdを外し、ひらいているファイルとの対応付けを解消します。

宣言は次のようになります。

```c
#include ユニストディードットエイチ。

int close(int fd);
```

ここで出てくる `int close(int fd);` も、実行コードではなくプロトタイプ宣言です。
`close関数` は成功すると `0`、失敗すると `-1` を返し、`エルノー` に理由を設定します。

```text
成功
    0

失敗
    -1
    エルノー に理由が入る
```

簡単な使い方は次の通りです。

```c
if (close(fd) < 0) {
    ペラー("close");
}
```

#### ２章の７の１　close関数 が行うこと

`close(fd)` を呼ぶと、そのプロセスのエフディー対応表から `fd` が取り除かれます。
以後、その `fd` 番号は無効になります。

```text
close(fd) 前
    fd 3 -> メモどっとテキスト

close(fd) 後
    fd 3 は無効
    次の オープン関数 で再利用される可能性がある
```

ここで大事なのは、fd番号は再利用されるという点です。
`close(3)` した後、次に `オープン関数` すると、また `3` が返ることがあります。
そのため、閉じた後のfdをうっかり使うと、まったく別のファイルを操作してしまう危険があります。

```text
危険な流れ

fd = オープン("a.txt", ...);   -> 3
close(fd);                 -> 3は無効
fd2 = オープン("b.txt", ...);  -> 3が再利用されるかもしれない

古いfd変数を使う
    実は b.txt を操作してしまう可能性がある
```

ウーシュのようなシェルでは、パイプやリダイレクトでfdをたくさん扱います。
不要になったfdを閉じ忘れると、パイプのエンドオブファイルが届かなかったり、子プロセスへ余計なfdが漏れたりします。
逆に、閉じたfdを再利用後に間違って触ると、別の入出力先を壊すことがあります。

#### ２章の７の２　close関数 とカーネル内の参照

`close関数` は、そのプロセスのエフディー対応表からfdを外します。
しかし、同じファイルが他のfdや他のプロセスから参照されている場合、カーネル内の「ひらいているファイル」の情報がすぐに完全消滅するとは限りません。

```text
同じファイルを複数fdで参照
    fd 3 -> ファイル.txt
    fd 4 -> ファイル.txt

close(3)
    fd 3 は無効
    しかし fd 4 が残っていれば、ひらいている状態は続く
```

フォーク関数 後のおや子プロセスでも似たことが起こります。
おやと子が同じひらいているファイルを参照している場合、片方が `close関数` しても、もう片方の参照が残っていれば、カーネル内の状態は残ります。

カーネル内では、参照数が0になったタイミングで、ひらいているファイルに対応するデータ構造を解放できるようになります。
アイノードのメモリ上のコピーも、他に参照がなければ解放候補になります。
ただし、性能上の理由で、不要になったアイノードやデータはすぐに完全破棄されず、キャッシュとして残ることもあります。

#### ２章の７の３　unlink済みファイルとclose関数

Unix系OSでは、ひらいているファイルを `unlink関数` で削除しても、そのファイルがすぐに物理的にきえるとは限りません。
ファイルメイはディレクトリからきえますが、ひらいているfdが残っている間は、ファイルの実体は残ります。

```text
1. オープン("temp.txt")
    fd 3 が返る

2. unlink("temp.txt")
    ディレクトリから temp.txt という名前がきえる

3. fd 3 ではまだ読み書きできる

4. close(3)
    最後の参照がきえると、実体を解放できる
```

この性質は、一時ファイルでよく使われます。
名前を消しておけば、プロセスが異常終了してもファイルメイだけが残りにくくなります。
一方で、fdをとじるまではディスク容量を使い続ける点に注意が必要です。

UmuOSでファイルシステムを作る場合も、ここは重要です。
ファイルメイの削除と、アイノードやデータブロックの解放は同じ瞬間とは限りません。
リンク数とオープン中の参照数を分けて考える必要があります。

#### ２章の７の４　close関数 は保存完了の保証ではない

前の同期あいおー節でも触れましたが、`close関数` は `fsync関数` の代わりではありません。
`close関数` はfdをとじる関数であり、データを必ず物理ストレージへ永続化するための関数ではありません。

```text
close関数
    fdをとじる

fsync関数
    データとメタデータの同期を要求する
```

重要なデータでは、次のように考えます。

```text
write_full関数
    書きたいデータを最後まで書く

fsync関数 または fdatasync関数
    必要な同期を行う

close関数
    fdをとじる
```

ただし、`close関数` のもどりち、も無視してよいわけではありません。
遅延書き込みに関係するエラーが、`close関数` のタイミングで見えることがあるからです。

#### ２章の７の５　close関数 の エルノー

`close関数` が `-1` を返した場合、`エルノー` に理由が入ります。
代ひょう的なものは次の通りです。

```text
EBADF
    fdが無効

EIO
    下位レベルのあいおーエラーが発生した

EINTR
    シグナルで割り込まれた
    POSIX上は起こり得る扱い
```

特に重要なのは `EIO` です。
遅延書き込みでは、実際のディスク書き込みエラーが `write関数` の時点で分からず、後の `fsync関数` や `close関数` で見えることがあります。
そのため、保存結果が重要なプログラムでは `close関数` のエラーチェックを怠るべきではありません。

```c
if (close(fd) < 0) {
    ペラー("close");
    イグジット(1);
}
```

`EINTR` については少し注意が必要です。
POSIXでは、`close関数` が `EINTR` を返すことを許しています。
しかし、`close関数` が `EINTR` になったからといって、単純に同じfd番号へもう一度 `close関数` するのは危険です。
fd番号がすでにとじられ、別の `オープン関数` で再利用されている可能性があるからです。

```text
close関数 が EINTR
    fdがとじられたかどうかを移植性だけで単純判断しにくい

あんいな再close
    再利用された別のfdをとじる危険がある
```

Linuxでは、`close関数` がエラーを返した場合でも、fd自体は基本的にとじられたものとして扱うのが実用的です。
エラーは「とじる処理の後で検出されたあいおーエラー」として扱い、同じfd番号を再度 `close関数` しない方が安全です。

#### ２章の７の６　close関数 の要点

ここまでの `close関数` の要点をまとめます。

```text
close(fd)
    エフディー対応表からfdを外す

fd番号
    close後に再利用される可能性がある

複数参照
    最後の参照がとじられるまで、カーネル内の状態は残る

unlink済みファイル
    名前がきえても、ひらいているfdがあれば実体は残る

保存完了
    close関数 は fsync関数 の代わりではない

エルノー
    EBADF、EIO、EINTR などに注意する
```

UmuOSの視点では、`close関数` はfd管理の出口です。
エフディー対応表から番号を外すだけでなく、参照数、アイノード、削除済みファイル、遅延書き込みエラーまで関係します。
ウーシュのパイプ処理でも、不要なfdを正しくとじることがエンドオブファイル伝播やプロセス終了に直結します。

### ２章の８　ファイルシーク: エルシーク関数

通常、ファイルあいおーは現在のファイルポジションから順番に行われます。
`read関数` すれば読んだ分だけ進み、`write関数` すれば書いた分だけ進みます。

しかし、アプリケーションによっては、ファイル内の任意の位置へ移動したいことがあります。
このとき使うのが `エルシーク関数` です。

宣言は次のようになります。

```c
#include <sys/types.h>
#include ユニストディードットエイチ。

off_t エルシーク(int fd, off_t offset, int whence);
```

古い資料では第2引数を `pos`、第3引数を `origin` と書いていることもあります。
現在のmanページでは、`offset` と `whence` という名前で説明されることが多いです。

ここで出てくる `off_t エルシーク(...)` も、実行コードではなくプロトタイプ宣言です。
`エルシーク関数` は移動後のファイルポジションを返します。
失敗すると `(off_t)-1` を返し、`エルノー` に理由を設定します。

```text
成功
    移動後のファイルポジション

失敗
    (off_t)-1
    エルノー に理由が入る
```

#### ２章の８の１　SEEK_SET、SEEK_CUR、SEEK_END

`エルシーク関数` の第3引数 `whence` には、どこを基準に移動するかを指定します。

```text
SEEK_SET
    ファイル先頭を基準にする

SEEK_CUR
    現在のファイルポジションを基準にする

SEEK_END
    ファイル末尾を基準にする
```

ファイル先頭から1825バイト目へ移動する例です。

```c
#include <fcntl.h>
#include <inttypes.h>
#include <エスティーディーio.h>
#include <エスティーディーlib.h>
#include <sys/types.h>
#include ユニストディードットエイチ。

int main(void) {
    off_t pos;
    int fd;

    fd = オープン("メモどっとテキスト", オー・リードオンリー | オー・クローズエグゼック);

    if (fd < 0) {
        ペラー("オープン");
        イグジット(1);
    }

    pos = エルシーク(fd, (off_t)1825, SEEK_SET);

    if (pos == (off_t)-1) {　　　　　　　　　//off_tとの比較だからキャストして-1のエラーか確認
        ペラー("エルシーク");
        close(fd);
        イグジット(1);
    }

    プリントエフ("new position: %jd\n", (intmax_t)pos);

    if (close(fd) < 0) {
        ペラー("close");
        イグジット(1);
    }

    return 0;
}
```

ファイル末尾へ移動する例です。

```c
off_t pos;

pos = エルシーク(fd, 0, SEEK_END);

if (pos == (off_t)-1) {
    ペラー("エルシーク");
}
```

現在位置を知りたい場合は、`SEEK_CUR` と `0` を使います。

```c
off_t pos;

pos = エルシーク(fd, 0, SEEK_CUR);

if (pos == (off_t)-1) {
    ペラー("エルシーク");
} else {
    プリントエフ("current position: %jd\n", (intmax_t)pos);
}
```

この例で `%jd` と `(intmax_t)` を使っているのは、`off_t` の具体的な型が環境によって違う可能性があるためです。
ここでは `<inttypes.h>` をインクルードしています。
学習段階では、`off_t` はファイル位置をひょうすための型、と押さえれば十分です。

#### ２章の８の２　エルシーク関数 は実際のあいおーをしない

`エルシーク関数` はファイルポジションを変更するだけです。
データを読み書きするわけではありません。

```text
エルシーク関数
    ファイルポジションを移動する
    実際のread/writeは行わない
```

そのため、ファイル末尾へ移動しただけでは、ファイルサイズは変わりません。
ファイルサイズを超えた位置へ移動しても、その時点ではディスク上にデータ領域が作られるわけではありません。

#### ２章の８の３　ファイルサイズを超えたシークとホール

`エルシーク関数` では、ファイルサイズを超えた位置へ移動できます。
その後、その位置へ `write関数` すると、元のファイル末尾から書き込み位置までの間に穴ができます。
この穴をホール、英語では hole と呼びます。

ホールを持つファイルを、穴空きファイル、または sparse ファイル と呼びます。

```text
元のファイルサイズ
    100バイト

エルシーク(fd, 1000, SEEK_SET)
    1000バイト目へ移動

write(fd, "A", 1)
    1000バイト目に1バイト書く

結果
    100バイト目から999バイト目まではホールになる
```

ホール部分を読むと、0で埋まっているように見えます。
しかし、ファイルシステムはその0の領域へ物理ブロックを割り当てないことがあります。
そのため、見かけのファイルサイズよりも、実際に消費するディスク容量が小さくなります。

```text
sparse ファイル
    見かけのサイズは大きい
    しかしホール部分には物理ブロックを割り当てない
```

簡単な例です。

```c
#include <fcntl.h>
#include <エスティーディーio.h>
#include <エスティーディーlib.h>
#include ユニストディードットエイチ。

int main(void) {
    int fd;

    fd = オープン("sparse.bin", オー・ライトオンリー | オー・クリエイト | オー・トランク | オー・クローズエグゼック, 0644);

    if (fd < 0) {
        ペラー("オープン");
        イグジット(1);
    }

    if (エルシーク(fd, 1024 * 1024, SEEK_SET) == (off_t)-1) {
        ペラー("エルシーク");
        close(fd);
        イグジット(1);
    }

    if (write(fd, "X", 1) < 0) {
        ペラー("write");
        close(fd);
        イグジット(1);
    }

    if (close(fd) < 0) {
        ペラー("close");
        イグジット(1);
    }

    return 0;
}
```

このプログラムは、1MiB先へ移動してから1バイトだけ書きます。
ファイルの見かけのサイズは大きくなりますが、ホール部分は物理領域を消費しない場合があります。

確認するときは、`ls -l` と `du` の結果が違うことがあります。

```text
ls -l
    見かけのファイルサイズを見る

du
    実際に割り当てられたディスク使用量を見る
```

UmuOSでファイルシステムを作る場合、sparse ファイルをサポートするかどうかは設計判断です。
最初はホールをサポートせず、ファイルサイズを超えた書き込みでは間を0で実際に埋める単純実装でもよいかもしれません。
しかし、Unixらしいファイルシステムを目さししめすなら、ホールと物理ブロック割り当てを分けて考える必要があります。

#### ２章の８の４　シークできないfd

すべてのfdが `エルシーク関数` できるわけではありません。
通常ファイルはシークできますが、パイプ、ファイフォ、ソケットなどはシークできません。

```text
シークできることが多い
    通常ファイル
    ブロックデバイス

シークできない
    パイプ
    ファイフォ
    ソケット
    端末など一部のキャラクタデバイス
```

シークできないfdに `エルシーク関数` すると、`-1` が返り、`エルノー` が `ESパイプ（イーエスパイプ）` になることがあります。
ESパイプは、パイプに対してシークしたときのエラーです。

```text
ESパイプ
    illegal seek
    パイプやソケットなど、シークできない対象にエルシーク関数した
```

ウーシュのパイプ処理では、この違いが重要です。
`cmd < ファイル.txt` の入力元は通常ファイルなのでシークできる可能性があります。
一方、`cmd1 | cmd2` の `cmd2` の標準入力はパイプなので、基本的にシークできません。

#### ２章の８の５　エルシーク関数 の エルノー

`エルシーク関数` が失敗すると `(off_t)-1` を返し、`エルノー` に理由を設定します。
代ひょう的なものは次の通りです。

```text
EBADF
    fdが無効

EINVAL
    whenceがSEEK_SET、SEEK_CUR、SEEK_ENDのいずれでもない
    または移動後のファイルポジションが負になる

EOVERFLOW
    移動後のファイルポジションをoff_tでひょう現できない

ESパイプ
    fdがパイプ、ファイフォ、ソケットなどでシークできない
```

`EINVAL` は少しややこしいです。
`whence` の指定ミスはプログラミングエラーです。
一方、移動後のファイルポジションが負になるのは、実行時の値によって起こる論理エラーです。
同じ `EINVAL` でも原因が違うため、デバッグ時には呼び出し引数をよく見る必要があります。

`EOVERFLOW` は、特に32ビット環境や古いビルド設定で大きなファイルを扱うときに関係します。
現代の64ビットLinuxでは、普通の用途で意識する場面はかなり減っています。
ただし、移植性を考えるなら、ファイル位置には `off_t` を使い、巨大ファイル対応では `ファイルオフセットビット64` のような設定も理解しておくとよいです。

#### ２章の８の６　エルシーク関数 の制限と現代的な補足

ファイルポジションは `off_t` でひょうされます。
`off_t` がひょう現できる範囲を超える位置へ移動しようとすると問題になります。

古い32ビット環境では、`off_t` が32ビットだと2GiB付近の制限が問題になりました。
現在の64ビットLinuxでは、`off_t` は通常64ビットで、大きなファイルを扱えます。

```text
現代の64ビットLinux
    通常は大きなファイルを扱いやすい

古い32ビット環境
    off_tのサイズやファイルオフセットビット64が問題になることがある
```

このノートでは、まず現代の64ビットLinuxを前提に理解します。
そのうえで、古い資料に出てくる `EOVERFLOW` や大きなファイル制限は、32ビット時代の互換性問題として押さえると整理しやすいです。

#### ２章の８の７　エルシーク関数 の要点

ここまでの `エルシーク関数` の要点をまとめます。

```text
エルシーク関数
    ファイルポジションを移動する
    実際のあいおーは行わない

SEEK_SET
    ファイル先頭を基準にする

SEEK_CUR
    現在位置を基準にする

SEEK_END
    ファイル末尾を基準にする

ファイルサイズ超えのシーク
    その時点ではファイルサイズは変わらない
    その後write関数するとホールができることがある

sparse ファイル
    ホールを持つファイル
    見かけのサイズと実使用量が違うことがある

ESパイプ
    パイプ、ファイフォ、ソケットなどシークできないfdで起こる
```

UmuOSの視点では、`エルシーク関数` はファイルポジション管理そのものです。
fdごとの現在位置、ファイルサイズ、ホール、シーク不能なfdをどうひょうすかは、ファイルあいおー設計の重要な部品になります。
ウーシュでも、通常ファイルとパイプを同じfdとして扱いつつ、「シークできるかどうか」は対象ごとに分かれる、という理解が大事です。

### ２章の９　ファイルポジション指定あいおー: pread関数 と pwrite関数

`read関数` と `write関数` は、fdが持っている現在のファイルポジションを使って読み書きします。
通常ファイルでは、読み書きが成功すると、そのぶんファイルポジションも進みます。

一方で、特定の位置から読み書きしたいけれど、fdの現在位置は動かしたくない、という場面があります。
このために用意されているのが `pread関数` と `pwrite関数` です。

```text
read関数 / write関数
    fdの現在のファイルポジションを使う
    成功するとファイルポジションが進む

pread関数 / pwrite関数
    呼び出し時に指定したoffsetを使う
    fdの現在のファイルポジションは動かさない
```

#### ２章の９の１　pread関数

`pread関数` は、指定したファイル位置から読み取る関数です。

宣言は次のようになります。

```c
#define _Xオープン_SOURCE 500
#include ユニストディードットエイチ。

エスサイズティー pread(int fd, void *buf, size_t count, off_t offset);
```

ここで出てくる `エスサイズティー pread(...)` も、実行コードではなくプロトタイプ宣言です。
`pread関数` は、`fd` がさししめすファイルの `offset` 位置から、最大 `count` バイトを `buf` へ読み取ります。

```text
fd
    読み取り対象のファイルディスクリプタ

buf
    読み取ったデータを入れるバッファ

count
    最大で何バイト読むか

offset
    読み取りを開始するファイル位置
```

もどりち、は `read関数` と同じ考え方です。

```text
正の値
    実際に読み取ったバイト数

0
    エンドオブファイル

-1
    エラー
    エルノー に理由が入る
```

#### ２章の９の２　pwrite関数

`pwrite関数` は、指定したファイル位置へ書き込む関数です。

宣言は次のようになります。

```c
#define _Xオープン_SOURCE 500
#include ユニストディードットエイチ。

エスサイズティー pwrite(int fd, const void *buf, size_t count, off_t offset);
```

`pwrite関数` は、`fd` がさししめすファイルの `offset` 位置へ、`buf` から最大 `count` バイトを書き込みます。

```text
fd
    書き込み対象のファイルディスクリプタ

buf
    書き込みたいデータが入っているバッファ

count
    最大で何バイト書くか

offset
    書き込みを開始するファイル位置
```

もどりち、は `write関数` と同じ考え方です。
成功時は実際に書き込んだバイト数、失敗時は `-1` です。
`pwrite関数` でも部分書き込みは起こり得るため、重要なデータではもどりち、を確認する必要があります。

#### ２章の９の３　エルシーク関数 + read/write との違い

`pread関数` は、考え方としては `エルシーク関数` で位置を移動してから `read関数` する処理に近いです。
`pwrite関数` も、`エルシーク関数` で位置を移動してから `write関数` する処理に近いです。

しかし、完全に同じではありません。

```text
エルシーク関数 + read関数
    fdのファイルポジションを変更する
    その後read関数する

pread関数
    指定したoffsetから読む
    fdのファイルポジションは変更しない
```

この違いは、マルチスレッドや共有fdでとても重要になります。
複数のスレッドが同じfdを共有している場合、次のような競合が起こり得ます。

```text
スレッドA
    エルシーク(fd, 100, SEEK_SET)

スレッドB
    エルシーク(fd, 200, SEEK_SET)

スレッドA
    read(fd, buf, len)
    本当は100から読みたかったのに、200から読んでしまうかもしれない
```

`pread関数` なら、位置指定と読み取りが1回の操作として扱われるため、この競合を避けやすくなります。

```c
#define _Xオープン_SOURCE 500

#include <fcntl.h>
#include <エスティーディーio.h>
#include <エスティーディーlib.h>
#include ユニストディードットエイチ。

int main(void) {
    char buf[16];
    int fd;
    エスサイズティー n;

    fd = オープン("メモどっとテキスト", オー・リードオンリー | オー・クローズエグゼック);

    if (fd < 0) {
        ペラー("オープン");
        イグジット(1);
    }

    n = pread(fd, buf, sizエンドオブファイル(buf), 32);

    if (n < 0) {
        ペラー("pread");
        close(fd);
        イグジット(1);
    }

    if (n > 0 && write(エスティーディーアウトファイルナンバー, buf, (size_t)n) < 0) {
        ペラー("write");
        close(fd);
        イグジット(1);
    }

    if (close(fd) < 0) {
        ペラー("close");
        イグジット(1);
    }

    return 0;
}
```

この例では、ファイルの32バイト目から最大16バイトを読み取ります。
読み取り後も、fdの現在のファイルポジションは変更されません。

#### ２章の９の４　pread関数 / pwrite関数 の注意点

`pread関数` と `pwrite関数` は、シーク可能なファイルで使うものです。
パイプ、ファイフォ、ソケットのようにファイルポジションを持たない対象では使えません。

また、`read関数` / `write関数` と `pread関数` / `pwrite関数` を同じfdで混ぜると、設計が分かりにくくなることがあります。
`pread関数` / `pwrite関数` はファイルポジションを動かしませんが、`read関数` / `write関数` は動かします。
どちらの規則でファイルを扱っているのかが曖昧になると、意図しない場所を読んだり書いたりする原因になります。

```text
混在に注意
    read関数 / write関数 は現在位置を使う
    pread関数 / pwrite関数 はoffsetを使う
    同じfdで混ぜるなら、設計意図を明確にする
```

`pread関数` の `エルノー` は、だいたい `read関数` と `エルシーク関数` の失敗理由を合わせたものと考えられます。
`pwrite関数` の `エルノー` は、`write関数` と `エルシーク関数` の失敗理由を合わせたものと考えられます。

```text
pread関数
    read関数 系のエラー
    エルシーク関数 系のエラー

pwrite関数
    write関数 系のエラー
    エルシーク関数 系のエラー
```

UmuOSの視点では、`pread関数` / `pwrite関数` は「fdの現在位置」と「明示的に指定した位置」を分ける設計です。
通常の `read関数` / `write関数` だけでも最初のOSは作れますが、マルチスレッドやデータベース的なアクセスを考えると、位置指定あいおーの考え方は重要になります。

### ２章の１０　ファイルトランケート

ファイルサイズを指定した長さへ変更する操作を、トランケートと呼びます。
英語では truncate です。

Linuxでは、パス名で指定する `truncate関数` と、fdで指定する `ftruncate関数` があります。

```c
#include <sys/types.h>
#include ユニストディードットエイチ。

int truncate(const char *path, off_t length);
int ftruncate(int fd, off_t length);
```

ここで出てくる宣言もプロトタイプ宣言です。
どちらも成功すると `0`、失敗すると `-1` を返し、`エルノー` に理由を設定します。

```text
truncate(path, length)
    パス名で指定したファイルのサイズを変更する

ftruncate(fd, length)
    fdで指定したファイルのサイズを変更する
```

`ftruncate関数` を使う場合、fdは通常、書き込み可能な状態でひらいておく必要があります。
`truncate関数` を使う場合も、対象ファイルへ書き込みできる権限が必要です。

#### ２章の１０の１　ファイルを小さくする

`truncate関数` や `ftruncate関数` は、ファイルを小さくする用途でよく使われます。
指定した `length` より後ろにあったデータは捨てられ、以後読み取れなくなります。

```text
元のファイル
    100バイト

truncate(path, 40)
    ファイルサイズは40バイトになる
    40バイト目より後ろのデータは失われる
```

例です。

```c
#include <エスティーディーio.h>
#include <エスティーディーlib.h>
#include ユニストディードットエイチ。

int main(void) {
    if (truncate("pirate.txt", 45) < 0) {
        ペラー("truncate");
        イグジット(1);
    }

    return 0;
}
```

このコードを実行すると、`pirate.txt` のサイズは45バイトになります。
それより後ろにあったデータは削除されます。

#### ２章の１０の２　ファイルを大きくする

名前はトランケートですが、指定した長さが現在のファイルサイズより大きい場合は、ファイルを大きくすることもできます。
拡張された部分は、読み取ると0として見えます。

```text
元のファイル
    100バイト

ftruncate(fd, 1000)
    ファイルサイズは1000バイトになる
    100バイト目から999バイト目までは0として読める
```

この拡張部分は、ファイルシステムによってはホールとして扱われ、物理ブロックがすぐには割り当てられないことがあります。
つまり、`エルシーク関数` でファイルサイズを超えた位置へ移動してから書いた場合と同じく、sparse ファイルに関係します。

#### ２章の１０の３　ファイルポジションは動かない

`truncate関数` と `ftruncate関数` は、ファイルサイズを変更します。
しかし、fdの現在のファイルポジションは変更しません。

```text
ftruncate(fd, length)
    ファイルサイズを変更する
    fdの現在位置は変更しない
```

そのため、現在位置がファイル末尾より後ろにある状態も起こり得ます。
その後に `write関数` すると、ホールを作る形で書き込まれることがあります。

#### ２章の１０の４　ftruncate関数 の例

すでにひらいているfdに対して使うなら、`ftruncate関数` が自然です。

```c
#include <fcntl.h>
#include <エスティーディーio.h>
#include <エスティーディーlib.h>
#include ユニストディードットエイチ。

int main(void) {
    int fd;

    fd = オープン("メモどっとテキスト", オー・ライトオンリー | オー・クローズエグゼック);

    if (fd < 0) {
        ペラー("オープン");
        イグジット(1);
    }

    if (ftruncate(fd, 0) < 0) {
        ペラー("ftruncate");
        close(fd);
        イグジット(1);
    }

    if (close(fd) < 0) {
        ペラー("close");
        イグジット(1);
    }

    return 0;
}
```

この例では、`メモどっとテキスト` のサイズを0にしています。
`オー・トランク` を付けて `オープン関数` するのと似ていますが、`ftruncate関数` はひらいた後の任意のタイミングでサイズを変更できる点が違います。

UmuOSの視点では、トランケートはファイルサイズとブロック割り当てを変更する操作です。
単に アイノード のサイズを書き換えるだけではなく、縮小時には不要になったブロックを解放し、拡張時にはホールとして扱うか、実際に0で埋めるかを
設計する必要があります。

### ２章の１１　あいおーの多重化

ここまでの `read関数` や `write関数` は、基本的に1つのfdへ対して処理を行うものでした。
しかし実用的なプログラムでは、複数のfdを同時に扱いたい場面がよくあります。

```text
複数fdを扱う例

標準入力
    キーボードからの入力

パイプ
    別プロセスとの通信

ソケット
    ネットワーク接続

端末
    ユーザーとの対話
```

問題は、1つのfdでブロックしてしまうと、他のfdを処理できなくなることです。
たとえば、ネットワークソケットからの入力待ちで止まっている間に、標準入力へユーザーが文字を打っても、プログラムはそれに反応できません。

この問題を解くために使うのが、あいおーの多重化です。
複数のfdをまとめて監視し、どれかが読み書き可能になったら起きて処理します。

```text
あいおー多重化の基本

1. 複数のfdを監視対象にする
2. どれかがあいおー可能になるまで待つ
3. あいおー可能になったfdを調べる
4. そのfdに対してread関数やwrite関数を行う
5. また監視へ戻る
```

ノンブロッキングあいおーだけでもブロックは避けられます。
しかし、データが来ていないfdへ何度も `read関数` して `イーアゲイン` を受け取り続ける設計は、CPUを無駄に使います。
あいおー多重化を使うと、カーネルに「どれかが使えるようになるまで寝かせて」と頼めます。

Linuxでは、代ひょう的に次の仕組みがあります。

```text
select関数
    古くからある、移植性の高い多重あいおー

ポール関数
    fd集合を配列で渡せる多重あいおー

イーポール関数
    Linux固有の高性能な多重あいおー

アイオー・ユーリング”
    さらに新しい非同期あいおー基盤
```

この章では、まず古典的で理解しやすい `select関数` と `ポール関数` を見ます。
現代Linuxで多数のfdを本格的に扱うなら、`イーポール関数` や `アイオー・ユーリング”` も候補になりますが、それらはさらに高度な話として分けて考えるのがよいです。

#### ２章の１１の１　select関数

`select関数` は、複数のfdの状態を調べ、どれかがあいおー可能になるまで待つための関数です。

宣言は次のようになります。

```c
#include <sys/select.h>

int select(int nfds,
           fd_set *readfds,
           fd_set *writefds,
           fd_set *exceptfds,
           struct timeval *timeout);
```

`fd_set` を操作するためのマクロも使います。

```c
FD_ZERO(fd_set *set);
FD_SET(int fd, fd_set *set);
FD_CLR(int fd, fd_set *set);
FD_ISSET(int fd, fd_set *set);
```

意味は次の通りです。

```text
readfds
    読み取り可能になったかを調べるfd集合

writefds
    書き込み可能になったかを調べるfd集合

exceptfds
    例外状態を調べるfd集合
    主にソケットの帯域外データなど

timeout
    最大でどれだけ待つか
    NULLなら無期限に待つ

nfds
    監視するfdの最大値 + 1
```

`select関数` の少し面倒な点は、`nfds` に「監視対象の最大fd番号 + 1」を渡すことです。
たとえば、fd 7 と fd 9 を監視するなら、`nfds` は `10` です。

また、`select関数` は戻ると `fd_set` の中身を書き換えます。
あいおー可能になったfdだけが集合に残ります。
そのため、ループで使う場合は、毎回 `FD_ZERO関数` と `FD_SET関数` で集合を作り直すのが基本です。

#### ２章の１１の２　select関数 の例

次は、標準入力からの入力を最大5秒待つ例です。
fdは1つだけなので本当の意味での多重化ではありませんが、`select関数` の使い方を確認するには十分です。

```c
#include <エルノー.h>
#include <エスティーディーio.h>
#include <エスティーディーlib.h>
#include <sys/select.h>
#include ユニストディードットエイチ。

#define TIMEOUT_SEC 5
#define BUF_LEN 1024

int main(void) {
    char buf[BUF_LEN + 1];
    fd_set readfds;
    struct timeval timeout;
    int ret;
    エスサイズティー n;

    FD_ZERO(&readfds);
    FD_SET(エスティーディーイン_ファイルNO, &readfds);

    timeout.tv_sec = TIMEOUT_SEC;
    timeout.tv_usec = 0;

    ret = select(エスティーディーイン_ファイルNO + 1, &readfds, NULL, NULL, &timeout);

    if (ret < 0) {
        if (エルノー == EINTR) {
            エフプリントエフ(エスティーディーエラー, "select was interrupted\n");
            return 1;
        }

        ペラー("select");
        return 1;
    }

    if (ret == 0) {
        プリントエフ("timeout\n");
        return 0;
    }

    if (FD_ISSET(エスティーディーイン_ファイルNO, &readfds)) {
        n = read(エスティーディーイン_ファイルNO, buf, BUF_LEN);

        if (n < 0) {
            ペラー("read");
            return 1;
        }

        buf[n] = '\0';
        プリントエフ("read: %s", buf);
    }

    return 0;
}
```

`select関数` が正の値を返したら、少なくとも1つのfdがあいおー可能です。
この例では `FD_ISSET(エスティーディーイン_ファイルNO, &readfds)` で、標準入力が読み取り可能として返されたかを確認しています。

注意点として、`select関数` が「読める」と返しても、その後の `read関数` が絶対に永久にブロックしないと雑に考えすぎない方がよいです。
実用コードでは、対象や設計によってノンブロッキングあいおーと組み合わせることがあります。

#### ２章の１１の３　select関数 の timeout と制限

`select関数` の `timeout` には `struct timeval` を渡します。

```c
struct timeval {
    long tv_sec;
    long tv_usec;
};
```

```text
tv_sec
    秒

tv_usec
    マイクロ秒
```

`timeout` に `NULL` を渡すと、どれかのfdがあいおー可能になるまで無期限に待ちます。
`tv_sec` と `tv_usec` を両方0にすると、待たずに状態だけ確認して戻ります。

古いコードでは、fdを何も渡さず、`select(0, NULL, NULL, NULL, &timeout)` として短時間スリープに使う例があります。
これは歴史的にはよく使われましたが、現代Linuxでは `nanosleep関数`、`clock_nanosleep関数` など、時間用のAPIを使う方が意図が明確です。

`select関数` には `FD_SETSIZE` の制限もあります。
Linux/glibcでは典型的に1024です。
大きなfd番号や大量のfdを扱うプログラムでは、`select関数` は不向きです。

```text
select関数 の弱点
    FD_SETSIZE の制限
    最大fd + 1 を自分で計算する必要がある
    fd_set を毎回作り直す必要がある
```

#### ２章の１１の４　select関数 のもどりち、と エルノー

`select関数` のもどりち、は次の通りです。

```text
正の値
    あいおー可能になったfdの数

0
    timeout

-1
    エラー
    エルノー に理由が入る
```

代ひょう的な `エルノー` は次の通りです。

```text
EBADF
    fd集合の中に無効なfdがある

EINTR
    シグナルで割り込まれた

EINVAL
    nfdsが負数、またはtimeoutが不正

ENOMEM
    内部処理に必要なメモリが不足した
```

`EINTR` は実用上よく意識します。
シグナルを受けたら終了したいのか、もう一度待ち直したいのかを、プログラムの設計として決める必要があります。

#### ２章の１１の５　pselect関数

`pselect関数` は、`select関数` に似ていますが、主にシグナルとの競合を避けるために使えるAPIです。

```c
#include <signal.h>
#include <sys/select.h>

int pselect(int nfds,
            fd_set *readfds,
            fd_set *writefds,
            fd_set *exceptfds,
            const struct timespec *timeout,
            const sigset_t *sigmask);
```

`select関数` との主な違いは次の通りです。

```text
timeout
    select関数 は timeval
    pselect関数 は timespec

timeoutの変更
    select関数 は実装によって変更されることがある
    pselect関数 は変更しない

sigmask
    pselect関数 は待っている間のシグナルマスクを指定できる
```

`pselect関数` の重要な目的は、シグナルハンドラとあいおー待ちの間に起こる競合を避けることです。
この話はシグナルの章とつながります。
最初は、`pselect関数` は「select関数 にシグナルマスク制御を足したもの」と理解すればよいです。

古いLinuxでは `pselect関数` がglibc側のラッパで実装されていた時期がありますが、現在のLinuxではカーネル側にも実装があります。
古い資料の注意点は、歴史的背景として読めば十分です。

#### ２章の１１の６　ポール関数

`ポール関数` は、`select関数` と同じく複数fdを待つためのAPIです。
`select関数` が `fd_set` を使うのに対し、`ポール関数` は `struct pollfd` の配列を使います。

宣言は次のようになります。

```c
#include <poll.h>

int poll(struct pollfd *fds, nfds_t nfds, int timeout);
```

`struct pollfd` は次のような形です。

```c
struct pollfd {
    int fd;
    short events;
    short revents;
};
```

```text
fd
    監視したいファイルディスクリプタ

events
    アプリケーションが監視したいイベント

revents
    実際に発生したイベント
    ポール関数 から戻ったあとにカーネルが設定する
```

よく使うイベントは次の通りです。

```text
POLLIN
    読み取り可能

POLLOUT
    書き込み可能

POLLPRI
    優先データや緊急データがある

POLLERR
    エラーが発生した

POLLHUP
    ハングアップした

POLLNVAL
    fdが無効
```

`POLLERR`、`POLLHUP`、`POLLNVAL` は、通常 `events` に指定して待つものではなく、`revents` に結果として返る状態です。

`timeout` はミリ秒単位です。

```text
timeout < 0
    無期限に待つ

timeout == 0
    待たずに状態だけ確認する

timeout > 0
    指定ミリ秒まで待つ
```

#### ２章の１１の７　ポール関数 の例

次は、標準入力が読み取り可能か、標準出力が書き込み可能かを同時に調べる例です。

```c
#include <poll.h>
#include <エスティーディーio.h>
#include <エスティーディーlib.h>
#include ユニストディードットエイチ。

#define TIMEOUT_MS 5000

int main(void) {
    struct pollfd fds[2];
    int ret;

    fds[0].fd = エスティーディーイン_ファイルNO;
    fds[0].events = POLLIN;
    fds[0].revents = 0;

    fds[1].fd = エスティーディーアウトファイルナンバー;
    fds[1].events = POLLOUT;
    fds[1].revents = 0;

    ret = poll(fds, 2, TIMEOUT_MS);

    if (ret < 0) {
        ペラー("poll");
        イグジット(1);
    }

    if (ret == 0) {
        プリントエフ("timeout\n");
        return 0;
    }

    if (fds[0].revents & POLLIN) {
        プリントエフ("エスティーディーイン is readable\n");
    }

    if (fds[1].revents & POLLOUT) {
        プリントエフ("エスティーディーアウト is writable\n");
    }

    if (fds[0].revents & (POLLERR | POLLHUP | POLLNVAL)) {
        プリントエフ("エスティーディーイン has an error-like event: 0x%x\n", fds[0].revents);
    }

    return 0;
}
```

多くの環境では、標準出力はすぐ書き込み可能なので、`エスティーディーアウト is writable` が表示されやすいです。
標準入力をファイルからリダイレクトした場合は、標準入力も読み取り可能として返ることがあります。

#### ２章の１１の８　ポール関数 のもどりち、と エルノー

`ポール関数` のもどりち、は次の通りです。

```text
正の値
    revents に何らかのイベントが入ったfdの数

0
    timeout

-1
    エラー
    エルノー に理由が入る
```

代ひょう的な `エルノー` は次の通りです。

```text
EFAULT
    fdsが不正なアドレスを指している

EINTR
    シグナルで割り込まれた

EINVAL
    nfdsが大きすぎるなど、指定が不正

ENOMEM
    内部処理に必要なメモリが不足した
```

古い資料では `EBADF` が `ポール関数` 自体の `エルノー` として説明されることがあります。
現在のLinuxでは、無効なfdは通常、`ポール関数` の `-1` ではなく、その要素の `revents` に `POLLNVAL` として返ります。
ここは `select関数` との違いとして重要です。

#### ２章の１１の９　pポール関数

`pポール関数` は、`ポール関数` に `pselect関数` と同じ方向の改良を加えたLinux系のAPIです。
タイムアウトに `struct timespec` を使い、待っている間のシグナルマスクを指定できます。

```c
#define _GNU_SOURCE
#include <signal.h>
#include <poll.h>

int ppoll(struct pollfd *fds,
          nfds_t nfds,
          const struct timespec *timeout,
          const sigset_t *sigmask);
```

通常の移植性重視なら `ポール関数`、シグナルとの競合をきちんと扱いたいLinuxコードなら `pポール関数` も候補になります。

#### ２章の１１の１０　select関数 と ポール関数 の比較

`select関数` と `ポール関数` は、どちらも複数fdのあいおー可能状態を待つためのAPIです。
基本的な目的は同じですが、使い勝手と制限が違います。

```text
select関数
    古くからあり移植性が高い
    fd_setを使う
    FD_SETSIZEの制限がある
    最大fd + 1を渡す必要がある
    戻るとfd_setが変更される

ポール関数
    pollfd配列を使う
    fd番号が大きくても扱いやすい
    eventsとreventsが分かれている
    select関数より扱いやすい場面が多い
```

ただし、現代Linuxで大量のfdを効率よく扱うなら、`select関数` や `ポール関数` より `イーポール関数` を検討することが多いです。
それでも、`select関数` と `ポール関数` は古いコードを読むうえで頻出しますし、あいおー多重化の基本概念を学ぶにはとてもよい題材です。

UmuOSやウーシュの視点では、あいおー多重化は将来的にかなり重要です。
対話シェル、ジョブ制御、パイプ、疑似端末、複数プロセスとの通信を扱うとき、1つのfdで止まって全体が固まる設計はさけたいからです。
最初はブロッキングあいおーで十分でも、ウーシュを本格的な対話シェルへ育てるなら、`ポール関数` や `イーポール関数` 的な待ち合わせ機構が必要になります。

### ２章の１２　カーネル内の動作

ここまで、ユーザープログラムから見える `オープン関数`、`read関数`、`write関数`、`close関数`、`エルシーク関数` などを見てきました。
この節では、これらのあいおーがLinuxカーネル内部でどのように支えられているのかを整理します。

特に重要なのは、次の3つです。

```text
VFS
    仮想ファイルシステム
    さまざまなファイルシステムを共通の形で扱う層

ページキャッシュ
    ファイルデータをメモリ上にキャッシュする仕組み

ページライトバック
    dirtyになったページをストレージへ書き戻す仕組み
```

この3つが協調することで、Linuxは通常ファイル、ディレクトリ、デバイス、procfs、tmpfs、ext4、XFS、NFSなどを、
かなり統一的なインターフェースで扱えます。

UmuOSの視点では、この節はかなり大事です。
なぜなら、ここで出てくるVFS、アイノード、dentry、ページキャッシュ、dirtyページ、writebackは、OSのファイルあいおー設計そのものだからです。

#### ２章の１２の１　全体像

アプリケーションが `read関数` を呼んだとき、単純にディスクからデータを読んでいるだけではありません。
大まかには、次のような経路を通ります。

```text
アプリケーション
    read(fd, buf, count)

Cライブラリ
    glibcなどのread関数ラッパ

カーネル
    システムコール入口
    fdからひらいているファイルを探す
    VFSで共通処理を行う
    対象ファイルシステムの処理へ渡す
    ページキャッシュを確認する
    必要ならストレージから読む

アプリケーション
    bufにデータが入って戻る
```

古い説明では、Cライブラリが「システムコールの割り込み命令を生成する」と説明されることがあります。
現在のLinux、特にx86-64では、通常は `syscall` 命令を使ってカーネルへ入ります。
ただし、アプリケーションから見ると、基本は libc の `read関数` ラッパを呼び、その先でカーネルの `read` 系システムコールが実行される、
と理解すれば十分です。

#### ２章の１２の２　VFS

VFSは virtual ファイルsystem の略です。
日本語では、仮想ファイルシステム、VFSレイヤなどと呼ばれます。

VFSの役割は、具体的なファイルシステムの違いを隠し、カーネルやユーザープログラムが統一的な形でファイルを扱えるようにすることです。

Linuxでは、ext4、XFS、Btrfs、tmpfs、procfs、sysfs、NFS、FAT、NTFSなど、さまざまなファイルシステムを扱えます。
しかし、ユーザープログラムは基本的に同じ `オープン関数`、`read関数`、`write関数`、`close関数` を使います。

```text
ユーザープログラム
    オープン関数
    read関数
    write関数
    close関数

VFS
    共通のファイル操作として受け取る

具体的なファイルシステム
    ext4
    XFS
    tmpfs
    procfs
    NFS
```

この統一を可能にしているのがVFSです。

#### ２章の１２の３　VFSの主なオブジェクト

VFSは、いくつかの抽象的なオブジェクトを使ってファイルシステムをひょうします。
代ひょう的なものは次の通りです。

```text
superblock
    ファイルシステム全体をひょうす情報
    マウントされた1つのファイルシステムに関係する

アイノード
    ファイルの実体や属性をひょうす情報
    ファイルサイズ、権限、所有者、時刻、データブロックへの対応など

dentry
    directory entry の略
    ファイルメイとアイノードの対応をひょうす

ファイル
    オープン関数されたファイルをひょうすカーネル内オブジェクト
    ファイルポジションやオープン時のフラグなどを持つ
```

ここで、初心者が混乱しやすい点があります。
ファイルメイとアイノードは同じではありません。

```text
ファイルメイ
    ディレクトリ内の名前
    dentryが関係する

アイノード
    ファイルの実体や属性

オープンされたファイル
    プロセスが実際に操作しているひらいたファイル状態
```

この分離があるため、同じアイノードに複数の名前を付けるハードリンクが可能になります。
また、`unlink関数` で名前がきえても、ひらいているfdが残っていればファイルの実体が残る、というUnixらしい動作も説明できます。

UmuOSを作る場合も、最初は単純化してよいですが、少なくとも次の区別は意識した方がよいです。

```text
パス名
    ユーザーが指定する文字列

ディレクトリエントリ
    名前とファイル実体の対応

アイノード相当の情報
    ファイルサイズ、権限、データ位置など

ひらいているファイル状態
    fd、ファイルポジション、アクセスモードなど
```

#### ２章の１２の４　関数ポインタによる抽象化

VFSは、C言語でオブジェクト指向的な設計をしています。
ここでいうオブジェクト指向的とは、C++のclassを使うという意味ではありません。
構造体と関数ポインタを使い、ファイルシステムごとに処理を差し替えられるようにする、という意味です。

たとえば、VFS側は「このファイルを読む」という共通操作を扱います。
しかし実際の読み取り処理は、ext4ならext4の実装、tmpfsならtmpfsの実装、procfsならprocfsの実装へ渡されます。

```text
VFS
    readしたい

ファイル_operationsのような関数テーブル
    read_iter
    write_iter
    fsync
    lエルシーク
    など

具体的なファイルシステム
    自分の実装関数を登録する
```

この仕組みによって、ユーザープログラムはファイルシステムごとの違いを意識せずに済みます。
`read(fd, buf, count)` と書けば、fdの先が通常ファイルでも、procfsでも、デバイスでも、VFSを通じて適切な実装へつながります。

もちろん、すべての対象が同じ機能を持つわけではありません。
パイプやソケットは `エルシーク関数` できませんし、procfsのファイルは普通のディスク上のファイルとは違う動作をすることがあります。
それでも、共通のfdインターフェースで扱えるところが重要です。

#### ２章の１２の５　read関数 の内部経路

`read関数` の内部経路を、もう少し具体的に見ます。

```text
1. アプリケーションが read(fd, buf, count) を呼ぶ
2. libcのread関数ラッパからカーネルへ入る
3. カーネルがエフディー対応表を調べる
4. fdに対応するオープン ファイルを見つける
5. VFSが対象のファイル操作を呼ぶ
6. ページキャッシュを確認する
7. 必要ならファイルシステムとブロック層を通じてストレージから読む
8. データをユーザー空間のbufへコピーする
9. 読み取ったバイト数を返す
```

この流れの中で、通常ファイルの読み取りではページキャッシュがかなり重要です。
すでに目的のデータがページキャッシュにあれば、ディスクあいおーなしで返せます。
なければ、ストレージから読み込み、ページキャッシュに載せてからユーザーへ返します。

```text
キャッシュヒット
    ページキャッシュにデータがある
    ディスクあいおーなしで返せる

キャッシュミス
    ページキャッシュにデータがない
    ストレージから読み込む
```

この仕組みが、同じファイルを2回読むと2回目が速い、という現象の大きな理由です。

#### ２章の１２の６　ページキャッシュ

ページキャッシュは、ファイルデータをメモリ上に保持するカーネルのキャッシュです。
ディスクやSSDへのアクセスは、CPUやメモリに比べるとかなり遅いため、よく使うデータをメモリに残すことは性能上とても重要です。

```text
ページキャッシュ
    ファイルの内容をメモリ上に保持する
    読み取りを高速化する
    書き込みをまとめる土台にもなる
```

ページキャッシュの考え方は、参照の局所性に支えられています。

```text
時間的局所性
    一度使ったデータは、近いうちにまた使われやすい

空間的局所性
    ある場所を読んだら、その近くも続けて読まれやすい
```

たとえば、設定ファイルを読む場合、最初の数KBだけでなく、その次の部分も続けて読む可能性があります。
大きなログファイルを先頭から順番に読む場合も、連続した領域を読むことが多いです。

ページキャッシュにデータがあれば、カーネルはストレージへ毎回アクセスせず、メモリから返せます。
これにより、同じデータへの繰り返しアクセスが速くなります。

#### ２章の１２の７　ページキャッシュとメモリ管理

Linuxのページキャッシュは、固定サイズではありません。
空きメモリが多ければ、カーネルはできるだけ多くのファイルデータをキャッシュしようとします。
一方で、アプリケーションがメモリを必要とすれば、あまり使われていないキャッシュを解放してメモリを確保します。

```text
空きメモリがある
    ページキャッシュとして活用する

メモリが必要になる
    使用頻度の低いページキャッシュを解放する
```

そのため、Linuxで `free` コマンドを見ると、キャッシュに多くのメモリが使われているように見えることがあります。
これは必ずしも悪い状態ではありません。
使っていないメモリをファイルキャッシュとして活用している、という見方ができます。

また、Linuxはページキャッシュを捨てるか、匿名メモリをスワップアウトするかのバランスも取ります。
この傾向には `/proc/sys/vm/swappiness` が関係します。

```text
/proc/sys/vm/swappiness
    スワップしやすさの傾向を調整する値
    0から100の範囲で扱われる
```

古い説明では「デフォルトは60」と固定的に説明されることがあります。
多くのLinux環境で60が使われてきましたが、ディストリビューションや用途によって調整されることがあります。
このノートでは、固定値として暗記するより「ページキャッシュ維持とスワップのバランスに関係する値」と理解しておけば十分です。

#### ２章の１２の８　先読み readahead

ページキャッシュは、単に読んだデータを保存するだけではありません。
カーネルは、アプリケーションが連続して読み進めていると判断すると、次に必要になりそうなデータを先に読み込むことがあります。
これを先読み、または readahead と呼びます。

```text
read(fd, buf, 4096)
    要求された4KBを読む

カーネル
    次に読まれそうな先の領域も読んでおく
```

先読みは、空間的局所性を利用した最適化です。
ファイルを先頭から順番に読む処理では、かなり効果があります。

一方で、ランダムアクセスが多い場合、先読みしたデータが使われないこともあります。
その場合、カーネルは先読み量を減らしたり、実質的に止めたりします。

古い資料では、先読み量が16KBから128KBの間で変化する、というような説明が出てくることがあります。
現在のLinuxでは、具体的な先読み量はカーネル、デバイス、ファイルシステム、アクセスパターンによって変わります。
そのため、固定値を覚えるより「シーケンシャルアクセスではカーネルが先読みを効かせる」と理解するのが大事です。

UmuOSを作る場合、最初から高度な先読みは不要です。
しかし、ファイルあいおーを速くする段階では、単純な1ブロック先読みだけでも効果を体感できるかもしれません。

#### ２章の１２の９　ページライトバック

`write関数` の節でも見たように、通常の書き込みでは、データはいったんページキャッシュへコピーされます。
変更されたページは dirty とマークされます。
write関数 はディスクにいきなりは、直接書かないです。まずカーネルのページキャッシュに書き込み、
未同期の変更として、dirty（汚れているみたいなひょう現することもありです。）とマークするということです。

```text
cleanページ
    ストレージ上の内容とメモリ上の内容が一致している

dirtyページ
    メモリ上では変更済み
    まだストレージへ反映されていない
```

dirtyページは、いつかストレージへ書き戻す必要があります。
この処理を writeback、ページライトバック、ページ書き戻しと呼びます。
あくまでも一般的な目安ですが、数秒から30秒以内で書き戻しすると聞いています。

```text
アプリケーション
    write(fd, buf, count)

カーネル
    ページキャッシュ上のページを更新する
    dirtyとしてマークする
    write関数を返す

後で
    dirtyページをストレージへ書き戻す
```

writebackが発生する代ひょう的なきっかけは次の通りです。

```text
dirtyデータが増えすぎた
    メモリを圧迫する前に書き戻す

dirtyになってから一定時間が経過した
    いつまでも未書き戻しにしない

fsync関数 / fdatasync関数 / sync関数
    アプリケーションが明示的に同期を要求した

メモリ回収
    cleanにしてからページを捨てたい
```

関連するカーネルパラメータとしては、たとえば次のようなものがあります。

```text
/proc/sys/vm/dirty_ratio
    dirtyページがメモリに占められる上限の目安

/proc/sys/vm/dirty_background_ratio
    バックグラウンド書き戻しを始める目安

/proc/sys/vm/dirty_expire_centisecs
    dirtyデータが古くなったと見なす時間

/proc/sys/vm/dirty_writeback_centisecs
    定期的な書き戻し間隔の目安
```

これらは環境やカーネル設定によって扱いが変わるため、最初から細かく調整する必要はありません。
まずは、dirtyページはカーネルが後でwritebackする、という構造を押さえるのが重要です。

#### ２章の１２の１０　pdflウーシュという古い説明の補正

古いLinux資料では、dirtyバッファの書き戻しを担当するものとして `pdflウーシュ` というカーネルスレッドが説明されることがあります。

ただし、これは古いLinuxカーネルの説明です。
現在のLinuxでは、`pdflウーシュ` は使われておらず、ストレージやbacking deviceごとの writeback worker、flウーシュer thread 系の仕組みに置き換わっています。

```text
古い説明
    pdflウーシュがdirtyバッファを書き戻す

現在の理解
    backing deviceごとのwriteback workerがdirtyページを書き戻す
```

なぜ置き換えられたかというと、ストレージごとの混雑や性能差をよりうまく扱うためです。
1つの書き戻し処理が遅いデバイスで詰まっても、別のデバイスのwritebackまで巻き添えにしにくくする必要があります。

このあたりはカーネル内部の実装詳細なので、初期にすべて覚える必要はありません。
大事なのは、次の点です。

```text
write関数はすぐ返ることがある
    データはdirtyページとしてメモリに残る

カーネルのwriteback機構
    後でストレージへ書き戻す

fsync関数
    必要ならアプリケーションが明示的に同期を要求する
```

#### ２章の１２の１１　buffer_head と統合ページキャッシュ

古いLinuxでは、ページキャッシュとバッファキャッシュが別々に存在していた時期があります。
その場合、同じデータがページキャッシュ側とバッファキャッシュ側に重複して存在し、整合性を取る必要がありました。

Linux 2.4以降、ページキャッシュとバッファキャッシュは統合され、ファイルデータのキャッシュはページキャッシュを中心に扱われるようになりました。

古い資料では、バッファをひょうす構造体として `buffer_head` が強く説明されることがあります。
現在でも `buffer_head` は一部で残っていますが、現代Linuxのファイルあいおーを理解する入口としては、まずページキャッシュ、
folio、address_space、writeback といった流れで見る方が自然です。

ただし、このノートではカーネル内部の細かい構造体名を暗記するより、次の関係を押さえることを優先します。

```text
ページキャッシュ
    ファイルデータをメモリ上に保持する中心

dirty状態
    メモリ上の内容がストレージより新しい状態

writeback
    dirtyな内容をストレージへ反映する処理
```

#### ２章の１２の１２　システムプログラマにとっての意味

システムプログラマにとって、VFSとページキャッシュの存在はとても重要です。
なぜなら、普通のアプリケーションは、具体的なファイルシステムやストレージを直接意識せずに、共通のAPIでファイルあいおーを書けるからです。

```text
アプリケーションが意識するもの
    fd
    read関数
    write関数
    fsync関数
    close関数

カーネルが吸収するもの
    ファイルシステム差
    キャッシュ
    writeback
    デバイスごとのあいおー特性
```

多くの場合、ページキャッシュを素直に活用すると、コードが速くなります。
シーケンシャルに読み、必要なタイミングで `fsync関数` し、むやみに `O_DIRECT` を使わない、という設計が基本になります。

一方で、データベースのような特殊なアプリケーションでは、自前のキャッシュ管理や `O_DIRECT` を検討することがあります。
これは「OSのキャッシュが悪い」からではなく、アプリケーションがOSより詳しいデータ配置やアクセスパターンを知っている場合があるからです。

#### ２章の１２の１３　UmuOSで考えること

UmuOSでファイルあいおーを設計するなら、この節は次の設計課題につながります。

```text
VFS相当の抽象化
    通常ファイル、ディレクトリ、デバイス、パイプをどう統一するか

アイノード相当の構造
    ファイルサイズ、権限、リンク数、データブロックをどう持つか

dentry相当の構造
    ファイルメイとアイノードの対応をどう管理するか

オープン ファイル相当の構造
    fd、ファイルポジション、オープンフラグをどう保持するか

ページキャッシュ相当の仕組み
    読み取りをどこまでキャッシュするか

dirty管理
    書き込み済みだが未反映のデータをどう持つか

writeback
    いつ、どの順序でストレージへ反映するか
```

最初のUmuOSでは、すべてをLinux並みに作る必要はありません。
しかし、エフディー対応表、アイノード、ファイルポジション、ページキャッシュ、dirty、writebackという言葉がつながると、
Linuxのファイルあいおーの骨格がかなり見えてきます。

#### ２章の１２の１４　カーネル内の動作の要点

ここまでの要点をまとめます。

```text
VFS
    具体的なファイルシステムの違いを隠す共通層

superblock
    マウントされたファイルシステム全体をひょうす

アイノード
    ファイルの実体や属性をひょうす

dentry
    ファイルメイとアイノードの対応をひょうす

ファイル
    オープン関数されたファイル状態をひょうす

ページキャッシュ
    ファイルデータをメモリ上に保持する

readahead
    連続読み取りを予測して先に読む

dirtyページ
    メモリ上では変更済みだが、まだストレージへ反映されていないページ

writeback
    dirtyページをストレージへ書き戻す処理

pdflウーシュ
    古いLinuxの説明
    現在はwriteback worker系の仕組みとして理解する
```

この節で、2章の低レベルファイルあいおーはかなり大きく一周しました。
ユーザープログラムから見える `オープン関数`、`read関数`、`write関数`、`close関数` の裏側には、VFS、ページキャッシュ、writebackという、
カーネル内部の仕組みがあります。
ここまで見えると、ファイルあいおーは単なる関数呼び出しではなく、OSの設計そのものとして理解できるようになります。

### ２章の１３　オープンat関数 と現代的なパス指定

現代のLinuxでは、`オープン関数` だけでなく `オープンat関数` もよく出てきます。
`オープンat関数` は、ディレクトリfdを基準にして相対パスをひらくための関数です。

```c
#include <fcntl.h>
#include <sys/stat.h>
#include <sys/types.h>
#include ユニストディードットエイチ。

int オープンat(int dirfd, const char *path, int flags);
int オープンat(int dirfd, const char *path, int flags, mode_t mode);
```

`dirfd` にディレクトリのfdを渡し、`path` に相対パスを渡すと、そのディレクトリを基準にファイルを開けます。
`dirfd` に `AT_FDCWD` を渡すと、現在のカレントディレクトリ基準になり、オープン関数 に近い使い方になります。

オープンat関数 が便利な理由は、主に次の2つです。

```text
パス解決の安全性
    途中のディレクトリが別プロセスに差し替えられる競合を減らしやすい

カレントディレクトリに依存しにくい設計
    マルチスレッドでも、プロセス全体のcwd変更に振り回されにくい
```

今では `オープンat関数` は特別に未来的な機能というより、普通に使われるAPIです。
さらに新しいLinuxでは `オープンat2関数` もあります。
`オープンat2関数` はLinux 5.6で追加された、より細かくパス解決を制御できるLinux固有のシステムコールです。
ただし、移植性を考える通常のCプログラムでは、まず `オープン関数` と `オープンat関数` を理解するのがよいです。

オープンat関数 の簡単な例です。

```c
#include <fcntl.h>
#include <エスティーディーio.h>
#include <エスティーディーlib.h>
#include ユニストディードットエイチ。

int main(void) {
    int dirfd;
    int fd;

    dirfd = オープン("/home/tama", オー・リードオンリー | オー・ディレクトリ | オー・クローズエグゼック);

    if (dirfd < 0) {
        ペラー("オープン directory");
        イグジット(1);
    }

    fd = オープンat(dirfd, "メモどっとテキスト", オー・リードオンリー | オー・クローズエグゼック);

    if (fd < 0) {
        ペラー("オープンat");
        close(dirfd);
        イグジット(1);
    }

    close(fd);
    close(dirfd);

    return 0;
}
```

この例では、まず `/home/tama` をディレクトリとして開き、そのディレクトリを基準に `メモどっとテキスト` をひらいています。
パス文字列だけに頼るのではなく、ディレクトリfdを基準にするのがポイントです。

UmuOSの視点では、最初から オープンat関数 まで実装する必要はないかもしれません。
しかし、将来的に安全なパス解決や、ディレクトリfdを基準にしたファイル操作を設計するなら、オープンat関数 系の考え方はかなり参考になります。

### ２章の１４　ここまでの整理

ここまでをまとめると、次のようになります。

```text
fd
    プロセスがひらいている対象をひょうす整数

オープン関数
    パス名からファイルを開き、fdを返す

read関数 / write関数
    fdを使って読み書きする

close関数
    fdをとじる

標準fd
    0: 標準入力
    1: 標準出力
    2: 標準エラー出力

オープンat関数
    ディレクトリfdを基準にファイルをひらく

read関数
    fdから最大countバイトを読み取る
    もどりち、で実際に読めたバイト数、エンドオブファイル、エラーを判断する

write関数
    fdへ最大countバイトを書き込む
    もどりち、で実際に書けたバイト数、部分書き込み、エラーを判断する

おーアペンド
    write関数 のたびにファイル末尾へ追記する

writeback
    write関数 成功後、カーネルが後でストレージへ書き戻すことがある

同期あいおー
    fsync関数、fdatasync関数、オー・シンク などで永続化タイミングを制御する

fsync関数
    指定fdのデータと必要なメタデータをストレージへ同期する

O_DIRECT
    ページキャッシュをなるべく避けるダイレクトあいおー用フラグ
    アラインメント制約が強く、通常用途ではまず使わない

close関数
    fdを閉じ、プロセスのエフディー対応表から外す
    遅延書き込みエラーが見えることもある

エルシーク関数
    ファイルポジションを移動する
    通常ファイルでは使えるが、パイプやソケットではESパイプになる

sparse ファイル
    ファイルサイズを超えて書くことでホールができることがある

pread関数 / pwrite関数
    offsetを指定して読み書きする
    fdの現在のファイルポジションを動かさない

truncate関数 / ftruncate関数
    ファイルサイズを指定した長さへ変更する

select関数 / ポール関数
    複数fdのあいおー可能状態をまとめて待つ
    対話シェルやネットワーク処理で重要

VFS
    具体的なファイルシステムの違いを隠す共通層

ページキャッシュ
    ファイルデータをメモリ上に保持してあいおーを高速化する

writeback
    dirtyページを後でストレージへ書き戻す

オー・クローズエグゼック
    現代的なコードではfd漏れ防止のためによく使う
```

まずは、Linuxのファイルあいおーでは fd が中心になる、と押さえるとよいです。
ファイルメイを扱うのは主に オープン関数 の段階です。
いったんファイルをひらいた後は、read関数、write関数、close関数 などの操作は fd を通して行います。

UmuOSを作る側の視点では、fdはユーザープログラムとカーネルをつなぐ小さな番号です。
しかし、その裏側には、プロセスごとのエフディー対応表、ファイルポジション、アイノード、パーミッション、パイプやソケットなどの抽象化がつながっています。
このfdの設計がしっかりしていると、シェル、リダイレクト、パイプ、ログ、デバイスあいおーなどを自然に組み立てられるようになります。

この2章では、ファイルあいおーをユーザー空間のAPIとしてだけでなく、UmuOSを作るためのOS内部構造として見てきました。
`オープン関数`、`read関数`、`write関数`、`close関数` から始まり、同期あいオー・ダイレクトあいオー・`エルシーク関数`、あいおー多重化、VFS、ページキャッシュ、writeback、
までつながると、ファイルあいおーは単なる入出力ではなく、OS全体の骨格であることが見えてきます。


