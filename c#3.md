---
title: "何も知らない人向けC#.net３（初級）　クラス・インスタンス・ファイル処理 編"
---

# 何も知らない人向けC#.net 3 — クラス・インスタンス・ファイル処理 編

このシリーズは最終的に、読み手が C# のコードを見ても身構えず、落ち着いて読めるようになることを目標にします。  
第3巻では、第2巻までで学んだ配列・メソッド・文字列の上に、

- クラス
- インスタンス
- ファイル処理

を積み上げます。

この巻の範囲：

- 第八章：クラス
- 第九章：インスタンス
- 第十章：ファイル処理

前提：

- OS：Windows 11
- 開発環境：VS Code
- 対象：.NET Framework 4.8.1 のコンソールアプリ

---

## 第八章でやること：

- クラスを「ひとまとまりの設計図」として理解する
- クラスの中に、値とメソッドを置けるようになる
- フィールド、プロパティ、コンストラクタの基本形を読む
- 関連する処理を1つの型にまとめる意味をつかむ

前提：第1巻と第2巻では、ほとんどの処理を `Program` に直接書いてきました。  
この章では、「ひとまとまりの役割ごとに箱を分ける」考え方に入ります。

---

## クラス

### クラスとは

クラスは、「どういう値を持ち、どういう仕事ができるか」をまとめた設計図です。  
最初は「関連するものをひとつにまとめる箱」だと思えば大丈夫です。

```csharp
using System;

class Student
{
    public string Name;
    public int Score;
}

class Program
{
    static void Main(string[] args)
    {
        Console.WriteLine("Student という設計図を作りました");
    }
}
```

出力イメージ：

```text
Student という設計図を作りました
```

### クラスの中に値を置く

クラスの中に置く値を、ここでは「そのクラスが覚えておく値」と考えれば十分です。

```csharp
using System;

class Book
{
    public string Title;
    public int Price;
}

class Program
{
    static void Main(string[] args)
    {
        Book book = new Book();
        book.Title = "C# の本";
        book.Price = 2800;

        Console.WriteLine(book.Title);
        Console.WriteLine(book.Price);
    }
}
```

出力イメージ：

```text
C# の本
2800
```

### フィールド

クラスの中で覚えておく値を **フィールド** と呼びます。  
ここでは「クラスの中の変数」だと思えば大丈夫です。

```csharp
using System;

class Counter
{
    public int Value;
}

class Program
{
    static void Main(string[] args)
    {
        Counter counter = new Counter();
        counter.Value = 3;

        Console.WriteLine(counter.Value);
    }
}
```

出力イメージ：

```text
3
```

### クラスの中にメソッドを置く

クラスは、値だけでなく仕事もまとめられます。

```csharp
using System;

class Greeting
{
    public void SayHello()
    {
        Console.WriteLine("こんにちは");
    }
}

class Program
{
    static void Main(string[] args)
    {
        Greeting greeting = new Greeting();
        greeting.SayHello();
    }
}
```

出力イメージ：

```text
こんにちは
```

### クラスの中の値を使うメソッド

同じクラスの中にあるメソッドは、そのクラスが持つ値を使えます。

```csharp
using System;

class Product
{
    public string Name;
    public int Price;

    public void Show()
    {
        Console.WriteLine($"{Name} は {Price} 円です");
    }
}

class Program
{
    static void Main(string[] args)
    {
        Product product = new Product();
        product.Name = "ノート";
        product.Price = 120;
        product.Show();
    }
}
```

出力イメージ：

```text
ノート は 120 円です
```

### コンストラクタ

クラスを作った直後の最初の準備を書く場所を **コンストラクタ** と呼びます。  
ここでは「作ったときに最初に動く特別なメソッド」と考えれば十分です。

```csharp
using System;

class User
{
    public string Name;

    public User()
    {
        Name = "名無し";
    }
}

class Program
{
    static void Main(string[] args)
    {
        User user = new User();
        Console.WriteLine(user.Name);
    }
}
```

出力イメージ：

```text
名無し
```

### 引数付きコンストラクタ

作るときに値を渡す形にもできます。

```csharp
using System;

class User
{
    public string Name;

    public User(string name)
    {
        Name = name;
    }
}

class Program
{
    static void Main(string[] args)
    {
        User user = new User("さくら");
        Console.WriteLine(user.Name);
    }
}
```

出力イメージ：

```text
さくら
```

### プロパティ

値を扱うための窓口として **プロパティ** を使うことがあります。  
最初は「フィールドを少し整えた書き方」だと考えれば大丈夫です。

```csharp
using System;

class Person
{
    public string Name { get; set; }
    public int Age { get; set; }
}

class Program
{
    static void Main(string[] args)
    {
        Person person = new Person();
        person.Name = "田中";
        person.Age = 20;

        Console.WriteLine(person.Name);
        Console.WriteLine(person.Age);
    }
}
```

出力イメージ：

```text
田中
20
```

### public とは

`public` は「外から使えるようにする」ときに付けます。  
ここでは「他の場所から見えるようにする目印」と考えれば十分です。

```csharp
using System;

class Message
{
    public string Text = "外から読めます";
}

class Program
{
    static void Main(string[] args)
    {
        Message message = new Message();
        Console.WriteLine(message.Text);
    }
}
```

出力イメージ：

```text
外から読めます
```

### クラスを分ける意味

関連する値と仕事を1つにまとめると、コードの役割が見えやすくなります。

```csharp
using System;

class Item
{
    public string Name;
    public int Price;

    public void ShowPriceTag()
    {
        Console.WriteLine($"{Name} : {Price} 円");
    }
}

class Program
{
    static void Main(string[] args)
    {
        Item item = new Item();
        item.Name = "えんぴつ";
        item.Price = 80;
        item.ShowPriceTag();
    }
}
```

出力イメージ：

```text
えんぴつ : 80 円
```

---

## 第八章まとめ

- クラスは、値と仕事をまとめた設計図
- クラスの中の変数をフィールドという
- クラスの中にメソッドも置ける
- コンストラクタは、作った直後の準備を書く場所
- プロパティは、値を扱うための窓口

---

## 第九章でやること：

- インスタンスを「設計図から作った実物」として理解する
- `new` の意味をつかむ
- 複数のインスタンスを作って使い分ける
- 配列と組み合わせて、たくさんのインスタンスを扱う
- 参照型の初歩的な動きを知る

---

## インスタンス

### インスタンスとは

クラスが設計図なら、**インスタンス** はその設計図から作った実物です。  
最初は「実際に使う1個ぶん」と考えれば大丈夫です。

```csharp
using System;

class Dog
{
    public string Name;
}

class Program
{
    static void Main(string[] args)
    {
        Dog dog = new Dog();
        dog.Name = "ポチ";

        Console.WriteLine(dog.Name);
    }
}
```

出力イメージ：

```text
ポチ
```

### new

`new` は、設計図から実物を作る書き方です。

```csharp
using System;

class Box
{
    public int Value;
}

class Program
{
    static void Main(string[] args)
    {
        Box box = new Box();
        box.Value = 100;

        Console.WriteLine(box.Value);
    }
}
```

出力イメージ：

```text
100
```

### メンバーアクセス

インスタンスが持っている値やメソッドに触るときは `.` を使います。  
これを **メンバーアクセス** と呼びますが、ここでは「中を見るためのドット」と考えれば十分です。

```csharp
using System;

class Lamp
{
    public bool IsOn;

    public void TurnOn()
    {
        IsOn = true;
    }
}

class Program
{
    static void Main(string[] args)
    {
        Lamp lamp = new Lamp();
        lamp.TurnOn();

        Console.WriteLine(lamp.IsOn);
    }
}
```

出力イメージ：

```text
True
```

### 複数のインスタンス

同じクラスから、別々の実物をいくつも作れます。

```csharp
using System;

class Account
{
    public string Name;
    public int Balance;
}

class Program
{
    static void Main(string[] args)
    {
        Account a = new Account();
        a.Name = "A";
        a.Balance = 1000;

        Account b = new Account();
        b.Name = "B";
        b.Balance = 3000;

        Console.WriteLine($"{a.Name} : {a.Balance}");
        Console.WriteLine($"{b.Name} : {b.Balance}");
    }
}
```

出力イメージ：

```text
A : 1000
B : 3000
```

### 同じクラスでも中身は別

同じ設計図から作っても、持つ値は別々です。

```csharp
using System;

class Counter
{
    public int Value;
}

class Program
{
    static void Main(string[] args)
    {
        Counter left = new Counter();
        Counter right = new Counter();

        left.Value = 1;
        right.Value = 9;

        Console.WriteLine(left.Value);
        Console.WriteLine(right.Value);
    }
}
```

出力イメージ：

```text
1
9
```

### インスタンスを配列で持つ

インスタンスも配列に入れられます。

```csharp
using System;

class Student
{
    public string Name;
}

class Program
{
    static void Main(string[] args)
    {
        Student[] students = new Student[2];

        students[0] = new Student();
        students[0].Name = "山田";

        students[1] = new Student();
        students[1].Name = "鈴木";

        for (int i = 0; i < students.Length; i++)
        {
            Console.WriteLine(students[i].Name);
        }
    }
}
```

出力イメージ：

```text
山田
鈴木
```

### インスタンスをメソッドに渡す

インスタンスもメソッドに渡して使えます。

```csharp
using System;

class User
{
    public string Name;
}

class Program
{
    static void ShowUser(User user)
    {
        Console.WriteLine(user.Name);
    }

    static void Main(string[] args)
    {
        User user = new User();
        user.Name = "高橋";

        ShowUser(user);
    }
}
```

出力イメージ：

```text
高橋
```

### 参照型の最初の理解

クラスから作ったものは、同じ実物を指している変数が複数できることがあります。  
ここでは「同じものを2つの名前で見ていることがある」と覚えれば十分です。

```csharp
using System;

class User
{
    public string Name;
}

class Program
{
    static void Main(string[] args)
    {
        User first = new User();
        first.Name = "佐藤";

        User second = first;
        second.Name = "伊藤";

        Console.WriteLine(first.Name);
        Console.WriteLine(second.Name);
    }
}
```

出力イメージ：

```text
伊藤
伊藤
```

### null

まだ何も作っていない状態を表すときに `null` を使うことがあります。

```csharp
using System;

class User
{
    public string Name;
}

class Program
{
    static void Main(string[] args)
    {
        User user = null;

        if (user == null)
        {
            Console.WriteLine("まだ作られていません");
        }
    }
}
```

出力イメージ：

```text
まだ作られていません
```

### static とインスタンスの違い

`static` は「クラスそのものに属するもの」、インスタンスのメンバーは「実物ごとに持つもの」です。  
最初は、

- `Console.WriteLine` のようなものは `static`
- `user.Name` のようなものは実物ごと

と見分けられれば十分です。

```csharp
using System;

class Counter
{
    public static int TotalCount;
    public int Value;
}

class Program
{
    static void Main(string[] args)
    {
        Counter.TotalCount = 2;

        Counter counter = new Counter();
        counter.Value = 10;

        Console.WriteLine(Counter.TotalCount);
        Console.WriteLine(counter.Value);
    }
}
```

出力イメージ：

```text
2
10
```

---

## 第九章まとめ

- インスタンスは、設計図から作った実物
- `new` で作る
- `.` で中の値やメソッドに触る
- 同じクラスから複数の実物を作れる
- クラスから作ったものは、同じ実物を複数の変数で見ることがある
- `null` は、まだ何も入っていない状態

---

## 第十章でやること：

- ファイルに文字を書ける
- ファイルから文字を読める
- 追記、存在確認、行ごとの読み込みができる
- 失敗することがある処理に備える考え方を知る

---

## ファイル処理

### ファイルとは

ファイルは、文字やデータを保存しておく入れ物です。  
この章では、まず文字を書いたり読んだりする最初の形を扱います。

### System.IO

ファイルを扱う道具は `System.IO` にあります。  
ここでは「ファイル用の道具箱を使えるようにする書き方」だと思えば大丈夫です。

```csharp
using System;
using System.IO;

class Program
{
    static void Main(string[] args)
    {
        Console.WriteLine("System.IO を使います");
    }
}
```

出力イメージ：

```text
System.IO を使います
```

### 文字をファイルに書き込む

`File.WriteAllText` を使うと、文字をまとめて書き込めます。

```csharp
using System;
using System.IO;

class Program
{
    static void Main(string[] args)
    {
        File.WriteAllText("sample.txt", "こんにちは");
        Console.WriteLine("sample.txt に書き込みました");
    }
}
```

出力イメージ：

```text
sample.txt に書き込みました
```

### ファイルから文字を読む

`File.ReadAllText` を使うと、全部まとめて読めます。

```csharp
using System;
using System.IO;

class Program
{
    static void Main(string[] args)
    {
        File.WriteAllText("sample.txt", "こんにちは");
        string text = File.ReadAllText("sample.txt");

        Console.WriteLine(text);
    }
}
```

出力イメージ：

```text
こんにちは
```

### 追記

すでにあるファイルの続きに書き足すことを **追記** と呼びます。

```csharp
using System;
using System.IO;

class Program
{
    static void Main(string[] args)
    {
        File.WriteAllText("log.txt", "1行目\n");
        File.AppendAllText("log.txt", "2行目\n");

        Console.WriteLine(File.ReadAllText("log.txt"));
    }
}
```

出力イメージ：

```text
1行目
2行目

```

### ファイルの存在確認

ファイルがあるかどうかを、先に確かめられます。

```csharp
using System;
using System.IO;

class Program
{
    static void Main(string[] args)
    {
        File.WriteAllText("check.txt", "ok");

        if (File.Exists("check.txt"))
        {
            Console.WriteLine("ファイルがあります");
        }
    }
}
```

出力イメージ：

```text
ファイルがあります
```

### 行ごとに読む

`File.ReadAllLines` を使うと、1行ずつ配列で受け取れます。

```csharp
using System;
using System.IO;

class Program
{
    static void Main(string[] args)
    {
        File.WriteAllText("lines.txt", "赤\n青\n緑");

        string[] lines = File.ReadAllLines("lines.txt");

        for (int i = 0; i < lines.Length; i++)
        {
            Console.WriteLine(lines[i]);
        }
    }
}
```

出力イメージ：

```text
赤
青
緑
```

### 配列として書く

文字の配列を、まとめてファイルへ書けます。

```csharp
using System;
using System.IO;

class Program
{
    static void Main(string[] args)
    {
        string[] lines = { "春", "夏", "秋", "冬" };

        File.WriteAllLines("season.txt", lines);

        Console.WriteLine(File.ReadAllText("season.txt"));
    }
}
```

出力イメージ：

```text
春
夏
秋
冬

```

### 相対パス

ファイル名だけを書くと、いま実行している場所を基準にします。  
このような書き方を **相対パス** と呼びますが、ここでは「いまいる場所から見た書き方」だと考えれば十分です。

```csharp
using System;
using System.IO;

class Program
{
    static void Main(string[] args)
    {
        string path = "memo.txt";
        File.WriteAllText(path, "メモ");

        Console.WriteLine(path);
    }
}
```

出力イメージ：

```text
memo.txt
```

### Path.Combine

ファイル名を安全につなげたいときは `Path.Combine` を使います。

```csharp
using System;
using System.IO;

class Program
{
    static void Main(string[] args)
    {
        string folder = "data";
        string fileName = "note.txt";
        string path = Path.Combine(folder, fileName);

        Console.WriteLine(path);
    }
}
```

出力イメージ：

```text
data\note.txt
```

### StreamWriter

少しずつ書きたいときは `StreamWriter` を使うことがあります。  
ここでは「ファイルに順番に書いていく道具」と考えれば十分です。

```csharp
using System;
using System.IO;

class Program
{
    static void Main(string[] args)
    {
        using (StreamWriter writer = new StreamWriter("writer.txt"))
        {
            writer.WriteLine("1行目");
            writer.WriteLine("2行目");
        }

        Console.WriteLine(File.ReadAllText("writer.txt"));
    }
}
```

出力イメージ：

```text
1行目
2行目

```

### StreamReader

読む側にも、順に読むための道具があります。

```csharp
using System;
using System.IO;

class Program
{
    static void Main(string[] args)
    {
        File.WriteAllText("reader.txt", "A\nB\nC");

        using (StreamReader reader = new StreamReader("reader.txt"))
        {
            Console.WriteLine(reader.ReadLine());
            Console.WriteLine(reader.ReadLine());
            Console.WriteLine(reader.ReadLine());
        }
    }
}
```

出力イメージ：

```text
A
B
C
```

### 失敗に備える

ファイル処理は、

- ファイルがない
- 開けない
- 名前が不正

などで失敗することがあります。  
そのため `try` と `catch` で備えることがあります。

```csharp
using System;
using System.IO;

class Program
{
    static void Main(string[] args)
    {
        try
        {
            string text = File.ReadAllText("not_found.txt");
            Console.WriteLine(text);
        }
        catch (Exception)
        {
            Console.WriteLine("読み込みに失敗しました");
        }
    }
}
```

出力イメージ：

```text
読み込みに失敗しました
```

### 文字列とファイル処理を組み合わせる

入力した文字を整えてから保存することもあります。

```csharp
using System;
using System.IO;

class Program
{
    static void Main(string[] args)
    {
        Console.Write("保存する文字を入力してください: ");
        string text = Console.ReadLine();
        string cleaned = text.Trim();

        File.WriteAllText("input.txt", cleaned);
        Console.WriteLine(File.ReadAllText("input.txt"));
    }
}
```

出力イメージ：

```text
保存する文字を入力してください:  hello 
hello
```

---

## 第十章まとめ

- `System.IO` を使うとファイルを扱える
- `WriteAllText` で書く、`ReadAllText` で読む
- `AppendAllText` で追記できる
- `Exists` で存在確認できる
- `ReadAllLines` で行ごとに読める
- `try` と `catch` で失敗に備えられる

---

## この巻の終わりに

ここまで読めたなら、

- 値と処理をクラスにまとめる
- 設計図から実物を作って使う
- 入力や結果をファイルに残す

という、アプリらしいコードの入口まで来ています。

次は、継承、例外、コレクションに進むと、実務のコードにさらに近い形が読みやすくなります。