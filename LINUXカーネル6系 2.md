---
title: "LINUXカーネル6系の研究2　メモリアドレッシング"
---

# LINUXカーネル6系の研究　２

この 2 章では、Linux カーネルを理解するうえで避けて通れないメモリアドレッシングの話に入っていきます。
1 章では、Linux 6.6 LTS を研究するときの全体像を先に整理しました。
ここからはもう少し踏み込んで、CPU が見ているアドレス、カーネルが扱っているアドレス、そして実際の RAM 上の位置が、
どういう関係で結びついているのかを見ていきます。

古い Linux 2.6 系の本では、80x86 のセグメンテーションとページングをかなり丁寧に追いかけながら説明していきます。
この入口自体は今でもとても大事です。
ただし、Linux 6.6 LTS を研究する立場では、単に昔の x86 の仕組みを暗記するのではなく、
「Linux が実際にはどの抽象化を重視しているのか」「いま読むべき中心概念は何か」という視点で整理した方が理解しやすいです。

特に重要なのは、プログラムが使うアドレス、CPU が変換途中で扱うアドレス、RAM 上の実アドレスを分けて考えることです。
この区別が曖昧だと、ページテーブル、MMU、TLB、mmap()、カーネル空間、ユーザー空間といった後続の話題が全部ぼやけます。
UmuOS の研究でも、最初は単純なアドレス空間から始めるにしても、最終的にはこの整理がかなり効いてくると思います。

## ２章　メモリアドレッシング

### ２章の１　メモリアドレス

まず最初に押さえたいのは、「アドレス」という言葉が 1 種類ではないことです。
普段 C 言語を書いていると、ポインタの値や変数の番地をそのままメモリの位置だと思いがちです。
しかし実際には、プログラムが見ているアドレスと、CPU が内部で扱っているアドレスと、物理 RAM 上の位置は、
必ずしも同じではありません。

古い x86 系の説明では、論理アドレス、リニアアドレス、物理アドレスという 3 つを区別します。
Linux 6.6 LTS を理解するうえでも、この 3 つの区別は今なお入口として有効です。
ただし、現代の Linux ではセグメンテーションを主役として使っているわけではなく、
実際の理解の中心は仮想アドレス空間とページングに寄っています。
それでも、CPU がどの段階でどの形のアドレスを扱っているのかを知っておくと、
後でページテーブルや例外処理を読むときにかなり迷いにくくなります。

#### ２章の１の１　論理アドレス

論理アドレスは、命令が直接扱う最初の形のアドレスです。
古典的な x86 では、セグメントとオフセットを組み合わせて指定する考え方になっていて、
CPU はまずこの論理アドレスを受け取ります。
古い OS や DOS 系の文脈では、このセグメントという発想がかなり前面に出てきました。

ただし、Linux 6.6 LTS を研究する立場では、ここを昔の流儀そのままで覚え込む必要はないです。
大事なのは、CPU には「命令が最初に見せるアドレスの形」と「そのあと変換された結果の形」がある、ということです。
つまり論理アドレスは、CPU から見た変換前の入口です。
この入口があるからこそ、ハードウェア側で保護や変換を段階的に行えます。

Linux では、x86 系でもセグメンテーションを細かく活用して複雑な仮想空間を作る、という方向には進んでいません。
むしろ、できるだけフラットに近い見え方を作り、そのうえでページングを主役として使う方が基本です。
なので、論理アドレスの話は「現代 Linux の主役」ではなく、「CPU の入口を理解するための前提」として捉えるのがよいと思います。

#### ２章の１の２　リニアアドレス

リニアアドレスは、論理アドレスをセグメンテーション機構が変換したあとのアドレスです。
一般的な Linux の学習では、ここを仮想アドレスとほぼ同じ感覚で捉えると分かりやすいです。
プログラムが大きな連続したメモリ空間を持っているように見えるのは、この段階の抽象化があるからです。

Linux 6.6 LTS の研究では、このリニアアドレスこそがかなり中心になります。
なぜなら、ページテーブルや mmap() やページフォルトは、基本的にこの仮想的なアドレス空間をどう管理するか、という問題だからです。
ユーザープロセスが見ているコード領域、データ領域、ヒープ、スタック、共有ライブラリ、ファイルマッピングなども、
まずはこの仮想的な空間の配置として理解することになります。

ここで重要なのは、リニアアドレスがそのまま物理アドレスではないことです。
たとえば、プロセスが連続した大きな配列を持っているように見えても、実際の物理メモリ上では、
それが飛び飛びのページフレームに分散していることは普通にあります。
それでもプロセス側からは連続して見えるわけです。
この「見え方」と「実体」を分離できることが、現代 OS の強さです。

#### ２章の１の３　物理アドレス

物理アドレスは、最終的に RAM やデバイス側の実際の場所を指すアドレスです。
CPU が本当にメモリバスへ信号を出してアクセスするときには、この物理アドレスが使われます。
つまり、最終的にハードウェアが理解するのは物理アドレスです。

Linux カーネルを読んでいると、仮想アドレス空間の話と物理ページの話がしばしば混ざって見えます。
しかし、この 2 つは役割が違います。
ユーザー空間やカーネル空間が主に扱うのは「どう見せるか」という仮想側の問題であり、
ページフレームの確保、NUMA ノード、DMA 制約、物理連続性の問題などは「実際にどこへ置くか」という物理側の問題です。
この区別は、あとで buddy allocator や page struct や zone の話を見るときに効いてきます。

また、現代の Linux では、物理アドレス空間も単純ではありません。
すべての RAM が一様に並んでいるとは限らず、予約領域、メモリマップド I/O、NUMA 構成、ファームウェア都合の穴などが存在します。
そのため、OS の仕事は単に「空いている番地を渡す」ことではなく、複雑な物理メモリの地形の上に、
使いやすい仮想アドレス空間を構築することだと言えます。

#### ２章の１の４　MMU が行っていること

これらの変換を実際に支えているのが MMU です。
MMU は Memory Management Unit の略で、CPU がメモリアクセスを行うたびに、
必要な変換と保護チェックをハードウェアとして実行します。
Linux カーネルが毎回ソフトウェアだけで全部変換していたら、とても実用的な速度にはなりません。
だからこそ、アドレス変換の中核は MMU が担い、カーネルはそのためのページテーブルなどを準備するわけです。

ここで見るべきなのは、Linux が MMU をどう利用しているかです。
Linux 6.6 LTS は、MMU が持つアドレス変換能力を前提として、プロセスごとに異なる仮想空間を見せ、
不正アクセスを防ぎ、必要に応じてページフォルトを起こし、遅延割り当てやコピーオンライトを実現しています。
つまり MMU は単なる補助機能ではなく、Linux の仮想記憶の土台そのものです。

UmuOS のような自作 OS を考えるときも、この発想はかなり大事です。
最初は単純な 1 対 1 対応や固定マッピングから始めるとしても、
最終的にプロセス分離や安全なユーザー空間を実現したいなら、MMU とページングをどう使うかが中心課題になります。

#### ２章の１の５　アドレス変換と並行実行

古い本の 2.1 節では、最後にマルチプロセッサ環境とメモリ調停回路の話が出てきます。
ここも現代的に読み替えると大事です。
今の Linux 6.6 LTS は、SMP や NUMA を前提とした巨大なカーネルです。
つまり、単に 1 個の CPU が 1 本の流れでメモリへ触る世界ではありません。
複数 CPU が同時にメモリへアクセスし、キャッシュを持ち、整合性を保ちながら動いています。

このため、アドレス変換の話は単独では終わりません。
TLB、キャッシュコヒーレンシ、メモリバリア、ページテーブル更新時の shootdown、NUMA ノードの局所性など、
後で出てくる話題が全部ここへつながってきます。
2.1 の段階ではまだ細部に入りすぎなくてよいですが、
「メモリアドレッシングは、単なる番地の話ではなく、並行実行される現代 CPU 上で安全かつ効率的に動くための土台である」
という見方は、ここで持っておいた方がよいです。

#### ２章の１の６　この節を 6.6 LTS 研究へつなぐ見方

2.1 の元の説明は、x86 の 3 種類のアドレスを区別するところから始まっていました。
この入口は今でも有効です。
ただし、Linux 6.6 LTS の研究として本当に大事なのは、セグメンテーションの細部を覚えることよりも、
Linux が実際には仮想アドレス空間とページングを主役にしていることをつかむことです。

その意味で、この節の要点は次のように整理できます。
プログラムが見ているアドレスと、CPU が変換に使うアドレスと、実 RAM 上の位置は分けて考えること。
Linux はその差を MMU とページテーブルで管理していること。
そして、その仕組みの上に、プロセス分離、保護、mmap()、ページキャッシュ、コピーオンライトといった現代 Linux の重要機能が乗っていることです。

次に読むべきソースの入口としては、mm/ 配下の全体像と、x86 を前提にするなら arch/x86/mm/ や arch/x86/include/asm/ の役目を意識するとよいです。
また、ユーザー空間との接点を意識するなら、mmap() や page fault につながる経路をあとで追うと、この 2.1 の話が単なる教科書知識ではなくなります。

### ２章の２　ハードウェアのセグメント機構

次に、古い x86 のメモリアドレッシングを理解するうえで必ず出てくるセグメント機構を見ていきます。
ここは、昔の Linux 解説書ではかなり重要な位置を占めていました。
実際、x86 という CPU 自体が、論理アドレスからリニアアドレスへ進む途中にセグメンテーションという段階を持っているからです。

ただし、Linux 6.6 LTS を研究する立場では、この節の読み方に少し注意が必要です。
セグメント機構そのものは CPU の重要な機能ですが、現代 Linux がメモリ管理の主役として積極的に使っているのは主にページングです。
そのため、この節は「Linux が日常的に細かく使い倒している仕組み」として読むよりも、
「x86 が本来どういう変換能力を持っていて、そのうち Linux は何を使い、何をあまり使わないのか」を見分けるための節として読む方がよいです。

#### ２章の２の１　リアルモードとプロテクトモード

x86 系 CPU では、昔からリアルモードとプロテクトモードという動作の考え方があります。
リアルモードは、古い互換性を強く引きずった単純な実行形態で、OS が本格的に保護機構を使う前の初期段階で関わります。
一方、プロテクトモードでは、保護機構、特権レベル、アドレス変換といった近代的な OS に必要な仕組みを利用できます。

Linux 6.6 LTS を普通に研究するとき、本体の理解で中心になるのは当然プロテクトモード側です。
ブートのごく初期には互換性の都合で古いモードが見える場面もありますが、カーネルの本質的な資源管理や保護の議論は、
特権レベルとページングが有効な側で考えるべきです。
つまり、この節で本当に重要なのは「昔のモード名を覚えること」ではなく、
Linux が保護された実行環境を前提に動いていることを理解することだと思います。

#### ２章の２の２　セグメントセレクタとセグメントレジスタ

古典的な x86 では、論理アドレスはセグメントとオフセットの組で表現されます。
そのとき CPU は、セグメントを識別するための値をセグメントセレクタとして扱い、
それを保持するために cs、ss、ds、es、fs、gs というレジスタを持っています。

ここで初心者が混乱しやすいのは、「ポインタがそのまま最終アドレスではない」ことです。
x86 の論理アドレスでは、少なくとも古典的な説明としては、どのセグメントを基準に見るかという情報が別にあります。
そして、その基準とオフセットを組み合わせて、CPU が次の段階のアドレスへ進めていきます。

この中で特に重要なのが cs と ss です。
cs は命令を取ってくるコード側の基準、ss はスタック側の基準に関わります。
また cs に含まれる特権レベルの情報は、CPU がいまユーザモード相当で動いているのか、カーネルモード相当で動いているのか、
という判定にも関わります。
Linux 6.6 LTS を読むときも、現在の CPU 実行レベルと特権の境界がどこにあるのかという発想はとても大事です。

ただし、ここで気をつけたいのは、現代 Linux の日常的な理解では、これらのレジスタを DOS 時代の感覚で細かく使い分ける、
という話にはならないことです。
Linux は、x86 が持つセグメント機構を最低限の土台としては使っていても、
メモリ空間そのものの本格的な切り分けは主にページングへ寄せています。

#### ２章の２の３　セグメントディスクリプタ

セグメントセレクタだけでは、実際にそのセグメントがどこから始まり、どこまで続き、どういう権限を持つのかは分かりません。
その情報を持つのがセグメントディスクリプタです。
ディスクリプタには、ベースアドレス、長さ、種別、特権レベル、存在フラグなど、
そのセグメントの性質を示す情報が入ります。

この発想自体はとても面白いです。
つまり x86 は、単に番地を変換するだけではなく、どの範囲にアクセスしてよいか、
その対象がコードかデータか、どの特権レベルなら使えるか、といった情報をハードウェア側へかなり持たせています。
OS はこの仕組みを使うことで、保護機構をソフトウェアだけで抱え込まずに済みます。

ただし、Linux 6.6 LTS の研究として大事なのは、ディスクリプタの全ビット配置を丸暗記することではありません。
本質は、CPU がアドレス変換と保護チェックを行うためのメタデータを別構造として持っている、という理解です。
あとでページテーブルエントリを見るときも、似た発想が別の形で出てきます。
つまり、「ただの番地」ではなく、「番地に付随する属性も一緒に管理する」という見方をここで掴んでおくと強いです。

#### ２章の２の４　GDT と LDT

セグメントディスクリプタは単独で宙に浮いているのではなく、テーブルに並べて管理されます。
代表が GDT と LDT です。
GDT はシステム全体で共有する基本的なセグメント記述を置く場所、LDT は必要であればプロセスごとに追加の記述を持たせるための仕組み、
という理解で入ると分かりやすいです。

古い x86 解説では、GDT や LDT の構造そのものがかなり細かく出てきます。
もちろん CPU の設計を理解するうえでは大切です。
しかし Linux 6.6 LTS の研究で優先度が高いのは、「Linux が LDT を主役にはしていない」ことを押さえることです。
つまり、x86 が用意した豊富なセグメント管理機能のうち、Linux は必要な土台だけを使い、
メモリ空間管理の中心はもっと別の場所へ置いているわけです。

この視点はとても大事です。
CPU が持つ機能を全部フル活用するのが良い設計とは限りません。
Linux は、移植性、単純さ、保守性、そしてページング中心の仮想記憶設計を優先して、
セグメント機構の使い方をかなり限定しています。

#### ２章の２の５　セグメントディスクリプタへの高速アクセス

元の本文では、CPU がセグメントレジスタに対応する内部レジスタを持ち、
毎回メモリ上のテーブルを引き直さなくてもよい仕組みが説明されています。
ここで見ておきたいのは、CPU がアドレス変換を速く行うために、変換に必要な情報を内部へ取り込んで使う、という発想です。

これは、後で TLB を学ぶときにもつながります。
ページングでも、毎回ページテーブルを最初から全部たどっていたら遅すぎます。
だから CPU は、よく使う変換結果や関連情報を内部に保持して高速化します。
セグメントディスクリプタのキャッシュ的な扱いは、そうしたハードウェアの設計思想の前段として見ると面白いです。

Linux 6.6 LTS の研究では、この節は「CPU が変換情報を内部に保持することで高速化する」という原理を理解する場所として読むとよいです。
細かなレジスタ名やビット幅よりも、変換のたびに全部をゼロから調べ直しているわけではない、という点が重要です。

#### ２章の２の６　セグメンテーション回路

セグメンテーション回路は、論理アドレスからリニアアドレスを得るまでの流れを担当します。
セグメントセレクタから対応するディスクリプタを見つけ、そのベースにオフセットを足し、
必要な保護チェックを行いながら次の段階へ進めます。

この説明自体は x86 固有ですが、OS を学ぶうえではかなり意味があります。
なぜなら、アドレス変換というのは単なる足し算ではなく、
「どの空間に属するか」「どの権限なら許されるか」「その範囲に収まっているか」という確認を伴う処理だからです。
Linux カーネルは、このような CPU 側の保護機能を前提にして、安全なユーザー空間と特権空間を成り立たせています。

ただし、現代 Linux の本筋としては、この段階を細かく追うだけで満足してはいけません。
本当に効いてくるのは、このあとページングによって仮想アドレス空間をどう構成するか、という話です。
つまり、セグメンテーション回路は入口であり、主役は次のページングへ渡される、と見るのがよいです。

#### ２章の２の７　この節を 6.6 LTS 研究へつなぐ見方

2.2 は、古い x86 の機能説明として読むと、かなり細かい話が続く節です。
けれど Linux 6.6 LTS の研究として重要なのは、x86 が持つセグメント管理機能の全貌を覚えることではありません。
むしろ、Linux がその豊富な機能をあえて限定的に使い、より移植しやすく整理しやすい形へ寄せていることを理解する方が大事です。

要するに、この節で持つべき見方はこうです。
x86 には、論理アドレスからリニアアドレスへ変換する仕組み、権限を段階的に管理する仕組み、
セグメント属性を持つ仕組みがある。
しかし Linux 6.6 LTS は、その仕組みを主役にはせず、フラットに近い空間とページング中心の設計を選んでいる。
この「使える機能」と「実際に採用している設計」を分けて見られるようになると、Linux のメモリ管理はかなり読みやすくなります。

次に読むべき入口としては、x86 に限定するなら arch/x86/include/asm/ 配下で特権や例外やメモリ関連の定義を見つつ、
より本筋としては 2.3 以降の「Linux がセグメント機構をどう割り切って使っているか」へ進むのが自然です。

### ２章の３　Linux のセグメント機構

ここまでで、x86 という CPU が本来かなり豊かなセグメント機構を持っていることを見てきました。
しかし、Linux 6.6 LTS を研究するうえで本当に重要なのは、「x86 にそういう機能がある」という事実そのものよりも、
Linux がその機能をどう使い、どこをあえて使わないのか、という設計判断です。

結論から言うと、Linux はセグメント機構を最小限にとどめ、メモリ空間管理の中心をページングへ置いています。
これは単なる好みではなく、Linux の性格そのものに関わる重要な判断です。
Linux は、できるだけ多くのアーキテクチャへ移植でき、しかもメモリ管理の考え方を全体でそろえやすい構造を求めています。
そのため、x86 に特有の濃いセグメント活用へ寄るより、より一般化しやすいページング中心の設計を選んでいます。

#### ２章の３の１　なぜ Linux はセグメント機構を主役にしないのか

古い x86 の設計を見ると、セグメント機構だけでもかなり多くのことができそうに見えます。
論理空間を分けたり、範囲制限をかけたり、特権を分けたりできるからです。
それでも Linux が主役をページングに置くのは、まずメモリ管理を単純化しやすいからです。

もし各プロセスが複雑に異なるセグメント構成を前提にしてしまうと、
カーネルは「どのセグメントを使っているか」を常に強く意識しなければなりません。
それに対して、できるだけフラットな見え方のリニアアドレス空間を共通で使い、
その先の物理マッピングをページングで切り替える方が、Linux 全体として整理しやすいです。

もう 1 つ重要なのは移植性です。
Linux 6.6 LTS は x86 専用 OS ではありません。
arm64、riscv、powerpc など、さまざまなアーキテクチャを支えています。
その中には、x86 のような本格的なセグメント機構を持たないものもあります。
したがって、OS の中心思想をセグメントに強く依存させると、Linux 全体の設計が不自然になります。
この意味で、ページング中心という選択は Linux にとってかなり本質的です。

#### ２章の３の２　フラットに近いアドレス空間という考え方

元の本文で特に重要なのは、Linux が主要なセグメントを 0 番地から始まる広い範囲として扱い、
論理アドレスのオフセットとリニアアドレスが一致するような使い方をしている、という点です。
これは、いわゆるフラットに近いメモリモデルと考えると分かりやすいです。

ここでの狙いは明快です。
セグメントごとに別々の複雑な世界を見せるのではなく、
できるだけ一様なアドレス空間を見せ、そのうえで保護や実配置の問題をほかの仕組みへ委ねるわけです。
そうすると、カーネルコードもユーザー空間も、かなり統一的な感覚でアドレスを扱えます。

もちろん、これは「セグメントが存在しない」という意味ではありません。
x86 の CPU は依然としてその仕組みを持っていますし、Linux も必要な最小限は使います。
ただし、設計思想としては、複雑なセグメント分割を前面に押し出すのではなく、
見かけ上フラットな空間を採ることで、後段のページング管理を主役にしている、と理解するのがよいです。

#### ２章の３の３　ユーザ空間とカーネル空間の区別

元の説明では、ユーザコード、ユーザデータ、カーネルコード、カーネルデータという 4 種類の基本的な区別が出てきます。
ここは現代 Linux を理解するうえでも大事です。
なぜなら、Linux は単に「ひとつの平らなメモリ世界」を作っているのではなく、
その中にユーザー側とカーネル側の厳密な境界を保っているからです。

ユーザモードのコードがカーネルの任意データへ自由に触れられないこと、
カーネルモードへ入るには syscall や例外や割り込みといった明確な入口が必要であること、
こうした保護の基本は CPU の特権レベルとメモリ保護の仕組みの上に成り立っています。

Linux 6.6 LTS では、Meltdown 対策以降の KPTI のように、
x86 系でもユーザー空間とカーネル空間の分け方は昔より繊細になっている面があります。
ただ、それでも大きな発想は変わりません。
ユーザー側とカーネル側は同じではなく、越境には厳格な条件がある。
この認識が、以後のメモリ管理や例外処理やシステムコール読解の土台になります。

#### ２章の３の４　CPL と実行レベルの切り替え

cs レジスタに含まれる特権レベル情報、つまり CPL の考え方も、この節の重要点です。
CPU は、いまどの権限レベルで実行しているかを把握し、それに応じてアクセス可能な資源を制限します。
Linux では、基本的にユーザモードとカーネルモードの 2 つの大きな世界を意識すればよいです。

ここで重要なのは、モード切り替えが単なる雰囲気の違いではなく、ハードウェア保護の実体だということです。
ユーザー空間のプログラムが勝手にカーネルメモリへ書けないのは、CPU がそのように許さないからです。
OS の保護は、善意や慣習ではなく、最終的にはこうした CPU の仕組みに支えられています。

Linux カーネルを読むと、システムコール入口、例外入口、割り込み入口で、
現在の文脈や特権レベルがどのように切り替わるかが重要になります。
その意味で CPL の概念は、単に古い x86 用語ではなく、実行文脈の境界を理解するための基本語だと思います。

#### ２章の３の５　Linux の GDT の見方

元の本文では、Linux の GDT にどのような種類のエントリが置かれているかが説明されています。
これも、すべての細部を覚える必要はありませんが、眺め方は重要です。
特に見ておくべきなのは、Linux が GDT を単なる歴史的遺物として放置しているのではなく、
必要な制御情報の置き場としてきちんと使っていることです。

たとえば、基本のコードセグメントやデータセグメントだけでなく、TSS や TLS のように、
実行制御やスレッド局所情報と関わる項目も登場します。
つまり GDT は、「昔のセグメント OS の名残」だけではなく、x86 上で Linux を動かすための低レベルな制御基盤の一部です。

ただし、Linux 6.6 LTS の読解で優先度が高いのは、GDT の全エントリ配置を暗記することではありません。
重要なのは、CPU が動作する前提として必要な低レベルの記述があり、Linux はそれを初期化し、必要に応じて CPU ごとに持つ、という理解です。
特に SMP 環境では CPU ごとの状態が関わるので、その意味でも「グローバルに見えて実際には CPU ごとの構造がある」という見方は後で効きます。

#### ２章の３の６　LDT と TLS の位置づけ

元の本文では、LDT をほとんどの通常アプリケーションが使わず、一部の特殊用途や互換用途で使うことが説明されています。
ここは現代 Linux らしさがよく出ています。
つまり、CPU が提供する仕組みを全部均等に重視するのではなく、
現実に必要なものと限定用途のものを切り分けているわけです。

LDT は歴史的にも面白いのですが、Linux 6.6 LTS の日常的な理解で主役になることはあまりありません。
それより重要なのは、TLS のようにスレッドごとの局所情報をどう効率よく扱うか、
あるいは特殊な互換実行や低レベル制御のために、セグメント機構の一部が今も残っていることです。

この見方は、Linux を学ぶときのバランス感覚として大事です。
CPU の全機能を同じ重さで覚えるのではなく、
「日常の主役」「限定用途だが重要」「歴史的理解として知っておく」の 3 段階くらいで重みづけしていく方が、研究ノートとして強くなります。

#### ２章の３の７　この節を 6.6 LTS 研究へつなぐ見方

2.3 のいちばん大きな意味は、Linux が x86 のセグメント機構を捨てたわけではなく、
しかし主役にもしていない、という中間の立場を取っていることを理解できる点にあります。
これは Linux の設計全体を読むときの重要な感覚です。
使える仕組みを全部前面に出すのではなく、移植性と保守性と実装の整理しやすさを考えて、使い方を絞るわけです。

この節の要点を短く言えば、Linux は x86 のセグメント機構を土台として必要最小限に使い、
見かけ上はフラットに近いアドレス空間を作り、その上でページングを中心に仮想記憶を構成している、ということです。
この理解が入ると、次のページング機構の節が急に本筋として見えてきます。

次に読むべき入口としては、概念面では mm/ の仮想記憶管理へ進むのが本筋です。
x86 の低レベル実装側を追いたいなら、arch/x86/include/asm/ や arch/x86/kernel/ の初期化まわり、
さらに today の Linux では x86_64 前提のエントリや page table 初期化の流れまで視野に入れると、
この節の「割り切り」が実コードとして見えてきます。

### ２章の４　ハードウェアのページング機構

ここから、いよいよ Linux 6.6 LTS のメモリ管理で本当に中心になるページング機構へ入ります。
2.2 と 2.3 ではセグメント機構を見てきましたが、Linux の仮想記憶を理解するうえで主役なのは明らかにこちらです。
プロセスごとに異なる仮想アドレス空間を見せること、ユーザー空間とカーネル空間を保護すること、
必要なときだけページを実体化すること、ファイルをメモリへ写像すること、こうした現代 Linux の重要機能は、ほぼすべてページングの上に乗っています。

元の本文は x86 の 32 ビットページングを入口にしていますが、この入口は今でも有効です。
ただし、Linux 6.6 LTS を研究する立場では、単に昔の 2 段ページテーブルを知るだけでは足りません。
ページ、ページフレーム、ページテーブル、保護ビット、巨大ページ、TLB、キャッシュ、そして x86_64 での多段化までを、
ひとつの連続した仕組みとして理解する必要があります。

#### ２章の４の１　ページとページフレーム

まず区別しなければいけないのは、ページとページフレームです。
ページは仮想アドレス空間を一定サイズで区切った単位であり、ページフレームは物理メモリ側を同じサイズで区切った単位です。
この 2 つを混同すると、仮想記憶の話がかなり分かりにくくなります。

プロセスが使っているのは仮想空間側のページです。
一方、RAM 上で実際に確保されるのは物理側のページフレームです。
Linux カーネルの仕事は、ある仮想ページをどの物理ページフレームへ対応付けるか、あるいはまだ何にも対応付けないかを管理することです。
つまり、仮想ページは見せ方の単位、ページフレームは実体の単位です。

Linux 6.6 LTS の研究では、この区別は最重要級です。
あとでページキャッシュ、匿名メモリ、copy-on-write、swap、NUMA、huge page を読むとき、
常に「いま話しているのは仮想ページなのか、物理ページフレームなのか」を見失わないことが大事になります。

#### ２章の４の２　ページテーブルという対応表

仮想ページと物理ページフレームの対応関係を持つのがページテーブルです。
CPU は仮想アドレスそのものでは RAM に届けません。
そのため、いま参照しようとしている仮想アドレスが、どの物理ページフレームの、どのオフセットに対応するのかを調べる必要があります。
この対応表を OS が用意し、MMU が実際の変換に使います。

元の本文では、32 ビット x86 の通常ページングとして、ページディレクトリとページテーブルの 2 段階構造が説明されています。
これは「大きな仮想空間全部に対して、最初から巨大な 1 枚表を持つのは非効率なので、必要なところだけ下位表を作る」という考え方です。
この考え方自体は今でも変わりません。
多段ページテーブルの本質は、広い仮想空間を細かく分割し、必要な部分にだけ変換表を実体化して RAM を節約することです。

Linux 6.6 LTS では 64 ビット環境が主流ですが、考え方は同じです。
段数が増えても、やっていることは「アドレスのビット列を何段かの索引に分けて、順番にたどって最後に物理ページフレームへ到達する」ということです。
だから 32 ビット時代の 2 段ページングは、いまでも多段ページングを理解する良い入口になります。

#### ２章の４の３　ページングと保護

ページングは、単に番地を変換するだけの仕組みではありません。
ページテーブルの各エントリには、存在しているか、書き込み可能か、ユーザーから触れてよいか、といった保護情報も入っています。
つまりページテーブルは、変換表であると同時に保護表でもあります。

この点が Linux にとってとても重要です。
ユーザー空間のプロセスがカーネルメモリを勝手に書き換えられないのは、
単に「そういう約束だから」ではなく、ページング機構がそのアクセスを禁止するからです。
書き込み禁止ページ、未割り当てページ、カーネル専用ページへ触れたときに例外が起きるのも、そのためです。

Linux 6.6 LTS を読むと、ページフォルトはエラーというより制御の入口としても使われます。
まだ実メモリを貼っていないページへ初回アクセスしたとき、copy-on-write で複製が必要になったとき、
ファイルマッピングのページを読み込む必要があるときなど、ページフォルトはカーネルが状態を進める契機になります。
その意味で、ページングと例外処理は密接に結びついています。

#### ２章の４の４　32 ビット x86 の通常ページング

元の本文がまず説明しているのは、4KB ページを使う通常の 32 ビットページングです。
ここではリニアアドレスをいくつかのフィールドに分け、上位側をページディレクトリやページテーブルの索引として使い、
下位 12 ビットをページ内オフセットとして使います。

この方式のポイントは、4KB という固定単位で仮想空間を扱うことにあります。
4KB は細かすぎず粗すぎず、保護や遅延割り当てや共有を行う単位として実用的です。
Linux の多くの基本操作も、このページ単位を前提に組み立てられています。

また、各プロセスは自分専用のページテーブル群を持ちます。
このため、同じ仮想アドレス値であっても、プロセス A とプロセス B で別の物理ページフレームを指すことができます。
これがプロセス分離の基本です。
各プロセスは自分だけの連続したメモリ世界を持っているように見えますが、実際にはカーネルが個別の対応表を持たせているわけです。

#### ２章の４の５　巨大ページという発想

元の本文では、拡張ページングとして 4MB ページの話が出てきます。
現代 Linux の文脈では、これを「巨大ページを使う発想の入口」として理解するとよいです。
ページを大きくすれば、広い仮想空間をより少ないエントリで表現でき、TLB ミスも減らしやすくなります。

ただし、ページを大きくすれば良いことばかりではありません。
細かな保護や割り当て粒度が粗くなり、内部断片化も増えやすくなります。
そのため、通常ページと巨大ページは用途によって使い分けられます。
Linux 6.6 LTS でも、transparent huge pages や hugetlb のように、大きなページを活用する仕組みがありますが、
それらは「とにかく大きくする」のではなく、性能と柔軟性のトレードオフの中で使われています。

ここでの学びは、ページサイズは単なる数値ではなく、変換コスト、管理コスト、性能特性に直結する設計要素だということです。

#### ２章の４の６　PAE と 32 ビットの限界

元の本文は次に PAE を説明しています。
これは 32 ビット時代に、リニアアドレス幅はそのままに、物理アドレス側だけを広げてより多くの RAM を扱えるようにした仕組みです。
歴史的にはとても重要です。
なぜなら、プロセスごとの仮想空間は 32 ビットのままでも、システム全体としては 4GB を超える RAM を活用したい、
という要求が強く存在したからです。

ただし、Linux 6.6 LTS を今から学ぶ立場では、PAE そのものを主役として深追いする必要は薄いです。
むしろ大事なのは、「仮想アドレス幅」と「物理アドレス幅」は必ずしも同じではない、という発想です。
そして、ハードウェアの制約が変わると、ページテーブルの段数やエントリの大きさまで変わりうる、ということです。

この見方は現代でも生きています。
x86_64 でも、理論上の 64 ビット全部をそのまま使うわけではなく、実際に有効なビット幅には制約があります。
つまり、アドレス空間は「型名のビット数」だけで決まる単純な話ではなく、CPU と OS の設計で運用される構造なのです。

#### ２章の４の７　64 ビットと多段ページング

現代の Linux 6.6 LTS を実際に読むなら、こちらの方が本筋です。
x86_64 では、32 ビット時代の単純な 2 段階では足りず、より多段のページテーブルを使います。
仮想アドレス空間が大きくなったぶん、それを現実的な RAM 消費で管理するために段数を増やしているわけです。

ここで重要なのは、段数が増えても本質は変わらないことです。
仮想アドレスをいくつかの索引とオフセットに分け、上位から順にテーブルをたどり、最後に実ページへ到達する。
この枠組みは同じです。
そのため、32 ビットの 2 段ページングを理解しておくことは、64 ビットの 4 段や 5 段ページングを理解するための土台になります。

Linux 6.6 LTS の実コードでは、アーキテクチャ差を吸収するために、できるだけ汎用的なページテーブル階層の見方を採っています。
したがって研究ノートとしては、「x86_64 は何段か」という暗記よりも、
Linux が多段ページテーブルという一般モデルの上で各アーキテクチャをどう吸収しているかを見る方が価値があります。

#### ２章の４の８　キャッシュと TLB

ページングの話は、変換表だけで終わりません。
もし CPU が毎回 RAM 上のページテーブルを深くたどっていたら、速度が出ません。
そのため CPU は、キャッシュや TLB を使って、データアクセスやアドレス変換を高速化しています。

まずキャッシュは、RAM より速い内部記憶を使って、よく使うデータや命令へのアクセスを速くする仕組みです。
Linux カーネルの設計でも、キャッシュライン、局所性、偽共有、アラインメントなどが性能に大きく効きます。
マルチコア環境ではキャッシュコヒーレンシも重要で、単にメモリへ書いたから終わり、とはなりません。

TLB は、ページテーブルの探索結果そのものを覚えておくキャッシュです。
これがあるおかげで、同じ仮想アドレス範囲への繰り返しアクセスで毎回多段テーブルを引き直さずに済みます。
しかし逆に、ページテーブルを書き換えたときには TLB の整合性を取らなければなりません。
ここで出てくるのが TLB flush や shootdown の話です。

Linux 6.6 LTS では、この TLB 管理がかなり重要です。
ページテーブルを変えたのに古い変換が CPU 内部へ残っていると、正しい保護も正しい対応付けも成り立たないからです。
つまり仮想記憶の実装は、「正しいページテーブルを作ること」で終わらず、
CPU 内部キャッシュまで含めて整合性を保つところまでが仕事になります。

#### ２章の４の９　この節を 6.6 LTS 研究へつなぐ見方

2.4 のいちばん大きな意味は、ページングが Linux の仮想記憶の主役であることをはっきり掴む点にあります。
ページ、ページフレーム、ページテーブル、保護ビット、ページフォルト、巨大ページ、TLB とキャッシュ。
これらは別々の小話ではなく、ひとつの大きな仕組みの各部品です。

この節の要点を短く言えば、Linux は仮想アドレス空間をページ単位で管理し、
ページテーブルによって物理メモリと結び付け、ハードウェア保護と例外機構を使って安全性を守り、
TLB とキャッシュで性能を稼いでいる、ということです。
この見方が入ると、次の Linux のページング機構の節で、なぜカーネルが汎用的な多段ページモデルを持つのかがかなり理解しやすくなります。

次に読むべき入口としては、概念面では mm/ のページフォルト、仮想メモリ領域、ページアロケータへ進むのが本筋です。
アーキテクチャ実装側では、x86_64 を前提にするなら arch/x86/mm/ や arch/x86/include/asm/pgtable 系を意識すると、
この節の説明が現代の Linux 6.6 LTS のコードと結びついてきます。
