---
title: "UmuOSの為のC言語４（中級）　４章　高度なファイルI/O"
---

# UmuOSの為のC言語（中級）　４

このノートは、UmuOSを進化させるためにプロセス管理・メモリ管理・ファイルI/O・シェル・システムコール設計に
直接役立つ形で抽象化し、再構成することであります。
すなわち、UmuOSの構造へ還元するための実践ノートであります。
LinuxのAPIやシステムコールは、OSがどのようにふるまうかでもあり、その構造を理解することはUmuOSの設計力を
高めることに直結すると思います。

## ４章　高度なファイルI/O

この章では、2章の低レベルI/O、3章の標準I/Oを土台にして、より高度なファイルI/Oを整理します。

2章では、`open()`、`read()`、`write()`、`close()`、`lseek()` といった基本を見ました。
3章では、`FILE *`、`fopen()`、`fread()`、`fwrite()`、`fflush()` など、標準I/Oによるユーザー空間バッファリングを見ました。

4章では、その先にある、より実践的でOS設計に近いI/Oの考え方を扱っていきます。
たとえば、ベクトルI/O、非同期I/O、多重化、メモリマップI/O、ファイルI/Oの性能や整合性に関わる話が中心になります。

UmuOSの視点では、この章はかなり重要です。
シェル、ログ、ファイルコピー、ネットワーク処理、デバイスI/O、ファイルシステム設計などでは、単純な `read()` / `write()` だけでは見通しが悪くなる場面が出てくるからです。

Ushのようなシェルを作る場合でも、リダイレクト、パイプ、標準入出力の制御、複数fdの監視などは、高度なI/Oの理解につながります。

この章も、古い説明や古いサンプルコードは、現在のLinux/POSIX/glibcの感覚に合わせて整理し直しながら進めます。

4章で扱う主な内容は、だいたい次のようなものになります。

scatter-gather I/O
複数のバッファを、一度のシステムコールでまとめて読み書きする仕組みです。
ヘッダ、本文、フッタのように、データが最初から複数の部品に分かれている場合に役立ちます。

epoll
複数のファイルディスクリプタを監視する仕組みです。
古くからある `select()` や `poll()` より、大量のfdを扱う用途に向いています。
現在のLinuxサーバープログラミングでは、ネットワークI/Oの多重化でかなり重要な位置にあります。

メモリマップI/O
ファイルをメモリ空間へ対応づけて、通常のメモリアクセスのようにファイルを扱う仕組みです。
`read()` / `write()` とは違う見方でファイルを扱えるため、特定のI/Oパターンでは便利です。

アクセスパターンのヒント、アドバイス
アプリケーションが「これから順番に読むと思います」「ランダムアクセスします」などの情報をカーネルへ伝える仕組みです。
カーネル側の先読みやキャッシュ戦略に影響するため、性能の話と関係してきます。

非同期I/O
I/Oの完了をその場で待たずに、あとから完了を受け取る考え方です。
古いLinux AIO、POSIX AIO、そして現在では `io_uring` なども含めて、時代ごとの違いを意識する必要があります。

この章では、単にAPIの使い方を覚えるというより、LinuxカーネルがI/Oをどう効率化しようとしているのか、そしてUmuOSで同じような仕組みを考えるなら
どの抽象化が必要になりそうか、という視点で見ていきます。

### ４章の１　scatter-gather I/O

scatter-gather I/Oとは、複数のバッファを一度のシステムコールで読み書きするI/O方式です。
日本語では、スキャッタ・ギャザI/O、または、ばらまき・まとめI/Oのように説明されます。

通常の `read()` や `write()` は、基本的には1つの連続したバッファを対象にします。
これをリニアI/Oと呼ぶことがあります。

一方、scatter-gather I/Oでは、複数のバッファを配列として渡します。
読み取りの場合は、1つのデータストリームから複数のバッファへ順番にデータをばらまきます。
書き込みの場合は、複数のバッファに分かれたデータを、1つのデータストリームへ順番にまとめて書き込みます。

このため、ベクタI/O、vectored I/Oとも呼ばれます。

イメージとしては、次のような感じです。

```text
通常の write():
	[ 1つの大きなバッファ ]  ->  fd

writev():
	[ ヘッダ ][ 本文 ][ 改行 ] ->  fd

readv():
	fd -> [ ヘッダ用バッファ ][ 本文用バッファ ][ 余り用バッファ ]
```

たとえば、ネットワークの応答やログ出力では、ヘッダ部分、本文部分、改行部分などが別々のメモリ領域にあることがあります。
このとき、いったん全部を1つの大きなバッファへコピーしてから `write()` することもできます。
しかし、それだとコピー用のメモリ確保や、データ結合の手間が増えます。

`writev()` を使えば、分かれているバッファを分かれたままカーネルへ渡して、一度のシステムコールで書き込めます。
ここが scatter-gather I/O の大きな利点です。

#### ４章の１の１　scatter-gather I/O の利点

scatter-gather I/Oには、主に次のような利点があります。

より自然な処理
データが最初から複数の部品に分かれている場合、その構造を保ったままI/Oできます。
たとえば、ヘッダ、本文、終端文字のように分かれているデータを、無理に1つへ結合しなくてもよくなります。

効率性
複数回の `write()` を、1回の `writev()` にまとめられます。
システムコールは普通の関数呼び出しより重いので、回数を減らせるのは重要です。

パフォーマンス
システムコール発行回数を減らせるだけでなく、カーネル内部のI/O処理にも合いやすい形になります。
Linuxカーネル内部では、I/Oがベクタ化された形で扱われる場面が多く、`read()` や `write()` も「要素が1つだけのベクタI/O」と見なせます。

UmuOSの視点では、これはかなり面白いです。
最初は `read()` / `write()` のような単純なI/Oだけで十分に見えますが、OS内部のI/O管理を考えると、実は「複数のメモリ領域をまとめて扱う」抽象化があると便利です。
ファイル、パイプ、ソケット、端末などを同じように扱いたい場合、内部表現としてベクタI/Oを持っておくと設計の見通しがよくなる可能性があります。

#### ４章の１の２　readv() と writev()

POSIXでは、scatter-gather I/Oを行うための関数として `readv()` と `writev()` が定義されています。
Linuxでも利用できます。

プロトタイプは次の通りです。

```c
#include <sys/uio.h>

ssize_t readv(int fd, const struct iovec *iov, int iovcnt);
ssize_t writev(int fd, const struct iovec *iov, int iovcnt);
```

`readv()` は、ファイルディスクリプタ `fd` からデータを読み取り、`iov` が指す複数のバッファへ順番に格納します。
`writev()` は、`iov` が指す複数のバッファの内容を、ファイルディスクリプタ `fd` へ順番に書き込みます。

ここで出てくる `struct iovec` は、1つのバッファを表す構造体です。

```c
#include <sys/uio.h>

struct iovec {
	void  *iov_base;  // バッファの先頭アドレス
	size_t iov_len;   // バッファのバイト数
};
```

`iov_base` は、読み書きしたいメモリ領域の先頭アドレスです。
`iov_len` は、そのメモリ領域のサイズです。

`struct iovec` が1個なら、1つのバッファを表します。
これを配列にすると、複数のバッファをまとめたベクタになります。

```text
iov[0] -> バッファA, サイズA
iov[1] -> バッファB, サイズB
iov[2] -> バッファC, サイズC
```

`readv()` も `writev()` も、`iov[0]`、`iov[1]`、`iov[2]` のように、先頭から順番に処理します。
最後に処理されるのは `iov[iovcnt - 1]` です。

たとえば `writev()` の場合、次のように書き込まれます。

```text
iov[0] の内容 -> iov[1] の内容 -> iov[2] の内容
```

ファイル側から見ると、複数のバッファに分かれていたことは見えません。
結果としては、順番に連結された1つのデータとして書き込まれます。

#### ４章の１の３　戻り値とエラー

`readv()` と `writev()` は、成功すると処理したバイト数を返します。
戻り値の型は `ssize_t` です。

エラーが起きた場合は `-1` を返し、`errno` に原因が設定されます。
基本的なエラーは `read()` や `write()` と同じように考えればよいです。

ただし、ベクタI/O特有の注意点もあります。

まず、すべての `iov_len` の合計が `SSIZE_MAX` を超える場合はエラーになります。
戻り値の型が `ssize_t` なので、表現できる範囲を超えるI/O要求は扱えないためです。
この場合、Linuxでは `EINVAL` になる可能性があります。

次に、`iovcnt` の値にも上限があります。
POSIXでは、`iovcnt` は1以上 `IOV_MAX` 以下とされています。
Linuxでは、現在多くの環境で `IOV_MAX` は1024です。
ただし、固定値を決め打ちするより、`sysconf(_SC_IOV_MAX)` で確認できることを覚えておくとよいです。

```c
#include <limits.h>
#include <unistd.h>

long max_iov = sysconf(_SC_IOV_MAX);
```

`iovcnt` に0を渡した場合の扱いは、Unix系OSによって違いがあります。
Linuxでは0を返す動作になりますが、移植性を考えるなら、基本的には0を渡さない設計にしておくのが無難です。

また、`writev()` は常に全バイトを書き込むとは限りません。
通常ファイルでは全部書けることが多いですが、パイプ、ソケット、非ブロッキングI/O、シグナル割り込みなどが絡むと、途中までしか書けないことがあります。
これは `write()` と同じで、戻り値を見て「何バイト処理できたか」を確認する必要があります。

#### ４章の１の４　writev() のサンプルコード

次に、`writev()` の簡単なサンプルを見ます。
古い本のサンプルでは `open()` に `O_CREAT` を指定しているのに、パーミッションの引数を渡していないものがあります。
現在のCでは、これは避けるべきです。

`O_CREAT` を使う場合、`open()` の第3引数に作成時のモードを指定します。
たとえば `0644` なら、所有者は読み書き可能、グループとその他は読み取り可能です。

```c
#include <errno.h>
#include <fcntl.h>
#include <stdio.h>
#include <string.h>
#include <sys/uio.h>
#include <unistd.h>

int main(void)
{
	const char *parts[] = {
		"header: tama-log\n",
		"body: scatter-gather I/O example\n",
		"end\n"
	};
	struct iovec iov[3];

	for (size_t i = 0; i < 3; i++) {
		iov[i].iov_base = (void *)parts[i];
		iov[i].iov_len = strlen(parts[i]);
	}

	int fd = open("writev-example.txt", O_WRONLY | O_CREAT | O_TRUNC | O_CLOEXEC, 0644);
	if (fd == -1) {
		perror("open");
		return 1;
	}

	ssize_t written = writev(fd, iov, 3);
	if (written == -1) {
		perror("writev");
		close(fd);
		return 1;
	}

	printf("wrote %zd bytes\n", written);

	if (close(fd) == -1) {
		perror("close");
		return 1;
	}

	return 0;
}
```

このコードでは、3つの文字列を1つのファイルへまとめて書き込んでいます。
ポイントは、文字列を1つの大きなバッファへコピーしていないことです。

```text
parts[0] -> "header: tama-log\n"
parts[1] -> "body: scatter-gather I/O example\n"
parts[2] -> "end\n"
```

この3つを `struct iovec` の配列として `writev()` に渡しています。
ファイルには、次のように連結された内容が書き込まれます。

```text
header: tama-log
body: scatter-gather I/O example
end
```

コンパイル例です。

```bash
gcc -Wall -Wextra -std=c17 writev_example.c -o writev_example
./writev_example
cat writev-example.txt
```

ここで、古いサンプルから少し直している点があります。

`main(void)` と書く
Cでは、引数を取らない `main` は `int main(void)` と書くのが明確です。

`O_CREAT` にはモードを渡す
`open()` でファイルを作成するなら、第3引数に `0644` などを指定します。

`strlen(parts[i]) + 1` にしない
テキストファイルへ文字列を書く場合、通常は末尾のヌル文字 `\0` まで書き込みません。
古いサンプルでは `+ 1` してヌル文字まで書くことがありますが、普通のテキストファイルとしては不要です。

`%zd` で `ssize_t` を表示する
`writev()` の戻り値は `ssize_t` なので、`printf()` では `%zd` を使います。

`O_CLOEXEC` を使う
`exec()` で別プログラムを起動したときに、意図せずfdを引き継がないようにする指定です。
シェルやサーバープログラムではfd漏れ対策として重要です。

UmuOSやUshの視点では、`O_CLOEXEC` はかなり大事です。
シェルが外部コマンドを起動するとき、不要なfdを子プロセスへ渡してしまうと、パイプが閉じない、ファイルが開きっぱなしになる、というバグにつながるからです。

#### ４章の１の５　writev() の部分書き込みについて

上のサンプルは通常ファイルへ小さいデータを書いているので、ほとんどの場合は一度で書き込めます。
しかし、実用コードでは、`writev()` の戻り値が要求した合計サイズより小さい場合があります。

たとえば、次のような場合です。

パイプへ書く場合
パイプのバッファに空きが少なければ、途中までしか書けないことがあります。

ソケットへ書く場合
ネットワーク送信バッファの状態によって、部分書き込みになることがあります。

非ブロッキングfdの場合
`O_NONBLOCK` が付いているfdでは、すぐに処理できる分だけ処理して戻ることがあります。

シグナルで割り込まれた場合
途中まで書いたところでシグナルが入り、戻ってくる可能性があります。

そのため、本当に全データを書きたい場合は、戻り値を見て残りを再度書く処理が必要になります。
ただし、`writev()` の部分書き込み処理は少し面倒です。
なぜなら、戻り値は「全体で何バイト書けたか」だけなので、どの `iovec` の途中まで進んだかを計算し直す必要があるからです。

イメージとしては、次のようになります。

```text
要求:
	iov[0] 10バイト
	iov[1] 20バイト
	iov[2] 30バイト

writev() の戻り値:
	25バイト

結果:
	iov[0] は10バイトすべて書けた
	iov[1] は15バイトだけ書けた
	iov[2] はまだ書けていない
```

この場合、次は `iov[1]` の残り5バイトと、`iov[2]` の30バイトを書けばよいことになります。

初心者段階では、まず `writev()` は複数バッファを一度に渡せる仕組みだと理解すれば十分です。
ただし、実用コードでは、戻り値を見て部分書き込みに対応する必要がある、という点は早めに意識しておくとよいです。

#### ４章の１の６　readv() のサンプルコード

次に、`readv()` のサンプルです。
先ほど `writev()` で作った `writev-example.txt` を、3つのバッファへ分けて読み取ります。

```c
#include <fcntl.h>
#include <stdio.h>
#include <sys/uio.h>
#include <unistd.h>

int main(void)
{
	char header[17];
	char body[37];
	char tail[4];

	struct iovec iov[3] = {
		{ .iov_base = header, .iov_len = sizeof(header) },
		{ .iov_base = body,   .iov_len = sizeof(body) },
		{ .iov_base = tail,   .iov_len = sizeof(tail) }
	};

	int fd = open("writev-example.txt", O_RDONLY | O_CLOEXEC);
	if (fd == -1) {
		perror("open");
		return 1;
	}

	ssize_t nread = readv(fd, iov, 3);
	if (nread == -1) {
		perror("readv");
		close(fd);
		return 1;
	}

	printf("read %zd bytes\n", nread);

	for (size_t i = 0; i < 3; i++) {
		printf("iov[%zu]: %.*s", i, (int)iov[i].iov_len, (char *)iov[i].iov_base);
	}

	if (close(fd) == -1) {
		perror("close");
		return 1;
	}

	return 0;
}
```

このコードでは、読み取った内容を3つの配列へ順番に格納しています。

```text
fdから読むデータ:
	header: tama-log\nbody: scatter-gather I/O example\nend\n

readv() の格納先:
	header[]
	body[]
	tail[]
```

注意点として、`readv()` は文字列を作る関数ではありません。
つまり、読み込んだバッファの末尾に自動で `\0` を付けてくれるわけではありません。
これは `read()` と同じです。

そのため、サンプルでは `printf("%.*s", ...)` の形で、表示するバイト数を指定しています。
`%s` だけで表示すると、ヌル終端されていないメモリを読み続けてしまう危険があります。

ここはC初心者にはかなり重要です。
`read()` や `readv()` は「文字列を読む」のではなく、「バイト列を読む」関数です。
文字列として扱いたい場合は、自分で終端 `\0` を入れるか、表示時に長さを指定する必要があります。

#### ４章の１の７　素朴な writev() 実装イメージ

`writev()` は、ユーザー空間で素朴に書くなら、複数回の `write()` をループする形でも似たことはできます。

```c
#include <sys/uio.h>
#include <unistd.h>

ssize_t naive_writev(int fd, const struct iovec *iov, int iovcnt)
{
	ssize_t total = 0;

	for (int i = 0; i < iovcnt; i++) {
		ssize_t written = write(fd, iov[i].iov_base, iov[i].iov_len);
		if (written == -1) {
			return -1;
		}

		total += written;

		if ((size_t)written != iov[i].iov_len) {
			break;
		}
	}

	return total;
}
```

ただし、これは本物の `writev()` と同じではありません。
単に理解用のイメージです。

違いとして、まずシステムコール回数が増えます。
`iovcnt` が3なら `write()` を最大3回呼ぶため、`writev()` 1回より重くなります。

また、複数のバッファを1つのI/Oとして扱う意味も弱くなります。
特にパイプやソケットでは、途中に別プロセスの書き込みが挟まる可能性や、部分書き込みの扱いなども考える必要があります。

Linuxの `readv()` / `writev()` はシステムコールとして実装されていて、カーネル内部でベクタI/Oを扱います。
そのため、単なる便利関数というより、カーネルのI/O処理に直接関係するAPIと考えたほうがよいです。

#### ４章の１の８　Linuxカーネル内部の見方

Linuxでは、`read()` と `write()` だけが基本で、`readv()` と `writev()` があとから無理やり足されたもの、というより、内部的にはベクタI/Oの考え方がかなり自然です。

ざっくり言えば、`read()` や `write()` は、要素数1の `iovec` を使うI/Oのように見ることができます。

```text
write(fd, buf, len)

これは内部的には、かなり単純化して言えば、

iov[0].iov_base = buf
iov[0].iov_len  = len
writev(fd, iov, 1)

のような考え方に近いです。
```

もちろん、実際のカーネル実装は単純な置き換えではありません。
しかし、I/O対象を「1つ以上のメモリ領域」として抽象化する、という考え方はLinuxの内部設計と相性がよいです。

古い本では、カーネル内部の小さなベクタ数向け最適化として、スタック上の小規模配列を使う話が出てきます。
たとえば `UIO_FASTIOV` のような定数です。
ただし、このような内部実装の細部はカーネルバージョンによって変わります。

現在の研究では、固定で「8個以下なら必ず速い」と丸暗記するより、次のように理解しておく方がよいです。

```text
ベクタ数が少ない場合:
	カーネル内部で軽く扱える可能性が高い

ベクタ数が多すぎる場合:
	iovec配列の検査やコピーなどのオーバーヘッドが増える

実用上:
	必要な数だけ使う
	ただし、極端に細かく分けすぎない
```

つまり、`writev()` は便利だからといって、1バイトごとに `iovec` を分けるような使い方はよくありません。
ヘッダ、本文、改行、固定フッタなど、自然な単位で分けるのがよさそうです。

#### ４章の１の９　UmuOSでどう考えるか

UmuOSのような自作OSを考えるとき、scatter-gather I/Oは次のような設計課題につながります。

fdは何を指すのか
通常ファイル、パイプ、端末、ソケットなどを、同じfdインターフェースで扱えるかどうかが重要です。

I/O要求をどう表現するか
1つのバッファだけを前提にするのか、複数バッファをまとめて扱えるようにするのかで、内部設計が変わります。

コピーを減らせるか
ユーザー空間の複数バッファを、カーネルがどのように扱うかは性能に影響します。
ただし、ユーザー空間のポインタをカーネルが触る場合は、安全性チェックも必要になります。

部分読み書きをどう返すか
I/Oは常に全部成功するとは限りません。
戻り値として「何バイト処理できたか」を返す設計は、LinuxのI/O APIでも一貫して重要です。

Ushのようなシェルを作る場合、最初から `readv()` / `writev()` 相当まで必要とは限りません。
しかし、ログ出力、パイプ処理、複数fdの扱い、将来のソケット通信などを考えると、ベクタI/Oの考え方を知っておく価値は大きいです。

特に、シェルは文字列をたくさん扱います。
プロンプト、コマンド名、引数、改行、エラーメッセージなどを、毎回1つのバッファへ結合してから出すのではなく、複数の部品としてまとめて出す設計も考えられます。

たとえば、エラーメッセージなら次のような部品に分けられます。

```text
"ush: "
コマンド名
": command not found\n"
```

これらを `writev()` で標準エラーへまとめて出せば、余計な文字列結合を減らせます。
LinuxのAPIを学ぶと、こういう小さな設計の選択肢が増えていく感じです。

次は、複数のファイルディスクリプタを効率よく監視する仕組みとして、`epoll` を見ていきます。

### ４章の２　Event Pollインターフェース

`epoll` は、複数のファイルディスクリプタを効率よく監視するためのLinux独自のI/O多重化インターフェースです。

2章で見た `select()` や `poll()` でも、複数のfdを監視できます。
たとえば、標準入力、ソケット、パイプなどのうち、どれが読み取り可能になったかを待つ、ということができます。

しかし、`select()` や `poll()` には大きな弱点があります。
監視したいfdの一覧を、システムコールを呼ぶたびに毎回カーネルへ渡す必要があることです。

fdが数個なら問題は小さいです。
しかし、ネットワークサーバーのように、数百、数千、あるいはそれ以上の接続を扱う場合、毎回すべてのfdをカーネルへ渡して、カーネル側でもすべてを調べるのは重くなります。

イメージとしては、次のような違いです。

```text
poll()/select():
	毎回、監視したいfd一覧を渡す
	毎回、カーネルがfd一覧を調べる
	毎回、結果をユーザー空間へ返す

epoll:
	先に監視したいfdをepollコンテキストへ登録する
	変更があるときだけ追加、変更、削除する
	待つときは「発生したイベント」だけを受け取る
```

`epoll` では、監視対象のfd集合をカーネル側に保持します。
そのため、イベント待ちのたびに全fd一覧を渡し直す必要がありません。
この設計により、大量のfdを扱うサーバープログラムでスケールしやすくなります。

現在のLinuxでは、Webサーバー、プロキシ、イベント駆動型のネットワークプログラムなどで、`epoll` は非常によく使われます。
ただし、POSIX標準ではなくLinux固有APIです。
移植性を重視するプログラムでは、BSD系の `kqueue`、WindowsのIOCP、または抽象化ライブラリを使うこともあります。

UmuOSの視点では、`epoll` は「fdをたくさん監視するには、fd一覧を毎回走査する設計だけでは限界がある」ということを教えてくれます。
自作OSで最初から `epoll` 相当まで作る必要はないかもしれません。
しかし、パイプ、端末、ソケット、プロセス通知などを扱うようになると、イベント待ちの設計は必ず重要になります。

#### ４章の２の１　epollの基本構造

`epoll` は、主に3つの操作で使います。

```text
1. epollコンテキストを作る
	   epoll_create1()

2. 監視したいfdを追加、変更、削除する
	   epoll_ctl()

3. イベントが発生するまで待つ
	   epoll_wait()
```

ここでいう epollコンテキスト とは、監視対象fdの集合をカーネル側で管理するためのオブジェクトです。
Linuxでは、このepollコンテキスト自体もファイルディスクリプタとして表現されます。

つまり、`epoll_create1()` を呼ぶと、epoll用のfdが返ってきます。
このfdは普通のファイルを指しているわけではありません。
`epoll_ctl()` や `epoll_wait()` に渡すためのハンドルです。

イメージとしては、次のようになります。

```text
epfd = epoll_create1(...)

epfd が指すもの:
	カーネル内のepollコンテキスト
		監視対象fd 4
		監視対象fd 5
		監視対象fd 8
```

この `epfd` もfdなので、使い終わったら `close(epfd)` で閉じます。
ここは地味ですが大事です。

#### ４章の２の２　epollコンテキストの作成

古い本では、`epoll_create()` が紹介されていることが多いです。
プロトタイプは次のような形です。

```c
#include <sys/epoll.h>

int epoll_create(int size);
```

`epoll_create()` は、epollコンテキストを作成し、それに対応するfdを返します。
エラーの場合は `-1` を返し、`errno` に原因が入ります。

ただし、現在のLinuxプログラミングでは、基本的には `epoll_create1()` を使う方がよいです。

```c
#include <sys/epoll.h>

int epoll_create1(int flags);
```

`epoll_create1()` は Linux 2.6.27 以降で利用できます。
現在の一般的なLinux環境では、こちらを使うのが自然です。

よく使う指定は `EPOLL_CLOEXEC` です。

```c
int epfd = epoll_create1(EPOLL_CLOEXEC);
if (epfd == -1) {
	perror("epoll_create1");
	return 1;
}
```

`EPOLL_CLOEXEC` は、作成したepoll fdに close-on-exec を設定します。
つまり、`exec()` で別プログラムを起動したときに、このfdを引き継がないようにします。

Ushのようなシェルでは、これは特に重要です。
シェルは `fork()` して `exec()` する処理をたくさん行います。
不要なfdを子プロセスへ渡してしまうと、パイプが閉じない、リダイレクトが変な形で残る、監視用fdが外部コマンドへ漏れる、などの原因になります。

古い `epoll_create()` の `size` 引数は、もともと「このくらいのfdを監視する予定です」というヒントでした。
しかし、Linux 2.6.8以降では実質的に意味を持たず、正の値かどうかの確認に使われる程度です。
そのため、現在は `epoll_create1()` を使うほうがすっきりします。

`epoll_create1()` で起こりうる主なエラーは次のようなものです。

EINVAL
`flags` に無効な値を渡した場合です。

EMFILE
そのプロセスが開けるfd数の上限に達している場合です。

ENFILE
システム全体で開けるファイル数の上限に達している場合です。

ENOMEM
カーネルが必要なメモリを確保できなかった場合です。

#### ４章の２の３　epollコンテキストの制御

epollコンテキストへfdを追加したり、監視イベントを変更したり、削除したりするには `epoll_ctl()` を使います。

```c
#include <sys/epoll.h>

int epoll_ctl(int epfd, int op, int fd, struct epoll_event *event);
```

引数の意味は次の通りです。

`epfd`
`epoll_create1()` などで作成したepollコンテキストのfdです。

`op`
追加、変更、削除のどれを行うかを指定します。

`fd`
監視対象にしたいファイルディスクリプタです。

`event`
どのイベントを監視するか、イベント発生時にどのデータを返すかを指定する構造体です。

`op` に渡せる主な値は次の3つです。

EPOLL_CTL_ADD
fdをepollコンテキストへ追加します。

EPOLL_CTL_MOD
すでに追加されているfdの監視イベントを変更します。

EPOLL_CTL_DEL
fdをepollコンテキストから削除します。

`struct epoll_event` は、現在のLinuxではおおむね次のような構造です。

```c
#include <stdint.h>
#include <sys/epoll.h>

typedef union epoll_data {
	void     *ptr;
	int       fd;
	uint32_t  u32;
	uint64_t  u64;
} epoll_data_t;

struct epoll_event {
	uint32_t     events;
	epoll_data_t data;
};
```

実際にはヘッダで定義済みなので、自分でこの構造体を定義する必要はありません。
ここではイメージをつかむために載せています。

`events` には、監視したいイベントをビットORで指定します。
よく使うものは次の通りです。

EPOLLIN
読み取り可能になったことを表します。
次の `read()` がブロックせずに進められる可能性があります。

EPOLLOUT
書き込み可能になったことを表します。
次の `write()` がブロックせずに進められる可能性があります。

EPOLLERR
エラーが発生したことを表します。
これは明示的に指定しなくても通知されます。

EPOLLHUP
ハングアップが発生したことを表します。
相手側が閉じたパイプやソケットなどで見かけます。
これも明示的に指定しなくても通知されます。

EPOLLPRI
緊急データ、または高優先データがあることを表します。
普通のファイルI/Oではあまり使いません。

EPOLLET
エッジトリガで監視します。
これを指定しない場合、通常はレベルトリガです。

EPOLLONESHOT
一度イベントが発生したら、そのfdの監視を一時的に無効化します。
再び監視したい場合は `EPOLL_CTL_MOD` で再設定します。

`data` には、ユーザーが自由に使える値を入れられます。
イベントが発生したとき、`epoll_wait()` の結果として同じ `data` が返ってきます。

一番分かりやすい使い方は、`event.data.fd = fd` として、イベント発生時にfdを取り出せるようにする方法です。

```c
struct epoll_event event;

event.events = EPOLLIN;
event.data.fd = fd;

if (epoll_ctl(epfd, EPOLL_CTL_ADD, fd, &event) == -1) {
	perror("epoll_ctl: add");
}
```

すでに登録したfdの監視イベントを変更する場合は、`EPOLL_CTL_MOD` を使います。

```c
struct epoll_event event;

event.events = EPOLLIN | EPOLLOUT;
event.data.fd = fd;

if (epoll_ctl(epfd, EPOLL_CTL_MOD, fd, &event) == -1) {
	perror("epoll_ctl: mod");
}
```

削除する場合は、現在のLinuxでは `event` に `NULL` を渡せます。

```c
if (epoll_ctl(epfd, EPOLL_CTL_DEL, fd, NULL) == -1) {
	perror("epoll_ctl: del");
}
```

古いLinuxカーネルでは、`EPOLL_CTL_DEL` でも `NULL` を嫌う時期がありました。
しかし、現在のLinuxを前提にするなら、削除時は `NULL` で問題ありません。
古い互換性を本気で考える場合だけ、ダミーの `struct epoll_event` を渡す、という理解でよいと思います。

`epoll_ctl()` で起こりうる主なエラーも整理しておきます。

EBADF
`epfd` または `fd` が無効なファイルディスクリプタです。

EEXIST
`EPOLL_CTL_ADD` しようとしたfdが、すでに登録されています。

EINVAL
`epfd` がepoll fdではない、`op` が無効、または `epfd` と `fd` が同じ、などです。

ENOENT
`EPOLL_CTL_MOD` や `EPOLL_CTL_DEL` を指定したのに、そのfdが登録されていません。

ENOMEM
カーネルが必要なメモリを確保できませんでした。

EPERM
そのfdがepollで監視できない種類のfdです。

#### ４章の２の４　epoll_wait()でイベントを待つ

epollコンテキストに登録したfdのイベントを待つには、`epoll_wait()` を使います。

```c
#include <sys/epoll.h>

int epoll_wait(int epfd,
			   struct epoll_event *events,
			   int maxevents,
			   int timeout);
```

引数の意味は次の通りです。

`epfd`
epollコンテキストのfdです。

`events`
発生したイベントを受け取る配列です。

`maxevents`
一度に受け取る最大イベント数です。1以上である必要があります。

`timeout`
待ち時間をミリ秒で指定します。

`timeout` の指定は重要です。

```text
timeout = -1
	イベントが発生するまでずっと待つ

timeout = 0
	待たずにすぐ返る
	イベントがなければ0を返す

timeout > 0
	指定ミリ秒だけ待つ
	その間にイベントがなければ0を返す
```

`epoll_wait()` は、成功すると発生したイベント数を返します。
0を返した場合は、タイムアウトしたという意味です。
エラーの場合は `-1` を返し、`errno` に原因が入ります。

よくある使い方は次のようになります。

```c
#include <errno.h>
#include <stdio.h>
#include <sys/epoll.h>

#define MAX_EVENTS 64

int wait_events(int epfd)
{
	struct epoll_event events[MAX_EVENTS];

	for (;;) {
		int nready = epoll_wait(epfd, events, MAX_EVENTS, -1);
		if (nready == -1) {
			if (errno == EINTR) {
				continue;
			}

			perror("epoll_wait");
			return -1;
		}

		for (int i = 0; i < nready; i++) {
			printf("event=%u on fd=%d\n",
				   events[i].events,
				   events[i].data.fd);

			/*
			 * 実際のプログラムでは、events[i].events を見て、
			 * events[i].data.fd に対して read() や write() を行います。
			 */
		}
	}
}
```

ここでは `malloc()` を使わず、固定長配列をスタック上に置いています。
イベントを一度にいくつ受け取るかが分かっている小さなサンプルなら、これで十分です。

実用コードでは、監視対象fdの数、イベント処理の設計、スレッド構成などによって、配列サイズやメモリ確保方法を決めます。

`epoll_wait()` で起こりうる主なエラーは次の通りです。

EBADF
`epfd` が無効なfdです。

EFAULT
`events` が書き込み可能なメモリを指していません。

EINTR
イベント待ち中にシグナルで割り込まれました。
実用コードでは、必要に応じて再試行します。

EINVAL
`epfd` がepoll fdではない、または `maxevents` が0以下です。

#### ４章の２の５　小さなepollサンプル

ここでは、標準入力を `epoll` で監視する小さなサンプルを見ます。
標準入力、つまり fd 0 が読み取り可能になったら、入力された行を読み取って表示します。

```c
#include <errno.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <sys/epoll.h>
#include <unistd.h>

#define MAX_EVENTS 8
#define BUF_SIZE 4096

int main(void)
{
	int epfd = epoll_create1(EPOLL_CLOEXEC);
	if (epfd == -1) {
		perror("epoll_create1");
		return 1;
	}

	struct epoll_event event;
	memset(&event, 0, sizeof(event));
	event.events = EPOLLIN;
	event.data.fd = STDIN_FILENO;

	if (epoll_ctl(epfd, EPOLL_CTL_ADD, STDIN_FILENO, &event) == -1) {
		perror("epoll_ctl");
		close(epfd);
		return 1;
	}

	puts("type something. Ctrl-D exits.");

	for (;;) {
		struct epoll_event events[MAX_EVENTS];
		int nready = epoll_wait(epfd, events, MAX_EVENTS, -1);

		if (nready == -1) {
			if (errno == EINTR) {
				continue;
			}

			perror("epoll_wait");
			close(epfd);
			return 1;
		}

		for (int i = 0; i < nready; i++) {
			if (events[i].data.fd == STDIN_FILENO &&
				(events[i].events & EPOLLIN)) {
				char buf[BUF_SIZE];
				ssize_t nread = read(STDIN_FILENO, buf, sizeof(buf));

				if (nread == -1) {
					perror("read");
					close(epfd);
					return 1;
				}

				if (nread == 0) {
					close(epfd);
					return 0;
				}

				if (write(STDOUT_FILENO, buf, (size_t)nread) == -1) {
					perror("write");
					close(epfd);
					return 1;
				}
			}
		}
	}
}
```

コンパイル例です。

```bash
gcc -Wall -Wextra -std=c17 epoll_stdin.c -o epoll_stdin
./epoll_stdin
```

このサンプルでは標準入力だけを監視しているので、`epoll` のありがたみはまだ小さいです。
しかし、fdを複数登録すれば、標準入力、パイプ、ソケットなどを同じ待ちループで扱えるようになります。

Ushで考えるなら、将来的に次のようなものを同時に待つ設計が考えられます。

```text
標準入力からのコマンド入力
ジョブ制御に関係する通知
パイプや疑似端末からの出力
内部イベント用のfd
```

小さなシェルではここまで必要ないかもしれません。
しかし、イベントループという考え方は、OSやシェルを大きくしていくときにかなり重要になります。

#### ４章の２の６　レベルトリガとエッジトリガ

`epoll` には、レベルトリガとエッジトリガという2つのイベント通知方式があります。

デフォルトはレベルトリガです。
`EPOLLET` を指定するとエッジトリガになります。

まず、レベルトリガは「状態」を見ます。
読み取り可能なデータが残っている間は、何度でも「読み取り可能です」と通知されます。

一方、エッジトリガは「変化」を見ます。
読み取り不可能な状態から読み取り可能な状態へ変わった瞬間など、状態が変化したときに通知されます。

パイプを例にすると分かりやすいです。

```text
1. 書き込み側がパイプへ1KBのデータを書く
2. 読み取り側が epoll_wait() する
3. 読み取り側が512バイトだけ読む
4. 読み取り側がもう一度 epoll_wait() する
```

ステップ2では、レベルトリガでもエッジトリガでも通知されます。
パイプにデータが入って、読み取り可能になったからです。

違いが出るのはステップ4です。

レベルトリガの場合、まだ512バイト残っているので、もう一度通知されます。
つまり「まだ読める状態ですよ」と教えてくれます。

エッジトリガの場合、ステップ3のあとに新しいデータが追加されていなければ、通知されない可能性があります。
なぜなら、読み取り可能という状態への新しい変化が起きていないからです。

図にすると、ざっくり次のようになります。

```text
レベルトリガ:
	データが残っている間は通知される

	パイプ内: 1024B -> 512B -> 512B
			  通知     通知     通知

エッジトリガ:
	状態が変わったときに通知される

	パイプ内: 0B -> 1024B -> 512B
			 変化   通知     通知なしの場合がある
```

このため、エッジトリガを使う場合は、基本的にノンブロッキングI/Oと組み合わせます。
そして、`read()` や `write()` を、`EAGAIN` または `EWOULDBLOCK` になるまで繰り返す、という書き方をします。

```text
エッジトリガの基本方針:
	fdをノンブロッキングにする
	イベントが来たら読めるだけ読む
	read() が EAGAIN になるまで読む
	そこで初めて「今はもう読めない」と判断する
```

これをしないと、データが残っているのに次の通知が来なくて、処理が止まったように見えることがあります。

初心者段階では、まずレベルトリガを使うのが安全です。
`select()` や `poll()` に近い感覚で扱えるためです。

エッジトリガは高性能なイベント駆動サーバーで使われることがありますが、書き方を間違えるとバグが見つけにくくなります。
特に、ノンブロッキングI/O、部分読み書き、`EAGAIN` の扱いを理解してから使うのがよいと思います。

#### ４章の２の７　epollで注意すること

`epoll` は強力ですが、いくつか注意点があります。

まず、`epoll` はLinux固有です。
POSIX標準ではありません。
Linux専用プログラムなら問題ありませんが、他のUnix系OSへ移植したい場合は別の仕組みが必要です。

次に、`epoll` で監視できるfdと、できないfdがあります。
ソケット、パイプ、端末などはよく使われます。
一方、普通の通常ファイルは、常に読み書き可能と見なされることが多く、`epoll` で待つ意味が薄いです。
通常ファイルのディスクI/O完了を非同期に待つ、という用途には、`epoll` は基本的に向いていません。
そのあたりは、後で非同期I/Oや `io_uring` の話に関係してきます。

また、`EPOLLERR` と `EPOLLHUP` は、明示的に指定していなくても返ることがあります。
そのため、イベント処理では `EPOLLIN` だけを見るのではなく、エラーや切断も考慮します。

```c
if (events[i].events & (EPOLLERR | EPOLLHUP)) {
	/* エラーまたは切断として扱う */
}
```

さらに、`epoll_wait()` はシグナルで割り込まれて `EINTR` を返すことがあります。
シェルのようにシグナルを扱うプログラムでは、これはかなり現実的な問題です。
`Ctrl-C`、ジョブ制御、子プロセス終了通知などと絡むため、`EINTR` をどう扱うかは丁寧に考える必要があります。

#### ４章の２の８　UmuOSでどう考えるか

UmuOSの設計として `epoll` を見ると、重要なのはAPIそのものよりも、イベント待ちの抽象化です。

単純なOSでは、プロセスが `read()` を呼んだら、データが来るまでそのプロセスを寝かせる、という設計から始められます。
しかし、1つのプロセスが複数の入力元を同時に待ちたい場合は、それだけでは足りません。

たとえば、シェルが次のようなものを同時に気にしたい場合です。

```text
キーボードから入力が来たか
子プロセスが終了したか
パイプからデータが来たか
端末状態が変わったか
タイマーが切れたか
```

これらを全部、単純なブロッキング `read()` だけで扱うのは難しいです。
そこで、イベントを登録し、発生したイベントだけを受け取る仕組みが欲しくなります。

UmuOSで最初に作るなら、Linuxの `epoll` を完全再現する必要はありません。
まずは、次のような小さな抽象化でもよいと思います。

```text
watch(fd, event_mask)
wait_event(timeout)
unwatch(fd)
```

このような仕組みがあると、シェル、端末、パイプ、将来のソケットなどをイベント駆動で扱いやすくなります。

Linuxの `epoll` は、そのかなり実用的で高性能な完成形の1つです。
UmuOSでは、まず「イベントを待つ対象を登録する」「発生したイベントだけ受け取る」「fdとイベントを結びつける」という考え方を吸収すると良さそうです。

次は、ファイルをメモリへ対応づけて扱う、メモリマップI/Oを見ていきます。

### ４章の３　ファイルをメモリへマッピングする

ここでは、ファイルI/Oのもう1つの重要な方法として、ファイルをメモリへマッピングする仕組みを見ていきます。

普通にファイルを読む場合は、`read()` を呼び、カーネルがファイル内容をユーザー空間のバッファへコピーします。
普通にファイルへ書く場合は、`write()` を呼び、ユーザー空間のバッファからカーネル側へデータを渡します。

一方、メモリマップI/Oでは、ファイルの一部または全部をプロセスのアドレス空間へ対応づけます。
すると、プログラムからはメモリを読んでいるように見えるのに、実際にはファイル内容を読んでいる、という状態になります。

イメージとしては、次のような感じです。

```text
通常の read():
	ファイル -> カーネル -> ユーザー空間のbuf

mmap():
	ファイルの内容が、プロセスのメモリアドレスに対応づく
	p[0] を読むと、ファイル先頭のバイトを読んでいるように扱える
```

`mmap()` を使うと、ファイル内のデータとメモリ上のアドレスが対応します。
そのため、配列にアクセスするような感覚でファイル内容を扱えます。

```text
ファイル:
	offset 0  offset 1  offset 2  offset 3
	   H         e         l         l

メモリマップ後:
	p[0]      p[1]      p[2]      p[3]
	  H         e         l         l
```

読み取りだけでなく、条件を満たせば、メモリへ書き込んだ内容をファイルへ反映することもできます。
ただし、いつディスクへ確実に書き出されるかは、`msync()` やカーネルの書き戻し処理と関係します。
ここを「メモリに書けば即座に物理ディスクへ保存される」と理解すると危険です。

POSIXでは、ファイルをメモリへマッピングするためのインターフェースとして `mmap()` が定義されています。
Linuxでも当然使えます。
`mmap()` にはファイルI/O以外の用途、たとえば無名メモリの確保や共有メモリなどもありますが、ここではファイルI/Oとしての `mmap()` を中心に見ます。

#### ４章の３の１　mmap()

`mmap()` のプロトタイプは次の通りです。

```c
#include <sys/mman.h>

void *mmap(void *addr,
		   size_t length,
		   int prot,
		   int flags,
		   int fd,
		   off_t offset);
```

ざっくり言うと、`fd` が指すファイルの `offset` バイト目から `length` バイトぶんを、プロセスのメモリ空間へマッピングします。

各引数の意味を整理します。

`addr`
どのあたりの仮想アドレスへマッピングしてほしいかを、カーネルへ伝えるヒントです。
通常は `NULL` を渡します。
ほとんどのプログラムでは、マッピング先アドレスはカーネルに任せるのが安全です。

`length`
マッピングしたいバイト数です。
0を渡すとエラーになります。
空ファイルをそのまま `mmap()` しようとすると失敗するので、サンプルコードでは空ファイルを先に判定します。

`prot`
マッピングしたメモリに対するアクセス権です。
読み取り可能、書き込み可能、実行可能などを指定します。

`flags`
マッピングの種類を指定します。
特に重要なのは `MAP_SHARED` と `MAP_PRIVATE` です。

`fd`
マッピングしたいファイルのファイルディスクリプタです。

`offset`
ファイルのどの位置からマッピングするかです。
ページサイズの整数倍である必要があります。
ファイル先頭からマッピングする場合は0を渡します。

成功すると、マッピングされたメモリ領域の先頭アドレスが返ります。
失敗すると `MAP_FAILED` が返り、`errno` に原因が入ります。

ここはかなり大事です。
`mmap()` の失敗判定では、`NULL` ではなく `MAP_FAILED` と比較します。

```c
void *addr = mmap(NULL, length, PROT_READ, MAP_PRIVATE, fd, 0);
if (addr == MAP_FAILED) {
	perror("mmap");
}
```

`MAP_FAILED` は通常 `(void *)-1` です。
理屈の上では、`NULL` というアドレスにマッピングされる可能性もあるため、`addr == NULL` で失敗判定してはいけません。

#### ４章の３の２　protで指定する保護属性

`prot` には、マッピングしたメモリ領域に対して何を許可するかを指定します。

PROT_READ
読み取りを許可します。

PROT_WRITE
書き込みを許可します。

PROT_EXEC
実行を許可します。
JITコンパイラや実行可能メモリなど特殊な用途で関係します。

PROT_NONE
読み取り、書き込み、実行をすべて禁止します。
通常のファイルI/Oではあまり使いません。

普通にファイル内容を読むだけなら、次のようにします。

```c
void *addr = mmap(NULL, length, PROT_READ, MAP_PRIVATE, fd, 0);
```

書き込みをファイルへ反映したい場合は、基本的にはファイルを読み書き可能で開き、`PROT_READ | PROT_WRITE` と `MAP_SHARED` を使います。

```c
void *addr = mmap(NULL, length,
				  PROT_READ | PROT_WRITE,
				  MAP_SHARED,
				  fd,
				  0);
```

保護属性と、ファイルを開いたときのモードは整合している必要があります。
たとえば、読み取り専用で開いたファイルを `MAP_SHARED | PROT_WRITE` でマッピングしようとすると失敗します。

ただし、`MAP_PRIVATE | PROT_WRITE` の場合は少し意味が変わります。
この場合、書き込みはcopy-on-write、つまりコピーオンライトで行われ、ファイル本体には反映されません。
読み取り専用で開いたファイルを、私的な変更用として `MAP_PRIVATE | PROT_WRITE` でマッピングできる場合があります。

#### ４章の３の３　MAP_SHAREDとMAP_PRIVATE

`flags` で特に重要なのは、`MAP_SHARED` と `MAP_PRIVATE` です。
この2つのどちらかは必ず指定します。
両方を同時に指定することはできません。

MAP_SHARED
マッピングを共有します。
メモリ上で変更した内容は、同じファイルをマッピングしている他プロセスからも見える可能性があります。
また、変更内容はファイルへ反映されます。
ただし、物理ディスクへいつ確実に書き出されるかは別問題です。

MAP_PRIVATE
マッピングを共有しません。
書き込みはcopy-on-writeで処理されます。
プロセスがメモリ上で変更しても、ファイル本体には反映されません。
他プロセスのマッピングにも反映されません。

コピーオンライトは、変更されるまではページを共有しておき、実際に書き込まれた時点でそのプロセス専用のコピーを作る仕組みです。
無駄なコピーを遅らせることで、メモリを節約できます。

イメージとしては、次のようになります。

```text
MAP_SHARED:
	メモリへの変更 -> ファイルへ反映される
	他プロセスからも見える可能性がある

MAP_PRIVATE:
	メモリへの変更 -> 自分だけのコピーへ反映される
	ファイル本体は変わらない
```

ほかに `MAP_FIXED` というフラグもあります。
これは `addr` で指定したアドレスに強制的にマッピングしようとするものです。
既存のマッピングと重なると、その領域を置き換えてしまう可能性があり、かなり危険です。
プロセスのアドレス空間を細かく理解している場合を除き、通常のアプリケーションでは使わないほうがよいです。

現在のLinuxには `MAP_FIXED_NOREPLACE` のように、既存マッピングを壊しにくい追加フラグもあります。
しかし、普通のファイルI/Oを学ぶ段階では、`addr` に `NULL` を渡し、マッピング先はカーネルに任せるのが基本です。

#### ４章の３の４　マッピング後にfdを閉じてもよい

ファイルを `mmap()` すると、カーネル内部ではそのファイルへの参照が保持されます。
そのため、`mmap()` に成功したあとで、元のファイルディスクリプタを `close()` しても、マッピングしたメモリ領域にはアクセスできます。

```c
void *addr = mmap(NULL, length, PROT_READ, MAP_PRIVATE, fd, 0);
if (addr == MAP_FAILED) {
	perror("mmap");
	close(fd);
	return 1;
}

close(fd);

/* addr からはまだ読める */
```

ただし、`MAP_SHARED` で書き込みをファイルへ反映し、さらに確実にディスクへ同期したい場合は、`msync()` や `fsync()` との関係も考えます。
fdを早く閉じすぎると、あとで `fsync(fd)` ができません。
研究用の読み取り専用マッピングなら、`mmap()` 後に `close()` しても問題ない、くらいに考えるとよいです。

#### ４章の３の５　ページサイズとアラインメント

`mmap()` はページ単位でメモリを扱います。

ページとは、OSが仮想メモリを管理するときの基本単位です。
多くのx86-64 Linux環境では4KBのページがよく使われますが、固定で4KBと決めつけないほうがよいです。
環境やアーキテクチャによって変わることがあります。

ページサイズは、POSIX的には `sysconf()` で取得します。

```c
#include <errno.h>
#include <stdio.h>
#include <unistd.h>

long page_size = sysconf(_SC_PAGESIZE);
if (page_size == -1) {
	perror("sysconf");
}
```

`_SC_PAGE_SIZE` という名前もあります。
Linuxでは `getpagesize()` もありますが、現在の移植性を考えるなら `sysconf(_SC_PAGESIZE)` を使うのが無難です。

また、カーネルヘッダ由来の `PAGE_SIZE` マクロをユーザープログラムで使うのは避けたほうがよいです。
コンパイル時の値に依存しやすく、実行環境との差や移植性の問題が出やすいためです。

`mmap()` の `offset` は、ページサイズの整数倍である必要があります。
ファイル先頭からマッピングするなら0なので問題ありません。
ファイル途中からマッピングするときは、ページ境界に合わせる必要があります。

`length` はページサイズの整数倍でなくても指定できます。
ただし、実際のマッピングはページ単位で行われるため、内部的にはページサイズに丸め上げられます。

たとえば、ページサイズが4096バイトで、100バイトのファイルをマッピングする場合、アドレス空間としては1ページぶん使われます。
ファイルサイズを超えた部分は0で読めることがありますが、その部分への書き込みがファイルへ反映されるわけではありません。

ここは、UmuOSのメモリ管理を考えるうえでも重要です。
ファイルはバイト単位で扱いますが、仮想メモリはページ単位で扱います。
`mmap()` は、この2つの世界をつなぐAPIだと見ると分かりやすいです。

#### ４章の３の６　戻り値とerrno

`mmap()` は成功すると、マッピングされた領域の先頭アドレスを返します。
失敗すると `MAP_FAILED` を返します。

代表的なエラーを整理します。

EACCES
ファイルのオープンモードと、`prot` / `flags` の組み合わせが合っていません。
たとえば、読み取り専用で開いたファイルへ `MAP_SHARED | PROT_WRITE` を指定した場合などです。

EAGAIN
ファイルがロックされている、または一時的に必要な資源を得られない場合です。

EBADF
無効なファイルディスクリプタを渡しました。

EINVAL
`addr`、`length`、`offset`、`flags` などが不正です。
`length` が0、`offset` がページ境界に揃っていない、などもここに含まれます。

ENODEV
対象のファイルシステムやデバイスがメモリマッピングに対応していません。

ENOMEM
プロセスのアドレス空間やメモリ資源が不足しています。

EOVERFLOW
指定した範囲がアドレス空間やファイルオフセットの表現範囲を超えました。

EPERM
`PROT_EXEC` を指定したが、ファイルシステムが `noexec` でマウントされている場合などに起こります。

#### ４章の３の７　関連するシグナル

`mmap()` は成功したあとでも、アクセス時にシグナルが発生することがあります。
ここが `read()` とかなり感覚の違うところです。

SIGSEGV
許可されていないアクセスをした場合に発生します。
たとえば、読み取り専用でマッピングした領域へ書き込もうとした場合です。

SIGBUS
マッピング自体は存在しているが、その背後にあるファイル範囲が無効になった場合などに発生します。
たとえば、ファイルをマッピングしたあと、別プロセスがそのファイルを小さく `truncate()` し、こちらが消えた範囲へアクセスした場合です。

このため、`mmap()` は「一度成功したら絶対安全」ではありません。
特に、他プロセスが同じファイルを変更する可能性がある場合は注意が必要です。

#### ４章の３の８　munmap()

`mmap()` で作ったマッピングを解除するには、`munmap()` を使います。

```c
#include <sys/mman.h>

int munmap(void *addr, size_t length);
```

通常は、`mmap()` が返したアドレスと、`mmap()` に渡した長さをそのまま渡します。

```c
if (munmap(addr, length) == -1) {
	perror("munmap");
}
```

`munmap()` に成功すると、その範囲のマッピングはプロセスのアドレス空間から取り除かれます。
解除後にそのアドレスへアクセスすると、通常は `SIGSEGV` が発生します。

`addr` はページ境界に揃っている必要があります。
`mmap()` の戻り値をそのまま使えば、普通は問題ありません。

#### ４章の３の９　mmap()でファイルを表示するサンプル

次に、指定したファイルを `mmap()` で読み取り、標準出力へ書き出すサンプルを見ます。
古いサンプルでは `putchar()` で1バイトずつ出力するものがありますが、現在のサンプルとしては `write()` でまとめて出すほうが分かりやすいです。
また、空ファイルを `mmap()` しないようにしています。

```c
#include <errno.h>
#include <fcntl.h>
#include <stdio.h>
#include <stdlib.h>
#include <sys/mman.h>
#include <sys/stat.h>
#include <unistd.h>

static int write_all(int fd, const char *buf, size_t len)
{
	while (len > 0) {
		ssize_t written = write(fd, buf, len);

		if (written == -1) {
			if (errno == EINTR) {
				continue;
			}

			return -1;
		}

		buf += written;
		len -= (size_t)written;
	}

	return 0;
}

int main(int argc, char *argv[])
{
	if (argc != 2) {
		fprintf(stderr, "usage: %s <file>\n", argv[0]);
		return 1;
	}

	int fd = open(argv[1], O_RDONLY | O_CLOEXEC);
	if (fd == -1) {
		perror("open");
		return 1;
	}

	struct stat st;
	if (fstat(fd, &st) == -1) {
		perror("fstat");
		close(fd);
		return 1;
	}

	if (!S_ISREG(st.st_mode)) {
		fprintf(stderr, "%s is not a regular file\n", argv[1]);
		close(fd);
		return 1;
	}

	if (st.st_size == 0) {
		close(fd);
		return 0;
	}

	void *addr = mmap(NULL, (size_t)st.st_size, PROT_READ, MAP_PRIVATE, fd, 0);
	if (addr == MAP_FAILED) {
		perror("mmap");
		close(fd);
		return 1;
	}

	if (close(fd) == -1) {
		perror("close");
		munmap(addr, (size_t)st.st_size);
		return 1;
	}

	if (write_all(STDOUT_FILENO, addr, (size_t)st.st_size) == -1) {
		perror("write");
		munmap(addr, (size_t)st.st_size);
		return 1;
	}

	if (munmap(addr, (size_t)st.st_size) == -1) {
		perror("munmap");
		return 1;
	}

	return 0;
}
```

コンパイル例です。

```bash
gcc -Wall -Wextra -std=c17 mmap_cat.c -o mmap_cat
./mmap_cat README.md
```

このサンプルの流れは次の通りです。

```text
1. ファイル名を受け取る
2. open() でファイルを開く
3. fstat() でファイル情報を得る
4. 通常ファイルか確認する
5. 空ファイルなら何もせず終了する
6. mmap() で読み取り専用マッピングを作る
7. fdを閉じる
8. マッピングされたメモリ内容を標準出力へ書く
9. munmap() でマッピングを解除する
```

`fstat()` は、開いているfdに対応するファイル情報を取得する関数です。
ここでは、ファイルサイズ `st.st_size` と、通常ファイルかどうかを確認するために使っています。

`S_ISREG(st.st_mode)` は、対象が通常ファイルかどうかを判定するマクロです。
ディレクトリ、デバイスファイル、ソケットなどを安易に `mmap()` すると、対象によって挙動が変わるため、研究用サンプルでは通常ファイルに限定しています。

#### ４章の３の１０　mmap()の利点

`mmap()` には、`read()` / `write()` と比べていくつかの利点があります。

コピーを減らせる
`read()` では、カーネル側のページキャッシュからユーザー空間のバッファへコピーが発生します。
`mmap()` では、ファイル内容をプロセスのアドレス空間へ対応づけるため、アプリケーションからはメモリを直接読むように扱えます。

システムコール回数を減らせる
一度マッピングすれば、データへのアクセス自体は普通のメモリアクセスになります。
そのため、細かく何度も `read()` するような処理では、システムコール回数を減らせる可能性があります。

ランダムアクセスと相性がよい
ファイル内のあちこちを読む場合、`lseek()` と `read()` を繰り返すより、ポインタ計算で目的位置へアクセスできるため書きやすいです。

プロセス間共有に使える
同じファイルを `MAP_SHARED` でマッピングすれば、複数プロセスで同じデータを共有できます。
これは共有メモリの考え方にもつながります。

ただし、「`mmap()` は常に `read()` より速い」と決めつけるのは危険です。
アクセスパターン、ファイルサイズ、ページフォルトの回数、キャッシュ状態、実装の複雑さによって結果は変わります。

#### ４章の３の１１　mmap()の欠点と注意点

`mmap()` にも欠点があります。

小さいファイルでは効果が薄い場合がある
マッピングはページ単位で行われます。
たとえばページサイズが4096バイトの場合、7バイトのファイルでも、アドレス空間としては少なくとも1ページぶんが関係します。
小さなファイルを大量に扱う場合は、`mmap()` の管理コストのほうが目立つことがあります。

ページフォルトが発生する
`mmap()` した直後に全データが物理メモリへ読み込まれるとは限りません。
実際にアクセスしたときにページフォルトが発生し、カーネルが必要なページを読み込むことがあります。
これは便利でもありますが、遅延が読みにくいこともあります。

アドレス空間を消費する
64ビット環境では問題になりにくいですが、32ビット環境では大きなファイルを多数マッピングするとアドレス空間の断片化が問題になることがあります。

ファイルサイズ変更に弱い
マッピング中に別プロセスがファイルを小さくすると、アクセス時に `SIGBUS` が発生することがあります。

エラーがシステムコール時ではなくアクセス時に出ることがある
`read()` ならエラーは `read()` の戻り値で分かります。
`mmap()` では、マッピング後のメモリアクセス時にシグナルとして問題が出ることがあります。

同期の考え方が少し難しい
`MAP_SHARED` で書き込んだ内容が、いつディスクへ確実に保存されるかは `msync()` やカーネルの書き戻し処理と関係します。
永続化が重要なデータでは、明示的な同期を考える必要があります。

UmuOSの視点では、`mmap()` はファイルシステムと仮想メモリ管理が強く結びつく機能です。
つまり、単なるファイルI/Oではなく、ページキャッシュ、ページフォルト、プロセスアドレス空間、ファイルのブロック管理が関係してきます。
ここはOS設計としてかなり面白い部分です。

#### ４章の３の１２　mremap()

Linuxには、既存のメモリマッピングのサイズを変更する `mremap()` というシステムコールがあります。
これはLinux固有APIで、POSIX標準ではありません。

```c
#define _GNU_SOURCE
#include <sys/mman.h>

void *mremap(void *old_address,
			 size_t old_size,
			 size_t new_size,
			 int flags,
			 ...);
```

`mremap()` は、`old_address` から始まる `old_size` バイトぶんのマッピングを、`new_size` へ変更します。

`flags` には、よく `MREMAP_MAYMOVE` を指定します。
これは、必要ならカーネルがマッピングの場所を移動してもよい、という意味です。

```c
void *new_addr = mremap(addr, old_size, new_size, MREMAP_MAYMOVE);
if (new_addr == MAP_FAILED) {
	perror("mremap");
}
```

サイズを大きくしたい場合、現在のアドレスの直後に空き領域があるとは限りません。
`MREMAP_MAYMOVE` を指定しておけば、カーネルは別の場所へ移動してでもサイズ変更できる可能性があります。

ただし、移動されると古いポインタは使えません。
必ず戻り値の新しいアドレスを使う必要があります。

`mremap()` は、メモリ管理の低レイヤに近い機能です。
通常のファイルI/Oの研究では、存在を知っておく程度で大丈夫です。
glibcなどのメモリアロケータ内部では、効率化のために `mmap()` や `mremap()` が関係することがあります。

#### ４章の３の１３　mprotect()

マッピング済みメモリ領域の保護属性を変更するには、`mprotect()` を使います。

```c
#include <sys/mman.h>

int mprotect(void *addr, size_t len, int prot);
```

`addr` から `len` バイトぶんの範囲について、ページの保護属性を変更します。
`addr` はページ境界に揃っている必要があります。

`prot` には、`mmap()` と同じように `PROT_READ`、`PROT_WRITE`、`PROT_EXEC`、`PROT_NONE` などを指定します。

ここで注意する点は、`mprotect()` の `prot` は追加ではなく置き換えだということです。

たとえば、もともと読み書き可能だった領域に対して、次のように呼ぶとします。

```c
mprotect(addr, len, PROT_WRITE);
```

これは「書き込み権限を追加する」ではありません。
「保護属性を `PROT_WRITE` に置き換える」という意味です。
読み取り権限を残したいなら、次のように書きます。

```c
mprotect(addr, len, PROT_READ | PROT_WRITE);
```

Linuxでは、`mmap()` で作った領域だけでなく、プロセスの他のメモリ領域にも `mprotect()` を使える場合があります。
ただし、ファイルのオープンモードやマッピング種別と矛盾する保護属性にはできません。

セキュリティの観点では、`PROT_EXEC` は特に重要です。
昔のx86では、読み取り可能なページが実行可能でもある、という扱いがありました。
しかし現在のCPUやOSでは、NXビット、no-execute、W^Xのような考え方により、書き込み可能なメモリと実行可能なメモリを分けるのが基本です。

普通のファイルI/Oで `PROT_EXEC` を使うことはほとんどありません。
実行する必要がないデータには、実行権限を付けないのが安全です。

#### ４章の３の１４　msync()

`MAP_SHARED` でマッピングした領域へ書き込むと、その変更はファイルへ反映されます。
しかし、反映されるということと、物理ディスクへすぐ安全に保存されるということは同じではありません。

マッピングしたファイルの変更をディスクへ同期したい場合は、`msync()` を使います。

```c
#include <sys/mman.h>

int msync(void *addr, size_t length, int flags);
```

`addr` から `length` バイトぶんのマッピング領域について、変更内容を同期します。
`addr` はページ境界に揃っている必要があります。
通常は `mmap()` の戻り値を渡します。

`flags` には、主に次の値を指定します。

MS_SYNC
同期的に書き出します。
対象ページの書き出しが完了するまで `msync()` は戻りません。
確実性を重視する場合はこちらです。

MS_ASYNC
非同期に書き出しを開始します。
ただし、現在のLinuxではカーネルがdirtyページを追跡しているため、`MS_ASYNC` の意味はかなり薄くなっています。
移植性を考えるなら、同期したいときは `MS_SYNC` を明示するほうが分かりやすいです。

MS_INVALIDATE
他のマッピングのキャッシュを無効化し、次回アクセス時に最新内容を読ませる意図で使います。
ただし、実際の使いどころは限られます。

サンプルです。

```c
if (msync(addr, length, MS_SYNC) == -1) {
	perror("msync");
}
```

`munmap()` したからといって、その瞬間にディスクへ完全に書き込まれると考えるのは危険です。
重要なデータを永続化したい場合は、`msync(MS_SYNC)` を使う、場合によってはファイルディスクリプタに対する `fsync()` も考える、という発想が必要です。

`msync()` の主なエラーは次のようなものです。

EINVAL
`flags` が不正、`addr` がページ境界に揃っていない、などです。

ENOMEM
指定した範囲がマッピングされていない場合などです。

#### ４章の３の１５　madvise()

`madvise()` は、マッピングしたメモリ領域をどのように使う予定かを、アプリケーションからカーネルへ伝えるためのシステムコールです。

```c
#include <sys/mman.h>

int madvise(void *addr, size_t length, int advice);
```

`addr` から `length` バイトぶんの範囲について、アクセスパターンのヒントを与えます。
これは命令というよりアドバイスです。
カーネルが必ずその通りに動くとは限りません。

代表的な `advice` は次の通りです。

MADV_NORMAL
特別なアドバイスなしです。
通常の扱いに戻します。

MADV_RANDOM
ランダムアクセスする予定だと伝えます。
カーネルは先読みを抑える方向に動くことがあります。

MADV_SEQUENTIAL
先頭から順番にアクセスする予定だと伝えます。
カーネルは先読みを増やす方向に動くことがあります。

MADV_WILLNEED
近いうちにその範囲へアクセスする予定だと伝えます。
先読みを促すために使います。

MADV_DONTNEED
しばらく使わない、または内容を捨ててもよいと伝えます。
カーネルは対応するページ資源を解放することがあります。
ただし、マッピング種別やdirtyページの扱いにより意味が変わるので、雑に使うと危険です。

順番に読む大きなファイルでは、次のように書けます。

```c
if (madvise(addr, length, MADV_SEQUENTIAL) == -1) {
	perror("madvise");
}
```

`madvise()` は、パフォーマンスチューニングのための補助的なAPIです。
まずは普通に動くコードを書き、アクセスパターンが明確で、実測上意味がある場合に使うのがよいです。

#### ４章の３の１６　先読みという考え方

Linuxカーネルは、ファイルを読むときに先読み、readaheadという最適化を行います。

たとえば、ファイルを先頭から順番に読んでいる場合、カーネルは「このプログラムは次のページも読むだろう」と予測し、少し先のデータを先に読み込むことがあります。
これにより、次にアクセスしたときにすぐデータを返せます。

```text
プログラム:
	page 0 を読む
	page 1 を読む
	page 2 を読む

カーネル:
	順番に読んでいると判断
	page 3, page 4 も先に読んでおく
```

シーケンシャルアクセスでは、先読みはかなり有効です。
一方で、ランダムアクセスでは、先読みしたページが使われないことも多く、無駄になる場合があります。

`madvise()` の `MADV_SEQUENTIAL` や `MADV_RANDOM` は、この先読みの方針にヒントを与えるものです。

ただし、現在のLinuxカーネルは自動的にもアクセスパターンを見ています。
そのため、明示的な `madvise()` が常に必要というわけではありません。
大事なのは、OSは単に要求されたバイトだけを読むのではなく、今後のアクセスを予測してキャッシュや先読みを調整している、という見方です。

UmuOSの視点では、先読みはファイルシステムとページキャッシュの設計に直結します。
単純な実装では要求されたブロックだけを読むところから始めればよいですが、性能を上げるには「次に読まれそうなブロック」を予測する発想が必要になります。

#### ４章の３の１７　UmuOSでどう考えるか

`mmap()` は、UmuOSにとってかなり高度な機能です。
なぜなら、次の仕組みが連携して初めて成り立つからです。

```text
ファイルシステム
	ファイルのバイト列、inode、ブロック配置を管理する

仮想メモリ
	プロセスごとのアドレス空間とページテーブルを管理する

ページフォルト処理
	まだ読み込まれていないページへアクセスしたときに、必要なデータを読み込む

ページキャッシュ
	ディスクから読んだファイル内容をメモリ上に保持する

書き戻し
	dirtyになったページを、適切なタイミングでディスクへ反映する
```

小さな自作OSでは、最初から `mmap()` を実装する必要はないと思います。
まずは `open()`、`read()`、`write()`、`close()` のような明示的なI/Oをしっかり作るのが先です。

しかし、`mmap()` の考え方を知っておくと、OSの見方がかなり変わります。
ファイルは単に `read()` で読む対象ではなく、プロセスのアドレス空間へ対応づけられるデータでもある、ということです。

UmuOSで将来的に `mmap()` 風の機能を考えるなら、最初は読み取り専用のファイルマッピングから始めるのが現実的です。

```text
最初の目標:
	通常ファイルを読み取り専用でページ単位にマッピングする
	書き込み共有は扱わない
	ファイルサイズ変更時の複雑な処理も後回しにする
```

そこから、`MAP_SHARED`、dirtyページ、`msync()`、copy-on-write、実行可能マッピングなどを少しずつ考えていく流れがよさそうです。

次は、ファイルI/Oのアクセスパターンやカーネルへのヒントを、もう少し広い視点で見ていきます。

### ４章の４　ファイルI/Oのヒント

前節では、`mmap()` でマッピングしたメモリ領域に対して、`madvise()` でアクセスパターンのヒントを与える話を見ました。

ここでは、通常のファイルI/O、つまり `read()` / `write()` で扱うファイルに対して、カーネルへヒントを与える方法を見ていきます。

Linuxでは、主に次の2つが関係します。

`posix_fadvise()`
POSIXで定義されている、ファイルアクセスパターンのアドバイスAPIです。
移植性を考えるなら、基本はこちらを優先して覚えるとよいです。

`readahead()`
Linux固有の先読みAPIです。
指定した範囲をページキャッシュへ読み込むようにカーネルへ依頼します。

この節のテーマは、ざっくり言うと「これからこのファイルをどう読む予定か」をカーネルへ伝えることです。

カーネルは、ページキャッシュや先読みを使ってI/Oを高速化しようとします。
しかし、アプリケーションがこれから順番に読むのか、ランダムに読むのか、一度だけ読むのか、すぐ使うのか、もう使わないのかまでは、完全には分かりません。

そこでアプリケーション側からヒントを出すと、カーネルがより良いキャッシュ戦略を選べる場合があります。

```text
順番に全部読む予定:
	先読みを増やすと有利

ランダムに読む予定:
	先読みを増やしても無駄になりやすい

すぐ使う予定:
	先にページキャッシュへ読み込んでおくと有利

もう使わない予定:
	ページキャッシュから追い出してもよい
```

ただし、これはあくまでヒントです。
カーネルが必ずその通りに動くとは限りません。
また、Linuxカーネルのバージョンによって、細かい動作が変わることもあります。

#### ４章の４の１　posix_fadvise()

`posix_fadvise()` は、ファイルディスクリプタに対して、アクセスパターンのヒントを与える関数です。

プロトタイプは次の通りです。

```c
#include <fcntl.h>

int posix_fadvise(int fd, off_t offset, off_t len, int advice);
```

`fd` は対象ファイルのファイルディスクリプタです。
`offset` と `len` は、アドバイスを適用したいファイル範囲を表します。

範囲は、次のように考えます。

```text
[offset, offset + len)
```

これは、`offset` から始まり、`offset + len` の直前まで、という意味です。
`len` に0を渡すと、`offset` からファイル終端までを表します。

ファイル全体に対してヒントを出したい場合は、よく次のようにします。

```c
int ret = posix_fadvise(fd, 0, 0, POSIX_FADV_SEQUENTIAL);
```

ここで `0, 0` は、ファイル先頭からファイル終端まで、という意味になります。

#### ４章の４の２　adviceに指定できる値

`advice` には、ファイルをどのように使う予定かを指定します。

POSIX_FADV_NORMAL
特別なヒントはありません。
通常の扱いに戻します。

POSIX_FADV_RANDOM
ランダムアクセスする予定だと伝えます。
カーネルは先読みを抑える方向に動くことがあります。

POSIX_FADV_SEQUENTIAL
先頭から順番に読む予定だと伝えます。
カーネルは先読みを増やす方向に動くことがあります。

POSIX_FADV_WILLNEED
近いうちにその範囲を使う予定だと伝えます。
カーネルはページキャッシュへ先に読み込もうとすることがあります。

POSIX_FADV_DONTNEED
その範囲をしばらく使わない、またはもう使わないと伝えます。
カーネルはページキャッシュからその範囲のデータを破棄することがあります。

POSIX_FADV_NOREUSE
一度だけ使う、再利用しない、というヒントです。
ただし、この値の実際の扱いはLinuxカーネルのバージョンによって変わってきました。
古いLinuxでは実質的に無視された時期もあります。
現在でも、強い保証のある指定というより、再利用性が低いことを伝えるヒントとして考えるのがよいです。

`madvise()` と名前が似ていますが、対象が違います。

```text
madvise():
	メモリマッピングされたアドレス範囲に対するヒント

posix_fadvise():
	ファイルディスクリプタとファイル範囲に対するヒント
```

つまり、`mmap()` したメモリへヒントを出すなら `madvise()`、普通にfdで開いたファイルへヒントを出すなら `posix_fadvise()`、という切り分けです。

#### ４章の４の３　戻り値の注意

`posix_fadvise()` は、普通のシステムコール風APIと少し戻り値の扱いが違います。

成功すると0を返します。
失敗すると、`-1` を返して `errno` を設定するのではなく、エラー番号そのものを戻り値として返します。

ここは古いサンプルや説明で間違えやすいところです。

正しい書き方は次のようになります。

```c
#include <errno.h>
#include <fcntl.h>
#include <stdio.h>
#include <string.h>

int ret = posix_fadvise(fd, 0, 0, POSIX_FADV_RANDOM);
if (ret != 0) {
	fprintf(stderr, "posix_fadvise: %s\n", strerror(ret));
}
```

`perror("posix_fadvise")` だけでは、正しいエラーを表示できない可能性があります。
なぜなら、`errno` にエラーが入るとは限らないからです。

代表的なエラーは次のようなものです。

EBADF
無効なファイルディスクリプタです。

EINVAL
`advice` が無効、または指定したfdや範囲に対してそのアドバイスを適用できない場合です。

ESPIPE
対象fdがパイプやFIFOのように、シークできない対象の場合に返ることがあります。
古い説明では `EINVAL` とされることもありますが、現在のLinux manページでは `ESPIPE` が関係します。

#### ４章の４の４　posix_fadvise() のサンプル

次は、ファイル全体を順番に読む予定だとカーネルへ伝えてから、普通に `read()` で読み出すサンプルです。

```c
#include <errno.h>
#include <fcntl.h>
#include <stdio.h>
#include <string.h>
#include <unistd.h>

#define BUF_SIZE 65536

int main(int argc, char *argv[])
{
	if (argc != 2) {
		fprintf(stderr, "usage: %s <file>\n", argv[0]);
		return 1;
	}

	int fd = open(argv[1], O_RDONLY | O_CLOEXEC);
	if (fd == -1) {
		perror("open");
		return 1;
	}

	int ret = posix_fadvise(fd, 0, 0, POSIX_FADV_SEQUENTIAL);
	if (ret != 0) {
		fprintf(stderr, "posix_fadvise: %s\n", strerror(ret));
		close(fd);
		return 1;
	}

	char buf[BUF_SIZE];

	for (;;) {
		ssize_t nread = read(fd, buf, sizeof(buf));
		if (nread == -1) {
			if (errno == EINTR) {
				continue;
			}

			perror("read");
			close(fd);
			return 1;
		}

		if (nread == 0) {
			break;
		}

		ssize_t offset = 0;
		while (offset < nread) {
			ssize_t written = write(STDOUT_FILENO, buf + offset, (size_t)(nread - offset));
			if (written == -1) {
				if (errno == EINTR) {
					continue;
				}

				perror("write");
				close(fd);
				return 1;
			}

			offset += written;
		}
	}

	close(fd);
	return 0;
}
```

コンパイル例です。

```bash
gcc -Wall -Wextra -std=c17 fadvise_cat.c -o fadvise_cat
./fadvise_cat README.md
```

このサンプルでは、`posix_fadvise()` を呼んだからといって、読み取り処理そのものが変わるわけではありません。
あくまで、カーネルへ「このファイルは順番に読みます」と伝えているだけです。

アプリケーションの動作としては、普通に `read()` で読んでいます。
違いは、カーネルのページキャッシュや先読みの方針にヒントを与えている点です。

#### ４章の４の５　POSIX_FADV_WILLNEED

`POSIX_FADV_WILLNEED` は、近いうちに使う予定の範囲をカーネルへ知らせるヒントです。

たとえば、大きなファイルのある範囲をあとで読むことが分かっている場合、先に次のように呼べます。

```c
int ret = posix_fadvise(fd, offset, len, POSIX_FADV_WILLNEED);
if (ret != 0) {
	fprintf(stderr, "posix_fadvise: %s\n", strerror(ret));
}
```

これにより、カーネルはその範囲をページキャッシュへ読み込もうとすることがあります。
アプリケーションが実際に `read()` したとき、すでにキャッシュに載っていれば、ディスクI/O待ちを減らせます。

ただし、これは完了を保証する同期読み込みではありません。
「この範囲を使いそうです」と伝える非同期的なヒントに近いものです。

#### ４章の４の６　POSIX_FADV_DONTNEED

`POSIX_FADV_DONTNEED` は、指定した範囲をもう使わない、またはしばらく使わないと伝えるヒントです。

大量のデータを一度だけ順番に処理する場合に役立ちます。
たとえば、動画ファイルのストリーミング、巨大ログの一括処理、バックアップ、チェックサム計算などです。

こうした処理では、大量のファイルデータがページキャッシュに入ります。
しかし、そのデータを再利用しないなら、ページキャッシュを占有するだけになってしまいます。
すると、本来キャッシュしておきたい別のデータが追い出される可能性があります。

そこで、処理済みの範囲に対して `POSIX_FADV_DONTNEED` を使うと、カーネルへ「この範囲はもうキャッシュしておかなくてもよい」と伝えられます。

```c
int ret = posix_fadvise(fd, processed_offset, processed_len, POSIX_FADV_DONTNEED);
if (ret != 0) {
	fprintf(stderr, "posix_fadvise: %s\n", strerror(ret));
}
```

ただし、dirtyなページ、つまり未書き戻しの変更があるページは、簡単には破棄できません。
必要なら先に `fsync()` などで同期することも考えます。

また、範囲の一部がページ境界に揃っていない場合、カーネルが部分ページを保持することがあります。
これは、同じページ内の別データを誤って捨てないためです。

#### ４章の４の７　readahead()

`readahead()` は、Linux固有のシステムコールです。
指定したファイル範囲をページキャッシュへ読み込むようにカーネルへ依頼します。

プロトタイプは次の通りです。

```c
#define _GNU_SOURCE
#include <fcntl.h>

ssize_t readahead(int fd, off64_t offset, size_t count);
```

`fd` の `[offset, offset + count)` の範囲を先読みします。
成功すると0を返します。
失敗すると `-1` を返し、`errno` に原因が入ります。

```c
if (readahead(fd, 0, 1024 * 1024) == -1) {
	perror("readahead");
}
```

`readahead()` はLinux固有なので、移植性はありません。
また、普通のアプリケーションでは `posix_fadvise(fd, offset, len, POSIX_FADV_WILLNEED)` のほうが意図が分かりやすいです。

そのため、現在の研究では、まず `posix_fadvise()` を優先して覚えればよいと思います。
`readahead()` は、Linux専用で明示的にページキャッシュへ読み込みたい場合のAPI、と考えておくとよいです。

`readahead()` の主なエラーは次のようなものです。

EBADF
無効なファイルディスクリプタです。

EINVAL
そのfdが先読みに対応していない場合などです。

#### ４章の４の８　ヒントはコストが安いが、実測は必要

ファイルI/Oのヒントは、比較的コストの安い最適化です。

アプリケーションが「順番に読みます」「ランダムに読みます」「すぐ使います」「もう使いません」と伝えるだけで、カーネルがページキャッシュや先読みを調整できる場合があります。

特に効果が出やすいのは、次のようなケースです。

巨大ファイルを先頭から順番に読む
`POSIX_FADV_SEQUENTIAL` が役立つ可能性があります。

巨大ファイルをランダムに読む
`POSIX_FADV_RANDOM` により、無駄な先読みを抑えられる可能性があります。

少し後で読む範囲が分かっている
`POSIX_FADV_WILLNEED` により、先にページキャッシュへ読み込める可能性があります。

一度だけ読んだ大量データを再利用しない
`POSIX_FADV_DONTNEED` により、ページキャッシュの圧迫を減らせる可能性があります。

ただし、最適化は実測が重要です。
現在のLinuxカーネルは、アクセスパターンを自動的にも推測します。
ファイルシステム、ストレージ、メモリ量、ページキャッシュの状態、同時に動いているプロセスによって結果は変わります。

なので、`posix_fadvise()` を入れたから必ず速くなる、というものではありません。
むしろ、小さいファイルや単純な処理では、違いが見えないことも多いです。

#### ４章の４の９　UmuOSでどう考えるか

UmuOSの視点では、`posix_fadvise()` や `readahead()` は、ユーザープログラムからカーネルのキャッシュ戦略へヒントを渡す仕組みです。

これは、OS設計としてかなり重要な発想です。
カーネルは汎用的に動く必要がありますが、アプリケーションは自分のアクセスパターンを知っていることがあります。

```text
カーネル:
	汎用的な先読み、ページキャッシュ管理を行う

アプリケーション:
	自分が順番に読むのか、ランダムに読むのかを知っている

ヒントAPI:
	アプリケーションの知識をカーネルへ渡す橋
```

UmuOSで最初から `posix_fadvise()` 相当を作る必要はないと思います。
まずは単純なページキャッシュ、またはブロックキャッシュを作り、最近使ったデータを保持するところから始めるのが現実的です。

その次の段階で、シーケンシャル読み取りを検出して先読みする、処理済みブロックを捨てる、という最適化を考えるとよさそうです。

たとえば、UmuOS内では次のような小さなヒントAPIを考えられます。

```text
file_advise(fd, offset, len, ADVICE_SEQUENTIAL)
file_advise(fd, offset, len, ADVICE_RANDOM)
file_advise(fd, offset, len, ADVICE_WILLNEED)
file_advise(fd, offset, len, ADVICE_DONTNEED)
```

最初は実際にすべてを実装しなくても、APIとしてこういう方向があると知っておくだけで、ファイルI/Oの設計が見えやすくなります。

この節で重要なのは、I/O性能は `read()` の呼び方だけで決まるわけではない、ということです。
ページキャッシュ、先読み、アクセスパターン、キャッシュ破棄の方針まで含めて、ファイルI/Oの性能が決まっていきます。

### ４章の５　2種類の同期/非同期

ここでは、LinuxやUnix系OSのI/Oで混同しやすい「同期」「非同期」という言葉を整理します。

このあたりは、かなりややこしいです。
なぜなら、日本語ではどちらも「同期」と訳されがちですが、英語では次のように別の軸の言葉が出てくるからです。

```text
synchronous / asynchronous
	処理の完了を待つかどうか

synchronized / nonsynchronized
	データがディスクなどの永続媒体と同期されたかどうか
```

つまり、似た言葉ですが、見ている観点が違います。

`synchronous` は、I/O要求を出した関数呼び出しが、必要な処理が終わるまで戻らない、という話です。
`asynchronous` は、I/O要求をキューへ入れてすぐ戻り、完了はあとで受け取る、という話です。

一方、`synchronized` は、データがカーネル内のページキャッシュやバッファだけではなく、ディスク上の状態とどこまで一致しているか、という話です。

この2軸を分けて考えると、混乱しにくくなります。

#### ４章の５の１　synchronous と asynchronous

まず、`synchronous` と `asynchronous` は、呼び出した処理が待つかどうかの違いです。

synchronous I/O
`read()` や `write()` を呼んだら、その処理がある程度完了するまで関数が戻りません。
通常の `read()` は、ユーザー空間のバッファへデータがコピーされるまで戻りません。
通常の `write()` は、ユーザー空間のバッファからカーネル内のバッファ、つまりページキャッシュなどへデータがコピーされるまで戻りません。

asynchronous I/O
I/O要求を出したら、実際の完了を待たずに戻ります。
処理はあとで実行され、完了したら別の方法で通知されます。
この場合、完了結果を受け取る仕組みが必須です。

イメージとしては、次のようになります。

```text
synchronous write():
	write() を呼ぶ
	カーネルへデータが渡るまで待つ
	write() が戻る

asynchronous write():
	書き込み要求を登録する
	すぐ戻る
	あとで「完了しました」「失敗しました」を受け取る
```

ここで注意したいのは、通常の `write()` が戻ったからといって、必ず物理ディスクへ書き込まれたとは限らないことです。
多くの場合、通常の `write()` は「カーネルへ渡した」段階で戻ります。
ディスクへの実際の書き出しは、カーネルが後で行うことがあります。

#### ４章の５の２　synchronized と nonsynchronized

次に、`synchronized` と `nonsynchronized` は、データが永続媒体と同期されているかどうかの違いです。

synchronized I/O
データがディスクへ書き出され、カーネル内の状態とディスク上の状態が同期していることを重視します。
`O_SYNC`、`O_DSYNC`、`fsync()`、`fdatasync()` などが関係します。

nonsynchronized I/O
データはカーネル内のページキャッシュやバッファへ入っただけで、物理ディスクへはまだ書き出されていない可能性があります。
通常の `write()` は、多くの場合こちらです。

書き込みで見ると、通常の動作は次のように考えられます。

```text
通常の write():
	synchronous
		write() はカーネルへデータを渡すまで戻らない

	nonsynchronized
		ただし、ディスクへ書き出されたとは限らない
```

つまり、通常の `write()` は「シンクロナスだけど、シンクロナイズドではない」と見ることができます。
この言い方はややこしいですが、かなり重要です。

ディスクへ確実に近い形で反映したい場合は、たとえば次のような方法があります。

```c
if (fsync(fd) == -1) {
	perror("fsync");
}
```

または、ファイルを開くときに `O_SYNC` や `O_DSYNC` を指定する方法もあります。
ただし、これらは性能に大きく影響します。
ログ、データベース、ファイルシステムのメタデータ更新など、永続性が重要な場面で慎重に使います。

#### ４章の５の３　書き込み処理での組み合わせ

書き込みでは、2つの軸を組み合わせて考えられます。
横長の表にすると見づらいので、縦に整理します。

シンクロナス + ノンシンクロナイズド
通常の `write()` に近い動作です。
ユーザー空間のデータがカーネルへコピーされるまで戻りません。
しかし、ディスクへ書き出されたとは限りません。

シンクロナス + シンクロナイズド
`O_SYNC` 付きの書き込みや、`write()` 後に `fsync()` するような動作です。
データがディスクへ反映されるところまで待ちます。
安全性は高くなりますが、遅くなりやすいです。

アシンクロナス + ノンシンクロナイズド
書き込み要求をキューへ入れてすぐ戻ります。
完了した時点では、少なくともカーネル内のバッファへデータが渡った、という扱いになります。
ただし、ディスクへ書き出されたとは限りません。

アシンクロナス + シンクロナイズド
書き込み要求をキューへ入れてすぐ戻ります。
あとで完了通知を受けた時点で、ディスク上にも反映されていることを期待する種類のI/Oです。
実装や指定するフラグによって細かい意味は変わります。

このように、非同期だから安全ではない、同期だから永続化されている、とは限りません。
待つかどうかと、ディスクへ同期されたかどうかは別の問題です。

#### ４章の５の４　読み取り処理での考え方

読み取りの場合は、書き込みより少し単純です。

通常の `read()` は、ユーザー空間のバッファへデータが入るまで戻りません。
つまり、シンクロナスです。

また、Linuxカーネルはページキャッシュ内のデータが、ディスク上のデータと同じか、それより新しい状態になるように管理します。
そのため、通常の読み取りは実用上、最新のデータを返すものとして扱えます。

読み取りの整理です。

シンクロナス読み取り
通常の `read()` です。
データがユーザー空間のバッファへコピーされるまで戻りません。

アシンクロナス読み取り
読み取り要求を出してすぐ戻ります。
あとで完了通知を受け取り、その時点で読み取られたデータを使います。

読み取りで「古いデータを返してよい」という設計は、普通のファイルI/Oではあまり意味がありません。
そのため、読み取りでは「待つかどうか」が主な違いになります。

#### ４章の５の５　非同期I/Oとは何か

非同期I/O、asynchronous I/O、AIOとは、I/O要求を出した関数が、実際のI/O完了を待たずに戻る仕組みです。

非同期I/Oでは、最低限次の流れが必要になります。

```text
1. I/O要求を作る
2. カーネルまたはワーカへ要求を登録する
3. 呼び出し元はすぐ別の処理へ進む
4. I/Oが完了する
5. 完了通知を受け取る
6. 成功/失敗、処理バイト数、errno相当の情報を確認する
```

通常の `read()` / `write()` では、戻り値で成功や失敗が分かります。
しかし、非同期I/Oでは、要求を出した時点ではまだ結果がありません。
だから、完了結果をあとで取り出す仕組みが必要になります。

これが非同期I/Oを難しくしている部分です。
単に「ブロックしない」だけではありません。
要求の管理、完了通知、バッファの寿命、キャンセル、エラー処理などを設計する必要があります。

#### ４章の５の６　POSIX AIO

POSIXでは、AIO用のインターフェースとして `<aio.h>` が定義されています。
代表的な構造体と関数は次の通りです。

```c
#include <aio.h>

struct aiocb {
	int aio_fildes;               // ファイルディスクリプタ
	off_t aio_offset;              // ファイル内のオフセット
	volatile void *aio_buf;        // I/O用バッファ
	size_t aio_nbytes;             // 処理するバイト数
	int aio_reqprio;               // 要求優先度の調整
	struct sigevent aio_sigevent;  // 完了通知方法
	int aio_lio_opcode;            // lio_listio() 用の操作種別
};

int aio_read(struct aiocb *aiocbp);
int aio_write(struct aiocb *aiocbp);
int aio_error(const struct aiocb *aiocbp);
ssize_t aio_return(struct aiocb *aiocbp);
int aio_cancel(int fd, struct aiocb *aiocbp);
int aio_fsync(int op, struct aiocb *aiocbp);
int aio_suspend(const struct aiocb *const list[], int nent,
				const struct timespec *timeout);
```

`aio_read()` は非同期読み取り要求を登録します。
`aio_write()` は非同期書き込み要求を登録します。
`aio_error()` は、その要求がまだ実行中か、完了したか、エラーになったかを確認します。
`aio_return()` は、完了した要求の戻り値、つまり通常の `read()` / `write()` の戻り値に相当する値を取り出します。

POSIX AIOで重要なのは、`struct aiocb` とバッファの寿命です。
非同期I/Oは、関数が戻ったあとも処理が続く可能性があります。
そのため、I/Oが完了する前に `struct aiocb` や `aio_buf` が指すバッファを解放したり、スタックから消えたりすると危険です。

```text
ダメな考え方:
	関数内のローカル配列を aio_buf にする
	aio_read() を呼ぶ
	関数から戻る
	まだI/Oが終わっていない

結果:
	バッファの寿命が切れている可能性がある
```

非同期I/Oでは、バッファがI/O完了まで確実に生きている必要があります。
ここはC初心者には特に注意です。

#### ４章の５の７　LinuxにおけるAIOの歴史的な注意

参考文献として扱う古い Linux システムプログラミング系の書籍では、非同期I/OとしてPOSIX AIO、Linux native AIO、`libaio`、`io_submit()` などが出てきます。
ここは現在では少し歴史を整理して読む必要があります。

POSIX AIO
glibcが提供するPOSIX準拠のインターフェースです。
環境によっては、内部的にスレッドを使って実装されることがあります。
その場合、APIとしては非同期でも、カーネルの低レイヤ非同期I/Oとは性質が違います。

Linux native AIO
`io_setup()`、`io_submit()`、`io_getevents()` などのシステムコールを使うLinux独自のAIOです。
歴史的には `libaio` 経由で使われることが多いです。
ただし、制約が多く、特に通常ファイルでは `O_DIRECT` と組み合わせる場面が中心でした。

io_uring
比較的新しいLinuxの非同期I/Oインターフェースです。
Linux 5.1以降で導入され、現在のLinuxで高性能な非同期I/Oを考える場合、重要な選択肢になっています。
ファイルI/O、ネットワークI/O、タイムアウト、poll的な待ちなど、かなり広い操作を扱えます。

つまり、古い本の説明を読むときは、次のように読み替えるとよいです。

```text
当時:
	POSIX AIO と Linux native AIO / libaio を比較する

現在:
	POSIX AIO は知識として押さえる
	Linux native AIO は歴史的APIとして理解する
	新しく設計するなら io_uring も候補に入る
```

このノートでは、まず概念として「非同期I/Oとは、要求と完了を分離する仕組み」と理解することを優先します。
`io_uring` の細部はかなり大きな話になるので、必要なら別の節で改めて掘るのがよいと思います。

#### ４章の５の８　スレッドを使った非同期I/O

非同期I/Oを実現する一番分かりやすい方法は、スレッドを使うことです。

たとえば、メインスレッドはI/O要求をキューに入れるだけにします。
別のワーカスレッドが、そのキューから要求を取り出して、普通の `read()` や `write()` を実行します。
I/Oが終わったら、結果を結果キューへ入れます。

流れは次のようになります。

```text
メインスレッド:
	I/O要求を作る
	ワークキューへ入れる
	すぐ別の処理へ進む

ワーカスレッド:
	ワークキューから要求を取り出す
	read() / write() を実行する
	完了結果を結果キューへ入れる

メインスレッド:
	結果キューから完了結果を取り出す
```

これにより、メインスレッドから見ると、I/Oでブロックされていないように見えます。
実際には、ワーカスレッドがブロックしています。

この方法には利点があります。

実装イメージが分かりやすい
普通の `read()` / `write()` をそのまま使えます。
カーネルの特殊なAIO APIを知らなくても設計できます。

通常ファイルでも扱いやすい
カーネルAIOの制約に引っかかりにくいです。

ただし、欠点もあります。

スレッド管理のコストがある
スレッドの作成、スケジューリング、同期、ロックなどのコストが増えます。

共有データの排他制御が必要になる
ワークキューや結果キューを複数スレッドで触るため、mutexや条件変数などが必要になります。

キャンセルや終了処理が難しくなる
ワーカスレッドがI/Oでブロックしていると、きれいに止める設計が必要になります。

このため、スレッド方式は分かりやすい一方で、規模が大きくなると並行処理の難しさが出てきます。

#### ４章の５の９　ワークキュー方式の設計イメージ

スレッドを使った非同期I/Oを、UmuOSやUshの設計に近い形で見ると、次のような部品が必要になります。

ワークキュー
これから実行するI/O要求を入れておくキューです。

I/O要求ディスクリプタ
どのfdに対して、読み取りなのか書き込みなのか、バッファはどこか、何バイト処理するのか、などを持つ構造体です。

ワーカスレッド
ワークキューからI/O要求を取り出して、実際に `read()` / `write()` を実行します。

結果キュー
完了したI/Oの結果を入れるキューです。
成功か失敗か、処理バイト数、エラー番号などを保持します。

完了通知
メイン側へ「結果キューに完了結果があります」と知らせる仕組みです。
条件変数、eventfd、パイプ、シグナル、イベントループなどが候補になります。

イメージです。

```text
I/O要求:
	id
	op = READ / WRITE
	fd
	buffer
	length
	offset

I/O結果:
	id
	result_bytes
	error_number
```

要求に `id` を付けておくと、あとで結果を受け取ったときに「どの要求の結果か」を対応づけられます。
これが非同期処理ではかなり重要です。

#### ４章の５の１０　UmuOSでどう考えるか

UmuOSの視点では、この節の重要ポイントはAPI名よりも、2つの軸を分けて考えることです。

```text
待つかどうか:
	synchronous / asynchronous

永続媒体と同期したか:
	synchronized / nonsynchronized
```

小さなOSでは、最初はすべてのI/Oをシンクロナスにしてよいと思います。
つまり、`read()` や `write()` を呼んだプロセスは、処理が終わるまで待つ設計です。

その次に必要になるのは、シンクロナイズド書き込みです。
たとえば、ファイルシステムのメタデータ、ログ、設定ファイルなどを安全に保存したい場合、`fsync()` 相当の仕組みが欲しくなります。

さらに先へ進むと、非同期I/Oやイベント通知が関係してきます。
たとえば、次のような設計です。

```text
プロセス:
	I/O要求を出して寝る

カーネル:
	I/O完了時にプロセスを起こす

イベントループ:
	複数のI/O完了をまとめて受け取る
```

Ushのようなシェルでは、本格的な非同期ファイルI/Oは最初から不要かもしれません。
しかし、パイプ、子プロセス、端末入力、ジョブ制御を扱うと、I/O待ちとイベント通知の考え方は避けて通れません。

まずは、通常の `read()` / `write()` が「シンクロナスかつノンシンクロナイズド寄りの書き込み」であること、そして `fsync()` や `O_SYNC` が「ディスクとの同期」を扱うことを分けて理解するとよいです。

次は、Linuxで実際に非同期I/Oをどう扱うか、もう少し具体的なAPIや設計へ進んでいきます。

### ４章の６　I/OスケジューラとI/Oパフォーマンス

ここでは、LinuxのI/O性能を考えるうえで重要な、I/OスケジューラとI/O要求の並び替えについて見ていきます。

参考文献として扱う古い Linux システムプログラミング系の書籍では、主にHDD、つまり回転する磁気ディスクを前提に説明されています。
現在はSSDやNVMeがかなり一般的になりました。
そのため、古い説明をそのまま読むと、少し時代が違うところがあります。

ただし、I/Oスケジューラの基本思想は今でも重要です。

```text
I/O要求は、発生した順番にそのまま処理するのが常に最適とは限らない
```

HDDでは、ディスクヘッドの移動、つまりシークが非常に遅い処理でした。
CPUやメモリに比べると、ディスクは桁違いに遅く、特にランダムアクセスではシーク時間が大きなボトルネックになります。

SSDやNVMeでは、HDDのような物理的なヘッド移動はありません。
しかし、I/O要求のマージ、キューイング、優先度、公平性、レイテンシ、デバイス内部の並列性などは今でも重要です。

つまり、現在の感覚では次のように読み替えるとよいです。

```text
古典的なHDD:
	シークを減らすために、I/O要求の順序を工夫する

現在のSSD/NVMe:
	並列性、キュー深度、公平性、レイテンシを考えてI/O要求を扱う
```

UmuOSの視点では、ここは「ブロックデバイスへ届いたI/O要求を、カーネルがどう並べ、どうまとめ、どのプロセスを待たせすぎないようにするか」という話になります。

#### ４章の６の１　ディスク上の配置

HDDを理解するには、昔ながらのディスク構造を軽く知っておくと分かりやすいです。

HDDの中には、プラッタと呼ばれる円盤があります。
その表面を読み書きヘッドが移動し、磁気的にデータを読み書きします。

古典的には、データ位置は次のような言葉で説明されていました。

シリンダ
各プラッタ上で、中心から同じ距離にあるトラックの集合です。

ヘッド
どの読み書きヘッドを使うかを表します。
プラッタ面に対応します。

セクタ
トラックをさらに分割した単位です。

この3つで位置を表す方式を、CHSアドレッシングと呼びます。

```text
CHS:
	Cylinder
	Head
	Sector
```

ただし、現在のOSやディスクでは、普通はCHSを直接扱いません。
OSは、ディスクへ論理ブロック番号を渡します。
これをLBA、Logical Block Addressing と呼びます。

```text
OSから見えるもの:
	LBA 0
	LBA 1
	LBA 2
	LBA 3

ディスク内部:
	それを実際の物理配置へ変換する
```

LBAでは、ディスクを連続したブロック番号の並びとして扱えます。
OSやファイルシステムから見ると、これはかなり扱いやすい抽象化です。

一方、ファイルシステムも独自のブロック単位でデータを管理します。
ここで用語が少しややこしいです。

物理ブロック、デバイスブロック
ブロックデバイス側の単位です。
ディスクやSSDが扱う単位に近いです。

ファイルシステムブロック、論理ブロック
ファイルシステムがファイル内容やメタデータを管理する単位です。
通常はデバイス側のブロックを1個または複数個まとめたサイズになります。

ページ
仮想メモリやページキャッシュの単位です。
多くの環境では4KBがよく見られますが、固定ではありません。

I/O性能を考えるときは、これらの単位が絡みます。

```text
アプリケーション:
	read(fd, buf, 1000)

VFS / ファイルシステム:
	ファイルオフセットをファイルシステムブロックへ変換

ブロック層:
	ブロックデバイスへのI/O要求へ変換

デバイス:
	LBA単位で読み書き
```

UmuOSでも、最初は単純に「ファイルの何バイト目から読む」だけでよいですが、性能を考える段階では、ファイルシステムブロックやデバイスブロックとの対応が重要になります。

#### ４章の６の２　I/Oスケジューラの目的

I/Oスケジューラの目的は、I/O要求をうまく並べ替えたり、まとめたりして、全体の性能とレイテンシを改善することです。

古典的なI/Oスケジューラの基本処理は、主に次の2つです。

マージ
隣接する複数のI/O要求を、1つの大きなI/O要求へまとめます。

ソート
I/O要求をブロック番号順に並び替えます。

マージの例です。

```text
要求A:
	ブロック5を読む

要求B:
	ブロック6, 7を読む

マージ後:
	ブロック5, 6, 7を読む
```

I/O量そのものは変わりません。
しかし、I/O要求の数は減ります。
要求数が減ると、キュー管理やデバイスへの発行回数の面で有利になります。

ソートの例です。

```text
到着順:
	ブロック52
	ブロック109
	ブロック7
	ブロック81

処理順:
	ブロック7
	ブロック52
	ブロック81
	ブロック109
```

HDDでは、これによりディスクヘッドの移動量を減らせます。
エレベータが階を順番に回るように、ディスク上の近い位置をまとめて処理するイメージです。

このため、I/Oスケジューラの方式はエレベータアルゴリズムと呼ばれることがあります。

ただし、ソートだけを単純に行うと問題も出ます。
遠く離れたブロックへの要求が、近い位置の要求に押され続けて、いつまでも処理されない可能性があるからです。
これを飢餓、starvation と言います。

I/Oスケジューラは、スループットを上げるだけでなく、待たされすぎる要求を作らないようにする必要があります。

#### ４章の６の３　読み取りレイテンシと writes-starving-reads 問題

読み取り要求は、アプリケーションの実行を直接止めやすいです。
要求したデータがページキャッシュにない場合、プロセスはディスクから読み込まれるまで待つことになります。

この待ち時間を、読み取りレイテンシ、read latency と呼びます。

一方、通常の書き込みは、ページキャッシュへデータを渡した時点で戻ることが多いです。
実際のディスク書き込みは、後でカーネルがまとめて行います。

そのため、書き込みはストリームのように大量に溜まりやすいです。
カーネルとデバイスが書き込み要求の処理に集中してしまうと、読み取り要求が待たされることがあります。

これが writes-starving-reads 問題です。

```text
大量の書き込み:
	ページキャッシュへどんどん溜まる
	後でまとめてディスクへ流れる

読み取り:
	ページキャッシュにないと、今すぐディスクから読みたい

問題:
	書き込み処理に押されて、読み取りが遅れる
```

ユーザー体感では、読み取りレイテンシはかなり重要です。
たとえば、アプリケーションの起動、設定ファイルの読み込み、ディレクトリ走査、データベースの問い合わせなどで、読み取りが遅いとすぐに体感へ出ます。

そのため、多くのI/Oスケジューラは、読み取り要求をある程度優先し、書き込みに押しつぶされないようにします。

#### ４章の６の４　古典的なI/Oスケジューラ

古いLinux 2.6時代の説明では、いくつかのI/Oスケジューラがよく出てきます。
現在ではデフォルトや利用可能なスケジューラが変わっていますが、考え方を理解するには役立ちます。

Deadline I/Oスケジューラ
I/O要求に期限、deadline を持たせる方式です。
通常はブロック番号順にソートして処理しますが、待たされすぎた要求がある場合は、その要求を優先します。
読み取り要求の期限を短めにすることで、読み取りレイテンシを抑えます。

Anticipatory I/Oスケジューラ
読み取り要求を処理したあと、次の近い読み取り要求が来るかもしれないと予測して、短時間だけ待つ方式です。
HDDでは、すぐ別の場所へシークするより、少し待ったほうが結果的に速い場合がありました。
ただし、現在のLinuxではこのスケジューラは歴史的なものとして見るのがよいです。

CFQ I/Oスケジューラ
Completely Fair Queuing の略です。
プロセスごとにキューを持ち、各プロセスにある程度公平にI/O機会を与える方式です。
かつてはデスクトップ用途などでよく使われました。
ただし、現在のLinuxではCFQは古い単一キュー時代のスケジューラとして扱われ、blk-mq環境では別のスケジューラが使われます。

Noop I/Oスケジューラ
ほとんど何もしないスケジューラです。
基本的にはマージ程度で、複雑なソートは行いません。
HDDのシーク最適化が不要なデバイスや、下位層で十分にスケジューリングされる環境で使われる考え方です。

古い本では、`as`、`cfq`、`deadline`、`noop` などを切り替える説明が出てきます。
これは当時のLinuxの状況としては自然です。
しかし、現在のLinuxではブロック層が blk-mq、つまり multi-queue block layer へ移行しており、利用できるスケジューラ名も変わっています。

#### ４章の６の５　現在のLinuxでのI/Oスケジューラ

現在のLinuxでは、デバイスごとに利用可能なI/Oスケジューラを確認できます。

たとえば、`sda` のスケジューラを見るなら次のようにします。

```bash
cat /sys/block/sda/queue/scheduler
```

NVMeデバイスなら、たとえば次のようなパスになります。

```bash
cat /sys/block/nvme0n1/queue/scheduler
```

出力例の形は、環境によって異なります。
現在選択されているスケジューラは、角括弧で囲まれて表示されます。

```text
none mq-deadline [kyber] bfq
```

これはあくまで例です。
実際に何が表示されるかは、カーネル、ディストリビューション、デバイス種類、設定によって変わります。

現在よく見かける名前には、次のようなものがあります。

none
I/Oスケジューラによる積極的な並び替えを行わない設定です。
NVMeのようにデバイス内部のキューや並列性が強い場合、`none` が選ばれることがあります。

mq-deadline
blk-mq向けのDeadline系スケジューラです。
期限を使ってレイテンシを抑えつつ、マージや並び替えを行います。

bfq
Budget Fair Queueing の略です。
デスクトップや対話的な処理で体感を良くする目的で使われることがあります。
プロセスやcgroupごとの公平性を意識します。

kyber
レイテンシ目標を意識するスケジューラです。
環境によって利用可能な場合があります。

スケジューラを変更するには、root権限で `/sys/block/.../queue/scheduler` へ書き込みます。

```bash
echo mq-deadline | sudo tee /sys/block/sda/queue/scheduler
```

ただし、これはシステム全体のI/O挙動に影響します。
研究目的で確認するのはよいですが、実運用のシステムで変更する場合は、対象デバイス、ワークロード、ディストリビューションの推奨、ベンチマーク結果を確認してからにするべきです。

#### ４章の６の６　I/Oパフォーマンス最適化の基本

アプリケーション側でできるI/O性能改善もあります。
基本は、次のような考え方です。

小さなI/Oを減らす
1バイトずつ `read()` / `write()` するような処理は避けます。
適切なサイズのバッファを使い、少数の大きなI/Oへまとめます。

ユーザー空間でバッファリングする
3章で見た標準I/Oや、自前のバッファを使い、システムコール回数を減らします。

ベクタI/Oを使う
複数のバッファをまとめて出したい場合は、`writev()` などを使うと、余計なコピーやシステムコール回数を減らせます。

アクセスパターンをカーネルへ伝える
`posix_fadvise()` や `madvise()` で、シーケンシャルアクセス、ランダムアクセス、不要になった範囲などを伝えます。

必要なら非同期I/Oやイベント駆動を使う
I/O待ちで処理全体が止まる場合は、スレッド、`epoll`、`io_uring` などの利用を検討します。

同期書き込みを乱用しない
`fsync()`、`O_SYNC`、`O_DSYNC` は安全性には重要ですが、性能への影響が大きいです。
必要な場所に絞って使います。

読み書きサイズとアラインメントを意識する
特に `O_DIRECT` を使う場合は、バッファアドレス、サイズ、ファイルオフセットのアラインメント制約があります。
通常のページキャッシュ経由I/Oでも、極端に細かいI/Oは不利になりやすいです。

#### ４章の６の７　ユーザー空間でのI/Oスケジューリング

非常にI/O負荷の高いアプリケーションでは、アプリケーション側でもI/O要求の順序を工夫することがあります。

たとえば、大量のファイルを読み込むバックアップツールやインデクサ、データベース、ストレージ系プログラムなどです。

カーネルのI/Oスケジューラがあるのに、なぜアプリケーション側でも順序を考えるのかというと、アプリケーションは未来の要求を知っている場合があるからです。

```text
カーネル:
	すでに届いたI/O要求を見て並び替える

アプリケーション:
	これから読む予定のファイル一覧を知っている場合がある
```

たとえば、アプリケーションが1000個のファイルを読む予定なら、読み始める前にある程度順序を並べ替えられます。
これにより、カーネルへ届くI/O要求の順序が最初から良くなり、結果的に効率が上がる場合があります。

ただし、これは特殊な最適化です。
普通のアプリケーションで無理にやるものではありません。
ファイルシステム、SSD、ページキャッシュ、カーネルのスケジューラがすでにかなり賢く動くため、アプリケーション側の独自スケジューリングが逆効果になることもあります。

#### ４章の６の８　パス名によるソート

一番簡単な近似は、パス名でソートすることです。

同じディレクトリ内のファイルは、作成時期やファイルシステムの配置方針により、近い位置に配置されることがあります。
そのため、パス名順に処理すると、ある程度ディスク上の近さを近似できる場合があります。

利点は単純さです。
ファイルシステムに依存せず、root権限も不要です。

欠点は、精度が低いことです。
ファイルシステムの断片化、SSD、コピーや移動の履歴、CoW系ファイルシステムなどにより、パス名順と物理配置は簡単にずれます。

現在の感覚では、パス名ソートは「簡単にできる軽い近似」くらいの位置づけで考えるとよいです。

#### ４章の６の９　inode番号によるソート

次の近似は、inode番号でソートする方法です。

inodeは、Unix系ファイルシステムでファイルのメタデータを表す重要な構造です。
ファイルサイズ、所有者、パーミッション、時刻、データブロックへの参照などを管理します。

inode番号は `stat()` や `fstat()` で取得できます。

```c
#include <fcntl.h>
#include <inttypes.h>
#include <stdio.h>
#include <sys/stat.h>
#include <unistd.h>

int main(int argc, char *argv[])
{
	if (argc != 2) {
		fprintf(stderr, "usage: %s <file>\n", argv[0]);
		return 1;
	}

	int fd = open(argv[1], O_RDONLY | O_CLOEXEC);
	if (fd == -1) {
		perror("open");
		return 1;
	}

	struct stat st;
	if (fstat(fd, &st) == -1) {
		perror("fstat");
		close(fd);
		return 1;
	}

	printf("inode=%ju\n", (uintmax_t)st.st_ino);

	close(fd);
	return 0;
}
```

古いサンプルでは inode番号を `int` で扱っていることがありますが、現在は避けたほうがよいです。
`st_ino` は `ino_t` 型であり、環境によってサイズが異なります。
表示するなら、上のように `uintmax_t` へキャストして `%ju` で出すと比較的安全です。

inode番号によるソートの利点は、パス名よりもファイルシステム内部の配置に近い情報を使える場合があることです。
特に古典的なUnix系ファイルシステムでは、inode番号とデータ配置にある程度の相関が期待できました。

欠点は、やはり近似でしかないことです。
ext4、XFS、Btrfs、ネットワークファイルシステム、CoWファイルシステムなどでは、inode番号と物理配置の関係は単純ではありません。
SSDでは物理配置という発想自体もHDDほど直接的ではありません。

したがって、inode番号ソートは「昔ながらのHDDとUnix系ファイルシステムでは効く可能性がある近似」と理解するのがよいです。

#### ４章の６の１０　物理ブロック番号によるソート

理屈の上では、もっとも直接的なのは物理ブロック番号でソートする方法です。
ファイルの各論理ブロックが、ディスク上のどの物理ブロックに対応するかが分かれば、それをもとに順序を決められます。

古いLinuxでは、`ioctl()` の `FIBMAP` を使って、ファイルの論理ブロックから物理ブロック番号を取得する例が紹介されることがあります。

```c
#include <linux/fs.h>
#include <sys/ioctl.h>

int block = logical_block;
if (ioctl(fd, FIBMAP, &block) == -1) {
	perror("ioctl FIBMAP");
}
```

ただし、現在の研究用サンプルとしては、これはあまりおすすめしません。

理由はいくつかあります。

root権限や `CAP_SYS_RAWIO` が必要になることがある
一般ユーザーのアプリケーションでは使えない場合があります。

ファイルシステム依存が強い
すべてのファイルシステムで同じように使えるわけではありません。

SSDや仮想化環境では物理ブロック番号の意味が薄い
デバイス内部のウェアレベリング、論理物理変換、仮想ディスク、RAID、LVMなどにより、OSから見えるブロック番号が実際の物理位置を素直に表すとは限りません。

現在はFIEMAPのほうが一般的な場面がある
Linuxでは `FIEMAP` という、ファイルのextent情報を取得する仕組みもあります。
ただし、これもファイルシステム依存であり、通常のアプリケーションが気軽に使うものではありません。

つまり、物理ブロック番号によるソートは、理論としては分かりやすいですが、現在の一般的なアプリケーション最適化としてはかなり特殊です。
ストレージ解析ツールやファイルシステム研究なら面白いですが、普通のCプログラムではまず使いません。

#### ４章の６の１１　現在のストレージでの読み替え

古い本のI/Oスケジューラの説明は、HDDをかなり強く前提にしています。
現在は次のような環境が一般的です。

SSD
物理ヘッドのシークはありません。
ランダムアクセスはHDDより圧倒的に速いです。
ただし、内部ではフラッシュメモリのページ、ブロック、消去単位、ウェアレベリングなどがあります。

NVMe
複数キュー、高い並列性、低レイテンシが特徴です。
古典的な単一キューのI/Oスケジューラとは考え方が変わります。

仮想ディスク
クラウド、VM、コンテナ環境では、見えているブロックデバイスの背後に別のストレージ層があります。
アプリケーションから物理配置を推測するのはかなり難しいです。

RAID / LVM / dm-crypt
ブロックデバイスの下にさらに変換層があります。
OSから見える連続ブロックが、実際のデバイス上でも連続とは限りません。

このため、現在のアプリケーションでまず意識すべきなのは、物理ブロック番号を頑張って推測することではなく、次のような基本です。

```text
システムコール回数を減らす
適切なサイズでまとめて読む/書く
ページキャッシュを味方につける
アクセスパターンを明確にする
必要なら posix_fadvise() を使う
fsync() を必要な場所に絞る
実測する
```

ストレージ最適化は、思い込みより実測が大事です。
HDDでは速くなった工夫が、SSDでは意味が薄いこともあります。
逆に、SSDでも小さすぎるI/Oを大量に出すと性能が落ちることがあります。

#### ４章の６の１２　UmuOSでどう考えるか

UmuOSの視点では、I/Oスケジューラはかなり高度な機能です。
最初からDeadlineやCFQのような本格的なものを作る必要はないと思います。

まずは、次の段階で考えるとよさそうです。

第一段階
I/O要求をそのままデバイスへ渡します。
実装が単純で、動作確認しやすいです。

第二段階
隣接する要求をマージします。
たとえば、ブロック10と11への連続読み取りを、1つの要求にまとめます。

第三段階
単純なソートを入れます。
HDD的なデバイスを想定するなら、ブロック番号順に並べるだけでも意味があります。

第四段階
読み取り要求を待たせすぎない仕組みを入れます。
期限や優先度を持たせると、writes-starving-reads問題を避けやすくなります。

第五段階
デバイス特性に応じて方針を変えます。
HDD、SSD、RAMディスク、仮想ブロックデバイスでは、最適なスケジューリングが違います。

UmuOSでは、最初は次のような単純な構造体から始めてもよいと思います。

```c
enum io_op {
	IO_READ,
	IO_WRITE
};

struct io_request {
	enum io_op op;
	unsigned long block;
	unsigned long nr_blocks;
	void *buffer;
	int process_id;
};
```

ここから、キューに入れる、隣接要求をまとめる、読み取りを少し優先する、といった機能を少しずつ足せます。

この節で大事なのは、I/O性能は `read()` / `write()` の呼び方だけでなく、その下にあるファイルシステム、ページキャッシュ、ブロック層、I/Oスケジューラ、デバイス特性まで含めて決まる、ということです。

次は、4章全体の結びとして、高度なファイルI/Oの要点を整理します。

### ４章の７　章の結び

ここまで、2章、3章、4章と続けて、LinuxにおけるファイルI/Oをかなり広く見てきました。

2章では、`open()`、`read()`、`write()`、`close()`、`lseek()` など、LinuxファイルI/Oの基礎を扱いました。
これはUnixプログラミングの基礎でもあります。
ファイルディスクリプタ、ファイルオフセット、通常ファイル、パイプ、端末などを、同じfdという抽象で扱う考え方が中心でした。

3章では、標準I/Oとユーザー空間バッファリングを見ました。
`FILE *`、`fopen()`、`fread()`、`fwrite()`、`fflush()` などを使うと、システムコールを毎回直接呼ぶのではなく、ユーザー空間側でバッファを持って効率化できます。
ここでは、カーネルのI/OとCライブラリのI/Oを混同しないことが重要でした。

そしてこの4章では、さらに高度なI/Oを扱いました。

まず、`readv()` / `writev()` によるscatter-gather I/Oを見ました。
複数のバッファを一度のシステムコールで読み書きできるため、ヘッダ、本文、改行のように分かれたデータを自然に扱えます。
Ushのエラーメッセージ出力などでも、文字列を毎回結合せずに出せる可能性がありました。

次に、`epoll` を見ました。
`select()` や `poll()` のように毎回fd集合を渡すのではなく、監視対象をカーネル側のepollコンテキストへ登録しておき、発生したイベントを受け取る仕組みです。
大量のfdを扱うネットワークサーバーやイベントループでは、かなり重要な考え方になります。

さらに、`mmap()` によるメモリマップI/Oを見ました。
ファイルをプロセスのアドレス空間へ対応づけることで、ファイルをメモリのように扱えます。
ここでは、ページサイズ、ページフォルト、`MAP_SHARED`、`MAP_PRIVATE`、`munmap()`、`msync()`、`madvise()` など、ファイルI/Oと仮想メモリがつながる部分が出てきました。

ファイルI/Oのヒントとして、`posix_fadvise()` や `readahead()` も見ました。
アプリケーションが「順番に読む」「ランダムに読む」「すぐ使う」「もう使わない」といった情報をカーネルへ伝えることで、ページキャッシュや先読みを調整できる場合があります。

同期と非同期についても整理しました。
`synchronous / asynchronous` は、処理の完了を待つかどうかの軸です。
`synchronized / nonsynchronized` は、ディスクなどの永続媒体と同期されているかどうかの軸です。
通常の `write()` はシンクロナスではありますが、必ずディスクへ書き出されたとは限りません。
この違いを理解しておくと、`fsync()`、`O_SYNC`、AIO、`io_uring` などの位置づけが見えやすくなります。

最後に、I/OスケジューラとI/Oパフォーマンスを見ました。
古典的なHDDでは、シークを減らすためにI/O要求をマージしたり、ブロック番号順へ並べ替えたりすることが重要でした。
現在はSSDやNVMeが一般的になり、事情は変わりましたが、I/O要求をどう並べるか、どの要求を待たせすぎないか、ページキャッシュやデバイス特性をどう活かすか、という考え方は今でも重要です。

この4章全体をUmuOSの視点で見ると、ファイルI/Oは単に `read()` と `write()` を実装すれば終わり、というものではないことが分かります。

```text
ファイルディスクリプタ
ファイルオフセット
ページキャッシュ
ユーザー空間バッファ
複数バッファI/O
イベント待ち
メモリマップ
同期と永続化
I/O要求キュー
I/Oスケジューリング
```

これらが重なって、LinuxのI/Oは成り立っています。

UmuOSでは、最初から全部を実装する必要はありません。
むしろ、最初は単純な `open()`、`read()`、`write()`、`close()` をしっかり動かすことが大事です。
その上で、必要に応じて、バッファリング、パイプ、イベント待ち、簡単なI/Oキュー、ファイルキャッシュ、と段階的に育てていけばよいと思います。

Ushのようなシェルを作る場合にも、この章の内容はかなり効いてきます。
リダイレクト、パイプ、標準入力、標準出力、標準エラー、子プロセス、端末、ジョブ制御は、すべてfdとI/Oの理解の上に乗っています。
シェルは文字列を処理するプログラムに見えますが、実際にはfdをつなぎ替え、プロセスを作り、I/Oの流れを制御するプログラムでもあります。

次の5章と6章では、プロセス管理へ進みます。
プロセスの作成、終了、親子関係、状態管理、シグナル、ジョブ制御などを理解すると、UshやUmuOSの設計はさらに一段深く見えるようになります。

ここまでのファイルI/Oの知識は、プロセス管理とつながったときに一気に生きてきます。
たとえば、シェルで次のような処理をするときです。

```bash
cat input.txt | grep tama > output.txt
```

この1行の裏側には、ファイルI/O、パイプ、fdの複製、リダイレクト、プロセス生成、`exec()`、終了待ちが全部入っています。

つまり、4章までで見てきたI/Oは、次章からのプロセス管理と合体して、いよいよシェルらしい世界へつながっていく感じです。



