---
title: "UmuOSの為のC言語（現代ポインタ）　3章　ポインタと関数"
---

# UmuOSの為のC言語（現代ポインタ）

このノートは、UmuOSを進化させるために必要となるC言語のポインタ理解を、現代のC17相当の視点で抽象化し、再構成することであります。
すなわち、UmuOSの実装・読解・デバッグへ還元するための実践ノートであります。
ポインタは、単なる文法項目ではなく、メモリ、配列、文字列、関数呼び出し、所有権、未定義動作と深く結びついています。
この構造を理解することは、UmuOSの設計力を高めることに直結すると思います。

## 3章　ポインタと関数

関数とポインタの関係は、C言語の実用性を支える中心部分です。
関数へ値を渡す、関数の中から呼び出し側のデータを更新する、可変個のデータを効率よく扱う、処理の切り替えを動的に行う、
といったことは、いずれもポインタ理解の上に成り立っています。

特に重要なのは、Cの関数引数が常に値渡しである、という原則です。
この原則は、単純な整数を渡す場合にも、配列の先頭を指すポインタを渡す場合にも、関数ポインタを渡す場合にも変わりません。
ただし、値として渡されたものが「どこかのオブジェクトを指しているアドレス」であるとき、関数の中からその先のデータへ到達できるため、
呼び出し側の状態を間接的に変更できます。
ここで混乱すると、「なぜ swap はポインタ版だと動いて、値渡し版だと動かないのか」
「なぜポインタそのものを関数の中で差し替えたいときは二重ポインタが必要なのか」が見えにくくなります。

また、関数の実行そのものも、スタックフレーム、戻り先、引数、局所変数などと深く関係しています。
古い説明では「ローカル変数は必ずスタックに置かれる」と断定されがちですが、現代の最適化コンパイラでは
レジスタ割り当てやインライン展開も普通に起きます。
そのため、この章では、まず実装イメージとしてのスタックとヒープを押さえつつ、どこまでが概念図で、どこからが処理系依存かを
切り分けて扱います。

後半では関数ポインタを扱います。
関数ポインタは構文こそ少し読みにくいものの、比較関数、コールバック、ディスパッチ表、簡易的なプラグイン的設計など、
Cプログラムで広く使われます。
UmuOSのように低レイヤを読む・組む文脈でも、割り込み処理表、デバイス操作群、イベントハンドラ、状態遷移の切り替えなどで、
発想として非常に重要です。

### 3章の1　スタックとヒープ

関数とポインタの関係を正しく理解するには、データがどこに置かれ、いつまで有効なのかを整理する必要があります。
ここで、まずは実装イメージとしてのスタックとヒープを押さえます。

```text
スタック:
	関数呼び出しに伴う管理情報や、自動記憶域期間のオブジェクトが使うことの多い領域
	関数呼び出しと復帰に合わせて伸び縮みする

ヒープ:
	malloc() などで動的に確保した領域
	必要になった時に確保し、free() で解放する

静的領域:
	大域変数や static 変数が置かれることの多い領域
	プログラム開始から終了まで存在する

コード領域:
	関数本体や読み取り専用データが置かれることのある領域
	関数ポインタを考える時に意識する
```

ただし、C言語標準が「スタック」や「ヒープ」という語でプログラムの意味を定義しているわけではありません。
言語仕様の側からは、次のように理解する方が正確です。

```text
自動記憶域期間:
	通常のローカル変数
	ブロックに入ると生存し、ブロックを抜けると寿命が終わる

静的記憶域期間:
	大域変数、ファイルスコープの static、関数内 static 変数など
	プログラム開始から終了まで存在する

動的確保領域:
	malloc() などで得た領域
	free() されるまで存在する
```

実務上は、「関数のローカル変数は典型的にはスタックに置かれる」「malloc() で得た領域は典型的にはヒープにある」と考えて
差し支えない場面が多いです。
しかし、ポインタの安全性を判断する時は、最終的には置き場所の俗称ではなく、寿命と到達可能性で考える必要があります。

#### 3章の1の1　スタックとは何か

関数が呼び出されると、実行環境はその呼び出しに必要な情報をどこかへ積みます。
その代表的な実装がスタックです。
ここには、典型的には次のような情報が関わります。

```text
戻り先の情報
	関数が終了したあと、どこへ戻るか

引数に関する情報
	どの値がどの引数として渡されたか

局所変数や一時領域
	関数の内部で使うデータ

保存されたレジスタなど
	呼び出し規約に従って退避される情報
```

よくある誤解として、「スタックは必ず高位アドレスから低位アドレスへ伸びる」と断定して覚えてしまうものがあります。
実際には、成長方向もフレーム構造も処理系依存です。
Linux/x86-64 では下方向へ伸びる実装をよく見ますが、それを言語仕様の保証だと思ってはいけません。

また、局所変数が常にメモリ上の連続した場所に並ぶとも限りません。
最適化が有効なビルドでは、ある変数はレジスタに置かれ、別の変数だけがメモリに置かれることもあります。
したがって、アドレスを表示して「変数は必ずこの順に積まれる」と一般化するのは危険です。
アドレス表示は、デバッグ用の観察としては有益ですが、仕様の説明には直接なりません。

#### 3章の1の2　スタックフレームは概念図として理解する

関数呼び出しごとに使われる領域を、ここではスタックフレームと呼びます。
概念図としては、次のように考えると分かりやすいです。

```text
スタックフレームに含まれうるもの:
	戻り先情報
	引数やその退避先
	局所変数
	保存されたレジスタ
	一時的な作業領域
```

ここで重要なのは、スタックフレームは「各変数の正確な配置を人間が当てるためのもの」ではなく、
「その関数専用の実行文脈がある」という理解のための概念だという点です。

例えば、次の関数を見ます。

```c
#include <stdio.h>

double average(const int *arr, size_t size)
{
	long long sum = 0;

	printf("&arr  = %p\n", (void *)&arr);
	printf("&size = %p\n", (void *)&size);
	printf("&sum  = %p\n", (void *)&sum);

	for (size_t index = 0; index < size; index++) {
		sum += arr[index];
	}

	return size == 0 ? 0.0 : (double)sum / (double)size;
}
```

このようなコードは、関数の中に複数の自動変数が存在し、それぞれが独立した実体として管理されていることを観察するには役立ちます。
ただし、表示されたアドレスの並びから、すべての環境で同じ配置順が保証されるわけではありません。
特に最適化レベル、ABI、アーキテクチャ、デバッグ情報の有無によって見え方は変わります。

#### 3章の1の3　ヒープとの違い

ヒープは、動的メモリ確保のための実装上の領域です。
関数から戻っても自動的には消えず、free() されるまで残ります。
この性質があるため、関数の中で確保したメモリを呼び出し側へ返すことができます。

一方、自動記憶域期間の局所変数は、関数を抜けた時点で寿命が終わります。
この違いが、次の二つの設計の差になります。

```text
安全な例:
	malloc() で確保した領域のポインタを返す

危険な例:
	局所配列や局所変数のアドレスを返す
```

この違いは、3章の2で詳しく扱います。

#### 3章の1の4　スタックオーバーフロー

スタックは無限ではありません。
深すぎる再帰、大きすぎる局所配列、異常な入れ子呼び出しなどにより、スタック領域を使い切ることがあります。
これがスタックオーバーフローです。

例えば、巨大な配列を毎回ローカル変数として確保する設計は危険です。

```c
void work(void)
{
	char buffer[1024 * 1024];
	buffer[0] = '\0';
}
```

1 MiB 程度でも、呼び出し回数やスレッド数によっては無視できません。
Linux ではスレッドごとにスタックサイズ制限があるため、UmuOS やツール群の設計でも「大きい一時領域をどこに置くか」は重要です。
大きな可変データを必要とするなら、ヒープ確保やストリーミング処理を検討する方が良い場面が多いです。

### 3章の2　ポインタを関数に渡し、関数からポインタを返す

ここからは、関数とポインタの具体的な関係を扱います。
まず大前提として、Cの引数は常に値渡しです。
つまり、関数に渡るのは実引数そのものではなく、そのコピーです。

```text
int を渡す:
	整数値のコピーが渡る

int * を渡す:
	アドレス値のコピーが渡る

int ** を渡す:
	ポインタへのポインタの値のコピーが渡る
```

この原則を見失わなければ、なぜある関数は呼び出し側のデータを変更できて、別の関数は変更できないのかが整理できます。

#### 3章の2の1　ポインタを渡すと、指している先を更新できる

もっとも基本的な例は、swap 関数です。
呼び出し側の整数を入れ替えたいなら、整数そのものではなく、そのアドレスを渡します。

```c
#include <stdio.h>

void swap_int(int *left, int *right)
{
	int tmp = *left;
	*left = *right;
	*right = tmp;
}

int main(void)
{
	int first = 5;
	int second = 10;

	printf("before: first=%d second=%d\n", first, second);
	swap_int(&first, &second);
	printf("after : first=%d second=%d\n", first, second);
	return 0;
}
```

ここで left と right は、それぞれ first と second のアドレス値をコピーとして受け取っています。
しかし、left が指している先は first そのものです。
そのため、*left に書き込めば呼び出し側の first が更新されます。

この時、「ポインタを渡したので参照渡しになった」と説明されることがありますが、厳密には違います。
Cには参照渡しはありません。
あくまで、アドレス値を値渡しした結果として、呼び出し側のオブジェクトへ到達できているだけです。

#### 3章の2の2　値渡しだけでは呼び出し側は変わらない

同じ swap を値渡しだけで書くと、呼び出し側の値は変わりません。

```c
void swap_broken(int left, int right)
{
	int tmp = left;
	left = right;
	right = tmp;
}
```

この関数は、呼び出し元の変数を入れ替えているのではなく、left と right というローカルなコピーを書き換えているだけです。
そのため、関数の外へ出ると変更は消えます。

ここは C の関数設計で非常に重要です。
呼び出し側の状態を変更したいのか、変更したくないのかを、引数の型で明示する必要があります。

#### 3章の2の3　変更させたくないなら const を付ける

関数の中でデータを書き換えてほしくない場合は、const を使います。

```c
void copy_value(const int *source, int *destination)
{
	*destination = *source;
}
```

この宣言は、source が指す int を関数内で変更しない、という契約を表しています。
コンパイラもその契約を利用し、誤って書き換えようとした時に検出できます。

```c
void broken_copy(const int *source, int *destination)
{
	/* *source = 100; */
	*destination = *source;
}
```

コメントアウトした行を有効にすると、通常はコンパイルエラーになります。
つまり const は、単なる飾りではなく、関数インターフェースにおける書き込み禁止の宣言です。

読み取り専用データを受け取る関数には、積極的に const を付けるべきです。
これは安全性だけでなく、API の意図を明確にします。

#### 3章の2の4　ポインタを返す時は寿命と所有権を明示する

関数からポインタを返すこと自体は普通に行えます。
ただし、返したポインタが何を指し、その寿命がどこまでで、最後に誰が解放するのかを明確にしなければなりません。

まず、関数の中で動的メモリを確保して返す例です。

```c
#include <stddef.h>
#include <stdlib.h>

int *allocate_int_array(size_t count, int value)
{
	int *array = malloc(count * sizeof *array);

	if (array == NULL) {
		return NULL;
	}

	for (size_t index = 0; index < count; index++) {
		array[index] = value;
	}

	return array;
}
```

使う側は次のようになります。

```c
#include <stdio.h>
#include <stdlib.h>

int main(void)
{
	int *vector = allocate_int_array(5, 45);

	if (vector == NULL) {
		fputs("allocation failed\n", stderr);
		return 1;
	}

	for (size_t index = 0; index < 5; index++) {
		printf("%d\n", vector[index]);
	}

	free(vector);
	return 0;
}
```

ここでの重要点は次の通りです。

```text
返されたポインタはヒープ上の領域を指す
	関数終了後も有効である

呼び出し側が所有権を受け取る
	最後に free() する責任を持つ

NULL を返しうる
	失敗時の分岐が必要である
```

古いコードでは malloc() の戻り値をキャストしている例をよく見ますが、C では通常不要です。
また、size 引数には int より size_t を使う方が、要素数やバイト数を表す意図に合っています。

#### 3章の2の5　局所変数のアドレスを返してはいけない

次のような関数は誤りです。

```c
int *broken_array(void)
{
	int values[4] = {1, 2, 3, 4};
	return values;
}
```

values は自動記憶域期間の局所配列です。
関数を抜けた時点で寿命が終わるため、返されたポインタはただちに不正になります。
このポインタを使うコードは未定義動作です。

この手のバグは、たまたま最初は動いて見えることがあります。
しかし、別の関数呼び出しや最適化によって簡単に壊れます。
「動いたから正しい」ではなく、「寿命が残っているから正しい」と判断しなければなりません。

#### 3章の2の6　返す代わりに、呼び出し側が用意した領域へ書く方法

ポインタを返す方法以外に、呼び出し側が領域を用意し、それを関数へ渡す設計もよく使われます。

```c
void fill_int_array(int *array, size_t count, int value)
{
	if (array == NULL) {
		return;
	}

	for (size_t index = 0; index < count; index++) {
		array[index] = value;
	}
}
```

呼び出し側は次のようになります。

```c
int *vector = malloc(5 * sizeof *vector);

if (vector == NULL) {
	return 1;
}

fill_int_array(vector, 5, 45);
free(vector);
```

この設計の利点は、メモリ確保の責任と初期化の責任を分離できることです。
ライブラリ設計では、「誰が確保し、誰が解放するか」を曖昧にしないために、この方式が好まれることがあります。

#### 3章の2の7　ポインタそのものを差し替えるなら二重ポインタが必要

関数の中で「ポインタが指す先」ではなく、「ポインタ変数の中身そのもの」を変更したい場合は、ポインタへのポインタを渡します。

```c
#include <stddef.h>
#include <stdlib.h>

int allocate_and_fill(int **array_ptr, size_t count, int value)
{
	if (array_ptr == NULL) {
		return -1;
	}

	int *array = malloc(count * sizeof *array);
	if (array == NULL) {
		return -1;
	}

	for (size_t index = 0; index < count; index++) {
		array[index] = value;
	}

	*array_ptr = array;
	return 0;
}
```

呼び出し側は次のように使います。

```c
int *vector = NULL;

if (allocate_and_fill(&vector, 5, 45) != 0) {
	return 1;
}

free(vector);
```

ここで渡しているのは vector の値ではなく、vector 変数自身のアドレスです。
そのため、関数の中で *array_ptr = array; と書くと、呼び出し側の vector が更新されます。

一方、次のように単なる int * を受け取るだけでは、呼び出し側のポインタ変数は書き換わりません。

```c
void allocate_broken(int *array, size_t count, int value)
{
	array = malloc(count * sizeof *array);
	if (array == NULL) {
		return;
	}

	for (size_t index = 0; index < count; index++) {
		array[index] = value;
	}
}
```

この関数が受け取っているのは、呼び出し側ポインタのコピーです。
そのコピーへ新しいアドレスを代入しても、呼び出し側の変数には反映されません。
しかも、確保したメモリへの参照を失えば、その場でメモリリークになります。

#### 3章の2の8　free 後に NULL を入れる補助関数について

解放後のポインタに NULL を代入するのは、ダングリングポインタの再利用を見つけやすくするための一つの手法です。
ただし、万能ではありません。
別名のポインタが残っていれば、その別名は依然としてダングリングです。

それでも、単一の所有者しか持たないポインタでは有益です。
例えば次のように書けます。

```c
#include <stdlib.h>

void safer_free(void **pointer)
{
	if (pointer == NULL || *pointer == NULL) {
		return;
	}

	free(*pointer);
	*pointer = NULL;
}

#define SAFE_FREE(ptr) safer_free((void **)&(ptr))
```

使い方は次の通りです。

```c
int *value = malloc(sizeof *value);
if (value == NULL) {
	return 1;
}

*value = 5;
SAFE_FREE(value);
SAFE_FREE(value);
```

ただし、void ** を使う汎用 free 補助関数には型安全性の限界があります。
また、free(NULL) 自体は標準で安全なので、「二重に free しても完全に安全になる道具」ではありません。
設計の本質は、所有権を明確にし、誰がいつ解放するかを曖昧にしないことです。

### 3章の3　関数ポインタ

関数ポインタは、関数のアドレスを保持するポインタです。
これを使うと、どの関数を実行するかをデータとして扱えるようになります。

例えば次のような場面で使います。

```text
コールバック:
	何かの処理が終わった時に呼ぶ関数を後から渡す

比較関数:
	qsort() のように、並べ替え規則を外から与える

ディスパッチ表:
	文字や番号に応じて実行する関数を切り替える

簡易的な多態性:
	構造体に関数ポインタを持たせて振る舞いを差し替える
```

構文は少し読みにくいですが、本質は「関数を指すポインタ」でしかありません。

#### 3章の3の1　関数ポインタの宣言

もっとも単純な形の一つは次です。

```c
void (*handler)(void);
```

これは「handler は、引数なしで何も返さない関数を指すポインタ」です。
読み方のコツは、変数名 handler から外側へ読むことです。

```text
handler
	変数名

*handler
	handler はポインタ

(*handler)(void)
	そのポインタは引数なし関数を指す

void (*handler)(void)
	その関数の戻り値は void
```

ほかの例も見ます。

```c
int (*f1)(double);
void (*f2)(char *);
double *(*f3)(int, int);
```

これは順に、double を受けて int を返す関数、char へのポインタを受けて何も返さない関数、2つの int を受けて double へのポインタを返す関数を指すポインタです。

関数ポインタと、ポインタを返す関数は見た目が似ています。

```c
int *f4(void);
int (*f5)(void);
int *(*f6)(void);
```

```text
f4:
	int へのポインタを返す関数

f5:
	int を返す関数へのポインタ

f6:
	int へのポインタを返す関数へのポインタ
```

この構文に慣れないうちは、typedef を使って別名を付ける方が読みやすくなります。

```c
typedef int (*binary_int_op)(int, int);
```

#### 3章の3の2　関数ポインタで関数を呼ぶ

次の例では、整数を受け取って二乗を返す関数を、関数ポインタ経由で呼び出します。

```c
#include <stdio.h>

int square(int value)
{
	return value * value;
}

int main(void)
{
	int (*function_ptr)(int) = square;
	int number = 5;

	printf("%d squared is %d\n", number, function_ptr(number));
	printf("%d squared is %d\n", number, (*function_ptr)(number));
	return 0;
}
```

function_ptr(number) でも、(*function_ptr)(number) でも呼び出せます。
どちらも同じ意味です。
配列と同じく、関数名 square はこの文脈では関数のアドレスとして扱われます。
そのため、次のように &square と書いても実用上は同じです。

```c
int (*function_ptr)(int) = &square;
```

#### 3章の3の3　関数ポインタを引数に取る

関数ポインタは、ほかの関数へ渡せます。
これはコールバックや戦略切り替えの基本形です。

```c
typedef int (*binary_int_op)(int, int);

int add(int left, int right)
{
	return left + right;
}

int subtract(int left, int right)
{
	return left - right;
}

int compute(binary_int_op operation, int left, int right)
{
	return operation(left, right);
}
```

使い方は次のようになります。

```c
printf("%d\n", compute(add, 5, 6));
printf("%d\n", compute(subtract, 5, 6));
```

この形は、if 文や switch 文で分岐を増やす代わりに、実行すべき処理そのものを引数として渡している、と見ることができます。

#### 3章の3の4　関数ポインタを返す

入力に応じて適切な関数を選び、そのポインタを返すこともできます。

```c
typedef int (*binary_int_op)(int, int);

binary_int_op select_operation(char opcode)
{
	switch (opcode) {
	case '+':
		return add;
	case '-':
		return subtract;
	default:
		return NULL;
	}
}
```

このように NULL を返せる設計にしておくと、不正な opcode を安全に扱えます。
それを使う側は、返ってきたポインタを検査してから呼び出します。

```c
int evaluate(char opcode, int left, int right, int *result)
{
	binary_int_op operation = select_operation(opcode);

	if (operation == NULL || result == NULL) {
		return -1;
	}

	*result = operation(left, right);
	return 0;
}
```

古い説明では default がない例も見られますが、現代的には「想定外入力にどう失敗するか」を明示した方が良いです。

#### 3章の3の5　関数ポインタの配列

関数ポインタを配列に入れると、ディスパッチ表が作れます。

```c
#include <limits.h>
#include <stddef.h>

typedef int (*binary_int_op)(int, int);

static binary_int_op operations[UCHAR_MAX + 1] = {0};

void initialize_operations(void)
{
	operations[(unsigned char)'+'] = add;
	operations[(unsigned char)'-'] = subtract;
}
```

評価関数は次のように書けます。

```c
int evaluate_from_table(char opcode, int left, int right, int *result)
{
	binary_int_op operation = operations[(unsigned char)opcode];

	if (operation == NULL || result == NULL) {
		return -1;
	}

	*result = operation(left, right);
	return 0;
}
```

ここでは 128 固定ではなく UCHAR_MAX + 1 を使っています。
これは、char が符号付きか符号なしかが処理系依存であることを踏まえた書き方です。
添字に char をそのまま使うと負値になりうるため、unsigned char へ変換してから配列添字にする方が安全です。

#### 3章の3の6　比較は等値・不等値にとどめる

関数ポインタ同士は、同じ関数を指しているかどうかを比較できます。

```c
binary_int_op operation = add;

if (operation == add) {
	puts("operation は add を指している");
}
```

実用上よく使うのは、このような等値比較です。
大小比較のような順序づけを期待してはいけません。
あくまで「同じか違うか」を見る道具として使います。

#### 3章の3の7　関数ポインタのキャストは最後の手段

古い資料では、異なる型の関数ポインタ間でキャストする例が出てくることがあります。
しかし、現代的にはこれは原則として避けるべきです。

理由は単純で、関数の実体が期待する引数や戻り値の型と、呼び出し側が想定している型がずれると、結果が未定義または処理系依存になりやすいからです。
呼び出し規約、引数の配置、戻り値の受け取り方が一致している保証が崩れます。

特に重要なのは、データポインタと関数ポインタを相互変換して汎用化しようとしてはいけない、という点です。
void * はオブジェクトポインタの受け皿としては使えますが、関数ポインタ用の共通入れ物ではありません。

```c
void *object_pointer;
/* object_pointer = add; */
```

上のような発想は避けるべきです。
関数ポインタを設計に取り込むなら、正しいシグネチャを typedef で与え、その型のまま受け渡す方が安全です。

### 3章の4　まとめ

この章では、関数とポインタの関係を、スタック、寿命、引数、戻り値、関数ポインタの観点から整理しました。

まず重要なのは、Cの引数は常に値渡しである、という原則です。
そのうえで、値として渡されたものがポインタであれば、関数の中から呼び出し側オブジェクトへ到達できます。
このため、呼び出し側のデータを変更したい時は、そのオブジェクトを指すポインタを渡します。
逆に、ポインタ変数そのものを差し替えたい時は、二重ポインタが必要になります。

次に、関数からポインタを返す時は、寿命と所有権を明確にしなければなりません。
malloc() で確保した領域を返すのは一般的ですが、その解放責任は通常呼び出し側にあります。
一方、局所変数や局所配列のアドレスを返すのは誤りです。
正しく動いて見えることがあっても、仕様上は未定義動作です。

さらに、関数ポインタを使うと、処理内容そのものを値として扱えます。
これにより、コールバック、演算選択、ディスパッチ表、状態に応じた処理切り替えなどが実現できます。
ただし、構文だけ覚えるのでは不十分で、どの関数型を期待しているのか、NULL の可能性をどう扱うのか、キャストを本当に避けられないのか、といった設計上の判断が必要です。

UmuOSのような低レイヤ寄りの実装では、ポインタと関数の関係は避けて通れません。
データの寿命、所有権、呼び出し規約、実行経路の切り替えを一つひとつ整理できるようになると、コードの読みやすさもデバッグのしやすさも大きく向上すると思います。

特に UmuOS を読む、あるいは自分で機能を足す時は、次の観点を常に意識すると実装の意図を追いやすくなります。

```text
このポインタは何を指しているか:
	バッファ、構造体、デバイス状態、テーブル要素のどれか

その参照先はいつまで有効か:
	局所変数なのか、静的領域なのか、動的確保領域なのか

その関数は何を書き換える契約か:
	読み取り専用なのか、呼び出し側データを更新するのか、ポインタ自体を差し替えるのか

関数ポインタやテーブルで実行経路が切り替わっていないか:
	初期化表、ディスパッチ表、ハンドラ登録のどこで流れが決まるか
```

UmuOS や comm のような低レイヤ実装では、不具合の多くが「どのデータを誰が所有し、どの時点で寿命が切れ、どの関数がどこまで書き換えるか」を見誤ることで起きます。
したがって、ポインタを単なる文法としてではなく、実行中のデータ経路そのものとして追跡する習慣を持つことが、読解にもデバッグにも直接効いてきます。