---
title: "何も知らない人向けC#.net１（初級）　基礎～ループ編"
---

# 何も知らない人向けC#.net 1 — 基礎からループまで。

このシリーズは最終的に、読み手が C# のコードを見ても身構えず、落ち着いて読めるようになることを目標にします。  
ただし **第1巻は何も知らないところから始める** ことを大事にし、できる限りやさしく・丁寧に説明します。

この教材は、何も知らない人に教える前提で書いています。  
「知らない言葉が出てきた瞬間に止まる」を防ぐため、言葉はその場で説明します。

## 第一章でやること：

- C# の「最小の形」を知る
- 文字や数を画面に表示できる
- 変数（値を覚えておく名前付きの箱）がわかる
- キーボード入力の入口を踏む

前提：

- OS：Windows 11
- 開発環境：VS Code
- 対象：.NET Framework 4.8.1 のコンソールアプリ

---

## まずは慣れよう

ここは「理屈より、まず形に慣れる」章です。  
最初は **写す → 実行する → 画面を見る** を繰り返して、C# の雰囲気をつかみます。

### 整数の加算の結果を表示

まずは「足し算して表示」だけをやります。

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        Console.WriteLine(1 + 2);
        Console.WriteLine(10 + 30);
    }
}
```

出力イメージ：

```text
3
40
```

ポイント：

- `1 + 2` は「計算して値を出す書き方」です
- `Console.WriteLine` は「画面に出して改行する道具」です
- `;` は「この1行はここで終わり」の記号です

### プログラムと実行

#### プログラムとは（超ざっくり）

自分が書いた `.cs` の文章を、Windows が動かせる形にして実行します。  
この「動かせる形にする作業」を **コンパイル** と呼びます。

ここでは難しく考えず、次の理解で大丈夫です。

- 人が読める形で書く
- ビルドの道具が動かせる形にしてくれる
- 実行して結果を見る

#### 実行手順（Windows 11 / .NET Framework 4.8.1 / VS Code）

ここでは VS Code を使う前提で進めます。

1. VS Code を開く
2. .NET Framework 4.8.1 のコンソールアプリのフォルダを開く
3. `Program.cs` を、この教材のコードに置き換える
4. ビルドして実行する

よくあるつまずき：

- .NET Framework 4.8.1 のプロジェクトではなく、別の種類のプロジェクトを開いている
- フレームワークの版が `4.8.1` になっていない
- `Main` の外にコードを書いてしまう

### コメント

コメントは「自分用のメモ」です。実行には使われません。

- 1行コメント：`// ...`
- 複数行コメント：`/* ... */`

例：

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        // これは1行コメントです
        /*
           これは複数行コメントです
        */
        Console.WriteLine("コメントがあっても実行できます");
    }
}
```

出力イメージ：

```text
コメントがあっても実行できます
```

コメントのコツ：

- 「何をしているか」より「なぜそうするか」を書くと役に立ちます

### プログラムの決まり文句

最小の C# プログラムの“型”はこれです。

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        Console.WriteLine("ここから始まります");
    }
}
```

出力イメージ：

```text
ここから始まります
```

順番に意味を押さえます。

#### `using System;`

`Console.WriteLine` などの便利な道具を使うための準備です。

ここでの理解としては：

- 「画面に出す道具を使えるようにする」

で大丈夫です。

#### `class Program`

`class` は「関係のあるものをまとめる入れ物」です。  
最初は深く考えなくて大丈夫です。

- `Program` という名前の箱を用意している
- その中に、実行の入口を入れる

くらいの理解で十分です。

#### `static void Main(string[] args)`

- `Main` はプログラムの入口です
- 実行すると、まずここから始まります
- `args` は実行時に渡された文字ですが、第1巻では使いません

#### `{ ... }`（ブロック）

波かっこ `{` `}` の中が、まとまりです。  
`Main` の中に「実際にやること」を書きます。

### Console.WriteLine メソッド

**メソッド** という言葉は「やってくれる仕事の名前」だと思ってください。  
`Console.WriteLine` は「画面に文字や数を出して、最後に改行する仕事」です。

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        Console.WriteLine("ただの文字");
        Console.WriteLine(123);
    }
}
```

出力イメージ：

```text
ただの文字
123
```

### 文

**文** は「処理の1単位」です。多くの文はセミコロン `;` で終わります。

例：

- `Console.WriteLine("hi");` ← 文
- `int number = 10;` ← 文

よくあるミス：

- `;` を忘れる

### 整数

整数は、小数点のない数です。C# ではまず `int` を使います。

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        int a = 10;
        int b = -3;

        Console.WriteLine(a);
        Console.WriteLine(b);
    }
}
```

出力イメージ：

```text
10
-3
```

### 文字に値を埋め込んで表示

C# では、文字の中に値を差し込んで表示できます。  
この書き方を **文字列補間** と呼びますが、ここでは「文字の中に値をはめこむ書き方」と考えれば大丈夫です。

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        int i = 42;
        double x = 3.14;
        char c = 'A';

        Console.WriteLine($"i={i}");
        Console.WriteLine($"x={x}");
        Console.WriteLine($"c={c}");
    }
}
```

出力イメージ：

```text
i=42
x=3.14
c=A
```

重要：

- `$"..."` の中の `{...}` に、表示したい値を書きます
- `char` は1文字、`string` は文字の並びです

### 記号文字の読み方

プログラムを読むための最低限の「読み方」をまとめます。

- `;` セミコロン
- `,` カンマ
- `()` 丸かっこ
- `{}` 波かっこ
- `[]` 角かっこ
- `"` ダブルクォート
- `'` シングルクォート
- `\` バックスラッシュ
- `$` ドル

### 書式化を行わない表示

ただ文字を出したいだけなら、こう書けます。

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        Console.WriteLine("Hello");
    }
}
```

出力イメージ：

```text
Hello
```

注意：

- `Console.WriteLine` は最後に改行を付けます
- 改行を付けたくないときは `Console.Write` を使います

### 文字列リテラル

`"Hello"` のようにダブルクォートで囲んだものを **文字列リテラル** と呼びます。  
難しく見えますが、「プログラムの中に直接書いた文字そのもの」です。

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        Console.WriteLine("Hello, world!");
    }
}
```

出力イメージ：

```text
Hello, world!
```

ここで重要な区別：

- `'A'`：1文字
- `"A"`：文字の並び

### 拡張表記

ここでは「難しい理屈」を後回しにして、現場でよく見る表記だけ先に紹介します。

#### エスケープシーケンス

文字の中で特別な意味を持つ、`\` から始まる書き方です。

- `\n`：改行
- `\t`：横に間を空ける
- `\\`：バックスラッシュそのもの
- `\"`：ダブルクォートそのもの

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        Console.WriteLine("1行目\n2行目");
        Console.WriteLine("A\tB");
    }
}
```

出力イメージ：

```text
1行目
2行目
A    B
```

#### 数値リテラルの表記（見かけるもの）

- `123`：ふつうの10進数
- `0x7B`：16進数
- `1.5`：小数

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        Console.WriteLine(123);
        Console.WriteLine(0x7B);
        Console.WriteLine(1.5);
    }
}
```

出力イメージ：

```text
123
123
1.5
```

---

## 変数

ここから「値を覚えておく箱」を扱います。

### 変数と宣言

変数は「値を入れる箱」で、使う前に「どんな値を入れる箱か」を先に書きます。  
これを **宣言** と呼びます。

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        int a;
        double x;

        a = 5;
        x = 2.5;

        Console.WriteLine(a);
        Console.WriteLine(x);
    }
}
```

出力イメージ：

```text
5
2.5
```

### 代入

`=` は「左右が同じ」という意味ではなく、**右の値を左の箱に入れる** という意味です。

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        int a;
        a = 10;
        a = a + 1;

        Console.WriteLine(a);
    }
}
```

出力イメージ：

```text
11
```

### 初期化

宣言と同時に値を入れるのが **初期化** です。

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        int a = 10;
        string name = "Sakura";

        Console.WriteLine(a);
        Console.WriteLine(name);
    }
}
```

出力イメージ：

```text
10
Sakura
```

なぜ初期化が大事か：

- 最初の値が決まっていると、考えやすい
- 「何が入っているかわからない」を防げる

---

## 読み込みと表示

この章では、

- キーボードから文字を読む
- その文字を数に変える
- 計算して表示する

をやります。

### Console.ReadLine メソッド

`Console.ReadLine` は、キーボードから1行読む道具です。  
読む結果は、まず文字として受け取ります。

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        Console.Write("名前を入力してください: ");
        string name = Console.ReadLine();

        Console.WriteLine($"こんにちは、{name} さん");
    }
}
```

出力イメージ：

```text
名前を入力してください: さくら
こんにちは、さくら さん
```

#### 文字を数に変えるって何？

キーボードから入るものは、最初は文字です。  
たとえば `25` と入力しても、受け取った直後は「2 と 5 という文字の並び」です。

足し算に使うには、数に変える必要があります。

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        Console.Write("年齢を入力してください: ");
        string text = Console.ReadLine();
        int age = int.Parse(text);

        Console.WriteLine($"10年後は {age + 10} 歳です");
    }
}
```

出力イメージ：

```text
年齢を入力してください: 20
10年後は 30 歳です
```

#### 入力の注意（最初に知っておくと事故が減る）

数字のつもりで文字を入れると、変換に失敗します。  
最初から安全に書くなら `int.TryParse` を使います。

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        Console.Write("数字を入力してください: ");
        string text = Console.ReadLine();

        int number;
        if (int.TryParse(text, out number))
        {
            Console.WriteLine($"入力できた数は {number} です");
        }
        else
        {
            Console.WriteLine("数字として読めませんでした");
        }
    }
}
```

出力イメージ：

```text
数字を入力してください: 12
入力できた数は 12 です
```

### 四則演算

四則演算は次の記号です。

- `+` 足し算
- `-` 引き算
- `*` 掛け算
- `/` 割り算

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        Console.Write("a を入力してください: ");
        int a = int.Parse(Console.ReadLine());

        Console.Write("b を入力してください: ");
        int b = int.Parse(Console.ReadLine());

        Console.WriteLine($"a + b = {a + b}");
        Console.WriteLine($"a - b = {a - b}");
        Console.WriteLine($"a * b = {a * b}");

        if (b != 0)
        {
            Console.WriteLine($"a / b = {a / b}");
        }
        else
        {
            Console.WriteLine("a / b は b が 0 なので計算できません");
        }
    }
}
```

出力イメージ：

```text
a を入力してください: 10
b を入力してください: 3
a + b = 13
a - b = 7
a * b = 30
a / b = 3
```

ここで押さえるポイント：

- `int` 同士の `/` は小数点以下が切り捨てられます
- 0 で割るのは危険なので、先に確かめます

### Console.Write と Console.WriteLine

`Console.WriteLine` は最後に改行します。  
`Console.Write` は改行しません。

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        Console.Write("おはよう");
        Console.Write("ございます");
        Console.WriteLine();
        Console.WriteLine("次の行です");
    }
}
```

出力イメージ：

```text
おはようございます
次の行です
```

---

## まとめ

- C# プログラムの最小構造は `using` / `class` / `Main` / `{}`
- `Console.WriteLine` は画面に出して改行する
- 変数は、宣言してから使う
- 入力は `Console.ReadLine` で受け取る
- 数に変えるときは `int.Parse` または `int.TryParse` を使う
- 四則演算と、0 で割れないことを押さえる

---

## 第二章でやること：

- 計算に使う記号の意味をそろえる
- 計算の順番を、かっこで思いどおりに固定できる
- `%` で余りを出せる
- 画面に複数の値をまとめて表示できる
- 小数の計算と整数の計算の違いを押さえる

---

## 演算と型

この章は「計算ができる」だけでなく、ソースコードを読むときに必ず出会う

- 計算の順番
- 値の種類の違い
- 混ぜて計算したときの動き

を、自分の手で確認できる形にします。

### 演算

#### 演算子とオペランド

- **演算子**：`+` `-` `*` `/` `%` `=` のような記号
- **オペランド**：その記号の相手になる値

例：`a + b` では、`+` が記号、`a` と `b` が相手の値です。

#### 乗除演算子と加減演算子（優先順位）

`*` と `/` は、`+` と `-` より先に計算されます。

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        Console.WriteLine($"1 + 2 * 3 = {1 + 2 * 3}");
        Console.WriteLine($"(1 + 2) * 3 = {(1 + 2) * 3}");
    }
}
```

出力イメージ：

```text
1 + 2 * 3 = 7
(1 + 2) * 3 = 9
```

#### 最下位の数字を求める（`%` と `/`）

`%` は「割った余り」を出します。

- `n % 10`：一の位を取り出す
- `n / 10`：一の位を落とす

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        int n = 1234;

        Console.WriteLine($"n = {n}");
        Console.WriteLine($"n % 10 = {n % 10}");
        Console.WriteLine($"n / 10 = {n / 10}");
    }
}
```

出力イメージ：

```text
n = 1234
n % 10 = 4
n / 10 = 123
```

#### 複数の値を一度に表示する

文字の中に複数の値を埋め込めます。

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        int left = 12;
        int right = 34;

        Console.WriteLine($"left={left} right={right} sum={left + right}");
    }
}
```

出力イメージ：

```text
left=12 right=34 sum=46
```

#### 単項の算術演算子（`+x` と `-x`）

`+` と `-` は、1つの値に対しても使えます。

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        int value = 10;

        Console.WriteLine($"value={value}");
        Console.WriteLine($"-value={-value}");
        Console.WriteLine($"+value={+value}");
    }
}
```

出力イメージ：

```text
value=10
-value=-10
+value=10
```

#### 代入演算子（`=` と `+=` など）

代入には短く書く形があります。

- `x += 3` は `x = x + 3`
- `x -= 2` は `x = x - 2`

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        int x = 10;

        x += 3;
        x *= 2;
        x -= 4;

        Console.WriteLine($"x={x}");
    }
}
```

出力イメージ：

```text
x=22
```

#### 式と代入式

`x = 10` は「代入するだけ」に見えますが、C# ではその結果の値も持ちます。

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        int x = 0;
        int y = 0;

        y = (x = 10);

        Console.WriteLine($"x={x} y={y}");
    }
}
```

出力イメージ：

```text
x=10 y=10
```

#### 式文

計算や呼び出しのあとに `;` を付けた1行が、実行される1単位になります。

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        int a = 1;
        int b = 2;

        a = a + b;
        Console.WriteLine($"a={a}");
    }
}
```

出力イメージ：

```text
a=3
```

### 型

#### 整数型と浮動小数点型

- `int`：整数
- `double`：小数を扱える型

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        int count = 3;
        double price = 1.5;

        Console.WriteLine(count);
        Console.WriteLine(price);
    }
}
```

出力イメージ：

```text
3
1.5
```

#### 型と変数

型は「その箱に何を入れるかの決まり」です。  
変数は「その決まりを持った箱」です。

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        int age = 20;
        string name = "さくら";

        Console.WriteLine($"{name} さんは {age} 歳です");
    }
}
```

出力イメージ：

```text
さくら さんは 20 歳です
```

#### 型と定数（リテラル）

プログラムの中に直接書いた値にも種類があります。

- `123`：整数
- `3.14`：小数
- `'A'`：1文字
- `"Hello"`：文字の並び

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        Console.WriteLine(123);
        Console.WriteLine(3.14);
        Console.WriteLine('A');
        Console.WriteLine("Hello");
    }
}
```

出力イメージ：

```text
123
3.14
A
Hello
```

#### double型の演算（整数除算との違い）

整数同士の割り算は切り捨てですが、小数の型を使うと小数として計算できます。

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        int a = 1;
        int b = 2;

        Console.WriteLine($"a / b (int) = {a / b}");
        Console.WriteLine($"a / b (double) = {(double)a / b}");
    }
}
```

出力イメージ：

```text
a / b (int) = 0
a / b (double) = 0.5
```

#### 型と演算（混ぜるとどうなるか）

整数と小数を混ぜると、小数として計算されます。

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        int count = 3;
        double price = 1.5;

        Console.WriteLine($"count * price = {count * price}");
    }
}
```

出力イメージ：

```text
count * price = 4.5
```

#### キャスト

**キャスト** は「今だけ別の種類として見る」書き方です。

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        double x = 3.9;
        int y = (int)x;

        Console.WriteLine($"x={x}");
        Console.WriteLine($"y={y}");
    }
}
```

出力イメージ：

```text
x=3.9
y=3
```

注意：

- 小数を整数に変えると、小数点以下は切り捨てられます

#### 表示の書き方（型に合った表示）

C# では、最初は文字列補間を使えば十分です。

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        int score = 80;
        double average = 72.5;
        bool passed = true;

        Console.WriteLine($"score={score}");
        Console.WriteLine($"average={average}");
        Console.WriteLine($"passed={passed}");
    }
}
```

出力イメージ：

```text
score=80
average=72.5
passed=True
```

---

### まとめ

- `*` `/` は `+` `-` より先に計算される
- `%` は余りを出す
- 文字の中に複数の値を埋め込める
- `int` と `double` は動きが違う
- 小数の計算をしたいときは、片方を `double` にする
- キャストは「今だけ別の種類として見る」書き方

---

## 第三章でやること：

- `if` 文の基本形を理解する
- 条件によって、やることを分けられる
- 大きい小さい、同じ違う、どちらも満たす、どちらかを満たすを表現できる
- `switch` で選択肢を分けられる

---

## プログラムの流れの分岐

ここまでのプログラムは、上から下へ一直線に進みました。  
この章では、条件によって処理を分ける「分岐」を扱います。

重要：C# では、条件の答えは **true か false** の2つです。  
これは「はい」か「いいえ」だと思えば大丈夫です。

### if文

#### if文（最小の形）

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        int x = 3;
        Console.WriteLine($"x={x}");

        if (x > 0)
        {
            Console.WriteLine("x は正です");
        }

        Console.WriteLine("ここは常に実行されます");
    }
}
```

出力イメージ：

```text
x=3
x は正です
ここは常に実行されます
```

#### 複合文（ブロック）

`{ ... }` を使うと、複数の行をまとめて「条件に合ったときだけ実行」できます。

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        int x = 5;

        if (x > 0)
        {
            Console.WriteLine("A");
            Console.WriteLine("B");
        }
    }
}
```

出力イメージ：

```text
A
B
```

#### 奇数の判定（剰余の利用）

2 で割った余りが 1 なら奇数です。

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        int n = 7;

        if (n % 2 != 0)
        {
            Console.WriteLine("奇数です");
        }
    }
}
```

出力イメージ：

```text
奇数です
```

#### else付きのif文（どちらか一方を必ず実行）

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        int n = 8;

        if (n % 2 == 0)
        {
            Console.WriteLine("偶数です");
        }
        else
        {
            Console.WriteLine("奇数です");
        }
    }
}
```

出力イメージ：

```text
偶数です
```

#### bool型（条件の答えを入れる箱）

条件の答えを入れる専用の箱が `bool` です。  
入るのは `true` と `false` だけです。

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        int x = 10;
        bool isPositive = x > 0;

        Console.WriteLine(isPositive);
    }
}
```

出力イメージ：

```text
True
```

#### if文の構造図（読み方の型）

`if (条件)` は、

- まず丸かっこの中を見る
- 答えが「はい」なら次のまとまりを実行する
- 答えが「いいえ」なら飛ばす

という順番で読むと混乱しにくいです。

#### 等価演算子（`==` と `!=`）

- `==` は「同じか」
- `!=` は「同じではないか」

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        int score = 80;

        if (score == 80)
        {
            Console.WriteLine("score は 80 です");
        }

        if (score != 100)
        {
            Console.WriteLine("score は 100 ではありません");
        }
    }
}
```

出力イメージ：

```text
score は 80 です
score は 100 ではありません
```

注意：

- `=` は入れる
- `==` は比べる

#### 剰余の判定（割り切れるかどうか）

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        int n = 12;

        if (n % 3 == 0)
        {
            Console.WriteLine("3 で割り切れます");
        }
    }
}
```

出力イメージ：

```text
3 で割り切れます
```

#### 関係演算子（大小を比べる）

- `<` より小さい
- `<=` 以下
- `>` より大きい
- `>=` 以上

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        int age = 20;

        if (age >= 18)
        {
            Console.WriteLine("18 歳以上です");
        }
    }
}
```

出力イメージ：

```text
18 歳以上です
```

#### 入れ子になったif文（ネスト）

条件の中に、さらに条件を書けます。

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        int score = 85;

        if (score >= 60)
        {
            if (score >= 80)
            {
                Console.WriteLine("合格で、しかも高得点です");
            }
        }
    }
}
```

出力イメージ：

```text
合格で、しかも高得点です
```

#### 評価（条件式がいつ調べられるか）

`if` に出てくる条件は、その場所に来たときに調べられます。  
先に全部調べるのではなく、その場で確認します。

#### 2値の最大値を求める

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        int a = 15;
        int b = 9;
        int max;

        if (a > b)
        {
            max = a;
        }
        else
        {
            max = b;
        }

        Console.WriteLine($"最大値は {max} です");
    }
}
```

出力イメージ：

```text
最大値は 15 です
```

#### 3値の最大値を求める（読みやすさ優先）

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        int a = 15;
        int b = 22;
        int c = 18;
        int max = a;

        if (b > max)
        {
            max = b;
        }

        if (c > max)
        {
            max = c;
        }

        Console.WriteLine($"最大値は {max} です");
    }
}
```

出力イメージ：

```text
最大値は 22 です
```

#### 条件演算子（`?:`）

短い「もしそうならこっち、そうでなければあっち」という書き方です。

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        int score = 75;
        string result = score >= 60 ? "合格" : "不合格";

        Console.WriteLine(result);
    }
}
```

出力イメージ：

```text
合格
```

#### 論理演算子（`&&` `||` `!`）

- `&&` は「両方とも満たす」
- `||` は「どちらかを満たす」
- `!` は「逆にする」

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        int age = 20;
        bool hasTicket = true;

        if (age >= 18 && hasTicket)
        {
            Console.WriteLine("入場できます");
        }
    }
}
```

出力イメージ：

```text
入場できます
```

### switch文

#### switch文とbreak文

選択肢がいくつもあるときは `switch` が読みやすくなることがあります。

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        int menu = 2;

        switch (menu)
        {
            case 1:
                Console.WriteLine("コーヒー");
                break;
            case 2:
                Console.WriteLine("紅茶");
                break;
            default:
                Console.WriteLine("その他");
                break;
        }
    }
}
```

出力イメージ：

```text
紅茶
```

#### switch文とif文（書ける範囲の違い）

- 数字や文字で分かれるなら `switch` が読みやすい
- 大小比較や複雑な条件なら `if` が向いています

---

### まとめ

- C# の条件は `true` と `false`
- `if` は条件で分ける基本
- `else` を使うと、どちらか一方を必ず実行できる
- `==` と `=` は意味がまったく違う
- `&&` `||` `!` で条件を組み合わせられる
- 選択肢が多いときは `switch` が読みやすい

---

## 第四章でやること：

- 同じ処理を何回も繰り返せるようになる
- `do` `while` `for` の違いをつかむ
- 途中でやめる、1回だけ飛ばす、を使い分ける
- 二重の繰り返しで表や図形を作る

---

## プログラムの流れの繰り返し

同じ形の処理を何度も書くのは大変です。  
繰り返しを使うと、短く、間違いも減らして書けます。

### do文（do ... while）

#### do文

`do` は「まず1回実行してから、続けるか調べる」書き方です。

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        int i = 1;

        do
        {
            Console.WriteLine(i);
            i++;
        }
        while (i <= 3);
    }
}
```

出力イメージ：

```text
1
2
3
```

#### 読み込む値を制限する（範囲外ならやり直す）

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        int number;

        do
        {
            Console.Write("1 から 5 の数字を入力してください: ");
        }
        while (!int.TryParse(Console.ReadLine(), out number) || number < 1 || number > 5);

        Console.WriteLine($"入力された数字は {number} です");
    }
}
```

出力イメージ：

```text
1 から 5 の数字を入力してください: 9
1 から 5 の数字を入力してください: 3
入力された数字は 3 です
```

#### 論理否定演算子とド・モルガンの法則

`!` は「逆にする」でした。  
条件が長いときは、「入りたい条件」を書くより「入りたくない条件をひっくり返す」ほうが読みやすいことがあります。

ここでは、まず次だけ押さえれば十分です。

- `!ok` は「ok ではない」
- 条件が長すぎたら、いったん変数に入れると読みやすい

#### 複数の整数値の合計と平均を求める

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        int sum = 0;
        int count = 0;
        int number = 1;

        do
        {
            sum += number;
            count++;
            number++;
        }
        while (number <= 5);

        double average = (double)sum / count;

        Console.WriteLine($"合計 = {sum}");
        Console.WriteLine($"平均 = {average}");
    }
}
```

出力イメージ：

```text
合計 = 15
平均 = 3
```

#### 複合代入演算子

繰り返しでは `+=` が特によく出ます。

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        int total = 0;

        total += 5;
        total += 8;

        Console.WriteLine(total);
    }
}
```

出力イメージ：

```text
13
```

#### 後置増分演算子と後置減分演算子（`i++` と `i--`）

`i++` は「使ったあとで 1 増やす」です。  
最初は「1 増やす短い書き方」と覚えれば大丈夫です。

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        int i = 3;
        i++;
        i--;

        Console.WriteLine(i);
    }
}
```

出力イメージ：

```text
3
```

### while文

#### while文

`while` は「先に続けるか調べて、よければ実行する」書き方です。

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        int i = 1;

        while (i <= 3)
        {
            Console.WriteLine(i);
            i++;
        }
    }
}
```

出力イメージ：

```text
1
2
3
```

#### 減分演算子を用いた手短な表現（`--`）

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        int count = 3;

        while (count > 0)
        {
            Console.WriteLine(count);
            count--;
        }
    }
}
```

出力イメージ：

```text
3
2
1
```

#### カウントアップ（`++`）

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        int number = 1;

        while (number <= 5)
        {
            Console.WriteLine(number);
            number++;
        }
    }
}
```

出力イメージ：

```text
1
2
3
4
5
```

#### 文字と Console.Write

1文字を表す型は `char` です。  
`Console.Write` を使うと、横に続けて出せます。

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        char ch = 'A';
        int i = 0;

        while (i < 5)
        {
            Console.Write(ch);
            i++;
        }

        Console.WriteLine();
    }
}
```

出力イメージ：

```text
AAAAA
```

#### do文とwhile文（条件をいつ見るか）

- `do` は、先に1回やる
- `while` は、先に条件を見る

この違いだけでもまず十分です。

#### 前置増分演算子と前置減分演算子（`++i` と `--i`）

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        int i = 1;
        Console.WriteLine(++i);
        Console.WriteLine(--i);
    }
}
```

出力イメージ：

```text
2
1
```

#### 整数値を逆順に表示

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        int n = 1234;

        while (n > 0)
        {
            Console.WriteLine(n % 10);
            n /= 10;
        }
    }
}
```

出力イメージ：

```text
4
3
2
1
```

#### break文とcontinue文

- `break` はその繰り返しを終わらせる
- `continue` はその1回だけ残りを飛ばして次へ行く

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        int i = 0;

        while (i < 5)
        {
            i++;

            if (i == 2)
            {
                continue;
            }

            if (i == 5)
            {
                break;
            }

            Console.WriteLine(i);
        }
    }
}
```

出力イメージ：

```text
1
3
4
```

### for文

#### for文

回数がはっきりしている繰り返しは `for` が読みやすいです。

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        for (int i = 1; i <= 3; i++)
        {
            Console.WriteLine(i);
        }
    }
}
```

出力イメージ：

```text
1
2
3
```

#### for文による一定回数の繰り返し

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        for (int i = 0; i < 5; i++)
        {
            Console.WriteLine("がんばりすぎず、1つずつ進めば大丈夫");
        }
    }
}
```

出力イメージ：

```text
がんばりすぎず、1つずつ進めば大丈夫
がんばりすぎず、1つずつ進めば大丈夫
がんばりすぎず、1つずつ進めば大丈夫
がんばりすぎず、1つずつ進めば大丈夫
がんばりすぎず、1つずつ進めば大丈夫
```

#### 偶数の列挙

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        for (int i = 0; i <= 10; i += 2)
        {
            Console.WriteLine(i);
        }
    }
}
```

出力イメージ：

```text
0
2
4
6
8
10
```

#### 約数の列挙

約数とは「割り切れる数」です。

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        int n = 12;

        for (int i = 1; i <= n; i++)
        {
            if (n % i == 0)
            {
                Console.WriteLine(i);
            }
        }
    }
}
```

出力イメージ：

```text
1
2
3
4
6
12
```

#### 式文と空文

`for` の丸かっこの中には、

- 最初にやること
- 続ける条件
- 1回ごとの更新

を並べます。  
何も書かない場所があるとき、それは「空にしている」だけです。

#### 繰り返し文（ループ本体）

`for` や `while` の直後のまとまりが、毎回くり返される本体です。  
まずは「波かっこの中が毎回動く」と覚えれば十分です。

### 多重ループ

#### 2重ループ

繰り返しの中に繰り返しを書くと、表や図形を作れます。

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        for (int row = 1; row <= 3; row++)
        {
            for (int col = 1; col <= 4; col++)
            {
                Console.Write("*");
            }

            Console.WriteLine();
        }
    }
}
```

出力イメージ：

```text
****
****
****
```

#### 図形の描画

```csharp
using System;

class Program
{
    static void Main(string[] args)
    {
        for (int row = 1; row <= 4; row++)
        {
            for (int col = 1; col <= row; col++)
            {
                Console.Write("#");
            }

            Console.WriteLine();
        }
    }
}
```

出力イメージ：

```text
#
##
###
####
```

#### 多重ループ（一般）

中の繰り返しが1回終わるたびに、外の繰り返しが少し進みます。  
最初は「外が行、内が列」と読むとわかりやすいです。

### プログラムの要素と書式

#### キーワード

言語の中で意味が決まっている語です。

- `if`
- `else`
- `while`
- `for`
- `class`
- `int`

#### 演算子

計算や比較に使う記号です。

- `+` `-` `*` `/`
- `%`
- `=` `==`
- `&&` `||` `!`

#### 識別子

自分で付ける名前です。

- `age`
- `sum`
- `count`
- `Program`

#### 区切り記号

区切りやまとまりを表す記号です。

- `;`
- `,`
- `(` `)`
- `{` `}`

#### 定数と文字列リテラル

- `123` は直接書いた数
- `"Hello"` は直接書いた文字の並び

#### 自由形式

C# は、改行や空白の入れ方に多少の自由があります。  
ただし、読みやすさのために形をそろえることが大事です。

#### インデント

行の先頭を少し下げて書くことです。  
どこまでが同じまとまりかが見やすくなります。

---

### まとめ

- `do` は先に1回動く
- `while` は先に条件を見る
- `for` は回数が見えやすい
- `break` は終わる、`continue` はその回だけ飛ばす
- 二重ループで表や図形を作れる
- 見やすいインデントは、理解の速さに直結する

---

## この巻の終わりに

ここまで読めたなら、読み手はもう「何もわからない状態」ではありません。  
少なくとも、

- 画面に出す
- 入力を受け取る
- 計算する
- 条件で分ける
- 繰り返す

までは、自分で少しずつ読める状態に入っています。

研修で一度遅れても、それは「向いていない」という意味ではありません。  
知らない言葉を1つずつなくして、短いコードを何本も自分で動かせば、理解は必ず追いつきます。

次は、配列、メソッド、文字列の扱いに進むと、実務のコードがさらに読みやすくなります。