---
title: "UmuOSの為のC言語（現代ポインタ）　1章　ポインタの復習"
---

# UmuOSの為のC言語（現代ポインタ）

このノートは、UmuOSを進化させるために必要となるC言語のポインタ理解を、現代のC17相当の視点で抽象化し、再構成することであります。
すなわち、UmuOSの実装・読解・デバッグへ還元するための実践ノートであります。
ポインタは、単なる文法項目ではなく、メモリ、配列、文字列、関数呼び出し、所有権、未定義動作と深く結びついています。
この構造を理解することは、UmuOSの設計力を高めることに直結すると思います。

## 1章　ポインタの復習

ポインタを正しく理解し、それを正しく使いこなせる能力は、Cプログラミングにおける初級者と上級者を分ける重要な境界になります。
C言語では、配列、文字列、関数引数、動的メモリ確保、データ構造、システムコールのバッファ操作など、多くの場面でポインタが深く関わります。

一方で、ポインタは長いあいだ C 学習者にとっての大きな壁でもありました。
理由は単純で、基本の考え方自体は「アドレスを保持する値」なのに、実際のコードでは `*`、`&`、配列との関係、関数呼び出し、`NULL`、寿命、
未定義動作などが一気に絡んでくるからです。

しかし、難しく考えすぎる必要はありません。
単純な例から始めて、メモリ上で何が起きているのか、どのオブジェクトがどの寿命を持ち、
どのポインタがそのオブジェクトを参照しているのかを整理すれば、かなり見通しが良くなります。

ポインタ理解の鍵は、Cプログラムにおいてメモリとオブジェクトがどのように扱われるかを理解することです。
ポインタそのものは、何らかのオブジェクトや関数を指し示す値にすぎません。
ただし、その「指している先」が有効なのか、どの型として扱うべきなのか、どこまでアクセスしてよいのかは、常に別途考えなければなりません。

この章では、ポインタとポインタ演算子の基本を確認しながら、それらがメモリとどのように関係するかを整理します。
最初に、ポインタ変数の宣言方法、アドレス演算子、間接演算子、`NULL` の考え方を扱います。
次に、現代的な C 環境でポインタの振る舞いに影響するデータモデルや、`size_t`、`intptr_t` のような型を見ていきます。
そのうえで、ポインタ演算や比較がどの範囲で意味を持つのかを理解する準備を整えます。

このノートは、初学者にも読める形を意識しつつ、古い説明をそのまま残さず、現在の Linux と C17 相当の感覚へ寄せて説明します。
特に重要なのは、「たまたま動くコード」と「言語仕様として正しいコード」は違う、という視点です。

### 1章の1　ポインタとメモリ

Cプログラムが動くとき、データは性質の違う複数の領域に配置されます。
ポインタを理解するためには、まず「データがどこに置かれ、どれくらいのあいだ生きているのか」を掴む必要があります。
まず次の実装イメージから入ると分かりやすいです。

```text
スタック:
	通常のローカル変数が置かれることが多い領域
	関数呼び出しに応じて積まれ、処理が戻ると外れる

ヒープ:
	malloc() などで動的に確保した領域
	必要になった時に確保し、free() で解放する

静的領域:
	大域変数（グローバル変数）や static 変数が置かれることが多い領域
	プログラム開始から終了まで存在する

コード領域:
	関数本体や読み取り専用データなどが置かれることがある領域
	関数ポインタを考えるときに意識する
```

ただし、これは理解のための代表的な実装イメージです。
C言語の仕様そのものは、まず「スタック」「ヒープ」という言葉ではなく、オブジェクトの記憶域期間とスコープの側から整理すると混乱しにくいです。

そこで、このノートでは実装イメージを頭に置きつつ、言語仕様としては次のように考えます。

```text
静的記憶域期間:
	大域変数（グローバル変数）、ファイルスコープの static 変数、関数内 static 変数など
	プログラム開始から終了まで存在する

自動記憶域期間:
	通常のローカル変数
	ブロックへ入ると確保され、ブロックを抜けると寿命が終わる

動的確保領域:
	malloc() などで確保した領域
	free() するまで、またはプログラム終了まで存在する

コード領域:
	関数本体や読み取り専用データなど
	実装依存だが、関数ポインタを考えるときに意識することがある
```

厳密には、スレッドローカル記憶域期間もあります。
ただ、この章ではまず「自動変数はスタック上にあることが多い」「動的確保領域はヒープとして扱うことが多い」
「大域変数（グローバル変数）や static 変数は静的領域に置かれることが多い」という感覚を持ち、そのうえで記憶域期間と関数コードの関係を
押さえれば十分です。

#### 1章の1の1　記憶域期間とスコープを分けて考える

ここで混同しやすいのが、スコープと寿命です。

```text
スコープ:
	ソースコード上で、その名前を使える範囲

寿命:
	そのオブジェクトが実際に存在している期間
```

たとえば関数内の `static` 変数は、その関数の中からしか見えませんが、寿命はプログラム全体にわたります。
逆に自動変数は、そのブロックの中で見えていても、ブロックを抜けた時点で寿命が終わります。

この違いが重要なのは、ポインタが「名前」ではなく「実体の場所」を追いかけるからです。
名前が見えなくてもオブジェクトが生きていればポインタ経由で使えることがありますし、名前が見えていた履歴があっても
寿命が終わったオブジェクトを指すポインタは使えません。

#### 1章の1の2　ポインタは何を保持しているのか

変数、配列要素、動的確保した領域、構造体、関数などには、それぞれ参照可能な位置があります。
ポインタは、その位置を指し示す値を保持します。

よく「ポインタはアドレスを格納する変数」と説明されますが、実際には次の点を必ず一緒に見る必要があります。

```text
どの型を指す想定か:
	int を指すのか、char を指すのか、struct を指すのか

その先は有効か:
	まだ寿命が残っているか

どこまでアクセス可能か:
	1 要素だけか、配列全体か、動的確保領域全体か
```

つまり、ポインタの中身はアドレス風の値であっても、正しく使えるかどうかは型と寿命と境界に依存します。

#### 1章の1の3　ポインタを学ぶ利点

ポインタを使いこなせるようになると、C の表現力は大きく広がります。

```text
効率的なデータ受け渡し:
	大きな構造体を丸ごとコピーせずに参照できる

柔軟なデータ構造:
	連結リスト、木構造、キュー、ハッシュ表などを実装しやすい

動的メモリ管理:
	必要な時に必要な大きさの領域を確保できる

低レイヤ操作:
	バッファ、I/O、システムコール、メモリマップ領域などを扱いやすい
```

ただし、「ポインタだから速い」と単純化しすぎるのも危険です。
実行性能は、データ配置、キャッシュ局所性、不要コピーの有無、分岐、アラインメントなど複数の要因で決まります。
現代の CPU では、配列による連続アクセスの方が、ポインタをたどる連結構造より高速になることも珍しくありません。

#### 1章の1の4　ポインタの不都合な面

ポインタは強力ですが、誤ると深刻な不具合を生みます。

```text
領域外アクセス:
	配列や確保領域の範囲を越えて読む、または書く

ダングリングポインタ:
	寿命が終わったオブジェクトを指し続ける

use-after-free:
	free() 済み領域へアクセスする

未初期化ポインタ:
	どこを指しているか分からない値を使う

NULL の間接参照:
	何も指していないポインタを参照する
```

こうした問題は、しばしば「その場では動く」のが厄介です。
しかし仕様上は未定義動作であり、最適化や環境差によって突然壊れます。
このノートでは、「偶然動いた」ではなく「仕様上も安全に説明できる」を基準に整理します。

#### 1章の1の5　処理系定義、未規定、未定義

ポインタの話をしていると、言語仕様がどこまで保証しているかが非常に重要になります。

```text
処理系定義:
	実装ごとに振る舞いは異なるが、文書化されるべきもの

未規定:
	複数の正しい振る舞いがあり、どれになるかは規定されないもの

未定義:
	何が起きてもよく、プログラム側が期待してはいけないもの
```

たとえば、`free()` した後のポインタを間接参照するのは未定義動作です。
また、`malloc(0)` の扱いのように、実装ごとの差が現れる箇所もあります。

この違いを意識しておくと、「どの環境でも通じる説明」と「特定環境ではたまたまそう見える説明」を区別しやすくなります。

### 1章の1の6　ポインタ変数の宣言

ポインタ変数は、基本的には「何を指すポインタなのか」を型で表します。

```c
int num;
int *pi;
char *text;
```

上の `pi` は `int` を指すポインタ、`text` は `char` を指すポインタです。

アスタリスクの空白は次のどれでも文法上は同じです。

```c
int *pi;
int* pi;
int * pi;
int*pi;
```

ただし、可読性のためには 1 行に 1 変数で宣言する方が安全です。

```c
int *pi;
int *pj;
```

特に次のような宣言は誤読しやすいです。

```c
int *pi, pj;
```

この場合、`pi` は `int *` ですが、`pj` は単なる `int` です。
`*` は型全体ではなく、個々の宣言子に結びつくと覚える方が事故が減ります。

#### 1章の1の7　初期化されていないポインタ

自動変数として宣言したポインタは、明示的に初期化しなければ不定値を持ちます。

```c
int *pi;   /* まだ使ってはいけない */
```

この状態の `pi` を間接参照すると未定義動作です。
昔は「ゴミが入っている」と説明されることが多いですが、現代的には「不定値であり、使ってはいけない」と捉える方が正確です。

安全策としては、すぐ有効なアドレスを入れるか、少なくとも `NULL` で初期化します。

```c
int num = 0;
int *pi = &num;

char *buffer = NULL;
```

#### 1章の1の8　ポインタ宣言の読み方

複雑な宣言を読むときは、変数名から外側へ広げて読むと分かりやすいです。

```c
const int *pci;
```

これは次のように読むと整理できます。

```text
pci は変数である
pci はポインタである
pci は int を指すポインタである
pci は const int を指すポインタである
```

つまり、`pci` 経由では指している `int` を変更してはいけません。
ただし `pci` 自身の値、つまりどこを指すかは別問題です。ここは後で `const` とポインタの節で詳しく扱います。

#### 1章の1の9　アドレス演算子 `&`

アドレス演算子 `&` は、オブジェクトのアドレスを取得します。

```c
int num = 0;
int *pi = &num;
```

このコードでは、`&num` が `num` のアドレスであり、それが `pi` に入ります。

```c
int num = 0;
int *pi = &num;

printf("num=%d\n", num);
printf("pi=%p\n", (void *)pi);
```

逆に、`int` の値をそのままポインタへ代入するのは誤りです。

```c
int num = 0;
int *pi;

pi = &num;   /* 正しい */
/* pi = num;  誤り */
```

整数からポインタへのキャストは文法上書けることがありますが、妥当なアドレスを意味するとは限りません。
通常のアプリケーションコードでは、正当な理由のない整数とポインタの相互変換は避けるべきです。

#### 1章の1の10　ポインタ値の表示

ポインタを表示するときは、printf() の %p を使います。
C標準の意味では、対応する引数は void * なので、移植性を意識するなら (void *) へキャストして渡すのが安全です。
ただし、実際のコード例ではキャストを省略した書き方もよく見かけます。

```c
#include <stdio.h>

int main(void)
{
	int num = 0;
	int *pi = &num;

	printf("address of num: %p, value=%d\n", (void *)&num, num);
	printf("address of pi : %p, value=%p\n", (void *)&pi, (void *)pi);
	return 0;
}
```

古い説明では `%d` や `%x` でポインタを表示する例も見かけますが、現代のコードでは避けるべきです。
ポインタサイズは `int` と一致するとは限らず、64 ビット環境では特に危険です。

#### 1章の1の11　仮想記憶とアドレス

Linux のような現代 OS では、プログラムが見るアドレスは通常、仮想アドレスです。
つまり、`printf()` で表示される値は、物理メモリ上の生の位置ではなく、プロセスごとに管理された仮想アドレス空間の位置です。

このため、次の点を意識しておくと混乱が減ります。

```text
同じソースでも毎回アドレスが変わることがある:
	ASLR などの影響で自然なこと

表示されるアドレスを固定値として期待してはいけない:
    説明で示すアドレス値は、理解のための一例にすぎない

プログラマは通常、物理アドレスを気にしない:
	OS と MMU が仮想アドレス変換を管理する
```

UmuOS のような OS 実装を学ぶときは、この「プログラムが見るアドレス」と「実際の物理配置」を分けて考える視点が大切です。

#### 1章の1の12　間接演算子 `*`

間接演算子 `*` は、ポインタが指している先のオブジェクトへアクセスします。

```c
int num = 5;
int *pi = &num;

printf("%d\n", *pi);
```

このとき `*pi` は、`pi` が指す `int` そのものです。
したがって、読み出すだけでなく書き換えにも使えます。

```c
int num = 5;
int *pi = &num;

*pi = 200;
printf("%d\n", num);
```

この例では `*pi = 200;` によって `num` が 200 へ変わります。

#### 1章の1の13　関数へのポインタ

ポインタは、オブジェクトだけでなく関数を指すこともできます。

```c
void (*foo)(void);
```

これは、「`foo` は、引数を取らず `void` を返す関数へのポインタ」である、という意味です。
古い記法では `void (*foo)();` のような形も見かけますが、現代の C では引数なしを明確にするため `void` を書く方が適切です。

関数ポインタは、コールバック、イベント処理、ディスパッチテーブルなどで重要になるので、後の章で詳しく扱います。

#### 1章の1の14　`NULL` の概念

`NULL` は「何も指していないポインタ」を表すための仕組みです。
ここで混同しやすいものを分けておきます。

```text
null pointer value:
	どのオブジェクトや関数も指していないポインタ値

null pointer constant:
	0、または 0 を void * にキャストした定数式

NULL マクロ:
	実装が提供する null pointer constant 用のマクロ

'\0':
	文字値 0。文字列終端で使う NUL 文字

空文字列 "":
	長さ 0 の文字列。ただし末尾には '\0' がある
```

現代の C でも `NULL` は重要ですが、`NULL` の実体が常に `((void *)0)` だと決め打ちしてはいけません。
実装によっては `0` で定義されることもあります。

```c
int *pi = NULL;

if (pi == NULL) {
	puts("pi does not point anywhere");
}
```

また、ポインタ文脈では 0 も null pointer constant として使えます。

```c
int *pi = 0;
```

ただし、可読性のためにはポインタには `NULL` を使う方が意図が明確です。

#### 1章の1の15　初期化されていないポインタと `NULL` は別物

ここは非常に重要です。

```text
初期化されていないポインタ:
	何が入っているか分からない。不定値

NULL:
	何も指していないことが明示された値
```

どちらも間接参照してはいけませんが、意味はまったく違います。
`NULL` は「まだどこも指していないので、先に有効なアドレスを入れる必要がある」と明確に表せます。

#### 1章の1の16　ポインタは真偽値文脈で使える

C では、ポインタは条件式の中でそのまま使えます。

```c
if (pi) {
	/* NULL ではない */
} else {
	/* NULL である */
}
```

これは簡潔でよく使われます。
一方で、可読性を重視して `if (pi != NULL)` と書く流儀もあります。
どちらでもよいですが、チームやコードベースで統一するのが大切です。

#### 1章の1の17　`void *` は汎用オブジェクトポインタ

`void *` は、任意のオブジェクト型を指すための汎用ポインタです。

```c
void *pv;
int num = 10;

pv = &num;
```

C では、オブジェクトポインタは `void *` と相互変換できます。
ただし、`void *` のままでは間接参照できません。何を指しているか型情報がないからです。

```c
int num = 10;
void *pv = &num;

printf("%d\n", *(int *)pv);
```

ここで重要なのは、`void *` は関数ポインタ用ではない、という点です。
また、元の型と異なる型へ無理にキャストすると、別の問題を生むことがあります。

#### 1章の1の18　大域ポインタと static ポインタ

ファイルスコープのポインタや `static` 記憶域期間を持つポインタは、明示的に初期化しなければ 0 初期化されます。
ポインタ型では、それは null pointer value になります。

```c
int *global_pi;

void foo(void)
{
	static int *static_pi;
	(void)static_pi;
}
```

この `global_pi` と `static_pi` は、明示的に代入しない限り `NULL` 相当で始まります。
一方、通常の自動変数ポインタはそうではありません。

### 1章の2　ポインタ型とポインタの大きさ

現代的なプラットフォームでは、多くの場合、すべてのオブジェクトポインタは同じ大きさです。
たとえば `char *`、`int *`、`struct foo *` が同じサイズになることが多いです。

ただし、C言語標準が「すべてのポインタが必ず同じ大きさ」と保証しているわけではありません。
特に関数ポインタは、オブジェクトポインタと異なる表現やサイズを持つ可能性があります。

#### 1章の2の1　データモデル

32 ビット環境と 64 ビット環境では、`int`、`long`、ポインタの大きさが異なることがあります。
代表的なデータモデルとして、次の名前を見かけます。

```text
ILP32:
	int, long, pointer が 32 ビット

LP64:
	int は 32 ビット、long と pointer が 64 ビット

LLP64:
	int と long は 32 ビット、long long と pointer が 64 ビット
```

Linux の 64 ビット環境では LP64 が一般的です。
Windows の 64 ビット環境では LLP64 が一般的です。

この違いが重要になるのは、`long` とポインタの大きさが常に同じだと思い込んだコードが壊れるからです。
そのため、ポインタを数値として扱う必要がある場面では、専用の型を使うべきです。

#### 1章の2の2　`size_t`

`size_t` は、オブジェクトの大きさや配列長、バイト数を表すための符号なし整数型です。

```c
#include <stddef.h>
#include <stdio.h>

int main(void)
{
	size_t count = sizeof(int);
	printf("%zu\n", count);
	return 0;
}
```

`sizeof` の結果型は `size_t` です。
したがって、サイズ、長さ、要素数、バイト数を表す変数では `size_t` を使うのが基本です。

```text
配列の要素数
バッファの長さ
malloc() に渡す確保サイズ
strlen() の結果
```

表示には `%zu` を使います。
古いコードでは `%u` や `%lu` も見かけますが、現代の C では `%zu` を使う方が自然です。

`size_t` は符号なしなので、負の値を入れる設計は避けるべきです。
減算結果が負になる可能性があるなら、別の型を検討する必要があります。

#### 1章の2の3　ポインタに `sizeof` を使う

ポインタのサイズは `sizeof` で調べられます。

```c
printf("size of char *: %zu\n", sizeof(char *));
printf("size of int * : %zu\n", sizeof(int *));
```

多くの環境では、オブジェクトポインタ同士は同じ値になります。
しかし、その値をソースコード上で固定値として決め打ちしてはいけません。

また、`sizeof(pointer)` と `sizeof(*pointer)` は意味が違います。

```text
sizeof(pi):
	ポインタ変数そのものの大きさ

sizeof(*pi):
	pi が指す先の型の大きさ
```

ここを取り違えると、`malloc()` のサイズ指定で誤りやすくなります。

#### 1章の2の4　`intptr_t` と `uintptr_t`

ポインタ値を整数として保持する必要があるときは、`stdint.h` に定義される `intptr_t` や `uintptr_t` を使います。
ただし、これらは実装が提供する場合に限って使える任意型です。

```c
#include <stdint.h>
#include <stdio.h>

int main(void)
{
	int num = 42;
	int *pi = &num;
	intptr_t raw = (intptr_t)(void *)pi;
	int *back = (int *)(void *)raw;

	printf("pi   = %p\n", (void *)pi);
	printf("back = %p\n", (void *)back);
	return 0;
}
```

重要なのは、`intptr_t` と `uintptr_t` はポインタ型ではなく整数型だという点です。
したがって、次のような宣言は意図として不適切です。

```c
/* intptr_t *pi; のように使うのではなく、
   intptr_t raw_address; のように整数として使う */
```

通常のアプリケーションコードでは、ポインタを整数へ変換しなければならない場面は多くありません。
必要になったときだけ、理由を明確にして使うべきです。

また、ポインタを安易に `int` や `long` へキャストするのは避けてください。
64 ビット環境では情報が欠ける可能性があります。

#### 1章の2の5　この段階で押さえるべきこと

ここまでで最低限押さえたいのは、次の点です。

```text
ポインタはアドレス風の値だが、型と寿命と境界が重要

初期化されていないポインタと NULL は別物

%p と (void *) を使ってポインタを表示する

size_t はサイズを表す型であり、sizeof の結果型でもある

ポインタサイズは環境依存であり、固定値を前提にしてはいけない
```

この土台が固まると、次に扱うポインタ演算、配列との関係、文字列、動的メモリ管理がかなり理解しやすくなります。

### 1章の3　ポインタ演算子

いくつかの演算子は、ポインタに対して特別な意味を持ちます。
ここまでに `&` と `*` は見ましたが、C ではそれ以外にも、加算、減算、比較、`->`、キャストなどがポインタと深く関わります。

まずは、よく使うものを整理します。

```text
*:
	ポインタの間接参照

->:
	構造体ポインタ経由でメンバへアクセス

+ と -:
	配列要素上を移動するためのポインタ演算

== !=:
	同じ場所を指しているかどうかの比較

< <= > >=:
	同じ配列内での前後関係の比較

(型名):
	ポインタ型の変換
```

ここで重要なのは、ポインタ演算は「生の数値計算」ではなく、「指している型の要素単位で進む操作」だという点です。

#### 1章の3の1　ポインタ演算でできること

標準 C で定義されるポインタ演算は、主にオブジェクトポインタに対するものです。

```text
ポインタ + 整数
	配列要素を前へ進む

ポインタ - 整数
	配列要素を後ろへ戻る

ポインタ - ポインタ
	同じ配列内で何要素離れているかを求める

ポインタ同士の比較
	同じ配列内で前後関係や等値を調べる
```

一方で、関数ポインタに対して加算や減算を行うことはできません。
また、無関係なオブジェクトを指すポインタ同士の大小比較や差分計算は、意味を持たないか未定義になります。

### 1章の3の2　ポインタと整数の和

ポインタに整数を足すと、指している型の要素数ぶん前へ進みます。

```c
int vector[] = {28, 41, 7};
int *pi = vector;

printf("%d\n", *pi);
pi += 1;
printf("%d\n", *pi);
pi += 1;
printf("%d\n", *pi);
```

この例では、`pi` は `vector[0]`、`vector[1]`、`vector[2]` を順に指します。
`int` が 4 バイトの環境では、見かけのアドレス値は 4 ずつ増えることが多いですが、
ここで本質なのは「1 要素進む」ことです。

配列名 `vector` は、この文脈では先頭要素 `&vector[0]` へ暗黙変換されます。

```text
pi = vector:
	vector[0] を指す

pi + 1:
	vector[1] を指す

pi + 2:
	vector[2] を指す
```

#### 1章の3の2の1　1つ先までは作れても、参照してはいけない

配列に対するポインタ演算では、「最後の要素の 1 つ先」までは作ることができます。

```c
int vector[] = {28, 41, 7};
int *end = vector + 3;
```

この `end` は合法に生成できますが、`*end` として間接参照してはいけません。
これは終端判定には便利ですが、有効な要素ではないからです。

```c
for (int *pi = vector; pi != vector + 3; ++pi) {
	printf("%d\n", *pi);
}
```

この書き方は正しいです。
一方、`vector + 3` を参照するのは未定義動作です。

#### 1章の3の2の2　型によって増分が違う

ポインタ演算では、増分は指している型に依存します。

```c
short s = 0;
short *ps = &s;
char c = 0;
char *pc = &c;

printf("ps before: %p\n", (void *)ps);
printf("ps after : %p\n", (void *)(ps + 1));

printf("pc before: %p\n", (void *)pc);
printf("pc after : %p\n", (void *)(pc + 1));
```

通常、`short *` は 2 バイト進み、`char *` は 1 バイト進みます。
ただし、ここでも「何バイト進むか」より「何要素進むか」が本質です。

#### 1章の3の3　`void *` に対する加算

ここは古い資料で誤解を招きやすい部分です。
標準 C では、`void *` に対する算術演算はできません。

```c
int num = 5;
void *pv = &num;

/* pv = pv + 1; */
```

GCC や Clang では拡張として通ることがありますが、移植性のあるコードとしては避けるべきです。
バイト単位で進めたいなら、`unsigned char *` や `char *` へ変換してから演算します。

```c
unsigned char *bytes = (unsigned char *)&num;
bytes += 1;
```

`char *` 系はオブジェクト表現を 1 バイト単位で見るための特別な役割を持つので、低レベル処理では重要です。

#### 1章の3の4　ポインタから整数の減算

ポインタから整数を引くと、要素単位で後ろへ戻れます。

```c
int vector[] = {28, 41, 7};
int *pi = vector + 2;

printf("%d\n", *pi);
pi--;
printf("%d\n", *pi);
pi--;
printf("%d\n", *pi);
```

この例では、`pi` は `vector[2]`、`vector[1]`、`vector[0]` を逆順にたどります。

### 1章の3の5　ポインタからポインタを引く

同じ配列の要素、またはその終端 1 つ先を指す 2 つのポインタ同士なら、差を取れます。
結果は「何バイト差か」ではなく、「何要素差か」です。

```c
#include <stddef.h>
#include <stdio.h>

int main(void)
{
	int vector[] = {28, 41, 7};
	int *p0 = vector;
	int *p1 = vector + 1;
	int *p2 = vector + 2;

	printf("p2 - p0: %td\n", p2 - p0);
	printf("p2 - p1: %td\n", p2 - p1);
	printf("p0 - p1: %td\n", p0 - p1);
	return 0;
}
```

`p2 - p0` が 2 になるのは、`p2` が `p0` より 2 要素先を指しているからです。

この結果型は `ptrdiff_t` です。
表示には `%td` を使います。

ここでも制約があり、無関係な配列や別オブジェクトを指すポインタ同士の差を取ってはいけません。

#### 1章の3の5の1　値の差とポインタの差は別物

次の 2 つは、まったく違う計算です。

```c
int vector[] = {28, 41, 7};
int *p0 = vector;
int *p1 = vector + 1;

printf("pointer diff: %td\n", p1 - p0);
printf("value diff  : %d\n", *p0 - *p1);
```

前者は要素位置の差、後者は格納されている整数値の差です。
`*` が付くかどうかで意味が大きく変わります。

### 1章の3の6　ポインタの比較

ポインタは比較できますが、大小比較に意味があるのは、同じ配列内の要素を指している場合が基本です。

```c
int vector[] = {28, 41, 7};
int *p0 = vector;
int *p1 = vector + 1;
int *p2 = vector + 2;

printf("p2 > p0: %d\n", p2 > p0);
printf("p2 < p0: %d\n", p2 < p0);
printf("p0 > p1: %d\n", p0 > p1);
```

同じ配列なら、この比較は要素順に対応します。
ただし、無関係な 2 つのオブジェクトへのポインタを `>` や `<` で比較するのは移植性がありません。

等値比較 `==` と `!=` は、より広い場面で使えます。
たとえば `NULL` かどうかの比較、同じアドレスを指しているかどうかの確認です。

### 1章の4　ポインタの一般的な使い方

ここでは、実際のコードでよく出る 2 つの話題を確認します。

```text
多重間接参照:
	ポインタへのポインタ

const とポインタ:
	どこを変更禁止にしたいかを型で表す
```

#### 1章の4の1　多重間接参照

ポインタは、さらにポインタを指すことができます。
いわゆるダブルポインタです。

代表例は `main()` の引数 `argv` です。
`argv` は「文字列へのポインタの配列」ですが、式の中では `char **` として扱われます。

次の例では、書名文字列を 1 か所に置き、それを複数のリストから参照します。

```c
const char *titles[] = {
	"A Tale of Two Cities",
	"Wuthering Heights",
	"Don Quixote",
	"Odyssey",
	"Moby-Dick",
	"Hamlet",
	"Gulliver's Travels"
};

const char **best_books[3];
const char **english_books[4];

best_books[0] = &titles[0];
best_books[1] = &titles[3];
best_books[2] = &titles[5];

english_books[0] = &titles[0];
english_books[1] = &titles[1];
english_books[2] = &titles[5];
english_books[3] = &titles[6];

printf("%s\n", *english_books[1]);
```

ここで `titles` の各要素は `const char *` です。
したがって `&titles[1]` の型は `const char **` になり、`english_books` の各要素に入れられます。

この構造の利点は、文字列本体を複製せずに、複数のリストから参照できることです。
ただし、間接参照の段数が増えるほど可読性は落ちるので、必要な分だけに留めるのが実務的です。

#### 1章の4の2　`const` とポインタ

`const` とポインタの組み合わせは、C で API を安全に設計するうえで非常に重要です。
何を固定したいのかで意味が変わります。

```text
指している先を変更不可にするのか

ポインタ自身の値を変更不可にするのか

その両方を変更不可にするのか
```

#### 1章の4の2の1　定数へのポインタ

次の宣言は、「`const int` を指すポインタ」です。

```c
int num = 5;
const int limit = 500;

int *pi = &num;
const int *pci = &limit;
```

`pci` を通して値を読むことはできますが、変更はできません。

```c
printf("%d\n", *pci);
```

一方、`pci` 自身は別の場所を指すように変更できます。

```c
pci = &num;
```

ただし、`*pci = 200;` のような書き方はできません。
`pci` 経由では読み取り専用として扱う、という契約だからです。

ここで大事なのは、「元の実体が `num` であっても、`pci` という経路では変更しない」と型で宣言していることです。

`const int *pci;` と `int const *pci;` は同じ意味です。

#### 1章の4の2の2　非定数への定数ポインタ

次は、「ポインタ自身は変更できないが、指している先は変更できる」形です。

```c
int num = 0;
int *const cpi = &num;
```

この場合、`cpi` は必ず初期化が必要です。
そして、別のアドレスを代入することはできません。

```c
*cpi = 25;   /* これはできる */
/* cpi = &other; これはできない */
```

「このポインタは常に同じ対象を指す」という契約を表したいときに使います。

#### 1章の4の2の3　定数への定数ポインタ

両方を固定することもできます。

```c
const int limit = 500;
const int *const cpci = &limit;
```

この `cpci` では、

```text
cpci 自身の値を変更できない

cpci が指す先の値も変更できない
```

という制約になります。

宣言時に初期化が必要であり、後から別の場所を指させることも、経由して値を書き換えることもできません。

#### 1章の4の2の4　定数ポインタを多重化する

`const` 付きポインタも、さらにポインタで指せます。

```c
const int limit = 500;
const int *const cpci = &limit;
const int *const *pcpci = &cpci;

printf("%d\n", *cpci);
printf("%d\n", **pcpci);
```

宣言が複雑になったら、変数名から外側へ読む方法に戻ると理解しやすいです。

#### 1章の4の2の5　4つの基本形

最初に押さえるべき代表形は次の 4 つです。

```text
int *p:
	ポインタも参照先も変更可能

const int *p:
	ポインタは変更可能、参照先は変更不可

int *const p:
	ポインタは変更不可、参照先は変更可能

const int *const p:
	ポインタも参照先も変更不可
```

関数引数でどれを使うかは、API 設計に直結します。
読むだけのバッファなら `const char *`、呼び出し先が書き換えるなら `char *` のように、意図を型へ反映させます。

### 1章の5　まとめ

この章では、ポインタの基本的な見方、メモリとの関係、`NULL`、`void *`、ポインタサイズ、`size_t`、`intptr_t`、ポインタ演算、比較、多重間接参照、`const` とポインタの組み合わせを整理しました。

ここで重要なのは、ポインタを単なる「番地」ではなく、次の 4 つを持つものとして理解することです。

```text
型:
	何を指す想定か

寿命:
	参照先はまだ生きているか

境界:
	どこまでアクセスしてよいか

契約:
	書き換えてよいか、読み取り専用か
```

この土台があると、関数引数、構造体、動的メモリ、文字列操作、配列処理、システムコールのバッファ受け渡しで
迷いにくくなります。

### 1章の6　UmuOSでの観点

UmuOS の文脈でポインタを学ぶ意味は、とても大きいです。
なぜなら、OS やシェルやエディタの実装では、「メモリ上のどこを、どの型として、どこまで安全に触るのか」が、
そのまま設計品質へつながるからです。

まず、文字列処理です。
シェルのコマンドライン解析、`argv` の組み立て、環境変数の受け渡し、行バッファの管理では、
`char *`、`const char *`、`char **` をほぼ避けて通れません。
ここで境界や終端 `\0` の理解が曖昧だと、すぐにバッファ破壊や誤動作へつながります。

次に、データ構造です。
UmuOS の内部でプロセス表、ファイル表、デバイス表、キュー、連結リスト、木構造のようなものを扱うなら、
ポインタは中心的な役割を持ちます。
配列で固定長にするのか、ポインタで動的につなぐのかは、実装の単純さ、柔軟性、性能に直結します。

さらに、低レイヤ I/O です。
`read()`、`write()`、`memcpy()`、`memset()`、独自バッファ管理などでは、「先頭アドレス」と「長さ」を
セットで扱います。
ここでは `void *`、`char *`、`size_t`、`const` の理解が欠かせません。
とくに UmuOS のように低レイヤ寄りの実装を意識するなら、「ポインタだけ渡して長さを失う」設計がどれだけ危険かを早い段階で意識しておくべきです。

最後に、デバッグです。
UmuOS やその周辺ツールで起きる不具合の中には、未初期化ポインタ、寿命切れ領域への参照、二重解放、範囲外アクセスのように、ポインタ理解が不足していると追いにくいものが多くあります。
逆に言えば、この章の内容をしっかり自分の言葉で説明できるようになると、クラッシュや破損の原因調査はかなりやりやすくなります。

UmuOS の為の C 言語として見るなら、この章で本当に身につけるべきことは、「ポインタを怖がらず、しかし雑にも扱わない」という姿勢です。
アドレス、型、寿命、境界、`const` の意味を一つずつ明確にしながら実装していけば、ポインタは危険な呪文ではなく、OS 実装を支える強力な道具になります。