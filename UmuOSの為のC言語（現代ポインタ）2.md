---
title: "UmuOSの為のC言語（現代ポインタ）　2章　C言語の動的メモリ管理"
---

# UmuOSの為のC言語（現代ポインタ）

このノートは、UmuOSを進化させるために必要となるC言語のポインタ理解を、現代のC17相当の視点で抽象化し、再構成することであります。
すなわち、UmuOSの実装・読解・デバッグへ還元するための実践ノートであります。
ポインタは、単なる文法項目ではなく、メモリ、配列、文字列、関数呼び出し、所有権、未定義動作と深く結びついています。
この構造を理解することは、UmuOSの設計力を高めることに直結すると思います。

## 2章　C言語の動的メモリ管理

動的に確保したメモリをポインタで追跡し、必要なあいだだけ保持し、不要になったら解放する能力は、C言語の表現力を
大きく支える要素です。
配列の要素数が実行時まで決まらない場合、入力サイズに応じて領域を伸縮させたい場合、リンクリストや木構造のように要素数が変化するデータ構造を扱いたい場合など、動的メモリ管理は避けて通れません。

ただし、動的メモリ管理は便利である一方で、誤ると深刻な不具合を生みます。
未解放によるメモリリーク、解放済み領域へのアクセス、解放済みポインタの再利用、確保サイズの計算ミス、領域外書き込み、`realloc()` 失敗時のアドレス喪失などは、どれも実務で頻出する問題です。
そのため、この章では「メモリを確保できる」という事実だけでなく、「どのような契約で確保し、どのような条件で安全に使い、いつどのように解放するか」という流れ全体を整理します。

Cプログラムは、OSなどの実行環境の上で動きます。
実行環境は、コード領域、静的記憶域期間のオブジェクトが置かれる領域、自動記憶域期間のオブジェクトが使うことの多いスタック、そして動的確保領域として扱われることの多いヒープを用意します。
ただし、ここでも重要なのは、C言語標準そのものが「スタック」や「ヒープ」という語で定義されているわけではない、という点です。
言語仕様としては、記憶域期間、寿命、到達可能性、未定義動作の観点から理解する方が正確です。

自動変数は、通常はブロックを抜けると寿命が終わります。
静的記憶域期間の変数は、プログラム開始から終了まで存在します。
それに対して、`malloc()` などで得た動的確保領域は、`free()` するまで、またはプロセスが終了するまで残ります。
この「寿命をプログラマが明示的に管理する」という点が、動的メモリ管理の本質です。

古いCの説明では、「配列は固定長だから、大きさが変わるデータには動的メモリが必要」という導入がよく使われます。
考え方としては今でも有効ですが、現代的には少し補足が必要です。
C99 では可変長配列（VLA）が導入されましたが、C11以降では実装が必須ではなく、C17でも処理系によっては無効です。
また、VLA はブロックを抜けると消える自動記憶域期間の仕組みであり、確保した後にサイズを伸縮させるための機能ではありません。
そのため、サイズが実行時まで分からないだけでなく、寿命や再確保や所有権まで管理したい場合には、依然として動的メモリ管理が重要になります。

この章では、まずメモリの動的確保と解放の基本を確認します。
次に、典型的な誤りとして、アドレスの紛失、領域外書き込み、メモリリークを扱います。
そのうえで、後続の節で `calloc()`、`realloc()`、`free()`、ぶら下がりポインタ、所有権の整理へ進められる土台を作ります。

### 2章の1　動的メモリ割り当て

C言語で動的にメモリを扱うときの基本手順は、概ね次の通りです。

```text
1. 必要な大きさを計算する

2. malloc() や calloc() などで領域を確保する

3. 返されたポインタが有効か確認する

4. その領域を使う

5. 不要になったら free() で解放する

6. 必要に応じて、元のポインタを NULL に戻す
```

実際には、途中で関数へ所有権を渡したり、`realloc()` でサイズ変更したり、構造体の内部にさらに動的領域を持ったりするので、
常にこの形に単純化できるわけではありません。
それでも、「確保」「使用」「解放」という流れが基本であることは変わりません。

まず、`int` 1個ぶんの領域をヒープから確保する例を見ます。

```c
#include <stdio.h>
#include <stdlib.h>

int main(void)
{
	int *pi = malloc(sizeof *pi);

	if (pi == NULL) {
		fputs("malloc failed\n", stderr);
		return 1;
	}

	*pi = 5;
	printf("*pi = %d\n", *pi);

	free(pi);
	return 0;
}
```

この例では、`malloc(sizeof *pi)` によって `int` 1個ぶんの領域を確保しています。
`malloc()` は成功すると、確保した先頭バイトを指すポインタを返します。
失敗した場合は `NULL` を返すので、戻り値を確認せずにすぐ間接参照するのは安全ではありません。

ここで重要なのは、Cでは `malloc()` の戻り値を通常はキャストしない、という点です。
`malloc()` の戻り値は `void *` であり、Cでは `void *` から任意のオブジェクトポインタ型へ暗黙変換できます。
したがって、`int *pi = (int *)malloc(sizeof(int));` のような書き方は、Cでは冗長です。
むしろ `stdlib.h` をインクルードし忘れた場合に、古い処理系では問題を隠してしまう書き方でもあるので、
現代のCコードではキャストしない方が一般的です。

また、サイズ計算に `sizeof *pi` を使っている点にも意味があります。
これは `pi` が指す先の型の大きさを使う、という書き方です。
`sizeof(int)` と同じ結果になりますが、型名を重複して書かなくてよいので、変数の型を変更した時に修正漏れを起こしにくくなります。

一方で、次のように固定値でサイズを書くのは避けるべきです。

```c
int *pi = malloc(4);
```

`int` が 4 バイトである環境は多いですが、それを言語仕様が常に保証しているわけではありません。
可搬性を保つには、`sizeof` を使って必要な大きさを求めるべきです。

#### 2章の1の1　ありがちな代入ミス

動的メモリ管理では、間接演算子 `*` の付ける場所を間違えると、非常に危険です。
次のようなコードは誤りです。

```c
int *pi;

*pi = malloc(sizeof *pi);
```

左辺の `*pi` は、「pi が指している先の `int`」を意味します。
しかし、この時点で `pi` 自体はまだ有効な領域を指していません。
つまり、未初期化ポインタを間接参照したうえで、そこへ `malloc()` の返したアドレスを書き込もうとしていることになります。
これは未定義動作です。

正しくは、まず `pi` 自体にアドレスを受け取らせます。

```c
int *pi = malloc(sizeof *pi);
```

そして、`pi != NULL` を確認した後で、`*pi` を使って中身へアクセスします。

```c
if (pi != NULL) {
	*pi = 5;
}
```

ここは初学者が混乱しやすい点です。
`pi` は「住所を入れる変数」であり、`*pi` は「その住所にある実体」です。
最初に受け取るべきなのは住所なので、代入先は `pi` であって `*pi` ではありません。

#### 2章の1の2　free() の役割

`malloc()` や `calloc()` や `realloc()` で得た動的確保領域は、不要になったら `free()` に渡して解放します。

```c
free(pi);
```

`free()` を呼ぶと、その領域は以後、プログラムが使ってよい領域ではなくなります。
ポインタ変数 `pi` の中身のビット列が直ちに消えるとは限りませんが、その値はもはや有効な参照先を意味しません。
したがって、解放後に `*pi` を読んだり書いたりするのは use-after-free であり、未定義動作です。

また、`malloc()` 系の呼び出しには、対応する `free()` が必要です。
もちろん、プログラム終了時にOSがプロセス全体の資源を回収する環境は多いですが、それを理由に `free()` を省略してよいとは
考えない方がよいです。
長時間動くプログラム、ライブラリ、サーバ、繰り返し呼ばれる処理では、解放漏れは実害になります。

#### 2章の1の3　確保領域の外へ書いてはいけない

ヒープに確保した領域は、確保した大きさの範囲内でだけアクセスできます。
たとえば 6 バイトだけ確保した文字配列に対して、それを越えて書き込むのは未定義動作です。

```c
#include <stdlib.h>

int main(void)
{
	char *buffer = malloc(6);

	if (buffer == NULL) {
		return 1;
	}

	for (int index = 0; index < 8; index++) {
		buffer[index] = '\0';
	}

	free(buffer);
	return 0;
}
```

このコードでは、`buffer` は 6 バイトしか指していないのに、`buffer[0]` から `buffer[7]` まで、
合計 8 バイトへ書き込んでいます。
これは単なる「少しはみ出した」では済みません。
隣接する別のオブジェクト、アロケータ内部の管理情報、未使用領域などを壊す可能性があります。

実装によっては、ヒープ管理のためのメタデータが確保領域の近くに置かれていることがあります。
しかし、その位置や内容は実装依存であり、プログラム側が前提にしてよいものではありません。
重要なのは、「確保したサイズを越える書き込みは、ヒープ破壊につながりうる」という点です。

文字列用の領域では、終端の `\0` ぶんも忘れてはいけません。
たとえば 5 文字の文字列を格納するなら、通常は少なくとも 6 バイト必要です。

#### 2章の1の4　メモリリーク

メモリリークとは、確保した動的メモリが、まだヒープ上に残っているのに、プログラムから正しく回収できなくなることです。
典型的には、次の2種類に分けて考えると分かりやすいです。

```text
アドレスを失うメモリリーク:
	確保した領域の先頭アドレスを追跡できなくなる

解放し忘れによるメモリリーク:
	アドレスは辿れるのに、不要になっても free() しない
```

どちらも結果としては「ヒープに返すべき領域を返していない」状態です。
短時間で終わる小さなプログラムでは目立たなくても、ループ、サーバ、繰り返し処理、長時間稼働するプロセスでは
少しずつメモリを失っていきます。
最終的には、新しい割り当てが失敗したり、性能が劣化したり、プロセスが異常終了したりします。

##### 2章の1の4の1　アドレスの紛失

最も単純なメモリリークは、確保した領域の先頭アドレスを上書きしてしまう例です。

```c
#include <stdlib.h>

int main(void)
{
	int *pi = malloc(sizeof *pi);

	if (pi == NULL) {
		return 1;
	}

	*pi = 5;

	pi = malloc(sizeof *pi);
	if (pi == NULL) {
		return 1;
	}

	free(pi);
	return 0;
}
```

このコードでは、最初の `malloc()` で得た領域のアドレスを `pi` が保持しています。
ところが、その `pi` に対して 2回目の `malloc()` の結果をそのまま代入してしまうと、最初の領域の先頭アドレスが
失われます。
すると最初の領域に対して `free()` を呼ぶ手段がなくなり、メモリリークになります。

実務では、`realloc()` の失敗時に似た問題を起こしやすいので注意が必要です。
いったん別の一時ポインタで受け、成功した時だけ元の変数へ反映する、という書き方が重要になります。
これは後で詳しく扱います。

文字列走査でも、同種の問題が起きます。

```c
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(void)
{
	char *name = malloc(strlen("Susan") + 1);

	if (name == NULL) {
		return 1;
	}

	strcpy(name, "Susan");

	while (*name != '\0') {
		putchar(*name);
		name++;
	}

	return 0;
}
```

このコードでは、`name++` によってポインタ変数そのものを進めています。
その結果、ループ終了時には `name` は元の先頭アドレスを指していません。
このままでは `free(name)` も正しく呼べず、確保した領域の先頭アドレスを失います。

このような場合は、先頭アドレスを保持する変数と、走査用の変数を分けるのが安全です。

```c
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(void)
{
	char *name = malloc(strlen("Susan") + 1);

	if (name == NULL) {
		return 1;
	}

	strcpy(name, "Susan");

	for (char *cursor = name; *cursor != '\0'; cursor++) {
		putchar(*cursor);
	}

	free(name);
	return 0;
}
```

`name` は解放用に保持し、`cursor` を走査専用に使うことで、所有権と走査位置を混同せずに済みます。

##### 2章の1の4の2　解放し忘れによるリーク

アドレスは失っていなくても、不要になった領域を解放しなければメモリリークです。
単純な例として、ループのたびに領域を確保するのに、一度も `free()` しないコードを考えます。

```c
#include <stdio.h>
#include <stdlib.h>

int main(void)
{
	for (;;) {
		char *chunk = malloc(1000000);

		if (chunk == NULL) {
			fputs("allocation failed\n", stderr);
			break;
		}

		puts("allocated");
	}

	return 0;
}
```

この例では、各反復で新しい 1,000,000 バイトの領域を確保しますが、以前の領域を返却していません。
しかも `chunk` はループの次回反復で新しい値に上書きされるため、前回の領域のアドレスも失われます。
結果として、割り当て可能なメモリは徐々に減っていき、やがて確保に失敗します。

もう1つ重要なのは、構造体の内部に動的領域をぶら下げている場合です。
たとえば、構造体本体を `free()` しても、その構造体のメンバが指していた別の動的領域は自動では解放されません。
そのため、複合データ構造では「外側を解放する前に内側を解放する」という順序が重要になります。
この問題は、後の章で構造体と所有権の話と合わせて詳しく整理します。

ここまでの段階で押さえるべきなのは、次の点です。

```text
malloc() の戻り値は NULL かどうか確認する

必要なサイズは sizeof で求める

Cでは malloc() の戻り値を通常はキャストしない

確保した範囲の外へ読んだり書いたりしてはいけない

先頭アドレスを失うと free() できなくなる

不要になった動的領域は、責任を持って解放する
```

この土台が固まると、`calloc()` によるゼロ初期化、`realloc()` による再確保、`free()` 後の `NULL` 代入、
ぶら下がりポインタ対策などが理解しやすくなります。

### 2章の2　動的メモリ割り当て関数

標準Cで動的メモリ管理に使う代表的な関数は、`stdlib.h` に宣言されています。

```text
malloc():
	指定したバイト数の領域を確保する

calloc():
	要素数と要素サイズを指定して領域を確保し、全バイトを 0 で埋める

realloc():
	既存の動的領域の大きさを変更する

free():
	動的に確保した領域を解放する
```

どの関数も重要ですが、役割は少しずつ異なります。
また、どの関数を使って確保したとしても、解放は `free()` で行います。

動的に確保された領域は、ヒープから供給されるのが一般的です。
ただし、連続して `malloc()` したからといって、返されるアドレスが連続するとは限りません。
アロケータは空き領域の状態や内部方針に従って配置を決めます。
一方で、返されるポインタは、その型としてアクセスするのに必要なアラインメントを満たすように調整されます。

#### 2章の2の1　malloc() の使い方

`malloc()` は、指定したバイト数ぶんの領域を確保します。
プロトタイプは次の通りです。

```c
void *malloc(size_t size);
```

引数 `size` は、確保したいバイト数です。
戻り値は、確保した領域の先頭を指す `void *` です。
確保に失敗した場合は `NULL` を返します。

ここで押さえるべき点は次の通りです。

```text
malloc() は領域を確保するだけで、内容を初期化しない

戻り値は NULL の可能性がある

引数は size_t であり、負数を渡す設計ではない

必要サイズは sizeof で求めるべき
```

`malloc()` が返す領域の中身は不定です。
昔は「ゴミが入っている」と説明されることが多いですが、ここでは「まだ意味のある値は入っていない」と
理解する方が正確です。
読み出す前に、自分で代入するか、別の方法で初期化しなければなりません。

典型例を示します。

```c
int *pi = malloc(sizeof *pi);

if (pi == NULL) {
	return 1;
}

*pi = 123;
```

##### 2章の2の1の1　malloc(0) の扱い

`malloc(0)` の扱いは、実装差が出やすい点です。
C標準では、`malloc(0)` は `NULL` を返すこともあれば、後で `free()` できる一意なポインタを返すこともあります。
どちらにせよ、その戻り値を通常の領域として読み書きしてよいとは考えるべきではありません。

そのため、サイズが 0 になる可能性がある設計では、呼び出し前に 0 をどう扱うかを自分で決める方が安全です。

##### 2章の2の1の2　キャストするかどうか

古い資料では、`malloc()` の戻り値に明示的なキャストを付ける例がよく出てきます。

```c
int *pi = (int *)malloc(sizeof(int));
```

しかし、Cでは通常このキャストは不要です。
`malloc()` は `void *` を返し、Cでは `void *` から任意のオブジェクトポインタ型へ暗黙変換できます。
したがって、現代のCコードでは次のように書くのが自然です。

```c
int *pi = malloc(sizeof *pi);
```

なお、C++では `void *` から別のポインタ型への暗黙変換ができないため、`malloc()` を使うならキャストが必要になります。
ただしC++では通常、`new` や標準ライブラリの手段を使うので、Cの書き方をそのまま持ち込むことは少ないです。

##### 2章の2の1の3　未確保ポインタを使ってはいけない

ポインタ変数を宣言しただけでは、動的領域はまだ存在しません。

```c
int *pi;
printf("%d\n", *pi);
```

この `pi` は未初期化であり、どこを指しているか分かりません。
その状態で `*pi` を評価すれば未定義動作です。

同じ問題は文字列入力でも起きます。

```c
char *name;
scanf("%s", name);
```

このコードでは、`name` が十分な書き込み可能領域を指していないのに、`scanf()` に書き込ませています。
これは非常に危険です。
入力を受けるには、配列を用意するか、十分な大きさの動的領域を先に確保する必要があります。

さらに実務では、`scanf("%s", ...)` 自体が境界管理の観点から危険になりやすいので、固定長バッファなら `fgets()` を優先する方が安全です。

##### 2章の2の1の4　サイズ指定の誤り

`malloc()` は、要素数ではなくバイト数を受け取ります。
そのため、配列相当の領域を確保するときは、
「要素数 × 1要素あたりの大きさ」
を明示的に計算しなければなりません。

```c
size_t count = 10;
double *values = malloc(count * sizeof *values);
```

次のように要素数だけを渡すのは誤りです。

```c
size_t count = 10;
double *values = malloc(count);
```

この場合、確保されるのは 10 バイトだけであり、`double` 10個ぶんには足りません。

さらに現代的には、サイズ計算のオーバーフローにも注意が必要です。
`count * sizeof *values` が `size_t` の上限を越えると、桁あふれした小さな値で `malloc()` が呼ばれる可能性があります。
大量確保があり得るコードでは、乗算前に上限チェックを入れる設計が必要です。

##### 2章の2の1の5　確保サイズは標準Cからは取得できない

確保済み領域について、「実際に何バイト確保されているか」を後から標準Cだけで問い合わせる手段はありません。
一部の処理系やデバッガや独自拡張には類似機能がありますが、移植性のあるコードでは、自分でサイズ情報を別に
保持する必要があります。

これは重要です。
動的配列やバッファを設計するときは、
ポインタだけでなく、現在の要素数や容量も一緒に管理する
という考え方が基本になります。

##### 2章の2の1の6　静的初期化子に malloc() は使えない

静的記憶域期間を持つ変数や大域変数は、コンパイル時に評価できる初期化子で初期化する必要があります。
そのため、次のような書き方はできません。

```c
static int *pi = malloc(sizeof *pi);
```

`malloc()` は実行時の関数呼び出しなので、静的初期化子には使えません。
必要なら、まず `NULL` 相当で初期化しておき、後で関数の中で代入します。

```c
static int *pi;

void init_buffer(void)
{
	pi = malloc(sizeof *pi);
}
```

#### 2章の2の2　calloc() の使い方

`calloc()` は、複数要素ぶんの領域を確保し、全バイトを 0 で埋めます。
プロトタイプは次の通りです。

```c
void *calloc(size_t count, size_t size);
```

典型例は次のようになります。

```c
int *values = calloc(5, sizeof *values);

if (values == NULL) {
	return 1;
}
```

ここでは `int` 5個ぶんの領域を確保し、その領域の全バイトを 0 にしています。

ただし、ここでいう「0で埋める」は、あくまでメモリ表現がすべて 0 になるという意味です。
`int` や `char` の 0、ヌル終端文字列の初期状態などには都合が良いですが、
「すべての型で、そのビット列が常に望ましい意味になる」
と雑に考えない方が安全です。

実務では、次のような場面で `calloc()` が便利です。

```text
数値配列を 0 初期化したい

ポインタ配列を最初は空状態にしたい

構造体配列を最初にゼロクリアしておきたい
```

古い説明では `malloc()` と `memset()` の組み合わせで同じだとされることがあります。
概念上は近いですが、現代の実装では `calloc()` 側でサイズ計算やゼロページ利用などの最適化が入ることもあるため、
ゼロ初期化が必要なら `calloc()` を素直に使う方が意図も明確です。

#### 2章の2の3　realloc() の使い方

`realloc()` は、既存の動的領域の大きさを変更したいときに使います。
プロトタイプは次の通りです。

```c
void *realloc(void *ptr, size_t size);
```

第1引数 `ptr` は、`malloc()` 系で確保した既存領域へのポインタです。
第2引数 `size` は、新しく欲しいバイト数です。

`realloc()` の重要な性質は次の通りです。

```text
小さくすることも大きくすることもできる

その場で拡張できれば同じアドレスのままかもしれない

拡張できなければ別の場所へ移動し、中身をコピーする

失敗した場合、元の領域はそのまま残る
```

この「失敗しても元の領域は残る」という点が非常に重要です。
そのため、戻り値を直接元の変数へ代入してはいけません。

```c
char *tmp = realloc(buffer, new_size);

if (tmp == NULL) {
	/* buffer はまだ有効 */
	return 1;
}

buffer = tmp;
```

もし `buffer = realloc(buffer, new_size);` と書いて失敗すると、`NULL` が代入され、元の領域の先頭アドレスを失う危険があります。
これも典型的なメモリリークの原因です。

##### 2章の2の3の1　サイズを小さくする場合

領域を小さくしたとき、返されたポインタが元と同じであることがあります。
しかし、だからといって古い大きさのまま使ってよいわけではありません。
新しいサイズを越えた部分は、もはや自分の領域ではありません。

たとえば文字列バッファを縮小したなら、その後の文字列も新しい容量内に収まるように整理する必要があります。
縮小後も以前の長い内容をそのまま使い続けるのは、境界管理として誤りです。

##### 2章の2の3の2　サイズを大きくする場合

領域を大きくした場合、その場で拡張できれば同じアドレスのままです。
できなければ別の領域へ移動し、元の内容がコピーされます。

そのため、`realloc()` の後は、元のポインタ値に依存した別名や内部参照が残っていないかを確認する必要があります。
別の場所へ移動した場合、古いアドレスを保持していた変数は無効になります。

##### 2章の2の3の3　realloc(NULL, size) と realloc(ptr, 0)

`realloc(NULL, size)` は、`malloc(size)` と同等に扱えます。
これは標準的な使い方です。

一方で `realloc(ptr, 0)` は、標準の版や実装差で読み違えやすい箇所です。
現代の実務では、解放したいなら `free(ptr)` を明示的に呼び、0 バイト再確保を制御フローに組み込まない方が
分かりやすいです。

#### 2章の2の4　alloca() と可変長配列

`alloca()` は、関数呼び出しのスタックフレームに領域を確保する非標準の拡張です。
関数から戻ると自動的に消えるため一見便利ですが、標準Cではなく、移植性を重視するコードでは避けるべきです。

可変長配列（VLA）も、実行時に大きさを決める仕組みですが、これもヒープの動的確保とは性質が異なります。

```c
void compute(size_t size)
{
	char buffer[size];
	(void)buffer;
}
```

この `buffer` はブロックを抜けると寿命が終わります。
`malloc()` した領域のように `free()` するものではなく、後からサイズ変更もできません。
さらに C11 以降では VLA が必須ではないため、使えることを前提にしすぎると可搬性が落ちます。

したがって、
一時的で小さく、寿命が短い領域
には VLA や固定長配列が候補になりえますが、
サイズ変更、所有権の受け渡し、長い寿命、可搬性
を重視するなら動的メモリ管理の方が中心になります。

### 2章の3　free() によるメモリの解放

動的に確保した領域は、不要になった時点で `free()` に渡して解放します。

```c
void free(void *ptr);
```

`free()` に渡せるのは、`malloc()`、`calloc()`、`realloc()` が返したポインタ、または `NULL` です。
それ以外を渡した場合の振る舞いは未定義です。

```c
int num = 0;
int *pi = &num;

free(pi); /* 未定義動作 */
```

この例の `pi` は、自動変数 `num` を指しているだけであり、ヒープから確保した領域ではありません。
このようなポインタを `free()` に渡してはいけません。

また、`free()` 後もポインタ変数のビット列が自動で書き換わるとは限りません。
しかし、その値はもはや有効な参照先ではありません。
この状態のポインタを、ぶら下がりポインタと呼びます。

#### 2章の3の1　解放後に NULL を代入する

解放したポインタに対して、続けて `NULL` を代入する流儀があります。

```c
free(pi);
pi = NULL;
```

これは万能の解決策ではありませんが、少なくとも「その変数はもう有効な領域を指していない」という意図を明示できます。
また、後で誤って `free(pi)` を繰り返した場合も、`free(NULL)` は何もしないので、
その変数単体については二重解放を防ぎやすくなります。

ただし、本質的に重要なのは `NULL` を代入すること自体ではなく、解放後にそのポインタを再利用しない設計と、
所有権を1か所に明確化する設計です。
他の別名ポインタが同じ領域を指していれば、1つの変数を `NULL` にしても問題は消えません。

#### 2章の3の2　二重解放

同じ動的領域に対して `free()` を2回呼ぶことを二重解放といいます。

```c
int *pi = malloc(sizeof *pi);

if (pi == NULL) {
	return 1;
}

free(pi);
free(pi); /* 未定義動作 */
```

これは未定義動作です。
実行時に即座に異常終了することもあれば、ヒープ内部の管理状態を壊して、もっと後の場所で破綻することもあります。

少し見えにくいのは、別名ポインタがある場合です。

```c
int *p1 = malloc(sizeof *p1);

if (p1 == NULL) {
	return 1;
}

int *p2 = p1;

free(p1);
free(p2); /* 未定義動作 */
```

`p1` と `p2` は同じ領域を指しているだけなので、解放責任は1回しかありません。
この種の事故を避けるには、誰が所有権を持つのかを明確にし、別名はできるだけ読み取り用または一時参照に留めることが重要です。

#### 2章の3の3　ヒープとOSのメモリ管理

`free()` は、必ずしも解放した領域を即座にOSへ返すとは限りません。
一般には、そのプロセス内のアロケータが空き領域として再利用できる状態に戻すだけです。

そのため、`free()` を呼んでも、OSの外部ツールから見たメモリ使用量が直ちに大きく下がらないことがあります。
これは必ずしもバグではなく、アロケータの挙動として自然な場合があります。

重要なのは、プログラムから再利用できる状態へ戻したかであって、OSへ即返却されたかではありません。

#### 2章の3の4　終了時に全部 free() すべきか

プロセスが終了すれば、多くの現代OSはそのプロセスの通常の仮想メモリ資源を回収します。
そのため、終了直前だから `free()` しなくてもOSが片付けてくれる、というのは一般論としては正しいです。

ただし、それを根拠に普段から解放規律を崩すべきではありません。
実務では次の点を考える必要があります。

```text
長時間動く処理では、終了前まで待たずに早めに解放する必要がある

リーク検出ツールを使うなら、終了前に整理しておくと問題を見分けやすい

共有メモリやファイル記述子など、OSが自動で同じ形には片付けない資源もある

終了直前だと思っていた場所に、後で別の処理を足すことがある
```

一方で、複雑な終了処理のためにコードが過度に複雑化し、新たな解放バグを埋め込むのも本末転倒です。
したがって、終了直前の一括解放が本当に必要かと、通常経路での寿命管理が明確かを分けて考えるのが現実的です。

### 2章の4　ぶら下がりポインタ

ぶら下がりポインタとは、すでに寿命が終わったオブジェクトや、すでに解放した動的領域を指し続けているポインタです。
動的メモリ管理では特に、`free()` 後のポインタが典型例になります。

```text
解放済み領域へのアクセス:
	use-after-free となる

自動変数の寿命切れ後の参照:
	スコープを抜けたローカル変数のアドレスを使い続ける
```

この種のバグは、即座にクラッシュすることもあれば、たまたま動いて見えてしまうこともあります。
しかし、どちらにしても未定義動作であり、再現性の低い不具合やセキュリティ問題の原因になります。

#### 2章の4の1　解放済み領域を指す例

```c
int *pi = malloc(sizeof *pi);

if (pi == NULL) {
	return 1;
}

*pi = 5;
free(pi);

*pi = 10; /* 未定義動作 */
```

`free(pi)` を呼んだ時点で、その領域は以後プログラムが使ってよい領域ではありません。
それでも `pi` 自体の値が直ちに消えるとは限らないので、つい使えてしまうように見えることがあります。
この「見かけ上触れてしまう」ことが、ぶら下がりポインタを厄介にしています。

#### 2章の4の2　別名がある場合

```c
int *p1 = malloc(sizeof *p1);

if (p1 == NULL) {
	return 1;
}

int *p2 = p1;

free(p1);
*p2 = 10; /* 未定義動作 */
```

この例では、`p1` を `NULL` にしても `p2` は残ります。
したがって、ぶら下がりポインタ対策は単純な `NULL` 代入だけでは不十分です。
本質は、1つの領域を複数のポインタがどう共有し、誰が解放責任を持つのかを整理することにあります。

#### 2章の4の3　ローカル変数の寿命切れ

ぶら下がりポインタはヒープだけの問題ではありません。

```c
int *pi;

{
	int tmp = 5;
	pi = &tmp;
}

/* ここで pi は寿命切れオブジェクトを指す */
```

`tmp` はこのブロックを抜けた時点で寿命が終わります。
そのため、`pi` はもう有効な `int` を指していません。
これは「ローカル変数のアドレスを外へ持ち出してはいけない」という基本ルールの具体例です。

#### 2章の4の4　対処法

ぶら下がりポインタ対策として有効なのは、個別の小手先よりも設計の整理です。

```text
解放後は可能なら NULL を代入する

先頭アドレスを保持する所有者を明確にする

別名ポインタを増やしすぎない

ローカル変数のアドレスをスコープ外へ持ち出さない

動的配列や構造体では、生成と破棄の責任を関数単位で揃える
```

デバッグでは、ポインタ値を `%p` で表示して追跡するのも有効です。
ただし、今どきは手作業の追跡だけに頼るより、アドレスサニタイザや Valgrind のような検出ツールを併用する方が
はるかに効率的です。

#### 2章の4の5　現代的な検出手段

古い資料では、特定コンパイラのデバッグヒープや Mudflap のような仕組みが紹介されることがあります。
歴史的には意味がありますが、今の Linux/C 開発でまず見るべきなのは次のような手段です。

```text
AddressSanitizer (ASan):
	領域外アクセス、use-after-free などを検出しやすい

LeakSanitizer (LSan):
	メモリリーク検出に有効

UndefinedBehaviorSanitizer (UBSan):
	未定義動作の一部を検出しやすい

Valgrind:
	実行速度は落ちるが、メモリアクセスとリークの調査に有効
```

これらのツールは、ポインタの問題を「たまたま再現した時だけ見る」のではなく、実行時にかなり直接的に
あぶり出してくれます。

### 2章の5　動的メモリ割り当て技術

ここまで見てきた `malloc()` 系の管理は、いわゆる手動メモリ管理です。
これはCで最も基本的な方法ですが、それ以外にも補助的な技術や代替的な考え方があります。

ただし、ここで重要なのは、標準Cそのものは依然として手動管理を基本としている、という点です。
したがって、次に挙げる話題は「標準Cの基本を置き換える常識」ではなく、「特定環境や特定目的で使える追加手段」として
捉える方がよいです。

#### 2章の5の1　ガベージコレクション

ガベージコレクションは、使われなくなった領域を自動的に回収する仕組みです。
プログラマが `free()` を直接呼ばなくても、到達不能になった領域を回収できるため、メモリリークや解放忘れを
減らせることがあります。

ただし、標準Cにはガベージコレクションはありません。
使う場合は外部ライブラリや独自実行環境に依存します。

利点は明確です。

```text
解放タイミングを毎回手で管理しなくてよい

メモリ解放忘れの一部を減らせる

複雑な所有権関係を単純化できることがある
```

一方で、いつ回収されるかを厳密に制御しにくく、性能やメモリ使用量の特性も変わります。
そのため、低レイヤや OS 寄りのCコードでは、一般的な標準手法というより特殊な選択肢です。

#### 2章の5の2　RAII 的な考え方

RAII は本来 C++ の主要な考え方で、オブジェクトの寿命に資源の取得と解放を結び付けます。
標準Cそのものには同等の言語機能はありませんが、
スコープを抜ける時に自動で後始末を行う
という発想自体は参考になります。

GNU C には `cleanup` 属性のような拡張があり、限定的に似たことができます。
ただし、これは標準Cではないため、移植性が必要なコードの前提にはしない方がよいです。

現代のCで実務的に近いことをするなら、
生成関数と破棄関数をペアにする
エラー経路でも必ず後始末する
単一出口や cleanup ラベルで解放を集約する
といった書き方の方が一般的です。

#### 2章の5の3　例外処理風の整理

一部の処理系は例外処理のような拡張を持っていますが、これも標準Cではありません。
現代の移植性あるCコードでは、例外そのものに頼るより、戻り値による失敗伝播と cleanup パターンで後始末を
保証する方が基本です。

たとえば次のように、後始末を 1 か所へ集約する書き方は、C では非常に実用的です。

```c
int func(void)
{
	char *buffer = malloc(128);
	int status = -1;

	if (buffer == NULL) {
		goto cleanup;
	}

	status = 0;

cleanup:
	free(buffer);
	return status;
}
```

これは華やかではありませんが、複数のエラー経路で解放漏れを防ぐという意味では非常に強い書き方です。

### 2章の6　まとめ

この章では、C言語の動的メモリ管理として、`malloc()`、`calloc()`、`realloc()`、`free()` の基本と、
その周辺で起こりやすい問題を整理しました。
特に重要なのは、動的メモリ管理を単なる関数の暗記ではなく、寿命・所有権・境界・失敗処理の設計として理解することです。

ここで押さえるべき点をまとめると、次のようになります。

```text
必要サイズは sizeof で求める

malloc() の戻り値は NULL を確認する

Cでは malloc() の戻り値を通常はキャストしない

free() できるのは malloc() 系が返したポインタだけである

free() 後のポインタは無効であり、use-after-free は未定義動作である

realloc() は一時ポインタで受ける

メモリリーク、二重解放、ぶら下がりポインタは典型的な設計不良である
```

この章の理解が固まると、後の章で扱う構造体、可変長データ構造、文字列管理、ファイルI/O バッファ、システムコール用の
作業領域などを、かなり安全に設計できるようになります。

### 2章の7　UmuOSでの観点

UmuOS の文脈で動的メモリ管理を学ぶ意味は非常に大きいです。
なぜなら、OS、シェル、エディタ、ネットワーク処理のどれを見ても、「必要な量だけ確保し、寿命が尽きたら確実に返す」という設計が、
そのまま品質へ直結するからです。

まず、シェルやコマンド処理では、入力行の保持、トークン分割、`argv` の組み立て、環境変数のコピーなどで
動的メモリが頻繁に出てきます。
ここで所有権が曖昧だと、

どこで free() すべきか分からなくなる
解放後の文字列を別の場所が参照してしまう
失敗経路で一部だけ解放されずリークする

といった問題がすぐ起きます。

次に、可変長データ構造です。
UmuOS の内部でプロセス表、ジョブ表、履歴、バッファ、キュー、連結リストのような可変要素数の構造を扱うなら、`realloc()` やノードごとの `malloc()` は避けにくくなります。
このとき重要なのは、
要素数
容量
先頭ポインタ
解放責任
を常にセットで設計することです。
ポインタだけを持ち回る設計は、境界も寿命も見失いやすく、後で必ず苦しくなります。

さらに、低レイヤI/O では、バッファ長と確保領域の整合が極めて重要です。
`read()` や `write()`、文字列連結、ログ生成、パケット組み立てのような処理で、
確保サイズより多く書く
NUL 終端ぶんを忘れる
縮小後の古い容量を前提にする
といったミスをすると、クラッシュやヒープ破壊に直結します。

UmuOS のように観測・研究用のコードを書く場面では、
とりあえず動いた
という状態に流されやすいですが、動的メモリ管理ではそれが最も危険です。
未定義動作は、ある日突然、別の入力、別の最適化、別のビルド、別の実行順で破綻します。

そのため、UmuOS の為の C 言語として本当に身につけるべきことは、次の姿勢です。

```text
確保したら、誰が解放するかをその場で決める

ポインタだけでなく、長さと容量も一緒に管理する

free() 後のポインタは使わない

realloc() は失敗を前提に一時変数で扱う

サニタイザや Valgrind を使って、偶然ではなく検証で安全を確認する
```

この章の内容を自分の言葉で説明できるようになると、UmuOS やその周辺ツールで起きるクラッシュ、文字化け、引数破壊、ヒープ破壊、謎の不安定動作のかなりの部分を、自力で切り分けられるようになります。
動的メモリ管理は難所ですが、ここを越えると、C で扱える設計の幅が一気に広がります。

