---
title: "UmuOSの為のC言語（現代ポインタ）　2章　C言語の動的メモリ管理"
---

# UmuOSの為のC言語（現代ポインタ）

このノートは、UmuOSを進化させるために必要となるC言語のポインタ理解を、現代のC17相当の視点で抽象化し、再構成することであります。
すなわち、UmuOSの実装・読解・デバッグへ還元するための実践ノートであります。
ポインタは、単なる文法項目ではなく、メモリ、配列、文字列、関数呼び出し、所有権、未定義動作と深く結びついています。
この構造を理解することは、UmuOSの設計力を高めることに直結すると思います。

## 2章　C言語の動的メモリ管理

動的に確保したメモリをポインタで追跡し、必要なあいだだけ保持し、不要になったら解放する能力は、C言語の表現力を大きく支える要素です。
配列の要素数が実行時まで決まらない場合、入力サイズに応じて領域を伸縮させたい場合、リンクリストや木構造のように要素数が変化するデータ構造を扱いたい場合など、動的メモリ管理は避けて通れません。

ただし、動的メモリ管理は便利である一方で、誤ると深刻な不具合を生みます。
未解放によるメモリリーク、解放済み領域へのアクセス、解放済みポインタの再利用、確保サイズの計算ミス、領域外書き込み、`realloc()` 失敗時のアドレス喪失などは、どれも実務で頻出する問題です。
そのため、この章では「メモリを確保できる」という事実だけでなく、「どのような契約で確保し、どのような条件で安全に使い、いつどのように解放するか」という流れ全体を整理します。

Cプログラムは、OSなどの実行環境の上で動きます。
実行環境は、コード領域、静的記憶域期間のオブジェクトが置かれる領域、自動記憶域期間のオブジェクトが使うことの多いスタック、そして動的確保領域として扱われることの多いヒープを用意します。
ただし、ここでも重要なのは、C言語標準そのものが「スタック」や「ヒープ」という語で定義されているわけではない、という点です。
言語仕様としては、記憶域期間、寿命、到達可能性、未定義動作の観点から理解する方が正確です。

自動変数は、通常はブロックを抜けると寿命が終わります。
静的記憶域期間の変数は、プログラム開始から終了まで存在します。
それに対して、`malloc()` などで得た動的確保領域は、`free()` するまで、またはプロセスが終了するまで残ります。
この「寿命をプログラマが明示的に管理する」という点が、動的メモリ管理の本質です。

古いCの説明では、「配列は固定長だから、大きさが変わるデータには動的メモリが必要」という導入がよく使われます。
考え方としては今でも有効ですが、現代的には少し補足が必要です。
C99 では可変長配列（VLA）が導入されましたが、C11以降では実装が必須ではなく、C17でも処理系によっては無効です。
また、VLA はブロックを抜けると消える自動記憶域期間の仕組みであり、確保した後にサイズを伸縮させるための機能ではありません。
そのため、サイズが実行時まで分からないだけでなく、寿命や再確保や所有権まで管理したい場合には、依然として動的メモリ管理が重要になります。

この章では、まずメモリの動的確保と解放の基本を確認します。
次に、典型的な誤りとして、アドレスの紛失、領域外書き込み、メモリリークを扱います。
そのうえで、後続の節で `calloc()`、`realloc()`、`free()`、ぶら下がりポインタ、所有権の整理へ進められる土台を作ります。

### 2章の1　動的メモリ割り当て

C言語で動的にメモリを扱うときの基本手順は、概ね次の通りです。

```text
1. 必要な大きさを計算する

2. malloc() や calloc() などで領域を確保する

3. 返されたポインタが有効か確認する

4. その領域を使う

5. 不要になったら free() で解放する

6. 必要に応じて、元のポインタを NULL に戻す
```

実際には、途中で関数へ所有権を渡したり、`realloc()` でサイズ変更したり、構造体の内部にさらに動的領域を持ったりするので、常にこの形に単純化できるわけではありません。
それでも、「確保」「使用」「解放」という流れが基本であることは変わりません。

まず、`int` 1個ぶんの領域をヒープから確保する例を見ます。

```c
#include <stdio.h>
#include <stdlib.h>

int main(void)
{
	int *pi = malloc(sizeof *pi);

	if (pi == NULL) {
		fputs("malloc failed\n", stderr);
		return 1;
	}

	*pi = 5;
	printf("*pi = %d\n", *pi);

	free(pi);
	return 0;
}
```

この例では、`malloc(sizeof *pi)` によって `int` 1個ぶんの領域を確保しています。
`malloc()` は成功すると、確保した先頭バイトを指すポインタを返します。
失敗した場合は `NULL` を返すので、戻り値を確認せずにすぐ間接参照するのは安全ではありません。

ここで重要なのは、Cでは `malloc()` の戻り値を通常はキャストしない、という点です。
`malloc()` の戻り値は `void *` であり、Cでは `void *` から任意のオブジェクトポインタ型へ暗黙変換できます。
したがって、`int *pi = (int *)malloc(sizeof(int));` のような書き方は、Cでは冗長です。
むしろ `stdlib.h` をインクルードし忘れた場合に、古い処理系では問題を隠してしまう書き方でもあるので、現代のCコードではキャストしない方が一般的です。

また、サイズ計算に `sizeof *pi` を使っている点にも意味があります。
これは `pi` が指す先の型の大きさを使う、という書き方です。
`sizeof(int)` と同じ結果になりますが、型名を重複して書かなくてよいので、変数の型を変更した時に修正漏れを起こしにくくなります。

一方で、次のように固定値でサイズを書くのは避けるべきです。

```c
int *pi = malloc(4);
```

`int` が 4 バイトである環境は多いですが、それを言語仕様が常に保証しているわけではありません。
可搬性を保つには、`sizeof` を使って必要な大きさを求めるべきです。

#### 2章の1の1　ありがちな代入ミス

動的メモリ管理では、間接演算子 `*` の付ける場所を間違えると、非常に危険です。
次のようなコードは誤りです。

```c
int *pi;

*pi = malloc(sizeof *pi);
```

左辺の `*pi` は、「pi が指している先の `int`」を意味します。
しかし、この時点で `pi` 自体はまだ有効な領域を指していません。
つまり、未初期化ポインタを間接参照したうえで、そこへ `malloc()` の返したアドレスを書き込もうとしていることになります。
これは未定義動作です。

正しくは、まず `pi` 自体にアドレスを受け取らせます。

```c
int *pi = malloc(sizeof *pi);
```

そして、`pi != NULL` を確認した後で、`*pi` を使って中身へアクセスします。

```c
if (pi != NULL) {
	*pi = 5;
}
```

ここは初学者が混乱しやすい点です。
`pi` は「住所を入れる変数」であり、`*pi` は「その住所にある実体」です。
最初に受け取るべきなのは住所なので、代入先は `pi` であって `*pi` ではありません。

#### 2章の1の2　free() の役割

`malloc()` や `calloc()` や `realloc()` で得た動的確保領域は、不要になったら `free()` に渡して解放します。

```c
free(pi);
```

`free()` を呼ぶと、その領域は以後、プログラムが使ってよい領域ではなくなります。
ポインタ変数 `pi` の中身のビット列が直ちに消えるとは限りませんが、その値はもはや有効な参照先を意味しません。
したがって、解放後に `*pi` を読んだり書いたりするのは use-after-free であり、未定義動作です。

また、`malloc()` 系の呼び出しには、対応する `free()` が必要です。
もちろん、プログラム終了時にOSがプロセス全体の資源を回収する環境は多いですが、それを理由に `free()` を省略してよいとは考えない方がよいです。
長時間動くプログラム、ライブラリ、サーバ、繰り返し呼ばれる処理では、解放漏れは実害になります。

#### 2章の1の3　確保領域の外へ書いてはいけない

ヒープに確保した領域は、確保した大きさの範囲内でだけアクセスできます。
たとえば 6 バイトだけ確保した文字配列に対して、それを越えて書き込むのは未定義動作です。

```c
#include <stdlib.h>

int main(void)
{
	char *buffer = malloc(6);

	if (buffer == NULL) {
		return 1;
	}

	for (int index = 0; index < 8; index++) {
		buffer[index] = '\0';
	}

	free(buffer);
	return 0;
}
```

このコードでは、`buffer` は 6 バイトしか指していないのに、`buffer[0]` から `buffer[7]` まで、合計 8 バイトへ書き込んでいます。
これは単なる「少しはみ出した」では済みません。
隣接する別のオブジェクト、アロケータ内部の管理情報、未使用領域などを壊す可能性があります。

実装によっては、ヒープ管理のためのメタデータが確保領域の近くに置かれていることがあります。
しかし、その位置や内容は実装依存であり、プログラム側が前提にしてよいものではありません。
重要なのは、「確保したサイズを越える書き込みは、ヒープ破壊につながりうる」という点です。

文字列用の領域では、終端の `\0` ぶんも忘れてはいけません。
たとえば 5 文字の文字列を格納するなら、通常は少なくとも 6 バイト必要です。

#### 2章の1の4　メモリリーク

メモリリークとは、確保した動的メモリが、まだヒープ上に残っているのに、プログラムから正しく回収できなくなることです。
典型的には、次の2種類に分けて考えると分かりやすいです。

```text
アドレスを失うメモリリーク:
	確保した領域の先頭アドレスを追跡できなくなる

解放し忘れによるメモリリーク:
	アドレスは辿れるのに、不要になっても free() しない
```

どちらも結果としては「ヒープに返すべき領域を返していない」状態です。
短時間で終わる小さなプログラムでは目立たなくても、ループ、サーバ、繰り返し処理、長時間稼働するプロセスでは少しずつメモリを失っていきます。
最終的には、新しい割り当てが失敗したり、性能が劣化したり、プロセスが異常終了したりします。

##### 2章の1の4の1　アドレスの紛失

最も単純なメモリリークは、確保した領域の先頭アドレスを上書きしてしまう例です。

```c
#include <stdlib.h>

int main(void)
{
	int *pi = malloc(sizeof *pi);

	if (pi == NULL) {
		return 1;
	}

	*pi = 5;

	pi = malloc(sizeof *pi);
	if (pi == NULL) {
		return 1;
	}

	free(pi);
	return 0;
}
```

このコードでは、最初の `malloc()` で得た領域のアドレスを `pi` が保持しています。
ところが、その `pi` に対して 2回目の `malloc()` の結果をそのまま代入してしまうと、最初の領域の先頭アドレスが失われます。
すると最初の領域に対して `free()` を呼ぶ手段がなくなり、メモリリークになります。

実務では、`realloc()` の失敗時に似た問題を起こしやすいので注意が必要です。
いったん別の一時ポインタで受け、成功した時だけ元の変数へ反映する、という書き方が重要になります。これは後で詳しく扱います。

文字列走査でも、同種の問題が起きます。

```c
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(void)
{
	char *name = malloc(strlen("Susan") + 1);

	if (name == NULL) {
		return 1;
	}

	strcpy(name, "Susan");

	while (*name != '\0') {
		putchar(*name);
		name++;
	}

	return 0;
}
```

このコードでは、`name++` によってポインタ変数そのものを進めています。
その結果、ループ終了時には `name` は元の先頭アドレスを指していません。
このままでは `free(name)` も正しく呼べず、確保した領域の先頭アドレスを失います。

このような場合は、先頭アドレスを保持する変数と、走査用の変数を分けるのが安全です。

```c
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(void)
{
	char *name = malloc(strlen("Susan") + 1);

	if (name == NULL) {
		return 1;
	}

	strcpy(name, "Susan");

	for (char *cursor = name; *cursor != '\0'; cursor++) {
		putchar(*cursor);
	}

	free(name);
	return 0;
}
```

`name` は解放用に保持し、`cursor` を走査専用に使うことで、所有権と走査位置を混同せずに済みます。

##### 2章の1の4の2　解放し忘れによるリーク

アドレスは失っていなくても、不要になった領域を解放しなければメモリリークです。
単純な例として、ループのたびに領域を確保するのに、一度も `free()` しないコードを考えます。

```c
#include <stdio.h>
#include <stdlib.h>

int main(void)
{
	for (;;) {
		char *chunk = malloc(1000000);

		if (chunk == NULL) {
			fputs("allocation failed\n", stderr);
			break;
		}

		puts("allocated");
	}

	return 0;
}
```

この例では、各反復で新しい 1,000,000 バイトの領域を確保しますが、以前の領域を返却していません。
しかも `chunk` はループの次回反復で新しい値に上書きされるため、前回の領域のアドレスも失われます。
結果として、割り当て可能なメモリは徐々に減っていき、やがて確保に失敗します。

もう1つ重要なのは、構造体の内部に動的領域をぶら下げている場合です。
たとえば、構造体本体を `free()` しても、その構造体のメンバが指していた別の動的領域は自動では解放されません。
そのため、複合データ構造では「外側を解放する前に内側を解放する」という順序が重要になります。
この問題は、後の章で構造体と所有権の話と合わせて詳しく整理します。

ここまでの段階で押さえるべきなのは、次の点です。

```text
malloc() の戻り値は NULL かどうか確認する

必要なサイズは sizeof で求める

Cでは malloc() の戻り値を通常はキャストしない

確保した範囲の外へ読んだり書いたりしてはいけない

先頭アドレスを失うと free() できなくなる

不要になった動的領域は、責任を持って解放する
```

この土台が固まると、`calloc()` によるゼロ初期化、`realloc()` による再確保、`free()` 後の `NULL` 代入、ぶら下がりポインタ対策などが理解しやすくなります。

