---
title: "UmuOSの為のC言語２（中級）　２章　ファイルI/O"
---

# UmuOSの為のC言語（中級）　２

このノートは、UmuOSを進化させるためにプロセス管理・メモリ管理・ファイルI/O・シェル・システムコール設計に
直接役立つ形で抽象化し、再構成することであります。
すなわち、UmuOSの構造へ還元するための実践ノートであります。
LinuxのAPIやシステムコールは、OSがどのようにふるまうかでもあり、その構造を理解することはUmuOSの設計力を
高めることに直結するとおもう。

## ２章　ファイルI/O

この章では、LinuxのファイルI/Oの基本を整理します。

LinuxやUnix系OSでは、ファイルI/Oはかなり中心的な仕組みです。
通常ファイルを読む、ログを書く、端末へ表示する、パイプで別プロセスへ渡す、ソケットで通信する、デバイスを操作する、という処理の多くが、ファイルディスクリプタを通して扱われます。

UmuOSの視点では、この章はかなり重要です。
なぜなら、シェル、リダイレクト、パイプ、ログ出力、設定ファイル読み込み、デバイス入出力などが、最終的には fd、open()、read()、write()、close() の理解に集約されていくからです。

この章では、まず低レベルI/Oを中心に見ていきます。
標準Cライブラリの FILE、fopen()、fgets()、fprintf() などは便利ですが、その下にはファイルディスクリプタを使う低レベルI/Oがあります。
UmuOSやUshの設計を考える場合は、まずこちらを押さえると見通しが良くなります。

### ２章の１　ファイルI/Oの基本

ファイルを読み書きするには、まずファイルを開く必要があります。
Linuxでは、ファイルを開くとカーネルがそのファイルをプロセスごとに管理し、ユーザープログラムにはファイルディスクリプタという整数を返します。

ファイルディスクリプタは、file descriptor の略で fd と呼ばれることが多いです。
C言語では普通の `int` として扱います。
fd専用の特別な型があるわけではありません。

たとえば、ファイルを開くと次のような流れになります。

```text
ユーザープログラム
    open() を呼ぶ

カーネル
    ファイルを開く
    プロセスのfd表に登録する

ユーザープログラム
    fdを受け取る
    以後 read()、write()、close() に fd を渡す
```

ここで重要なのは、ユーザープログラムが直接inodeやディスクブロックを触るわけではない、という点です。
プログラムは fd という番号を持ち、その番号をカーネルに渡すことで、読み書きやクローズを依頼します。

#### ２章の１の１　fdとプロセスのファイルテーブル

プロセスは、自分用のファイルディスクリプタ表を持っています。
この表には、fd番号と、開いているファイルに関する情報が対応付けられます。

イメージとしては、次のようになります。

```text
プロセスAのfd表

fd 0  -> 標準入力
fd 1  -> 標準出力
fd 2  -> 標準エラー出力
fd 3  -> /home/tama/memo.txt
fd 4  -> pipeの読み取り口
```

fdそのものは単なる整数です。
しかし、その先にはカーネルが管理する「開いているファイル」の情報があります。

そこには、たとえば次のような情報が関係します。

```text
ファイルの実体
    inodeやデバイスなど、対象そのものに関係する情報

ファイルポジション
    次にread()やwrite()する位置

アクセスモード
    読み取り専用、書き込み専用、読み書き両用など

フラグ
    O_APPEND、O_NONBLOCK、O_CLOEXEC など
```

ファイルポジションは、読み書きのたびに進む「ファイル内の現在位置」です。
通常ファイルを read() すると、読んだバイト数ぶんだけ位置が進みます。
write() した場合も、書いたバイト数ぶんだけ位置が進みます。

UmuOSでファイルI/Oを考える場合も、単に「ファイルを開く」だけでは足りません。
少なくとも、プロセスごとのfd表、fd番号、ファイルポジション、アクセスモード、対象ファイルへの参照を管理する必要があります。

#### ２章の１の２　親子プロセスとfd

Linuxでは、fork() で子プロセスを作ると、子プロセスは親プロセスのfdを受け継ぎます。

これは、シェルやパイプを理解するうえで非常に重要です。
たとえば、シェルが pipe() でfdを作り、fork() で子プロセスを作ると、子プロセスもそのfdを持った状態で開始できます。
その後、dup2() で標準入力や標準出力につなぎ替え、exec() で別のコマンドへ置き換えることで、パイプやリダイレクトが実現できます。

イメージとしては、次のようになります。

```text
親プロセス
    fd 3 -> file.txt

fork() 後の子プロセス
    fd 3 -> file.txt
```

ただし、親と子は別々のプロセスです。
片方が close(fd) しても、もう片方のfd表から同じfd番号が自動的に消えるわけではありません。
一方で、fork()直後は同じ「開いているファイル」を参照するため、ファイルポジションなどを共有する場合があります。

ここは少し混乱しやすいです。
最初は、次のように考えると分かりやすいです。

```text
fd表
    プロセスごとにある

開いているファイルの状態
    fork()後に親子で共有されることがある

close(fd)
    そのプロセスのfd表からfdを外す
```

スレッドの場合は、同じプロセス内で動くため、基本的にfd表を共有します。
そのため、あるスレッドが close(fd) すると、同じプロセス内の別スレッドにも影響します。

#### ２章の１の３　標準入力・標準出力・標準エラー出力

通常、プロセスは起動時点で少なくとも3つのfdを持っています。

```text
fd 0
    標準入力
    stdin
    STDIN_FILENO

fd 1
    標準出力
    stdout
    STDOUT_FILENO

fd 2
    標準エラー出力
    stderr
    STDERR_FILENO
```

C言語では、直接 `0`、`1`、`2` と書くこともできます。
しかし、意味が分かりやすいので、低レベルI/Oでは `STDIN_FILENO`、`STDOUT_FILENO`、`STDERR_FILENO` を使う方が読みやすいです。
これらは `<unistd.h>` で定義されています。

たとえば、標準出力へ文字列を書く最小例は次のようになります。

```c
#include <unistd.h>

int main(void) {
    write(STDOUT_FILENO, "hello\n", 6);

    return 0;
}
```

ただし、実際のプログラムでは write() の戻り値確認が必要です。
システムプログラミングでは、I/Oは失敗するものとして扱うのが基本です。

少し丁寧に書くと、次のようになります。

```c
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

int main(void) {
    const char message[] = "hello\n";
    ssize_t n;

    n = write(STDOUT_FILENO, message, sizeof(message) - 1);

    if (n < 0) {
        perror("write");
        exit(1);
    }

    return 0;
}
```

ここで `ssize_t` は、成功時には書き込んだバイト数、失敗時には `-1` を返せる符号付きの型です。
read() や write() の戻り値でよく出てきます。

#### ２章の１の４　fdが指すもの

fdが指すものは、通常ファイルだけではありません。
Unix系OSの「すべてのものはファイルである」という考え方により、いろいろな対象がfdで扱えます。

```text
通常ファイル
    テキストファイル、バイナリファイル、ログファイルなど

端末
    キーボード入力や画面出力

パイプ
    プロセス間でバイト列を流す通信路

FIFO
    ファイルシステム上に名前を持つパイプ

ソケット
    プロセス間通信やネットワーク通信

デバイスファイル
    ディスク、端末、乱数デバイスなど
```

この統一感がLinuxの強いところです。
UmuOSでも、端末、パイプ、ファイル、デバイスをできるだけ同じfdインターフェースで扱えるようにすると、シェルやコマンドの設計がかなりきれいになります。

### ２章の２　ファイルのオープン

ファイルを使う流れは、かなり基本的には次の形になります。

```text
open()
    ファイルを開いてfdを得る

read() または write()
    fdを使って読み書きする

close()
    fdを閉じる
```

現在のLinuxプログラミングでは、ファイル作成も含めて `open()` を使うことが多いです。
古いコードでは `creat()` が出てくることもありますが、`creat(path, mode)` は、概念的には `open(path, O_WRONLY | O_CREAT | O_TRUNC, mode)` に近いものです。
新しく書くコードでは、意図が分かりやすい `open()` を使う方がよいと思います。

#### ２章の２の１　open()

open() は、パス名で指定したファイルを開き、fdを返します。

```c
#include <fcntl.h>
#include <sys/stat.h>
#include <sys/types.h>
#include <unistd.h>

int open(const char *path, int flags);
int open(const char *path, int flags, mode_t mode);
```

`path` は開きたいファイルのパスです。
`flags` には、読み取り用なのか、書き込み用なのか、作成するのか、追記するのか、といった指定を渡します。

戻り値は次のようになります。

```text
成功
    0以上のfd

失敗
    -1
    errno に理由が入る
```

ファイルを読み取り専用で開く例です。

```c
#include <errno.h>
#include <fcntl.h>
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

int main(void) {
    int fd;

    fd = open("/home/tama/memo.txt", O_RDONLY);

    if (fd < 0) {
        perror("open");
        exit(1);
    }

    close(fd);

    return 0;
}
```

古いサンプルでは、存在しないユーザー名や架空のパスを使う例が出てくることがあります。
このノートでは、UmuOS研究の文脈に合わせて `/home/tama/memo.txt` のような分かりやすいパスにしています。
実際に試すときは、自分の環境に存在するファイルへ変更してください。

#### ２章の２の２　open()の基本フラグ

open() の `flags` には、まず次のどれか1つを指定します。

```text
O_RDONLY
    読み取り専用で開く

O_WRONLY
    書き込み専用で開く

O_RDWR
    読み書き両用で開く
```

読み取り専用で開いたfdには、基本的に write() できません。
書き込み専用で開いたfdからは、基本的に read() できません。

ファイルを作成し、書き込み用に開く例です。

```c
#include <errno.h>
#include <fcntl.h>
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

int main(void) {
    int fd;

    fd = open("memo.txt", O_WRONLY | O_CREAT | O_TRUNC | O_CLOEXEC, 0644);

    if (fd < 0) {
        perror("open");
        exit(1);
    }

    if (write(fd, "hello\n", 6) < 0) {
        perror("write");
        close(fd);
        exit(1);
    }

    if (close(fd) < 0) {
        perror("close");
        exit(1);
    }

    return 0;
}
```

ここでは `O_CLOEXEC` も付けています。
これは現代的にはかなり重要なフラグです。
exec() で別プログラムを起動するとき、不要なfdが子プロセスへ漏れないようにするためです。

Ushのようなシェルを作る場合、fdの継承は便利でもあり危険でもあります。
パイプやリダイレクトに必要なfdは意図的に渡し、不要なfdは渡さない、という設計が必要になります。

#### ２章の２の３　open()でよく使う追加フラグ

open() では、基本のアクセスモードに加えて、ビットORで追加フラグを指定できます。

```text
O_APPEND
    常にファイル末尾へ追記する

O_CREAT
    ファイルがなければ作る

O_EXCL
    O_CREAT と一緒に使い、すでに存在したら失敗させる

O_TRUNC
    既存ファイルを開いたときにサイズを0へ切り詰める

O_CLOEXEC
    exec() 時にfdを自動的に閉じる

O_DIRECTORY
    対象がディレクトリでなければ失敗させる

O_NOFOLLOW
    最後の要素がシンボリックリンクなら失敗させる

O_NONBLOCK
    可能ならノンブロッキングモードで開く

O_SYNC
    書き込みを同期的に行う

O_DIRECT
    ページキャッシュをなるべく避けるダイレクトI/O用に開く
```

古い説明では `O_LARGEFILE` が大きく扱われることがあります。
しかし、現在の64ビットLinuxでは大きなファイルを扱うことは通常の前提になっており、`O_LARGEFILE` を意識する場面はかなり減っています。
32ビット環境で大きなファイルを扱う場合は、`_FILE_OFFSET_BITS=64` を使う話が出てきますが、まずは「現代の64ビットLinuxでは通常あまり意識しない」と理解しておけばよいと思います。

また、古い説明では `O_ASYNC` が紹介されることがあります。
これはI/O可能になったときにシグナルを受け取るための仕組みですが、現在の一般的なイベント駆動I/Oでは、`poll()`、`epoll()`、場合によっては `io_uring` などを検討することが多いです。
最初の段階では、`O_ASYNC` は歴史的・特殊用途寄りとして見ておけばよいと思います。

##### ２章の２の３の１　O_CREATとO_EXCL

新しいファイルを作るときは、`O_CREAT` を使います。
このとき、第3引数にパーミッションを指定します。

```c
fd = open("memo.txt", O_WRONLY | O_CREAT | O_CLOEXEC, 0644);
```

ただし、ファイルがすでに存在する場合に上書きしたくないときは、`O_EXCL` も一緒に使います。

```c
fd = open("memo.txt", O_WRONLY | O_CREAT | O_EXCL | O_CLOEXEC, 0644);
```

この指定では、`memo.txt` がすでに存在すると open() は失敗します。
一時ファイルやロックファイルを作るときなど、競合を避けたい場面で重要になります。

##### ２章の２の３の２　O_APPEND

`O_APPEND` は、書き込みのたびにファイル末尾へ追記するためのフラグです。

ログファイルのように、複数のプロセスから追記したい場合に便利です。
単に lseek() で末尾へ移動してから write() するよりも、`O_APPEND` を使う方が安全です。
なぜなら、末尾へ移動する処理と書き込み処理を、カーネルが一連の操作として扱えるからです。

```c
#include <fcntl.h>
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

int main(void) {
    int fd;

    fd = open("app.log", O_WRONLY | O_CREAT | O_APPEND | O_CLOEXEC, 0644);

    if (fd < 0) {
        perror("open");
        exit(1);
    }

    if (write(fd, "started\n", 8) < 0) {
        perror("write");
        close(fd);
        exit(1);
    }

    close(fd);

    return 0;
}
```

UmuOSでログ機能を考える場合も、追記モードはかなり大事です。
単純に「最後へ移動して書く」だけでは、複数プロセスが同時に書いたときに競合する可能性があります。

##### ２章の２の３の３　O_TRUNC

`O_TRUNC` は、既存ファイルを開いたときに中身を空にするフラグです。

たとえば、次の指定では、`memo.txt` が存在していればサイズを0にしてから書き込み用に開きます。
存在しない場合は、`O_CREAT` があるので新しく作ります。

```c
fd = open("memo.txt", O_WRONLY | O_CREAT | O_TRUNC | O_CLOEXEC, 0644);
```

注意点として、`O_TRUNC` は破壊的です。
既存の内容が消えます。
設定ファイルや重要なデータを扱う場合は、安易に使わない方がよいです。

##### ２章の２の３の４　O_NONBLOCK

`O_NONBLOCK` は、可能ならI/Oで待たないようにするフラグです。

通常ファイルではあまり意識しません。
一方で、FIFO、ソケット、端末などでは重要になります。
データが来るまで read() が待ち続ける、接続できるまで待つ、といった動作を避けたい場合に使います。

Ushのようなシェルの基本実装では、最初から `O_NONBLOCK` を多用する必要はありません。
ただし、将来的にジョブ制御、イベントループ、非同期I/Oを扱う場合には重要になります。

##### ２章の２の３の５　O_SYNCとO_DIRECT

`O_SYNC` は、書き込みをディスクへ同期させたい場合に使います。
通常、write() が成功しても、その時点で物理ディスクへ完全に書き込まれているとは限りません。
カーネルやファイルシステムのキャッシュに入っている場合があります。

`O_SYNC` を使うと安全性は上がりますが、性能は落ちやすいです。
ログ、データベース、ジャーナリングなど、データ消失をどこまで許容するかによって判断する領域です。

`O_DIRECT` は、ページキャッシュをなるべく使わずにI/Oするためのフラグです。
ただし、バッファのアラインメントやサイズ制約があり、初心者向けではありません。
通常のアプリケーションでは、まず普通の read()、write()、必要に応じて fsync() を理解する方が先です。

#### ２章の２の４　新規作成したファイルのオーナ

`open()` に `O_CREAT` を付けて新しいファイルを作ると、そのファイルにはオーナとグループが設定されます。

まず、オーナは分かりやすいです。
新規作成したファイルのオーナは、そのファイルを作成したプロセスの実効ユーザーIDになります。

```text
プロセスの実効uid
    新しく作られたファイルのオーナになる
```

ここで出てくる実効uidは、effective user ID のことです。
普段の単純なプログラムでは、ログインしているユーザーのuidと同じだと思ってよいです。
ただし、setuidプログラムなどでは、実ユーザーIDと実効ユーザーIDが異なる場合があります。
パーミッション判定では、この実効uidが重要になります。

一方で、ファイルのグループは少しだけ複雑です。
Linuxの標準的な動作では、新規作成したファイルのグループは、そのファイルを作成したプロセスの実効グループIDになります。

```text
Linuxの標準的な動作
    プロセスの実効gid
        -> 新しく作られたファイルのグループになる
```

これはSystem V系の動作に合わせたものです。
Linuxは多くの部分でSystem V系Unixの流れを受け継いでいるため、この動作が標準として扱われています。

ただし、BSD系Unixでは考え方が違います。
BSD系では、新しく作られたファイルのグループは、親ディレクトリのグループIDを受け継ぎます。

```text
BSD系の動作
    親ディレクトリのgid
        -> 新しく作られたファイルのグループになる
```

Linuxでも、親ディレクトリに setgid ビットが立っている場合は、BSD系に近い動作になります。
つまり、そのディレクトリの中に作られたファイルやディレクトリは、親ディレクトリのグループを受け継ぎます。

これは、共同作業用ディレクトリでよく使われます。
たとえば、複数ユーザーが同じグループで作業するディレクトリでは、作成者ごとの主グループではなく、作業ディレクトリのグループを引き継いでほしいことがあります。

```text
共同作業ディレクトリ
    group: dev
    setgidあり

その中で新規作成したファイル
    group: dev
```

UmuOSの初期段階では、ここまで複雑なグループ継承をすぐに実装する必要はないかもしれません。
まずは「新規作成ファイルのオーナは作成プロセスの実効uid」「グループは実効gidを使うのが基本」と押さえれば十分です。
そのうえで、将来的にマルチユーザーOSとして育てるなら、親ディレクトリのsetgidによるグループ継承を設計候補に入れるとよいです。

#### ２章の２の５　新規作成したファイルのパーミッション

`open()` は、使い方によって第3引数 `mode` を取ります。
この `mode` は、新しく作るファイルのパーミッションを指定するための値です。

```c
int open(const char *path, int flags);
int open(const char *path, int flags, mode_t mode);
```

ここで大事なのは、`mode` が必要になるのは `O_CREAT` を指定したときだけ、という点です。
すでに存在するファイルを開くだけなら、`mode` は使われません。

```text
O_CREAT なし
    mode は不要

O_CREAT あり
    mode が必要
```

`O_CREAT` を指定したのに `mode` を渡し忘れると、C言語としてもシステムプログラミングとしても危険です。
関数呼び出しの形としてはコンパイルできてしまう場合がありますが、open() 側は第3引数があるものとして値を読もうとするため、意図しないパーミッションでファイルが作られる可能性があります。

そのため、`O_CREAT` を使うときは必ず第3引数を書く、と覚えておくのが安全です。

```c
fd = open("memo.txt", O_WRONLY | O_CREAT | O_TRUNC | O_CLOEXEC, 0644);
```

`0644` は8進数です。
C言語では、先頭に `0` が付いた整数リテラルは8進数として扱われます。
そのため、`0644` は10進数の644ではなく、Unixのパーミッション表記としての `rw-r--r--` を表します。

```text
0644
    オーナ: 読み取り + 書き込み
    グループ: 読み取りのみ
    その他: 読み取りのみ

0600
    オーナ: 読み取り + 書き込み
    グループ: 権限なし
    その他: 権限なし

0755
    オーナ: 読み取り + 書き込み + 実行
    グループ: 読み取り + 実行
    その他: 読み取り + 実行
```

数値で書く代わりに、POSIXで定義されている定数をビットORで組み合わせることもできます。
定数を使うと、どの権限を与えているのかが読みやすくなります。

```text
S_IRWXU
    オーナに対する読み取り、書き込み、実行許可

S_IRUSR
    オーナに対する読み取り許可

S_IWUSR
    オーナに対する書き込み許可

S_IXUSR
    オーナに対する実行許可

S_IRWXG
    グループに対する読み取り、書き込み、実行許可

S_IRGRP
    グループに対する読み取り許可

S_IWGRP
    グループに対する書き込み許可

S_IXGRP
    グループに対する実行許可

S_IRWXO
    その他のユーザーに対する読み取り、書き込み、実行許可

S_IROTH
    その他のユーザーに対する読み取り許可

S_IWOTH
    その他のユーザーに対する書き込み許可

S_IXOTH
    その他のユーザーに対する実行許可
```

たとえば、`0644` 相当を定数で書くと次のようになります。

```c
S_IRUSR | S_IWUSR | S_IRGRP | S_IROTH
```

これは次の意味です。

```text
S_IRUSR
    オーナに読み取りを許可

S_IWUSR
    オーナに書き込みを許可

S_IRGRP
    グループに読み取りを許可

S_IROTH
    その他のユーザーに読み取りを許可
```

ファイル作成の例です。

```c
#include <fcntl.h>
#include <stdio.h>
#include <stdlib.h>
#include <sys/stat.h>
#include <unistd.h>

int main(void) {
    int fd;
    mode_t mode;

    mode = S_IRUSR | S_IWUSR | S_IRGRP | S_IROTH;

    fd = open("memo.txt", O_WRONLY | O_CREAT | O_TRUNC | O_CLOEXEC, mode);

    if (fd < 0) {
        perror("open");
        exit(1);
    }

    if (close(fd) < 0) {
        perror("close");
        exit(1);
    }

    return 0;
}
```

この例では `mode` として `0644` 相当を渡しています。
ただし、実際にディスク上へ作られるファイルのパーミッションは、必ずしも `mode` そのままではありません。
ここで `umask` が関係します。

##### ２章の２の５の１　umask

`umask` は、ファイル作成時に「落とす権限」を指定する仕組みです。
正式には file creation mask と呼ばれます。

新規作成されるファイルの最終的なパーミッションは、だいたい次の式で決まります。

```text
実際のパーミッション = mode & ~umask
```

たとえば、プログラムが `0666` を指定し、umask が `0022` だったとします。

```text
mode
    0666

umask
    0022

実際のパーミッション
    0666 & ~0022
    0644
```

つまり、プログラムが「みんな読み書きできるファイル」を作ろうとしても、umask がグループとその他ユーザーの書き込み権限を落とすため、実際には `0644` になります。

よくあるumaskは `0022` です。
この場合、普通のファイルは `0644`、ディレクトリは `0755` になりやすいです。

```text
ファイル作成時によく使う指定
    mode: 0666
    umask: 0022
    result: 0644

ディレクトリ作成時によく使う指定
    mode: 0777
    umask: 0022
    result: 0755
```

なぜファイル作成で `0666` が多いかというと、通常ファイルには実行ビットを最初から付けないのが普通だからです。
実行ファイルやスクリプトとして使う場合は、必要に応じて `chmod` などで実行権限を付けます。

UmuOSの設計でも、umaskはユーザー空間の安全装置として考えると分かりやすいです。
カーネルやファイルシステムは `mode` をそのまま使うのではなく、プロセスが持つumaskで権限を削ってから最終的なパーミッションを決めます。

##### ２章の２の５の２　modeとアクセスモードは別物

ここで混乱しやすい点があります。
`open()` の `flags` に指定する `O_RDONLY`、`O_WRONLY`、`O_RDWR` と、第3引数の `mode` は別物です。

```text
flags のアクセスモード
    今回開いたfdで、読み取りできるか、書き込みできるかを決める

mode のパーミッション
    新しく作るファイルに、誰が読み書き実行できるかを決める
```

たとえば、次のような指定も形としては可能です。

```c
fd = open("memo.txt", O_WRONLY | O_CREAT | O_TRUNC | O_CLOEXEC, 0444);
```

これは少し変に見えます。
`O_WRONLY` で書き込み用に開いているのに、作成するファイルのパーミッションは `0444`、つまり読み取り専用だからです。

しかし、これは矛盾ではありません。
`O_WRONLY` は「今回返されるfdで書けるか」を決めます。
`0444` は「作成されたファイルが、その後どのようなパーミッションを持つか」を決めます。

この違いはUmuOSでも重要です。
fdのアクセスモードと、ファイルシステム上のパーミッションは、同じものではありません。

#### ２章の２の６　creat()システムコール

古いUnix系のサンプルでは、`creat()` というシステムコールが出てきます。
名前は `create` ではなく `creat` です。
最後の `e` がありません。

`creat()` は、次の組み合わせを短く書くための関数です。

```text
O_WRONLY | O_CREAT | O_TRUNC
```

宣言は次のようになります。

```c
#include <fcntl.h>
#include <sys/stat.h>
#include <sys/types.h>

int creat(const char *path, mode_t mode);
```

たとえば、次のコードは `file` で指定されたファイルを書き込み用に作成または切り詰めます。

```c
int fd;

fd = creat(file, 0644);

if (fd < 0) {
    /* error */
}
```

これは、次の `open()` とほぼ同じ意味です。

```c
int fd;

fd = open(file, O_WRONLY | O_CREAT | O_TRUNC, 0644);

if (fd < 0) {
    /* error */
}
```

さらに、考え方としてはユーザー空間でも次のように書けます。

```c
int creat(const char *path, mode_t mode) {
    return open(path, O_WRONLY | O_CREAT | O_TRUNC, mode);
}
```

ただし、現代のコードでは、基本的には `open()` を使う方が分かりやすいです。
なぜなら、`open()` の方が指定しているフラグが明示されるからです。

特に今のLinuxプログラムでは、`O_CLOEXEC` を付けたい場面が多くあります。
`creat()` では `O_CLOEXEC` を同時に指定できません。
そのため、fd漏れを避けたい現代的なコードでは、次のように `open()` を使う方が実用的です。

```c
fd = open(file, O_WRONLY | O_CREAT | O_TRUNC | O_CLOEXEC, 0644);
```

`creat()` は歴史的な互換性のために残っているAPI、と考えるとよいです。
古い本や古いコードを読むためには知っておく価値があります。
しかし、UmuOSやUshの新しいサンプルを書くなら、基本は `open()` に寄せるのが読みやすいと思います。

#### ２章の２の７　open() と creat() の戻り値と errno

`open()` と `creat()` は、成功するとファイルディスクリプタを返します。
失敗すると `-1` を返し、失敗理由を `errno` に設定します。

```text
成功
    0以上のfdを返す

失敗
    -1を返す
    errno に失敗理由が入る
```

ここで注意したいのは、正常なfdとして `0` が返る可能性があることです。
標準入力が閉じられている状態で `open()` すると、空いている最小のfdとして `0` が返ることがあります。
そのため、エラー判定は `fd == -1` または `fd < 0` で行います。

```c
int fd;

fd = open("memo.txt", O_RDONLY | O_CLOEXEC);

if (fd < 0) {
    perror("open");
    exit(1);
}
```

次のような判定はよくありません。

```c
if (!fd) {
    /* wrong */
}
```

なぜなら、`fd` が `0` のとき、本当は成功しているのに失敗扱いしてしまうからです。

`errno` は、直前のシステムコールやライブラリ関数が失敗した理由を表すための変数です。
ただし、成功したときに `errno` が自動的に0へ戻るとは限りません。
そのため、`errno` は「戻り値が失敗を示したときだけ読む」と考えるのが基本です。

```text
正しい考え方
    戻り値を見る
    失敗だった場合だけ errno を見る

避けたい考え方
    errno だけを見て成功・失敗を判断する
```

open() の失敗理由としては、たとえば次のようなものがあります。

```text
ENOENT
    指定したファイルや途中のディレクトリが存在しない

EACCES
    パーミッションがなく、開けない

EEXIST
    O_CREAT | O_EXCL 指定時に、ファイルがすでに存在した

EISDIR
    ディレクトリを、通常ファイルのように書き込み用で開こうとした

EMFILE
    そのプロセスで開けるfd数の上限に達した

ENFILE
    システム全体で開けるファイル数の上限に達した
```

実用上は、まず `perror()` でエラー理由を表示できるようにしておくと分かりやすいです。

```c
#include <fcntl.h>
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

int main(void) {
    int fd;

    fd = open("memo.txt", O_RDONLY | O_CLOEXEC);

    if (fd < 0) {
        perror("open memo.txt");
        exit(1);
    }

    if (close(fd) < 0) {
        perror("close");
        exit(1);
    }

    return 0;
}
```

UmuOSの設計でも、システムコールは単に「成功・失敗」を返すだけでは足りません。
失敗したときに、ユーザープログラムが原因を区別できる必要があります。
Linuxの `errno` は、そのための代表的な設計です。

### ２章の３　ファイル読み取り: read()

ファイルを開いてfdを得たら、次は読み取りです。
Linuxで低レベルにファイルを読み取る基本のシステムコールが `read()` です。

```c
#include <unistd.h>

ssize_t read(int fd, void *buf, size_t count);
```

意味は次の通りです。

```text
fd
    読み取り対象のファイルディスクリプタ

buf
    読み取ったデータを入れるバッファ

count
    最大で何バイト読み取るか

戻り値
    実際に読み取ったバイト数
    EOFなら0
    エラーなら-1
```

`read()` は、fdが指す対象から最大 `count` バイトを読み取り、`buf` にコピーします。
通常ファイルの場合は、現在のファイルポジションから読み取り、読み取ったバイト数ぶんファイルポジションが進みます。

```text
read(fd, buf, 100)
    現在位置から最大100バイト読む
    実際に読めたバイト数を返す
    読めた分だけファイルポジションが進む
```

ただし、端末、パイプ、ソケット、キャラクタデバイスなどは、通常ファイルのようなシーク可能なファイルポジションを持たない場合があります。
その場合でも、`read()` は「今読める入力ストリームから読む」という形で使えます。

#### ２章の３の１　read() の最小例

まずは、ファイルから読み取って標準出力へ表示する小さな例です。

```c
#include <fcntl.h>
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

int main(void) {
    char buf[128];
    int fd;
    ssize_t n;

    fd = open("memo.txt", O_RDONLY | O_CLOEXEC);

    if (fd < 0) {
        perror("open");
        exit(1);
    }

    n = read(fd, buf, sizeof(buf));

    if (n < 0) {
        perror("read");
        close(fd);
        exit(1);
    }

    if (n > 0) {
        if (write(STDOUT_FILENO, buf, n) < 0) {
            perror("write");
            close(fd);
            exit(1);
        }
    }

    if (close(fd) < 0) {
        perror("close");
        exit(1);
    }

    return 0;
}
```

この例では、最大128バイトを読み取ります。
重要なのは、`read()` の戻り値 `n` をそのまま `write()` のバイト数に使っている点です。
`buf` 全体を必ず128バイト読み取れるとは限りません。

#### ２章の３の２　read() の戻り値

`read()` の戻り値は、かなり重要です。
戻り値の意味を雑に扱うと、ファイルI/Oのバグがすぐに出ます。

```text
戻り値 == count
    要求したバイト数をすべて読み取れた

0 < 戻り値 < count
    一部だけ読み取れた
    エラーとは限らない

戻り値 == 0
    EOF
    つまり、これ以上読み取るデータがない

戻り値 == -1
    エラー
    errno に理由が入る
```

ここで特に大事なのは、`read()` が `count` より小さい正の値を返しても異常とは限らないことです。
たとえば、次のような場合に部分読み取りが起きます。

```text
ファイルの残りが少ない
    100バイト要求したが、残りが30バイトしかなかった

パイプやソケットから読んだ
    今届いている分だけ読めた

端末から読んだ
    ユーザーが入力した1行分だけ読めた

シグナルに割り込まれた
    一部だけ読めたところで処理が戻った
```

つまり、次のようなコードは危険です。

```c
char buf[100];
ssize_t n;

n = read(fd, buf, sizeof(buf));

if (n < 0) {
    perror("read");
    exit(1);
}

/* 危険: 100バイト全部読めたとは限らない */
```

`read()` が成功した場合でも、実際に読めたバイト数は必ず戻り値で確認します。

#### ２章の３の３　EOFと「まだデータがない」は違う

`read()` が `0` を返した場合は EOF です。
EOF は end of file の略で、ファイル終端を意味します。

通常ファイルでは、ファイルの末尾まで読み終わったときに `read()` が `0` を返します。

```text
read() が 0 を返す
    EOF
    読み取るデータがもうない
    エラーではない
```

一方で、パイプ、ソケット、端末などでは、「今はまだデータが届いていない」という状態があります。
ブロッキングモードでは、`read()` はデータが来るまで待ちます。

```text
ブロッキングread()
    データがある
        読んで返る

    データがまだない
        待つ

    EOF
        0を返す
```

つまり、「データがまだない」と「EOF」は違います。
EOFは終わりです。
データがまだない状態は、将来データが来るかもしれないので、ブロッキングread()では待ちます。

#### ２章の３の４　EINTR

`read()` は、シグナルによって中断されることがあります。
1バイトも読み取っていない状態でシグナルに割り込まれた場合、`read()` は `-1` を返し、`errno` に `EINTR` を設定することがあります。

```text
EINTR
    interrupted system call
    システムコールがシグナルで中断された
```

この場合、多くの読み取り処理では、もう一度 `read()` をやり直せます。

```c
ssize_t n;

for (;;) {
    n = read(fd, buf, sizeof(buf));

    if (n < 0 && errno == EINTR) {
        continue;
    }

    break;
}
```

ただし、すべてのプログラムで機械的にやり直せばよいわけではありません。
シグナルを受けたら終了したいプログラムもあります。
まずは「EINTRは、重大なI/O失敗というより、割り込みによる中断を表すことがある」と理解するとよいです。

#### ２章の３の５　指定したバイト数を最後まで読み取る

`read(fd, buf, len)` は、`len` バイトを必ず読み取る関数ではありません。
最大で `len` バイト読む関数です。

そのため、「どうしても `len` バイト分ほしい」という処理では、ループが必要です。
たとえば、固定長ヘッダ、構造体、プロトコルの決まった長さのデータを読むときです。

次の関数は、EOFまたはエラーになるまで、指定されたバイト数をできるだけ読み取ります。

```c
#include <errno.h>
#include <stddef.h>
#include <unistd.h>

ssize_t read_full(int fd, void *buf, size_t len) {
    char *p;
    size_t left;
    ssize_t n;

    p = buf;
    left = len;

    while (left > 0) {
        n = read(fd, p, left);

        if (n < 0) {
            if (errno == EINTR) {
                continue;
            }

            return -1;
        }

        if (n == 0) {
            break;
        }

        p += n;
        left -= n;
    }

    return (ssize_t)(len - left);
}
```

この関数のポイントは、`p` と `left` を更新しているところです。

```text
p
    次に読み込む先の位置

left
    まだ読み取りたい残りバイト数
```

たとえば、100バイト読みたいのに30バイトしか読めなかった場合、次は `buf + 30` の位置へ、残り70バイトを読み取ります。

```text
最初
    p = buf
    left = 100

30バイト読めた後
    p = buf + 30
    left = 70
```

Cでは `void *` のままではバイト単位のポインタ演算をしにくいため、`char *` に変換しています。
`char` は1バイト単位で進むため、読み取りバッファを扱うときに便利です。

使い方は次のようになります。

```c
#include <fcntl.h>
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

int main(void) {
    char header[16];
    int fd;
    ssize_t n;

    fd = open("memo.txt", O_RDONLY | O_CLOEXEC);

    if (fd < 0) {
        perror("open");
        exit(1);
    }

    n = read_full(fd, header, sizeof(header));

    if (n < 0) {
        perror("read_full");
        close(fd);
        exit(1);
    }

    if ((size_t)n != sizeof(header)) {
        fprintf(stderr, "short read: expected %zu bytes, got %zd bytes\n",
                sizeof(header), n);
        close(fd);
        exit(1);
    }

    close(fd);

    return 0;
}
```

この例では、16バイトのヘッダを必ず読みたい、という想定です。
EOFで16バイト未満しか読めなかった場合は、短い読み取りとして扱っています。

#### ２章の３の６　ファイル全体を読む基本ループ

通常ファイルを先頭から最後まで読むなら、「固定バイト数を必ず読む」よりも、「読めるだけ読み、EOFまで繰り返す」形がよく使われます。

次は、`memo.txt` の内容を標準出力へ流す簡単な `cat` 風の例です。

```c
#include <errno.h>
#include <fcntl.h>
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

int main(void) {
    char buf[4096];
    int fd;
    ssize_t n;

    fd = open("memo.txt", O_RDONLY | O_CLOEXEC);

    if (fd < 0) {
        perror("open");
        exit(1);
    }

    for (;;) {
        n = read(fd, buf, sizeof(buf));

        if (n < 0) {
            if (errno == EINTR) {
                continue;
            }

            perror("read");
            close(fd);
            exit(1);
        }

        if (n == 0) {
            break;
        }

        if (write(STDOUT_FILENO, buf, n) < 0) {
            perror("write");
            close(fd);
            exit(1);
        }
    }

    if (close(fd) < 0) {
        perror("close");
        exit(1);
    }

    return 0;
}
```

このコードでは、`read()` が0を返したらEOFなのでループを抜けます。
`read()` が正の値を返したら、そのバイト数だけ標準出力へ書き出します。

ただし、この例の `write()` も本当は部分書き込みを考える必要があります。
読み取りと同じく、書き込みも「要求したバイト数を必ず全部書ける」とは限りません。
この点は、後の `write()` の節で詳しく扱います。

#### ２章の３の７　ノンブロッキングI/Oと EAGAIN

`O_NONBLOCK` を付けてfdを開くと、対象によってはノンブロッキングI/Oになります。
ノンブロッキングI/Oでは、データがまだないときに `read()` が待たずに戻ります。

```text
ブロッキングI/O
    データがなければ待つ

ノンブロッキングI/O
    データがなければ待たずに戻る
```

このとき `read()` は `-1` を返し、`errno` に `EAGAIN` または `EWOULDBLOCK` を設定することがあります。
Linuxでは、この2つは同じ値であることが多いですが、移植性を考えるなら両方を確認するのが無難です。

```text
EAGAIN / EWOULDBLOCK
    今は読み取れるデータがない
    後でもう一度試せる
    重大なI/Oエラーとは限らない
```

簡単な例です。

```c
#include <errno.h>
#include <fcntl.h>
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

int main(void) {
    char buf[1024];
    int fd;
    ssize_t n;

    fd = open("memo.txt", O_RDONLY | O_NONBLOCK | O_CLOEXEC);

    if (fd < 0) {
        perror("open");
        exit(1);
    }

    n = read(fd, buf, sizeof(buf));

    if (n < 0) {
        if (errno == EINTR) {
            fprintf(stderr, "read was interrupted; try again\n");
        } else if (errno == EAGAIN || errno == EWOULDBLOCK) {
            fprintf(stderr, "no data available now; try again later\n");
        } else {
            perror("read");
            close(fd);
            exit(1);
        }
    } else if (n == 0) {
        printf("EOF\n");
    } else {
        printf("read %zd bytes\n", n);
    }

    close(fd);

    return 0;
}
```

通常ファイルでは、`O_NONBLOCK` を付けても大きな意味がないことが多いです。
ノンブロッキングI/Oが重要になるのは、主に端末、パイプ、FIFO、ソケットなどです。

また、ノンブロッキングにしたからプログラムが必ず速くなるわけではありません。
データがないのに何度も `read()` を呼び続けると、CPUを無駄に使うだけになります。
実用的には、`poll()`、`select()`、`epoll()` などで「読める状態になったか」を待つ設計と組み合わせることが多いです。

Ushのようなシェルを最初に作る段階では、普通のブロッキングI/Oで十分な場面が多いです。
ただし、将来的にジョブ制御、疑似端末、イベントループを扱うなら、`EAGAIN` とノンブロッキングI/Oの理解は重要になります。

#### ２章の３の８　read() でよく見る errno

`read()` が `-1` を返した場合は、`errno` に失敗理由が入ります。
代表的なものを整理します。

```text
EBADF
    fdが無効
    または読み取り用に開かれていない

EFAULT
    bufが不正なアドレスを指している

EINVAL
    fdが指す対象や指定がread()に対応していない

EIO
    低レベルのI/Oエラー

EINTR
    シグナルで割り込まれた

EAGAIN / EWOULDBLOCK
    ノンブロッキングI/Oで、今は読み取れるデータがない
```

UmuOSを作る側の視点では、このようなエラーをどこまで細かく実装するかが設計ポイントになります。
最初は `EBADF` や `EINVAL` のような基本的なエラーからでもよいですが、ユーザープログラムが原因を区別できるようにするほど、OSらしい振る舞いに近づきます。

#### ２章の３の９　read() のサイズ上限

`read()` の第3引数 `count` は `size_t` です。
`size_t` は、サイズを表す符号なし整数型です。

一方で、`read()` の戻り値は `ssize_t` です。
`ssize_t` は、`size_t` の符号付き版のような型です。
なぜ符号付きなのかというと、エラー時に `-1` を返す必要があるからです。

```text
size_t
    サイズを表す型
    符号なし

ssize_t
    read() や write() の戻り値でよく使う型
    符号付き
    -1 でエラーを表せる
```

ここで問題になるのは、`count` にあまりにも大きな値を渡した場合です。
POSIXでは、`count` が `SSIZE_MAX` より大きい場合の動作は実装依存です。
つまり、移植性のあるコードでは避けるべきです。

```text
count <= SSIZE_MAX
    扱いやすい範囲

count > SSIZE_MAX
    動作が実装依存
    避ける方がよい
```

汎用的な読み取り関数を書くなら、次のように上限を丸めることがあります。

```c
#include <limits.h>

if (len > SSIZE_MAX) {
    len = SSIZE_MAX;
}
```

また、`count` に0を渡した場合、`read()` は通常0を返します。
何も読み取らないので、ファイルポジションも進みません。

```c
n = read(fd, buf, 0);
```

これはエラーではありませんが、実用的な読み取り処理ではあまり意味がありません。

#### ２章の３の１０　read() の要点

ここまでの `read()` の要点をまとめます。

```text
read() は最大countバイト読む
    countバイト必ず読むわけではない

戻り値が正
    実際に読めたバイト数

戻り値が0
    EOF

戻り値が-1
    エラー
    errnoを見る

EINTR
    シグナルで中断された
    やり直せる場合が多い

EAGAIN / EWOULDBLOCK
    ノンブロッキングI/Oで、今はデータがない

部分読み取り
    正常に起こり得る
    バグではない
```

UmuOSの視点では、`read()` は単に「ファイルから読む関数」ではありません。
通常ファイル、端末、パイプ、ソケット、デバイスを同じfdインターフェースで扱うための入口です。
Ushの標準入力、パイプ、リダイレクトを実装するうえでも、`read()` の戻り値と部分読み取りの扱いはかなり重要になります。

### ２章の４　ファイル書き込み: write()

ファイルへ書き込むときに、もっとも基本になる低レベルI/Oが `write()` です。
`write()` は `read()` と対になるシステムコールです。

`read()` が「fdからバッファへ読む」のに対して、`write()` は「バッファからfdへ書く」と考えると分かりやすいです。

宣言は次のようになります。

```c
#include <unistd.h>

ssize_t write(int fd, const void *buf, size_t count);
```

ここで出てくる `ssize_t write(...)` は、実行コードではなく、関数の形を示すプロトタイプ宣言です。
つまり、`write()` は `ssize_t` 型の値を返す関数、という意味です。

意味は次の通りです。

```text
fd
    書き込み先のファイルディスクリプタ

buf
    書き込みたいデータが入っているバッファ
    const void * なので、write() 側はbufの中身を書き換えない

count
    最大で何バイト書き込むか

戻り値
    実際に書き込んだバイト数
    エラーなら-1
```

`write()` は、`buf` から最大 `count` バイトを取り出し、`fd` が指す対象へ書き込みます。
通常ファイルの場合は、現在のファイルポジションへ書き込み、書き込んだバイト数ぶんだけファイルポジションが進みます。

```text
write(fd, buf, 100)
    bufから最大100バイトを書き込む
    実際に書けたバイト数を返す
    書けた分だけファイルポジションが進む
```

古い説明では、シークできないキャラクタデバイスでは常に「先頭」に書く、という表現が出てくることがあります。
ただ、現在の理解としては、端末、パイプ、ソケット、キャラクタデバイスなどは、通常ファイルのような「何バイト目」というファイルポジションを持たないことが多い、
と考える方が分かりやすいです。
その場合、`write()` は「現在の出力ストリームへデータを流す」ような動作になります。

#### ２章の４の１　write() の最小例

まずは、文字列を標準出力へ書き込む例です。

```c
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>

int main(void) {
    const char *message = "My ship is solid!\n";
    ssize_t n;

    n = write(STDOUT_FILENO, message, strlen(message));

    if (n < 0) {
        perror("write");
        exit(1);
    }

    return 0;
}
```

ここでは、`message` の文字列を標準出力へ書いています。
`strlen(message)` は、文字列の長さをバイト数として数えています。

ただし、このコードは最小例です。
`write()` が成功しても、要求したバイト数を全部書けたとは限りません。
そのため、実用的なコードでは、戻り値を見て「何バイト書けたか」を確認する必要があります。

#### ２章の４の２　write() の戻り値

`write()` の戻り値は、`read()` と同じく `ssize_t` です。
成功時には実際に書き込んだバイト数を返し、失敗時には `-1` を返します。

```text
戻り値 == count
    要求したバイト数をすべて書き込めた

0 < 戻り値 < count
    一部だけ書き込めた
    エラーとは限らない

戻り値 == 0
    1バイトも書き込めなかった
    countが0の場合など

戻り値 == -1
    エラー
    errno に理由が入る
```

`read()` では戻り値 `0` が EOF を意味しました。
しかし、`write()` の戻り値 `0` は EOF ではありません。
書き込みには「ファイル終端に達したから終わり」という考え方は基本的にありません。
通常ファイルなら、末尾を越えて書けばファイルサイズが大きくなります。

ここは混同しやすいです。

```text
read() の 0
    EOF
    もう読めるデータがない

write() の 0
    1バイトも書けなかった
    EOFという意味ではない
```

#### ２章の４の３　一部しか書き込めない場合

`write()` も、要求したバイト数を必ず全部書き込む関数ではありません。
最大で `count` バイト書き込む関数です。

通常ファイルでは、十分な空き容量があり、エラーも起きなければ、要求した分を書けることが多いです。
そのため、古い説明では「通常ファイルでは部分書き込みはあまり気にしなくてよい」と書かれることがあります。

ただし、現代の実用コードでは、`write()` の戻り値をきちんと扱う方が安全です。
特に、パイプ、FIFO、ソケット、端末、ノンブロッキングI/Oでは、部分書き込みが普通に起こり得ます。

たとえば、次のようなコードは不十分です。

```c
unsigned long word = 1720;
size_t count;
ssize_t n;

count = sizeof(word);
n = write(fd, &word, count);

if (n < 0) {
    perror("write");
    exit(1);
}

/* 危険: sizeof(word) バイト全部を書けたとは限らない */
```

このコードでは、`write()` が `sizeof(word)` より小さい正の値を返した場合に対応できません。
つまり、途中までしか書けていないのに、全部書けたつもりで先へ進んでしまいます。

#### ２章の４の４　指定したバイト数を最後まで書き込む

指定したバイト列を最後まで書き込みたい場合は、ループが必要です。
これは `read_full()` の書き込み版のようなものです。

```c
#include <errno.h>
#include <stddef.h>
#include <unistd.h>

ssize_t write_full(int fd, const void *buf, size_t len) {
    const char *p;
    size_t left;
    ssize_t n;

    p = buf;
    left = len;

    while (left > 0) {
        n = write(fd, p, left);

        if (n < 0) {
            if (errno == EINTR) {
                continue;
            }

            return -1;
        }

        if (n == 0) {
            break;
        }

        p += n;
        left -= n;
    }

    return (ssize_t)(len - left);
}
```

この関数の考え方は `read_full()` とほぼ同じです。

```text
p
    次に書き込むデータの位置

left
    まだ書き込みたい残りバイト数
```

たとえば、100バイト書きたいのに30バイトしか書けなかった場合、次は `buf + 30` の位置から、残り70バイトを書き込みます。

```text
最初
    p = buf
    left = 100

30バイト書けた後
    p = buf + 30
    left = 70
```

ここで `const void *` を `const char *` に変換しているのは、バイト単位でポインタを進めたいからです。
`const` が付いているのは、書き込み元のバッファ内容をこの関数が変更しないためです。

使い方は次のようになります。

```c
#include <fcntl.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>

int main(void) {
    const char *message = "hello from write_full\n";
    int fd;
    ssize_t n;

    fd = open("memo.txt", O_WRONLY | O_CREAT | O_TRUNC | O_CLOEXEC, 0644);

    if (fd < 0) {
        perror("open");
        exit(1);
    }

    n = write_full(fd, message, strlen(message));

    if (n < 0) {
        perror("write_full");
        close(fd);
        exit(1);
    }

    if ((size_t)n != strlen(message)) {
        fprintf(stderr, "short write: expected %zu bytes, wrote %zd bytes\n",
                strlen(message), n);
        close(fd);
        exit(1);
    }

    if (close(fd) < 0) {
        perror("close");
        exit(1);
    }

    return 0;
}
```

この例では、`message` の内容を最後まで書けたか確認しています。

#### ２章の４の５　アペンドモード

`O_APPEND` フラグを付けてファイルを開くと、書き込みは現在のファイルポジションではなく、常にファイル末尾に対して行われます。

```c
fd = open("app.log", O_WRONLY | O_CREAT | O_APPEND | O_CLOEXEC, 0644);
```

アペンドモードが重要になる代表例はログファイルです。
複数のプロセスが同じログファイルへ追記する場合、単純に `lseek(fd, 0, SEEK_END)` で末尾へ移動してから `write()` すると競合する可能性があります。

たとえば、次のような流れが起きるかもしれません。

```text
プロセスA
    ファイル末尾へ移動する

プロセスB
    ファイル末尾へ移動する
    ログを書き込む

プロセスA
    さっき見た末尾位置へログを書き込む
```

この場合、プロセスAが見ていた「末尾」は、プロセスBの書き込み後には本当の末尾ではなくなっています。
これが競合状態です。

`O_APPEND` を使うと、カーネルが各 `write()` の直前にファイル末尾へ移動してから書き込みます。
この「末尾へ移動して書く」という処理が、他の書き込みに割り込まれない形で扱われるため、ログの追記に向いています。

```text
O_APPENDなし
    lseek() と write() の間に別プロセスが割り込む可能性がある

O_APPENDあり
    write() のたびにカーネルが末尾へ追記する
```

ただし、`O_APPEND` を使えば複数プロセスのログが必ずきれいな行単位で混ざらない、という意味ではありません。
1回の `write()` の単位、ファイルシステム、ログのサイズ、実装条件によって考えるべきことがあります。
ログを書くなら、1行をできるだけ1回の `write()` で出す、という設計が分かりやすいです。

UmuOSでログ機能を作る場合も、`O_APPEND` 相当の考え方は重要です。
単に「ファイル末尾へカーソルを移動してから書く」だけだと、複数プロセス対応のときに競合が出ます。

#### ２章の４の６　ノンブロッキング書き込み

`O_NONBLOCK` を付けてfdを開くと、対象によってはノンブロッキング書き込みになります。
ノンブロッキングI/Oでは、本来なら待つ場面で `write()` が待たずに戻ることがあります。

書き込み側でよくあるのは、パイプやソケットの送信バッファがいっぱいになっている場合です。
ブロッキングI/Oなら空きができるまで待ちます。
ノンブロッキングI/Oなら、`write()` は `-1` を返し、`errno` に `EAGAIN` または `EWOULDBLOCK` を設定することがあります。

```text
EAGAIN / EWOULDBLOCK
    今は書き込めない
    後でもう一度試せる
    重大なI/Oエラーとは限らない
```

通常ファイルでは、ノンブロッキング書き込みを意識する場面は多くありません。
重要になるのは、主にパイプ、FIFO、ソケット、端末などです。

実用的には、`poll()`、`select()`、`epoll()` などで「書き込める状態になったか」を待つ設計と組み合わせます。
データを書けないのに何度も `write()` を呼び続けると、CPUを無駄に使ってしまうからです。

#### ２章の４の７　write() でよく見る errno

`write()` が `-1` を返した場合は、`errno` に失敗理由が入ります。
代表的なものを整理します。

```text
EBADF
    fdが無効
    または書き込み用に開かれていない

EFAULT
    bufが不正なアドレスを指している

EFBIG
    ファイルサイズ上限を超える書き込みをしようとした

EINVAL
    fdが指す対象や指定がwrite()に対応していない

EIO
    低レベルのI/Oエラー

ENOSPC
    ファイルシステムの空き容量が足りない

EINTR
    シグナルで割り込まれた

EAGAIN / EWOULDBLOCK
    ノンブロッキングI/Oで、今は書き込めない

EPIPE
    パイプやソケットの読み取り側が閉じられている
```

`EPIPE` はシェルやパイプを作るときに重要です。
パイプの読み取り側が閉じられている状態で書き込むと、`write()` は失敗することがあります。
このとき、プロセスには通常 `SIGPIPE` シグナルも送られます。

`SIGPIPE` のデフォルト動作はプロセス終了です。
そのため、プログラムが `SIGPIPE` を無視または処理している場合にだけ、`write()` の戻り値として `-1` と `errno == EPIPE` を観測できることがあります。

これは、シェルのパイプでよく見える現象です。
たとえば、出力側が大量に書いている途中で、読み取り側が先に終了すると、書き込み側に `SIGPIPE` が飛ぶことがあります。

```text
yes | head -n 1

head は1行読んだら終了する
yes はまだ書こうとする
読み取り側が閉じたパイプへ書くため SIGPIPE が発生する
```

Ushを作る場合、パイプ処理では `EPIPE` と `SIGPIPE` の扱いを意識する必要があります。

#### ２章の４の８　write() のサイズ上限

`write()` の第3引数 `count` は `size_t` です。
一方で、戻り値は `ssize_t` です。

`read()` と同じく、`count` にあまりにも大きな値を渡すのは避けるべきです。
POSIXでは、`count` が `SSIZE_MAX` より大きい場合の動作は実装依存です。

```text
count <= SSIZE_MAX
    扱いやすい範囲

count > SSIZE_MAX
    動作が実装依存
    避ける方がよい
```

なお、貼り付け元の古い説明では「未定義」と説明されることがありますが、POSIXの考え方としては実装依存と捉える方が正確です。
いずれにしても、移植性のあるコードでは `SSIZE_MAX` を超える巨大な1回の `write()` は避けます。

`count` に0を渡した場合、通常は `write()` は0を返します。
何も書き込まないので、ファイルポジションも進みません。

```c
n = write(fd, buf, 0);
```

これはエラーではありませんが、実用的な書き込み処理ではあまり意味がありません。

#### ２章の４の９　write() が返ってもディスクへ書けたとは限らない

ここは非常に重要です。
`write()` が成功しても、その時点でデータが物理ディスクへ完全に保存されたとは限りません。

多くの場合、`write()` はユーザープログラムのバッファから、カーネルが管理するページキャッシュへデータをコピーします。
その後、カーネルが都合のよいタイミングで、実際のストレージへ書き出します。
この後から行われる書き出しを、ライトバック、または writeback と呼びます。

```text
アプリケーション
    write(fd, buf, count)

カーネル
    ユーザーバッファからページキャッシュへコピーする
    write() を成功として返す

後で
    dirtyなページをストレージへ書き出す
```

ここで `dirty` とは、メモリ上では変更済みだが、まだストレージへ反映されていない状態を意味します。
日本語では「汚れた」と直訳されることもありますが、実際には「未書き戻しの変更済みデータ」と考えると分かりやすいです。

この仕組みによって、`write()` はかなり速く戻れます。
毎回すぐにディスクへ同期していたら、プログラムはディスクの遅さに強く引っ張られてしまいます。
カーネルは複数の書き込みをまとめたり、順序を調整したりして、効率よくストレージへ書き出します。

ただし、注意点があります。

```text
write() 成功
    カーネルには渡せた
    しかし、物理ディスクへ保存済みとは限らない

システムクラッシュ
    ページキャッシュ上の未書き戻しデータが失われる可能性がある
```

つまり、アプリケーションからは書き込みが成功したように見えても、クラッシュ時にはディスクへ残っていない可能性があります。

また、実際の書き戻し時にI/Oエラーが起きる場合もあります。
このエラーは、最初に `write()` を呼んだプロセスへその場で返せるとは限りません。
なぜなら、エラーが起きる時点では、書き込んだプロセスがすでに終了しているかもしれないからです。

このため、重要なデータでは `fsync()`、`fdatasync()`、`O_SYNC` などを使って、どのタイミングで永続化を保証したいかを明示する必要があります。
この話は、次の同期I/Oの節で詳しく扱うことになります。

現在のLinuxでは、dirtyデータの書き戻しタイミングは `/proc/sys/vm/dirty_expire_centisecs` などのカーネルパラメータにも関係します。
古い資料では `/proc/sys/vm/dirty_expire_centiseconds` と説明されることがありますが、現在一般的に使われる名前は `dirty_expire_centisecs` です。
単位は 1/100 秒です。

ただし、ここを最初から細かく調整する必要はありません。
まずは、`write()` 成功は「カーネルへ渡せた」という意味であり、「ディスクへ完全保存された」という意味ではない、と押さえるのが重要です。

#### ２章の４の１０　write() の要点

ここまでの `write()` の要点をまとめます。

```text
write() は最大countバイト書く
    countバイト必ず書くわけではない

戻り値が正
    実際に書けたバイト数

戻り値が0
    1バイトも書けなかった
    EOFではない

戻り値が-1
    エラー
    errnoを見る

部分書き込み
    パイプ、ソケット、端末、ノンブロッキングI/Oで特に重要

O_APPEND
    write() のたびにファイル末尾へ追記する
    ログ出力で重要

EPIPE / SIGPIPE
    パイプやソケットの読み取り側が閉じられたときに重要

write() 成功
    ディスクへの永続化完了とは限らない
```

UmuOSの視点では、`write()` は単に「文字を出す関数」ではありません。
標準出力、ログ、リダイレクト、パイプ、デバイス出力を同じfdインターフェースで扱うための中心的な入口です。

Ushの `echo hello > file.txt` や `cmd1 | cmd2` の裏側でも、最終的には `write()` 相当の処理が必要になります。
そのとき、部分書き込み、アペンド、パイプ切断、遅延書き込みをどう扱うかが、OSらしい動作の設計ポイントになります。

### ２章の５　同期I/O

前の節で見たように、`write()` が成功しても、その時点でデータが物理ディスクへ完全に保存されたとは限りません。
多くの場合、Linuxカーネルは書き込まれたデータをページキャッシュへ置き、あとでまとめてストレージへ書き戻します。

この遅延書き込みは、普通のプログラムにとってはとても大事な最適化です。
毎回の `write()` で必ずストレージへ同期していたら、ログ出力や一時ファイル作成のたびにプログラムが大きく待たされます。
そのため、現代のLinuxでも、書き込みをいったんメモリ上にためて、効率のよいタイミングでディスクへ反映する仕組みが使われています。

ただし、すべてのプログラムが「あとで書き戻されればよい」と考えられるわけではありません。
たとえば、次のような処理では、どの時点でデータが永続化されたかが重要になります。

```text
データベース
    トランザクションのコミット後にデータを失いたくない

エディタ
    保存完了と表示した直後にクラッシュしても、内容を残したい

パッケージマネージャ
    インストール途中のメタデータ破損を避けたい

ログシステム
    障害直前のログをできるだけ残したい

ファイルシステムやOS開発
    メタデータとデータの順序を明確に制御したい
```

このような場面で使うのが、同期I/O、または synchronized I/O です。
同期I/Oは、性能をある程度犠牲にして、書き込んだデータをより確実なタイミングでストレージへ反映させるための仕組みです。

#### ２章の５の１　同期I/Oが必要になる理由

通常の `write()` は、ざっくり言うと「ユーザープログラムからカーネルへデータを渡す」処理です。
その後、カーネルがページキャッシュ上の dirty データを実際のストレージへ書き戻します。

```text
通常のwrite()

アプリケーション
    write(fd, buf, count)

カーネル
    ページキャッシュへコピーする
    write()を成功として返す

あとで
    dirtyデータをストレージへ書き戻す
```

この方式は高速です。
しかし、`write()` が成功した直後に電源断やカーネルクラッシュが起きると、ページキャッシュ上に残っていたデータは失われる可能性があります。

同期I/Oを使うと、アプリケーションは「この時点までに、必要なデータをストレージへ反映してほしい」とカーネルへ明示できます。
もちろん、同期を増やすほど安全寄りになりますが、そのぶん遅くなります。

```text
安全性を上げる
    同期する回数を増やす
    ただし遅くなりやすい

性能を上げる
    同期する回数を減らす
    ただしクラッシュ時に失う範囲が増えやすい
```

つまり、同期I/Oは「常に使えばよい」ものではありません。
どこまでのデータ消失を許容できるか、どの時点を保存完了と呼びたいかを考えて使うものです。

#### ２章の５の２　fsync()

`fsync()` は、指定したfdに関係するデータとメタデータをストレージへ同期するための関数です。
書き込んだデータをディスクへ書き出したいときに、もっとも基本になる同期用APIです。

宣言は次のようになります。

```c
#include <unistd.h>

int fsync(int fd);
```

ここで出てくる `int fsync(int fd);` も、実行コードではなくプロトタイプ宣言です。
`fsync()` は成功すると `0`、失敗すると `-1` を返し、`errno` に理由を設定します。

```text
成功
    0

失敗
    -1
    errno に理由が入る
```

`fsync(fd)` を呼ぶと、そのfdで参照しているファイルについて、書き込まれたデータと、ファイルサイズや更新時刻などのメタデータをストレージへ反映しようとします。
ここでいうメタデータとは、ファイルの中身そのものではなく、inodeが持つ属性情報のことです。

たとえば、次のような情報が関係します。

```text
ファイルサイズ
    何バイトのファイルか

更新時刻
    いつ内容が更新されたか

割り当て済みブロック
    どのディスクブロックを使っているか

パーミッションや所有者
    誰が読み書きできるか
```

```text
fsync(fd)
    ファイルのデータ
    ファイルに関係するメタデータ
    これらをストレージへ同期する
```

注意点として、`fsync()` は「カーネルとストレージに、必要な書き出しとフラッシュを要求する」ためのAPIです。
しかし、壊れたストレージ、揮発性のディスク内部キャッシュ、仮想化環境、RAIDコントローラなどが絡むと、物理媒体へ本当に到達したかをアプリケーションが完全に証明できるわけではありません。

古い説明では「ディスク内部キャッシュに残っていても、短い間隔で物理的に書き出されるので通常問題ない」とされることがあります。
現在でも普通の用途では過度に心配しすぎる必要はありませんが、データベースやファイルシステムのようにクラッシュ耐性を厳密に考える領域では、ストレージのライトキャッシュ、バリア、flush命令、マウントオプションまで含めて考えることがあります。

最初の理解としては、次のように押さえるとよいです。

```text
write()
    カーネルへデータを渡す

fsync()
    そのファイルのデータとメタデータをストレージへ反映するよう要求する

ただし
    ハードウェアや仮想化環境が正しく応答することまでは、アプリケーションだけでは保証できない
```

簡単な例です。

```c
#include <fcntl.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>

int main(void) {
    const char *message = "important data\n";
    int fd;

    fd = open("important.txt", O_WRONLY | O_CREAT | O_TRUNC | O_CLOEXEC, 0644);

    if (fd < 0) {
        perror("open");
        exit(1);
    }

    if (write(fd, message, strlen(message)) < 0) {
        perror("write");
        close(fd);
        exit(1);
    }

    if (fsync(fd) < 0) {
        perror("fsync");
        close(fd);
        exit(1);
    }

    if (close(fd) < 0) {
        perror("close");
        exit(1);
    }

    return 0;
}
```

この例では、`write()` のあとに `fsync()` を呼んでいます。
これにより、`write()` でカーネルへ渡したデータを、より確実にストレージへ反映させてから処理を進めます。

ただし、このコードも最小例です。
本当に堅牢にするなら、前の節で作った `write_full()` のように部分書き込みへ対応する必要があります。

また、`fsync()` は対象ファイルのデータとメタデータを同期しますが、親ディレクトリのディレクトリエントリまで必ず同期するとは限りません。
ここはかなり重要です。

ディレクトリエントリとは、親ディレクトリの中にある「ファイル名とinode番号の対応」のことです。

```text
ディレクトリエントリ
    ファイル名 -> inode番号

例
    important.txt -> inode 12345
```

たとえば、新しいファイルを作ったり、`rename()` で名前を置き換えたりした場合、ファイル本体だけでなく、親ディレクトリ側の「この名前がこのinodeを指す」という情報も更新されます。
ファイル本体を `fsync(fd)` しても、親ディレクトリの変更がクラッシュ前に保存されていなければ、再起動後にその名前でファイルを見つけられない可能性があります。

そのため、重要なファイル作成や `rename()` 後には、親ディレクトリを開いて、そのディレクトリfdに対しても `fsync()` することがあります。

```c
#include <fcntl.h>
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

int main(void) {
    int dirfd;

    dirfd = open(".", O_RDONLY | O_DIRECTORY | O_CLOEXEC);

    if (dirfd < 0) {
        perror("open directory");
        exit(1);
    }

    if (fsync(dirfd) < 0) {
        perror("fsync directory");
        close(dirfd);
        exit(1);
    }

    if (close(dirfd) < 0) {
        perror("close directory");
        exit(1);
    }

    return 0;
}
```

実際の安全な更新では、ファイル本体の `fsync()` と、親ディレクトリの `fsync()` を組み合わせます。
この話は後の「安全なファイル更新の基本形」でもう一度整理します。

#### ２章の５の３　fdatasync()

`fdatasync()` は、`fsync()` に似ていますが、主にファイルのデータを同期するための関数です。

宣言は次のようになります。

```c
#include <unistd.h>

int fdatasync(int fd);
```

`fsync()` と `fdatasync()` の違いは、メタデータの扱いです。

```text
fsync()
    データと、必要なメタデータを同期する

fdatasync()
    主にデータを同期する
    ただし、データを正しく読むために必要なメタデータは同期される
```

たとえば、ファイルサイズが伸びた場合、その新しいサイズ情報が保存されないと、あとでデータを正しく読めません。
そのようなメタデータは `fdatasync()` でも同期対象になります。

一方で、アクセス時刻や更新時刻のような、ファイル内容を読むために必須ではないメタデータは、`fsync()` より同期範囲が抑えられる場合があります。

```text
データベースやログ
    fdatasync() が向く場合がある

ファイル全体の状態をより強く同期したい
    fsync() を考える
```

ただし、実際の性能差や挙動はファイルシステム、マウントオプション、ストレージ、カーネルによって変わります。
ファイルシステムによっては、`fdatasync()` と `fsync()` をほとんど同じように実装していることもあります。
最初は、`fsync()` は広めに同期、`fdatasync()` はデータ中心に同期、と押さえれば十分です。

#### ２章の５の４　fsync() と fdatasync() の戻り値と errno

`fsync()` と `fdatasync()` は、どちらも成功すると `0` を返します。
エラーが発生した場合は `-1` を返し、`errno` に理由を設定します。

```text
成功
    0

失敗
    -1
    errno に理由が入る
```

代表的な `errno` は次の通りです。

```text
EBADF
    fdが無効
    または同期対象として使えないfdである

EINVAL
    fdが指す対象が同期書き込みに対応していない

EIO
    書き出し中に低レベルのI/Oエラーが発生した
```

古い説明では、`EBADF` は「渡されたfdが書き込み用にオープンされていない」と説明されることがあります。
実用上は、少なくとも「無効なfdでは失敗する」「書き込み対象として扱えないfdでは失敗し得る」と覚えておくとよいです。

`EIO` は特に重要です。
遅延書き込みでは、実際のI/Oエラーが `write()` の時点ではなく、後の `fsync()`、`fdatasync()`、または `close()` の時点で見えることがあります。
そのため、重要な保存処理では、`write()` だけでなく `fsync()` や `close()` の戻り値も確認します。

単純な呼び出しは次のようになります。

```c
int ret;

ret = fsync(fd);

if (ret < 0) {
    perror("fsync");
}
```

古い資料には、`fsync()` が `EINVAL` で失敗した場合だけ `fdatasync()` も試す、というかなり慎重なコードが載っていることがあります。

```c
#include <errno.h>
#include <stdio.h>
#include <unistd.h>

if (fsync(fd) < 0) {
    if (errno == EINVAL) {
        if (fdatasync(fd) < 0) {
            perror("fdatasync");
        }
    } else {
        perror("fsync");
    }
}
```

このコードの考え方は、「基本は `fsync()` を使いたいが、対象によって `fsync()` が使えない場合に `fdatasync()` も試してみる」というものです。
現在のLinuxの一般的なファイルシステムで、通常ファイルを扱うなら、まず `fsync()` または `fdatasync()` のどちらを使うべきかを設計として選ぶ方が普通です。

```text
設定ファイルや置き換え更新
    fsync() を考える

ログやデータ本体中心の同期
    fdatasync() を考える

特殊なfdや特殊なファイルシステム
    EINVAL などのエラー処理も考える
```

UmuOSの視点では、この `errno` 設計も重要です。
同期に対応していない対象へ `fsync()` した場合、単に失敗とするだけでなく、なぜ失敗したかをユーザープログラムへ返せると、OSとして扱いやすくなります。

#### ２章の５の５　sync() と syncfs()

`sync()` は、システム全体のdirtyデータを書き戻すようにカーネルへ依頼する関数です。

宣言は次のようになります。

```c
#include <unistd.h>

void sync(void);
```

`sync()` は、特定のファイルだけではなく、システム全体の未書き戻しデータを対象にします。
昔のUnixでは、シャットダウン前に `sync` コマンドを実行する話がよく出てきました。

```text
fsync(fd)
    特定のfdに関係するファイルを同期する

sync()
    システム全体のdirtyデータを書き戻すよう依頼する
```

現代のアプリケーションで「このファイルを保存したい」という目的なら、普通は `sync()` ではなく `fsync()` や `fdatasync()` を使います。
`sync()` は対象が広すぎるため、自分のファイルの保存完了を細かく制御する用途には向きません。

Linuxには `syncfs()` もあります。
これは、指定したfdが属するファイルシステムを対象に同期する関数です。

```c
#define _GNU_SOURCE
#include <unistd.h>

int syncfs(int fd);
```

`syncfs()` はLinux固有寄りのAPIです。
移植性を重視する通常のPOSIX的なコードでは、まず `fsync()` と `fdatasync()` を押さえるのがよいです。

#### ２章の５の６　O_SYNC と O_DSYNC

`open()` のフラグとして `O_SYNC` や `O_DSYNC` を指定すると、そのfdへの書き込みを同期的に行うようにできます。

```c
fd = open("important.log", O_WRONLY | O_CREAT | O_APPEND | O_SYNC | O_CLOEXEC, 0644);
```

`O_SYNC` を付けたfdでは、`write()` が返る前に、書き込んだデータと必要なメタデータの同期が行われます。
つまり、毎回の `write()` が `fsync()` に近い重さを持つ場合があります。

```text
O_SYNC
    write() のたびに同期性を強める
    安全寄りだが遅くなりやすい

O_DSYNC
    データ同期寄り
    fdatasync() に近い考え方
```

古い資料では `O_RSYNC` が並んで紹介されることもあります。
ただし、Linuxで普段よく意識するのは主に `O_SYNC` と `O_DSYNC` です。
最初の学習段階では、`O_RSYNC` は移植性や実装差が絡む補助的な知識として見ておけばよいと思います。

`O_SYNC` を使うとコード上は簡単です。
しかし、すべてのログ書き込みへ機械的に `O_SYNC` を付けると、性能が大きく落ちる可能性があります。
多くの場合は、重要な区切りで `fsync()` する、一定間隔で同期する、ジャーナルや一時ファイルとrenameを組み合わせる、といった設計を考えます。

#### ２章の５の７　close() は fsync() の代わりではない

ファイルを閉じる `close()` は、fdを閉じるための関数です。
`close()` によってカーネル内のリソースは解放されますが、`close()` を呼べば必ず `fsync()` と同じ意味でデータが物理ストレージへ同期される、とは考えない方がよいです。

```text
close(fd)
    fdを閉じる
    エラーが返ることもある
    しかし fsync() の代わりとして考えない
```

重要なデータでは、`close()` の前に `fsync()` または `fdatasync()` を明示的に呼ぶ方が意図がはっきりします。

また、`close()` 自体も失敗する可能性があります。
遅延書き込みのエラーが `close()` のタイミングで見えることもあります。
そのため、本当に保存結果を気にするコードでは、`close()` の戻り値も確認します。

```text
重要な保存処理
    write_full()
    fsync() または fdatasync()
    close()
    それぞれの戻り値を確認する
```

#### ２章の５の８　安全なファイル更新の基本形

設定ファイルや重要なデータを更新するとき、既存ファイルをいきなり `O_TRUNC` で空にして書き直すのは危険な場合があります。
途中でクラッシュすると、元の内容も新しい内容も失う可能性があるからです。

よく使われる考え方は、一時ファイルへ書いて同期し、最後に `rename()` で置き換える方法です。

```text
安全寄りの更新手順

1. 同じディレクトリに一時ファイルを作る
2. 一時ファイルへ新しい内容を書く
3. 一時ファイルを fsync() する
4. 一時ファイルを close() する
5. rename() で本来のファイル名へ置き換える
6. ディレクトリを fsync() する
```

最後にディレクトリを `fsync()` するのは、`rename()` によるディレクトリエントリの変更をストレージへ反映するためです。
ここは見落としやすいですが、クラッシュ耐性を考えると重要です。

ただし、この話はファイルシステムやジャーナリングの挙動にも関係します。
最初から完璧に覚える必要はありません。
まずは「重要なファイル更新では、書いたデータだけでなく、ファイル名の置き換えやディレクトリ側の変更も同期対象になる」と理解すればよいです。

UmuOSでファイルシステムを作る場合、この考え方はかなり重要になります。
単に `write()` でデータブロックを書くだけでは、クラッシュ時にファイル名、inode、サイズ、データブロックの整合性が崩れる可能性があります。
OS側は、どの順序でメタデータとデータを反映するかを設計しなければなりません。

#### ２章の５の９　同期I/Oの要点

ここまでの同期I/Oの要点をまとめます。

```text
通常のwrite()
    ページキャッシュへコピーして戻ることが多い
    物理ストレージへ保存完了とは限らない

同期I/O
    データをストレージへ反映するタイミングをアプリケーションが制御する

fsync()
    指定fdのデータと必要なメタデータを同期する

fdatasync()
    主にデータを同期する

fsync() / fdatasync() の戻り値
    成功なら0、失敗なら-1
    errnoで原因を見る

ディレクトリエントリ
    ファイル名とinode番号の対応
    重要なrename()後は親ディレクトリのfsync()も考える

sync()
    システム全体のdirtyデータを書き戻すよう依頼する

syncfs()
    指定fdが属するファイルシステムを同期するLinux寄りのAPI

O_SYNC / O_DSYNC
    write() のたびに同期性を強めるopen()フラグ

close()
    fsync() の代わりではない

安全なファイル更新
    一時ファイル、fsync()、rename()、ディレクトリfsync()を組み合わせる
```

UmuOSの視点では、同期I/Oは単なる便利機能ではありません。
クラッシュしたときに、ファイルシステムがどの状態まで戻れるのか、どの順序でデータとメタデータを保存するのか、というOS設計そのものに関係します。

Ushのようなシェルでは、最初から同期I/Oを細かく制御する場面は多くないかもしれません。
しかし、エディタ、パッケージ管理、ログ、設定ファイル更新を作る段階では、`write()` だけで保存完了と考えない感覚が重要になります。

### ２章の６　openat() と現代的なパス指定

現代のLinuxでは、`open()` だけでなく `openat()` もよく出てきます。
`openat()` は、ディレクトリfdを基準にして相対パスを開くための関数です。

```c
#include <fcntl.h>
#include <sys/stat.h>
#include <sys/types.h>
#include <unistd.h>

int openat(int dirfd, const char *path, int flags);
int openat(int dirfd, const char *path, int flags, mode_t mode);
```

`dirfd` にディレクトリのfdを渡し、`path` に相対パスを渡すと、そのディレクトリを基準にファイルを開けます。
`dirfd` に `AT_FDCWD` を渡すと、現在のカレントディレクトリ基準になり、open() に近い使い方になります。

openat() が便利な理由は、主に次の2つです。

```text
パス解決の安全性
    途中のディレクトリが別プロセスに差し替えられる競合を減らしやすい

カレントディレクトリに依存しにくい設計
    マルチスレッドでも、プロセス全体のcwd変更に振り回されにくい
```

今では `openat()` は特別に未来的な機能というより、普通に使われるAPIです。
さらに新しいLinuxでは `openat2()` もあります。
`openat2()` はLinux 5.6で追加された、より細かくパス解決を制御できるLinux固有のシステムコールです。
ただし、移植性を考える通常のCプログラムでは、まず `open()` と `openat()` を理解するのがよいです。

openat() の簡単な例です。

```c
#include <fcntl.h>
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

int main(void) {
    int dirfd;
    int fd;

    dirfd = open("/home/tama", O_RDONLY | O_DIRECTORY | O_CLOEXEC);

    if (dirfd < 0) {
        perror("open directory");
        exit(1);
    }

    fd = openat(dirfd, "memo.txt", O_RDONLY | O_CLOEXEC);

    if (fd < 0) {
        perror("openat");
        close(dirfd);
        exit(1);
    }

    close(fd);
    close(dirfd);

    return 0;
}
```

この例では、まず `/home/tama` をディレクトリとして開き、そのディレクトリを基準に `memo.txt` を開いています。
パス文字列だけに頼るのではなく、ディレクトリfdを基準にするのがポイントです。

UmuOSの視点では、最初から openat() まで実装する必要はないかもしれません。
しかし、将来的に安全なパス解決や、ディレクトリfdを基準にしたファイル操作を設計するなら、openat() 系の考え方はかなり参考になります。

### ２章の７　ここまでの整理

ここまでをまとめると、次のようになります。

```text
fd
    プロセスが開いている対象を表す整数

open()
    パス名からファイルを開き、fdを返す

read() / write()
    fdを使って読み書きする

close()
    fdを閉じる

標準fd
    0: 標準入力
    1: 標準出力
    2: 標準エラー出力

openat()
    ディレクトリfdを基準にファイルを開く

read()
    fdから最大countバイトを読み取る
    戻り値で実際に読めたバイト数、EOF、エラーを判断する

write()
    fdへ最大countバイトを書き込む
    戻り値で実際に書けたバイト数、部分書き込み、エラーを判断する

O_APPEND
    write() のたびにファイル末尾へ追記する

writeback
    write() 成功後、カーネルが後でストレージへ書き戻すことがある

同期I/O
    fsync()、fdatasync()、O_SYNC などで永続化タイミングを制御する

fsync()
    指定fdのデータと必要なメタデータをストレージへ同期する

O_CLOEXEC
    現代的なコードではfd漏れ防止のためによく使う
```

まずは、LinuxのファイルI/Oでは fd が中心になる、と押さえるとよいです。
ファイル名を扱うのは主に open() の段階です。
いったんファイルを開いた後は、read()、write()、close() などの操作は fd を通して行います。

UmuOSを作る側の視点では、fdはユーザープログラムとカーネルをつなぐ小さな番号です。
しかし、その裏側には、プロセスごとのfd表、ファイルポジション、inode、パーミッション、パイプやソケットなどの抽象化がつながっています。
このfdの設計がしっかりしていると、シェル、リダイレクト、パイプ、ログ、デバイスI/Oなどを自然に組み立てられるようになります。










































