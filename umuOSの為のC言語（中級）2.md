---
title: "UmuOSの為のC言語２（中級）　２章　ファイルI/O"
---

# UmuOSの為のC言語（中級）　２

このノートは、UmuOSを進化させるためにプロセス管理・メモリ管理・ファイルI/O・シェル・システムコール設計に
直接役立つ形で抽象化し、再構成することであります。
すなわち、UmuOSの構造へ還元するための実践ノートであります。
LinuxのAPIやシステムコールは、OSがどのようにふるまうかでもあり、その構造を理解することはUmuOSの設計力を
高めることに直結するとおもう。

## ２章　ファイルI/O

この章では、LinuxのファイルI/Oの基本を整理します。

LinuxやUnix系OSでは、ファイルI/Oはかなり中心的な仕組みです。
通常ファイルを読む、ログを書く、端末へ表示する、パイプで別プロセスへ渡す、ソケットで通信する、デバイスを操作する、という処理の多くが、ファイルディスクリプタを通して扱われます。

UmuOSの視点では、この章はかなり重要です。
なぜなら、シェル、リダイレクト、パイプ、ログ出力、設定ファイル読み込み、デバイス入出力などが、最終的には fd、open()、read()、write()、close() の理解に集約されていくからです。

この章では、まず低レベルI/Oを中心に見ていきます。
標準Cライブラリの FILE、fopen()、fgets()、fprintf() などは便利ですが、その下にはファイルディスクリプタを使う低レベルI/Oがあります。
UmuOSやUshの設計を考える場合は、まずこちらを押さえると見通しが良くなります。

### ２章の１　ファイルI/Oの基本

ファイルを読み書きするには、まずファイルを開く必要があります。
Linuxでは、ファイルを開くとカーネルがそのファイルをプロセスごとに管理し、ユーザープログラムにはファイルディスクリプタという整数を返します。

ファイルディスクリプタは、file descriptor の略で fd と呼ばれることが多いです。
C言語では普通の `int` として扱います。
fd専用の特別な型があるわけではありません。

たとえば、ファイルを開くと次のような流れになります。

```text
ユーザープログラム
    open() を呼ぶ

カーネル
    ファイルを開く
    プロセスのfd表に登録する

ユーザープログラム
    fdを受け取る
    以後 read()、write()、close() に fd を渡す
```

ここで重要なのは、ユーザープログラムが直接inodeやディスクブロックを触るわけではない、という点です。
プログラムは fd という番号を持ち、その番号をカーネルに渡すことで、読み書きやクローズを依頼します。

#### ２章の１の１　fdとプロセスのファイルテーブル

プロセスは、自分用のファイルディスクリプタ表を持っています。
この表には、fd番号と、開いているファイルに関する情報が対応付けられます。

イメージとしては、次のようになります。

```text
プロセスAのfd表

fd 0  -> 標準入力
fd 1  -> 標準出力
fd 2  -> 標準エラー出力
fd 3  -> /home/tama/memo.txt
fd 4  -> pipeの読み取り口
```

fdそのものは単なる整数です。
しかし、その先にはカーネルが管理する「開いているファイル」の情報があります。

そこには、たとえば次のような情報が関係します。

```text
ファイルの実体
    inodeやデバイスなど、対象そのものに関係する情報

ファイルポジション
    次にread()やwrite()する位置

アクセスモード
    読み取り専用、書き込み専用、読み書き両用など

フラグ
    O_APPEND、O_NONBLOCK、O_CLOEXEC など
```

ファイルポジションは、読み書きのたびに進む「ファイル内の現在位置」です。
通常ファイルを read() すると、読んだバイト数ぶんだけ位置が進みます。
write() した場合も、書いたバイト数ぶんだけ位置が進みます。

UmuOSでファイルI/Oを考える場合も、単に「ファイルを開く」だけでは足りません。
少なくとも、プロセスごとのfd表、fd番号、ファイルポジション、アクセスモード、対象ファイルへの参照を管理する必要があります。

#### ２章の１の２　親子プロセスとfd

Linuxでは、fork() で子プロセスを作ると、子プロセスは親プロセスのfdを受け継ぎます。

これは、シェルやパイプを理解するうえで非常に重要です。
たとえば、シェルが pipe() でfdを作り、fork() で子プロセスを作ると、子プロセスもそのfdを持った状態で開始できます。
その後、dup2() で標準入力や標準出力につなぎ替え、exec() で別のコマンドへ置き換えることで、パイプやリダイレクトが実現できます。

イメージとしては、次のようになります。

```text
親プロセス
    fd 3 -> file.txt

fork() 後の子プロセス
    fd 3 -> file.txt
```

ただし、親と子は別々のプロセスです。
片方が close(fd) しても、もう片方のfd表から同じfd番号が自動的に消えるわけではありません。
一方で、fork()直後は同じ「開いているファイル」を参照するため、ファイルポジションなどを共有する場合があります。

ここは少し混乱しやすいです。
最初は、次のように考えると分かりやすいです。

```text
fd表
    プロセスごとにある

開いているファイルの状態
    fork()後に親子で共有されることがある

close(fd)
    そのプロセスのfd表からfdを外す
```

スレッドの場合は、同じプロセス内で動くため、基本的にfd表を共有します。
そのため、あるスレッドが close(fd) すると、同じプロセス内の別スレッドにも影響します。

#### ２章の１の３　標準入力・標準出力・標準エラー出力

通常、プロセスは起動時点で少なくとも3つのfdを持っています。

```text
fd 0
    標準入力
    stdin
    STDIN_FILENO

fd 1
    標準出力
    stdout
    STDOUT_FILENO

fd 2
    標準エラー出力
    stderr
    STDERR_FILENO
```

C言語では、直接 `0`、`1`、`2` と書くこともできます。
しかし、意味が分かりやすいので、低レベルI/Oでは `STDIN_FILENO`、`STDOUT_FILENO`、`STDERR_FILENO` を使う方が読みやすいです。
これらは `<unistd.h>` で定義されています。

たとえば、標準出力へ文字列を書く最小例は次のようになります。

```c
#include <unistd.h>

int main(void) {
    write(STDOUT_FILENO, "hello\n", 6);

    return 0;
}
```

ただし、実際のプログラムでは write() の戻り値確認が必要です。
システムプログラミングでは、I/Oは失敗するものとして扱うのが基本です。

少し丁寧に書くと、次のようになります。

```c
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

int main(void) {
    const char message[] = "hello\n";
    ssize_t n;

    n = write(STDOUT_FILENO, message, sizeof(message) - 1);

    if (n < 0) {
        perror("write");
        exit(1);
    }

    return 0;
}
```

ここで `ssize_t` は、成功時には書き込んだバイト数、失敗時には `-1` を返せる符号付きの型です。
read() や write() の戻り値でよく出てきます。

#### ２章の１の４　fdが指すもの

fdが指すものは、通常ファイルだけではありません。
Unix系OSの「すべてのものはファイルである」という考え方により、いろいろな対象がfdで扱えます。

```text
通常ファイル
    テキストファイル、バイナリファイル、ログファイルなど

端末
    キーボード入力や画面出力

パイプ
    プロセス間でバイト列を流す通信路

FIFO
    ファイルシステム上に名前を持つパイプ

ソケット
    プロセス間通信やネットワーク通信

デバイスファイル
    ディスク、端末、乱数デバイスなど
```

この統一感がLinuxの強いところです。
UmuOSでも、端末、パイプ、ファイル、デバイスをできるだけ同じfdインターフェースで扱えるようにすると、シェルやコマンドの設計がかなりきれいになります。

### ２章の２　ファイルのオープン

ファイルを使う流れは、かなり基本的には次の形になります。

```text
open()
    ファイルを開いてfdを得る

read() または write()
    fdを使って読み書きする

close()
    fdを閉じる
```

現在のLinuxプログラミングでは、ファイル作成も含めて `open()` を使うことが多いです。
古いコードでは `creat()` が出てくることもありますが、`creat(path, mode)` は、概念的には `open(path, O_WRONLY | O_CREAT | O_TRUNC, mode)` に近いものです。
新しく書くコードでは、意図が分かりやすい `open()` を使う方がよいと思います。

#### ２章の２の１　open()

open() は、パス名で指定したファイルを開き、fdを返します。

```c
#include <fcntl.h>
#include <sys/stat.h>
#include <sys/types.h>
#include <unistd.h>

int open(const char *path, int flags);
int open(const char *path, int flags, mode_t mode);
```

`path` は開きたいファイルのパスです。
`flags` には、読み取り用なのか、書き込み用なのか、作成するのか、追記するのか、といった指定を渡します。

戻り値は次のようになります。

```text
成功
    0以上のfd

失敗
    -1
    errno に理由が入る
```

ファイルを読み取り専用で開く例です。

```c
#include <errno.h>
#include <fcntl.h>
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

int main(void) {
    int fd;

    fd = open("/home/tama/memo.txt", O_RDONLY);

    if (fd < 0) {
        perror("open");
        exit(1);
    }

    close(fd);

    return 0;
}
```

古いサンプルでは、存在しないユーザー名や架空のパスを使う例が出てくることがあります。
このノートでは、UmuOS研究の文脈に合わせて `/home/tama/memo.txt` のような分かりやすいパスにしています。
実際に試すときは、自分の環境に存在するファイルへ変更してください。

#### ２章の２の２　open()の基本フラグ

open() の `flags` には、まず次のどれか1つを指定します。

```text
O_RDONLY
    読み取り専用で開く

O_WRONLY
    書き込み専用で開く

O_RDWR
    読み書き両用で開く
```

読み取り専用で開いたfdには、基本的に write() できません。
書き込み専用で開いたfdからは、基本的に read() できません。

ファイルを作成し、書き込み用に開く例です。

```c
#include <errno.h>
#include <fcntl.h>
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

int main(void) {
    int fd;

    fd = open("memo.txt", O_WRONLY | O_CREAT | O_TRUNC | O_CLOEXEC, 0644);

    if (fd < 0) {
        perror("open");
        exit(1);
    }

    if (write(fd, "hello\n", 6) < 0) {
        perror("write");
        close(fd);
        exit(1);
    }

    if (close(fd) < 0) {
        perror("close");
        exit(1);
    }

    return 0;
}
```

ここでは `O_CLOEXEC` も付けています。
これは現代的にはかなり重要なフラグです。
exec() で別プログラムを起動するとき、不要なfdが子プロセスへ漏れないようにするためです。

Ushのようなシェルを作る場合、fdの継承は便利でもあり危険でもあります。
パイプやリダイレクトに必要なfdは意図的に渡し、不要なfdは渡さない、という設計が必要になります。

#### ２章の２の３　open()でよく使う追加フラグ

open() では、基本のアクセスモードに加えて、ビットORで追加フラグを指定できます。

```text
O_APPEND
    常にファイル末尾へ追記する

O_CREAT
    ファイルがなければ作る

O_EXCL
    O_CREAT と一緒に使い、すでに存在したら失敗させる

O_TRUNC
    既存ファイルを開いたときにサイズを0へ切り詰める

O_CLOEXEC
    exec() 時にfdを自動的に閉じる

O_DIRECTORY
    対象がディレクトリでなければ失敗させる

O_NOFOLLOW
    最後の要素がシンボリックリンクなら失敗させる

O_NONBLOCK
    可能ならノンブロッキングモードで開く

O_SYNC
    書き込みを同期的に行う

O_DIRECT
    ページキャッシュをなるべく避けるダイレクトI/O用に開く
```

古い説明では `O_LARGEFILE` が大きく扱われることがあります。
しかし、現在の64ビットLinuxでは大きなファイルを扱うことは通常の前提になっており、`O_LARGEFILE` を意識する場面はかなり減っています。
32ビット環境で大きなファイルを扱う場合は、`_FILE_OFFSET_BITS=64` を使う話が出てきますが、まずは「現代の64ビットLinuxでは通常あまり意識しない」と理解しておけばよいと思います。

また、古い説明では `O_ASYNC` が紹介されることがあります。
これはI/O可能になったときにシグナルを受け取るための仕組みですが、現在の一般的なイベント駆動I/Oでは、`poll()`、`epoll()`、場合によっては `io_uring` などを検討することが多いです。
最初の段階では、`O_ASYNC` は歴史的・特殊用途寄りとして見ておけばよいと思います。

##### ２章の２の３の１　O_CREATとO_EXCL

新しいファイルを作るときは、`O_CREAT` を使います。
このとき、第3引数にパーミッションを指定します。

```c
fd = open("memo.txt", O_WRONLY | O_CREAT | O_CLOEXEC, 0644);
```

ただし、ファイルがすでに存在する場合に上書きしたくないときは、`O_EXCL` も一緒に使います。

```c
fd = open("memo.txt", O_WRONLY | O_CREAT | O_EXCL | O_CLOEXEC, 0644);
```

この指定では、`memo.txt` がすでに存在すると open() は失敗します。
一時ファイルやロックファイルを作るときなど、競合を避けたい場面で重要になります。

##### ２章の２の３の２　O_APPEND

`O_APPEND` は、書き込みのたびにファイル末尾へ追記するためのフラグです。

ログファイルのように、複数のプロセスから追記したい場合に便利です。
単に lseek() で末尾へ移動してから write() するよりも、`O_APPEND` を使う方が安全です。
なぜなら、末尾へ移動する処理と書き込み処理を、カーネルが一連の操作として扱えるからです。

```c
#include <fcntl.h>
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

int main(void) {
    int fd;

    fd = open("app.log", O_WRONLY | O_CREAT | O_APPEND | O_CLOEXEC, 0644);

    if (fd < 0) {
        perror("open");
        exit(1);
    }

    if (write(fd, "started\n", 8) < 0) {
        perror("write");
        close(fd);
        exit(1);
    }

    close(fd);

    return 0;
}
```

UmuOSでログ機能を考える場合も、追記モードはかなり大事です。
単純に「最後へ移動して書く」だけでは、複数プロセスが同時に書いたときに競合する可能性があります。

##### ２章の２の３の３　O_TRUNC

`O_TRUNC` は、既存ファイルを開いたときに中身を空にするフラグです。

たとえば、次の指定では、`memo.txt` が存在していればサイズを0にしてから書き込み用に開きます。
存在しない場合は、`O_CREAT` があるので新しく作ります。

```c
fd = open("memo.txt", O_WRONLY | O_CREAT | O_TRUNC | O_CLOEXEC, 0644);
```

注意点として、`O_TRUNC` は破壊的です。
既存の内容が消えます。
設定ファイルや重要なデータを扱う場合は、安易に使わない方がよいです。

##### ２章の２の３の４　O_NONBLOCK

`O_NONBLOCK` は、可能ならI/Oで待たないようにするフラグです。

通常ファイルではあまり意識しません。
一方で、FIFO、ソケット、端末などでは重要になります。
データが来るまで read() が待ち続ける、接続できるまで待つ、といった動作を避けたい場合に使います。

Ushのようなシェルの基本実装では、最初から `O_NONBLOCK` を多用する必要はありません。
ただし、将来的にジョブ制御、イベントループ、非同期I/Oを扱う場合には重要になります。

##### ２章の２の３の５　O_SYNCとO_DIRECT

`O_SYNC` は、書き込みをディスクへ同期させたい場合に使います。
通常、write() が成功しても、その時点で物理ディスクへ完全に書き込まれているとは限りません。
カーネルやファイルシステムのキャッシュに入っている場合があります。

`O_SYNC` を使うと安全性は上がりますが、性能は落ちやすいです。
ログ、データベース、ジャーナリングなど、データ消失をどこまで許容するかによって判断する領域です。

`O_DIRECT` は、ページキャッシュをなるべく使わずにI/Oするためのフラグです。
ただし、バッファのアラインメントやサイズ制約があり、初心者向けではありません。
通常のアプリケーションでは、まず普通の read()、write()、必要に応じて fsync() を理解する方が先です。

### ２章の３　openat() と現代的なパス指定

現代のLinuxでは、`open()` だけでなく `openat()` もよく出てきます。
`openat()` は、ディレクトリfdを基準にして相対パスを開くための関数です。

```c
#include <fcntl.h>
#include <sys/stat.h>
#include <sys/types.h>
#include <unistd.h>

int openat(int dirfd, const char *path, int flags);
int openat(int dirfd, const char *path, int flags, mode_t mode);
```

`dirfd` にディレクトリのfdを渡し、`path` に相対パスを渡すと、そのディレクトリを基準にファイルを開けます。
`dirfd` に `AT_FDCWD` を渡すと、現在のカレントディレクトリ基準になり、open() に近い使い方になります。

openat() が便利な理由は、主に次の2つです。

```text
パス解決の安全性
    途中のディレクトリが別プロセスに差し替えられる競合を減らしやすい

カレントディレクトリに依存しにくい設計
    マルチスレッドでも、プロセス全体のcwd変更に振り回されにくい
```

今では `openat()` は特別に未来的な機能というより、普通に使われるAPIです。
さらに新しいLinuxでは `openat2()` もあります。
`openat2()` はLinux 5.6で追加された、より細かくパス解決を制御できるLinux固有のシステムコールです。
ただし、移植性を考える通常のCプログラムでは、まず `open()` と `openat()` を理解するのがよいです。

openat() の簡単な例です。

```c
#include <fcntl.h>
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

int main(void) {
    int dirfd;
    int fd;

    dirfd = open("/home/tama", O_RDONLY | O_DIRECTORY | O_CLOEXEC);

    if (dirfd < 0) {
        perror("open directory");
        exit(1);
    }

    fd = openat(dirfd, "memo.txt", O_RDONLY | O_CLOEXEC);

    if (fd < 0) {
        perror("openat");
        close(dirfd);
        exit(1);
    }

    close(fd);
    close(dirfd);

    return 0;
}
```

この例では、まず `/home/tama` をディレクトリとして開き、そのディレクトリを基準に `memo.txt` を開いています。
パス文字列だけに頼るのではなく、ディレクトリfdを基準にするのがポイントです。

UmuOSの視点では、最初から openat() まで実装する必要はないかもしれません。
しかし、将来的に安全なパス解決や、ディレクトリfdを基準にしたファイル操作を設計するなら、openat() 系の考え方はかなり参考になります。

### ２章の４　ここまでの整理

ここまでをまとめると、次のようになります。

```text
fd
    プロセスが開いている対象を表す整数

open()
    パス名からファイルを開き、fdを返す

read() / write()
    fdを使って読み書きする

close()
    fdを閉じる

標準fd
    0: 標準入力
    1: 標準出力
    2: 標準エラー出力

openat()
    ディレクトリfdを基準にファイルを開く

O_CLOEXEC
    現代的なコードではfd漏れ防止のためによく使う
```

まずは、LinuxのファイルI/Oでは fd が中心になる、と押さえるとよいです。
ファイル名を扱うのは主に open() の段階です。
いったんファイルを開いた後は、read()、write()、close() などの操作は fd を通して行います。

UmuOSを作る側の視点では、fdはユーザープログラムとカーネルをつなぐ小さな番号です。
しかし、その裏側には、プロセスごとのfd表、ファイルポジション、inode、パーミッション、パイプやソケットなどの抽象化がつながっています。
このfdの設計がしっかりしていると、シェル、リダイレクト、パイプ、ログ、デバイスI/Oなどを自然に組み立てられるようになります。
