---
title: "UmuOSの為のC言語６（中級）　６章　高度なプロセス管理"
---

# UmuOSの為のC言語（中級）　６

このノートは、UmuOSを進化させるためにプロセス管理・メモリ管理・ファイルI/O・シェル・システムコール設計に
直接役立つ形で抽象化し、再構成することであります。
すなわち、UmuOSの構造へ還元するための実践ノートであります。
LinuxのAPIやシステムコールは、OSがどのようにふるまうかでもあり、その構造を理解することはUmuOSの設計力を
高めることに直結すると思います。

## ６章　高度なプロセス管理

5章では、プロセスの作成、終了、待ち合わせ、ユーザとグループ、セッション、プロセスグループ、デーモンの基本を見ました。
この章ではその続きとして、より高度なプロセス管理へ進みます。

特に重要になるのが、プロセスが「いつ」「どれくらいの時間」CPUを使えるのか、という観点です。
プロセスは作れるだけでは十分ではありません。
多数のプロセスが同時に存在するとき、カーネルはどのプロセスを先に動かし、どのプロセスを少し待たせるかを判断しなければなりません。

この判断を行うのがプロセススケジューラです。
シェル、デーモン、対話的なツール、ビルド、エンコーダ、サーバ、監視プロセスなどが同居するLinuxでは、スケジューリングの性質が体感性能に直結します。

本章では、まずLinuxのプロセススケジューリングの考え方を整理し、その後で優先度やスケジューリング関連のAPIへ進んでいきます。
なお、古い書籍では Linux 2.4 や 2.6 初期の事情を前提に説明されることがありますが、このノートでは現在のLinuxカーネルの感覚に寄せて読み替えます。

### ６章の１　プロセススケジューリング

プロセススケジューラは、どのプロセスを次に実行するかを決めるカーネル内の仕組みです。
CPU時間は有限なので、実行可能なプロセスが複数あるときは、それらへCPU時間をどう分配するかを決める必要があります。

```text
スケジューラの仕事:
	実行可能なプロセスを管理する
	どのプロセスを次に走らせるか決める
	必要なら実行中のプロセスを中断する
	CPU時間を公平性や応答性を考えながら配分する
```

ここで重要なのが、実行可能、つまり runnable という言葉です。
実行可能なプロセスとは、CPUを与えればすぐに実行を続けられる状態のプロセスです。
反対に、ディスクI/O、ソケット、端末入力、タイマ、ロックなどを待って止まっているプロセスは、ブロック中であり、その間は実行可能ではありません。

```text
実行可能:
	CPUがあればすぐ動ける

ブロック中:
	何らかのイベント待ちで止まっている
```

Linuxカーネルは、実行可能なタスクをランキューで管理します。
CPUが空いたとき、あるいは現在走っているタスクを中断すべきときに、ランキューから次の候補を選びます。

実行可能プロセスが1つしかなければ話は簡単です。
しかし実際には、シェル、エディタ、ブラウザ、ビルド、ログ収集、サーバ、バックグラウンドジョブなど、多数のプロセスやスレッドが同時に存在します。
CPUコア数よりも実行可能タスクが多ければ、誰かを走らせている間は、他を待たせなければなりません。

シングルコア環境で、複数のプロセスを短く切り替えながら実行することをマルチタスクと呼びます。
マルチコア環境では、複数のCPU上で並列に走れる分だけ話は少し楽になりますが、それでも実行可能タスク数がコア数を超えれば、やはりスケジューリングが必要です。

LinuxはプリエンプティブなマルチタスクOSです。
つまり、現在走っているプロセスが自分からCPUを返すのを待たず、カーネル側の判断で中断して別のプロセスへ切り替えられます。

```text
プリエンプティブ:
	カーネルが必要に応じて実行を中断できる

協調的:
	プロセスが自発的にCPUを返すまで走り続ける
```

協調的マルチタスクは、行儀の悪いプロセスがCPUを握り続けると全体が固まりやすくなります。
そのため、現在の主流OSはほぼすべてプリエンプティブです。
Linuxも例外ではありません。

#### ６章の１の１　ビッグO記法

古いLinuxの解説では、スケジューラの中心として O(1) スケジューラがよく登場します。
これはLinux 2.5 系から 2.6 初期にかけて重要だった実装で、プロセス数が増えても、主要な選択処理を一定時間で行いやすいことが特徴でした。

ここで言う O(1) は、ビッグO記法による計算量の表現です。

```text
O(1):
	入力サイズが増えても、必要な処理量がほぼ一定

O(n):
	入力サイズに比例して処理量が増える

O(log n):
	入力サイズよりはゆるやかに増える
```

スケジューラの文脈で重要なのは、実行可能タスク数が増えたときに、次のタスク選択が極端に遅くならないことです。
もし毎回すべてのタスクを総なめして選ぶような設計だと、タスク数が多い環境でスケジューラ自身が重くなってしまいます。

ただし、現在のLinuxを説明するなら、そのまま O(1) スケジューラが中核だと言うのは古くなっています。
長く使われたのは CFS、つまり Completely Fair Scheduler で、最近のカーネルでは EEVDF ベースの考え方も取り込まれています。

そのため、現代の理解としては次のように整理するのがよいです。

```text
歴史的なLinux:
	O(1) スケジューラが重要だった時代がある

現在のLinux:
	公平性、応答性、レイテンシを重視したスケジューリングが中心
	固定長タイムスライスだけで理解するとズレる場面がある
```

それでもビッグO記法自体は重要です。
カーネル内部のデータ構造やアルゴリズムを考えるとき、タスク数が増えたときに処理がどう伸びるか、という視点は今でも欠かせません。

#### ６章の１の２　タイムスライス

タイムスライスとは、あるプロセスが連続してCPUを使う時間のまとまりを指す学習用語です。
古典的な説明では、各プロセスに一定時間のタイムスライスを与え、それを使い切ったら別のプロセスへ切り替える、と説明されます。

この考え方は入門としては分かりやすいです。
ただし、現在のLinuxでは、単純な固定長スライスを全プロセスへ機械的に配る、という理解だけでは足りません。
実際には、nice値、タスク数、重み、公平性、起床直後の応答性などを考慮して、実行時間の与え方が動的に調整されます。

```text
長すぎる実行時間:
	他のタスクの待ち時間が長くなる
	対話性が悪くなる

短すぎる実行時間:
	切り替えコストが増える
	キャッシュ効率が落ちやすい
```

このため、理想的な長さを1つ固定で決めるのは難しいです。
対話的なアプリケーションでは応答性が重要ですし、CPUバウンドな処理ではある程度まとまった実行時間があった方が効率がよいことがあります。

また、プロセスは与えられた実行時間を必ずしも使い切るとは限りません。
たとえば少しだけ動いて `read()` や `poll()` で待ちに入れば、その時点でブロックし、CPUを手放します。

```text
例:
	少し実行する
	I/O要求を出す
	待ち状態へ入る
	イベント到着後に再び実行可能になる
```

このようなタスクは、CPUを長く占有するよりも、起きたときにすばやく再開できることの方が重要です。
現在のLinuxスケジューラも、そのような傾向をできるだけ反映しようとします。

#### ６章の１の３　I/Oバウンドなプロセスとプロセッサバウンドなプロセス

CPU時間をほぼ使い切り続けるタスクを、CPUバウンドなプロセスと言います。
たとえば大規模な数値計算、画像変換、動画エンコード、圧縮、コンパイルの一部などはCPUバウンド寄りになりやすいです。

```text
CPUバウンド:
	計算そのものが重い
	CPU時間を欲しがり続ける
	長めに連続実行できると効率がよいことがある
```

一方で、ファイルI/O、ネットワーク、端末入力、タイマ、イベント待ちなどで止まる時間が長いタスクを I/Oバウンド と呼びます。

```text
I/Oバウンド:
	少し動く
	I/Oやイベント待ちで止まる
	再開時の応答性が重要
```

たとえば、対話的なシェル、エディタ、GUIアプリケーション、サーバの待受処理、ログ監視などはI/Oバウンド寄りの振る舞いを見せやすいです。

スケジューラにとって難しいのは、システム内にこの両方が混在することです。
CPUバウンドなタスクにだけ都合よくすると、対話的アプリケーションがもたつきます。
逆に応答性だけを優先しすぎると、計算処理のスループットが落ちます。

現在のLinuxでは、起床した対話的タスクのレイテンシを抑えつつ、全体の公平性も崩しすぎないように設計されています。
つまり、I/OバウンドかCPUバウンドかを完全に二分して扱うというより、実際の振る舞いから「どのくらい早く再開させるべきか」を調整するイメージです。

実際には、1つのアプリケーションが常に片方の性質だけを示すとは限りません。
たとえばゲーム、ブラウザ、ビデオ処理、ビルドツール、データベースは、時期によってCPU寄りにもI/O寄りにもなります。

そのため、スケジューリングを考えるときは、アプリケーションを固定的に分類しすぎるよりも、今その瞬間にどういう資源待ちと実行を繰り返しているかを見る方が実践的です。

UmuOSの視点では、最初のスケジューラ実装では、まず「実行可能キューから1つ選んで一定時間動かす」という単純な形で十分です。
その後、対話性、優先度、公平性、I/O待ちからの復帰速度などを段階的に追加していくと、Linuxの設計意図がかなり見えやすくなると思います。

#### ６章の１の４　プリエンプティブスケジューリング

LinuxはプリエンプティブなマルチタスクOSです。
つまり、現在CPU上で動いているタスクがまだ処理を続けたがっていても、カーネルの判断で実行を中断し、別のタスクへ切り替えられます。

古典的な説明では、「タイムスライスを使い切ったら別のプロセスへ切り替える」と表現されます。
この説明は入門としては有効です。
ただし現在のLinuxでは、単純に全タスクへ固定長スライスを配るだけではなく、公平性、優先度、起床直後の応答性、リアルタイム性などを踏まえて切り替えが決まります。

```text
プリエンプションが起こる典型例:
	今のタスクが十分走った
	より優先されるべきタスクが実行可能になった
	リアルタイムタスクが起床した
	タイマ割り込みなどを契機に再判断が必要になった
```

重要なのは、実行可能なタスクがあるのに、いつまでもCPUをもらえないまま放置されるべきではない、という点です。
古い本でいう「すべてのプロセスは進行しなければならない」という考え方は、今も本質的には同じです。
もちろん、リアルタイムスケジューリングや優先度設定によって待ち時間の差は出ますが、通常の公平クラスでは、極端な飢餓状態を避ける方向で設計されています。

実行可能タスクが存在しない場合、CPUはアイドル状態へ入ります。
昔の説明では「アイドルプロセスを実行する」と書かれることがあります。
学習上はその理解で大きくは外れませんが、現在のLinuxでは、単に何もしないのではなく、CPUを低消費電力状態へ移したり、次の割り込みを待ったりする流れも重要です。

```text
実行可能タスクがない:
	アイドル処理へ入る
	必要なら省電力状態へ移る
	割り込みやタイマで復帰する
```

また、現在走っているタスクよりも高い優先度のタスクが実行可能になれば、カーネルは早い段階で切り替えを検討します。
たとえば、端末入力待ちだった対話的プロセスがユーザー入力で起床した場合、その応答をなるべく早く返したいからです。

この性質のおかげで、バックグラウンドでCPUを消費する処理が動いていても、エディタやシェルのような対話的アプリケーションは比較的軽快に感じられます。

#### ６章の１の５　スレッド

スレッドは、プロセス内の実行単位です。
少なくとも1つのスレッドがなければ、プロセスは命令を実行できません。

各スレッドは、実行状態としては独立したCPU文脈を持ちます。
たとえば、レジスタ、スタック、命令ポインタ、スケジューリング対象としての状態はスレッドごとに存在します。
一方で、同じプロセス内のスレッドどうしは、多くの資源を共有します。

```text
スレッドごとに分かれるもの:
	レジスタ状態
	スタック
	実行位置
	スケジューリング対象としての状態

同じプロセス内で共有しやすいもの:
	アドレス空間
	ヒープ
	メモリマップ
	開いているfd
	多くのプロセス属性
```

Linuxカーネルの見方は、伝統的な教科書的説明より少し独特です。
Linuxは内部的には、スレッドを「多くの資源を共有している実行単位」として扱います。
実装上は、まったく別の概念体系を持つというより、共有フラグ付きで作られたタスクの集まりとして理解すると分かりやすいです。

そのため、ユーザー空間から見ると「1つのプロセスの中に複数スレッドがある」ように見えても、カーネルのスケジューリング対象としては各スレッドがそれぞれ個別のタスクとして扱われます。
つまり、CPUを取り合う単位はプロセスというより、実際にはスレッドだと考える方が実践的です。

```text
ユーザー空間の見え方:
	1プロセスの中に複数スレッドがある

カーネルの見え方:
	共有資源を持つ複数タスクを個別にスケジュールする
```

マルチスレッドプログラミングでは、POSIX Threads、つまり pthread が広く使われます。
ただし、pthread API はかなり大きく、ロック、条件変数、スレッド属性、キャンセル、TLSなど多くの話題があります。
このノートでは、pthread 自体の詳細よりも、「スレッドもスケジューラから見れば独立した実行単位である」という点をまず押さえておけば十分です。

### ６章の２　プロセッサを自発的に手離す

LinuxはプリエンプティブなOSですが、現在走っているタスクが自分からCPUを譲るための仕組みもあります。
代表的なのが `sched_yield()` です。

```c
#include <sched.h>

int sched_yield(void);
```

`sched_yield()` は、呼び出し元が「いまは他の実行可能タスクへ譲ってもよい」とスケジューラへ伝えるための関数です。
呼び出し後、カーネルは別の実行可能タスクを選ぼうとします。

ただし、他に適切な実行可能タスクがなければ、ほぼすぐ同じタスクが再開されることもあります。
そのため、「必ず誰かに長くCPUを渡せる」と期待して使うものではありません。

```text
sched_yield() のイメージ:
	いまの実行権をいったん手放す
	他に走るべきタスクがいればそちらへ回る
	いなければ自分がすぐ再開することもある
```

実用上は、`sched_yield()` はあまり多用されません。
理由は単純で、普通はカーネルのスケジューラの方が、個々のアプリケーションより全体最適をうまく判断できるからです。

戻り値は成功時に0、失敗時に-1です。
ただし通常のLinux環境では、実際にエラーになる場面はかなり限られます。

```c
if (sched_yield() == -1) {
	perror("sched_yield");
}
```

#### ６章の２の１　正しい使い方をどう考えるか

古い説明では、「他のプロセスへ親切にCPUを譲るために `sched_yield()` を呼ぶ」という話が出てきます。
しかし、現在のLinuxで一般的なアプリケーションを書く立場では、これは通常おすすめしにくいです。

まず、CPUをいつ誰に回すべきかは、カーネルの方が全体像を見ています。
ユーザー空間の1プロセスは、自分以外のタスク状況、優先度、CPUコア、I/O待ち、リアルタイムタスクの存在などを十分には把握できません。
そのため、自前で「ここで譲った方がいいはず」と決めても、全体最適になるとは限りません。

次に、`sched_yield()` を待機の代わりに使うと、設計が悪化しやすいです。
たとえば「相手の準備ができるまで繰り返し `sched_yield()` する」という実装は、実質的にはビジーウェイト寄りであり、きれいな待機方式とは言えません。

```c
do {
	while (!producer_ready()) {
		sched_yield();
	}

	process_data();
} while (!time_to_quit());
```

このような形は、一見するとCPUを譲っているので穏当そうに見えます。
しかし、本質的には「準備ができたか」を何度も見に行くポーリングの変種です。
Unix系の設計では、こういう場面ではブロック可能な仕組みを使う方が自然です。

#### ６章の２の２　経緯と現状

古いLinuxの本では、`sched_yield()` の挙動がカーネルバージョンごとにかなり違って見える説明が出てきます。
特に Linux 2.4 系から 2.6 初期へ移る時期には、スケジューラの実装が大きく変わったため、この関数の印象も変わりました。

2.4系や古い時代の説明では、`sched_yield()` は「他に実行可能なタスクがいれば、そちらへ少し譲る」比較的マイルドな操作として理解されることがありました。
つまり、ランキューの末尾へ回ることで、少なくとも自分以外へ実行機会を与えようとする動きです。

その後、2.6初期の O(1) スケジューラ時代には、`active` キューと `expired` キューを使う説明がよく見られます。
この時代の説明では、`sched_yield()` を呼ぶと、非リアルタイムタスクは実質的に「いまのラウンドでは後回し」に近い扱いを受ける、と整理されることがありました。

```text
歴史的な説明の流れ:
	古い時代は比較的マイルドに譲る
	2.6初期の O(1) では後回し効果が強く見える
```

この変更の背景には、いわゆるピンポン問題を避けたいという事情があります。
たとえば2つのタスクAとBが、どちらもすぐ `sched_yield()` するような作りだと、A、B、A、B と細かく切り替わり続け、進捗より切り替えの印象ばかり強くなります。

```text
ピンポンの例:
	A が yield
	B が yield
	A が yield
	B が yield
```

このような挙動は、他のタスクがいる環境では公平性や効率の面で好ましくありません。
そのため、「yield したタスクをすぐまた前へ戻しすぎない」方向の実装が取られてきました。

ただし、ここで注意が必要です。
この `active` キューと `expired` キューの説明を、そのまま現在の Linux 6.x 系へ持ち込むのは不正確です。

現在のLinuxでは、長く中心だったのは CFS であり、最近のカーネルでは EEVDF の考え方も取り込まれています。
そのため、2.6初期のO(1)スケジューラで出てくる内部キュー構造を、そのまま今の挙動説明として覚える必要はありません。

現代的には、次のように理解するのがよいです。

```text
2.6初期の説明としては:
	active/expired キューの話が出てくる

現在の6.x系では:
	その内部構造を前提にしない
	sched_yield() は「自分を有利にしない」方向へ働く
	待機や同期の主手段としては使わない
```

つまり、歴史的には「マイルドに譲る時代」や「かなり後回しになる時代」がありましたが、現在の実践で重要なのは細かな内部キュー名よりも、`sched_yield()` を安易な待機手段として使うべきではない、という点です。

リアルタイムスケジューリングクラスでは、また別の意味を持つことがあります。
特に `SCHED_FIFO` や `SCHED_RR` の文脈では、同優先度タスク間で実行順に影響することがあるため、通常クラスより意味を持ちやすいです。
ただし、通常のアプリケーションが使う公平クラスでは、まずブロック待ちを優先する方が自然です。

#### ６章の２の３　ブロック待ちを使う方が自然

Unix系プログラムでは、イベント待ちはなるべくブロッキングI/Oや待機APIで表現する方がよいです。
たとえば次のような手段があります。

```text
pipe
socket
eventfd
futex
poll / ppoll
select / pselect
epoll_wait
read がブロックするfd
```

この形なら、待つ側は本当に眠れます。
データやイベントが届くまでCPUを消費せず、必要になったときだけカーネルが起こしてくれます。

```text
よい待ち方:
	待つ間はブロックする
	必要なときだけ起床する

避けたい待ち方:
	何度も状態を見に行く
	譲ることを繰り返しながら待つ
```

スレッド同期でも同じ発想が重要です。
現在のLinuxでは、ユーザー空間ロックの実装は futex を基盤にしており、「少しだけユーザー空間で試し、だめならカーネル待機へ入る」という効率のよい形が使われます。
そのため、昔よりも `sched_yield()` を同期の切り札として使う理由はかなり減っています。

#### ６章の２の４　行儀よく振る舞うために yield するべきか

CPUバウンドな処理を書くと、「たまに `sched_yield()` しておいた方が他のアプリに優しいのでは」と考えたくなることがあります。
しかし、多くの場合はそれも最善ではありません。

なぜなら、どの程度CPUを使わせるかは、基本的にスケジューラが決める仕事だからです。
さらに、ユーザーは必要なら `nice` や cgroup、CPU affinity、リアルタイム優先度設定などで方針を調整できます。

つまり、アプリケーションが自前で断続的に `sched_yield()` を差し込むより、まずは普通に書き、必要なら優先度や資源制御で調整する方が筋がよいことが多いです。

例外として、非常に特殊な低レベル実装、ベンチマーク、実験コード、あるいは特定のスケジューリング条件を前提にした制御では `sched_yield()` を使うことがあります。
ただし、それは一般的なアプリケーション設計の第一選択ではありません。

UmuOSの視点では、`yield` 系の仕組みはスケジューラ実装の理解には役立ちます。
最初の自作OSでは「自発的にCPUを返すAPI」を用意すると動作確認しやすいでしょう。
しかし、最終的に使いやすいOSに近づけるには、`yield` へ頼るより、ブロック・起床・優先度・プリエンプションの仕組みを整える方が本質的です。

### ６章の３　プロセスの優先度

ここで扱うのは、通常のプロセス、つまり一般的なスケジューリングクラスで動くタスクの優先度です。
リアルタイムプロセスでは別のルールと優先度体系が使われるため、同じ感覚のまま読むと混乱します。
リアルタイム系については後の節で分けて考えます。

Linuxは、実行可能なタスクを無差別に並べているわけではありません。
タスクには優先度に相当する情報があり、それが「どのくらいCPU時間をもらいやすいか」に影響します。
歴史的なUnixでは、この調整値を nice 値と呼んできました。

```text
nice値が小さい:
	優先されやすい
	他より多めにCPU時間を得やすい

nice値が大きい:
	優先されにくい
	他へCPU時間を譲る方向になる
```

名前の由来は、「他のプロセスに対して行儀よくなる」という昔ながらの言い方です。
数値の向きは少し直感に反していて、nice値が小さいほど優先度は高く、nice値が大きいほど優先度は低くなります。

古い本では、「nice値はいつ実行されるかを決め、さらにタイムスライス長も決める」と説明されることがあります。
これは昔のLinuxを理解するには役立ちます。
ただし現在のLinuxでは、nice値を固定長タイムスライスの長さへそのまま直結させて理解するのは少し古いです。

現代的には、nice値は主に「通常クラス内でどれくらいCPU配分の重みを持つか」に効く、と考える方が実態に近いです。
つまり、nice値が小さいタスクほどCPUをもらいやすくなり、nice値が大きいタスクほど相対的に譲る側になります。

nice値の範囲は通常 `-20` から `19` で、デフォルトは `0` です。

```text
-20:
	非常に高い優先度

0:
	通常の優先度

19:
	かなり低い優先度
```

一般ユーザーが使う範囲では、「重い処理を少し控えめに動かしたいなら nice値を大きくする」と覚えると分かりやすいです。

#### ６章の３の１　nice()

もっとも単純なAPIの1つが `nice()` です。

```c
#include <errno.h>
#include <stdio.h>
#include <unistd.h>

int nice(int inc);
```

`nice()` は、現在の nice 値へ `inc` を加算する形で変更します。
つまり絶対値指定ではなく、増減指定です。

```text
nice(10):
	nice値を10増やす
	優先度を下げる方向

nice(-5):
	nice値を5減らす
	優先度を上げる方向
```

通常、一般ユーザーは nice 値を大きくする、つまり自分の優先度を下げる方向には動かせます。
反対に、nice 値を小さくして優先度を上げるには、`CAP_SYS_NICE` などの権限が必要です。

`nice()` は成功時に新しい nice 値を返しますが、`-1` が正しい nice 値として返りうるため、エラー判定では `errno` も合わせて見る必要があります。

```c
int ret;

errno = 0;
ret = nice(10);
if (ret == -1 && errno != 0) {
	perror("nice");
} else {
	printf("nice value is now %d\n", ret);
}
```

現在の nice 値を知りたいだけなら、`inc` に `0` を渡せます。

```c
int ret;

errno = 0;
ret = nice(0);
if (ret == -1 && errno != 0) {
	perror("nice");
} else {
	printf("current nice value = %d\n", ret);
}
```

ただし、実用コードでは `nice()` より `getpriority()` と `setpriority()` の方が柔軟で分かりやすいことが多いです。

#### ６章の３の２　getpriority() と setpriority()

`getpriority()` と `setpriority()` は、nice値をより明示的に扱うためのAPIです。

```c
#include <errno.h>
#include <stdio.h>
#include <sys/resource.h>

int getpriority(int which, id_t who);
int setpriority(int which, id_t who, int prio);
```

`which` には、対象の種類を指定します。

```text
PRIO_PROCESS:
	プロセスIDを対象にする

PRIO_PGRP:
	プロセスグループIDを対象にする

PRIO_USER:
	ユーザーIDを対象にする
```

`who` に `0` を渡すと、「現在のプロセス」「現在のプロセスグループ」「現在のユーザー」といった現在値を意味します。

`getpriority()` は、指定された範囲の中で、もっとも高い優先度、つまりもっとも小さい nice 値を返します。
`setpriority()` は、指定範囲に属する対象へ nice 値を設定します。

現在プロセスの nice 値を取得する簡単な例です。

```c
int ret;

errno = 0;
ret = getpriority(PRIO_PROCESS, 0);
if (ret == -1 && errno != 0) {
	perror("getpriority");
} else {
	printf("nice value = %d\n", ret);
}
```

現在のプロセスグループ全体の nice 値を `10` へ変更する例です。

```c
if (setpriority(PRIO_PGRP, 0, 10) == -1) {
	perror("setpriority");
}
```

優先度を上げる、つまり nice 値を小さくするには、やはり特権が必要です。
また、自分以外のユーザーのプロセスへ変更をかける場合も制約があります。

代表的なエラーは次の通りです。

```text
EACCES:
	十分な権限がないまま優先度を上げようとした

EINVAL:
	which が不正

EPERM:
	対象を変更する権限がない

ESRCH:
	対象プロセスや対象集合が見つからない
```

`nice()` と違って、`setpriority()` は絶対値で指定できるので、実用上はこちらの方が扱いやすい場面が多いです。

#### ６章の３の３　nice値と現在のLinuxの関係

古い説明では、nice値の違いがタイムスライス長の違いとしてかなり直接的に語られることがあります。
しかし現在のLinuxでは、通常クラスのスケジューラは、単純に「niceが低いから固定で何ミリ秒長い」という理解ではありません。

むしろ、nice値はCPU配分の重みに影響すると考える方が自然です。
複数のCPUバウンドタスクが同時に走っているとき、nice値が小さい方が相対的に多くCPU時間を得やすくなります。

```text
現在の理解:
	nice値はCPU配分の重みへ影響する
	固定長タイムスライスだけで理解しない方がよい
```

また、対話的なI/Oバウンドタスクは、単に nice 値だけでなく、起床直後の扱いやレイテンシ配慮によって体感が左右されます。
そのため、実際の応答性は「nice値だけですべて決まる」わけではありません。

さらに現在のLinuxでは、サービス単位の制御に cgroup を使う場面も多いです。
そのため、大規模運用ではプロセス単体の nice 値だけでなく、cgroup v2 の CPU 制御も合わせて考えることがあります。

#### ６章の３の４　I/O の優先度

CPUスケジューリングの優先度とは別に、Linuxには I/O 優先度の考え方もあります。
これは、ディスクなどのブロックI/Oをどのくらい優先して処理するかに関係します。

古い本では、I/O優先度は主に CFQ I/O スケジューラと結びつけて説明されることが多いです。
しかし、CFQ は現在の主流カーネルでは中心的存在ではありません。
そのため、この説明をそのまま現在のLinuxへ当てはめるのは少し古いです。

現代的には、次のように読むと分かりやすいです。

```text
I/O優先度は存在する
ただし有効性は利用中のブロックI/Oスケジューラや環境に依存する
CPUのnice値ほど一律には効かない
```

Linuxカーネルには `ioprio_get` と `ioprio_set` に対応するシステムコールがあります。
ただし、glibc が一般的なラッパ関数を常に提供しているとは限らないため、Cプログラムから使うには `syscall()` を使うことがあります。

```c
#define _GNU_SOURCE
#include <sys/syscall.h>
#include <unistd.h>

int ioprio_get(int which, int who)
{
	return (int)syscall(SYS_ioprio_get, which, who);
}

int ioprio_set(int which, int who, int ioprio)
{
	return (int)syscall(SYS_ioprio_set, which, who, ioprio);
}
```

学習や運用では、Cコードから直接呼ぶより、まず `ionice` コマンドで挙動を見る方が分かりやすいことも多いです。

```bash
ionice -c2 -n7 some_command
```

ただし、I/O優先度は「設定すれば必ず体感差が出る」とは限りません。
ストレージの種類、I/Oスケジューラ、cgroup設定、NVMe環境かどうか、バッファキャッシュの効き方などで効果の見え方はかなり変わります。

現在のLinuxでは、サービス単位でI/O帯域を調整したいなら、cgroup v2 の I/O 制御や systemd のリソース制御と合わせて考える場面も増えています。

#### ６章の３の５　UmuOSでどう考えるか

UmuOSで最初に実装する優先度は、そこまで複雑でなくてよいと思います。
まずは、実行可能キュー内の順番に少し差をつけられる程度でも十分です。

```text
第一段階:
	全プロセス同一優先度

第二段階:
	整数の優先度を持たせる

第三段階:
	優先度と待ち時間の両方を考える

第四段階:
	I/O待ちからの復帰を少し優遇する
```

Linuxのような重み付き公平スケジューリングや cgroup 連携まで最初から再現する必要はありません。
しかし、「優先度は単なる飾りではなく、CPU時間の配分方針そのものに関わる」という理解は、シェルやOS設計を進めるうえで非常に重要です。

### ６章の４　プロセッサアフィニティ

Linuxは、複数CPUを持つシステムでも動作します。
現在では、物理CPUが複数あるだけでなく、1つのCPUパッケージに複数コアがあり、さらにSMT、いわゆるハイパースレッディングで論理CPUが増えている構成も珍しくありません。

そのためスケジューラは、「どのタスクを実行するか」だけでなく、「どのCPUで実行するか」も考えなければなりません。

```text
マルチCPU環境での課題:
	空いているCPUを遊ばせすぎない
	必要以上にタスクをCPU間で動かしすぎない
	キャッシュやNUMA局所性を壊しすぎない
```

あるタスクを一度特定のCPUで動かすと、そのCPUのキャッシュに命令やデータが乗ります。
その後も同じCPUで動かせれば、キャッシュヒットが増えやすく、性能面で有利です。
これが、同じCPUへできるだけ留めたい理由です。

古い説明では、「別のCPUへ移すと、そのCPU固有のキャッシュが使えず不利になる」と整理されます。
この理解は今でも本質的には正しいです。
ただし現在は、共有L3キャッシュ、NUMAノード、SMT sibling、メモリ局所性なども絡むため、単純に「CPUごとのキャッシュ」だけで語り切れないことがあります。

それでも、タスクをむやみに別CPUへ移すと、キャッシュの温まり直しやメモリアクセスの局所性悪化が起こりやすく、コストになる点は変わりません。

一方で、あるCPUだけに負荷が集中し、他のCPUが遊んでいるなら、タスクを移した方が全体性能はよくなることがあります。
このバランスを取るのがロードバランスです。

```text
同じCPUに残す利点:
	キャッシュ局所性がよい
	移動コストを減らせる

他CPUへ移す利点:
	全CPUを有効活用しやすい
	偏った負荷をならせる
```

#### ６章の４の１　ソフトアフィニティとハードアフィニティ

プロセッサアフィニティとは、あるタスクが特定のCPUで動き続けやすい性質や制約を指します。

ソフトアフィニティは、スケジューラが「できれば同じCPUで走らせたい」と考える性質です。
これはユーザーが厳密に固定したものではなく、負荷状況次第では他CPUへ移ることがあります。

ハードアフィニティは、明示的に「このCPU集合の上でだけ動け」と制約をかけるものです。
こちらはカーネルに対する強い条件です。

```text
ソフトアフィニティ:
	なるべく同じCPUで動かしたい
	必要なら移動する

ハードアフィニティ:
	指定したCPU集合の中でしか動けない
```

通常のLinuxでは、何も設定しなければスケジューラがソフトアフィニティとロードバランスの間を自動で調整します。
それでも、ベンチマーク、低レイテンシ処理、CPU専有ワーカー、IRQとの棲み分け、NUMA検証などでは、ユーザーが明示的にCPUを縛りたいことがあります。

#### ６章の４の２　sched_getaffinity() と sched_setaffinity()

Linuxでは、プロセスやスレッドのCPUアフィニティを取得・変更するために `sched_getaffinity()` と `sched_setaffinity()` が使えます。

```c
#define _GNU_SOURCE
#include <sched.h>

int sched_setaffinity(pid_t pid, size_t cpusetsize, const cpu_set_t *mask);
int sched_getaffinity(pid_t pid, size_t cpusetsize, cpu_set_t *mask);
```

`pid` に `0` を渡すと、呼び出し元自身を意味します。
Linuxでは、実際にはスケジューリングされる単位はスレッドなので、この `pid` は用途によってはスレッドIDとして扱う方が実感に合います。

`cpu_set_t` はCPU集合を表す型で、専用マクロで操作します。

```c
CPU_ZERO(&set);
CPU_SET(cpu, &set);
CPU_CLR(cpu, &set);
CPU_ISSET(cpu, &set);
```

現在のアフィニティを表示する簡単な例です。

```c
#define _GNU_SOURCE
#include <sched.h>
#include <stdio.h>
#include <stdlib.h>

int main(void)
{
	cpu_set_t set;
	int cpu;

	CPU_ZERO(&set);

	if (sched_getaffinity(0, sizeof(set), &set) == -1) {
		perror("sched_getaffinity");
		return EXIT_FAILURE;
	}

	for (cpu = 0; cpu < CPU_SETSIZE; cpu++) {
		if (CPU_ISSET(cpu, &set)) {
			printf("cpu %d: set\n", cpu);
		}
	}

	return EXIT_SUCCESS;
}
```

この例では、実際に許可されているCPUだけを表示しています。
古い本のように `CPU_SETSIZE` まで全部 `set/unset` を列挙すると、現在の環境では情報量が多すぎることがあります。

#### ６章の４の３　特定CPUへバインドする

たとえば CPU 0 だけで実行させたいなら、次のようにします。

```c
#define _GNU_SOURCE
#include <sched.h>
#include <stdio.h>
#include <stdlib.h>

int main(void)
{
	cpu_set_t set;

	CPU_ZERO(&set);
	CPU_SET(0, &set);

	if (sched_setaffinity(0, sizeof(set), &set) == -1) {
		perror("sched_setaffinity");
		return EXIT_FAILURE;
	}

	puts("bound to CPU 0");
	return EXIT_SUCCESS;
}
```

この状態では、呼び出し元はCPU 0を含む許可集合の上でだけ動作します。
ただし、実際にそのCPUが利用可能か、cpuset制約やコンテナ制約の中で見えているか、権限が足りているか、といった条件で結果は変わります。

また、現在のLinuxでは `taskset` コマンドで確認・変更することも多いです。

```bash
taskset -p $$
taskset -c 0 ./some_program
```

学習段階では、まず `taskset` で挙動を観察してから、Cコードで `sched_setaffinity()` を使うと分かりやすいです。

#### ６章の４の４　CPU_SETSIZE と最近の注意点

古い説明では、`CPU_SETSIZE` が 1024 であることを前提にした例がよく出てきます。
glibc環境では今でもその前提が成り立つことは多いですが、ここで重要なのは `CPU_SETSIZE` は「`cpu_set_t` で扱えるCPU数の上限」であって、`cpu_set_t` のバイトサイズそのものではないことです。

```text
cpusetsize に渡すもの:
	sizeof(cpu_set_t)

CPU_SETSIZE:
	扱えるCPU番号の上限側の定数
```

さらに、大規模なマシンや特殊環境では、固定長の `cpu_set_t` では足りないことがあります。
その場合は `CPU_ALLOC()`、`CPU_ALLOC_SIZE()`、`CPU_FREE()` などの動的CPU集合マクロを使う方法もあります。

このノートではまず固定長 `cpu_set_t` を押さえれば十分ですが、将来より大きなシステムを扱うなら覚えておく価値があります。

#### ６章の４の５　エラーと制約

代表的なエラーは次のようなものです。

```text
EFAULT:
	無効なポインタを渡した

EINVAL:
	cpusetsize が小さすぎる
	存在しないCPUだけを指定した
	有効なCPUが1つも残らない

EPERM:
	対象タスクを変更する権限がない

ESRCH:
	対象のpidやスレッドが存在しない
```

ただし、現在のLinuxでは「指定したCPUにバインドできるか」は、単に物理CPUがあるかどうかだけでは決まりません。
cpuset cgroup、コンテナの制限、管理者が設定した許可CPU集合の影響も受けます。

つまり、「CPU 7 が物理的には存在するのに設定できない」ということがありえます。
これはバグではなく、上位の制約でそのCPUが見えない、あるいは使用禁止になっている可能性があります。

#### ６章の４の６　いつ使うべきか

CPUアフィニティは、使えば常に速くなる魔法ではありません。
むしろ固定しすぎると、ロードバランスの恩恵を失って遅くなることもあります。

有効になりやすい場面としては、次のようなものがあります。

```text
ベンチマークで揺らぎを減らしたい
特定ワーカーを特定CPUへ寄せたい
IRQ処理とユーザータスクを分けたい
NUMA局所性を観察したい
リアルタイム寄りの処理でCPUを隔離したい
```

逆に、普通のアプリケーションでは、まずカーネルの自動ロードバランスに任せる方が自然です。
アフィニティ固定は、計測や明確な要件があるときに使うものと考えた方が安全です。

#### ６章の４の７　UmuOSでどう考えるか

UmuOSで最初からSMPやNUMAを完全実装する必要はありません。
最初は単一CPU前提でも十分です。

ただし、将来的に複数CPUへ広げるなら、次の考え方は重要になります。

```text
実行可能キューをCPUごとに持つか
タスクを他CPUへ移す条件をどう決めるか
キャッシュ局所性をどこまで意識するか
ユーザーにCPU固定APIを与えるか
```

単純な設計としては、「各CPUにローカルキューを持たせ、空いたCPUが他から仕事を奪う」形も考えられます。
Linuxの実装はもっと複雑ですが、根本には「局所性を壊しすぎず、偏りすぎた負荷をならす」という同じ問題があります。

そのため、プロセッサアフィニティは単なる最適化オプションではなく、マルチCPU時代のスケジューラ設計そのものに深く関わる概念です。

### ６章の５　リアルタイムシステム

コンピュータ分野で リアルタイム という言葉は、しばしば「とても速い」という意味で誤解されます。
しかし本来の意味は、処理要求に対して、守るべき時間制約、つまりデッドラインがあることです。

```text
リアルタイムの本質:
	速さそのものではない
	決められた時間内に応答できることが重要
```

たとえば、自動車のABS、医療機器、制御装置、産業機械、通信機器などでは、応答が遅れすぎると機能や安全性に直接影響します。
こうした世界では、「平均的に速い」だけでは足りず、「遅れてはいけない」という性質の方が重要です。

Linuxも何らかのリアルタイム対応を持っています。
ただし、汎用Linuxがそのまま厳密なハードリアルタイムOSと同じ意味になるわけではありません。
まずは、何をもってリアルタイムと呼ぶのかを分けて考える必要があります。

#### ６章の５の１　ハードリアルタイムシステムとソフトリアルタイムシステム

リアルタイムには大きく分けて、ハードリアルタイムとソフトリアルタイムがあります。

```text
ハードリアルタイム:
	デッドライン超過が失敗そのものになる
	超過は致命的な不具合として扱う

ソフトリアルタイム:
	デッドライン超過は望ましくない
	ただし即座に致命的失敗とは限らない
```

ハードリアルタイムの例としては、制御系、安全系、厳密な期限が必要な装置が分かりやすいです。
ソフトリアルタイムの例としては、動画再生、音声処理、ゲーム、通信処理、ある種の対話的アプリケーションなどがあります。

たとえば動画再生でフレームが少し落ちても、体験は悪くなりますが、即座にシステム破綻とは限りません。
一方で制御系では、期限超過そのものが故障や危険につながることがあります。

ここで重要なのは、リアルタイムは「とにかく最速」という意味ではないことです。
同じハードウェアなら、厳密な予測可能性を確保するための制御やオーバーヘッドがあるぶん、平均スループットだけ見れば汎用システムの方が高いこともありえます。

また、ハードかソフトかは、単純にデッドラインが短いか長いかでは決まりません。
短い締切のソフトリアルタイムもあれば、比較的長い締切のハードリアルタイムもあります。

#### ６章の５の２　レイテンシ、ジッタ、デッドライン

リアルタイムを考えるときは、少なくとも次の3語を区別しておく必要があります。

```text
デッドライン:
	この時刻までに応答しなければならない期限

レイテンシ:
	要求発生から応答までの遅延時間

ジッタ:
	応答時間のばらつき
```

レイテンシは、要求が出てから応答が返るまでの時間そのものです。
ジッタは、その応答時間がどれくらい揺れるかを表します。

ソフトリアルタイムの世界では、平均レイテンシだけでなく、ばらつきの少なさ、つまりジッタの小ささもかなり重要です。
音声再生や動画出力では、平均的には速くても、たまに大きく遅れると体感品質が急に悪くなります。

古い資料では「レイテンシを測っているつもりでも、実際にはジッタを見ていることが多い」と説明されることがあります。
これは今でも有効な注意です。
要求発生時刻を厳密に取れていないと、観測しているのは要求から応答までの絶対時間ではなく、周期的応答のばらつきになりやすいからです。

```text
観測で起こりやすい混同:
	要求発生時刻が厳密に取れない
	応答時刻だけを見てしまう
	結果としてジッタを見ているのにレイテンシだと思い込む
```

レイテンシもジッタも重要ですが、別物です。
リアルタイムの議論では、この2つを混同しない方が整理しやすいです。

#### ６章の５の３　Linuxのリアルタイム対応

LinuxはPOSIX系のリアルタイムスケジューリングAPIを実装しており、少なくともソフトリアルタイム用途には広く使われています。
ただし、標準の汎用カーネルがそのまま厳密なハードリアルタイム保証をする、という理解は避けた方が安全です。

現在のLinuxは、長年の改善により、一般用途でもレイテンシや応答性がかなり向上しています。
さらに PREEMPT_RT 系の改善は、以前は外部パッチとして語られることが多かったですが、現在では相当部分がメインラインへ取り込まれています。
そのため、古い本にある「リアルタイム改善はほとんど本流外にある」という記述は、今ではそのままでは古いです。

現代的には、次のように整理すると分かりやすいです。

```text
標準のLinux:
	ソフトリアルタイム用途に広く使われる
	一般的な応答性も高い

PREEMPT_RT系の改善:
	メインラインへかなり取り込まれてきた
	より低レイテンシで予測しやすい動作を目指す
```

それでも、厳密なハードリアルタイム保証が必要な用途では、カーネル設定、ドライバ、割り込み、CPU隔離、メモリロック、ユーザー空間設計などを含めて、システム全体として詰める必要があります。

#### ６章の５の４　Linuxのスケジューリングポリシーと優先度

Linuxでは、タスクのスケジューリングポリシーによって動作が変わります。
古い本では `SCHED_FIFO`、`SCHED_RR`、`SCHED_OTHER` の3つが中心に紹介されることが多いです。
これは重要ですが、現在のLinuxでは `SCHED_BATCH`、`SCHED_IDLE`、さらに `SCHED_DEADLINE` もあります。

大まかには次のように整理できます。

```text
SCHED_OTHER:
	通常の非リアルタイムポリシー

SCHED_BATCH:
	対話性をあまり重視しないバッチ向け

SCHED_IDLE:
	極めて低い優先度の通常クラス

SCHED_FIFO:
	リアルタイムのFIFO

SCHED_RR:
	リアルタイムのラウンドロビン

SCHED_DEADLINE:
	期限と実行予算を明示するリアルタイム系
```

ここで、通常クラスとリアルタイムクラスは分けて考える必要があります。
`SCHED_FIFO` や `SCHED_RR` の静的優先度は通常 `1` から `99` の範囲です。
一方、通常クラス側では、nice値による相対的な重み付けが行われます。

古い資料では「通常プロセスの静的優先度は常に0」といった説明が見られます。
概念整理としては悪くありませんが、現在のLinux内部実装をそのまま表していると考えない方がよいです。
学習上は、次のように理解すると十分です。

```text
通常クラス:
	nice値で相対的なCPU配分を調整する

リアルタイムクラス:
	明示的なリアルタイム優先度で通常クラスより優先される
```

`SCHED_FIFO` や `SCHED_RR` の実行可能タスクが存在すると、通常クラスのタスクは基本的にCPUを奪われます。
そのため、リアルタイムポリシーの設定は非常に強力で、誤るとシステム全体の応答性を壊すことがあります。

##### ６章の５の４の１　ファーストイン/ファーストアウトポリシー

`SCHED_FIFO` は、非常に単純なリアルタイムポリシーです。
同じ優先度帯では、基本的にFIFO順で実行されます。
タイムスライスはありません。

そのため、ある `SCHED_FIFO` タスクが、現在もっとも高い実行可能優先度を持っているなら、そのタスクは次のどれかが起こるまで走り続けます。

```text
自分でブロックする
sched_yield() を呼ぶ
より高い優先度のリアルタイムタスクが実行可能になる
```

同じ優先度の `SCHED_FIFO` タスクが複数ある場合は、ランキュー内の順序が重要になります。
`sched_yield()` を呼ぶと、同優先度キューの後ろへ回るイメージで考えると分かりやすいです。

このポリシーは強力ですが、誤用するとCPUを長時間占有しやすいため、非常に注意が必要です。

##### ６章の５の４の２　ラウンドロビンポリシー

`SCHED_RR` は、`SCHED_FIFO` とほぼ同じ考え方ですが、同一優先度の複数タスクの間に時間片を導入します。

```text
SCHED_RR:
	リアルタイム優先度で動く
	同じ優先度内では順番に回す
	時間片を使い切ると同優先度キューの後ろへ回る
```

同じ優先度のタスクが1つしかいなければ、実質的な挙動は `SCHED_FIFO` にかなり近くなります。
違いが目立つのは、同優先度のリアルタイムタスクを複数動かすときです。

##### ６章の５の４の３　ノーマルポリシー

`SCHED_OTHER` は、通常の非リアルタイムポリシーです。
普段のシェル、エディタ、ビルド、サーバなど、多くのプロセスはここで動きます。

現在のLinuxでは、このクラスの内部挙動は CFS や EEVDF などの通常スケジューラの仕組みで決まります。
ここでは nice 値が相対的なCPU配分へ影響します。

##### ６章の５の４の４　バッチポリシーとアイドルポリシー

古い本では `SCHED_BATCH` をバッチまたはアイドルポリシーのように説明していることがありますが、現在は分けて考える方が正確です。

`SCHED_BATCH` は、対話性をあまり必要としないCPU消費型の仕事向けです。
通常クラスの一種ですが、対話的タスクほど起床直後の優遇を期待しない前提で使います。

`SCHED_IDLE` はさらに低く、他に実行すべき通常タスクがほぼないときにだけ走るような極低優先度の通常クラスです。

```text
SCHED_BATCH:
	バッチ処理向け
	通常クラスだが対話性は重視しない

SCHED_IDLE:
	極低優先度
	本当に余っているCPU時間で走るイメージ
```

##### ６章の５の４の５　SCHED_DEADLINE

現在のLinuxを語るなら、`SCHED_DEADLINE` にも触れておいた方がよいです。
これは deadline、runtime、period のようなパラメータで、より明示的に時間制約を扱うリアルタイム系ポリシーです。

ただし、`SCHED_DEADLINE` は `sched_setscheduler()` では設定できません。
専用の `sched_setattr()` 系インタフェースを使います。
このノートの現段階では、まず `SCHED_FIFO` と `SCHED_RR` を押さえる方が優先でよいでしょう。

#### ６章の５の５　スケジューリングポリシーの参照と設定

プロセスのスケジューリングポリシーを参照するには `sched_getscheduler()`、変更するには `sched_setscheduler()` を使います。

```c
#include <sched.h>

struct sched_param {
	int sched_priority;
};

int sched_getscheduler(pid_t pid);
int sched_setscheduler(pid_t pid, int policy,
			       const struct sched_param *param);
```

現在のポリシーを確認する簡単な例です。

```c
#include <sched.h>
#include <stdio.h>
#include <stdlib.h>

int main(void)
{
	int policy = sched_getscheduler(0);

	if (policy == -1) {
		perror("sched_getscheduler");
		return EXIT_FAILURE;
	}

	switch (policy) {
	case SCHED_OTHER:
		puts("policy: SCHED_OTHER");
		break;
	case SCHED_BATCH:
		puts("policy: SCHED_BATCH");
		break;
	case SCHED_IDLE:
		puts("policy: SCHED_IDLE");
		break;
	case SCHED_FIFO:
		puts("policy: SCHED_FIFO");
		break;
	case SCHED_RR:
		puts("policy: SCHED_RR");
		break;
	default:
		printf("policy: %d\n", policy);
		break;
	}

	return EXIT_SUCCESS;
}
```

リアルタイムポリシーへ変更する例です。

```c
#include <sched.h>
#include <stdio.h>
#include <stdlib.h>

int main(void)
{
	struct sched_param param = { .sched_priority = 1 };

	if (sched_setscheduler(0, SCHED_RR, &param) == -1) {
		perror("sched_setscheduler");
		return EXIT_FAILURE;
	}

	puts("policy changed to SCHED_RR priority 1");
	return EXIT_SUCCESS;
}
```

ただし、`SCHED_OTHER` 以外へ変更するには、通常は `CAP_SYS_NICE` などの権限が必要です。
そのため、一般ユーザーでは失敗することが多いです。
現在のLinuxでは、`RLIMIT_RTPRIO` により、一般ユーザーへ許可するリアルタイム優先度の上限を設定する運用もあります。

#### ６章の５の６　優先度範囲の確認

リアルタイム優先度の有効範囲は、移植性を考えるなら決め打ちしない方が安全です。
Linuxでは通常 `1` から `99` ですが、APIで確認できます。

```c
#include <sched.h>
#include <stdio.h>

int main(void)
{
	printf("SCHED_FIFO min=%d max=%d\n",
	       sched_get_priority_min(SCHED_FIFO),
	       sched_get_priority_max(SCHED_FIFO));

	printf("SCHED_RR min=%d max=%d\n",
	       sched_get_priority_min(SCHED_RR),
	       sched_get_priority_max(SCHED_RR));

	return 0;
}
```

この確認を入れておくと、コードの意図が分かりやすくなります。

#### ６章の５の７　エラーと実運用上の注意

`sched_getscheduler()` や `sched_setscheduler()` の代表的なエラーは次の通りです。

```text
EFAULT:
	無効なポインタを渡した

EINVAL:
	無効なポリシーやパラメータを渡した

EPERM:
	必要な権限がない

ESRCH:
	対象pidが存在しない
```

ただし、本当に注意すべきなのは、エラー番号そのものより、リアルタイム設定が成功したあとです。
不適切に高い優先度の `SCHED_FIFO` タスクを作ると、普通のシェルやデーモン、場合によっては入力処理まで押しのけてしまい、システムが固まったように見えることがあります。

```text
危険な例:
	CPUを使い続ける SCHED_FIFO タスクを高優先度で動かす
	ブロックしない
	yieldもしない
	結果として通常タスクがほとんど走れない
```

リアルタイム設定を試すときは、別の端末から止められるようにする、CPU時間制限を考える、テスト環境で行う、などの安全策を取った方がよいです。

#### ６章の５の８　UmuOSでどう考えるか

UmuOSで最初から完全なリアルタイムOSを目指す必要はありません。
しかし、優先度が通常タスクとまったく別枠で扱われるクラスがある、という考え方は非常に重要です。

```text
通常タスク:
	公平性や全体応答性を重視する

リアルタイムタスク:
	期限や優先順位を強く重視する
```

まずは通常スケジューラを作り、その上で「特定の高優先度タスクは通常タスクより常に先に走る」という仕組みを追加すると、リアルタイムクラスの感覚を掴みやすいです。
その後、FIFO型、RR型、期限ベース型へ段階的に進めると、Linuxの設計も理解しやすくなると思います。



