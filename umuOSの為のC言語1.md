
# umuOSの為のC言語 1 — 基礎から（基本文法と入出力の入口）

このシリーズは最終的に、UmuOS／ush／uim のソースコードを**自力で読める**ようになることを目標にします。  
ただし **第1巻はUmuOSに一切依存しない**「C言語の基本の基本」だけを、できる限りやさしく・丁寧に説明します。

## 第一章でやること：

- Cの「最小の形」を知る
- `printf` で数を表示できる
- 変数（宣言・代入・初期化）がわかる
- `scanf` / `puts` で読み込みと表示の入口を踏む

前提：

- OS：Linux想定（UmuOS周辺を触るので）
- コンパイラ：`gcc`（`clang`でも基本同じ）
- 規格：`-std=c17`

---


## まずは慣れよう

ここは「理屈より、まず形に慣れる」章です。  
最初は **コピペ→コンパイル→実行→出力を見る** を繰り返して、Cの雰囲気を掴みます。

### 整数の加算の結果を表示

まずは「足し算して表示」だけをやります。

```c
/* file: add.c */
#include <stdio.h>

int main(void)
{
    printf("1 + 2 = %d\n", 1 + 2);
    printf("10 + 30 = %d\n", 10 + 30);
    return 0;
}
```

ポイント：

- `1 + 2` は「式（expression）」で、結果は `3`
- `%d` は「整数（int）を表示する変換指定子」
- `\n` は改行

### プログラムとコンパイル

#### プログラムとは（超ざっくり）

自身が書いた `.c`（ソースコード）を、コンピュータが実行できる形（実行ファイル）に変換して動かします。

#### コンパイル手順（Linux）

ファイル名を `add.c` として保存したら：

```bash
gcc -Wall -Wextra -std=c17 -O0 add.c -o add
./add
```

- `-Wall -Wextra`：警告をたくさん出す（最初は特に重要）
- `-O0`：最適化なし（最初は挙動を追いやすい）

よくあるつまずき：

- `./add` の `./` を忘れると「見つからない」と言われる（カレントディレクトリは通常 PATH に入っていないため）

### コメント

コメントは「自分用メモ」です。基本的に実行には影響しません。

- 1行コメント：`// ...`
- 複数行コメント：`/* ... */`

例：

```c
// これは1行コメント
/*
   これは複数行コメント
*/
```

コメントのコツ：

- 「何をしているか」より「なぜそうするか」をコメントに書くと役に立つ

### プログラムの決まり文句

最小のCプログラムの“型”はこれです。

```c
#include <stdio.h>

int main(void)
{
    /* ここに処理を書く */
    return 0;
}
```

順番に意味を押さえます。

#### `#include <stdio.h>`

`printf` や `scanf` を使うための「宣言」が入ったヘッダを読み込みます。

ここでの理解としては：

- 「`printf` という道具を使っていいよ」とコンパイラに伝える

でOKです。

#### `int main(void)`

- `main` はプログラムの入口（ここから始まる）
- `int` は「終了コード（整数）を返す」
- `(void)` は「引数なし」

#### `{ ... }`（ブロック）

波かっこ `{` `}` の中が、`main` の中身です。

### printf関数

`printf` は「表示する」関数です。  
基本形は次の2種類をまず覚えます。

```c
printf("ただの文字\n");
printf("数を入れて表示: %d\n", 123);
```

#### `printf` の引数（かんたんに）

- 1つ目：**書式文字列**（format string）
- 2つ目以降：書式文字列の `%...` に当てはめる値

「%がある分だけ、後ろに値が必要」と覚えると事故が減ります。

### 文

**文（statement）**は「処理の単位」です。多くの文はセミコロン `;` で終わります。

例：

- `printf("hi\n");` ← 文（最後に `;`）
- `return 0;` ← 文

よくあるミス：

- `;` を忘れる（コンパイルエラーになる）

### 整数

整数は小数点のない数です。Cではまず `int` を使います。

```c
int a = 10;
int b = -3;
```

ここでは「`int` は整数」だけ覚えればOKです（サイズや範囲は後の巻で丁寧に扱います）。

### 書式文字列と変換指定

`printf` の中の `%d` のようなものを **変換指定子**（conversion specifier）と呼びます。

最初に覚える3つ：

- `%d`：整数（`int`）
- `%f`：小数（`double`）
- `%c`：1文字（`char`）

注意：`printf` と `scanf` では「小数」の指定が少し違います。

- `printf`：`%f` は `double`（`float` で渡しても内部で `double` に昇格する）
- `scanf`：`double` に読み込むなら `%lf` を使う

この巻では入力は `int` だけに絞るので、ここでは「そういう違いがある」とだけ覚えておけばOKです。

例：

```c
#include <stdio.h>

int main(void)
{
    int i = 42;
    double x = 3.14;
    char c = 'A';

    printf("i=%d\n", i);
    printf("x=%f\n", x);
    printf("c=%c\n", c);
    return 0;
}
```

重要：

- 型と変換指定子がズレると、表示が壊れたり危険（未定義動作）になり得る

### 記号文字の読み方

プログラムを読むための最低限の「読み方」をまとめます。

- `;` セミコロン
- `,` カンマ
- `()` 丸かっこ（丸括弧）
- `{}` 波かっこ（波括弧）
- `[]` 角かっこ（角括弧）
- `#` シャープ
- `"` ダブルクォート
- `'` シングルクォート
- `\` バックスラッシュ（環境によっては円記号に見えることも）
- `%` パーセント

### 書式化を行わない表示

「数を埋め込まない」表示です。

```c
printf("Hello\n");
```

これは「ただの文字列を表示する」だけ。

注意：

- `printf` は **改行を自動で付けない**。必要なら自分で `\n` を入れる。

### 文字列リテラル

`"Hello"` のようにダブルクォートで囲んだものを **文字列リテラル**と言います。

```c
printf("Hello, world!\n");
```

ここで重要な区別：

- `'A'`（シングルクォート）：**文字**（`char`）
- `"A"`（ダブルクォート）：**文字列**（複数文字。別物）

### 拡張表記

ここでは「難しい理屈」を後回しにして、現場でよく見る表記だけ先に紹介します。

#### エスケープシーケンス

文字列中で特別な意味を持つ `\` から始まる表記です。

- `\n`：改行
- `\t`：タブ
- `\\`：バックスラッシュそのもの
- `\"`：ダブルクォートそのもの

例：

```c
printf("1行目\n2行目\n");
printf("タブ\tで区切る\n");
```

#### 数値リテラルの表記（見かけるもの）

- `123`：10進
- `0x7b`：16進（`0x` で始まる）

UmuOSなど低レイヤのコードでは16進が頻繁に出ます。今は「0xが付くと16進」だけ覚えればOKです。

---

## 変数

ここから「値を覚えておく箱」を扱います。

### 変数と宣言

変数は「値を入れる箱」で、使う前に **宣言** が必要です。

```c
int a;      // a という名前の int 変数を用意（宣言）
double x;   // x という名前の double 変数を用意
```

宣言は「この名前はこういう型で使う」とコンパイラに伝える行為です。

### 代入

`=` は「等しい」ではなく **代入** です。

```c
int a;
a = 10;     // a に 10 を入れる
```

よくある混乱：

- `a = a + 1;` は「aを1増やす」という意味（数学の等号とは違う）

### 初期化

宣言と同時に値を入れるのが **初期化** です。

```c
int a = 10;     // 宣言 + 初期化
```

なぜ初期化が大事？

- 初期化しない変数の中身は「不定」になり得る（ゴミ値）
- ゴミ値を使うと原因不明の挙動になりやすい

まずは「変数は宣言したら、基本は初期化」くらいの感覚でOKです。

---

## 読み込みと表示

この章では、

- キーボードから数を読む（`scanf`）
- 計算する（四則演算）
- 文字列を表示する（`puts`）

をやります。

### scanf関数

`scanf` は「入力を読む」関数です。  
ただし、最初につまずきやすい点（`&` など）があるので、最小の使い方だけを丁寧に扱います。

まずはこれ：

```c
/* file: scan_add.c */
#include <stdio.h>

int main(void)
{
    int a = 0;
    int b = 0;

    printf("a を入力してください: ");
    scanf("%d", &a);

    printf("b を入力してください: ");
    scanf("%d", &b);

    printf("a + b = %d\n", a + b);
    return 0;
}
```

#### `&a` の `&` って何？

ここでは次のように捉えると安全です：

- `scanf` は「変数の場所」に書き込みたい
- だから `&a` で「aの場所（アドレス）」を渡す

（ポインタという概念の入口ですが、第1巻ではここまでで十分です）

#### scanfの注意（最初に知っておくと事故が減る）

- 入力に失敗することがある（文字を入れた、など）
- 本格的には戻り値チェックが必要

補足：`scanf` の戻り値は「読み取れた項目数」です。たとえば `scanf("%d", &a)` なら `1` が返れば成功です。

この巻では「動かしながら慣れる」優先で進めますが、後の巻で安全な読み込み方も扱います。

### 四則演算

四則演算は次の演算子です。

- `+` 足し算
- `-` 引き算
- `*` 掛け算
- `/` 割り算

例（入力した2つの数で計算）：

```c
/* file: calc.c */
#include <stdio.h>

int main(void)
{
    int a = 0;
    int b = 0;

    printf("a b をスペース区切りで入力してください: ");
    if (scanf("%d %d", &a, &b) != 2) {
        puts("入力に失敗しました");
        return 1;
    }

    printf("a + b = %d\n", a + b);
    printf("a - b = %d\n", a - b);
    printf("a * b = %d\n", a * b);

    // 注意：b が 0 だと a / b は危険（ゼロ除算）
    if (b != 0) {
        printf("a / b = %d\n", a / b); // int同士なので小数は切り捨て
    } else {
        printf("a / b は b が 0 なので計算できません\n");
    }

    return 0;
}
```

ここで押さえるポイント（重要）：

- `int` 同士の `/` は **整数除算**（小数点以下が切り捨て）
- `/` は b が 0 のとき危険（まず `b != 0` を確認する癖）

※ `if` はまだ本格的に説明していませんが、「0除算は危険」という事実を守るために最小限だけ使っています。次巻以降で条件分岐を体系的に学びます。

### puts

`puts` は「文字列を表示して、最後に改行を付ける」関数です。

```c
#include <stdio.h>

int main(void)
{
    puts("Hello");
    puts("puts は自動で改行します");
    return 0;
}
```

違い（ざっくり）：

- `printf`：書式化できる（`%d` など）／改行は自分で
- `puts`：書式化しない／改行は自動

---

## まとめ

- Cプログラムの最小構造：`#include` / `main` / `{}` / `return 0;`
- 文はだいたい `;` で終わる
- `printf`：書式文字列 + 変換指定子（`%d` など）
- 変数：宣言→代入→（できれば）初期化
- 入力：`scanf("%d", &a);` の形と、`&` の意味の入口
- 計算：四則演算、整数除算の切り捨て、ゼロ除算の危険
- `puts`：文字列を出して改行

---

## 第二章でやること：

- 「演算子」「オペランド」「式」「式文」という言葉を揃える
- `*` `/` と `+` `-` の優先順位を、括弧で意図どおりに固定できる
- `%`（剰余）で最下位桁を取り出し、`/` と組み合わせて桁を落とせる
- `printf` で複数の値を一度に表示できる（複数の変換指定）
- 単項演算子（`+x` `-x`）と、代入演算子（`=` `+=` など）に慣れる
- 整数型と浮動小数点型の違いを押さえ、必要なときにキャストできる

前提：この章も「何も知らなくても」読めるように書きますが、Cは細かい罠が多いので、危険なところははっきり注意を入れます。

---

## 演算と型

この章は「計算ができる」だけでなく、ソースコードを読むときに必ず出会う

- 演算の優先順位
- 代入が式であること
- 型が違う値を混ぜたときのルール

を、自身の手で確認できる形にします。

### 演算

#### 演算子とオペランド

用語を先に揃えます。

- **演算子**：`+` `-` `*` `/` `%` `=` のような記号
- **オペランド**：演算子が相手にする値（数や変数）

例：`a + b` では、`+` が演算子、`a` と `b` がオペランドです。

また、`a + b` のようなものは **式** で、評価すると「値」になります。

#### 乗除演算子と加減演算子（優先順位）

`*` と `/` は、`+` と `-` より先に計算されます。

```c
/* file: precedence.c */
#include <stdio.h>

int main(void)
{
    printf("1 + 2 * 3 = %d\n", 1 + 2 * 3);
    printf("(1 + 2) * 3 = %d\n", (1 + 2) * 3);
    return 0;
}
```

ポイント：

- `1 + 2 * 3` は `1 + (2 * 3)` と同じ
- 先に足したいときは括弧で意図を固定する

ソースを読むときも、「ここは括弧が無いけど優先順位でそうなる」という読み方が必要になります。

#### 最下位桁の値を求める（`%` と `/`）

`%` は **剰余（余り）** の演算子です。

- `n % 10`：10で割った余り → 一の位（最下位桁）
- `n / 10`：10で割った商（整数除算）→ 一の位を捨てる

```c
/* file: digits.c */
#include <stdio.h>

int main(void)
{
    int n = 1234;

    printf("n = %d\n", n);
    printf("n %% 10 = %d\n", n % 10);
    printf("n / 10 = %d\n", n / 10);

    return 0;
}
```

この考え方は、ログの桁分解や、数値を文字列にする処理などで頻繁に出ます。

補足：負の数を `%` に入れると余りの符号がややこしくなるため、この章では「`n` は 0以上」を想定します。

#### 複数の変換指定（`printf` に値を複数渡す）

`printf` は、書式文字列の中に `%...` を複数書けます。

```c
/* file: multi_format.c */
#include <stdio.h>

int main(void)
{
    int left = 12;
    int right = 34;

    printf("left=%d right=%d sum=%d\n", left, right, left + right);
    return 0;
}
```

ここで重要なのは次です。

- `%d` の数だけ、後ろに値が必要
- 値の順番は、左から順に対応する

順番や個数がズレると、表示が壊れたり危険（未定義動作）になり得ます。

#### 単項の算術演算子（`+x` と `-x`）

`+` と `-` は「2つの値の計算」だけでなく、「1つの値に作用する」形でも使われます。

- `a - b`：2項（引き算）
- `-a`：単項（符号を反転）

```c
/* file: unary.c */
#include <stdio.h>

int main(void)
{
    int value = 10;
    printf("value=%d\n", value);
    printf("-value=%d\n", -value);
    printf("+value=%d\n", +value);
    return 0;
}
```

`+value` は見かけることが少ないですが、「ここは明示的に正の値として扱う」という意図で出ることがあります。

#### 代入演算子（`=` と `+=` など）

代入は `=` だけではありません。

- `x = x + 3;` を短く書いたのが `x += 3;`
- 同様に `-=` `*=` `/=` `%=` などがある

```c
/* file: assign_ops.c */
#include <stdio.h>

int main(void)
{
    int x = 10;

    x += 3;  /* x = x + 3 */
    x *= 2;  /* x = x * 2 */
    x -= 4;  /* x = x - 4 */

    printf("x=%d\n", x);
    return 0;
}
```

注意：`/=` や `%=` を使うときは、割る側が 0 でないことを先に確認します。

#### 式と代入式

ここがCらしい（そして混乱しやすい）ポイントです。

- `1 + 2` は式で、評価すると `3`
- `x = 10` も式で、評価すると「代入した値（この場合 10）」になる

つまり、代入は「文」ではなく **式** として扱えます。

```c
/* file: assignment_is_expression.c */
#include <stdio.h>

int main(void)
{
    int x = 0;
    int y = 0;

    y = (x = 10);

    printf("x=%d y=%d\n", x, y);
    return 0;
}
```

この例では、`(x = 10)` が 10 という値になり、それを `y` に代入しています。

実務では、読みやすさのために「代入は単独の行に分ける」方が安全な場面も多いです。

#### 式文

式の後ろに `;` を付けると、**式文**（expression statement）になります。

```c
/* file: expression_statement.c */
#include <stdio.h>

int main(void)
{
    int a = 1;
    int b = 2;

    a = a + b;     /* 代入も式文になる */
    printf("a=%d\n", a);  /* 関数呼び出しも式文になる */

    a + b;         /* 値を計算して捨てるだけ（何も起きない） */

    return 0;
}
```

最後の `a + b;` は「計算はするが結果を使わない」ので、基本は書かない方がよいです。

---

### 型

#### 整数型と浮動小数点型

第1巻で使った主な型は次のとおりです。

- 整数型：`int`（ほかに `short` `long` `long long`、`unsigned` 系など）
- 浮動小数点型：`float` `double`（ほかに `long double`）

重要：型のサイズ（何バイトか）は環境で変わり得ます。必要なら `sizeof` で確認します。

```c
/* file: sizeof_types.c */
#include <stdio.h>

int main(void)
{
    printf("sizeof(int)=%zu\n", sizeof(int));
    printf("sizeof(double)=%zu\n", sizeof(double));
    return 0;
}
```

ここで出てくる `%zu` は「`sizeof` の結果（`size_t`）を表示する指定」です。

#### 型とオブジェクト

この章では次の言い方を使います。

- **型**：値の性質（整数か、小数か、何バイトか、など）
- **オブジェクト**：値を入れておく“実体”（メモリ上の入れ物）

変数は「型を持つオブジェクト」です。

`scanf` が `&a` を要求するのは、「`a` というオブジェクトの場所に書き込みたい」ためでした。

#### 型と定数（リテラル）

`123` や `3.14` のような定数（リテラル）にも型があります。

- `123`：整数リテラル（多くの場合 `int` で表せる範囲なら `int`）
- `123U`：`unsigned` 系として扱いたい意図を明示
- `123L`：`long` 系として扱いたい意図を明示
- `123LL`：`long long` 系として扱いたい意図を明示
- `3.14`：浮動小数点リテラル（`double`）

```c
/* file: literal_types.c */
#include <stdio.h>

int main(void)
{
    unsigned int u = 10U;
    long long big = 1234567890123LL;
    double pi = 3.141592;

    printf("u=%u big=%lld pi=%f\n", u, big, pi);
    return 0;
}
```

#### double型の演算（整数除算との違い）

整数同士の割り算は切り捨てでした。

```c
/* file: int_vs_double_div.c */
#include <stdio.h>

int main(void)
{
    int a = 1;
    int b = 2;

    printf("a/b (int) = %d\n", a / b);
    printf("a/b (double) = %f\n", (double)a / b);
    return 0;
}
```

補足：`(double)a` の `(double)` は **キャスト**（型変換）の構文で、ここは丸括弧が必須です（関数呼び出しではありません）。

- `(double)a / b`：`a` を `double` にしてから割る → 小数の割り算になる
- `(double)(a / b)`：先に `a / b` が整数除算される → その結果を `double` にしても小数部分は戻らない

また、`double` は「多くの小数」を扱えますが、2進数で表せない小数は誤差が出ます。
この誤差はバグではなく「そういう表現」なので、比較（`==`）を乱用しないなどの工夫が必要になります（ここも必要になったら次巻以降で掘ります）。

#### 型と演算（混ぜるとどうなるか）

型が違う値を混ぜると、Cは一定のルールで型を揃えて計算します。

```c
/* file: mixed_types.c */
#include <stdio.h>

int main(void)
{
    int count = 3;
    double price = 1.5;

    printf("count*price=%f\n", count * price);
    return 0;
}
```

ここでは `count` が `double` 側に寄せられて計算され、結果も `double` になります。

#### キャスト

キャストは「型を明示的に変換する」書き方です。

```c
double q = (double)a / b;
```

この例では `a` を `double` に変換してから割り算することで、整数除算ではなく小数の割り算にしています。

注意：キャストは便利ですが、乱用すると「本当は型が合っていない」問題を隠してしまうことがあります。まずは、必要な場面（整数除算を避けたい、など）に絞って使うのが安全です。

#### 変換指定（型に合った表示の指定）

`printf` / `scanf` の変換指定は、型に合わせます。

よく使うもの（この巻の範囲）：

- `int`：`%d`
- `unsigned int`：`%u`
- `long`：`%ld`
- `long long`：`%lld`
- `double`（`printf`）：`%f`
- `double`（`scanf`）：`%lf`
- `char`：`%c`
- 文字列（`char[]` や `char *`）：`%s`
- `sizeof` の結果（`size_t`）：`%zu`

`printf` は引数の型を自動で合わせてくれません。ズレると危険なので、コンパイル警告（`-Wall -Wextra`）は常に有効にしておくのが安全です。

---

### まとめ

- 演算子とオペランド、式と式文という言葉を揃えた
- `*` `/` は `+` `-` より先に計算され、括弧で意図を固定できる
- `n % 10` で最下位桁、`n / 10` で最下位桁を落とせる
- `printf` は `%...` を複数書けるが、個数と順番を必ず合わせる
- 代入（`=`）は式であり、`x = 10` は 10 という値にもなる
- 整数型と浮動小数点型の違い、混ぜたときの型変換、キャスト、変換指定の対応を押さえた

---

## 第三章でやること：

- `if` 文の基本形を理解し、奇数/偶数や非ゼロ判定が書ける
- 条件式の「評価」と、`==`（等価）/`=`（代入）の違いで事故を起こさない
- 関係演算子（`<` `<=` `>` `>=`）と論理演算子（`&&` `||` `!`）で条件を組み立てられる
- 入れ子の `if` とブロック（複合文）を読み、最大値（2値/3値）を求められる
- 条件演算子（`?:`）を「短い分岐」として読める
- `switch` 文と `break`、`if` との使い分けができる

前提：この章から「分岐」が入ります。Cでは「条件」は `true/false` ではなく **0 と非0** で扱うので、ここを最初に固めます。

---

## プログラムの流れの分岐

ここまでのプログラムは、上から下へ一直線に処理が進みました。
この章では、条件によって処理を分ける「分岐」を扱います。

まず言葉を揃えます。

- **条件式**：`if (...)` や `switch (...)` が見る「条件」の式
- **評価**：式を計算して値を決めること
- **選択文**：条件によって実行する文を選ぶ文（`if` と `switch`）

重要：Cの条件は「0 なら偽、0以外なら真」です。
`if (x)` は「x が 0 でないなら…」という意味になります。

この章のコード例は入力を使いません。`int x = ...;` の値を変えて挙動を確認します。

### if文

#### if文（最小の形）

最小の形は次のとおりです。

```c
/* file: if_basic.c */
#include <stdio.h>

int main(void)
{
    int x = 3; /* ここを変えて試す */
    printf("x=%d\n", x);

    if (x > 0) {
        puts("x は正です");
    }

    puts("ここは常に実行されます");
    return 0;
}
```

読み方：

- `if (x > 0)` の丸括弧の中（条件式）を評価する
- 条件式が非0（真）なら、その直後の文（ここではブロック）を実行する
- 条件式が 0（偽）なら、その直後の文は実行しない

#### 複合文（ブロック）

`{ ... }` は **複合文**（ブロック）です。複数の文を「ひとまとまり」にできます。

```c
if (x > 0) {
    puts("A");
    puts("B");
}
```

この形にしておくと、後から文を追加しても安全です。
1文だけでもブロックにする癖は、読み間違いを減らします。

#### 奇数の判定（剰余の利用）

整数 `n` が奇数かどうかは、`n % 2`（2で割った余り）で判定できます。

```c
/* file: is_odd.c */
#include <stdio.h>

int main(void)
{
    int n = 7; /* ここを変えて試す（0以上を想定） */
    printf("n=%d\n", n);

    if (n % 2 != 0) {
        puts("奇数です");
    }

    return 0;
}
```

補足：負の数でも `n % 2 != 0` 自体は「奇数らしさ」の判定として機能しますが、負の剰余の符号は環境差の話が絡んで読み手が混乱しやすいので、ここでは `n` は 0以上として話を進めます。

#### else付きのif文（どちらか一方を必ず実行）

`else` を付けると、「条件が偽のときの処理」を書けます。

```c
/* file: if_else.c */
#include <stdio.h>

int main(void)
{
    int n = 8; /* ここを変えて試す（0以上を想定） */
    printf("n=%d\n", n);

    if (n % 2 != 0) {
        puts("奇数です");
    } else {
        puts("偶数です");
    }

    return 0;
}
```

#### 非ゼロの判定（Cの「真/偽」）

この形はCのコードで非常によく出ます。

```c
/* file: nonzero_check.c */
#include <stdio.h>

int main(void)
{
    int x = -5; /* ここを変えて試す */
    printf("x=%d\n", x);

    if (x) {
        puts("x は 0 ではありません");
    } else {
        puts("x は 0 です");
    }

    return 0;
}
```

この `if (x)` は、`if (x != 0)` と同じ意味です。

#### if文の構造図（読み方の型）

`if` の見た目を、図として固定します。

```text
        条件式を評価
              |
      +-------+-------+
      |               |
   真（非0）       偽（0）
      |               |
  then側を実行     else側を実行
  （または何もしない）
      |               |
      +-------+-------+
              |
        次の処理へ
```

#### 等価演算子（`==` と `!=`）

等しいかどうかを調べるのは `==` です。

- `==`：等しい
- `!=`：等しくない

```c
/* file: equality_ops.c */
#include <stdio.h>

int main(void)
{
    int x = 10; /* ここを変えて試す */
    printf("x=%d\n", x);

    if (x == 10) {
        puts("x は 10 です");
    }
    if (x != 10) {
        puts("x は 10 ではありません");
    }

    return 0;
}
```

注意：`=` は代入で、比較ではありません。
`if (x = 10)` のように書くと「x に 10 を代入して、その結果（10）を条件に使う」になり、ほぼ確実に意図とズレます。
この事故は `-Wall -Wextra` で警告されることが多いので、警告は常に有効にします。

#### 剰余の判定（割り切れるかどうか）

`n` が `k` で割り切れるかどうかは `n % k == 0` で判定できます。

```c
/* file: divisible_by_3.c */
#include <stdio.h>

int main(void)
{
    int n = 9; /* ここを変えて試す（0以上を想定） */
    printf("n=%d\n", n);

    if (n % 3 == 0) {
        puts("3 で割り切れます");
    } else {
        puts("3 で割り切れません");
    }

    return 0;
}
```

#### 関係演算子（大小を比べる）

- `<`：小さい
- `<=`：以下
- `>`：大きい
- `>=`：以上

```c
/* file: relation_ops.c */
#include <stdio.h>

int main(void)
{
    int x = -1; /* ここを変えて試す */
    printf("x=%d\n", x);

    if (x < 0) {
        puts("x は負です");
    } else if (x == 0) {
        puts("x は 0 です");
    } else {
        puts("x は正です");
    }

    return 0;
}
```

ここで出てきた `else if` は「`if` の連鎖」です。
上から順に条件を評価し、最初に真になった分岐だけが実行されます。

#### 入れ子になったif文（ネスト）

`if` の中に `if` を入れることがあります。読み方のコツは「ブロックを単位に追う」ことです。

```c
/* file: nested_if.c */
#include <stdio.h>

int main(void)
{
    int x = 5; /* ここを変えて試す */
    printf("x=%d\n", x);

    if (x >= 0) {
        if (x % 2 == 0) {
            puts("0以上の偶数です");
        } else {
            puts("0以上の奇数です");
        }
    } else {
        puts("負の数です（この例では分類しません）");
    }

    return 0;
}
```

#### 評価（条件式がいつ計算されるか）

`if (条件式)` の条件式は、`if` に入った瞬間に評価されます。

重要な注意：条件式の中に「代入」や「インクリメント」などの副作用を入れると、読みづらくなり、バグの温床になりやすいです。
この巻では、条件式は「比較」と「論理演算子」の組み合わせを基本にします。

#### 2値の最大値を求める

```c
/* file: max2.c */
#include <stdio.h>

int main(void)
{
    int a = 10; /* ここを変えて試す */
    int b = 20; /* ここを変えて試す */
    printf("a=%d b=%d\n", a, b);

    if (a > b) {
        printf("max=%d\n", a);
    } else {
        printf("max=%d\n", b);
    }

    return 0;
}
```

#### 3値の最大値を求める（読みやすさ優先）

3つの値の最大値は、まず「2つの最大」を作ってからもう1つと比べると読みやすいです。

```c
/* file: max3.c */
#include <stdio.h>

int main(void)
{
    int a = 10; /* ここを変えて試す */
    int b = 4;  /* ここを変えて試す */
    int c = 7;  /* ここを変えて試す */
    printf("a=%d b=%d c=%d\n", a, b, c);

    int m = a;
    if (b > m) {
        m = b;
    }
    if (c > m) {
        m = c;
    }

    printf("max=%d\n", m);
    return 0;
}
```

#### 条件演算子（`?:`）

条件演算子は「短い分岐」を **式** として書けます。

```c
/* file: conditional_operator.c */
#include <stdio.h>

int main(void)
{
    int a = 10; /* ここを変えて試す */
    int b = 20; /* ここを変えて試す */
    printf("a=%d b=%d\n", a, b);

    int m = (a > b) ? a : b;
    printf("max=%d\n", m);
    return 0;
}
```

読み方：

- 条件が真なら、`?` の後ろ（ここでは `a`）
- 条件が偽なら、`:` の後ろ（ここでは `b`）

注意：条件演算子は便利ですが、入れ子にすると急に読みづらくなります。
この巻では「1段だけ」を基本にします。

#### 論理演算子（`&&` `||` `!`）

条件を組み合わせるときに使います。

- `&&`：かつ（両方真なら真）
- `||`：または（どちらか真なら真）
- `!`：否定（真偽を反転）

```c
/* file: logical_ops.c */
#include <stdio.h>

int main(void)
{
    int x = 11; /* ここを変えて試す */
    printf("x=%d\n", x);

    if (x >= 1 && x <= 10) {
        puts("x は 1〜10 の範囲です");
    } else {
        puts("x は 1〜10 の範囲ではありません");
    }

    if (!(x % 2 == 0)) {
        puts("x は偶数ではありません（つまり奇数です）");
    }

    return 0;
}
```

重要：`&&` と `||` は **短絡評価** です。

- `A && B` は、`A` が偽なら `B` を評価しない
- `A || B` は、`A` が真なら `B` を評価しない

この性質は「安全のための条件」によく使います。

```c
/* file: safe_div_condition.c */
#include <stdio.h>

int main(void)
{
    int a = 10;
    int b = 0;

    if (b != 0 && (a / b) > 1) {
        puts("ここには来ません");
    } else {
        puts("b が 0 なので割り算を避けました");
    }

    return 0;
}
```

`b != 0` が偽（b が 0）なら、`a / b` は評価されず、ゼロ除算が起きません。

### switch文

`switch` は「値によって分岐する」選択文です。
`if` の連鎖で書けるものの一部を、読みやすく書けます。

#### switch文とbreak文

```c
/* file: switch_basic.c */
#include <stdio.h>

int main(void)
{
    int n = 2; /* ここを変えて試す */
    printf("n=%d\n", n);

    switch (n) {
    case 1:
        puts("one");
        break;
    case 2:
        puts("two");
        break;
    case 3:
        puts("three");
        break;
    default:
        puts("1〜3 ではありません");
        break;
    }

    return 0;
}
```

重要：`break` がないと、次の `case` に処理が流れます（これを fall-through と呼びます）。
意図して fall-through する場合もありますが、最初は「各 case に break を書く」を基本にします。

#### switch文とif文（書ける範囲の違い）

`switch` は基本的に「等しいかどうか」で分岐します。

- `switch` が得意：`x == 1` / `x == 2` / `x == 3` のような分岐
- `switch` が苦手：`x < 10` のような範囲判定（これは `if` の仕事）

例として「成績を文字で分類する」を `if` で書くと次のようになります。

```c
/* file: grade_if.c */
#include <stdio.h>

int main(void)
{
    int score = 85; /* ここを変えて試す（0〜100） */
    printf("score=%d\n", score);

    if (score >= 90) {
        puts("A");
    } else if (score >= 80) {
        puts("B");
    } else if (score >= 70) {
        puts("C");
    } else {
        puts("D");
    }

    return 0;
}
```

これは範囲判定なので `switch` では表現しづらい例です。

一方で「コマンド文字で動作を選ぶ」ような例は `switch` に向いています。

```c
/* file: command_switch.c */
#include <stdio.h>

int main(void)
{
    char cmd = 'b'; /* ここを変えて試す（a/b/q） */
    printf("cmd=%c\n", cmd);

    switch (cmd) {
    case 'a':
        puts("cmd = a");
        break;
    case 'b':
        puts("cmd = b");
        break;
    case 'q':
        puts("quit");
        break;
    default:
        puts("unknown");
        break;
    }

    return 0;
}
```



### まとめ

- Cの条件は「0 が偽、非0 が真」なので、`if (x)` は「x が非0」の判定になる
- `if` は条件が真のときだけ then側が動き、`else` を付けると偽のときの処理も書ける
- `==` は比較、`=` は代入で意味が違う。条件式に `=` を紛れ込ませない
- `%` を使うと奇数/偶数や「割り切れるか」を判定できる
- 関係演算子と論理演算子で条件を組み立てられる。`&&` と `||` は短絡評価で安全条件に使える
- `switch` は「値が一致する分岐」に向き、`break` を忘れると次の case に流れる


## 付録

この付録では、ソースコードを読むときに必ず出会う「ヘッダ（.h）」まわりを、できるだけ丁寧に整理します。

### .hファイルの説明

#### .h は何のためにあるか

`.h`（ヘッダファイル）は、ざっくり言うと「別の `.c` から使いたい情報（宣言）をまとめて置く場所」です。

代表的に `.h` に置くもの：

- 関数の宣言（プロトタイプ）
- `struct` / `enum` / `typedef` などの型定義
- `#define` マクロ定義
- 定数（`#define` や `enum`）
- `extern` による「別ファイルにある変数」の宣言

逆に、基本的に `.h` に置かない方がよいもの：

- “普通の”関数の定義（実装）
- “普通の”グローバル変数の定義（実体）

理由：`.h` は複数の `.c` に `#include` され得るため、そこに「実体（定義）」があると多重定義や衝突の原因になりやすいからです。

#### `#include` は「貼り付け」に近い

`#include` はCの文法というより、プリプロセッサ（前処理）の命令です。
概念としては「その場所にファイルの中身を展開する」に近い挙動になります。

```c
// ここに #include <stdio.h> と書くと、
// コンパイル前に stdio.h の内容がこの位置に展開される（イメージ）
#include <stdio.h>
```

#### `<...>` と `"..."` の違い

- `#include <stdio.h>`
    - 多くの環境では「標準ライブラリ等の検索パス」から探す
- `#include "myheader.h"`
    - 多くの環境では「まず自分のプロジェクト側（同じディレクトリ等）」を優先して探す

※ 実際の探索順序はコンパイラやオプション（`-I` 等）で変わります。

#### 宣言と定義（かなり重要）

ソースを読めるようになるために、ここは最初に言葉を揃えておくと後で楽です。

- **宣言**：名前と型（使い方）を知らせる
- **定義**：実体を作る（メモリを確保する／関数の本体を書く）

例：関数

```c
// 宣言（.h に置くことが多い）
int add(int a, int b);

// 定義（.c に置くことが多い）
int add(int a, int b)
{
    return a + b;
}
```

例：グローバル変数

```c
// 宣言（.h に置くことがある）
extern int g_counter;

// 定義（どこか1つの .c に置く）
int g_counter = 0;
```

#### 1回だけ読み込ませる仕組み（インクルードガード）

同じ `.h` が何度も `#include` されると、同じ宣言や定義が重なって困ることがあります。
そこで「1回だけ有効にする」ための定番が **インクルードガード** です。

```c
/* file: myheader.h */
#ifndef MYHEADER_H
#define MYHEADER_H

int add(int a, int b);

#endif
```

別の書き方として `#pragma once` もありますが、コードベースの方針に合わせるのが安全です。

#### `.c` と `.h` の分業（最小パターン）

「ヘッダに宣言、ソースに定義」の最小例です。

```c
/* file: add.h */
#ifndef ADD_H
#define ADD_H

int add(int a, int b);

#endif
```

```c
/* file: add.c */
#include "add.h"

int add(int a, int b)
{
    return a + b;
}
```

```c
/* file: main.c */
#include <stdio.h>
#include "add.h"

int main(void)
{
    printf("%d\n", add(1, 2));
    return 0;
}
```

ここでの狙い：

- `main.c` は `add` の「中身」を知らなくても使える
- `add.c` だけを差し替えても、`main.c` は基本そのまま

---

### stdio.hの説明

#### `stdio.h` は何か

`stdio.h` は、C標準ライブラリ（標準入出力）のヘッダです。  
`printf` / `scanf` / `puts` などを使うときに `#include <stdio.h>` を書くのは、
「これらの関数の宣言（使い方）をコンパイラに知らせる」ためです。

重要な点：

- `stdio.h` 自体は「宣言」を提供する
- 実際の処理（実装）は、通常はCライブラリ（glibc等）側にある

つまり、コンパイルとリンクの関係としては次のようなイメージです。

- コンパイル：`printf` の型（引数や戻り値）を知っている必要がある → `stdio.h`
- リンク：`printf` の本体がどこかに必要 → libc

#### `stdio.h` に入っていることが多いもの（概観）

環境によって差はありますが、代表的には次の要素を含みます。

- 関数宣言：`printf` `scanf` `puts` `putchar` `getchar` `fopen` `fread` など
- 型：`FILE`（ファイルやストリームを扱うための型）
- 定数/マクロ：`EOF` `NULL` など
- 標準ストリーム：`stdin` `stdout` `stderr`（多くの環境で）

#### `printf` と `scanf` の「書式文字列」

この巻では `%d` を中心に使いましたが、書式文字列は奥が深いです。
ここで押さえると読みやすくなるポイントだけまとめます。

- `%d`：`int`
- `%f`：`double`（`printf` に渡すとき、`float` は自動で `double` 扱いになる）
- `%c`：`char`
- `%%`：`%` 自体

`scanf` の場合、読み込み先に `&` が付くのは「値を受け取る場所」を渡すためです。

#### 重要：OS開発では `stdio` が使えない／使わないことがある

UmuOSのように「OSそのもの」を作る世界では、環境が **freestanding**（標準ライブラリが前提でない）になることがあります。
その場合：

- `stdio.h` が存在しない
- あるいは、互換の `stdio.h` を自前で用意している
- あるいは、`printf` 相当を独自の `kprintf` など別名で持っている

といった設計があり得ます。

ただし、これは主に「カーネルやブート直後のコード」の話です。詳細設計書に登場する `initramfs/src/init.c` や `work/umu_su.c` のような userspace プログラムは、`musl-gcc` や `gcc` で libc と一緒にビルドして動かすため、`stdio.h` を含む標準ヘッダが普通に出てきます。

自身がUmuOSのソースを読むときは、「`stdio.h` を `#include` しているか」「していないなら、どの出力機構を使っているか」を起点に追うと整理しやすいです。

---

### UmuOSでは.hをどう扱うか（よくわからない方は読み飛ばしてOKです。C言語の基本には関係ないです）

ここは、UmuOS-0.1.7-base-stable の詳細設計書に書かれている「実際のビルド手順」を根拠に、`.h` が **どのレイヤで・何のために**使われているかを整理します。

結論を先に言うと、設計書の手順に出てくる `.h` は主に次の扱いです。

- ゲストOS（initramfs / disk.img）に置くものではなく、ホスト側のツールチェーン（musl-gcc / gcc）が参照する「開発用ヘッダ」
- `#include <...>`（標準/外部）中心で、少なくとも設計書に登場する userspace プログラムは「プロジェクト内の独自 `.h` を大量に抱える」感じではない

#### 設計書に明記されている事実（`.h` の根拠になる部分）

詳細設計書には、少なくとも次の2つのCプログラムが「どのコンパイラで、どうリンクするか」まで具体的に書かれています。

- initramfs の `/init` を `initramfs/src/init.c` からビルドする

```bash
musl-gcc -static -O2 -Wall -Wextra \
    -o /home/tama/umu_project/UmuOS-0.1.7-base-stable/initramfs/rootfs/init \
    /home/tama/umu_project/UmuOS-0.1.7-base-stable/initramfs/src/init.c
```

- 自作 `su`（`/umu_bin/su`）を `work/umu_su.c` から静的リンクでビルドする（`-lcrypt` が通らない場合は `-lxcrypt` を試す）

```bash
gcc -static -Os -s -o umu_su umu_su.c -lcrypt || true
if [ ! -f umu_su ]; then
    gcc -static -Os -s -o umu_su umu_su.c -lxcrypt
fi
```

また、設計書の事前準備として `musl-tools` を入れること、`su` の静的リンクのために `libcrypt-dev`（環境によっては `libxcrypt-dev`）を入れる可能性が書かれています。

さらに、設計書の `umu_su.c` の例では、`#include <crypt.h>` のほか、`#include <errno.h>` / `#include <stdio.h>` / `#include <stdlib.h>` / `#include <string.h>` / `#include <sys/types.h>` / `#include <unistd.h>` といった「標準/外部」のヘッダが並びます。

この記述は、「どの `.h` を見るべきか」を決める強い手がかりになります。

#### この事実から分かる `.h` の扱い（UmuOS-0.1.7-base-stable の場合）

`#include` が意味を持つ場所（= `.h` を追うべき場所）は、ざっくり次の2層に分かれます。

- `musl-gcc` でビルドする `init.c` の世界
    - `#include <...>` は **musl のヘッダ群**（musl-tools が提供する開発用ヘッダ）に向かう
    - ここでの `.h` は「initramfs 内に置くファイル」ではなく、ホストのビルド環境に存在するもの

- `gcc` + `-lcrypt` / `-lxcrypt` でビルドする `umu_su.c` の世界
    - `#include <crypt.h>` などは **libcrypt / libxcrypt の開発用ヘッダ**に向かう
    - `-lcrypt` が通らない環境があるのは、リンクするライブラリ名や提供パッケージが環境差を持つため

そして静的リンク（`-static`）を使っているため、実行時に「ゲスト側に `.h` を置く」必要はありません。
実行時に必要なのは出来上がった実行ファイル（`/init` や `/umu_bin/su`）で、`.h` はビルド時の型チェックや宣言の解決に使われます。

#### 自身がソースを読むときの“最短の追い方”（設計書ベース）

設計書に合わせて、`.h` の追跡も「ビルド単位」から入ると迷いにくいです。

- 対象の `.c` が `musl-gcc` でビルドされるのか、`gcc` でビルドされるのかを先に確定する
    - これだけで、参照される標準ヘッダの出どころ（musl系か、glibc系/追加devパッケージ系か）が大きく絞れます

- `.c` 先頭の `#include <...>` を列挙し、どのパッケージのヘッダかを突き止める
    - 例：`<crypt.h>` は `libcrypt-dev` / `libxcrypt-dev` のどちら側かで差が出やすい

- 「宣言はあるが実体が見えない」ものは、まずリンクオプションを見る
    - `-lcrypt` / `-lxcrypt` のように、どのライブラリに本体があるかが設計書に書かれている場合、ヘッダ探索より先にリンク先の当たりが付けられます

この流れで読むと、`.h` を“プロジェクト内の地図”として追う前に、まず「ホストのツールチェーンが提供する `.h`」と「追加パッケージが提供する `.h`」を切り分けられます。UmuOS-0.1.7-base-stable の設計書は、この切り分けをやりやすい形（ビルドコマンドと依存パッケージの明記）で書かれています。

