
# umuOSの為のC言語 1 — 基礎から（基本文法と入出力の入口）

このシリーズは最終的に、UmuOS／ush／uim のソースコードを**自力で読める**ようになることを目標にします。  
ただし **第1巻はUmuOSに一切依存しない**「C言語の基本の基本」だけを、できる限りやさしく・丁寧に説明します。

この巻でやること：

- Cの「最小の形」を知る
- `printf` で数を表示できる
- 変数（宣言・代入・初期化）がわかる
- `scanf` / `puts` で読み込みと表示の入口を踏む

前提：

- OS：Linux想定（UmuOS周辺を触るので）
- コンパイラ：`gcc`（`clang`でも基本同じ）
- 規格：`-std=c17`

---

## まずは慣れよう

ここは「理屈より、まず形に慣れる」章です。  
最初は **コピペ→コンパイル→実行→出力を見る** を繰り返して、Cの雰囲気を掴みます。

### 整数の加算の結果を表示

まずは「足し算して表示」だけをやります。

```c
/* file: add.c */
#include <stdio.h>

int main(void)
{
    printf("1 + 2 = %d\n", 1 + 2);
    printf("10 + 30 = %d\n", 10 + 30);
    return 0;
}
```

ポイント：

- `1 + 2` は「式（expression）」で、結果は `3`
- `%d` は「整数（int）を表示する変換指定子」
- `\n` は改行

### プログラムとコンパイル

#### プログラムとは（超ざっくり）

自身が書いた `.c`（ソースコード）を、コンピュータが実行できる形（実行ファイル）に変換して動かします。

#### コンパイル手順（Linux）

ファイル名を `add.c` として保存したら：

```bash
gcc -Wall -Wextra -std=c17 -O0 add.c -o add
./add
```

- `-Wall -Wextra`：警告をたくさん出す（最初は特に重要）
- `-O0`：最適化なし（最初は挙動を追いやすい）

よくあるつまずき：

- `./add` の `./` を忘れると「見つからない」と言われる（カレントディレクトリは通常 PATH に入っていないため）

### コメント

コメントは「自分用メモ」です。基本的に実行には影響しません。

- 1行コメント：`// ...`
- 複数行コメント：`/* ... */`

例：

```c
// これは1行コメント
/*
   これは複数行コメント
*/
```

コメントのコツ：

- 「何をしているか」より「なぜそうするか」をコメントに書くと役に立つ

### プログラムの決まり文句

最小のCプログラムの“型”はこれです。

```c
#include <stdio.h>

int main(void)
{
    /* ここに処理を書く */
    return 0;
}
```

順番に意味を押さえます。

#### `#include <stdio.h>`

`printf` や `scanf` を使うための「宣言」が入ったヘッダを読み込みます。

ここでの理解としては：

- 「`printf` という道具を使っていいよ」とコンパイラに伝える

でOKです。

#### `int main(void)`

- `main` はプログラムの入口（ここから始まる）
- `int` は「終了コード（整数）を返す」
- `(void)` は「引数なし」

#### `{ ... }`（ブロック）

波かっこ `{` `}` の中が、`main` の中身です。

### printf関数

`printf` は「表示する」関数です。  
基本形は次の2種類をまず覚えます。

```c
printf("ただの文字\n");
printf("数を入れて表示: %d\n", 123);
```

#### `printf` の引数（かんたんに）

- 1つ目：**書式文字列**（format string）
- 2つ目以降：書式文字列の `%...` に当てはめる値

「%がある分だけ、後ろに値が必要」と覚えると事故が減ります。

### 文

**文（statement）**は「処理の単位」です。多くの文はセミコロン `;` で終わります。

例：

- `printf("hi\n");` ← 文（最後に `;`）
- `return 0;` ← 文

よくあるミス：

- `;` を忘れる（コンパイルエラーになる）

### 整数

整数は小数点のない数です。Cではまず `int` を使います。

```c
int a = 10;
int b = -3;
```

ここでは「`int` は整数」だけ覚えればOKです（サイズや範囲は後の巻で丁寧に扱います）。

### 書式文字列と変換指定

`printf` の中の `%d` のようなものを **変換指定子**（conversion specifier）と呼びます。

最初に覚える3つ：

- `%d`：整数（`int`）
- `%f`：小数（`double`）
- `%c`：1文字（`char`）

例：

```c
#include <stdio.h>

int main(void)
{
    int i = 42;
    double x = 3.14;
    char c = 'A';

    printf("i=%d\n", i);
    printf("x=%f\n", x);
    printf("c=%c\n", c);
    return 0;
}
```

重要：

- 型と変換指定子がズレると、表示が壊れたり危険（未定義動作）になり得る

### 記号文字の読み方

プログラムを読むための最低限の「読み方」をまとめます。

- `;` セミコロン
- `,` カンマ
- `()` 丸かっこ（丸括弧）
- `{}` 波かっこ（波括弧）
- `[]` 角かっこ（角括弧）
- `#` シャープ
- `"` ダブルクォート
- `'` シングルクォート
- `\` バックスラッシュ（環境によっては円記号に見えることも）
- `%` パーセント

### 書式化を行わない表示

「数を埋め込まない」表示です。

```c
printf("Hello\n");
```

これは「ただの文字列を表示する」だけ。

注意：

- `printf` は **改行を自動で付けない**。必要なら自分で `\n` を入れる。

### 文字列リテラル

`"Hello"` のようにダブルクォートで囲んだものを **文字列リテラル**と言います。

```c
printf("Hello, world!\n");
```

ここで重要な区別：

- `'A'`（シングルクォート）：**文字**（`char`）
- `"A"`（ダブルクォート）：**文字列**（複数文字。別物）

### 拡張表記

ここでは「難しい理屈」を後回しにして、現場でよく見る表記だけ先に紹介します。

#### エスケープシーケンス

文字列中で特別な意味を持つ `\` から始まる表記です。

- `\n`：改行
- `\t`：タブ
- `\\`：バックスラッシュそのもの
- `\"`：ダブルクォートそのもの

例：

```c
printf("1行目\n2行目\n");
printf("タブ\tで区切る\n");
```

#### 数値リテラルの表記（見かけるもの）

- `123`：10進
- `0x7b`：16進（`0x` で始まる）

UmuOSなど低レイヤのコードでは16進が頻繁に出ます。今は「0xが付くと16進」だけ覚えればOKです。

---

## 変数

ここから「値を覚えておく箱」を扱います。

### 変数と宣言

変数は「値を入れる箱」で、使う前に **宣言** が必要です。

```c
int a;      // a という名前の int 変数を用意（宣言）
double x;   // x という名前の double 変数を用意
```

宣言は「この名前はこういう型で使う」とコンパイラに伝える行為です。

### 代入

`=` は「等しい」ではなく **代入** です。

```c
int a;
a = 10;     // a に 10 を入れる
```

よくある混乱：

- `a = a + 1;` は「aを1増やす」という意味（数学の等号とは違う）

### 初期化

宣言と同時に値を入れるのが **初期化** です。

```c
int a = 10;     // 宣言 + 初期化
```

なぜ初期化が大事？

- 初期化しない変数の中身は「不定」になり得る（ゴミ値）
- ゴミ値を使うと原因不明の挙動になりやすい

まずは「変数は宣言したら、基本は初期化」くらいの感覚でOKです。

---

## 読み込みと表示

この章では、

- キーボードから数を読む（`scanf`）
- 計算する（四則演算）
- 文字列を表示する（`puts`）

をやります。

### scanf関数

`scanf` は「入力を読む」関数です。  
ただし、最初につまずきやすい点（`&` など）があるので、最小の使い方だけを丁寧に扱います。

まずはこれ：

```c
/* file: scan_add.c */
#include <stdio.h>

int main(void)
{
    int a = 0;
    int b = 0;

    printf("a を入力してください: ");
    scanf("%d", &a);

    printf("b を入力してください: ");
    scanf("%d", &b);

    printf("a + b = %d\n", a + b);
    return 0;
}
```

#### `&a` の `&` って何？

ここでは次のように捉えると安全です：

- `scanf` は「変数の場所」に書き込みたい
- だから `&a` で「aの場所（アドレス）」を渡す

（ポインタという概念の入口ですが、第1巻ではここまでで十分です）

#### scanfの注意（最初に知っておくと事故が減る）

- 入力に失敗することがある（文字を入れた、など）
- 本格的には戻り値チェックが必要

この巻では「動かしながら慣れる」優先で進めますが、後の巻で安全な読み込み方も扱います。

### 四則演算

四則演算は次の演算子です。

- `+` 足し算
- `-` 引き算
- `*` 掛け算
- `/` 割り算

例（入力した2つの数で計算）：

```c
/* file: calc.c */
#include <stdio.h>

int main(void)
{
    int a = 0;
    int b = 0;

    printf("a b をスペース区切りで入力してください: ");
    scanf("%d %d", &a, &b);

    printf("a + b = %d\n", a + b);
    printf("a - b = %d\n", a - b);
    printf("a * b = %d\n", a * b);

    // 注意：b が 0 だと a / b は危険（ゼロ除算）
    if (b != 0) {
        printf("a / b = %d\n", a / b); // int同士なので小数は切り捨て
    } else {
        printf("a / b は b が 0 なので計算できません\n");
    }

    return 0;
}
```

ここで押さえるポイント（重要）：

- `int` 同士の `/` は **整数除算**（小数点以下が切り捨て）
- `/` は b が 0 のとき危険（まず `b != 0` を確認する癖）

※ `if` はまだ本格的に説明していませんが、「0除算は危険」という事実を守るために最小限だけ使っています。次巻以降で条件分岐を体系的に学びます。

### puts

`puts` は「文字列を表示して、最後に改行を付ける」関数です。

```c
#include <stdio.h>

int main(void)
{
    puts("Hello");
    puts("puts は自動で改行します");
    return 0;
}
```

違い（ざっくり）：

- `printf`：書式化できる（`%d` など）／改行は自分で
- `puts`：書式化しない／改行は自動

---

## まとめ

- Cプログラムの最小構造：`#include` / `main` / `{}` / `return 0;`
- 文はだいたい `;` で終わる
- `printf`：書式文字列 + 変換指定子（`%d` など）
- 変数：宣言→代入→（できれば）初期化
- 入力：`scanf("%d", &a);` の形と、`&` の意味の入口
- 計算：四則演算、整数除算の切り捨て、ゼロ除算の危険
- `puts`：文字列を出して改行

---

## 付録

この付録では、ソースコードを読むときに必ず出会う「ヘッダ（.h）」まわりを、できるだけ丁寧に整理します。

### .hファイルの説明

#### .h は何のためにあるか

`.h`（ヘッダファイル）は、ざっくり言うと「別の `.c` から使いたい情報（宣言）をまとめて置く場所」です。

代表的に `.h` に置くもの：

- 関数の宣言（プロトタイプ）
- `struct` / `enum` / `typedef` などの型定義
- `#define` マクロ定義
- 定数（`#define` や `enum`）
- `extern` による「別ファイルにある変数」の宣言

逆に、基本的に `.h` に置かない方がよいもの：

- “普通の”関数の定義（実装）
- “普通の”グローバル変数の定義（実体）

理由：`.h` は複数の `.c` に `#include` され得るため、そこに「実体（定義）」があると多重定義や衝突の原因になりやすいからです。

#### `#include` は「貼り付け」に近い

`#include` はCの文法というより、プリプロセッサ（前処理）の命令です。
概念としては「その場所にファイルの中身を展開する」に近い挙動になります。

```c
// ここに #include <stdio.h> と書くと、
// コンパイル前に stdio.h の内容がこの位置に展開される（イメージ）
#include <stdio.h>
```

#### `<...>` と `"..."` の違い

- `#include <stdio.h>`
    - 多くの環境では「標準ライブラリ等の検索パス」から探す
- `#include "myheader.h"`
    - 多くの環境では「まず自分のプロジェクト側（同じディレクトリ等）」を優先して探す

※ 実際の探索順序はコンパイラやオプション（`-I` 等）で変わります。

#### 宣言と定義（かなり重要）

ソースを読めるようになるために、ここは最初に言葉を揃えておくと後で楽です。

- **宣言**：名前と型（使い方）を知らせる
- **定義**：実体を作る（メモリを確保する／関数の本体を書く）

例：関数

```c
// 宣言（.h に置くことが多い）
int add(int a, int b);

// 定義（.c に置くことが多い）
int add(int a, int b)
{
    return a + b;
}
```

例：グローバル変数

```c
// 宣言（.h に置くことがある）
extern int g_counter;

// 定義（どこか1つの .c に置く）
int g_counter = 0;
```

#### 1回だけ読み込ませる仕組み（インクルードガード）

同じ `.h` が何度も `#include` されると、同じ宣言や定義が重なって困ることがあります。
そこで「1回だけ有効にする」ための定番が **インクルードガード** です。

```c
/* file: myheader.h */
#ifndef MYHEADER_H
#define MYHEADER_H

int add(int a, int b);

#endif
```

別の書き方として `#pragma once` もありますが、コードベースの方針に合わせるのが安全です。

#### `.c` と `.h` の分業（最小パターン）

「ヘッダに宣言、ソースに定義」の最小例です。

```c
/* file: add.h */
#ifndef ADD_H
#define ADD_H

int add(int a, int b);

#endif
```

```c
/* file: add.c */
#include "add.h"

int add(int a, int b)
{
    return a + b;
}
```

```c
/* file: main.c */
#include <stdio.h>
#include "add.h"

int main(void)
{
    printf("%d\n", add(1, 2));
    return 0;
}
```

ここでの狙い：

- `main.c` は `add` の「中身」を知らなくても使える
- `add.c` だけを差し替えても、`main.c` は基本そのまま

---

### stdio.hの説明

#### `stdio.h` は何か

`stdio.h` は、C標準ライブラリ（標準入出力）のヘッダです。  
`printf` / `scanf` / `puts` などを使うときに `#include <stdio.h>` を書くのは、
「これらの関数の宣言（使い方）をコンパイラに知らせる」ためです。

重要な点：

- `stdio.h` 自体は「宣言」を提供する
- 実際の処理（実装）は、通常はCライブラリ（glibc等）側にある

つまり、コンパイルとリンクの関係としては次のようなイメージです。

- コンパイル：`printf` の型（引数や戻り値）を知っている必要がある → `stdio.h`
- リンク：`printf` の本体がどこかに必要 → libc

#### `stdio.h` に入っていることが多いもの（概観）

環境によって差はありますが、代表的には次の要素を含みます。

- 関数宣言：`printf` `scanf` `puts` `putchar` `getchar` `fopen` `fread` など
- 型：`FILE`（ファイルやストリームを扱うための型）
- 定数/マクロ：`EOF` `NULL` など
- 標準ストリーム：`stdin` `stdout` `stderr`（多くの環境で）

#### `printf` と `scanf` の「書式文字列」

この巻では `%d` を中心に使いましたが、書式文字列は奥が深いです。
ここで押さえると読みやすくなるポイントだけまとめます。

- `%d`：`int`
- `%f`：`double`（`printf` に渡すとき、`float` は自動で `double` 扱いになる）
- `%c`：`char`
- `%%`：`%` 自体

`scanf` の場合、読み込み先に `&` が付くのは「値を受け取る場所」を渡すためです。

#### 重要：OS開発では `stdio` が使えない／使わないことがある

UmuOSのように「OSそのもの」を作る世界では、環境が **freestanding**（標準ライブラリが前提でない）になることがあります。
その場合：

- `stdio.h` が存在しない
- あるいは、互換の `stdio.h` を自前で用意している
- あるいは、`printf` 相当を独自の `kprintf` など別名で持っている

といった設計があり得ます。

自身がUmuOSのソースを読むときは、「`stdio.h` を `#include` しているか」「していないなら、どの出力機構を使っているか」を起点に追うと整理しやすいです。

---

### UmuOSでは.hをどう扱うか

ここは、UmuOS-0.1.7-base-stable の詳細設計書に書かれている「実際のビルド手順」を根拠に、`.h` が **どのレイヤで・何のために**使われているかを整理します。

結論を先に言うと、設計書の手順に出てくる `.h` は主に次の扱いです。

- ゲストOS（initramfs / disk.img）に置くものではなく、ホスト側のツールチェーン（musl-gcc / gcc）が参照する「開発用ヘッダ」
- `#include <...>`（標準/外部）中心で、少なくとも設計書に登場する userspace プログラムは「プロジェクト内の独自 `.h` を大量に抱える」感じではない

#### 設計書に明記されている事実（`.h` の根拠になる部分）

詳細設計書には、少なくとも次の2つのCプログラムが「どのコンパイラで、どうリンクするか」まで具体的に書かれています。

- initramfs の `/init` を `initramfs/src/init.c` からビルドする

```bash
musl-gcc -static -O2 -Wall -Wextra \
    -o /home/tama/umu_project/UmuOS-0.1.7-base-stable/initramfs/rootfs/init \
    /home/tama/umu_project/UmuOS-0.1.7-base-stable/initramfs/src/init.c
```

- 自作 `su`（`/umu_bin/su`）を `work/umu_su.c` から静的リンクでビルドする（`-lcrypt` が通らない場合は `-lxcrypt` を試す）

```bash
gcc -static -Os -s -o umu_su umu_su.c -lcrypt || true
if [ ! -f umu_su ]; then
    gcc -static -Os -s -o umu_su umu_su.c -lxcrypt
fi
```

また、設計書の事前準備として `musl-tools` を入れること、`su` の静的リンクのために `libcrypt-dev`（環境によっては `libxcrypt-dev`）を入れる可能性が書かれています。

この記述は、「どの `.h` を見るべきか」を決める強い手がかりになります。

#### この事実から分かる `.h` の扱い（UmuOS-0.1.7-base-stable の場合）

`#include` が意味を持つ場所（= `.h` を追うべき場所）は、ざっくり次の2層に分かれます。

- `musl-gcc` でビルドする `init.c` の世界
    - `#include <...>` は **musl のヘッダ群**（musl-tools が提供する開発用ヘッダ）に向かう
    - ここでの `.h` は「initramfs 内に置くファイル」ではなく、ホストのビルド環境に存在するもの

- `gcc` + `-lcrypt` / `-lxcrypt` でビルドする `umu_su.c` の世界
    - `#include <crypt.h>` などは **libcrypt / libxcrypt の開発用ヘッダ**に向かう
    - `-lcrypt` が通らない環境があるのは、リンクするライブラリ名や提供パッケージが環境差を持つため

そして静的リンク（`-static`）を使っているため、実行時に「ゲスト側に `.h` を置く」必要はありません。
実行時に必要なのは出来上がった実行ファイル（`/init` や `/umu_bin/su`）で、`.h` はビルド時の型チェックや宣言の解決に使われます。

#### 自身がソースを読むときの“最短の追い方”（設計書ベース）

設計書に合わせて、`.h` の追跡も「ビルド単位」から入ると迷いにくいです。

- 対象の `.c` が `musl-gcc` でビルドされるのか、`gcc` でビルドされるのかを先に確定する
    - これだけで、参照される標準ヘッダの出どころ（musl系か、glibc系/追加devパッケージ系か）が大きく絞れます

- `.c` 先頭の `#include <...>` を列挙し、どのパッケージのヘッダかを突き止める
    - 例：`<crypt.h>` は `libcrypt-dev` / `libxcrypt-dev` のどちら側かで差が出やすい

- 「宣言はあるが実体が見えない」ものは、まずリンクオプションを見る
    - `-lcrypt` / `-lxcrypt` のように、どのライブラリに本体があるかが設計書に書かれている場合、ヘッダ探索より先にリンク先の当たりが付けられます

この流れで読むと、`.h` を“プロジェクト内の地図”として追う前に、まず「ホストのツールチェーンが提供する `.h`」と「追加パッケージが提供する `.h`」を切り分けられます。UmuOS-0.1.7-base-stable の設計書は、この切り分けをやりやすい形（ビルドコマンドと依存パッケージの明記）で書かれています。

